---
date: "2026-05-17 14:10"
promoted: false
---

`OmManualDocxService` had drifted from `resources/views/pdf/om-manual.blade.php` on the per-room narrative axis — F-OM-02 in the parity audit (`.planning/audits/worksheet-om-parity-audit-2026-05-17.md`). The PDF blade builds `$narrativesByRoom` at lines 301-318 (preference chain: AI-generated `$data['system_overviews'][*]` keyed by `room_name` → fallback to each room's `$r['narrative']` then `$r['description']`) and renders the result as a "Room Overviews" sub-section inside §1 Executive Summary at lines 639-647. The DOCX renderer had ZERO references to `narrative` — a `grep` across the file returned no matches — so a user downloading the DOCX for a project saw a markedly worse document than the PDF for the same `generated_data` payload (cover + scope tables + contacts + sections 2..N operation rooms, but no per-room overview block). Fix shipped as a single private method `OmManualDocxService::buildRoomOverviewsSection(Section $s, array $systemOverviews, array $roomsSummary)` ported verbatim from the PDF's preference chain. Placement: §1.4 inside the Introduction section (the PDF's §1 Executive Summary cousin — closest structural match in the DOCX's section taxonomy; sections 2..N still address per-room operation procedures, which is a different concern than the cross-room overview block). The section is skipped entirely when neither `system_overviews` nor `rooms_summary[*].narrative|description` carries any data, mirroring the PDF's `@if (! empty($narrativesByRoom))` guard. Section heading uses `addHeading2('1.4  Room Overviews')` — two-space gap mirrors the existing 1.1/1.2/1.3 sibling pattern. Per-room rows render a teal-bold room name (mirrors the PDF's `.room-title` div) followed by the narrative as a regular paragraph via the existing `addParagraph()` helper, so styling stays inside the already-configured PhpWord paragraph styles. Test coverage in new file `tests/Feature/OmManual/OmDocxRoomNarrativeTest.php` with 5 cases / 28 assertions: (1) AI `system_overviews` primary path renders both heading and per-room bodies, (2) `rooms_summary[*].narrative` fallback when AI failed/skipped, (3) legacy `rooms_summary[*].description` fallback for pre-Phase-8 records, (4) `system_overviews` wins precedence over `rooms_summary` mirror (matches PDF order), (5) negative case — no heading rendered when neither source has data. Test technique mirrors `tests/Feature/Rams/DocxBuilderPdfParityTest`: render the DOCX, unzip the .docx, grep `word/document.xml` for expected fragments. Pre-existing failure in `OmManualProjectLinkageTest::test_project_show_page_renders_om_manuals_section_for_empty_state` confirmed unrelated (fails on master/cbe6397 as well — separate issue with `O&amp;M Manuals` HTML on the project show page, NOT caused by this fix). No surface other than `app/Services/OmManualDocxService.php` touched. PDF blade (`resources/views/pdf/om-manual.blade.php`) deliberately untouched per audit guidance — PDF is the canonical reference; DOCX catches up to it. Deploy: push to `live` remote, regenerate any in-flight O&M DOCX (any manual currently in `draft` status would benefit from re-generation to pick up the new section). Bundle context: shipped immediately after F-OM-01's `cbe6397` (canonical-overview-key fallback) — together these two close the high-severity O&M gaps from today's audit, leaving F-OM-03 (test scaffolding), F-OM-04 (resolver helper), F-OM-05 (dead-code audit) and F-OM-06 (off-by-pluralisation) for a future polish session.
