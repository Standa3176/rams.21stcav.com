---
date: "2026-05-14 19:50"
promoted: false
---

Extracted-data room-key mismatch: project-package review form renders "No spaces detected" on packages re-extracted via `QuoteImportService::reimport()` even when the Claude PDF-vision extractor returned a clean Boardroom entry. Root cause: `QuoteExtractorService` (Claude doc-vision) emits room data under `room_summaries` (shape `[{room, summary}, ...]`) but `ProjectPackageReviewController::show()` only consults `room_overviews` and equipment areas to derive the first-load room list. The two import paths diverge — `ExtractQuoteJob::mergeParsedQuoteData()` maps `parsed['room_overviews']` and scaffolds from `ai['rooms']`, but `QuoteImportService::extract()` (used by `import()` + `reimport()`) does no room-key normalisation. Source: Light Forms Ltd 21CQ30451-01-OPS, ProjectPackage id 124, revision 2 (re-extracted), created 2026-05-14 19:29:37 — post-dating today's parser fixes 3e936b0/2c7d1fa so unrelated to QuoteParserService. Surface: `app/Http/Controllers/ProjectPackageReviewController.php::show()` first-load branch (line 193 gate + 209-223 derivation). Fix: defensive `room_summaries → room_overviews` seeding in the controller — preserves all existing `room_summaries` readers (PDF rams.blade.php, WordDocumentService, quote-import/review.blade.php) and fixes both the existing package AND any future re-extracts on read. Unrelated to Phase 23 renderer scope; sidebar fix to Phase 23 mid-execution.
