---
date: "2026-05-14 20:45"
promoted: false
---

Third OCR-garble corruption family in QuoteWerks marker tokens — the PARTDESC family. On the Light Forms Ltd 21CQ30451-01-OPS quote (ProjectPackage 124) the closing `PARTDESCEND` marker was extracted by smalot/pdfparser as "ARTOESCEND" (leading P dropped + inner D→O substitution from glyph subsetting). The tuple-extraction regex on `QuoteParserService::parseTagBased()` line 1695 only tolerates the canonical form (`p.?ARTDESCEND`), so its non-greedy `(.*?)` description capture kept consuming text until the NEXT canonical `PARTDESCEND` — which lived one or more rows downstream. Result on the review form: only ONE hardware row visible where there should be THREE, its description merged with a sibling row's content and containing the leaked "ARTOESCEND 1.00 ~" marker glyph; the third (FW-85BZ40L Sony display) row vanished entirely from `extracted_data['equipment']` because its tuple was swallowed inside the merged capture. Cascades: `ProjectPackageReviewController::show()` lines 74-87 prefer `$raw['equipment']` over `$raw['equipment_list']` and `$raw['line_items']`, so the parser's mangled output overrides the cleaner Claude PDF-vision line_items on the persisted package. Fix follows the established 2026-05-14 same-day pattern (3e936b0 OVERVIEW V→y normaliser, 2c7d1fa QUOTENUM Q→a/T→r normaliser): extend `normaliseQuoteWerksMarkers()` at the `parse()` entry-point with a conservative anchored regex `/\bP?AR[Tt]?[DOdo]ESC(START|END)\b/i` — anchor on the canonical `ESC(START|END)` suffix (no English word ends in OESCEND/DESCSTART), accept optional leading P and inner D↔O. Surface: `app/Services/QuoteParserService.php::normaliseQuoteWerksMarkers()`. Tests: `tests/Unit/Rams/QuoteParserServiceTest.php` — 4 new cases / 17 assertions covering the observed garble (3-row merge guard), P-dropped-only variant, D→O-only variant, and a false-positive guard for legitimate prose ("descends", "ascend"). Total parser-fix test coverage now 11 cases. Separately: the Services-vs-Consumables sectioning question on the DELIVERY row is a deliberate `ExtractQuoteJob::classifyItemType` policy decision (line 455 lists DELIVERY in `$consumablePrefixes`), not a bug in this surface — flagged at human-verify checkpoint for separate user decision. No v1.3 invariant surfaces touched (parser is outside the 5-file guard list). Sidebar fix to Phase 23 mid-execution.
