---
phase: 09-email-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_04_19_000001_add_email_sent_columns_for_phase_09.php
  - config/rams.php
  - .env.example
  - app/Models/RamsDocument.php
  - app/Models/OmManual.php
  - app/Models/Worksheet.php
  - app/Models/CableSchedule.php
autonomous: true
requirements: [NOTF-01c, NOTF-03c, NOTF-04b, NOTF-04c, NOTF-05d]

must_haves:
  truths:
    - "Each of the 4 notifiable models (rams_documents, om_manuals, worksheets, cable_schedules) has a `completion_email_sent_at` nullable timestamp column"
    - "Each of the 4 notifiable models has a `failed_email_sent_at` nullable timestamp column"
    - "`rams_documents` table has a `review_needed_email_sent_at` nullable timestamp column (NOTF-03c — idempotency for review-needed trigger across job retries)"
    - "`cable_schedules` table has an `error_message` column (string, nullable, ≤1000 chars) so failure email body can include the cause (NOTF-04c)"
    - "`config/rams.php` exposes `notifications.bcc` reading `env('RAMS_NOTIFICATION_BCC')`"
    - "`.env.example` documents `RAMS_NOTIFICATION_BCC`, `POSTMARK_API_KEY`, `MAIL_FROM_ADDRESS`, `MAIL_FROM_NAME` with empty/placeholder values"
    - "`php artisan migrate` succeeds and is reversible"
    - "All 4 notifiable models have the new email-timestamp columns added to `$fillable` and cast as `'datetime'` in `$casts` so Eloquent mass-assignment writes do not silently drop the values (idempotency design depends on this)"
    - "`CableSchedule $fillable` additionally includes `error_message` so the failure-alert trigger (09-05) can write it via `update()`"
    - "`RamsDocument` and `CableSchedule` `use HasFactory` (added in this plan so plan 09-02b's factories can resolve via `Model::factory()` static call — moved from 09-02b per checker B-01 to avoid Wave 1 file collision)"
  artifacts:
    - path: "database/migrations/2026_04_19_000001_add_email_sent_columns_for_phase_09.php"
      provides: "Single multi-table migration adding 9 timestamp columns + cable_schedules.error_message"
      contains: "Schema::table"
    - path: "config/rams.php"
      provides: "notifications.bcc config key bound to RAMS_NOTIFICATION_BCC"
      contains: "'notifications'"
    - path: ".env.example"
      provides: "Documented placeholders for Postmark + BCC env vars"
    - path: "app/Models/RamsDocument.php"
      provides: "Mass-assignment + cast wiring for the 3 new email-timestamp columns + HasFactory trait"
      contains: "completion_email_sent_at"
    - path: "app/Models/OmManual.php"
      provides: "Mass-assignment + cast wiring for the 2 new email-timestamp columns"
      contains: "completion_email_sent_at"
    - path: "app/Models/Worksheet.php"
      provides: "Mass-assignment + cast wiring for the 2 new email-timestamp columns"
      contains: "completion_email_sent_at"
    - path: "app/Models/CableSchedule.php"
      provides: "Mass-assignment + cast wiring for the 2 new email-timestamp columns AND the (newly fillable) error_message + HasFactory trait"
      contains: "completion_email_sent_at"
  key_links:
    - from: "downstream Phase 09 trigger code (09-05)"
      to: "completion_email_sent_at / failed_email_sent_at / review_needed_email_sent_at columns"
      via: "Eloquent model `->update([...])` + `=== null` guard checks (requires `$fillable` entry)"
      pattern: "completion_email_sent_at|failed_email_sent_at|review_needed_email_sent_at"
    - from: "shared dispatcher (09-02 / 09-05)"
      to: "global BCC value"
      via: "`config('rams.notifications.bcc')`"
      pattern: "config\\('rams\\.notifications\\.bcc'\\)"
    - from: "ExtractRamsDraftJob review-needed dispatch (09-05 Block C)"
      to: "rams_documents.review_needed_email_sent_at"
      via: "`if ($record->review_needed_email_sent_at === null) { $record->update([...]); }`"
      pattern: "review_needed_email_sent_at"
    - from: "plan 09-02b factory definitions"
      to: "RamsDocument / CableSchedule HasFactory trait"
      via: "`Model::factory()` static call (only resolves when HasFactory is on the model)"
      pattern: "use HasFactory"
---

<objective>
Add the database + config + model-fillable foundations Phase 09 needs before any
mailable or trigger code can run. This plan is intentionally tiny and wholly
independent so it can run in parallel with 09-02 (composer + resolver service).
**09-02b now depends on this plan (Wave 2)** because we own the model edits — including
the HasFactory trait additions on RamsDocument and CableSchedule — to avoid a Wave 1
file collision (per checker B-01).

Purpose: The completion / failure / review-needed idempotency guards (D-13, D-14,
D-15, NOTF-03c) and the configurable BCC (D-09 / NOTF-05d) need DB columns, model
mass-assignment wiring, and a config key to exist before any send code is added; the
cable-schedules `error_message` column (RESEARCH item 6, Pitfall 3) is mandatory
because NOTF-04c requires the failure email body to include the cause. **Crucially**,
without `$fillable` entries, `$record->update(['completion_email_sent_at' => now()])`
silently drops the field — every retry would then re-send (T-09-05 cost runaway). This
plan closes that loop in the same wave as the column creation. We additionally add
the `HasFactory` trait to `RamsDocument` and `CableSchedule` here (one line each) so
plan 09-02b can ship factories without re-touching the same model files in the same
wave.

Output: One migration file, one config file edit, one `.env.example` edit, and four
Model file edits (`$fillable` + `$casts` + HasFactory trait on RamsDocument/CableSchedule).
Reversible schema. Migration runs green on the existing dev DB.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-email-notifications/09-CONTEXT.md
@.planning/phases/09-email-notifications/09-RESEARCH.md
@.planning/phases/09-email-notifications/09-VALIDATION.md

@config/rams.php
@config/services.php
@.env.example
@app/Models/RamsDocument.php
@app/Models/OmManual.php
@app/Models/Worksheet.php
@app/Models/CableSchedule.php

<interfaces>
<!-- Existing model status constants the timestamp columns relate to. Executor must NOT touch these. -->

From app/Models/RamsDocument.php (already exists):
- STATUS_AWAITING_REVIEW, STATUS_COMPLETED, STATUS_FAILED constants
- existing column `email_sent_at` is OWNED by manual-send path (RamsController@email) — DO NOT REUSE
- existing `$fillable` already includes `email_sent_at` and `error_message`
- existing `$casts` block already casts `email_sent_at` as `'datetime'`
- currently `use SoftDeletes;` only — Task 3 changes this to `use HasFactory, SoftDeletes;` (B-01 fix — moved from 09-02b)

From app/Models/OmManual.php / Worksheet.php / CableSchedule.php:
- All have STATUS_GENERATING, STATUS_DRAFT, STATUS_FINAL, STATUS_FAILED
- O&M / Worksheet / Cable jobs flip status to STATUS_DRAFT (NOT STATUS_FINAL) on success — RESEARCH item 4
- `cable_schedules` currently has NO `error_message` column (RESEARCH item 6) — this plan adds it
- `OmManual` has a `casts()` METHOD (Laravel 11+ style) — extend that method. ALREADY uses HasFactory (no trait change needed).
- `Worksheet` has a `casts()` METHOD — extend that method. ALREADY uses HasFactory (no trait change needed).
- `CableSchedule` has NO `$casts` property today — add one (or a `casts()` method to match the new convention). Currently `use SoftDeletes;` only — Task 3 changes this to `use HasFactory, SoftDeletes;` (B-01 fix — moved from 09-02b).
- `CableSchedule $fillable` does NOT include `error_message` today — task 3 adds it.

User::isAdmin() method is CONFIRMED to exist (User.php line 45) — this plan does not touch it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the Phase 09 multi-table migration</name>
  <files>database/migrations/2026_04_19_000001_add_email_sent_columns_for_phase_09.php</files>
  <read_first>
    - 09-RESEARCH.md (Example 4 has the verbatim migration body; Pitfall 3 explains why cable_schedules also gets `error_message`)
    - database/migrations/ — pick the latest existing migration to confirm the date-prefix style and `return new class extends Migration` shape
    - app/Models/CableSchedule.php — confirm `source_filename` and `status` columns exist (so the new columns slot after `status` cleanly)
    - **Migration filename convention** — run `ls database/migrations/2026_04_*.php | tail -3` to see the time-component style currently in use (Laravel convention is `YYYY_MM_DD_HHMMSS_`). Adjust the `_000001_` portion of the new filename to match the surrounding style if it differs (e.g., if other 2026_04_* migrations use `_HHMMSS_` not `_NNNNNN_`). Acceptable forms: `2026_04_19_000001_…` (sequence) or `2026_04_19_HHMMSS_…` (timestamp). Pick the one that matches recent neighbours.
  </read_first>
  <action>
Create `database/migrations/2026_04_19_000001_add_email_sent_columns_for_phase_09.php` (or the timestamp-style equivalent if the existing convention differs — see read_first) that:

1. Adds `completion_email_sent_at` (timestamp, nullable) and `failed_email_sent_at` (timestamp, nullable) to each of these tables: `rams_documents`, `om_manuals`, `worksheets`, `cable_schedules`. On `rams_documents`, place the new columns AFTER the existing `email_sent_at` column (do NOT touch `email_sent_at` — it belongs to the manual-send path per D-13). On the other three tables, place after `filename` (om_manuals, worksheets) or after `status` (cable_schedules).
2. Adds `review_needed_email_sent_at` (timestamp, nullable) to `rams_documents` ONLY (NOTF-03c — review-needed idempotency, RAMS-only since other doc types have no review state per D-04). Place it AFTER `failed_email_sent_at` so the three Phase-09 columns sit contiguously on `rams_documents`.
3. Adds `error_message` (string, length 1000, nullable) to `cable_schedules` — guarded by `Schema::hasColumn('cable_schedules', 'error_message')` so a re-run is safe. This satisfies NOTF-04c per RESEARCH Pitfall 3 (the column genuinely does not exist on cable_schedules today).
4. `down()` drops the four pairs of email-timestamp columns AND the `rams_documents.review_needed_email_sent_at` column. Do NOT drop `cable_schedules.error_message` on rollback — leaving it preserves any operational error data captured between deploys (RESEARCH Example 4 footnote).

Use the verbatim body shape from 09-RESEARCH.md "Example 4: Single multi-table migration" (lines 502–551) as the base; extend the `rams_documents` block to include the third column (`review_needed_email_sent_at`) and update `down()` accordingly.
Filename convention: match recent neighbours in `database/migrations/2026_04_*.php` (per read_first).

Class style: `return new class extends Migration { ... };` (Laravel 12 anonymous-class convention used by every recent migration).
  </action>
  <verify>
    <automated>php artisan migrate --pretend 2>&1 | grep -q "completion_email_sent_at"</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the `2026_04_19_*_add_email_sent_columns_for_phase_09.php` shape (one file matched by glob `database/migrations/2026_04_19_*_add_email_sent_columns_for_phase_09.php`)
    - `grep -q "completion_email_sent_at" database/migrations/2026_04_19_*_add_email_sent_columns_for_phase_09.php` exits 0
    - `grep -q "failed_email_sent_at" database/migrations/2026_04_19_*_add_email_sent_columns_for_phase_09.php` exits 0
    - `grep -q "review_needed_email_sent_at" database/migrations/2026_04_19_*_add_email_sent_columns_for_phase_09.php` exits 0
    - `grep -qE "Schema::hasColumn\('cable_schedules', 'error_message'\)" database/migrations/2026_04_19_*_add_email_sent_columns_for_phase_09.php` exits 0
    - `php artisan migrate` exits 0
    - After migration, `php artisan tinker --execute="echo Schema::hasColumn('cable_schedules', 'completion_email_sent_at') ? 'YES' : 'NO';"` prints `YES`
    - Same hasColumn check returns YES for `om_manuals.completion_email_sent_at`, `worksheets.completion_email_sent_at`, `rams_documents.completion_email_sent_at` and the four `failed_email_sent_at` peers
    - `php artisan tinker --execute="echo Schema::hasColumn('rams_documents', 'review_needed_email_sent_at') ? 'YES' : 'NO';"` prints `YES`
    - `php artisan tinker --execute="echo Schema::hasColumn('cable_schedules', 'error_message') ? 'YES' : 'NO';"` prints `YES`
    - `php artisan migrate:rollback --step=1` reverses the email-timestamp columns AND `review_needed_email_sent_at` without error; cable_schedules.error_message remains intact (`Schema::hasColumn('cable_schedules', 'error_message')` still YES after rollback per Example 4 footnote)
    - Re-run `php artisan migrate` to leave DB in the migrated state for downstream plans
  </acceptance_criteria>
  <done>All 9 timestamp columns + cable_schedules.error_message exist in DB; migration is reversible; rollback leaves cable_schedules.error_message intact (never dropped on rollback).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add notifications.bcc config key + document new env vars</name>
  <files>config/rams.php, .env.example</files>
  <read_first>
    - config/rams.php (current file — only company_* keys exist; you append a new top-level `notifications` array)
    - .env.example (current file — to confirm style for adding the four new keys with placeholder values)
    - 09-RESEARCH.md "Postmark Setup" §4 + §5 for the verbatim env block to add to `.env.example`
    - 09-RESEARCH.md "BCC Implementation Pattern" final code block for the exact shape of the `notifications` array
  </read_first>
  <action>
Two file edits, neither destructive:

1. **`config/rams.php`** — append a new top-level `'notifications'` array after the existing `company_email` line:
   ```php
   'notifications' => [
       'bcc' => env('RAMS_NOTIFICATION_BCC'),   // null/empty = no BCC applied to system mail
   ],
   ```
   Do NOT modify or reorder any existing `company_*` key. Preserve the file's existing `<?php` header and aligned-formatting style.

2. **`.env.example`** — append a new section (or extend the mail section) documenting the four Phase 09 env vars:
   ```dotenv
   # ── Phase 09: Email Notifications ────────────────────────────
   # Production transport (leave MAIL_MAILER=log in dev)
   # MAIL_MAILER=postmark
   POSTMARK_API_KEY=
   MAIL_FROM_ADDRESS=rams@21stcav.com
   MAIL_FROM_NAME="RAMS Platform"
   # Global BCC for audit copy of every system email (leave empty in dev/staging)
   RAMS_NOTIFICATION_BCC=
   ```
   The exact env var name MUST be `POSTMARK_API_KEY` (NOT `POSTMARK_TOKEN` — RESEARCH item 3; `config/services.php:18` already wires `'key' => env('POSTMARK_API_KEY')`).
   Variable name MUST be `RAMS_NOTIFICATION_BCC` (matches the `RAMS_*` namespace used by other env vars).

Do NOT add the keys to the actual `.env` (the runtime file is git-ignored and managed per environment). Only `.env.example` is committed.
  </action>
  <verify>
    <automated>php -r "require 'vendor/autoload.php'; \$app = require 'bootstrap/app.php'; \$app->boot(); echo array_key_exists('notifications', config('rams')) ? 'OK' : 'FAIL';" | grep -q OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE "'notifications'\\s*=>\\s*\\[" config/rams.php` exits 0
    - `grep -q "RAMS_NOTIFICATION_BCC" config/rams.php` exits 0
    - `grep -q "RAMS_NOTIFICATION_BCC" .env.example` exits 0
    - `grep -q "POSTMARK_API_KEY" .env.example` exits 0  (NOT `POSTMARK_TOKEN` — RESEARCH item 3)
    - `grep -q "MAIL_FROM_ADDRESS=rams@21stcav.com" .env.example` exits 0
    - `php artisan tinker --execute="echo array_key_exists('notifications', config('rams')) ? 'OK' : 'MISS';"` prints `OK`
    - `php artisan tinker --execute="echo config('rams.notifications.bcc') === null ? 'NULL_OK' : 'SET';"` prints `NULL_OK` (because `.env` does not set it in dev)
    - All existing `config/rams.php` `company_*` keys still resolve: `php artisan tinker --execute="echo config('rams.company_short');"` prints `21CAV`
  </acceptance_criteria>
  <done>Config key resolvable via `config('rams.notifications.bcc')`; `.env.example` documents the 4 new Phase 09 env vars with correct names; existing config keys untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update model `$fillable` + `$casts` + add HasFactory trait to RamsDocument & CableSchedule (B-01 — moved from 09-02b to avoid Wave 1 file collision)</name>
  <files>app/Models/RamsDocument.php, app/Models/OmManual.php, app/Models/Worksheet.php, app/Models/CableSchedule.php</files>
  <read_first>
    - app/Models/RamsDocument.php — note the existing `$fillable` array shape and the `$casts` PROPERTY (not method); `email_sent_at` is already cast as datetime — mirror that style. Currently `use SoftDeletes;` (line 13) — must change to `use HasFactory, SoftDeletes;` and add the import.
    - app/Models/OmManual.php — note `$casts` is a METHOD (`protected function casts(): array`) — Laravel 11+ style; extend the array returned by the method. Already has HasFactory trait (no trait change needed).
    - app/Models/Worksheet.php — note `$casts` is also a METHOD; extend the array. Already has HasFactory trait (no trait change needed).
    - app/Models/CableSchedule.php — note there is NO `$casts` property OR `casts()` method; add either the property OR the method (match the surrounding modern style — prefer `protected function casts(): array` to match OmManual/Worksheet). Currently `use SoftDeletes;` (line 12) — must change to `use HasFactory, SoftDeletes;` and add the import.
    - CHECKER ISSUE B-01 in revision context — explains exactly WHY the HasFactory trait additions live here (this plan owns model files in Wave 1; 09-02b would otherwise collide on the same files).
  </read_first>
  <action>
Mass-assignment must be wired before plan 09-05's `$record->update(['completion_email_sent_at' => now()])` calls can succeed. Without `$fillable` entries, Eloquent silently drops the field and the idempotency guard `=== null` always passes → every retry re-sends (T-09-05 cost runaway). This task fixes that across all 4 models. **It also adds the HasFactory trait to RamsDocument and CableSchedule** so plan 09-02b's factories can resolve via `Model::factory()` static call (B-01 fix — moved here to avoid a Wave 1 file collision with 09-02b).

**Edit 1 — `app/Models/RamsDocument.php`:**
- **HasFactory trait (B-01):**
  - Add `use Illuminate\Database\Eloquent\Factories\HasFactory;` import near the top, alphabetically near the other `Illuminate\Database\Eloquent\*` imports (above the `Model` import, or wherever PSR-12 ordering puts it)
  - Change `use SoftDeletes;` (line 13) to `use HasFactory, SoftDeletes;`
- In `$fillable` (lines 32–52), add `'completion_email_sent_at'`, `'failed_email_sent_at'`, `'review_needed_email_sent_at'` after the existing `'email_sent_at'` entry (keep aligned formatting)
- In the `$casts` property (lines 54–62), add three entries:
  - `'completion_email_sent_at' => 'datetime',`
  - `'failed_email_sent_at'     => 'datetime',`
  - `'review_needed_email_sent_at' => 'datetime',`
  Insert right after the existing `'email_sent_at' => 'datetime',` entry to keep related casts together.

**Edit 2 — `app/Models/OmManual.php`:**
- HasFactory trait: ALREADY PRESENT (line 12: `use HasFactory, SoftDeletes;`) — do NOT touch
- In `$fillable` (lines 23–38), append `'completion_email_sent_at'` and `'failed_email_sent_at'` (after `'filename'` to mirror the migration column order)
- In the `casts()` method (lines 40–46), extend the returned array to include both columns as `'datetime'`. Final return shape:
  ```php
  return [
      'extracted_data' => 'array',
      'generated_data' => 'array',
      'completion_email_sent_at' => 'datetime',
      'failed_email_sent_at'     => 'datetime',
  ];
  ```

**Edit 3 — `app/Models/Worksheet.php`:**
- HasFactory trait: ALREADY PRESENT (line 26: `use HasFactory, SoftDeletes;`) — do NOT touch
- In `$fillable` (lines 38–49), append `'completion_email_sent_at'` and `'failed_email_sent_at'` after `'filename'`
- In the `casts()` method (lines 53–58), extend the returned array:
  ```php
  return [
      'generated_data' => 'array',
      'completion_email_sent_at' => 'datetime',
      'failed_email_sent_at'     => 'datetime',
  ];
  ```

**Edit 4 — `app/Models/CableSchedule.php`:**
- **HasFactory trait (B-01):**
  - Add `use Illuminate\Database\Eloquent\Factories\HasFactory;` import near the top, alphabetically with the other `Illuminate\Database\Eloquent\*` imports
  - Change `use SoftDeletes;` (line 12) to `use HasFactory, SoftDeletes;`
- In `$fillable` (lines 21–29), append THREE fields: `'completion_email_sent_at'`, `'failed_email_sent_at'`, AND `'error_message'` (the latter is required because plan 09-05's failure-alert path writes `$record->update(['error_message' => ...])` indirectly via the existing failed() hook flow; without `$fillable` it silently drops)
- Add a `casts()` method (CableSchedule currently has none) matching the OmManual/Worksheet modern style:
  ```php
  protected function casts(): array
  {
      return [
          'completion_email_sent_at' => 'datetime',
          'failed_email_sent_at'     => 'datetime',
      ];
  }
  ```
  Insert AFTER `$fillable` and BEFORE the `// ── Relationships ──` divider.

**General constraints:**
- Do NOT remove or reorder any existing `$fillable` or `$casts` entry.
- Preserve the column-aligned formatting style used in each file (RamsDocument uses aligned `=>` columns).
- Do NOT touch the migration file from Task 1 — this task is purely model PHP edits.
- Run after Task 1 has migrated the DB so the smoke checks below have real columns to introspect.
- Plan 09-02b (factories) now runs in Wave 2 and depends on this plan — it will read RamsDocument and CableSchedule expecting HasFactory to be present. Do NOT skip the trait additions here.
  </action>
  <verify>
    <automated>php artisan tinker --execute="echo (new App\\Models\\RamsDocument)->isFillable('completion_email_sent_at') && (new App\\Models\\RamsDocument)->isFillable('failed_email_sent_at') && (new App\\Models\\RamsDocument)->isFillable('review_needed_email_sent_at') && (new App\\Models\\OmManual)->isFillable('completion_email_sent_at') && (new App\\Models\\OmManual)->isFillable('failed_email_sent_at') && (new App\\Models\\Worksheet)->isFillable('completion_email_sent_at') && (new App\\Models\\Worksheet)->isFillable('failed_email_sent_at') && (new App\\Models\\CableSchedule)->isFillable('completion_email_sent_at') && (new App\\Models\\CableSchedule)->isFillable('failed_email_sent_at') && (new App\\Models\\CableSchedule)->isFillable('error_message') ? 'ALL_OK' : 'MISSING';" | grep -q ALL_OK</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan tinker --execute="echo (new App\\Models\\RamsDocument)->isFillable('completion_email_sent_at') ? 'YES' : 'NO';"` prints `YES`
    - `php artisan tinker --execute="echo (new App\\Models\\RamsDocument)->isFillable('failed_email_sent_at') ? 'YES' : 'NO';"` prints `YES`
    - `php artisan tinker --execute="echo (new App\\Models\\RamsDocument)->isFillable('review_needed_email_sent_at') ? 'YES' : 'NO';"` prints `YES`
    - `php artisan tinker --execute="echo (new App\\Models\\OmManual)->isFillable('completion_email_sent_at') ? 'YES' : 'NO';"` prints `YES`
    - `php artisan tinker --execute="echo (new App\\Models\\OmManual)->isFillable('failed_email_sent_at') ? 'YES' : 'NO';"` prints `YES`
    - `php artisan tinker --execute="echo (new App\\Models\\Worksheet)->isFillable('completion_email_sent_at') ? 'YES' : 'NO';"` prints `YES`
    - `php artisan tinker --execute="echo (new App\\Models\\Worksheet)->isFillable('failed_email_sent_at') ? 'YES' : 'NO';"` prints `YES`
    - `php artisan tinker --execute="echo (new App\\Models\\CableSchedule)->isFillable('completion_email_sent_at') ? 'YES' : 'NO';"` prints `YES`
    - `php artisan tinker --execute="echo (new App\\Models\\CableSchedule)->isFillable('failed_email_sent_at') ? 'YES' : 'NO';"` prints `YES`
    - `php artisan tinker --execute="echo (new App\\Models\\CableSchedule)->isFillable('error_message') ? 'YES' : 'NO';"` prints `YES`
    - `php artisan tinker --execute="echo (new App\\Models\\RamsDocument)->getCasts()['completion_email_sent_at'] ?? 'MISSING';"` prints `datetime`
    - `php artisan tinker --execute="echo (new App\\Models\\RamsDocument)->getCasts()['failed_email_sent_at'] ?? 'MISSING';"` prints `datetime`
    - `php artisan tinker --execute="echo (new App\\Models\\RamsDocument)->getCasts()['review_needed_email_sent_at'] ?? 'MISSING';"` prints `datetime`
    - `php artisan tinker --execute="echo (new App\\Models\\OmManual)->getCasts()['completion_email_sent_at'] ?? 'MISSING';"` prints `datetime`
    - `php artisan tinker --execute="echo (new App\\Models\\OmManual)->getCasts()['failed_email_sent_at'] ?? 'MISSING';"` prints `datetime`
    - `php artisan tinker --execute="echo (new App\\Models\\Worksheet)->getCasts()['completion_email_sent_at'] ?? 'MISSING';"` prints `datetime`
    - `php artisan tinker --execute="echo (new App\\Models\\Worksheet)->getCasts()['failed_email_sent_at'] ?? 'MISSING';"` prints `datetime`
    - `php artisan tinker --execute="echo (new App\\Models\\CableSchedule)->getCasts()['completion_email_sent_at'] ?? 'MISSING';"` prints `datetime`
    - `php artisan tinker --execute="echo (new App\\Models\\CableSchedule)->getCasts()['failed_email_sent_at'] ?? 'MISSING';"` prints `datetime`
    - **HasFactory trait checks (B-01):**
      - `grep -q "use HasFactory" app/Models/RamsDocument.php` exits 0
      - `grep -q "use HasFactory" app/Models/CableSchedule.php` exits 0
      - `grep -q "use Illuminate\\\\Database\\\\Eloquent\\\\Factories\\\\HasFactory" app/Models/RamsDocument.php` exits 0
      - `grep -q "use Illuminate\\\\Database\\\\Eloquent\\\\Factories\\\\HasFactory" app/Models/CableSchedule.php` exits 0
    - **Direct-create round-trip smoke (W-02 — replaces the factory-based smoke; factory does not exist yet at this plan's runtime — 09-02b is now Wave 2):**
      ```
      php artisan tinker --execute="
      \$r = App\\Models\\RamsDocument::create([
          'user_id' => 1, 'project_ref' => 'TEST-09-01', 'project_name' => 'Test', 'client_name' => 'Test', 'site_address' => 'Test', 'filename' => 'test.docx', 'status' => 'awaiting_review', 'completion_email_sent_at' => now()
      ]);
      echo \$r->completion_email_sent_at instanceof \\Carbon\\CarbonInterface ? 'PERSISTED_AND_CAST' : 'BROKEN';
      \$r->forceDelete();
      "
      ```
      MUST print `PERSISTED_AND_CAST`. If the dev DB does not have `users.id = 1`, substitute with `App\\Models\\User::factory()->create()->id` (UserFactory exists today). If the round-trip is too brittle for any reason (NOT-NULL columns vary across environments), the executor MAY drop this single check and rely on the `isFillable()` + `getCasts()` checks above (which already prove the columns are mass-assignable + cast). Document the choice in the SUMMARY.
    - `vendor/bin/phpunit --testsuite=Unit` exits 0 (no regression in existing unit suite from the model edits)
  </acceptance_criteria>
  <done>All 4 models have the new email-timestamp columns in `$fillable` and cast as `'datetime'`. CableSchedule additionally has `error_message` in `$fillable`. RamsDocument and CableSchedule now `use HasFactory, SoftDeletes;` so plan 09-02b's factories can resolve via `Model::factory()`. Eloquent mass-assignment writes from the trigger sites in plan 09-05 will now persist correctly, making the idempotency design viable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → committed config files | `config/rams.php` and `.env.example` live in version control; secrets must never live in either file |
| Eloquent mass-assignment guard → DB writes | Any field not in `$fillable` is silently discarded by `update()`; missing entries break the idempotency design |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | I (Information disclosure) | `.env.example` and `config/rams.php` | mitigate | `.env.example` only contains the env var **name** with empty value (`POSTMARK_API_KEY=`); the real key lives in `.env` (git-ignored). `config/rams.php` reads `env('RAMS_NOTIFICATION_BCC')` so no actual address is hard-coded. |
| T-09-05 | D (Denial of service / cost runaway) | `completion_email_sent_at` / `failed_email_sent_at` / `review_needed_email_sent_at` columns + model `$fillable` | mitigate | Columns provide the idempotency surface that downstream send code (09-05) uses; **and** Task 3 ensures the columns are mass-assignable so writes actually persist (without `$fillable`, the guard `=== null` would always pass and every retry would re-send). Without both halves, the entire design fails. |
| T-09-01 | T (Tampering) | `cable_schedules.error_message` | mitigate | New column is internal — only set by `BuildCableScheduleJob`; never accepts user input; max length 1000 chars prevents stored-payload overflow. Task 3 also adds it to CableSchedule `$fillable` so the failure path can write it. |
| T-09-05 | D (Denial of service / cost runaway) | review-needed retries on `ExtractRamsDraftJob` | mitigate | NOTF-03c's `review_needed_email_sent_at` column closes the previously identified gap where `$tries = 2` could fire the review email twice on retry. Idempotency guard pattern in 09-05 Block C matches the completion-mail pattern. |
</threat_model>

<verification>
- `php artisan migrate` runs green on dev DB; rollback reversible (email-timestamp columns + review_needed dropped; cable error_message preserved).
- `config('rams.notifications.bcc')` returns null in dev (env not set), returns the trimmed string when set.
- All 9 new email-timestamp columns + 1 cable_schedules.error_message column exist (verified via `Schema::hasColumn`).
- All 4 notifiable models have the corresponding `$fillable` entries and `'datetime'` casts (verified via `isFillable()` and `getCasts()` smoke checks).
- RamsDocument and CableSchedule now `use HasFactory, SoftDeletes;` (B-01 fix — verified via grep).
- Round-trip smoke: `RamsDocument::create([...])` then read-back → cast as Carbon instance.
- No existing column or config key was modified.
</verification>

<success_criteria>
- Migration committed and runs to completion on a clean DB.
- Migration is reversible without losing the `cable_schedules.error_message` column on rollback.
- `config/rams.php` adds `notifications.bcc`; no other key touched.
- `.env.example` documents `POSTMARK_API_KEY` (NOT `POSTMARK_TOKEN`), `MAIL_FROM_ADDRESS`, `MAIL_FROM_NAME`, `RAMS_NOTIFICATION_BCC`.
- All 4 notifiable models accept and persist writes to the new email-timestamp columns AND cast them as Carbon instances.
- RamsDocument and CableSchedule have HasFactory trait, unblocking 09-02b's Wave 2 factory plan.
- Phase 09 wave 2/3 plans can rely on these columns, casts, fillable entries, and traits existing.
</success_criteria>

<output>
After completion, create `.planning/phases/09-email-notifications/09-01-SUMMARY.md` with:
- Files modified (with line counts)
- Migration filename + columns added (including `review_needed_email_sent_at` for NOTF-03c)
- Confirmation that `cable_schedules.error_message` was added to the table AND to the model `$fillable`
- Confirmation that `HasFactory` trait was added to RamsDocument and CableSchedule (B-01 fix)
- Confirmation that all 4 models accept and cast the new columns (with the round-trip smoke output, OR a note that the smoke was skipped in favour of `isFillable()`/`getCasts()` checks per W-02 fallback)
- Any gotchas hit (e.g., conflicting existing column on a table, casts() method vs property style mismatch, NOT-NULL columns requiring extra round-trip args)
</output>
</content>