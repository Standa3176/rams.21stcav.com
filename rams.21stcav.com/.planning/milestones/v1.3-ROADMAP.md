---
milestone: v1.3
milestone_name: Technical Drawings & Schematics
status: shipped
shipped_at: 2026-05-09
phases: [17, 18, 20]
phases_deferred: [19]
plans_total: 7
plans_complete: 7
---

# v1.3 Technical Drawings & Schematics — SHIPPED 2026-05-09

## Milestone Goal

Generate AV technical drawings — schematics + rack elevations — from the same canonical project data that powers RAMS, O&M, and worksheets. Internal engineers view drawings on tablets and print during install; clients receive them as part of the O&M Manual handover. Drawings derive from canonical project data only — AI may assist with layout but never invents equipment, cables, or rooms.

## Stats

| Metric | Value |
|---|---|
| Phases complete | 3 (17, 18, 20) |
| Phases deferred | 1 (19 — Floor Plans → v2.0 backlog 999.1) |
| Plans shipped | 7/7 |
| Commits in window | ~199 (2026-04-26 → 2026-05-09) |
| Files touched | ~3,210 unique |
| Window opened | 2026-04-26 (after v1.2 ship) |
| Window closed | 2026-05-09 (this archive) |

## Scope reduction (2026-05-02)

Phase 19 (Floor Plans / Konva) was explicitly removed from v1.3 mid-milestone and pushed to the v2.0 engineering-grade backlog (`999.1`). Reasoning: Konva canvas editor + Browsershot+Konva PDF round-trip would be throwaway work once v2.0's port-aware native renderer (or vendor integration) lands. v2.0 needs to build floor plans properly with port catalog + zones anyway. DXF export (DRAW-29) moved with floor plans. v1.3 shipped ~3-4 weeks sooner.

Affected requirements moved to v2.0: DRAW-14, DRAW-15, DRAW-16, DRAW-17, DRAW-18, DRAW-19, DRAW-20, DRAW-29.

## Phase 17 — System Schematics + Shared Foundations (3 plans)

Foundations for ALL drawing kinds + first deliverable (auto-generated schematics). The shared layer (table + model + policy + storage type + job pattern + edit-adapter scaffolding + `PdfRenderService::waitForJs`) is what made Phases 18 + 20 land as pure additions.

- **17-01 Foundations** (LANDED 2026-05-01) — `project_drawings` table, `ProjectDrawing` model + policy, `DocumentArtifactStorage::TYPE_DRAWING`, `DrawingService` (createForProject/generateInitial/regenerate/archivePrior), `BuildSchematicJob` skeleton, `DrawingReadyMail`, `DrawingEditAdapter` scaffolding, `PdfRenderService::waitForJs` + `fromBladeAsPng` extension.
- **17-02 Schematic Generator** (LANDED 2026-05-01) — D2 CLI driven generator: `SchematicGeneratorService` + `SchematicD2SourceBuilder` with full sanitiseLabel() escape, 25-symbol AVIXA-aligned SVG pack (~18 KB), `config/drawings.php` (D2 binary path + signal-type colour map), `DrawingDataResolverService::adjacencyForProject` body filled, `BuildSchematicJob` wired to real generator.
- **17-03 Render UI + Handover Wiring** (LANDED 2026-05-01) — `DrawingExportRendererService` delegates PNG to `PdfRenderService::fromBladeAsPng`; `OmManualDocxService` Drawings section opens fresh section (no reuse); `BuildSchematicJob` thumbnail block; `pdf:smoke-test --drawings` flag; 6 drawings routes registered.

**Requirements covered:** DRAW-01, DRAW-02, DRAW-03, DRAW-04, DRAW-05 (UX scaffolding), DRAW-06, DRAW-22, DRAW-24, DRAW-25, DRAW-26, DRAW-27, DRAW-30 (scaffolding).

## Phase 18 — Rack Elevations (2 plans, 1 deferred)

1U-precise rack drawings from equipment list with U-height + ventilation metadata. Custom Blade SVG renderer (not D2 — racks are list-shaped not graph-shaped). Drag-into-U-slots Konva-style editor.

- **18-01 Picker + Schema** (LANDED 2026-05-02) — `devices.u_height` + ventilation/`is_rack_mounted` columns (CRIT-06 nullable-first); 53-entry hand-curated manufacturer JSON pack at `resources/data/device-port-catalog.json`; `DeviceCatalogService` + idempotent `DeviceCatalogSeeder`; unified "+ Create Drawing" Alpine modal replacing per-kind buttons (Floor Plan card disabled with "Coming in v2.0" tooltip).
- **18-03 Rack Editor** (LANDED 2026-05-02) — `RackElevationRenderService` synchronous custom Blade SVG renderer (~340 LOC, measured 0.06s for full 42U/30-items vs 1s budget); rack editor Blade with Alpine + Sortable.js drag-into-U-slots palette + 42U scaffold + per-item lock toggle + cursor-walk lock-aware reorder algorithm; AJAX saveRackCanvas endpoint; `flipRackMounted` endpoint authorised against `ProjectPolicy::update`; `pdf/drawings/rack.blade.php` landscape A4 view.

**Requirements covered:** DRAW-07, DRAW-08, DRAW-09 (palette ordering — partial; bottom-up rendering + AVIXA auto-place algorithm deferred to v1.3.x or v2.0), DRAW-10, DRAW-11, DRAW-12, DRAW-13.

## Phase 20 — Drawing Export Pipeline + O&M Integration (2 plans)

Bound multi-page project PDF + ZIP bundle + O&M Manual auto-embed of rendered drawings. Production hardening across drawings queue, license audit, font fallbacks, and smoke testing.

- **20-01 Bound PDF + Sheet Numbering + ZIP** (LANDED 2026-05-03) — DRAW-21 / 23 / 28 shipped: `SheetNumberAllocator` (AV-201..299 schematics, AV-301..399 racks); `BoundPdfBuilderService` (FPDI page-by-page concat of cover + register + every per-drawing PDF; per-drawing failures isolated); `BuildBoundPdfJob` ($projectId-keyed; `WithoutOverlapping` middleware); `BoundPdfReadyMail`; `pdf/drawings/bound-cover.blade.php` (A4 portrait cover + drawing register table); `pdf/drawings/_title-block.blade.php` Sheet row; 3 routes (`bound-pdf` download / build / `bundle.zip`); index controller `boundPdfStaleBadge` (amber pill); new deps `setasign/fpdi:^2.6` + `setasign/fpdf:^1.8.6` (MIT).
- **20-02 Production Hardening + O&M Rack Embed** (LANDED 2026-05-03) — `drawings:audit-licenses` Artisan command (composer + npm GPL/AGPL detector with `--strict` mode); dedicated `drawings` queue connection in `config/queue.php` (driver=database, queue=drawings, retry_after=600); `CHROME_HEADLESS_SHELL_VERSION=147.0.7727.57` pin in `.env.example`; `@font-face` Liberation Sans + DejaVu Sans declarations in 3 drawing Blades; `public/fonts/.gitkeep`; 218-line `docs/runbooks/drawings-queue-runbook.md`; `pdf:smoke-test --drawings` extended to render BOTH schematic + rack via paired helpers; `OmManualDocxService` drawings loop kind-agnostic.

**Requirements covered:** DRAW-21, DRAW-23, DRAW-28.

## Companion work — draw.io embed spike (260509-ibx)

Built tonight as the v2.0 build-vs-buy evaluation gate. Validates draw.io / mxGraph as the engineering-grade rendering platform for v2.0. Deliverables:
- Vendored draw.io v29.7.12 bundle (Apache 2.0)
- Hand-coded 5-stencil pack (Neat Bar Pro / Samsung 65" / ClickShare Bar Pro / Sennheiser TC mic / Netgear M4250) at `resources/data/draw-io-stencils/21cav-mtr-spike.json`
- `DrawIoSpikeBuilderService` — pure-aggregator mxGraph XML emitter
- Admin sandbox route `admin/drawings/draw-io-spike/{project}` with iframe + postMessage handshake
- `DrawingService::saveSpikeXml()` + `saveSpikeSvg()` reusing existing `canvas_state` column + lock-on-edit pattern from Phase 18 P03
- 7 atomic commits (`d43847a` through `ab34a70`)

The spike's 5 hand-coded stencils + builder service become **seed data for v2.0 Phase 21** (Device Port Catalog) — nothing wasted.

## Carry-over verification debt

- Phase 17 has UAT items marked `human_needed` — minor; deferred to v1.3.x patch sweep
- DRAW-09 palette ordering: bottom-up rendering + AVIXA auto-place algorithm not yet implemented; engineer manually drags from palette today (acceptable per Phase 18 spec)

## Why this milestone matters

v1.3 closes the loop on "every project's documentation outputs are auto-generated from one canonical dataset". Before v1.3, drawings lived outside the system — engineers produced them in Visio / XTEN-AV / Lucidchart and emailed PDFs. After v1.3, every project has schematics + racks + bound PDF available in one click, embedded in the O&M Manual handover automatically. The output quality is **passable basic** — engineering-grade fidelity (XTEN-AV / D-Tools-level port-aware drawings) is the v2.0 milestone, evaluated and platform-validated by the draw.io spike that landed alongside this milestone close.

## Source artifacts

| Phase | Plans (PLAN.md) | Summaries (SUMMARY.md) |
|---|---|---|
| 17 | `.planning/phases/17-system-schematics-shared-foundations/17-01-foundations-PLAN.md`, `17-02-schematic-generator-PLAN.md`, `17-03-render-ui-handover-PLAN.md` | `17-01-foundations-SUMMARY.md`, `17-02-schematic-generator-SUMMARY.md`, `17-03-render-ui-handover-SUMMARY.md` |
| 18 | `.planning/phases/18-rack-elevations/18-01-picker-and-schema-PLAN.md`, `18-03-rack-editor-PLAN.md` | `18-01-picker-and-schema-SUMMARY.md`, `18-03-rack-editor-SUMMARY.md` |
| 20 | `.planning/phases/20-drawing-export-pipeline-o-m-integration/20-01-bound-pdf-sheet-numbering-zip-PLAN.md`, `20-02-production-hardening-om-rack-embed-PLAN.md` | `20-01-bound-pdf-sheet-numbering-zip-SUMMARY.md`, `20-02-production-hardening-om-rack-embed-SUMMARY.md` |

Phase 19 deferral note: `.planning/phases/999.1-v2-engineering-grade-av-drawings/`.

draw.io spike: `.planning/quick/260509-ibx-draw-io-embed-spike-sandbox-one-stencil-/`.
