# Milestone Archive: v1.0 RAMS MVP

**Status:** SHIPPED
**Shipped:** 2026-04-12
**Phases:** 01–07 (7 phases, 29 plans, 42 tasks)
**Timeline:** 2026-04-09 → 2026-04-12

---

## Phases

### Phase 01 — Project Layer & Data Foundation (6/6 plans)

Plans:
- 01-01: `projects` table schema — quote_reference column, Project model, state machine constants
- 01-02: Project index/filter UI — client name filter, state badge, search
- 01-03: `ProjectDataService` — 4-tier canonical data merge contract (TDD)
- 01-04: Auto-create on import + state machine auto-advance hooks
- 01-05: ProjectController — 7 targeted changes (CRUD, state transitions, dashboard)
- 01-06: QuoteImportService integration — importFromData() convenience method

**Delivered:** Projects as top-level entity, canonical data merge point, lifecycle state machine, auto-advance triggers, project dashboard.

---

### Phase 02 — QuoteWerks SQL Import (3/3 plans)

Plans:
- 02-01: Named DB connection + env template + quotewerks:ping health check command
- 02-02: QuoteWerksRepository — raw SQL queries for header, line items, room groups
- 02-03: QuoteWerksImportService + controller + routes + form request

**Delivered:** Full QuoteWerks SQL import pipeline — read-only MS SQL connection, repository pattern, import service that produces identical extracted_data structure as PDF import.

---

### Phase 03 — Survey Data Integration (4/4 plans)

Plans:
- 03-01: Migration — global_notes, hs_constraints, submitted_at, completed_at columns on site_surveys
- 03-02: ProjectDataService survey merge — survey_data key populated from SiteSurvey
- 03-03: SurveyService enforcement — project_id required, token generation, completion timestamps
- 03-04: UI wiring — global notes, H&S constraints, submission timestamps in survey forms

**Delivered:** Survey data fully wired into ProjectDataService, global site data captured, submission/completion timestamps tracked, survey data available to all generators.

---

### Phase 04 — Document Generators (4/4 plans)

Plans:
- 04-01: Worksheet generator — WorksheetGeneratorService, BuildWorksheetJob, DOCX output
- 04-02: O&M Manual generator — OmManualGeneratorService (refactored from existing), BuildOmManualJob
- 04-03: Cable Schedule generator — CableScheduleGeneratorService, BuildCableScheduleJob, XLSX output
- 04-04: ProjectController::show() dashboard — all generator statuses, generation buttons

**Delivered:** Three queue-based document generators (Worksheet DOCX, O&M DOCX, Cable Schedule XLSX), all consuming from ProjectDataService, project dashboard showing all generator statuses.

---

### Phase 05 — Project Content Pack (4/4 plans)

Plans:
- 05-01: RoomOverviewSummaryPrompt — AI generates room-by-room scope narrative from equipment + survey data
- 05-02: RamsReviewDataService — generates content pack; maps AI room summaries into RAMS review payload
- 05-03: ProjectPackageReviewController — content pack flow wiring, review form pre-population
- 05-04: MethodStatementService — uses content pack to improve method statement quality

**Delivered:** Single AI call generates structured scope narratives for all rooms; RAMS review pre-populated with AI-enriched content; method statements improved via content pack context.

---

### Phase 06 — RAMS Document Quality (2/2 plans)

Plans:
- 06-01: RamsBuilderService skip-guard — skip-if-no-change protection, content pack validation
- 06-02: RAMS PDF scope conditional — suppress placeholder text, show only real scope data

**Delivered:** RAMS documents now suppress generic placeholder content; builder protected against partial/missing content pack data.

---

### Phase 07 — Dynamic Site Survey AI-Generated Room Questions (6/6 plans)

Plans:
- 07-01: TDD stubs — 5 RED test files for model, prompt, job, endpoints, completion gate
- 07-02: Data layer — `site_survey_room_questions` migration, SiteSurveyRoomQuestion model, questions() relationship
- 07-03: GenerateSurveyQuestionsJob — async AI question generation per room on survey creation
- 07-04: Answer persistence endpoints — public (token-gated) and auth-gated routes + controllers
- 07-05: Pre-Install Checks UI — amber-palette panel in public survey + admin survey edit form
- 07-06: Completion gate — completeRoom() 422 guard when unanswered pre-install questions remain

**Delivered:** AI generates solution-type-appropriate pre-install check questions per survey room on creation; engineers/clients answer yes/no/other; completion blocked until all questions answered.

---

## Milestone Summary

### Key Decisions

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| `projects` table separate from `project_packages` | Projects are the top-level entity; packages are one input source | ✓ Good — clean separation |
| ProjectDataService 4-tier merge (reviewed > survey > sql > extracted > defaults) | Single canonical data point prevents each generator resolving data independently | ✓ Good — all generators use it |
| Direct SQL connection to QuoteWerks (not API) | VPN available, simpler than building intermediary service | ✓ Good — working pipeline |
| Token-only access for external survey users | Simplicity; no account management overhead for one-off surveys | ✓ Good — UUID tokens in use |
| Cable schedules as XLSX, everything else DOCX | Engineers need cable data in spreadsheet format for field use | ✓ Good |
| AI questions dispatched async via Job on survey create | Non-blocking; survey creation succeeds even if AI call fails | ✓ Good — silent failure pattern |
| SurveyQuestionsPrompt temperature 0.2 | Consistent, practical questions — not creative responses | ✓ Good |

### Tech Debt

- `DATA-04` confidence scoring (per-field source annotation) was designed but only partially implemented — soft columns present in service, no UI surface
- REQUIREMENTS.md checkboxes were not maintained during execution — archive reflects shipped state, not checkbox state
- Test stubs from Phase 07 (07-01) are RED stubs — were not greened during execution (Phase 07 used a simplified TDD approach)

---

*Archived: 2026-04-12*
