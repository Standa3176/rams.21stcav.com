---
phase: 15-time-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_04_21_000001_extend_time_entries_for_phase_15.php
  - database/migrations/2026_04_21_000002_create_time_entry_audits_table.php
  - app/Models/TimeEntry.php
  - app/Models/TimeEntryAudit.php
  - app/Models/User.php
  - database/factories/TimeEntryFactory.php
autonomous: true
requirements:
  - INST-04
  - INST-04a
  - INST-04f
  - INST-04i

must_haves:
  truths:
    - "time_entries table has category, notes, closure_reason columns in addition to Phase 14 columns"
    - "time_entry_audits table exists with append-only shape for retro-edit history"
    - "TimeEntry model exposes category, notes, closure_reason as fillable + casts"
    - "TimeEntryAudit model belongs to TimeEntry and to User (edited_by_user_id)"
    - "All four category values (installation / commissioning / testing / other) are valid enum members"
    - "User model has timeEntries() hasMany + timeEntryAudits() hasMany relationships"
  artifacts:
    - path: "database/migrations/2026_04_21_000001_extend_time_entries_for_phase_15.php"
      provides: "Adds category enum + notes text + closure_reason string columns to time_entries"
      contains: "Schema::table('time_entries'"
    - path: "database/migrations/2026_04_21_000002_create_time_entry_audits_table.php"
      provides: "time_entry_audits append-only history table"
      contains: "Schema::create('time_entry_audits'"
    - path: "app/Models/TimeEntry.php"
      provides: "Category + notes + closure_reason fillables, audits() relation, CATEGORY_* + CLOSURE_REASON_* constants"
      contains: "CATEGORY_INSTALLATION"
    - path: "app/Models/TimeEntryAudit.php"
      provides: "Audit row model (timeEntry + editor belongsTo)"
      contains: "class TimeEntryAudit"
  key_links:
    - from: "app/Models/TimeEntry.php"
      to: "time_entry_audits table"
      via: "audits() hasMany relation"
      pattern: "hasMany.*TimeEntryAudit"
    - from: "app/Models/TimeEntryAudit.php"
      to: "users table"
      via: "editor() belongsTo via edited_by_user_id"
      pattern: "belongsTo.*User.*edited_by_user_id"
---

<objective>
Extend the Phase 14 `time_entries` schema with the four columns Phase 15 needs (`category`, `notes`, `closure_reason`) and add an append-only `time_entry_audits` table for retro-edit history (D-04, D-07). Wire the corresponding model attributes and relationships so downstream plans can write to them.

Purpose: Establishes the data layer all Phase 15 behaviour builds on. Heartbeat (15-02), stale-close (15-03), retro-edit (15-02/15-04), dashboard widget (15-05) all depend on these columns existing.

Output: Two new migrations, two new/updated Eloquent models, and `php artisan migrate` runs green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-time-tracking/15-CONTEXT.md
@.planning/phases/14-mobile-field-view/14-CONTEXT.md
@./CLAUDE.md

<interfaces>
<!-- Existing Phase 14 TimeEntry schema + model — extending, not rewriting. -->

From database/migrations/2026_04_20_000002_create_time_entries_table.php:
```php
Schema::create('time_entries', function (Blueprint $table) {
    $table->id();
    $table->foreignId('project_id')->constrained('projects')->cascadeOnDelete();
    $table->foreignId('user_id')->constrained('users')->cascadeOnDelete();
    $table->timestamp('clocked_in_at');
    $table->timestamp('clocked_out_at')->nullable();
    $table->timestamp('last_heartbeat_at')->nullable();
    $table->timestamps();
    $table->index(['project_id', 'user_id']);
    $table->index(['user_id', 'clocked_out_at']);
});
```

From app/Models/TimeEntry.php (current fillable + casts):
```php
protected $fillable = [
    'project_id', 'user_id', 'clocked_in_at', 'clocked_out_at', 'last_heartbeat_at',
];
protected function casts(): array {
    return [
        'clocked_in_at' => 'datetime',
        'clocked_out_at' => 'datetime',
        'last_heartbeat_at' => 'datetime',
    ];
}
public function project(): BelongsTo { return $this->belongsTo(Project::class); }
public function user(): BelongsTo { return $this->belongsTo(User::class); }
public function isOpen(): bool { return $this->clocked_out_at === null; }
```

From CONTEXT.md §Claude's Discretion:
> Audit log table shape — `time_entry_audits(id, time_entry_id, edited_by_user_id, field, old_value, new_value, edited_at)` is the baseline; planner may extend
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend time_entries schema — category, notes, closure_reason + indexes</name>
  <files>database/migrations/2026_04_21_000001_extend_time_entries_for_phase_15.php, tests/Unit/Migrations/TimeEntriesPhase15MigrationTest.php</files>
  <read_first>
    - database/migrations/2026_04_20_000002_create_time_entries_table.php (existing Phase 14 schema — columns you MUST NOT redefine)
    - .planning/phases/15-time-tracking/15-CONTEXT.md (decisions D-01…D-19 — category values, closure_reason semantics, UTC storage)
    - app/Models/TimeEntry.php (existing fillable shape — you mirror this in Task 3)
    - tests/Unit/Migrations (existing migration test pattern, if any — else create first such test)
    - CLAUDE.md (safe migrations requirement — additive only, no column renames)
  </read_first>
  <behavior>
    - After migration runs, `time_entries` has columns: id, project_id, user_id, category (string, nullable), clocked_in_at, clocked_out_at, last_heartbeat_at, notes (text, nullable), closure_reason (string, nullable), created_at, updated_at
    - `category` accepts exactly these values: 'installation', 'commissioning', 'testing', 'other'; enforcement is application-level (enum-style check in TimeEntryService), NOT a DB CHECK constraint (SQLite/MySQL parity)
    - `closure_reason` column accepts null (normal manual clock-out) or the string 'stale_auto_close' (written by Phase 15-03 command)
    - `notes` is text (not varchar) to accept up to 500 chars per D-06 without arbitrary MySQL column-length limits
    - An index on `(project_id, clocked_in_at)` exists to speed dashboard widget totals queries
    - Migration is reversible: `down()` drops the three added columns + the new index in a single `Schema::table` call
    - On SQLite (test env) and MySQL the migration runs clean with no warnings
  </behavior>
  <action>
    Create a new migration `database/migrations/2026_04_21_000001_extend_time_entries_for_phase_15.php` (anonymous class, standard Laravel 12 pattern) that performs `Schema::table('time_entries', ...)` adding:

    1. `$table->string('category', 32)->nullable()->after('user_id');` — nullable so existing Phase 14 rows backfill to null; Phase 15-02 will enforce non-null at the service layer per D-03. Max length 32 to comfortably fit 'commissioning' + headroom.
    2. `$table->text('notes')->nullable()->after('last_heartbeat_at');` — per D-06 clock-out note, max 500 chars enforced at validation layer, not DB.
    3. `$table->string('closure_reason', 32)->nullable()->after('notes');` — per D-12. Null = manual clock-out; 'stale_auto_close' = written by the scheduled command.
    4. `$table->index(['project_id', 'clocked_in_at'], 'time_entries_project_clocked_in_index');` — supports `SELECT category, SUM(TIMESTAMPDIFF(MINUTE, clocked_in_at, clocked_out_at)) ... GROUP BY category` for the dashboard widget.

    `down()` must drop the index FIRST then drop the three columns in a single `Schema::table('time_entries', fn($t) => $t->dropIndex(...); $t->dropColumn(['category','notes','closure_reason']))` call. Use the explicit index name so the drop is portable.

    DO NOT add CHECK constraints on `category` (SQLite test env cannot portably express them and MySQL support is version-dependent). Enforcement belongs in TimeEntryService::start() per Phase 14 precedent.

    DO NOT touch existing columns. This migration is additive-only per CLAUDE.md safe-migration rule.

    Create the migration test at `tests/Unit/Migrations/TimeEntriesPhase15MigrationTest.php` using `RefreshDatabase` that asserts:
    - `Schema::hasColumn('time_entries', 'category')` returns true
    - `Schema::hasColumn('time_entries', 'notes')` returns true
    - `Schema::hasColumn('time_entries', 'closure_reason')` returns true
    - `Schema::hasColumn('time_entries', 'last_heartbeat_at')` still returns true (Phase 14 column preserved)
  </action>
  <verify>
    <automated>php artisan migrate --pretend && php artisan migrate && php artisan test --filter=TimeEntriesPhase15MigrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "\\\$table->string\\('category'" database/migrations/2026_04_21_000001_extend_time_entries_for_phase_15.php` returns the line (category column added)
    - `grep -E "\\\$table->text\\('notes'\\)" database/migrations/2026_04_21_000001_extend_time_entries_for_phase_15.php` returns the line
    - `grep -E "\\\$table->string\\('closure_reason'" database/migrations/2026_04_21_000001_extend_time_entries_for_phase_15.php` returns the line
    - `grep "time_entries_project_clocked_in_index" database/migrations/2026_04_21_000001_extend_time_entries_for_phase_15.php` returns two lines (up + down)
    - `grep -c "dropColumn" database/migrations/2026_04_21_000001_extend_time_entries_for_phase_15.php` >= 1
    - `php artisan migrate` exits 0
    - `php artisan test --filter=TimeEntriesPhase15MigrationTest` reports all assertions pass
  </acceptance_criteria>
  <done>Migration runs up and down cleanly on SQLite; three new columns present; index created; reversible; migration test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create time_entry_audits migration + TimeEntryAudit model</name>
  <files>database/migrations/2026_04_21_000002_create_time_entry_audits_table.php, app/Models/TimeEntryAudit.php, database/factories/TimeEntryFactory.php, tests/Unit/Models/TimeEntryAuditTest.php</files>
  <read_first>
    - app/Models/TimeEntry.php (belongsTo User / Project pattern to copy)
    - app/Models/RamsDocument.php (audit-row model pattern if one exists — check for immutable/append-only conventions; fall back to plain Model if not)
    - .planning/phases/15-time-tracking/15-CONTEXT.md §decisions D-04, D-05, D-07 (who can edit, what gets logged, append-only)
    - database/migrations/2026_04_20_000002_create_time_entries_table.php (FK constraint pattern with cascadeOnDelete)
    - database/factories/ (check for existing TimeEntryFactory; Phase 14 feature test uses `TimeEntry::first()` suggesting none — you create one if missing)
  </read_first>
  <behavior>
    - `time_entry_audits` table created with columns: id, time_entry_id, edited_by_user_id, field, old_value, new_value, edited_at, created_at, updated_at
    - `time_entry_id` is a foreignId constrained to `time_entries` with cascadeOnDelete (if parent entry is deleted, audits go too)
    - `edited_by_user_id` is a foreignId constrained to `users` with restrictOnDelete (audit history must survive user soft-deletion for ops review)
    - `field` column stores exactly one of: 'category', 'notes' (the two retro-editable fields per D-04 / D-07); enforced at service layer in Plan 15-02
    - `old_value` and `new_value` are text (nullable — `old_value` is null only for the synthetic "first edit" if we ever record a pristine→edited diff)
    - `edited_at` is a UTC timestamp (separate from `created_at` so the service can stamp edit time explicitly)
    - Index on `(time_entry_id, edited_at)` supports "show audit history for entry X chronologically"
    - TimeEntryAudit model has fillables for all user-writable columns, casts `edited_at` to datetime, exposes `timeEntry()` and `editor()` belongsTo relations
    - TimeEntryAudit has NO `update()` method bypass — remains a plain Eloquent model but Plan 15-02 service only ever `create()`s audit rows, never updates/deletes them
  </behavior>
  <action>
    Create migration `database/migrations/2026_04_21_000002_create_time_entry_audits_table.php` (anonymous class) with:

    ```
    Schema::create('time_entry_audits', function (Blueprint $table) {
        $table->id();
        $table->foreignId('time_entry_id')->constrained('time_entries')->cascadeOnDelete();
        $table->foreignId('edited_by_user_id')->constrained('users')->restrictOnDelete();
        $table->string('field', 32);         // 'category' | 'notes'
        $table->text('old_value')->nullable();
        $table->text('new_value')->nullable();
        $table->timestamp('edited_at');
        $table->timestamps();
        $table->index(['time_entry_id', 'edited_at'], 'time_entry_audits_entry_edited_index');
    });
    ```

    `down()` = `Schema::dropIfExists('time_entry_audits')`.

    Create `app/Models/TimeEntryAudit.php` extending `Model` with:
    - `use HasFactory` (factory added later if needed for tests; leaving the trait in place keeps the pattern consistent with other models)
    - `protected $fillable = ['time_entry_id', 'edited_by_user_id', 'field', 'old_value', 'new_value', 'edited_at'];`
    - `protected function casts(): array { return ['edited_at' => 'datetime']; }`
    - Class constant `FIELD_CATEGORY = 'category'`, `FIELD_NOTES = 'notes'`
    - Constant array `FIELDS = [self::FIELD_CATEGORY, self::FIELD_NOTES]`
    - `public function timeEntry(): BelongsTo { return $this->belongsTo(TimeEntry::class); }`
    - `public function editor(): BelongsTo { return $this->belongsTo(User::class, 'edited_by_user_id'); }`
    - PHPDoc class block explaining the append-only semantic: "Service layer only ever calls TimeEntryAudit::create(); no update/delete paths exist. Retcon detection for ops."

    Create the unit test at `tests/Unit/Models/TimeEntryAuditTest.php` with these tests:
    - `test_audit_belongs_to_time_entry` — creates an audit row pointing at an entry, asserts `$audit->timeEntry->is($entry)` (requires a TimeEntry factory — `TimeEntry::factory()->create()` — already exists per Phase 14)
    - `test_audit_belongs_to_editor` — creates an audit row with `edited_by_user_id = $user->id`, asserts `$audit->editor->is($user)`
    - `test_field_constant_covers_both_retro_editable_fields` — asserts `TimeEntryAudit::FIELDS === ['category', 'notes']`

    **TimeEntryFactory** — check first: `ls database/factories/TimeEntryFactory.php`. Phase 14's `tests/Feature/TimeEntries/TimeEntryTest.php` uses `TimeEntry::first()` rather than `::factory()`, so the factory likely does not exist yet. If missing, create `database/factories/TimeEntryFactory.php` with a minimal definition: project_id from `Project::factory()`, user_id from `User::factory()`, clocked_in_at = now(), clocked_out_at = null, category = 'installation'. Standard `Illuminate\Database\Eloquent\Factories\Factory` subclass returning the define() array. Also ensure `TimeEntry` model has `use HasFactory;` trait in place (Phase 14 precedent should already have this — verify via grep). This file is listed in the plan's files_modified frontmatter.

    DO NOT add a `User::timeEntryAudits()` relationship in this task — that belongs with Task 3 where we touch User.php.
  </action>
  <verify>
    <automated>php artisan migrate:fresh && php artisan test --filter=TimeEntryAuditTest</automated>
  </verify>
  <acceptance_criteria>
    - File `database/migrations/2026_04_21_000002_create_time_entry_audits_table.php` exists
    - `grep "Schema::create('time_entry_audits'" database/migrations/2026_04_21_000002_create_time_entry_audits_table.php` returns the match
    - `grep "cascadeOnDelete" database/migrations/2026_04_21_000002_create_time_entry_audits_table.php` returns at least 1 match (time_entry_id FK)
    - `grep "restrictOnDelete" database/migrations/2026_04_21_000002_create_time_entry_audits_table.php` returns 1 match (edited_by_user_id FK)
    - `grep "class TimeEntryAudit" app/Models/TimeEntryAudit.php` returns the class declaration
    - `grep "FIELD_CATEGORY = 'category'" app/Models/TimeEntryAudit.php` returns the constant
    - `grep "FIELD_NOTES = 'notes'" app/Models/TimeEntryAudit.php` returns the constant
    - `grep -E "public function timeEntry\\(\\)" app/Models/TimeEntryAudit.php` returns the method
    - `grep -E "public function editor\\(\\)" app/Models/TimeEntryAudit.php` returns the method
    - If `database/factories/TimeEntryFactory.php` was created: `grep "class TimeEntryFactory" database/factories/TimeEntryFactory.php` matches
    - `php artisan migrate:fresh` exits 0
    - `php artisan test --filter=TimeEntryAuditTest` reports all 3 tests pass
  </acceptance_criteria>
  <done>time_entry_audits table created with correct shape; TimeEntryAudit model has relations + FIELDS constant; three model tests green; TimeEntryFactory exists (either pre-existing or newly created).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend TimeEntry model + User model with Phase 15 fields and relations</name>
  <files>app/Models/TimeEntry.php, app/Models/User.php, tests/Unit/Models/TimeEntryTest.php</files>
  <read_first>
    - app/Models/TimeEntry.php (existing fillable + casts — you extend, don't rewrite)
    - app/Models/User.php (existing relationships pattern — add two hasMany in the same style)
    - app/Models/TimeEntryAudit.php (created in Task 2 — you wire TimeEntry::audits() to it)
    - .planning/phases/15-time-tracking/15-CONTEXT.md §decisions D-01, D-11, D-12 (category enum values, closure_reason semantics)
  </read_first>
  <behavior>
    - TimeEntry `$fillable` includes the three new columns: 'category', 'notes', 'closure_reason'
    - TimeEntry has class constants for the 4 categories (all lowercase): CATEGORY_INSTALLATION = 'installation', CATEGORY_COMMISSIONING = 'commissioning', CATEGORY_TESTING = 'testing', CATEGORY_OTHER = 'other'
    - TimeEntry has `const CATEGORIES = [self::CATEGORY_INSTALLATION, self::CATEGORY_COMMISSIONING, self::CATEGORY_TESTING, self::CATEGORY_OTHER];` used by validation in Plan 15-02
    - TimeEntry has `const CLOSURE_REASON_STALE_AUTO_CLOSE = 'stale_auto_close';` (only non-null value used in Phase 15)
    - TimeEntry exposes `audits()` hasMany → TimeEntryAudit ordered by edited_at DESC scope (scope as a separate `orderBy` in the caller; relation stays vanilla)
    - User model gains `timeEntries()` hasMany → TimeEntry
    - User model gains `timeEntryAudits()` hasMany → TimeEntryAudit with foreignKey 'edited_by_user_id'
    - Casts unchanged (datetime casts on all three timestamp columns already in place — no DateTime cast on `category`, `notes`, `closure_reason` needed)
  </behavior>
  <action>
    Edit `app/Models/TimeEntry.php`:

    1. Extend `$fillable` to: `['project_id', 'user_id', 'category', 'clocked_in_at', 'clocked_out_at', 'last_heartbeat_at', 'notes', 'closure_reason']`
    2. Keep `casts()` as-is (no new casts needed — category/notes/closure_reason are strings with no transformation)
    3. Add these class constants (uppercase-snake per CLAUDE.md convention; add right after `use HasFactory`):
       ```php
       public const CATEGORY_INSTALLATION  = 'installation';
       public const CATEGORY_COMMISSIONING = 'commissioning';
       public const CATEGORY_TESTING       = 'testing';
       public const CATEGORY_OTHER         = 'other';

       public const CATEGORIES = [
           self::CATEGORY_INSTALLATION,
           self::CATEGORY_COMMISSIONING,
           self::CATEGORY_TESTING,
           self::CATEGORY_OTHER,
       ];

       public const CLOSURE_REASON_STALE_AUTO_CLOSE = 'stale_auto_close';
       ```
    4. Add `use Illuminate\Database\Eloquent\Relations\HasMany;` to imports.
    5. Add relation method after user():
       ```php
       public function audits(): HasMany
       {
           return $this->hasMany(TimeEntryAudit::class);
       }
       ```
    6. Update the class-level PHPDoc block: replace "Phase 15 will extend with category, notes, heartbeat loop" with "Phase 15 extends: category (CATEGORIES enum), notes, closure_reason, audits()." — keeps code-comments honest per CLAUDE.md.

    Edit `app/Models/User.php`:

    1. Add `use Illuminate\Database\Eloquent\Relations\HasMany;` to imports if absent.
    2. Add two relation methods (place after existing ramsDocuments() or similar hasMany; if no other hasMany exists, add at the bottom of the class before the final `}`):
       ```php
       public function timeEntries(): HasMany
       {
           return $this->hasMany(TimeEntry::class);
       }

       public function timeEntryAudits(): HasMany
       {
           return $this->hasMany(TimeEntryAudit::class, 'edited_by_user_id');
       }
       ```

    Create/extend `tests/Unit/Models/TimeEntryTest.php` (if the file doesn't exist in tests/Unit/Models, create it — the existing test is tests/Feature/TimeEntries/TimeEntryTest.php which is the feature test, separate file) with:
    - `test_categories_constant_has_four_values` — asserts `count(TimeEntry::CATEGORIES) === 4` and asserts each expected value is present
    - `test_closure_reason_stale_auto_close_constant` — asserts `TimeEntry::CLOSURE_REASON_STALE_AUTO_CLOSE === 'stale_auto_close'`
    - `test_entry_has_audits_relation` — creates a TimeEntry + 2 TimeEntryAudit rows pointing at it, asserts `$entry->audits->count() === 2`
    - `test_user_has_time_entries_relation` — creates a user + 2 time entries for them, asserts `$user->timeEntries->count() === 2`
    - `test_user_has_time_entry_audits_relation_by_editor` — creates a user who edits 2 entries (rows in time_entry_audits with edited_by_user_id = user->id), asserts `$user->timeEntryAudits->count() === 2`

    DO NOT modify the existing `isOpen()` method. DO NOT add a category-validation helper to TimeEntry — validation lives in Plan 15-02 service.
  </action>
  <verify>
    <automated>php artisan test --filter=TimeEntryTest</automated>
  </verify>
  <acceptance_criteria>
    - `grep "'category'" app/Models/TimeEntry.php | head -1` returns a line showing category in $fillable
    - `grep "'notes'" app/Models/TimeEntry.php | head -1` returns a line showing notes in $fillable
    - `grep "'closure_reason'" app/Models/TimeEntry.php | head -1` returns a line showing closure_reason in $fillable
    - `grep "CATEGORY_INSTALLATION = 'installation'" app/Models/TimeEntry.php` matches
    - `grep "CATEGORY_COMMISSIONING = 'commissioning'" app/Models/TimeEntry.php` matches
    - `grep "CATEGORY_TESTING = 'testing'" app/Models/TimeEntry.php` matches
    - `grep "CATEGORY_OTHER = 'other'" app/Models/TimeEntry.php` matches
    - `grep "CLOSURE_REASON_STALE_AUTO_CLOSE = 'stale_auto_close'" app/Models/TimeEntry.php` matches
    - `grep "const CATEGORIES" app/Models/TimeEntry.php` matches
    - `grep -E "public function audits\\(\\): HasMany" app/Models/TimeEntry.php` matches
    - `grep -E "public function timeEntries\\(\\): HasMany" app/Models/User.php` matches
    - `grep -E "public function timeEntryAudits\\(\\): HasMany" app/Models/User.php` matches
    - `grep "edited_by_user_id" app/Models/User.php` matches (confirms the FK param is passed)
    - `php artisan test --filter="TimeEntryTest"` reports 5+ passing tests in tests/Unit/Models/TimeEntryTest.php
    - `php artisan test --filter="tests/Feature/TimeEntries"` — the existing Phase 14 tests still pass (no regression)
  </acceptance_criteria>
  <done>TimeEntry has fillables + constants + audits() relation; User has two new hasMany relations; unit tests green; Phase 14 feature tests still green (no regression).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema → application | All category enforcement is application-side, not DB CHECK constraint — relying on layered validation in 15-02 |
| client → DB via Eloquent mass-assignment | New fillable fields expose extra surface for mass-assignment — request validation in 15-02 gates what actually reaches `update()` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | Tampering | time_entries.category (enum-by-convention) | mitigate | TimeEntry::CATEGORIES constant is the single source of truth; Plan 15-02 TimeEntryService::start() validates input against this constant and throws InvalidArgumentException on mismatch (reused pattern from Phase 14 blocked_reason guard) |
| T-15-01-02 | Tampering | time_entries.notes mass-assignment | mitigate | Fillable includes notes, but Plan 15-02 controller uses Form Request validation (max:500) before calling update(); model fillable is the inner ring, FormRequest is the outer ring |
| T-15-01-03 | Repudiation | time_entry_audits append-only | mitigate | No update/delete code paths to TimeEntryAudit ship in Phase 15; only `TimeEntryAudit::create()` in TimeEntryService::editEntry(). Reviewed in Plan 15-02 checker pass. FK restrictOnDelete on edited_by_user_id preserves history even if user is removed |
| T-15-01-04 | Information disclosure | time_entry_audits reveals editor identity | accept | Ops reviews audits; editor identity is the point. No additional mitigation needed — table is not exposed to clients |
| T-15-01-05 | Denial of service | Unbounded notes text | mitigate | FormRequest max:500 in Plan 15-02; MySQL `TEXT` column hard ceiling is 65,535 bytes (Laravel default) — safe cap even if validation bypassed |
| T-15-01-06 | Elevation of privilege | Anyone can insert audit rows via Eloquent mass-assignment | accept | TimeEntryAudit::create() is called only from TimeEntryService::editEntry(); no HTTP endpoint writes directly. The fillable surface here only matters if a future controller bypass exists — covered by Plan 15-02 threat model |
</threat_model>

<verification>
- `php artisan migrate:fresh` runs cleanly (both new migrations + all Phase 14 migrations)
- All three unit tests (migration, TimeEntryAudit, TimeEntry) pass
- Phase 14 feature tests (`tests/Feature/TimeEntries/TimeEntryTest.php`) still pass — no regression on the Phase 14 happy paths
- `php artisan tinker` + `TimeEntry::CATEGORIES` returns the 4-element array
- `php artisan tinker` + `TimeEntry::create([...])` with `category => 'installation'` persists without error
</verification>

<success_criteria>
- time_entries table has 11 columns (id, project_id, user_id, category, clocked_in_at, clocked_out_at, last_heartbeat_at, notes, closure_reason, created_at, updated_at) — verified via `Schema::getColumnListing('time_entries')` in tinker
- time_entry_audits table has 9 columns — verified via `Schema::getColumnListing('time_entry_audits')`
- TimeEntry::CATEGORIES enum array exists with exactly 4 values
- TimeEntryAudit model has both belongsTo relations wired
- User model has timeEntries() + timeEntryAudits() hasMany relations
- Downstream plans (15-02, 15-03, 15-05) can read/write these columns + relations without schema changes
</success_criteria>

<output>
After completion, create `.planning/phases/15-time-tracking/15-01-SUMMARY.md` documenting:
- Final schema columns added to time_entries
- time_entry_audits table shape
- Category constants + array
- Two new User relationships
- Known constraints (application-side category enforcement, not DB CHECK)
</output>
</output>
