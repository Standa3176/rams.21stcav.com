---
phase: 13-task-assignment-scheduling
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/InstallProgrammeController.php
  - resources/views/install-programmes/schedule.blade.php
autonomous: true
gap_closure: true
requirements:
  - INST-02e
  - INST-02f

must_haves:
  truths:
    - "The ganttTasks map in InstallProgrammeController::schedule() includes 'room' and 'engineer' keys for every task"
    - "The slide-over panel in schedule.blade.php renders activeTask.room and activeTask.engineer via x-text bindings"
    - "frappe-gantt is installed in node_modules and present in the built JS bundle"
  artifacts:
    - path: "app/Http/Controllers/InstallProgrammeController.php"
      provides: "'room' => $t->room_name and 'engineer' => $t->assignedUser?->name in the $ganttTasks map"
      contains: "'room'     => $t->room_name"
    - path: "resources/views/install-programmes/schedule.blade.php"
      provides: "slide-over dt/dd pairs for Room and Engineer bound to activeTask.room and activeTask.engineer"
      contains: "x-text=\"activeTask.room"
    - path: "public/build/assets/"
      provides: "built JS bundle containing frappe-gantt code"
  key_links:
    - from: "InstallProgrammeController::schedule() $ganttTasks map"
      to: "schedule.blade.php slide-over panel"
      via: "{{ Js::from($ganttTasks) }} → activeTask.room / activeTask.engineer x-text bindings"
      pattern: "'room'.*room_name"
---

<objective>
Close two gaps found in VERIFICATION.md for Phase 13:

Gap 1 (Blocker — INST-02f): The slide-over panel must show task room and assigned engineer name. The $ganttTasks
map in InstallProgrammeController::schedule() must include 'room' and 'engineer' keys; the Blade slide-over must
bind those fields via x-text.

Gap 2 (Environment — INST-02e): frappe-gantt must be installed in node_modules and built into the JS bundle so
window.Gantt is available at runtime. The package.json and app.js imports are already correct — the fix is
running npm install and npm run build.

Purpose: Fully satisfy INST-02e (Gantt renders when programme > 4 days) and INST-02f (click-to-detail shows
room + engineer, read-only).

Output: Controller with room/engineer in ganttTasks map, Blade with matching slide-over bindings, built JS
bundle containing frappe-gantt (>150KB).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-task-assignment-scheduling/13-02-SUMMARY.md
@.planning/phases/13-task-assignment-scheduling/13-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Confirm and complete Gap 1 — room and engineer in ganttTasks map and slide-over</name>

  <read_first>
    - app/Http/Controllers/InstallProgrammeController.php (lines 178–195 — the $ganttTasks map)
    - resources/views/install-programmes/schedule.blade.php (lines 196–233 — the slide-over template)
  </read_first>

  <files>app/Http/Controllers/InstallProgrammeController.php, resources/views/install-programmes/schedule.blade.php</files>

  <action>
    Read both files first. Inspect the current state of each file against the required conditions below.

    CONTROLLER — the $ganttTasks map (around lines 179–195 of InstallProgrammeController.php) must produce
    objects with these exact keys:
      'id'       => 'task-' . $t->id,
      'name'     => $t->title,
      'start'    => Carbon::parse($t->planned_start_date)->format('Y-m-d'),
      'end'      => Carbon::parse($t->planned_end_date)->format('Y-m-d'),
      'progress' => match ($t->status) { ... },
      'room'     => $t->room_name,
      'engineer' => $t->assignedUser?->name,

    If 'room' => $t->room_name AND 'engineer' => $t->assignedUser?->name are ALREADY present — no change needed.
    If either is missing — add it to the map. The map is inside the ->map(fn ($t) => [...]) closure.

    BLADE — the slide-over <dl> section inside <template x-if="activeTask"> must include:
      <div>
          <dt style="font-size:.75rem;color:var(--text-muted);text-transform:uppercase;letter-spacing:.05em;">Room</dt>
          <dd style="font-size:.875rem;margin-top:.15rem;" x-text="activeTask.room || '—'"></dd>
      </div>
      <div>
          <dt style="font-size:.75rem;color:var(--text-muted);text-transform:uppercase;letter-spacing:.05em;">Engineer</dt>
          <dd style="font-size:.875rem;margin-top:.15rem;" x-text="activeTask.engineer || 'Unassigned'"></dd>
      </div>

    If both <dd> elements with x-text="activeTask.room" and x-text="activeTask.engineer" are ALREADY present —
    no change needed.
    If either is missing — add both <div> blocks immediately before the existing Start <div> block in the <dl>.

    After confirming or editing, run the grep acceptance checks below to verify.
  </action>

  <verify>
    <automated>
      grep -n "room.*room_name" app/Http/Controllers/InstallProgrammeController.php
      grep -n "engineer.*assignedUser" app/Http/Controllers/InstallProgrammeController.php
      grep -n "activeTask.room" resources/views/install-programmes/schedule.blade.php
      grep -n "activeTask.engineer" resources/views/install-programmes/schedule.blade.php
    </automated>
  </verify>

  <acceptance_criteria>
    - grep "room.*room_name" app/Http/Controllers/InstallProgrammeController.php returns at least 1 match on a line inside the $ganttTasks ->map() closure
    - grep "engineer.*assignedUser" app/Http/Controllers/InstallProgrammeController.php returns at least 1 match on a line inside the $ganttTasks ->map() closure
    - grep "activeTask.room" resources/views/install-programmes/schedule.blade.php returns at least 1 match inside the <template x-if="activeTask"> block
    - grep "activeTask.engineer" resources/views/install-programmes/schedule.blade.php returns at least 1 match inside the <template x-if="activeTask"> block
    - The slide-over panel has Room above Engineer, and both appear before the Start entry
  </acceptance_criteria>

  <done>Both 'room' and 'engineer' are present as keys in the $ganttTasks map in the controller, and both activeTask.room and activeTask.engineer are bound via x-text in the Blade slide-over.</done>
</task>

<task type="auto">
  <name>Task 2: Close Gap 2 — install frappe-gantt and rebuild JS bundle</name>

  <read_first>
    - package.json (confirm frappe-gantt is listed in dependencies)
    - resources/js/app.js (confirm import Gantt from 'frappe-gantt' and window.Gantt = Gantt are present)
  </read_first>

  <files>public/build/assets/ (output — no source file change)</files>

  <action>
    This is an environment setup task. The source files (package.json and resources/js/app.js) are already correct.
    The only action required is to install the package and rebuild the bundle.

    Step 1: Confirm frappe-gantt appears in package.json dependencies:
      grep "frappe-gantt" package.json
    Expected: "frappe-gantt": "^1.2.2"

    Step 2: Confirm app.js has the import:
      grep "frappe-gantt\|window.Gantt" resources/js/app.js
    Expected: import line and window.Gantt = Gantt assignment both present.

    Step 3: Run the build:
      npm install && npm run build

    Step 4: Verify the output bundle contains Gantt code:
      grep -ic "gantt" public/build/assets/app-*.js
    Expected: a count greater than 0 (frappe-gantt adds ~70KB minified; the bundle should be >150KB).

    Step 5: Check the bundle file size to confirm frappe-gantt was included:
      ls -lh public/build/assets/app-*.js
    Expected: JS file size > 150KB (current unbundled size is ~83KB; frappe-gantt adds ~70KB).

    If npm install fails due to network or permission issues, log the error. Do NOT modify package.json or
    app.js — those files are already correct. The fix is purely the build command.
  </action>

  <verify>
    <automated>
      grep -ic "gantt" public/build/assets/app-*.js
    </automated>
  </verify>

  <acceptance_criteria>
    - grep -ic "gantt" public/build/assets/app-*.js returns a count > 0 (meaning frappe-gantt code is present in the built bundle)
    - ls -lh public/build/assets/app-*.js shows a JS file larger than 150KB
    - node_modules/frappe-gantt/dist/frappe-gantt.esm.js exists (confirms npm install ran successfully)
    - No errors in npm install output (or only minor deprecation warnings — not failures)
  </acceptance_criteria>

  <done>npm install completed, npm run build produced a new bundle in public/build/assets/ that contains frappe-gantt code, and the bundle size exceeds 150KB.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Blade → Alpine.js | $ganttTasks JSON is rendered server-side via Js::from() — HTML-escaped, no XSS risk |
| Alpine → DOM | activeTask.room and activeTask.engineer are bound via x-text (text content, not innerHTML) — safe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03-01 | Information Disclosure | slide-over engineer field | accept | Engineer names are already visible in the week-view table to the same users — no new disclosure. INST-02g server-side filter already scopes task visibility correctly. |
| T-13-03-02 | Information Disclosure | $ganttTasks JSON in page source | accept | ganttTasks is filtered by the same INST-02g auth logic (field engineers see only their tasks). JSON is scoped to authorised tasks only. |
</threat_model>

<verification>
After both tasks complete, verify the following:

1. Controller check:
   grep -c "'room'" app/Http/Controllers/InstallProgrammeController.php
   -- Must return >= 1 match in the ganttTasks map

2. Controller check:
   grep -c "'engineer'" app/Http/Controllers/InstallProgrammeController.php
   -- Must return >= 1 match in the ganttTasks map

3. Blade check:
   grep -c "activeTask.room" resources/views/install-programmes/schedule.blade.php
   -- Must return >= 1

4. Blade check:
   grep -c "activeTask.engineer" resources/views/install-programmes/schedule.blade.php
   -- Must return >= 1

5. Build check:
   grep -ic "gantt" public/build/assets/app-*.js
   -- Must return > 0
</verification>

<success_criteria>
- INST-02f (Gantt click-to-detail): The $ganttTasks map passes 'room' and 'engineer' to the Blade JSON payload; the Alpine slide-over renders both via x-text bindings — a Gantt task click will show title, room, engineer, start, end, and progress.
- INST-02e (Gantt renders): frappe-gantt is present in node_modules and compiled into the built JS bundle. window.Gantt will be defined at runtime when the schedule page loads, and the Gantt will render for programmes with duration > 4 days.
- Both gaps from 13-VERIFICATION.md are resolved: score advances from 5/7 to 7/7 ROADMAP success criteria.
</success_criteria>

<output>
After completion, create `.planning/phases/13-task-assignment-scheduling/13-03-SUMMARY.md` documenting:
- Which gaps were found already resolved vs requiring changes
- The npm build output (bundle size and file name)
- Final grep evidence for all four acceptance checks
</output>
