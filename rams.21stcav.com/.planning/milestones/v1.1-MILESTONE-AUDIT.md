---
milestone: v1.1
milestone_name: Operations Dashboard & Notifications
audited: 2026-04-24T00:00:00Z
status: tech_debt
scores:
  requirements: 34/34
  phases: 2/2 (originally scoped 4, executed as 2 — 10/11 dropped)
  integration: 14/14 wiring + 6/6 flows
  nyquist: 0 fully compliant + 2 partial of 2 phases
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 08-enterprise-dashboard
    items:
      - "VALIDATION.md present but `nyquist_compliant: false` and `wave_0_complete: false` — Nyquist contract not closed"
      - "Phase scope was narrowed during execution: ROADMAP overview table marks v1.1 as 'phases 08–11' but only 08 + 09 were executed; 10 and 11 dropped or absorbed elsewhere — record this scope reduction in MILESTONES.md"
  - phase: 09-email-notifications
    items:
      - "VALIDATION.md present, `nyquist_compliant: true` but `wave_0_complete: false` — partial Nyquist closure"
      - "REQUIREMENTS.md checkboxes for NOTF-01a..f, NOTF-02a, NOTF-03a..c, NOTF-04a..c, NOTF-05a..h still `[ ]` even though VERIFICATION.md `requirement_coverage` marks every one `pass` — needs sync"
      - "REVIEW WR-01..WR-04 polish items: error-message truncation inconsistencies + narrow `failed()` try/catch gap in BuildRams/BuildWorksheet that could drop a failure alert on transient DB error during the timestamp write — non-blocking, tracked"
      - "**Operational debt (NOTF-05g)**: production Postmark cutover deferred — DNS records (SPF/DKIM/DMARC), POSTMARK_API_KEY in production .env, sender signature verification for rams@21stcav.com, first-send confirmation. Runbook at `.planning/phases/09-email-notifications/POSTMARK-OPS-CHECKLIST.md`"
      - "**Operational debt (dev smoke)**: end-to-end log-mailer rendering smoke against a real RAMS generation in dev — needs a manual queue-worker run; 6 mailables × Mail::fake() are unit-locked but the full log round-trip is not"
nyquist:
  compliant_phases: []
  partial_phases: ["08-enterprise-dashboard", "09-email-notifications"]
  missing_phases: []
  overall: partial_compliance
---

# Milestone v1.1 Audit Report

**Milestone:** v1.1 — Operations Dashboard & Notifications
**Theme:** *"Real-time visibility across active projects + automated email triggers for AV-ops events"*
**Audited:** 2026-04-24
**Phases:** 08, 09 (originally scoped as 08–11; 10 and 11 dropped during execution)
**Status:** ⚡ **tech_debt** — all requirements satisfied; operational + metadata debt accumulated

---

## Executive Summary

Phases 08 and 09 deliver the v1.1 promise end-to-end. The integration checker confirmed **14/14 wiring checks** and **6/6 E2E flows PASS**. All 26 NOTF-* requirements + 8 DASH-01* sub-IDs are marked SATISFIED in the corresponding VERIFICATION.md with concrete evidence (mailables × 6, idempotency columns × 9, recipient resolver service, BCC config wiring, eager-loaded dashboard query, ProjectHealthService DTO + rules).

What's outstanding is **operational, not implementation**: NOTF-05g (production Postmark cutover) is a DNS + secrets + sender-verification task that lives outside the codebase, with a documented runbook. The dev log-mailer smoke needs a manual queue-worker run. Nyquist contracts exist but neither phase reached `wave_0_complete: true`.

**Scope reduction note:** v1.1 was originally scoped as phases 08–11 in the ROADMAP overview table, but only 08 (Enterprise Dashboard) and 09 (Email Notifications) were executed. Phases 10 and 11 do not exist on disk. This scope reduction should be recorded in MILESTONES.md when archiving.

---

## 1. Requirements Coverage (3-source cross-reference)

### Final status per requirement

| REQ-ID group | Phase | VERIFICATION | SUMMARY FM | REQUIREMENTS.md | Final |
|--------------|-------|--------------|------------|-----------------|-------|
| DASH-01 + DASH-01a–h (9) | 08 | ✅ all SATISFIED (`requirements_verified` lists all 8 sub-IDs) | ⚠ missing | ☑ all checked | **satisfied** |
| NOTF-01 + NOTF-01a–f (7) | 09 | ✅ all `pass` | ✅ listed across 09-01/02b/05 | ☐ unchecked | **satisfied** (sync needed) |
| NOTF-02 + NOTF-02a (2) | 09 | ✅ all `pass` | ✅ listed (09-02b, 09-05) | ☐ unchecked | **satisfied** (sync needed) |
| NOTF-03 + NOTF-03a–c (4) | 09 | ✅ all `pass` | ✅ listed (09-01, 09-05) | ☐ unchecked | **satisfied** (sync needed) |
| NOTF-04 + NOTF-04a–c (4) | 09 | ✅ all `pass` | ✅ listed (09-01, 09-02b, 09-05) | ☐ unchecked | **satisfied** (sync needed) |
| NOTF-05 + NOTF-05a–h (9) | 09 | ✅ 8 `pass` + 1 `pass_operational_pending` (NOTF-05g) | ✅ listed (09-01, 09-05, 09-06) | ☐ unchecked | **satisfied** (sync needed; NOTF-05g code-wired, prod-cutover pending) |

**Result:** 34 / 34 satisfied at the code level. NOTF-05g additionally requires production cutover before audit closure can claim "operational PASS"; treated as known operational debt.

### Verification status per phase

| Phase | VERIFICATION status | Score |
|-------|--------------------|-------|
| 08 | passed (initial verification) | 7/7 must-haves; human-checkpoint approved 2026-04-19 (commit 20d84a3 + FOUC fix 04cf678) |
| 09 | human_needed (auto verification + 2 ops-human items documented) | 9/9 automated must-haves; 26/26 NOTF-* `pass`; NOTF-05g `pass_operational_pending` |

`human_needed` here means *operational* verification (Postmark cutover) — not "implementation incomplete." Code wiring is fully verified.

---

## 2. Cross-Phase Integration

(Full report from gsd-integration-checker agent run dated 2026-04-24, agentId a4b23ec40e80a3389.)

### Wiring checks: 14/14 PASS

| # | Check | Verdict |
|---|-------|---------|
| W1 | `grep -r is_admin app/` → 0 matches | ✅ |
| W2 | `User::where('role', 'admin')` in `NotificationRecipientResolver.php` (lines 64, 82) | ✅ |
| W3 | `config('rams.notifications.bcc')` consumed in all 5 jobs | ✅ |
| W4 | Migration `2026_04_19_000001_add_email_sent_columns_for_phase_09.php` ships 9 idempotency columns | ✅ |
| W5 | All 6 mailables `implements ShouldQueue` | ✅ |
| W6 | `DocumentArtifactStorage::readPath()` used by all 4 *ReadyMail attachments | ✅ |
| W7 | Dashboard route → `DashboardController::index` (routes/web.php:73, auth middleware) | ✅ |
| W8 | `DashboardController` constructor-injects `ProjectHealthService` | ✅ |
| W9 | Eager-loads cover all blade dependencies (owner, ramsDocuments, siteSurveys, activeInstallProgramme.tasks) | ✅ |
| W10 | `activeInstallProgramme` rendered in dashboard.blade.php:136 | ✅ |
| W11 | SurveyService refactored to use NotificationRecipientResolver — no `Project::with('user')`, no `is_admin` | ✅ |
| W12 | RamsReviewNeededMail dispatched after STATUS_AWAITING_REVIEW; `review_needed_email_sent_at` set BEFORE send | ✅ |
| W13 | All 4 `Build*Job::failed()` resolve admins via `resolveAdminRecipients()` | ✅ |
| W14 | All 4 failure paths use `substr($rawError, 0, 500)` for error_message truncation | ✅ |

### E2E flows: 6/6 COMPLETE

| # | Flow | Status |
|---|------|--------|
| 1 | Login → Dashboard → Health Grid + Status Filter + Programme Widget (DASH-01a..h) | ✅ COMPLETE |
| 2 | RAMS Generation Completes → Owner Email With Attachment (NOTF-01a/c/d/f, NOTF-05a/d/f/h) | ✅ COMPLETE |
| 3 | OmManual / Worksheet / CableSchedule completion (NOTF-01b/c/d/f) | ✅ COMPLETE |
| 4 | Build*Job retries exhausted → Admin Failure Email (NOTF-04a..c, NOTF-05b) | ✅ COMPLETE |
| 5 | ExtractRamsDraftJob → AWAITING_REVIEW → Review-Needed Email (NOTF-03a..c) | ✅ COMPLETE |
| 6 | Public Survey Submission → Owner Email (NOTF-02a, no regression) | ✅ COMPLETE |

---

## 3. Nyquist Compliance

| Phase | VALIDATION.md | nyquist_compliant | wave_0_complete | Action |
|-------|---------------|-------------------|-----------------|--------|
| 08-enterprise-dashboard | exists | **false** | false | `/gsd-validate-phase 08` (post-hoc) |
| 09-email-notifications | exists | true | **false** | `/gsd-validate-phase 09` (close wave_0) |

Both phases authored a Nyquist contract but neither closed it. Tests and verification evidence stand on their own (Phase 08: 10 unit + 4 feature tests; Phase 09: 24 notification feature tests + extended PublicSurveyControllerTest); the missing Nyquist closure is process debt.

---

## 4. Tech Debt by Phase

### Phase 08 — Enterprise Dashboard
- VALIDATION.md `nyquist_compliant: false` and `wave_0_complete: false` — Nyquist contract not closed.
- SUMMARY.md frontmatter `requirements-completed` field absent across 08-01/02 (REQUIREMENTS.md DASH-01* boxes ARE checked, so traceability is intact via the VERIFICATION.md `requirements_verified` list).

### Phase 09 — Email Notifications
- VALIDATION.md `wave_0_complete: false` — partial Nyquist closure.
- REQUIREMENTS.md checkboxes for **all 21 NOTF-* boxes** still `[ ]` despite VERIFICATION.md `requirement_coverage` marking every one `pass` — needs sync (option to tick inline before archive, same as v1.2 INST-* sync).
- **Outstanding human verification** (documented in 09-VERIFICATION.md `human_verification`):
  - **Dev smoke**: real RAMS/OM/Worksheet/Cable/Review/Failure generation against `MAIL_MAILER=log`; verify rendered bodies in `storage/logs/laravel.log` (subject, recipient, attachment metadata).
  - **NOTF-05g production Postmark cutover**: `MAIL_MAILER=postmark` + `POSTMARK_API_KEY` in production .env; SPF/DKIM/DMARC live on 21stcav.com; sender signature verified; first production send confirmed; BCC audit copy at `ops@21stcav.com`. Runbook at `.planning/phases/09-email-notifications/POSTMARK-OPS-CHECKLIST.md`.
- **REVIEW WR-01..WR-04** (4 polish items, non-blocking):
  - WR-01..WR-03: error-message truncation pattern inconsistencies between dispatch sites.
  - WR-04: narrow `failed()` try/catch gap in `BuildRamsDocumentJob::failed` and `BuildWorksheetJob::failed` where the DB write that sets `failed_email_sent_at` is outside the catch — could drop a failure alert on transient DB error during the timestamp write. Real but rare; tracked for a post-Phase-9 quick fix.

### Cross-phase / Hygiene
- **Scope reduction**: v1.1 ROADMAP overview lists "phases 08–11"; only 08 and 09 were executed. Phases 10 and 11 do not exist on disk and were not built (likely deferred to v1.2 or post-v1.2). **Recommend** documenting this scope reduction in MILESTONES.md when archiving.

---

## 5. Verdict

**Status:** ⚡ **tech_debt** — all 34 requirements satisfied at the code level; integration sound; operational + metadata debt should be reviewed before archive.

### Recommended path

1. **(Low effort)** Sync REQUIREMENTS.md NOTF-* checkboxes to `[x]` inline. Pure paperwork — implementation is verified.
2. **(Optional)** Run `/gsd-validate-phase 08 09` to close Nyquist gaps — both phases have VALIDATION.md drafts but neither completed `wave_0`.
3. **(Operational, can ship after archive)** Schedule the Postmark production cutover. Tracked separately via the runbook; doesn't block the v1.1 archive since the code is wire-complete and the runbook is the source of truth for the cutover.
4. **(Optional, nice to have)** Address WR-01..WR-04 polish items before next release. Real but non-blocking.
5. **(Then)** `/gsd-complete-milestone v1.1` to archive — making sure NOT to delete `.planning/REQUIREMENTS.md` afterward (v1.2 archive still needs it).

### Or accept as-is

Tech debt is documented here. Phase directories remain inspectable. `/gsd-complete-milestone v1.1` will archive the milestone with the unchecked boxes intact. Postmark cutover stays in the runbook regardless.

---

*Audit produced by /gsd-audit-milestone on 2026-04-24.*
*Integration verification: gsd-integration-checker agent (a4b23ec40e80a3389).*
