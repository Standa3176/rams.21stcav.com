---
phase: 24
plan: 7
type: execute
wave: 6
depends_on: ["24-06"]
files_modified:
  - app/Services/Drawings/StencilPromotionValidator.php
  - app/Http/Controllers/Admin/DeviceStencilController.php
  - routes/web.php
  - resources/views/admin/device-stencils/edit.blade.php
  - tests/Feature/Drawings/DeviceStencilPromotionTest.php
  - tests/Feature/Drawings/DeviceStencilCurationFlowTest.php
autonomous: true
requirements: [DRAW-53]

must_haves:
  truths:
    - "Promoting a stencil with zero ports, or a port missing a required field, or duplicate port_ids, is blocked server-side even if a hostile client POSTs directly to the promote route"
    - "A successful promote flips source to engineer-curated, clears needs_review, and writes a device_stencil_audits row"
    - "Promoting a stencil makes its new ports visible on every project already using that part_number, on next render, with zero per-project migration"
    - "Discard & Regenerate always succeeds regardless of the current port set, restoring the category-template default"
  artifacts:
    - path: "app/Services/Drawings/StencilPromotionValidator.php"
      provides: "D-04 two-tier hard-block / soft-warn evaluate() gate, callable from both HTTP and console contexts"
      exports: ["evaluate"]
    - path: "app/Http/Controllers/Admin/DeviceStencilController.php"
      provides: "promote()/discard() actions, both audit-logged"
  key_links:
    - from: "app/Http/Controllers/Admin/DeviceStencilController.php"
      to: "app/Services/Drawings/StencilPromotionValidator.php"
      via: "promote() re-runs the FULL hard-block check server-side regardless of client state"
      pattern: "StencilPromotionValidator"
    - from: "app/Http/Controllers/Admin/DeviceStencilController.php"
      to: "app/Models/DeviceStencilAudit.php"
      via: "promote()/discard() write an audit row with before/after port snapshots"
      pattern: "DeviceStencilAudit::create"
---

<objective>
Ship DRAW-53: the "Promote to Engineer-Curated" action, its D-04 hard-block/soft-warn validation gate (`StencilPromotionValidator`, callable outside HTTP too -- no direct precedent exists for this shape in the codebase, built fresh per RESEARCH.md's "No Analog Found" entry), the `device_stencil_audits` write (D-03), and "Discard & Regenerate". Also ships `DeviceStencilCurationFlowTest`, the end-to-end browse-edit-upload-promote integration test that only becomes possible once every prior UI plan (24-03 through this one) is in place.

Purpose: promotion is the moment a stub's coverage becomes real AND starts propagating cross-project -- D-04's asymmetry ("a promoted zero-port stencil hides the coverage gap it was created to expose") is why this gate is hard, not the soft "warn, never block" precedent Phase 22 set elsewhere.
Output: `StencilPromotionValidator`, `DeviceStencilController::promote()`/`discard()`, footer button wiring, audit trail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md
@.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-UI-SPEC.md
@app/Services/Drawings/DeviceStencilCacheService.php
@app/Models/DeviceStencil.php
@app/Models/DeviceStencilAudit.php
</context>

<interfaces>
D-04 (locked) hard-block conditions: zero ports; any port missing label/connector_type/signal_type/direction; duplicate port_id within the stencil. Soft-warn (non-blocking) conditions: no manufacturer logo (neither logo_svg nor logo_path); signal_type left unclassified; missing x_pct/y_pct.

From Plan 24-01: app/Models/DeviceStencilAudit.php -- ACTION_PROMOTE, ACTION_EDIT, ACTION_DISCARD_REGENERATE constants; fillable = [device_stencil_id, user_id, action, before_snapshot, after_snapshot], both snapshot columns cast to array.

From 24-UI-SPEC.md Copywriting Contract (exact strings, do not paraphrase):
- Promote blocked: "Blocked: this stencil has zero ports." / "Blocked: {N} ports are missing a signal type." / "Blocked: duplicate port ID \"{id}\"."
- Promote warning: "This stencil has no manufacturer logo -- promotion will proceed without one." / "{N} port has an unclassified signal type."
- Promote success (flash): "{Manufacturer} {Model} promoted to Engineer-Curated. It now renders with full ports on every project using part number {part_number}."
- Discard confirm: "Discard all edits and regenerate this stencil from its category template?"
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StencilPromotionValidator (D-04 hard/soft gate)</name>
  <files>app/Services/Drawings/StencilPromotionValidator.php, tests/Feature/Drawings/DeviceStencilPromotionTest.php</files>
  <read_first>
    - app/Models/DeviceStencil.php (ports() relation, logo_svg/logo_path columns from Plan 24-01)
    - app/Models/DevicePort.php (label/connector_type/signal_type/direction/port_id columns)
    - .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md D-04 (exact hard-block vs soft-warn condition list)
    - .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-UI-SPEC.md Copywriting Contract table (exact enumerated-reason strings, grouped per field-type count, not one line per port)
  </read_first>
  <behavior>
    - Test 1: a stencil with zero ports evaluates to blocking = ["This stencil has zero ports."].
    - Test 2: a stencil with 2 ports both missing signal_type evaluates to a SINGLE grouped blocking reason mentioning "2 ports" (not 2 separate lines) -- UI-SPEC's explicit grouping requirement.
    - Test 3: a stencil with two ports sharing the same port_id evaluates to blocking containing the exact duplicate-port-ID string with the offending ID interpolated.
    - Test 4: a stencil with valid required fields but no logo and one signal_type=unclassified port evaluates to blocking=[] and warnings containing both the no-logo and unclassified-signal-type strings -- promotable despite warnings.
    - Test 5: a fully valid, fully classified, logo-present stencil evaluates to blocking=[] and warnings=[].
  </behavior>
  <action>
    Create app/Services/Drawings/StencilPromotionValidator.php with public function evaluate(DeviceStencil $stencil): array returning ['blocking' => string[], 'warnings' => string[]]. Load $ports = $stencil->ports (already ordered via the model's ports() relation). Hard-block checks: if $ports->isEmpty(), add "This stencil has zero ports."; group missing-field counts across ALL ports for each of label/connector_type/signal_type/direction ($ports->filter(fn ($p) => blank($p->{$field}))->count()) and for each field with count greater than 0 add a grouped line using a human field-label map (label -> "label", connector_type -> "connector type", signal_type -> "signal type", direction -> "direction"), pluralising "port"/"ports" and "is"/"are" correctly; for each duplicate value returned by $ports->pluck('port_id')->duplicates(), add a "Blocked: duplicate port ID" line with the offending ID interpolated. Soft-warn checks (only evaluated when $ports->isNotEmpty() -- an already-blocked zero-port stencil does not also need a logo warning): if $stencil->logo_svg is null AND $stencil->logo_path is null, add the no-logo warning line; count ports with signal_type === 'unclassified', if greater than 0 add the grouped unclassified-signal-type warning line; count ports where BOTH x_pct and y_pct are null, if greater than 0 add a missing-position-hints warning line. This class has NO constructor dependencies and is directly instantiable via app(StencilPromotionValidator::class) from both DeviceStencilController::promote() (Task 2) and any future console context -- this is why it is a plain service class, not a FormRequest (RESEARCH.md's explicit "No Analog Found" entry documents this reasoning).
  </action>
  <acceptance_criteria>All 5 behaviour tests pass, using the EXACT copy strings from 24-UI-SPEC.md's Copywriting Contract table (byte-for-byte, not paraphrased).</acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=DeviceStencilPromotionTest</automated>
  </verify>
  <done>StencilPromotionValidator correctly distinguishes hard-block from soft-warn per D-04, with grouped (not per-port) messaging matching UI-SPEC verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: promote()/discard() actions + routes + footer wiring + audit trail</name>
  <files>app/Http/Controllers/Admin/DeviceStencilController.php, routes/web.php, resources/views/admin/device-stencils/edit.blade.php, tests/Feature/Drawings/DeviceStencilPromotionTest.php</files>
  <read_first>
    - app/Http/Controllers/Admin/DeviceCableRuleController.php lines 86-99 (destroy() -- the nearest structural precedent for a state-changing POST with Log::info + flash redirect, adapted for promote/discard)
    - app/Services/Drawings/DeviceStencilCacheService.php lines 62-96 (resolveForPartNumber -- criterion 4's propagation proof: the SAME part_number lookup on a re-render after promote must return the updated row, zero extra code)
    - resources/views/admin/device-cable-rules/index.blade.php lines 228-234 (data-confirm destructive-action pattern to copy verbatim for Discard and Regenerate)
    - resources/views/admin/device-stencils/edit.blade.php (the file Plans 24-05/24-06 built -- the footer placeholder comment left by Plan 24-05 is where these two buttons go)
  </read_first>
  <action>
    Add public function promote(DeviceStencil $deviceStencil, StencilPromotionValidator $validator): RedirectResponse to DeviceStencilController. Re-run app(StencilPromotionValidator::class)->evaluate($deviceStencil) SERVER-SIDE regardless of what the client's disabled-button state implied (Security Domain threat: promote-bypass via direct POST) -- if blocking is non-empty, redirect back with a validation-style error listing every blocking reason, never proceed. On success, inside a DB::transaction: capture $before = $deviceStencil->ports->toArray() as the before-snapshot; $deviceStencil->update(['source' => DeviceStencil::SOURCE_ENGINEER_CURATED, 'needs_review' => false]); DeviceStencilAudit::create(['device_stencil_id' => $deviceStencil->id, 'user_id' => auth()->id(), 'action' => DeviceStencilAudit::ACTION_PROMOTE, 'before_snapshot' => $before, 'after_snapshot' => $deviceStencil->fresh()->ports->toArray()]); Log::info('Admin: device stencil promoted', ['stencil_id' => $deviceStencil->id, 'admin_id' => auth()->id()]); redirect()->route('admin.device-stencils.index')->with('success', the exact UI-SPEC success copy with manufacturer/model/part_number interpolated). Add public function discard(DeviceStencil $deviceStencil, CategoryPortTemplateResolver $resolver, AutoGenericStencilGenerator $generator): RedirectResponse -- re-resolves the category template from the stencil's own name/part_number (same call shape as Plan 24-08's reapply-templates), replaces device_ports (delete + bulk-insert) and regenerates mxgraph_xml exactly like update() does in Plan 24-05, writes a DeviceStencilAudit row with action=ACTION_DISCARD_REGENERATE and before/after snapshots, and redirects back to the edit screen with a confirmation flash -- Discard always succeeds (no validator gate) because it is explicitly a reset-to-known-good action, not a promotion. In routes/web.php add Route::post('/admin/device-stencils/{deviceStencil}/promote', ...)->name('admin.device-stencils.promote') and Route::post('/admin/device-stencils/{deviceStencil}/discard', ...)->name('admin.device-stencils.discard') inside the admin group. In edit.blade.php, replace Plan 24-05's footer placeholder comment with: a "Promote to Engineer-Curated" button (.btn-primary) whose disabled attribute is driven client-side by the SAME Alpine ports array's live blocking-condition check (mirroring StencilPromotionValidator's rules in JS purely for UX -- the disabled state is NEVER the enforcement boundary, only the server-side re-check in promote() is) plus a one-line reason above it enumerating the actual blocking reasons; a "Discard & Regenerate" button (.btn-danger-outline, always enabled) wrapped in its own form with the exact data-confirm/data-confirm-label/data-confirm-danger attributes copied verbatim from the device-cable-rules delete-button pattern, using the exact UI-SPEC confirm copy.
  </action>
  <acceptance_criteria>
    - Directly POSTing to the promote route for a zero-port stencil (bypassing any client-side disabled state) returns without flipping source -- server-side re-validation proven, not just client-side.
    - A successful promote flips source to engineer-curated, sets needs_review to false, and creates exactly one device_stencil_audits row with action=promote and non-empty before/after snapshots.
    - Criterion 4 propagation: render project A's devices (via DeviceStencilCacheService::resolveForPartNumber against the same part_number) BEFORE promote returns the stub; call it again AFTER promote returns the updated engineer-curated row with the new ports -- zero extra code, same cache lookup.
    - Discard & Regenerate succeeds even when the current port set already fails the promote hard-gate (it never runs the validator).
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=DeviceStencilPromotionTest</automated>
  </verify>
  <done>Promote hard-gates server-side, writes an audit row, propagates cross-project via the existing cache lookup; Discard always resets to the template default with its own audit row.</done>
</task>

<task type="auto">
  <name>Task 3: DeviceStencilCurationFlowTest (criterion 3, full end-to-end)</name>
  <files>tests/Feature/Drawings/DeviceStencilCurationFlowTest.php</files>
  <read_first>
    - tests/Feature/Drawings/ProjectDevicesWithStencilsTest.php (fixture-builder pattern to reuse for creating an auto-generated stub to curate)
    - .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md Success Criterion 3 (the exact flow this test proves)
  </read_first>
  <action>
    Create tests/Feature/Drawings/DeviceStencilCurationFlowTest.php exercising the full browse -> edit -> upload logo -> promote loop against the real routes (not unit-level service calls): (1) seed one auto-generated, needs_review stencil with zero ports; (2) GET the list route filtered to needs_review=1 and assert the stencil appears; (3) GET the edit route and assert the port table is empty and Promote is effectively blocked (assert the response contains the "zero ports" blocking copy); (4) PUT a valid ports array to the update route; (5) POST a small valid PNG to the upload-logo route; (6) POST to the promote route and assert a 200-family redirect with the success flash; (7) GET the list route again (no filter) and assert the stencil now shows the engineer-curated badge and no needs-review badge.
  </action>
  <acceptance_criteria>The full 7-step flow passes as a single test method exercising every route this phase's curation UI plans (24-03 through this plan) shipped, end to end.</acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=DeviceStencilCurationFlowTest</automated>
  </verify>
  <done>End-to-end curation flow proven via real HTTP routes, not mocked service calls.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin POST to promote/discard | Authenticated admin session; promote's disabled client-side button state is UX only -- the real boundary is the server-side StencilPromotionValidator re-check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-24-17 | Tampering | Promote-bypass via a direct POST that skips the disabled client-side button | mitigate | `promote()` re-runs `StencilPromotionValidator::evaluate()` server-side unconditionally on every request, regardless of what the client sent -- proven by Task 2's bypass test |
| T-24-18 | Repudiation | No record of who promoted/discarded a stencil and what changed | mitigate | Every promote/discard writes a `device_stencil_audits` row with `user_id`, `action`, and before/after port snapshots (D-03) |
</threat_model>

<verification>
`php artisan test --filter=DeviceStencilPromotionTest` and `--filter=DeviceStencilCurationFlowTest` green. Lint clean on every touched file. Full suite (`php artisan test`) green as the phase-close gate.
</verification>

<success_criteria>
Promotion is server-side hard-gated per D-04, audit-logged per D-03, and propagates cross-project with zero extra code via the existing cache lookup; Discard always resets cleanly; the full curation flow works end to end through real HTTP routes.
</success_criteria>

<output>
Create `.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-07-SUMMARY.md` when done, ending with a "🚨 Files to upload to live" section listing every file in `files_modified`. No migration in this plan. This is the last curation-UI plan -- the SUMMARY should note that Plans 24-01 through 24-07 must all be live (including 24-01's migration) before Plan 24-09's engineer-driven bounded fill can begin.
</output>
