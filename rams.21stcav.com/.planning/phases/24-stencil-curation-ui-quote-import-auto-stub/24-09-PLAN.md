---
phase: 24
plan: 9
type: execute
wave: 7
depends_on: ["24-07", "24-08"]
files_modified: []
autonomous: false
requirements: []
user_setup: []

must_haves:
  truths:
    - "The 10 highest quote-volume hardware part_numbers have full Tier 1 (real port) coverage at phase close"
    - "Each promoted device renders correctly in a real Phase 23 drawing, verified against a known-good baseline"
  artifacts: []
  key_links: []
---

<objective>
Bounded delivery task, not a code plan: use `stencils:coverage-report` (Plan 24-08) to identify the top-10 highest quote-volume hardware part_numbers, then curate each one to full Tier 1 coverage through the `/admin/device-stencils` UI (Plans 24-03 through 24-07). This is engineer labour, not engineering -- CONTEXT.md D-13 explicitly excludes it from carrying a requirement ID; it fulfils ROADMAP's unnumbered Success Criterion 5 ("bounded delivery target rather than a 91-device sprint").

Purpose: proves the tooling built in this phase actually closes real coverage gaps, without committing to curating all ~92 existing stubs (explicitly out of scope, per 24-CONTEXT.md's "NOT in scope: Bulk 91-device curation sprint").
Output: 10 promoted, engineer-curated `device_stencils` rows; before/after `stencils:coverage-report` output as the audit trail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md
@.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Curate the top-10 highest-volume hardware part_numbers to Tier 1 coverage</name>
  <files>none -- this task edits data via the live admin UI, not source files</files>
  <read_first>
    - .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-VALIDATION.md (Manual-Only Verifications table -- confirms Criterion 5 is explicitly manual-only, no automated assertion should be fabricated)
    - app/Console/Commands/StencilsCoverageReportCommand.php (Plan 24-08 -- the exact command producing the before/after ranking)
  </read_first>
  <what-built>Everything needed to run this: `stencils:coverage-report` (Plan 24-08) and the full `/admin/device-stencils` curation UI (Plans 24-03 through 24-07) are live.</what-built>
  <how-to-verify>
    1. Run `php artisan stencils:coverage-report --limit=10` on the live server and capture the output as the "before" snapshot.
    2. For each of the 10 reported part_numbers still showing Tier 1: open `/admin/device-stencils?q={part_number}`, click Edit, add the correct real ports for that device (per its actual datasheet/known hardware -- this is genuine engineering judgement, not automatable), upload a manufacturer logo if available, and click "Promote to Engineer-Curated".
    3. For each promoted device, open an existing real project's drawing that uses it (Phase 23 renderer) and visually confirm it renders correctly with populated port rails, comparing against a known-good baseline drawing for that device class.
    4. Re-run `php artisan stencils:coverage-report --limit=10` and capture the "after" snapshot -- confirm all 10 now show Tier 2 (engineer-curated).
  </how-to-verify>
  <action>Curate the top-10 highest quote-volume hardware part_numbers (per `stencils:coverage-report`) through the live `/admin/device-stencils` edit screen: real ports, manufacturer logo where available, then Promote to Engineer-Curated. See the how-to-verify steps above for the exact sequence.</action>
  <acceptance_criteria>
    - `stencils:coverage-report --limit=10`'s "after" output shows all 10 part_numbers as Tier 2 (engineer-curated), or the SUMMARY explicitly lists which remain Tier 1 and why (e.g. datasheet unavailable).
    - Each promoted device's drawing renders correctly (populated port rails, no crash) in at least one real project using it.
  </acceptance_criteria>
  <resume-signal>Paste both coverage-report outputs (before/after) and confirm all 10 devices visually render correctly in a real project drawing, or list which remain and why (e.g. datasheet unavailable).</resume-signal>
  <verify>
    <manual>Compare before/after `php artisan stencils:coverage-report --limit=10` output; open each promoted device's drawing in a real project and visually confirm port rails render.</manual>
  </verify>
  <done>All 10 (or as many as datasheets allow, with the shortfall documented) top-volume hardware part_numbers show Tier 2 coverage, each visually verified in a real project drawing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new -- this plan performs zero code changes, exercising only the already-threat-modelled routes and commands shipped in Plans 24-01 through 24-08.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-24-19 | N/A | This plan is delivery labour, not code | accept | No new attack surface introduced; inherits every mitigation already shipped by Plans 24-01 through 24-08 |
</threat_model>

<verification>
Manual only, per 24-VALIDATION.md's explicit "Criterion 5 is manual-only" classification -- engineer labour with `stencils:coverage-report` before/after output as the audit trail. Do not fabricate an automated assertion for this criterion.
</verification>

<success_criteria>
Top-10 highest quote-volume hardware part_numbers show Tier 2 (engineer-curated) coverage in `stencils:coverage-report`'s output at phase close, each visually verified against a real project drawing.
</success_criteria>

<output>
Create `.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-09-SUMMARY.md` when done, including the before/after `stencils:coverage-report` output as the audit trail. No files to upload -- this plan is pure data curation via the already-deployed admin UI.
</output>
