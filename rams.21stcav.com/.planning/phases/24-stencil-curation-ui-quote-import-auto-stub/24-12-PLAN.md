---
phase: 24
plan: 12
type: execute
wave: 2
depends_on: ["24-10", "24-11"]
files_modified:
  - .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md
autonomous: true
gap_closure: true
requirements: []

must_haves:
  truths:
    - "CONTEXT.md D-11 no longer states the false claim that the 91 stubs are source=auto-generated, and documents the corrected needs_review-based eligibility predicate that actually reaches them"
    - "CONTEXT.md D-17 documents the corrected ports()->exists() guard predicate, so a future reader understands why the guard no longer fires on all 96 engineer-curated stencils"
  artifacts:
    - path: ".planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md"
      provides: "Accurate historical record of D-11 and D-17, correction blocks appended in the same amendment style already used for D-17's original 'Status: AMENDED' note"
  key_links: []
---

<objective>
Close the documentation half of UAT Gaps 1 and 2. `24-CONTEXT.md` is the locked decision record for this phase and downstream agents (future planners, other Claude sessions, human engineers) will read D-11 and D-17 as ground truth. D-11 currently states a factual claim UAT proved false ("[the 91 stubs] are all source = auto-generated"); D-17 documents a guard trigger that Gap 2 proved mis-chosen. Plans 24-10 and 24-11 already fixed the code and tests — this plan corrects the record to match, using the same in-place amendment pattern D-17 already established for itself ("Status: ✅ AMENDED 2026-08-14...").

Do NOT delete or rewrite the original D-11/D-17 text — append correction blocks, exactly as D-17's own "Status: AMENDED" note was appended rather than replacing the decision's original reasoning. The original text remains valuable as a record of what was believed and why the correction was necessary.

Purpose: keeps the phase's locked-decision record trustworthy for anyone who reads it after this gap closure — including a future `stencils:reapply-templates` or curation-UI change that might otherwise re-derive the same false `source`-as-proxy assumption.
Output: `24-CONTEXT.md` with two new correction blocks, one under D-11 and one under D-17.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-UAT.md
@.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct D-11 — the false "91 stubs are auto-generated" premise</name>
  <files>.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md</files>
  <read_first>
    - .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md lines 140-143 (D-11's current full text — already in context)
    - .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-UAT.md "Gap 1" section (lines 104-123) — the exact empirical finding and the fix this correction must describe
    - .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-10-PLAN.md (this gap closure plan's own objective — describes the corrected predicate to cite here)
  </read_first>
  <action>
    Immediately after D-11's existing paragraph ("They are all `source = auto-generated`... Do not build a second one-shot backfill command.") and before the `**D-12` heading that follows it, insert a new correction block:

    A bolded line `**Correction (2026-08-14, UAT gap closure — Plan 24-10):**` followed by a paragraph stating: UAT proved this premise false. All 91 zero-port stubs are actually `source = engineer-curated` (Phase 21 D-05 deliberately seeded the promoted v1.3 + top-50-gap entries this way, since they had hand-derived manufacturer/model/XML even without ports), carrying `metadata.needs_phase_24_curation = true`. Zero stencils in the live-shaped catalogue have `source = auto-generated`. `needs_review` — not `source` — is the column that actually and correctly marks these 91 rows (the Plan 24-01 migration backfill keyed off the metadata flag, not `source`, and was always correct). `stencils:reapply-templates`'s eligibility was corrected from `source = auto-generated AND whereDoesntHave('audits')` to `needs_review = true AND whereDoesntHave('audits')` (see `app/Console/Commands/StencilsReapplyTemplatesCommand.php`) — this reaches the real 91 stubs while `whereDoesntHave('audits')`, now the sole `source`-independent safety clause, continues to exclude anything an engineer has promoted/edited/discard-regenerated. The original conclusion — no separate one-shot backfill command needed — still holds; the existing command now correctly reaches all 91 via the corrected predicate rather than needing a second mechanism.
  </action>
  <acceptance_criteria>
    - `grep -c "Correction (2026-08-14, UAT gap closure — Plan 24-10)" .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md` returns 1.
    - D-11's original paragraph text is still present unmodified (grep confirms `"already qualify under D-08's re-apply rule"` still returns 1 match).
    - The correction block appears strictly between the end of D-11's original text and the start of `**D-12` (visually confirm via Read after edit).
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Correction (2026-08-14, UAT gap closure" ".planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md"</automated>
  </verify>
  <done>D-11 carries an appended correction block describing the real source/needs_review shape and the fixed eligibility predicate, without deleting the original decision text.</done>
</task>

<task type="auto">
  <name>Task 2: Correct D-17 — the mis-chosen guard trigger</name>
  <files>.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md</files>
  <read_first>
    - .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md D-17 (its full text, including the "Status: ✅ AMENDED" and "Resolved treatment"/"Known and intended consequence" paragraphs — already in context, ends right before `</decisions>`)
    - .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-UAT.md "Gap 2" section — the exact empirical finding (5 of 96 real, 91 bare stubs) and the fix this correction must describe
    - .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-11-PLAN.md (this gap closure plan's own objective — describes the corrected predicate to cite here)
  </read_first>
  <action>
    Immediately after D-17's final existing paragraph ("Known and intended consequence: any edit writes an audit row... automated re-templating must never reach it again.") and before the `</decisions>` closing tag, insert a new correction block:

    A bolded line `**Correction (2026-08-14, UAT gap closure — Plan 24-11):**` followed by a paragraph stating: UAT found the guard as shipped fired on 96 of 96 stencils, because only 5 of the 96 `engineer-curated` stencils (ClickShare Bar Pro, Neat Bar Pro, Netgear GS312TP, Samsung QM65C-T, Sennheiser TCC2) actually carry hand-built mxGraph artwork — the other 91 share that `source` value but are bare zero-port stubs with nothing for the guard to protect. This directly violated this decision's own stated requirement that the ordinary stub-curation path stay a single-click save with zero added friction. The guard's trigger was corrected from `source === SOURCE_ENGINEER_CURATED` (alone) to `source === SOURCE_ENGINEER_CURATED AND $deviceStencil->ports()->exists()` — a stencil with no existing ports has no artwork to protect, so the confirm requirement no longer fires on it (see `app/Http/Controllers/Admin/DeviceStencilController.php::update()`). This is UAT's candidate fix (a): the smallest change that directly matches this decision's intent. Note the resulting behavior for one of the 91 stubs: the FIRST save that adds ports is zero-friction (no ports exist yet, guard does not fire); a SECOND edit of that same stencil after it has ports DOES trigger the guard, exactly as it would for genuinely hand-curated artwork — because by then there is saved content worth protecting.
  </action>
  <acceptance_criteria>
    - `grep -c "Correction (2026-08-14, UAT gap closure — Plan 24-11)" .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md` returns 1.
    - D-17's original "Resolved treatment" and "Known and intended consequence" paragraphs are still present unmodified (grep confirms `"confirm-to-proceed, not a hard block"` still returns 1 match).
    - The correction block appears strictly between the end of D-17's original text and `</decisions>` (visually confirm via Read after edit).
    - Combined with Task 1: `grep -c "Correction (2026-08-14, UAT gap closure" .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md` now returns 2.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "Correction (2026-08-14, UAT gap closure" ".planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md"</automated>
  </verify>
  <done>D-17 carries an appended correction block describing the real 5-vs-91 artwork split and the fixed ports()->exists() guard predicate, without deleting the original decision text; both D-11 and D-17 correction blocks are present (grep count 2).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local repo edit | `.planning/phases/.../24-CONTEXT.md` is a planning document, not runtime code — no execution surface, no user input, no HTTP exposure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-24-12-01 | Tampering | `.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md` | accept | Documentation-only change with no runtime or security impact; git history preserves the prior (now-superseded) text, and this plan explicitly appends rather than deletes, so the amendment is auditable in the file itself as well as in git |
</threat_model>

<verification>
`grep -c "Correction (2026-08-14, UAT gap closure" 24-CONTEXT.md` returns 2 after both tasks. Original D-11/D-17 text still present and unmodified (spot-checked via the acceptance criteria greps above).
</verification>

<success_criteria>
CONTEXT.md's D-11 and D-17 accurately describe the real catalogue shape and the corrected predicates, in the same in-place amendment style D-17 already used for its own prior correction — no locked decision text is deleted, only corrected in place.
</success_criteria>

<output>
Create `.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-12-SUMMARY.md` when done, ending with a "🚨 Files to upload to live" section stating explicitly that NO files from this plan need uploading to live — `.planning/` is a local planning artifact, not part of the deployed application. No migration in this plan.
</output>
