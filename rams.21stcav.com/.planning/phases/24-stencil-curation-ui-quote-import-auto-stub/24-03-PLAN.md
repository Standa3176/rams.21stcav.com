---
phase: 24
plan: 3
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - routes/web.php
  - resources/views/layouts/navigation.blade.php
  - app/Http/Controllers/Admin/DeviceStencilController.php
  - resources/views/admin/device-stencils/index.blade.php
  - tests/Feature/Drawings/DeviceStencilListTest.php
autonomous: true
requirements: [DRAW-50]

must_haves:
  truths:
    - "Admin can open /admin/device-stencils and see every stencil in the system"
    - "Admin can filter the list by source, needs_review, and manufacturer, and search by part_number"
    - "Non-admin users cannot reach the route (inherits the existing admin middleware gate)"
  artifacts:
    - path: "app/Http/Controllers/Admin/DeviceStencilController.php"
      provides: "index() action — filtered, paginated stencil list"
    - path: "resources/views/admin/device-stencils/index.blade.php"
      provides: "List view: filter row, data table, badges, two distinct empty states"
  key_links:
    - from: "routes/web.php"
      to: "app/Http/Controllers/Admin/DeviceStencilController.php"
      via: "admin.device-stencils.index route inside the existing admin middleware group"
      pattern: "admin.device-stencils.index"
    - from: "resources/views/layouts/navigation.blade.php"
      to: "admin.device-stencils.index"
      via: "new nav entry"
      pattern: "admin.device-stencils"
---

<objective>
Ship DRAW-50: the `/admin/device-stencils` list view — filterable by `source` / `needs_review` / `manufacturer`, searchable by `part_number`. This is the entry point of the curation UI; Plans 24-04 through 24-07 add edit/preview/logo/promote actions to the SAME controller file in later waves (sequential, since they share `DeviceStencilController.php`).

Purpose: gives the admin a queue of stubs awaiting curation (D-09's stated goal — "the review queue populates before anyone opens a drawing").
Output: `admin.device-stencils.index` route, controller, Blade view, nav entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md
@.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-UI-SPEC.md
@.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-PATTERNS.md
@app/Http/Controllers/Admin/DeviceController.php
@resources/views/admin/devices/index.blade.php
@resources/views/admin/device-cable-rules/index.blade.php
@app/Models/DeviceStencil.php
</context>

<interfaces>
From app/Models/DeviceStencil.php (existing): `SOURCE_AUTO_GENERATED`, `SOURCE_ENGINEER_CURATED`, `SOURCE_AI_EXTRACTED` constants; `needs_review` boolean (Plan 24-01); `ports(): HasMany` relation for `withCount('ports')`.

D-14 (locked): route names `admin.device-stencils.index` / `.edit` / `.update` / `.promote` / `.preview` — no bare `Route::resource`, no `create`/`store`/`destroy` (stencils are only ever created by `firstOrCreate`, never by hand, per D-14/24-CONTEXT.md).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Routes + nav entry + DeviceStencilController::index()</name>
  <files>routes/web.php, resources/views/layouts/navigation.blade.php, app/Http/Controllers/Admin/DeviceStencilController.php</files>
  <read_first>
    - routes/web.php lines 250-304 (admin middleware group opens line 251; device-cable-rules block shows the preview-before-resource-route ordering comment at lines 295-299 — no bare resource here since D-14 lists explicit named actions, but keep index registered cleanly alongside admin.devices.index at line 283)
    - resources/views/layouts/navigation.blade.php lines 380-410 (admin.devices.index and admin.device-cable-rules.index nav entries — exact svg/class pattern to mirror)
    - app/Http/Controllers/Admin/DeviceController.php lines 33-75 (index() filter-row backing logic: conditional ->where(), q-search across multiple columns, paginate(15)->appends($request->query()))
    - app/Models/DeviceStencil.php (SOURCE_* constants, ports() relation)
  </read_first>
  <action>
    In `routes/web.php`, add `use App\Http\Controllers\Admin\DeviceStencilController;` alongside the other Admin controller imports at the top of the file. Inside the existing `Route::middleware('admin')->group()` block (opens line 251), add `Route::get('/admin/device-stencils', [DeviceStencilController::class, 'index'])->name('admin.device-stencils.index');` near the `admin/devices` block (line 283-288). In `resources/views/layouts/navigation.blade.php`, add a new `<a>` entry immediately after the existing `admin.device-cable-rules.index` entry (after its closing `</a>` at line 409): `href="{{ route('admin.device-stencils.index') }}"`, `class="tnav-admin-item {{ request()->routeIs('admin.device-stencils.*') ? 'active' : '' }}"`, a fresh hand-rolled `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">` (do not import an icon library — draw a simple card/rectangle-with-ports glyph, visually distinct from the existing Devices icon), label text "Stencils". Create `app/Http/Controllers/Admin/DeviceStencilController.php` with `public function index(Request $request): View`: base query `DeviceStencil::query()->withCount('ports')`; conditional `->where('source', $request->input('source'))` only when the value is one of `DeviceStencil::SOURCE_AUTO_GENERATED`/`SOURCE_ENGINEER_CURATED`/`SOURCE_AI_EXTRACTED` (reject anything else silently — no SQL injection surface, but also no crash on a garbage query string); conditional `->where('needs_review', $request->boolean('needs_review'))` only when the `needs_review` query key is actually present in the request (index-backed per D-10, never `LIKE`); conditional `->where('manufacturer', $request->input('manufacturer'))` when non-empty; conditional `->where('part_number', 'like', '%' . trim((string) $request->input('q', '')) . '%')` when the trimmed `q` term is non-empty; `->orderBy('updated_at', 'desc')->paginate(15)->appends($request->query())`. Also fetch `$manufacturers = DeviceStencil::query()->whereNotNull('manufacturer')->distinct()->orderBy('manufacturer')->pluck('manufacturer');` for the filter dropdown. Return `view('admin.device-stencils.index', compact('stencils', 'manufacturers', ...$request->only(['source', 'needs_review', 'manufacturer', 'q'])))`.
  </action>
  <acceptance_criteria>
    - `route('admin.device-stencils.index')` resolves without error.
    - `GET /admin/device-stencils` as a non-admin user is blocked by the existing `admin` middleware (no new auth code written).
    - `GET /admin/device-stencils?source=auto-generated&needs_review=1` returns only rows matching both filters.
    - `GET /admin/device-stencils?q=neat` matches on `part_number` substring only (not manufacturer/model).
  </acceptance_criteria>
  <verify>
    <automated>php artisan route:list --name=admin.device-stencils.index</automated>
  </verify>
  <done>Route registered inside the admin group, controller filters correctly, nav entry present and highlights active state.</done>
</task>

<task type="auto">
  <name>Task 2: index.blade.php list view + feature test</name>
  <files>resources/views/admin/device-stencils/index.blade.php, tests/Feature/Drawings/DeviceStencilListTest.php</files>
  <read_first>
    - resources/views/admin/devices/index.blade.php lines 62-130 (.dv-filter-row CSS block at 62-91 + form markup at 112-130 — copy verbatim under .stc-filter-row per UI-SPEC's explicit "reuse-as-is, 12px 14px padding, do not correct to an 8pt multiple" instruction)
    - resources/views/admin/device-cable-rules/index.blade.php lines 214-244 (badge markup pattern + empty-state <tr> pattern to adapt)
    - .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-UI-SPEC.md Component Inventory points 1-2 and Copywriting Contract table (exact filter fields, badge class mapping, exact empty-state copy strings)
  </read_first>
  <action>
    Create `resources/views/admin/device-stencils/index.blade.php`. Filter row (`.stc-filter-row`, CSS copied verbatim from `.dv-filter-row`'s `12px 14px` padding under the new scoped class name): Search text input (`name="q"`, mono font per UI-SPEC's part_number treatment, placeholder "part number…"); Source `<select name="source">` with options All / Auto-generated / Engineer-curated / AI-extracted (values matching `DeviceStencil::SOURCE_*`); Needs review `<select name="needs_review">` with options All / Yes / No; Manufacturer `<select name="manufacturer">` populated from `$manufacturers`; `Apply` button (`.btn-teal.btn-sm`) and `Clear` link (`.btn-outline.btn-sm`, rendered only when any filter is active) — byte-identical shape to `admin/devices/index.blade.php:112-130`, both labels reused verbatim (not new copy). `.data-table` (card-flush wrapper matching `admin/device-cable-rules` chrome) with columns: Part Number (mono, `--font-mono`); Manufacturer/Model (bold `--ink-900` + part_number sub-line, mirrors `.dv-equipment`/`.dv-partno`); Source badge (`.badge-grey` "Auto-generated" / `.badge-green` "Engineer-curated" / `.badge-blue` "AI-extracted"); Needs-review badge (`.badge-yellow` "Needs review", rendered ONLY when `$stencil->needs_review` is true — absence IS the "no" state, do not render a muted "No" pill); Ports (`$stencil->ports_count`, `font-variant-numeric: tabular-nums`); Logo (28px square thumbnail via `asset($stencil->logo_path)` with 1px `--ink-200` border + `--radius-sm` when `logo_path` is set, else em-dash — will always show em-dash until Plan 24-06 ships upload, which is correct); Updated (relative date via `$stencil->updated_at->diffForHumans()`); Actions (`Edit` link `.btn-outline.btn-sm` to `route('admin.device-stencils.edit', $stencil)` — safe to reference now even though the route ships in Plan 24-04, since Blade `route()` calls resolve at request time). Header row uses 13px/`--fs-small`/weight 600 uppercase tracked text (NOT `--fs-micro`, NOT a hardcoded 10/11px value). Two distinct empty-state blocks per UI-SPEC's Copywriting Contract table, exact copy: unfiltered — heading "No stencils awaiting curation." body "Every auto-generated stub has been promoted. New stubs appear here automatically when a quote import references an uncatalogued part number."; filtered (any filter active, zero matches) — "No stencils match your filters." plus a "Clear filters" link. No "Create one" link anywhere (D-14: no create screen).
  </action>
  <acceptance_criteria>
    - Feature test creates 3 `DeviceStencil` fixtures (1 auto-generated+needs_review, 1 engineer-curated, 1 with a distinct manufacturer) and asserts `GET /admin/device-stencils` returns 200 and all 3 part_numbers appear in the response body.
    - `GET /admin/device-stencils?source=engineer-curated` response contains only the engineer-curated fixture's part_number.
    - With zero stencils in the DB, the response contains the exact unfiltered empty-state heading text.
    - With stencils present but `?q=nomatch123`, the response contains the exact filtered empty-state text and a "Clear filters" link.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=DeviceStencilListTest</automated>
  </verify>
  <done>tests/Feature/Drawings/DeviceStencilListTest.php passes; list view renders filters, badges, thumbnails/em-dash, and both empty states per the UI-SPEC copy contract verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin filter query string -> DB query | `source`/`needs_review`/`manufacturer`/`q` are user-controlled GET params from an authenticated admin session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-24-05 | Tampering | `DeviceStencilController::index()` filter query | mitigate | `source` is allow-listed against `DeviceStencil::SOURCE_*` before use in a `->where()`; `needs_review` cast via `$request->boolean()`; `q`/`manufacturer` bound as parameterised Eloquent query values (never raw SQL string concatenation) |
| T-24-06 | Elevation of Privilege | Reaching `/admin/device-stencils` without admin rights | accept | Route sits inside the pre-existing `Route::middleware('admin')->group()` block — no new auth surface introduced by this plan |
</threat_model>

<verification>
`php artisan test --filter=DeviceStencilListTest` green. Lint clean on every touched file. `php artisan route:list --name=admin.device-stencils` confirms the route registered inside the admin group.
</verification>

<success_criteria>
Admin can browse, filter (source/needs_review/manufacturer), and search (part_number) the stencil list; non-admins are blocked; nav entry present.
</success_criteria>

<output>
Create `.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-03-SUMMARY.md` when done, ending with a "🚨 Files to upload to live" section listing every file in `files_modified`. Requires Plan 24-01's migration already live (needs_review column).
</output>
