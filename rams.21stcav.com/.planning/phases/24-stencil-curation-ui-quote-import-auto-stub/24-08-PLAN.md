---
phase: 24
plan: 8
type: execute
wave: 2
depends_on: ["24-01"]
files_modified:
  - app/Console/Commands/StencilsReapplyTemplatesCommand.php
  - app/Console/Commands/StencilsCoverageReportCommand.php
  - tests/Feature/Console/StencilsReapplyTemplatesCommandTest.php
  - tests/Feature/Console/StencilsCoverageReportCommandTest.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "stencils:reapply-templates in dry-run mode (default) never writes to the database"
    - "stencils:reapply-templates --commit only touches stencils that are still source=auto-generated AND have zero device_stencil_audits rows"
    - "Running stencils:reapply-templates --commit twice in a row produces zero further diffs on the second run (idempotent)"
    - "stencils:coverage-report derives its top-N ranking from a live DB query over ProjectPackage equipment lines, never from the seed pack itself"
  artifacts:
    - path: "app/Console/Commands/StencilsReapplyTemplatesCommand.php"
      provides: "D-08 dry-run/--commit template re-application, never touches curated or audited stencils"
    - path: "app/Console/Commands/StencilsCoverageReportCommand.php"
      provides: "Top-N part_number-by-quote-volume + Tier1/Tier2 split report, feeds Plan 24-09's bounded fill"
  key_links:
    - from: "app/Console/Commands/StencilsReapplyTemplatesCommand.php"
      to: "app/Models/DeviceStencil.php"
      via: "whereDoesntHave('audits') eligibility filter"
      pattern: "whereDoesntHave\\('audits'\\)"
---

<objective>
Ship the two read/write CLI tools this phase's own delivery loop depends on: `stencils:reapply-templates` (D-08 — lets a config vocabulary fix retroactively re-template existing untouched stubs, including the 92 pre-existing zero-port stubs per D-11) and `stencils:coverage-report` (feeds Plan 24-09's bounded top-10 fill and Criterion 5's audit trail). Both are pure backend tooling with zero admin-UI dependency, so this plan runs parallel to Plans 24-02 and 24-03.

Purpose: D-08's rationale — `firstOrCreate` protects curated work by never overwriting (21 D-03), which also freezes every stub on the template version current when it was created; this command is the explicit, opt-in escape hatch. Coverage-report gives Criterion 5 an independently-sourced ranking (mirrors 21 D-15's "live DB query preferred, never derived from the seed pack" independence rule).
Output: `app/Console/Commands/StencilsReapplyTemplatesCommand.php`, `app/Console/Commands/StencilsCoverageReportCommand.php`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md
@.planning/phases/21-device-port-catalog-stencil-cache/21-CONTEXT.md
@app/Console/Commands/PackagesReclassifyEquipmentCommand.php
@app/Console/Commands/BackfillCablePortFksCommand.php
@app/Models/DeviceStencil.php
</context>

<interfaces>
From app/Console/Commands/PackagesReclassifyEquipmentCommand.php (existing, exact skeleton to mirror):
```
protected $signature = 'packages:reclassify-equipment
                        {package? : Package ID (optional; without = all packages)}
                        {--commit : Actually persist changes (default is dry-run)}';
public function handle(EquipmentCategoryClassifier $classifier): int
```
Report via `$this->table([...], $reportRows)`, dry-run/commit messaging block at the end (verbatim pattern: warn "DRY-RUN — no packages were changed." / info "Changes persisted. Re-run with --commit to verify idempotence...").

From app/Models/DeviceStencil.php (Plan 24-01 adds): `audits(): HasMany` relation — `stencils:reapply-templates`'s eligibility query is `DeviceStencil::where('source', DeviceStencil::SOURCE_AUTO_GENERATED)->whereDoesntHave('audits')`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: stencils:reapply-templates command (D-08)</name>
  <files>app/Console/Commands/StencilsReapplyTemplatesCommand.php, tests/Feature/Console/StencilsReapplyTemplatesCommandTest.php</files>
  <read_first>
    - app/Console/Commands/PackagesReclassifyEquipmentCommand.php (full file, already read this session — signature, dry-run/--commit skeleton, $this->table() report pattern, exact idempotency-messaging strings)
    - app/Services/Drawings/CategoryPortTemplateResolver.php (24-01 — resolve(name, partNumber): ?array)
    - app/Services/Drawings/AutoGenericStencilGenerator.php (24-01 extension — build() with 'ports' hint)
    - app/Models/DeviceStencil.php (SOURCE_AUTO_GENERATED, audits() relation from 24-01)
  </read_first>
  <action>
    Create `app/Console/Commands/StencilsReapplyTemplatesCommand.php` with `protected $signature = 'stencils:reapply-templates {--commit : Actually persist changes (default is dry-run)}';`. `handle(CategoryPortTemplateResolver $resolver, AutoGenericStencilGenerator $generator)`: eligibility query `DeviceStencil::where('source', DeviceStencil::SOURCE_AUTO_GENERATED)->whereDoesntHave('audits')->get()` — per D-08, this NEVER touches anything an engineer has edited or promoted (any row with an audit trail row is excluded regardless of its current `source` value). For each eligible stencil: re-resolve `$portTemplate = $resolver->resolve($stencil->display_name ?? '', $stencil->part_number);`, re-build `$payload = $generator->build(['manufacturer' => $stencil->manufacturer, 'model' => $stencil->model, 'name' => $stencil->display_name, 'part_number' => $stencil->part_number, 'ports' => $portTemplate ?? []]);`, compare `$payload['mxgraph_xml']` to `$stencil->mxgraph_xml` — if different, record a report row (`stencil_id`, `part_number`, `old port count` via existing `device_ports` count, `new port count` via `count($portTemplate ?? [])`); when `--commit`, update `mxgraph_xml` AND replace `device_ports` rows (delete existing + bulk-insert from `$portTemplate`, same pattern as `QuoteImportStencilStubber`'s bulk-insert in Plan 24-02 — reuse that exact insert shape, do not duplicate the logic differently). Report via `$this->table(['Stencil', 'Part Number', 'Old Ports', 'New Ports'], $reportRows);` then the exact idempotency messaging pattern from `PackagesReclassifyEquipmentCommand.php:151-158` (dry-run warn / commit info, "Re-run with --commit to verify idempotence (should show zero further diffs)").
  </action>
  <acceptance_criteria>
    - Default invocation (no `--commit`) makes zero DB writes even when eligible stencils have a stale template (dry-run only).
    - A stencil with `source=engineer-curated` OR any `device_stencil_audits` row is never touched, regardless of `--commit`.
    - Running `--commit` twice in a row: first run reports N diffs and writes them; second run reports zero diffs (determinism from Plan 24-01's port_id derivation makes re-running against unchanged config idempotent).
    - The 92 pre-existing zero-port `metadata.needs_phase_24_curation` stubs (D-11) are all eligible and get templated in one `--commit` pass when their name/part_number resolves a non-null template.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=StencilsReapplyTemplatesCommandTest</automated>
  </verify>
  <done>Dry-run-by-default, --commit persists, never touches curated/audited stencils, idempotent on repeat --commit runs.</done>
</task>

<task type="auto">
  <name>Task 2: stencils:coverage-report command</name>
  <files>app/Console/Commands/StencilsCoverageReportCommand.php, tests/Feature/Console/StencilsCoverageReportCommandTest.php</files>
  <read_first>
    - app/Console/Commands/PackagesReclassifyEquipmentCommand.php lines 137-160 ($this->table() reporting half only — this command is read-only, no --commit needed)
    - .planning/phases/21-device-port-catalog-stencil-cache/21-CONTEXT.md D-15 (independence rule for reference-list provenance — live DB query preferred, never derived from the seed pack itself)
    - app/Models/DeviceStencil.php, app/Models/ProjectPackage.php (extracted_data['equipment'] / equipment_list shape)
  </read_first>
  <action>
    Create `app/Console/Commands/StencilsCoverageReportCommand.php`, `protected $signature = 'stencils:coverage-report {--limit=10 : How many top part_numbers to report}';`, read-only (no `--commit`). `handle()`: query `ProjectPackage::query()->whereNotNull('extracted_data')->get()`, iterate each package's `extracted_data['equipment'] ?? $package->equipment_list ?? []`, tally occurrence counts keyed by normalised `part_number` (via `DeviceStencil::normalisePartNumber()`) for lines whose category is `hardware` (reuse `EquipmentCategoryClassifier::classify()` per-line, exactly like `QuoteImportStencilStubber` does, so the ranking only ever counts real hardware). This is a LIVE DB query over production quote-import data, independent of the seed pack — satisfies the 21 D-15 independence rule by construction (no `_top-50-gap.json`/`_v1.3-promoted.json` file is ever read by this command). Sort descending by occurrence count, take the top `--limit` (default 10). For each, look up the matching `DeviceStencil` (if any) and report `Tier 1` (auto-generated or missing) vs `Tier 2` (engineer-curated) status plus current port count. Render via `$this->table(['Part Number', 'Quote Occurrences', 'Tier', 'Ports'], $reportRows);` and a summary line counting how many of the top-N are still Tier 1.
  </action>
  <acceptance_criteria>
    - Command never reads any file under `resources/data/device-stencils-seed/` (grep the class file to confirm — independence rule).
    - Given 3 `ProjectPackage` fixtures where part_number `X` appears in 3 packages and `Y` appears in 1, `X` ranks above `Y` in the report.
    - A part_number that only appears on `category='cable'`/`'services'` lines never appears in the ranking.
    - `--limit=5` returns at most 5 rows.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=StencilsCoverageReportCommandTest</automated>
  </verify>
  <done>Command ranks part_numbers by live quote-import occurrence (hardware only), reports Tier 1/2 split, is the audit-trail input for Plan 24-09's bounded fill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| CLI-only tooling | Both commands run via `php artisan` on the server, no HTTP surface, no admin-session boundary applies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-24-07 | Tampering | `stencils:reapply-templates --commit` overwriting an unintended stencil | mitigate | Eligibility query is a hard `whereDoesntHave('audits')` + `source=auto-generated` filter — an engineer's promote/edit/discard action (any of which writes an audit row, Plan 24-01/24-07) permanently removes a stencil from this command's reach |
| T-24-08 | Repudiation | No record of what `--commit` changed | accept | Command output is logged to stdout/CI logs by the operator running it; a full audit-row-per-change is out of scope for a config-drift-correction tool (distinct from the promote/edit audit trail, which IS in `device_stencil_audits`) |
</threat_model>

<verification>
`php artisan test --filter=StencilsReapplyTemplatesCommandTest` and `--filter=StencilsCoverageReportCommandTest` green. Lint clean on every touched file.
</verification>

<success_criteria>
`stencils:reapply-templates` safely re-templates only untouched auto-generated stubs, dry-run by default, idempotent; `stencils:coverage-report` produces an independently-sourced top-N ranking for Plan 24-09.
</success_criteria>

<output>
Create `.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-08-SUMMARY.md` when done, ending with a "🚨 Files to upload to live" section. No migration in this plan.
</output>
