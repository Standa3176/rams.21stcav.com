---
phase: 24
plan: 5
type: execute
wave: 4
depends_on: ["24-04"]
files_modified:
  - app/Http/Requests/Admin/UpdateDeviceStencilPortsRequest.php
  - app/Http/Controllers/Admin/DeviceStencilController.php
  - routes/web.php
  - resources/views/admin/device-stencils/edit.blade.php
  - resources/views/admin/device-stencils/_port-table.blade.php
  - tests/Feature/Drawings/DeviceStencilEditTest.php
autonomous: true
requirements: [DRAW-51]

must_haves:
  truths:
    - "Admin can open a stencil's edit screen, add/edit/delete port rows in a table (not drag), and save"
    - "Saving replaces device_ports rows and regenerates mxgraph_xml in the same request, so the two never drift"
    - "The live preview pane updates within 600ms of a port-table edit, showing the same shape Save would persist"
    - "Every port_id saved to device_ports has an exact matching <constraint name> in the saved mxgraph_xml"
  artifacts:
    - path: "resources/views/admin/device-stencils/edit.blade.php"
      provides: "Two-column edit screen: port table (source of truth) + live preview pane"
    - path: "resources/views/admin/device-stencils/_port-table.blade.php"
      provides: "Alpine x-for reactive port-row repeater, 600ms debounced preview trigger with AbortController cancellation"
    - path: "app/Http/Requests/Admin/UpdateDeviceStencilPortsRequest.php"
      provides: "Batched ports-array validation, allowlists from config('drawings.port_templates'), intra-array port_id distinct rule"
  key_links:
    - from: "resources/views/admin/device-stencils/_port-table.blade.php"
      to: "admin.device-stencils.preview"
      via: "600ms debounced fetch() POST, AbortController-cancelled"
      pattern: "debouncedPreview"
    - from: "app/Http/Controllers/Admin/DeviceStencilController.php"
      to: "app/Services/Drawings/AutoGenericStencilGenerator.php"
      via: "update() regenerates mxgraph_xml in the same request as the device_ports replace"
      pattern: "AutoGenericStencilGenerator"
---

<objective>
Ship the DRAW-51 edit screen: an editable port table (D-01 — table is the source of truth, explicitly NOT drag-on-canvas) beside a live, debounced, server-rendered preview pane (D-02/D-16, wired to Plan 24-04's preview endpoint). Save batches the port array, replaces `device_ports`, and regenerates `mxgraph_xml` in the SAME request so the two never drift (RESEARCH.md Pitfall 2).

Purpose: this is where an engineer actually promotes stub coverage — the core loop Criterion 5's bounded top-10 fill (Plan 24-09) runs through.
Output: `UpdateDeviceStencilPortsRequest`, `DeviceStencilController::edit()`/`update()`, `edit.blade.php`, `_port-table.blade.php`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-CONTEXT.md
@.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-UI-SPEC.md
@.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-PATTERNS.md
@app/Http/Requests/Admin/DeviceCableRuleRequest.php
@resources/views/admin/devices/edit.blade.php
@resources/views/components/survey/repeater-equipment.blade.php
@resources/views/surveys/show.blade.php
</context>

<interfaces>
From app/Http/Requests/Admin/DeviceCableRuleRequest.php (existing, exact shape for a batched nested-array save — mirror verbatim, do not reinvent):
```
public function authorize(): bool { return auth()->user()?->isAdmin() ?? false; }
public function rules(): array { return ['field.*.subfield' => [...]]; }
```

From resources/views/components/survey/repeater-equipment.blade.php (existing, the reactive `x-for`/`x-model` shape to mirror — NOT project-packages/review.blade.php's DOM-toggle pattern, confirmed poor fit by research):
```
<template x-for="(item, idx) in (array ?? [])" :key="idx">
  <select x-model="item.field">...</select>
</template>
```

From resources/views/surveys/show.blade.php lines 1761-1799 (existing 600ms debounce + CSRF fetch pattern — copy the debounce constant verbatim, UI-SPEC explicitly locks 600ms against the 200ms ⌘K-search convention):
```
debouncedAutosave() {
    if (this._autosaveTimer) clearTimeout(this._autosaveTimer);
    this._autosaveTimer = setTimeout(() => { this.autosave(); }, 600);
}
```
No local `AbortController` precedent was directly read this session, but STATE.md session log commit `3368fdb` confirms the ⌘K command palette already uses "200ms debounced fetch with AbortController for inflight cancellation" — a proven in-codebase pattern; adapt its debounce/abort shape here at 600ms.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UpdateDeviceStencilPortsRequest + edit()/update() actions + routes</name>
  <files>app/Http/Requests/Admin/UpdateDeviceStencilPortsRequest.php, app/Http/Controllers/Admin/DeviceStencilController.php, routes/web.php, tests/Feature/Drawings/DeviceStencilEditTest.php</files>
  <read_first>
    - app/Http/Requests/Admin/DeviceCableRuleRequest.php (full file, already read this session — authorize()/rules() shape, nested-array-element validation pattern for length_tiers.*)
    - app/Http/Controllers/Admin/DeviceCableRuleController.php lines 62-84 (edit()/update() skeleton — extractData(), Log::info('Admin: ...', ['admin_id' => auth()->id()]) convention, redirect+flash)
    - app/Services/Drawings/AutoGenericStencilGenerator.php (build() — regenerate mxgraph_xml from the SAME hints shape the preview endpoint uses, Plan 24-04)
    - app/Models/DevicePort.php ($fillable columns for the delete+reinsert)
  </read_first>
  <behavior>
    - Test 1: `PUT /admin/device-stencils/{id}` with a valid `ports` array replaces all existing `device_ports` rows for that stencil and regenerates `mxgraph_xml` in the same request.
    - Test 2: posting two port rows with the same `port_id` returns a 422 with a `ports.*.port_id` distinct-rule error (D-04: "catch it in validation, not as a 500" — the DB's `device_ports_stencil_port_unique` compound index would otherwise throw).
    - Test 3: posting a `connector_type` not present in `config('drawings.port_templates')`'s known connector vocabulary is REJECTED... actually per D-02, ports ARE engineer-extensible (`device_ports.connector_type` is documented "NOT an enum — engineer-extensible" in 21-CONTEXT.md D-02) — so `connector_type` validation is `string, max:50`, NOT an `in:` allowlist; `signal_type` similarly stays a bounded but not database-enforced set (`in:audio,video,control,network,usb,power,speaker,dante,unclassified` per UI-SPEC Component Inventory point 4). Adjust Test 3 to assert an INVALID `side` value (not in `left,right,top,bottom`) is rejected, and a free-text `connector_type` value NOT in any known vocabulary is ACCEPTED (engineer-extensible, per 21 D-02).
    - Test 4: after save, every `port_id` present in the newly-saved `device_ports` rows has an exact `<constraint name="{port_id}">` substring in the newly-saved `mxgraph_xml` (parity, Pitfall 2).
    - Test 5 (**D-17 guard**): `PUT` against a stencil whose `source` is `engineer-curated`, WITHOUT `confirm_regenerate`, persists NOTHING — `mxgraph_xml` is byte-identical afterwards, `device_ports` rows are unchanged, no `device_stencil_audits` row is written — and redirects back with a warning flash naming the artwork-replacement consequence.
    - Test 6 (**D-17 guard**): the same `PUT` WITH `confirm_regenerate=1` DOES persist, and writes a `device_stencil_audits` row whose `before_snapshot` contains the PRIOR `mxgraph_xml` verbatim (so the hand-built artwork is recoverable).
    - Test 7: a stencil whose `source` is `auto-generated` saves WITHOUT `confirm_regenerate` — the guard must not obstruct the ordinary stub-curation path, which is the phase's main workflow.
  </behavior>
  <action>
    Create `app/Http/Requests/Admin/UpdateDeviceStencilPortsRequest.php` mirroring `DeviceCableRuleRequest`'s exact shape: `authorize(): bool { return auth()->user()?->isAdmin() ?? false; }`; `rules(): array` returning `'ports' => ['present', 'array'], 'ports.*.label' => ['nullable', 'string', 'max:100'], 'ports.*.side' => ['required', 'in:left,right,top,bottom'], 'ports.*.connector_type' => ['nullable', 'string', 'max:50'], 'ports.*.signal_type' => ['nullable', 'in:audio,video,control,network,usb,power,speaker,dante,unclassified'], 'ports.*.direction' => ['required', 'in:in,out,io'], 'ports.*.sort_order' => ['nullable', 'integer'], 'ports.*.port_id' => ['required', 'string', 'max:50', 'distinct'], 'ports.*.x_pct' => ['nullable', 'numeric', 'between:0,1'], 'ports.*.y_pct' => ['nullable', 'numeric', 'between:0,1'], 'confirm_regenerate' => ['nullable', 'boolean']`. Add `public function edit(DeviceStencil $deviceStencil): View` to `DeviceStencilController` (append — do not touch `index()`): `return view('admin.device-stencils.edit', ['stencil' => $deviceStencil->load('ports')]);`. Add `public function update(UpdateDeviceStencilPortsRequest $request, DeviceStencil $deviceStencil): RedirectResponse`.

    **⚠ D-17 GUARD — implement this FIRST, before any write.** Saving regenerates `mxgraph_xml` from the template generator, which REPLACES any hand-built artwork. The five hand-curated spike stencils (ClickShare Bar Pro, Neat Bar Pro, Netgear GS312TP, Samsung QM65C-T, Sennheiser TCC2) are the only ones in the catalogue with genuine hand-built mxGraph art, and are also the most likely to be edited. Under the unguarded behaviour an engineer adjusting one port label would silently destroy that artwork.

    At the very top of `update()`, before the transaction:
    `if ($deviceStencil->source === DeviceStencil::SOURCE_ENGINEER_CURATED && ! $request->boolean('confirm_regenerate')) { return redirect()->route('admin.device-stencils.edit', $deviceStencil)->withInput()->with('warning', 'This stencil is engineer-curated. Saving will replace its existing artwork with a generated shape. Re-submit with confirmation to proceed.'); }`
    This path must persist NOTHING — no port changes, no `mxgraph_xml` change, no audit row.

    When the guard passes (either the source is not `engineer-curated`, or the engineer explicitly confirmed), capture the prior state BEFORE mutating and write a `DeviceStencilAudit` row inside the same transaction: `action` = `DeviceStencilAudit::ACTION_EDIT`, `user_id` = `auth()->id()`, `before_snapshot` = `['mxgraph_xml' => $deviceStencil->mxgraph_xml, 'ports' => $deviceStencil->ports()->get()->toArray()]`, `after_snapshot` = `['mxgraph_xml' => $payload['mxgraph_xml'], 'ports' => $request->validated('ports')]`. The prior artwork is therefore always recoverable from the audit trail — which is why confirm-to-proceed is sufficient and a hard block is not required.

    **Known and intended side effect:** writing an audit row makes this stencil permanently ineligible for `stencils:reapply-templates` (Plan 24-08's D-08 rule skips anything with `whereDoesntHave('audits')`). That is correct — once an engineer has edited a stencil, automated re-templating must never touch it again. Note this in the SUMMARY.

    Then, inside a `DB::transaction` (this one IS safe to wrap — it is a single stencil's own ports + its own mxgraph_xml column, not the cross-stencil `firstOrCreate` race Pitfall 5 warns about), delete existing `$deviceStencil->ports()->delete();`, bulk-insert the validated `ports` array (same shape as `QuoteImportStencilStubber`'s insert in Plan 24-02 — mirror it, do not diverge), then regenerate `$payload = app(AutoGenericStencilGenerator::class)->build(['manufacturer' => $deviceStencil->manufacturer, 'model' => $deviceStencil->model, 'name' => $deviceStencil->display_name, 'part_number' => $deviceStencil->part_number, 'ports' => $request->validated('ports')]);` and `$deviceStencil->update(['mxgraph_xml' => $payload['mxgraph_xml']]);`. `Log::info('Admin: device stencil ports updated', ['stencil_id' => $deviceStencil->id, 'admin_id' => auth()->id()]);` then `redirect()->route('admin.device-stencils.edit', $deviceStencil)->with('success', 'Ports saved.');`. In `routes/web.php`, add `Route::get('/admin/device-stencils/{deviceStencil}/edit', [DeviceStencilController::class, 'edit'])->name('admin.device-stencils.edit');` and `Route::put('/admin/device-stencils/{deviceStencil}', [DeviceStencilController::class, 'update'])->name('admin.device-stencils.update');` inside the admin group.
  </action>
  <acceptance_criteria>All 7 behaviour tests pass, including the three D-17 guard tests (5, 6, 7).</acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=DeviceStencilEditTest</automated>
  </verify>
  <done>Batched port save works, validates structurally (not the D-04 promote gate — that is Plan 24-07), regenerates mxgraph_xml with proven parity to the saved device_ports, and the D-17 guard prevents an engineer-curated stencil's artwork being replaced without explicit confirmation — with the prior mxgraph_xml preserved in device_stencil_audits.before_snapshot.</done>
</task>

<task type="auto">
  <name>Task 2: edit.blade.php + _port-table.blade.php (Alpine reactive repeater + debounced preview)</name>
  <files>resources/views/admin/device-stencils/edit.blade.php, resources/views/admin/device-stencils/_port-table.blade.php</files>
  <read_first>
    - resources/views/admin/devices/edit.blade.php lines 132-141, 159-190, 260-265 (x-edit-action-bar slot usage, .card layout, .dv-footer-actions convention)
    - resources/views/components/survey/repeater-equipment.blade.php (full file, already read this session — x-for/x-model/addEquipment()/removeEquipment(idx) reactive shape)
    - resources/views/surveys/show.blade.php lines 1761-1799 (600ms debounce + CSRF fetch skeleton)
    - .planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-UI-SPEC.md Component Inventory points 3-5 (two-column ~60/40 layout, port-table row columns + validation tinting values, preview-pane state indicator copy)
  </read_first>
  <action>
    Create `resources/views/admin/device-stencils/edit.blade.php`: `<x-edit-action-bar :form-id="'stencil-ports-form'" :cancel-url="route('admin.device-stencils.index')">` with title = `trim($stencil->manufacturer.' '.$stencil->model) ?: 'Stencil #'.$stencil->id` (no plain Save button in the bar — batched port-table submit happens via the port-table card's own form, per UI-SPEC point 3). Below it, a two-column CSS-grid layout (genuinely new markup — no existing screen has this shape): left column ~60% width containing an Alpine `x-data` root (`ports: @json($stencil->ports->toArray())`, plus `previewSvg: null`, `previewState: 'idle'`, `_previewAbort: null`, `_debounceTimer: null`) wrapping a `<form method="POST" action="{{ route('admin.device-stencils.update', $stencil) }}" id="stencil-ports-form">@csrf @method('PUT')` that `@include('admin.device-stencils._port-table')`; right column ~40% width, sticky, a `.card` with `.section-heading` "Live Preview" (18px/`--fs-h3`/600, NOT `--fs-h2`) containing an `<img>` or inline `<div x-html="previewSvg">` target, a small state indicator (`Rendering…` text with `opacity-50` while `previewState === 'loading'`, an `.alert-error` "Preview failed to render — showing the last successful preview." on failure WITHOUT blanking the last good render). Stack to single-column under 900px (media query). Footer (`.dv-footer-actions` convention): `Cancel` (`.btn-outline`, `url()->previous()` via the action-bar's own default) — `Promote to Engineer-Curated` and `Discard & Regenerate` buttons are added by Plan 24-07 (leave a Blade comment placeholder noting this). Create `resources/views/admin/device-stencils/_port-table.blade.php`: header row "Ports" (`.section-heading`, 18px/600) + "+ Add port" trigger button; `<template x-for="(port, idx) in ports" :key="idx">` row with `x-model="port.label"` (text), `x-model="port.side"` (select: left/right/top/bottom), `x-model="port.connector_type"` (free-text input — engineer-extensible per 21 D-02, not a closed select), `x-model="port.signal_type"` (select: audio/video/control/network/usb/power/speaker/dante/unclassified), `x-model="port.direction"` (select: in/out/io), `x-model="port.sort_order"` (number), a Position number input bound to `port.x_pct` or `port.y_pct` depending on `port.side` (placeholder "auto", step 0.01, 0-1 range), `x-model="port.port_id"` (mono text), a delete `×` button (`.btn-ghost`, `:aria-label="'Remove port ' + (port.label || 'row ' + (idx+1))"` per the a11y batch-6 convention — `repeater-equipment.blade.php`'s own delete button predates that convention and has NO aria-label; do not copy that gap). `addPort()`/`removePort(idx)` push/splice the `ports` array. `$watch('ports', () => this.debouncedPreview(), { deep: true })`. `debouncedPreview()`: clear `_debounceTimer`, `setTimeout(() => this.runPreview(), 600)` (verbatim 600ms constant, matching `surveys/show.blade.php`'s convention, deliberately NOT the 200ms ⌘K-search value). `runPreview()`: if `_previewAbort` is set, call `_previewAbort.abort()` first (new to this codebase but standard `AbortController` usage — cancels a superseded in-flight request so a late response never clobbers a newer one); create a fresh `AbortController`, `fetch(route('admin.device-stencils.preview', stencil.id), { method: 'POST', signal: controller.signal, headers: {'Content-Type':'application/json','X-CSRF-TOKEN': document.querySelector('meta[name=\"csrf-token\"]').content,'Accept':'image/svg+xml'}, body: JSON.stringify({ ports: this.ports }) })`, on success set `previewSvg` from `resp.text()` and `previewState = 'clean'`; on error (excluding `AbortError`, which is silently swallowed — a superseded request, not a real failure) set `previewState = 'error'` WITHOUT clearing the previous `previewSvg`. Row validation tinting per UI-SPEC point 4 (client-side, preview-only signal — NOT the promote hard-gate, which is server-enforced in Plan 24-07): a row missing `label`/`connector_type`/`signal_type`/`direction` gets `border-left: 3px solid var(--danger)`; a row with `signal_type === 'unclassified'` or both position fields null gets the amber warning treatment.

    **⚠ D-17 GUARD — UI half.** Task 1 blocks the server-side save for an `engineer-curated` stencil unless `confirm_regenerate` is present. Surface that here so the engineer meets it as a deliberate choice rather than a bounced form:
    - When `$stencil->source === \App\Models\DeviceStencil::SOURCE_ENGINEER_CURATED`, render a persistent warning banner above the port table using the existing warning tokens (NOT `.alert-error` — this is a caution, not a failure): "This stencil has engineer-curated artwork. Saving will replace it with a generated shape. The previous version is kept in the audit trail."
    - The submit button in that case carries an `x-on:click` confirm step that sets a hidden `<input type="hidden" name="confirm_regenerate" value="1">` only after the engineer accepts, so an accidental Enter-key submit cannot silently confirm.
    - For every other `source` value, emit NO banner and NO hidden field — the ordinary stub-curation path (the phase's main workflow) must stay a single-click save with zero added friction.
    - Render the `warning` flash from Task 1's guard redirect (`session('warning')`) at the top of the page, so a bounced save explains itself.
  </action>
  <acceptance_criteria>
    - Loading the edit screen for a stencil with existing ports pre-populates the Alpine `ports` array from `$stencil->ports`.
    - Typing in a port-table field triggers the preview fetch after 600ms of inactivity, not on every keystroke (debounce proven by a JS-level assertion or by asserting the network call count in a Dusk/browser-level check is out of scope for this PHPUnit-only phase — assert instead via a feature test that the Blade view emits `setTimeout(...600)` and NOT `200`).
    - The delete button on every port row has a non-empty `aria-label`.
    - Under 900px viewport width the two-column grid collapses to one column (CSS media query present in the compiled view — assert via a string-contains check on the raw Blade source for the breakpoint).
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=DeviceStencilEditTest</automated>
  </verify>
  <done>Two-column edit screen renders, port table is Alpine-reactive (x-for/x-model, add/remove), preview debounces at 600ms with AbortController cancellation, row tinting reflects D-04's blocking/warning field set for preview purposes only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin-posted ports array -> device_ports + mxgraph_xml | Authenticated admin session; posted array still validated server-side (structural rules only — the D-04 promote hard-gate is a SEPARATE, stricter check in Plan 24-07) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-24-11 | Tampering | `UpdateDeviceStencilPortsRequest` bypass via direct PUT with malformed ports | mitigate | FormRequest `rules()` enforces `side`/`direction`/`signal_type` allowlists and intra-array `port_id` distinct BEFORE the DB's compound unique index would otherwise throw a 500 |
| T-24-12 | Tampering | Port label/connector_type text reaching mxGraph XML on Save | mitigate | Save's regenerated `mxgraph_xml` flows through the SAME `AutoGenericStencilGenerator::xml()` escaper proven in Plan 24-01 |
</threat_model>

<verification>
`php artisan test --filter=DeviceStencilEditTest` green. Lint clean on every touched file.
</verification>

<success_criteria>
Admin can edit a stencil's ports in a reactive table, see a live 600ms-debounced server-rendered preview, and save with guaranteed mxgraph_xml/device_ports parity.
</success_criteria>

<output>
Create `.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/24-05-SUMMARY.md` when done, ending with a "🚨 Files to upload to live" section. No migration in this plan.
</output>
