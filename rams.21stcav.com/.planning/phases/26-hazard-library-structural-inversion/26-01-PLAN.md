---
phase: 26-hazard-library-structural-inversion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_08_23_160000_add_include_when_to_hazard_templates.php
  - database/seeders/HazardTemplateSeeder.php
  - app/Models/HazardTemplate.php
  - tests/Feature/HazardTemplates/HazardTemplateSeederIncludeWhenTest.php
autonomous: false
requirements: [HAZ-01, HAZ-03]
user_setup: []

must_haves:
  truths:
    - "All 18 hazard-library.md hazards exist as is_global=true rows in hazard_templates, each with a name matching the skill's exact heading text"
    - "Working at height's seeded row carries pre_likelihood=3, pre_severity=4, post_likelihood=1, post_severity=4 (residual 1x4, not the old baseline's 2x3)"
    - "Re-running the seeder does not duplicate rows and does not touch is_global=false (user-created) rows"
    - "The 6 superseded old global hazard names (Struck by Falling Objects, Hidden Services (Electrical, Plumbing, Gas), Sharps & Hand / Power Tools, Display Installation / Wall Mounting, Fixings / Substrate Failure, Interaction with Other Trades) no longer exist as is_global rows after seeding"
    - "include_when is never settable through HazardTemplateController's mass-assignment surface even though it is now in HazardTemplate::$fillable"
  artifacts:
    - path: "database/migrations/2026_08_23_160000_add_include_when_to_hazard_templates.php"
      provides: "include_when nullable text column on hazard_templates"
    - path: "database/seeders/HazardTemplateSeeder.php"
      provides: "the 18-hazard library content + include_when signal keys, upsert-by-name, orphan-row cleanup"
    - path: "app/Models/HazardTemplate.php"
      provides: "include_when in $fillable (seeder write path only)"
  key_links:
    - from: "database/seeders/HazardTemplateSeeder.php"
      to: "hazard_templates.include_when"
      via: "HazardTemplate::create()/update() payload"
      pattern: "include_when"
---

<objective>
Port the skill's full 18-hazard library (`.planning/reference/21cav-rams-skill/references/hazard-library.md`) into `hazard_templates` as the new global register, each row carrying a machine-parseable `include_when` condition, and retire the 13 old global rows this content supersedes — without touching any `is_global=false` (user-created) row. This is the data foundation every later plan in this phase reads from.

Purpose: D-01 makes the DB the runtime store and the seeder file the git-auditable source of truth; nothing downstream (resolver, blade views, DOCX) can be correctly verified until the 18-row library exists with correct scores and include_when values.
Output: Migration adding `include_when`; rewritten `HazardTemplateSeeder`; `HazardTemplate::$fillable` updated; local DB migrated + seeded (BLOCKING); live-deploy instructions documented.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-hazard-library-structural-inversion/26-CONTEXT.md
@.planning/phases/26-hazard-library-structural-inversion/26-RESEARCH.md
@.planning/phases/26-hazard-library-structural-inversion/26-PATTERNS.md
@.planning/reference/21cav-rams-skill/references/hazard-library.md
@database/seeders/HazardTemplateSeeder.php
@database/migrations/2026_03_09_000001_create_hazard_templates_table.php
@app/Models/HazardTemplate.php
@app/Http/Controllers/HazardTemplateController.php

<interfaces>
`HazardTemplate` model (`app/Models/HazardTemplate.php`) current `$fillable`:
user_id, name, description, pre_likelihood, pre_severity, post_likelihood, post_severity, controls, is_global.
`$casts`: controls => array, is_global => boolean.
`scopeVisibleTo(int $userId)`: `is_global = true OR user_id = $userId`.

`include_when` string convention this plan establishes (consumed by Plan 26-02's resolver, do not deviate):
- `'always'` — tier 1, unconditional.
- `'signal:<key>'` — tier 2, deterministic. Exactly 9 keys, one per tier-2 hazard (see Task 2 table).
- `'ai:<key>'` — tier 3, judgement-required. Exactly 5 keys, one per tier-3 hazard (see Task 2 table).
- `null` — manual-only (D-04). Every `is_global=false` row must have `include_when = null`; no seeder row is ever null.

`HazardTemplateController::store()`/`update()` (`app/Http/Controllers/HazardTemplateController.php:30-89`) build explicit literal arrays for `HazardTemplate::create()`/`update()` — `include_when` is not one of the listed keys and `validateTemplate()` does not return one either. Do not add `include_when` to either. This is the existing, already-sufficient mitigation for T-26-01 (see threat_model) — verify it with a test, do not "fix" it with new code.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — add include_when column, extend HazardTemplate::$fillable</name>
  <files>database/migrations/2026_08_23_160000_add_include_when_to_hazard_templates.php, app/Models/HazardTemplate.php</files>
  <read_first>database/migrations/2026_03_09_000001_create_hazard_templates_table.php, database/migrations/2026_04_19_000001_add_email_sent_columns_for_phase_09.php, app/Models/HazardTemplate.php</read_first>
  <action>
    Create `database/migrations/2026_08_23_160000_add_include_when_to_hazard_templates.php` following the `add_email_sent_columns_for_phase_09` add-column pattern (guard with `Schema::hasColumn`, no try/catch wrapper — that wrapper is only for `Schema::create`). `up()`: on `hazard_templates`, add `include_when` as a nullable `text` column positioned `->after('controls')`, guarded by `! Schema::hasColumn('hazard_templates', 'include_when')`. `down()`: `dropColumn('include_when')`. Docblock states: Phase 26 HAZ-01/HAZ-02, stores the tiered include-when condition string (`always` / `signal:<key>` / `ai:<key>` / `null`=manual-only per D-04), reversible via `down()`.

    In `app/Models/HazardTemplate.php`, add `'include_when'` to `$fillable` (after `'controls'`). Do NOT add a cast — it is a plain nullable string. Do not touch `HazardTemplateController.php` in this task — its literal-array construction already excludes `include_when` from mass assignment; that is the T-26-01 mitigation and must remain unmodified.
  </action>
  <verify>
    <automated>grep -c "include_when" database/migrations/2026_08_23_160000_add_include_when_to_hazard_templates.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "include_when" database/migrations/2026_08_23_160000_add_include_when_to_hazard_templates.php` returns 2 or more (column add + down)
    - `grep -c "hasColumn" database/migrations/2026_08_23_160000_add_include_when_to_hazard_templates.php` returns 1
    - `grep -c "include_when" app/Models/HazardTemplate.php` returns 1 (added to `$fillable` only, no cast)
    - `grep -c "include_when" app/Http/Controllers/HazardTemplateController.php` returns 0 (unmodified — confirms T-26-01 mitigation untouched)
  </acceptance_criteria>
  <done>Migration file exists with the guarded add-column pattern; HazardTemplate::$fillable includes include_when; HazardTemplateController is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite HazardTemplateSeeder — the 18-hazard library + orphan-row cleanup</name>
  <files>database/seeders/HazardTemplateSeeder.php</files>
  <read_first>.planning/reference/21cav-rams-skill/references/hazard-library.md, database/seeders/HazardTemplateSeeder.php, .planning/phases/26-hazard-library-structural-inversion/26-CONTEXT.md</read_first>
  <action>
    Replace `standardHazards()`'s return value with all 18 hazards from `hazard-library.md`, transcribed verbatim (name, description synthesized as one concise sentence per hazard from its heading context, full numbered control list, `pre_likelihood`/`pre_severity`/`post_likelihood`/`post_severity` from the "Typical: initial LxS, residual LxS" line). Keep the existing upsert-by-name loop (`database/seeders/HazardTemplateSeeder.php:22-39`) unmodified — do not redesign it.

    Use these exact hazard-library.md heading strings as `name` (sentence case, verbatim) and these exact `include_when` values:

    Tier 1 (`include_when = 'always'`, 4 hazards): Slips, trips and falls (3x3/2x2); Low voltage AV connections (2x3/1x2); Fire and evacuation (2x5/1x4); COSHH substances (2x2/1x2). Note: COSHH substances is not named in REQUIREMENTS.md HAZ-01's illustrative 8-item list but IS one of the 18 and ROADMAP.md's Phase 26 Success Criterion 1 requires "All 18" — include it; this is a documentation-completeness fix, not new scope.

    Tier 2 (`include_when = 'signal:<key>'`, 9 hazards): Working at height -> `signal:mounting_above_reach` (3x4/1x4); Manual handling -> `signal:display_mount_or_rack` (4x3/2x3); Electrical -> `signal:mains_connection` (2x5/1x4); Noise and vibration -> `signal:drilling_or_percussive` (3x3/1x2); Restricted access and ceiling voids -> `signal:ceiling_void_access` (3x3/1x2); Cable pulling and termination -> `signal:first_fix_cabling` (3x2/1x1); Fixings into walls, ceilings and pillars -> `signal:any_penetration` (3x4/1x4); Dust from drilling and cutting -> `signal:any_drilling` (3x3/1x2); Decommissioning and WEEE -> `signal:strip_out_or_decommission` (2x3/1x2).

    Tier 3 (`include_when = 'ai:<key>'`, 5 hazards): Occupied premises -> `ai:occupied_premises` (3x3/1x2); Asbestos-containing materials -> `ai:asbestos` (2x5/1x5); Vehicle and plant movement -> `ai:vehicle_plant` (3x5/1x4); Lone and small-team working -> `ai:lone_working` (2x4/1x3); Occupational road risk -> `ai:road_risk` (3x4/1x3).

    D-02 fold-mapping (do not create rows for these 6 old names; their content is superseded, not carried forward as separate rows — read each old row's `controls` in the current file before deleting to judge whether any single control line is worth appending to the target's control list, but hazard-library.md's own text is the authority and is already complete): Struck by Falling Objects -> superseded by Working at height; Hidden Services (Electrical, Plumbing, Gas) -> superseded by Fixings into walls, ceilings and pillars; Sharps & Hand / Power Tools -> superseded by Cable pulling and termination; Display Installation / Wall Mounting -> superseded by Manual handling; Fixings / Substrate Failure -> superseded by Fixings into walls, ceilings and pillars; Interaction with Other Trades -> superseded by Occupied premises.

    After the existing upsert loop, add an orphan-row cleanup step: delete any `is_global=true` row whose `name` is not in the new 18-name list (`HazardTemplate::where('is_global', true)->whereNotIn('name', $newNames)->delete()`, where `$newNames` is `array_column($hazards, 'name')`). This never touches `is_global=false` rows (the `where('is_global', true)` clause excludes them) and is not a truncate — it is a scoped delete of exactly the 6 (or however many remain unmatched) rows the reseed supersedes, satisfying D-03's "no truncate, user rows untouched" guarantee while preventing the 6 stale old rows from lingering as orphaned fuzzy-match candidates. Update the seeder's final `$this->command->info(...)` line to also report the delete count.
  </action>
  <verify>
    <automated>php artisan tinker --execute="echo (new Database\Seeders\HazardTemplateSeeder())->run();"</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan tinker --execute="echo (new Database\Seeders\HazardTemplateSeeder())->run();"` is not required; instead Task 3's artisan db:seed run proves this — this task's acceptance is structural
    - `grep -c "'name'" database/seeders/HazardTemplateSeeder.php` returns 18 (one per hazard entry)
    - `grep -c "include_when" database/seeders/HazardTemplateSeeder.php` returns 19 or more (18 entries + the orphan-cleanup whereNotIn line, or the name-list variable)
    - `grep -c "whereNotIn" database/seeders/HazardTemplateSeeder.php` returns 1
    - `grep -c "truncate\|delete()->\|->delete();" database/seeders/HazardTemplateSeeder.php` returns exactly 1 (the single scoped orphan-cleanup delete, no truncate call anywhere)
    - `grep -n "'Working at height'" database/seeders/HazardTemplateSeeder.php` shows `pre_likelihood' => 3` and `post_likelihood' => 1` and `post_severity' => 4` in the same entry (residual 1x4, not 2x3)
  </acceptance_criteria>
  <done>HazardTemplateSeeder::standardHazards() returns exactly the 18 hazard-library.md hazards with correct include_when tier keys and scores; the run() method deletes superseded orphan global rows without a truncate and without touching is_global=false rows.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Migrate + seed locally, verify row state, document live deploy</name>
  <files>tests/Feature/HazardTemplates/HazardTemplateSeederIncludeWhenTest.php</files>
  <read_first>tests/Feature/Drawings/DeviceCatalogSeederTest.php</read_first>
  <action>
    Run `php artisan migrate` against the local test/dev DB, then `php artisan db:seed --class=HazardTemplateSeeder`. Confirm via `php artisan tinker` or a quick script that `HazardTemplate::where('is_global', true)->count()` is 18 and that none of the 6 superseded names remain. This step MUST run before any later plan's automated tests execute against `hazard_templates`, since those tests read seeded rows and will fail against a stale schema/empty table.

    Write `tests/Feature/HazardTemplates/HazardTemplateSeederIncludeWhenTest.php` (uses `RefreshDatabase`) with 3 tests: (1) after seeding, `HazardTemplate::where('is_global', true)->count()` is exactly 18; (2) re-running `$this->seed(HazardTemplateSeeder::class)` a second time produces no duplicate rows (count stays 18) and an existing `is_global=false` fixture row (create one manually before seeding) is byte-identical before/after both seed runs; (3) `HazardTemplateController::store()` called with a spoofed `include_when` field in the request payload (e.g. `$this->actingAs($user)->post(route('hazard-templates.store'), ['name' => 'Test', 'pre_likelihood' => 1, 'pre_severity' => 1, 'post_likelihood' => 1, 'post_severity' => 1, 'include_when' => 'always'])`) creates a row whose `include_when` is `null`, proving T-26-01's mitigation holds end-to-end through the actual HTTP route, not just by code inspection.

    Document the live deploy sequence as a code comment block at the top of this test file (not executed by the test, informational for the human checkpoint in Plan 26-06): deploy as `stcav` (never root), `git pull`, `php artisan migrate --force`, `php artisan db:seed --class=HazardTemplateSeeder --force`.
  </action>
  <verify>
    <automated>php artisan test --filter=HazardTemplateSeederIncludeWhenTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=HazardTemplateSeederIncludeWhenTest` exits 0
    - Local `hazard_templates` table has `include_when` column: `php artisan tinker --execute="echo Schema::hasColumn('hazard_templates','include_when') ? 'yes' : 'no';"` outputs `yes`
    - Local `HazardTemplate::where('is_global', true)->count()` equals 18
  </acceptance_criteria>
  <done>Local DB is migrated and seeded with the 18-hazard library; a committed regression test proves idempotency, user-row safety, and the include_when mass-assignment guard; live deploy steps are documented for the Plan 26-06 checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Engineer browser -> HazardTemplateController | Authenticated user submits hazard-template create/edit form data |
| Seeder -> hazard_templates | Trusted, version-controlled content; not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-26-01 | Tampering | `HazardTemplate::$fillable` / `HazardTemplateController::store()`,`update()` | mitigate | `include_when` added to `$fillable` for the seeder's write path, but `HazardTemplateController`'s literal-array construction never includes it — Task 3's test 3 proves a spoofed `include_when` in the POST body is silently dropped, keeping D-04's "user rows are always manual-only" guarantee intact |
| T-26-02 | Tampering | `HazardTemplateSeeder` orphan-row cleanup | accept | `whereNotIn('name', ...)->delete()` is scoped to `is_global=true` only; cannot reach `is_global=false` rows because the query's own `where('is_global', true)` clause excludes them regardless of name collision |
| T-26-SC | Tampering | Composer installs | accept | No new packages in this plan |
</threat_model>

<verification>
`php artisan test --filter=HazardTemplateSeederIncludeWhenTest` passes. `hazard_templates` has 18 `is_global=true` rows after seeding, each with a non-null `include_when`. No `is_global=false` row is affected by the seeder.
</verification>

<success_criteria>
Migration + seeder committed; local DB migrated and seeded; HAZ-01 (18 hazards ported) and HAZ-03 (Working at height residual 1x4, and correct typical scores for all 18) satisfied at the data layer, ready for the resolver (26-02) and wiring (26-04) plans to consume.
</success_criteria>

<output>
Create `.planning/phases/26-hazard-library-structural-inversion/26-01-SUMMARY.md` when done
</output>
