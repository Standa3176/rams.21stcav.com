---
phase: 26-hazard-library-structural-inversion
plan: 06
type: execute
wave: 4
depends_on: ["26-01", "26-04", "26-05"]
files_modified:
  - tests/Unit/Services/Rams/MethodStatementAssociatedRisksTest.php
  - tests/Feature/Rams/WorkingAtHeightResidualScoreTest.php
  - tests/Feature/Rams/HazardTemplateSeederIdempotencyTest.php
autonomous: false
requirements: [HAZ-01, HAZ-02, HAZ-03, HAZ-04]
user_setup: []

must_haves:
  truths:
    - "A variable-length (non-11) hazard register still produces RA{NN} refs that resolve 1:1 against the method statement's cited RA-IDs within a single generation"
    - "Working at height renders residual 1x4 in the actual generated DOCX output (word/document.xml), not just in the seeded template row"
    - "A near-empty scope (no ceiling/drilling/display/mains signals) produces a DOCX hazard register containing only the 4 always-tier hazard names, proven by unzipping the real generated file"
    - "Regenerating live project 21CQ30960 shows only hazards its actual scope supports, manually confirmed against the source quote"
    - "Toggling RAMS_HAZARD_LIBRARY_TIERING off and back on live is provably a one-.env-edit operation, not a redeploy"
  artifacts:
    - path: "tests/Feature/Rams/WorkingAtHeightResidualScoreTest.php"
      provides: "DOCX-path proof of HAZ-02 (near-empty scope) and HAZ-03 (1x4 residual)"
    - path: "tests/Feature/Rams/HazardTemplateSeederIdempotencyTest.php"
      provides: "belt-and-braces idempotency regression distinct from Plan 26-01's inline test"
  key_links:
    - from: "app/Services/RiskTemplateResolverService.php"
      to: "app/Services/DocxBuilderService.php::buildRiskAssessment()"
      via: "resolved hazards array -> generated_data['hazards'] -> DOCX render"
      pattern: "buildRiskAssessment"
---

<objective>
Prove the whole phase end-to-end through the path that matters: the live DOCX renderer, not the non-live PDF composer. Extend the RA-ref regression test for a variable-length register, add a DOCX-path feature test proving HAZ-02's near-empty-scope behaviour and HAZ-03's 1x4 residual score render correctly in the actual generated file, run the full suite, then deploy to `rams.21stcav.com` and manually verify against the real trigger case (21CQ30960) and the reversibility flag.

Purpose: Per CONTEXT.md's constraint 2, "any acceptance criterion proving hazard population must go through the DOCX path, or it proves nothing about what engineers actually receive." This is the phase's final gate before verify-work — every other plan builds the mechanism; this plan proves it against the actual deliverable and the actual production trigger case.
Output: 3 test files (2 new, 1 extended) proving DOCX-path correctness and register-length independence; a completed manual spot-check against 21CQ30960 and the env-flag reversibility guarantee.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-hazard-library-structural-inversion/26-CONTEXT.md
@.planning/phases/26-hazard-library-structural-inversion/26-VALIDATION.md
@tests/Unit/Services/Rams/MethodStatementAssociatedRisksTest.php
@tests/Feature/Rams/DocxBuilderNewSectionsTest.php
@app/Services/DocxBuilderService.php
@app/Services/RiskTemplateResolverService.php

<interfaces>
`DocxBuilderNewSectionsTest::renderDocumentXml()` (`tests/Feature/Rams/DocxBuilderNewSectionsTest.php:41-88`) is the exact pattern for opening a real generated DOCX and asserting on its XML: `app(DocxBuilderService::class)->build($record->generated_data ?? [], $record->fresh())` returns a file path, then `new \ZipArchive()` -> `$zip->getFromName('word/document.xml')` -> string assertions. Reuse this pattern verbatim rather than inventing a new one.

`app/Services/DocxBuilderService.php` `buildRiskAssessment()` (`:~1222-1230`) computes `$refLabel = 'RA' . str_pad((string)($idx + 1), 2, '0', STR_PAD_LEFT)` from raw array position and reads `pre_likelihood`/`pre_severity`/`post_likelihood`/`post_severity` directly off each `$data['hazards']` row — no fallback exists in this file, so whatever `RiskTemplateResolverService::resolve()` (Plan 26-04) produces renders as-is.

`MethodStatementAssociatedRisksTest::hazards()` (`tests/Unit/Services/Rams/MethodStatementAssociatedRisksTest.php:51-58`) is the fixture to extend — currently 3 hazards with non-sequential ids (4/9/11). `test_emitted_ra_ids_all_exist_in_the_rendered_risk_register()` (`:136-176`) already computes valid refs generically from `array_keys(array_values($hazards))`, so it extends to any register length without new assertion logic.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RA-ref regression extension + seeder idempotency hardening</name>
  <files>tests/Unit/Services/Rams/MethodStatementAssociatedRisksTest.php, tests/Feature/Rams/HazardTemplateSeederIdempotencyTest.php</files>
  <read_first>tests/Unit/Services/Rams/MethodStatementAssociatedRisksTest.php, tests/Feature/Drawings/DeviceCatalogSeederTest.php</read_first>
  <action>
    In `MethodStatementAssociatedRisksTest`, extend the private `hazards()` fixture from 3 entries to a variable-length set of at least 8 entries with non-sequential `id` values (e.g. 4/9/11/15/16/20/22/25), mirroring the existing entries' shape (`id`, `hazard`, `controls`). Add one new test method `test_emitted_ra_ids_all_exist_in_the_rendered_risk_register_at_variable_length()` that duplicates `test_emitted_ra_ids_all_exist_in_the_rendered_risk_register()`'s body against the new bigger fixture (do not modify the original test — both must coexist as regression guards for different register sizes) — asserting the same "every emitted RA-ID is in `RA01..RA{count}`" invariant now holds at count=8, not just count=3.

    Write `tests/Feature/Rams/HazardTemplateSeederIdempotencyTest.php` (uses `RefreshDatabase`) as a belt-and-braces regression distinct from Plan 26-01's inline seeder test: seed twice (`$this->seed(HazardTemplateSeeder::class)` called twice), assert `HazardTemplate::where('is_global', true)->count()` is 18 after each call, and assert every one of the 18 rows has a non-null `include_when` value after the second seed run (proves the upsert path doesn't accidentally null out the column on re-run).
  </action>
  <verify>
    <automated>php artisan test --filter=MethodStatementAssociatedRisksTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=MethodStatementAssociatedRisksTest` exits 0, including the new variable-length test
    - `php artisan test --filter=HazardTemplateSeederIdempotencyTest` exits 0
    - The original `test_emitted_ra_ids_all_exist_in_the_rendered_risk_register` (3-hazard fixture) still exists unmodified and still passes
  </acceptance_criteria>
  <done>RA-ref stability is proven at both a 3-item and an 8-item register length; seeder idempotency is proven by a dedicated, independent test in addition to Plan 26-01's inline coverage.</done>
</task>

<task type="auto">
  <name>Task 2: DOCX-path proof — HAZ-02 near-empty scope + HAZ-03 residual score, full suite run</name>
  <files>tests/Feature/Rams/WorkingAtHeightResidualScoreTest.php</files>
  <read_first>tests/Feature/Rams/DocxBuilderNewSectionsTest.php, app/Services/RiskTemplateResolverService.php</read_first>
  <action>
    Write `tests/Feature/Rams/WorkingAtHeightResidualScoreTest.php` using `RefreshDatabase`, seeding `HazardTemplateSeeder` in `setUp()`. Two tests:

    Test 1 `test_near_empty_scope_produces_only_always_tier_hazards_in_docx`: call `app(RiskTemplateResolverService::class)->resolve([], false, $user->id, [], [])` (empty activities, no drilling, no explicit names, no scope narrative — the genuinely near-empty case) to get `$risk['hazards']`, build a `generated_data` array using `renderDocumentXml()`'s pattern (copy the helper method from `DocxBuilderNewSectionsTest` into this file or extract it to a shared trait if one already exists — check first) with `'hazards' => $risk['hazards']`, render via `DocxBuilderService::build()`, unzip `word/document.xml`, assert it contains exactly the 4 always-tier hazard names (`Slips, trips and falls`, `Low voltage AV connections`, `Fire and evacuation`, `COSHH substances`) and does NOT contain any old baseline title (`Manual Handling of AV Equipment`, `Electrical Isolation`, `Working at Height` in Title Case).

    Test 2 `test_working_at_height_renders_residual_1x4_in_docx`: call `resolve(['ceiling_works'], false, $user->id, [], [])` to trigger the `mounting_above_reach`/`display_mount_or_rack` tier-2 signals, confirm `Working at height` is present in `$risk['hazards']` with `post_likelihood===1` and `post_severity===4` at the PHP-array level BEFORE rendering (fail fast with a clear message if the seeded row doesn't carry the expected residual), then render via DOCX and assert the XML contains the substring pattern that proves 1×4 renders (the DOCX table cell text — inspect how `buildRiskAssessment()` formats the residual score, e.g. `1×4` or `1x4` or two separate cells; use whichever exact string the render loop actually produces, discovered by running the test once and reading the failure diff, then asserting on that exact string, not a guess).
  </action>
  <verify>
    <automated>php artisan test --filter=WorkingAtHeightResidualScoreTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=WorkingAtHeightResidualScoreTest` exits 0
    - `php artisan test` (full suite) exits with the same pre-existing failure count documented in RESEARCH.md (2, unrelated) and zero NEW failures
  </acceptance_criteria>
  <done>Two feature tests prove, through the actual live DOCX renderer, that a near-empty scope yields only the 4 always-tier hazards and that Working at height's residual score renders 1x4; full suite is green modulo the 2 pre-existing unrelated failures.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live deploy + manual 21CQ30960 spot-check + reversibility proof</name>
  <files>.env (live server only, not committed)</files>
  <action>
    Deploy Plans 26-01 through 26-05 to rams.21stcav.com as the stcav user, run the migration and seeder live, regenerate project 21CQ30960, and manually spot-check the hazard register against the source quote per the how-to-verify steps below. Also prove the RAMS_HAZARD_LIBRARY_TIERING env flag is a genuine one-edit rollback (no redeploy) by toggling it off and back on live.
  </action>
  <what-built>
    All 5 hazard-injection paths replaced with the 18-hazard include-when library; a new RAMS starts from a conditional register instead of a fixed 11; Working at height residual now scores 1x4; the review screen exposes editable numeric scores with a reviewed/unreviewed marker and a needs-confirmation badge; the whole mechanism is gated behind RAMS_HAZARD_LIBRARY_TIERING, independent of the existing RAMS_TIER1_DEFAULTS flag.
  </what-built>
  <how-to-verify>
    1. Deploy to `rams.21stcav.com` as the `stcav` user (never root): `git pull`, `php artisan migrate --force`, `php artisan db:seed --class=HazardTemplateSeeder --force`, then clear caches per this app's normal deploy convention.
    2. Confirm `RAMS_HAZARD_LIBRARY_TIERING` is unset or `true` in the live `.env` (defaults to enabled).
    3. Regenerate project 21CQ30960 (VW Blakelands) and open the generated DOCX output — the live primary renderer, not the PDF.
    4. Compare the hazard register against 21CQ30960's actual source-quote scope. Confirm no hazard appears that the job's scope does not support. Confirm any hazard marked "needs confirmation" is expected, not a defect (D-06 hazards are supposed to appear pre-flagged when the underlying signal isn't captured — this is correct behaviour, not a bug to fix).
    5. Confirm Working at height's residual score reads 1x4 in the rendered document.
    6. Set `RAMS_HAZARD_LIBRARY_TIERING=false` in the live `.env`, clear config cache, regenerate 21CQ30960 again. Confirm the register now contains only whatever was explicitly picked (or is empty if nothing was explicitly picked) — confirm it does NOT resurrect the old fixed 11/7-item baseline. Then set the flag back to `true` (or remove the line to fall back to the default), clear config cache, and confirm normal behaviour returns.
  </how-to-verify>
  <resume-signal>Type "approved" once 21CQ30960 has been visually spot-checked and the flag toggle has been proven reversible without a redeploy, or describe what needs fixing.</resume-signal>
  <verify>
    <automated>MISSING — manual-only per CONTEXT.md; no automated command can verify production data against a human's judgement of 21CQ30960's real scope</automated>
  </verify>
  <done>21CQ30960 regenerated live shows only scope-supported hazards with Working at height at 1x4; RAMS_HAZARD_LIBRARY_TIERING has been toggled off and back on live without a redeploy and behaved as documented; human has typed "approved".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Live deploy (`stcav` user) -> production DB | Standard deploy trust boundary, unchanged by this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-26-08 | Elevation of Privilege | Deploying as root instead of `stcav` | mitigate | Task 3's how-to-verify explicitly states "never root" per CONTEXT.md's specifics section; this is a process control, not a code control |
| T-26-SC | Tampering | Composer installs | accept | No new packages in this plan |
</threat_model>

<verification>
`php artisan test --filter=Hazard` exits 0. `php artisan test --filter=MethodStatementAssociatedRisksTest` exits 0. `php artisan test --filter=WorkingAtHeightResidualScoreTest` exits 0. Full suite green modulo the 2 pre-existing unrelated failures. Human checkpoint approved against live 21CQ30960.
</verification>

<success_criteria>
All 4 phase requirements (HAZ-01..04) are proven through the live DOCX render path and against real production data, not fixtures alone. Reversibility is demonstrated live, not just asserted in a unit test. The phase is ready for `/gsd:verify-work`.
</success_criteria>

<output>
Create `.planning/phases/26-hazard-library-structural-inversion/26-06-SUMMARY.md` when done
</output>
