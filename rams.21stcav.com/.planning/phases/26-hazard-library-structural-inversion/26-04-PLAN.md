---
phase: 26-hazard-library-structural-inversion
plan: 04
type: execute
wave: 2
depends_on: ["26-01", "26-02", "26-03"]
files_modified:
  - app/Core/Modules/KnowledgeLibrary/HazardLibraryService.php
  - app/Services/RiskTemplateResolverService.php
  - app/Services/RamsExtractionDraftBuilderService.php
  - app/Services/RamsBuilderService.php
  - tests/Feature/Rams/HazardInjectionPathsRemovedGuardTest.php
autonomous: true
requirements: [HAZ-02]
user_setup: []

must_haves:
  truths:
    - "A quote-import extraction with no drilling/ceiling/display/mains/void/strip-out/first-fix signals in its equipment or scope text produces a hazards draft containing only the 4 always-tier hazards"
    - "A quote-import extraction with ceiling-mounted display equipment produces a hazards draft that also includes Working at height and Manual handling"
    - "Explicit engineer-picked hazard names (manual create form) are always present in the resolved set, merged with (not replaced by) tier-matched hazards"
    - "No live code path references HazardLibraryService::MANDATORY_KEYWORDS or config('rams_tier1.baseline_hazards')"
    - "Setting RAMS_HAZARD_LIBRARY_TIERING=false in .env causes the resolver to return only explicit picks, with zero auto-population, and never re-introduces the old fixed baseline"
  artifacts:
    - path: "app/Services/RiskTemplateResolverService.php"
      provides: "buildHazards()/resolveHazards() wired to HazardIncludeWhenResolver, merged with explicit picks"
    - path: "app/Services/RamsExtractionDraftBuilderService.php"
      provides: "scope-narrative construction + numeric score + needs_confirmation passthrough into extracted_data['hazards']"
    - path: "tests/Feature/Rams/HazardInjectionPathsRemovedGuardTest.php"
      provides: "structural guard — zero references to the removed dead code across app/ and tests/"
  key_links:
    - from: "app/Services/RamsExtractionDraftBuilderService.php"
      to: "app/Services/RiskTemplateResolverService.php"
      via: "resolve($activities, $drillingRequired, null, [], [], $scopeNarrative)"
      pattern: "scope_narrative"
    - from: "app/Services/RiskTemplateResolverService.php"
      to: "app/Services/Rams/HazardIncludeWhenResolver.php"
      via: "includeWhenResolver->resolve($library, $signals)"
      pattern: "HazardIncludeWhenResolver"
---

<objective>
Remove path #5 — the `HazardLibraryService::MANDATORY_KEYWORDS`/`mandatoryBaseline()`/`mergeWithMandatory()` machinery that fires whenever `RiskTemplateResolverService::buildHazards()` is called with no explicit hazard names — and replace it with the tiered `HazardIncludeWhenResolver` (Plan 26-02) evaluated against real captured job signals. This is the plan that actually makes a fresh quote-import RAMS start conditional instead of unconditional.

Purpose: Per the CONTEXT.md correction, path #5 is narrower than originally scoped (fires only on empty explicit names, not "always"), but it is still the ONLY place that decides what auto-populates the two call sites that matter — `RamsExtractionDraftBuilderService::build()` (the quote-import auto-seed, which passes zero hazard names today) and `RamsBuilderService::runPipeline()` (manual create form + regenerate). This plan wires the replacement into both.
Output: HazardLibraryService dead code removed; RiskTemplateResolverService produces a merged explicit-pick + tiered-match hazard set; both call sites build and forward a scope-narrative string; a structural dead-path guard test proves no remnants remain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-hazard-library-structural-inversion/26-CONTEXT.md
@.planning/phases/26-hazard-library-structural-inversion/26-PATTERNS.md
@app/Core/Modules/KnowledgeLibrary/HazardLibraryService.php
@app/Services/RiskTemplateResolverService.php
@app/Services/RamsExtractionDraftBuilderService.php
@app/Services/EquipmentClassifierService.php
@tests/Feature/Rams/DeadPathRemovalGuardTest.php

<interfaces>
`HazardIncludeWhenResolver::resolve(Collection $library, array $signals): Collection` (Plan 26-02) — `$signals = ['activities' => string[], 'drilling_required' => bool, 'scope_narrative' => string]`. Returns HazardTemplate instances with transient `->needs_confirmation` (bool) and `->match_tier` (string) attributes attached.

Current call graph this plan changes (verified 2026-08-23):
- `app/Services/RamsExtractionDraftBuilderService.php:45-52` — `build()` calls `$this->riskResolver->resolve($classified['activities'], $classified['drilling_required'] ?? false)` with NO hazard names, NO scope text. This is the dominant auto-seed call site — its output becomes `extracted_data['hazards']`, the rows an engineer first sees on the review screen.
- `app/Services/RamsExtractionDraftBuilderService.php:149-165` — `buildHazards(array $hazards)` currently downgrades numeric `pre_likelihood`/`pre_severity` into a `risk` Low/Medium/High string via `riskLabel()`. This plan stops that downgrade — the numeric fields pass through unchanged, plus two new boolean keys.
- `app/Services/RamsBuilderService.php:684-706` — `runPipeline()` calls `$this->riskResolver->resolve($classified['activities'], $classified['drilling_required'] ?? false, $record->user_id, $formData['hazards'] ?? [], $formData['persons_at_risk'] ?? [])`. `$formData['hazards']` carries explicit engineer picks from the manual create form when present.
- `app/Services/RamsBuilderService.php:397-444` — `reviewedToRisk()` calls `$this->hazardLibrary->resolveFromSeeds($userId ?? 0, [$name], false)` per hazard row already in `reviewed_data['hazards']` — this is a score/control LOOKUP only (by then the register is already fixed by the review screen), not a re-run of tiered resolution. Only the trailing `false` argument needs to be dropped here to match the simplified `resolveFromSeeds()` signature — do not touch this method's broader logic in this plan (Plan 26-05 owns its HAZ-04 extension).
- `RiskTemplateResolverService::buildHazards()` currently outputs rows with keys `id, hazard, persons_at_risk, pre_likelihood, pre_severity, controls, post_likelihood, post_severity` (`:160-176`) — this shape is ALREADY numeric; only a new `needs_confirmation` key needs adding, not a restructure.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HazardLibraryService — remove mandatory-baseline dead code</name>
  <files>app/Core/Modules/KnowledgeLibrary/HazardLibraryService.php</files>
  <read_first>app/Core/Modules/KnowledgeLibrary/HazardLibraryService.php</read_first>
  <action>
    Delete `MANDATORY_KEYWORDS` (`:36-44`), `mandatoryBaseline()` (`:195-249`), `mergeWithMandatory()` (`:254-273`). Keep `fuzzyMatch()` (`:285-318`) exactly as-is — it is PRESERVED, not deleted, because it remains the mechanism `resolveFromSeeds()` uses today for the existing engineer-selection/seed-resolution path. 2026-08-23 correction (user decision, supersedes RESEARCH.md's Open Question 1 and its Architectural Responsibility Map): `fuzzyMatch()` is NOT a tier-3 AI-judgement plug-in point. Tier 3 is handled entirely by Plan 26-02's `HazardIncludeWhenResolver` (always-include + always-`needs_confirmation=true` + keyword pre-tick, no AI call) — do not wire `fuzzyMatch()`, `resolveFromSeeds()`, or any `app/Core/AI/Prompts/*` prompt into tier-3 evaluation in this or any future phase; `CLAUDE.md`'s AI-usage constraint rules out an AI pass deciding hazard inclusion.

    Simplify `resolveByIds(int $userId, array $ids)` — drop the `bool $includeMandatory = true` parameter; body becomes: if `$ids` empty, return `collect()`; else return `HazardTemplate::visibleTo($userId)->whereIn('id', $ids)->get()`.

    Simplify `resolveFromSeeds(int $userId, array $seeds)` — drop the `bool $includeMandatory = true` parameter; body keeps the existing fuzzy-match loop over `$seeds` (`:83-92`) and the unmatched-seed pseudo-object wrapping (`:97-110`) exactly as-is, but the line `$resolved = $includeMandatory ? $this->mergeWithMandatory($userId, $matched) : $matched;` becomes simply `$resolved = $matched;`.

    Update the class docblock (`:9-29`) — remove "Guarantee mandatory baseline hazards always appear in every RAMS" from the Responsibilities list and the two usage-example comment lines that reference mandatory behaviour.
  </action>
  <verify>
    <automated>grep -c "MANDATORY_KEYWORDS\|mandatoryBaseline\|mergeWithMandatory" app/Core/Modules/KnowledgeLibrary/HazardLibraryService.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MANDATORY_KEYWORDS\|mandatoryBaseline\|mergeWithMandatory" app/Core/Modules/KnowledgeLibrary/HazardLibraryService.php` returns 0
    - `grep -c "function fuzzyMatch" app/Core/Modules/KnowledgeLibrary/HazardLibraryService.php` returns 1 (kept)
    - `grep -n "function resolveFromSeeds" app/Core/Modules/KnowledgeLibrary/HazardLibraryService.php` shows a 2-parameter signature (`int $userId, array $seeds`), no `$includeMandatory`
    - `grep -n "function resolveByIds" app/Core/Modules/KnowledgeLibrary/HazardLibraryService.php` shows a 2-parameter signature, no `$includeMandatory`
  </acceptance_criteria>
  <done>All mandatory-baseline machinery is deleted; resolveByIds/resolveFromSeeds have simplified 2-parameter signatures; fuzzyMatch is untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RiskTemplateResolverService — wire the tiered resolver, merge with explicit picks</name>
  <files>app/Services/RiskTemplateResolverService.php</files>
  <read_first>app/Services/RiskTemplateResolverService.php, app/Services/Rams/HazardIncludeWhenResolver.php</read_first>
  <behavior>
    - Test 1: `resolve([], false, null, [], [])` (no activities, no drilling, no names, no scope narrative — i.e. the auto-seed call with a genuinely blank signal set) returns exactly the 4 always-tier hazard rows in `hazards`, given a seeded library (feature test, RefreshDatabase, seed `HazardTemplateSeeder` first).
    - Test 2: `resolve(['ceiling_works'], false, null, [], [])` additionally includes Working at height / Manual handling / Restricted access and ceiling voids rows (whichever tier-2 signal keys map to the `ceiling_works` activity).
    - Test 3: `resolve([], false, $userId, ['Manual handling'], [])` includes the explicitly-named hazard even when no tier condition matches it, AND still includes the 4 always-tier hazards (merge, not replace).
    - Test 4: with `config(['rams_tier1.hazard_tiering_enabled' => false])`, `resolve([], false, $userId, ['Manual handling'], [])` returns ONLY the explicitly-named hazard — zero auto-population, and critically zero old-baseline titles (proves the reversibility flag degrades to manual-only, never to the old fixed 11).
  </behavior>
  <action>
    Inject `HazardIncludeWhenResolver $includeWhenResolver` into the constructor (add as a new readonly property alongside the existing `HazardLibraryService $hazardLibrary`).

    Change `resolve()`'s public signature (`:71-77`) to accept a 6th optional parameter `string $scopeNarrative = ''`. Forward `$activities`, `$drillingRequired`, and `$scopeNarrative` into `buildHazards()` (currently only `$userId, $hazardNames, $personsAtRisk` are passed at `:80`).

    Change `buildHazards()`'s signature (`:140`) to accept `array $activities` and `bool $drillingRequired` and `string $scopeNarrative` as new parameters, forwarded into `resolveHazards()`.

    Rewrite `resolveHazards()` (`:189-194`): resolve explicit picks via `$this->hazardLibrary->resolveFromSeeds($userId, $names)` (no longer passing a mandatory flag — matches Task 1's simplified signature) into `$explicit`. If `config('rams_tier1.hazard_tiering_enabled', true)` is true, additionally fetch `HazardTemplate::visibleTo($userId)->get()` and call `$this->includeWhenResolver->resolve($library, ['activities' => $activities, 'drilling_required' => $drillingRequired, 'scope_narrative' => $scopeNarrative])`, then merge into `$explicit` — for each tiered match, skip it if `$explicit` already contains an item with the same `id` (when `id` is not null) or the same lower-cased `name` (for pseudo-objects with null `id`), otherwise append it. Return the merged Collection. When the flag is false, return `$explicit` alone (zero auto-population — the reversibility guarantee).

    In `buildHazards()`'s row-construction loop (`:160-176`), add `'needs_confirmation' => (bool) ($h->needs_confirmation ?? false)` to the emitted row array (read the dynamic property Plan 26-02 attaches; default false for explicit-pick items that never went through the tiered resolver).
  </action>
  <verify>
    <automated>php artisan test --filter=RiskTemplateResolverService</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=RiskTemplateResolverService` exits 0 covering all 4 behaviors
    - `grep -c "includeWhenResolver" app/Services/RiskTemplateResolverService.php` returns 2 or more (constructor property + usage)
    - `grep -c "hazard_tiering_enabled" app/Services/RiskTemplateResolverService.php` returns 1
    - `grep -c "needs_confirmation" app/Services/RiskTemplateResolverService.php` returns 1
  </acceptance_criteria>
  <done>RiskTemplateResolverService merges explicit engineer picks with tier-matched hazards; the env flag degrades to manual-only (never the old baseline) when disabled; needs_confirmation flows into every output row.</done>
</task>

<task type="auto">
  <name>Task 3: Wire call sites — scope narrative, numeric passthrough, structural guard test</name>
  <files>app/Services/RamsExtractionDraftBuilderService.php, app/Services/RamsBuilderService.php, tests/Feature/Rams/HazardInjectionPathsRemovedGuardTest.php</files>
  <read_first>app/Services/RamsExtractionDraftBuilderService.php, app/Services/RamsBuilderService.php, tests/Feature/Rams/DeadPathRemovalGuardTest.php</read_first>
  <action>
    In `RamsExtractionDraftBuilderService::build()` (`:45-52`), before calling `$this->riskResolver->resolve(...)`, build `$scopeNarrative = trim(implode(' ', array_filter([(string) ($parsed['works_summary'] ?? ''), (string) ($formData['works_description'] ?? ''), implode(' ', array_column($parsed['equipment'] ?? [], 'description'))])));` and pass it as the resolver's 6th argument: `$this->riskResolver->resolve($classified['activities'], $classified['drilling_required'] ?? false, null, [], [], $scopeNarrative)`.

    In `buildHazards(array $hazards)` (`:149-165`), stop computing `risk` via `riskLabel()`. Instead emit `'pre_likelihood' => max(1, (int) ($h['pre_likelihood'] ?? 3))`, `'pre_severity' => max(1, (int) ($h['pre_severity'] ?? 3))`, `'post_likelihood' => max(1, (int) ($h['post_likelihood'] ?? 1))`, `'post_severity' => max(1, (int) ($h['post_severity'] ?? 2))`, `'needs_confirmation' => (bool) ($h['needs_confirmation'] ?? false)`, `'score_reviewed' => false` (nothing has been human-reviewed at extraction time), alongside the existing `activity_key`/`hazard`/`control_measures` keys. Remove the now-unused `riskLabel()` private method if nothing else calls it (grep first to confirm).

    In `RamsBuilderService::runPipeline()` (`:684-706`), before the `$this->riskResolver->resolve(...)` call, build the same `$scopeNarrative` expression (using `$parsed['works_summary']`/`$formData['works_description']`/`$parsed['equipment']` — the variables already in scope at this point in the method) and pass it as the 6th argument.

    In `RamsBuilderService::reviewedToRisk()` (`:410`), drop the trailing `false` argument from `$this->hazardLibrary->resolveFromSeeds($userId ?? 0, [$name], false)` — it becomes `resolveFromSeeds($userId ?? 0, [$name])`, matching Task 1's simplified signature.

    Create `tests/Feature/Rams/HazardInjectionPathsRemovedGuardTest.php` modeled exactly on `tests/Feature/Rams/DeadPathRemovalGuardTest.php`'s two-test structure (static substring scan + filesystem check), but targeting: forbidden strings `MANDATORY_KEYWORDS`, `mandatoryBaseline`, `mergeWithMandatory`, `rams_tier1.baseline_hazards` scanned across all `*.php` under `app/` and `tests/` (excluding this guard file itself via the same `realpath(__FILE__)` whitelist pattern), PLUS a filesystem check that is a no-op here (no files were deleted wholesale in this phase, only code removed in place) — so include only the substring-scan test, not the file-existence test.
  </action>
  <verify>
    <automated>php artisan test --filter=HazardInjectionPathsRemovedGuardTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=HazardInjectionPathsRemovedGuardTest` exits 0
    - `php artisan test --filter=RamsExtractionDraftBuilderService` exits 0 (or the relevant existing test class covering this file, if named differently — locate via grep before writing/running)
    - `grep -c "riskLabel" app/Services/RamsExtractionDraftBuilderService.php` returns 0 (method removed) OR the method is confirmed still referenced elsewhere before keeping it
    - `grep -c "scopeNarrative\|scope_narrative" app/Services/RamsExtractionDraftBuilderService.php app/Services/RamsBuilderService.php` returns 2 or more combined
    - `grep -n "resolveFromSeeds(\$userId ?? 0, \[\$name\])" app/Services/RamsBuilderService.php` matches (trailing arg dropped)
  </acceptance_criteria>
  <done>Both upstream call sites build and forward a scope narrative; extraction-time hazard rows carry numeric scores + needs_confirmation + score_reviewed instead of a Low/Medium/High bucket; a structural guard test proves zero references to any of the deleted symbols remain anywhere in app/ or tests/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Parsed quote text / form_data -> scope_narrative -> HazardIncludeWhenResolver | Already-parsed/validated text, not new raw user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-26-02 | Tampering/correctness | Deterministic keyword matching in the wired resolver path | mitigate | Fixed vocabulary only (Plan 26-02's const arrays), consumed unchanged here — no new matching logic introduced in this plan |
| T-26-05 | Repudiation | `HazardInjectionPathsRemovedGuardTest` false negative if a forbidden string is reintroduced via string concatenation | accept | Same accepted risk profile as the existing `DeadPathRemovalGuardTest` precedent this test is modeled on — a deliberate obfuscation to defeat a substring guard is not a realistic threat for an internal-only codebase |
| T-26-SC | Tampering | Composer installs | accept | No new packages in this plan |
</threat_model>

<verification>
`php artisan test --filter=Hazard` exits 0. `php artisan test` (full suite) shows no new failures beyond the 2 pre-existing unrelated ones documented in RESEARCH.md.
</verification>

<success_criteria>
Path #5 is fully replaced by tiered include-when resolution merged with explicit picks; both upstream call sites forward real captured signals; HAZ-02 is now provably satisfied end-to-end at the service layer (extraction draft + manual/regenerate pipeline).
</success_criteria>

<output>
Create `.planning/phases/26-hazard-library-structural-inversion/26-04-SUMMARY.md` when done
</output>
