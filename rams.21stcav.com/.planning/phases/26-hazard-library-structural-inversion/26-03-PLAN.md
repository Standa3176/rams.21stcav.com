---
phase: 26-hazard-library-structural-inversion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - config/rams_tier1.php
  - app/Services/Rams/Tier1RamsDefaultsService.php
  - app/Support/Rams/SectionComposers/RiskAssessmentComposer.php
  - resources/views/pdf/rams.blade.php
  - resources/views/pdf/rams-v2.blade.php
  - tests/Feature/Rams/Tier1BaselineHazardsRenderTest.php
autonomous: true
requirements: [HAZ-02, HAZ-03]
user_setup: []

must_haves:
  truths:
    - "config('rams_tier1.baseline_hazards') no longer exists anywhere in the codebase"
    - "An empty $data['hazards'] array reaching Tier1RamsDefaultsService, RiskAssessmentComposer, rams.blade.php, or rams-v2.blade.php stays empty — none of the four re-injects the fixed 11"
    - "A new config('rams_tier1.hazard_tiering_enabled') flag exists, independent of the existing config('rams_tier1.enabled'), so disabling hazard auto-population does not also disable standards_references or coshh_baseline"
    - "config('rams_tier1.standards_references') and config('rams_tier1.coshh_products') are unchanged (out of scope)"
  artifacts:
    - path: "config/rams_tier1.php"
      provides: "baseline_hazards array removed; hazard_tiering_enabled flag added; standards_references/coshh_products untouched"
    - path: "app/Services/Rams/Tier1RamsDefaultsService.php"
      provides: "hazards fallback branch removed; standards_references/coshh_baseline branches untouched"
  key_links:
    - from: "config/rams_tier1.php"
      to: "app/Services/Rams/HazardIncludeWhenResolver.php (Plan 26-04 consumer)"
      via: "env('RAMS_HAZARD_LIBRARY_TIERING', true) -> config('rams_tier1.hazard_tiering_enabled')"
      pattern: "hazard_tiering_enabled"
---

<objective>
Kill injection paths #1 (`Tier1RamsDefaultsService`), #2 (`rams.blade.php` — the LIVE PDF fallback), #3 (`rams-v2.blade.php`), and #4 (`RiskAssessmentComposer`) — the four render/build-time places that re-inject the fixed 11 `baseline_hazards` whenever the hazards array is empty. Introduce a new, narrower env-gated kill-switch so the whole hazard-tiering mechanism (delivered across this phase) can be turned off with one `.env` edit without also disabling standards references or the COSHH baseline.

Purpose: Per CONTEXT.md's own correction, path #2 is the LIVE PDF template and independently re-injects the fixed 11 regardless of what generation-time services decide — removing only paths #1/#5 (as REQUIREMENTS.md originally named) would leave the live template silently defeating the whole phase. This plan is the reversibility mechanism the phase's live-validation constraint requires.
Output: 4 files with their baseline-hazard fallback branches deleted; a new `hazard_tiering_enabled` config key; `Tier1BaselineHazardsRenderTest` rewritten to assert the new (opposite) behaviour.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-hazard-library-structural-inversion/26-CONTEXT.md
@.planning/phases/26-hazard-library-structural-inversion/26-PATTERNS.md
@config/rams_tier1.php
@app/Services/Rams/Tier1RamsDefaultsService.php
@app/Support/Rams/SectionComposers/RiskAssessmentComposer.php
@tests/Feature/Rams/Tier1BaselineHazardsRenderTest.php

<interfaces>
Exact removal targets (verified by direct read 2026-08-23):
- `config/rams_tier1.php:52-232` — the entire `'baseline_hazards' => [...]` array (11 entries). Leave `'enabled'` (`:36`), `'coshh_products'` (`:246-325`), `'standards_references'` (`:337-399`), `'av_prompt_bullets'` (`:415-422`) untouched.
- `app/Services/Rams/Tier1RamsDefaultsService.php:69-71` — the `if (empty($data['hazards']) || ! is_array($data['hazards'])) { $data['hazards'] = (array) config('rams_tier1.baseline_hazards', []); }` block. Leave the `standards_references` block (`:74-76`) and `coshh_baseline` block (`:78-82`) untouched.
- `app/Support/Rams/SectionComposers/RiskAssessmentComposer.php:34-38` — the `$raw = (array) ($rd['hazards'] ?? ($gd['hazards'] ?? ($this->config->get('rams_tier1.enabled', true) ? $this->config->get('rams_tier1.baseline_hazards', []) : [])));` chain. Replace with `$raw = (array) ($rd['hazards'] ?? $gd['hazards'] ?? []);` — no config fallback survives.
- `resources/views/pdf/rams.blade.php:309-317` (comment header through the `if` block) — the LIVE PDF template's render-time fallback. Delete the whole `if (empty($hazards) && config('rams_tier1.enabled', true)) { $hazards = (array) config('rams_tier1.baseline_hazards', []); }` block and its preceding explanatory comment block.
- `resources/views/pdf/rams-v2.blade.php:~360-373` — identical structure to rams.blade.php's block, same deletion (not live, but must not silently resurrect the 11 if `RAMS_UNIFIED_COMPOSER` is ever flipped on).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: New narrower kill-switch + config/Tier1RamsDefaultsService cleanup</name>
  <files>config/rams_tier1.php, app/Services/Rams/Tier1RamsDefaultsService.php</files>
  <read_first>config/rams_tier1.php, app/Services/Rams/Tier1RamsDefaultsService.php</read_first>
  <action>
    In `config/rams_tier1.php`, add a new top-level key immediately after `'enabled'` (`:36`): `'hazard_tiering_enabled' => env('RAMS_HAZARD_LIBRARY_TIERING', true),` with a docblock comment explaining: this flag gates ONLY the Phase 26 hazard include-when auto-population (Plan 26-04's `HazardIncludeWhenResolver` wiring) — it is intentionally decoupled from the existing `'enabled'` flag because that flag also gates `standards_references` and `coshh_products`/`coshh_baseline`, which Phase 26 does not touch. Setting `RAMS_HAZARD_LIBRARY_TIERING=false` disables auto-population only; explicit engineer hazard picks are unaffected; the register does NOT fall back to the old fixed baseline — it simply contains only what was explicitly picked. Then delete the entire `'baseline_hazards' => [ ... ]` array (`:52-232`). Leave `'coshh_products'`, `'standards_references'`, `'av_prompt_bullets'` exactly as they are.

    In `app/Services/Rams/Tier1RamsDefaultsService.php`, delete the hazards-branch block (`:65-71`, including its explanatory comment) inside `injectDefaultsIntoRamsData()`. Update the method's own docblock (`:32-37`, the "Non-clobbering behaviour" list) to remove the `hazards` bullet, and add a one-line note that hazard population is now handled entirely by `RiskTemplateResolverService`/`HazardIncludeWhenResolver` before this method ever runs. Leave the `standards_references` block and `coshh_baseline` block completely unchanged.
  </action>
  <verify>
    <automated>grep -c "baseline_hazards" config/rams_tier1.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "baseline_hazards" config/rams_tier1.php` returns 0
    - `grep -c "hazard_tiering_enabled" config/rams_tier1.php` returns 1
    - `grep -c "'coshh_products'\|'standards_references'\|'av_prompt_bullets'" config/rams_tier1.php` returns 3 (all three untouched keys still present)
    - `grep -c "baseline_hazards" app/Services/Rams/Tier1RamsDefaultsService.php` returns 0
    - `grep -c "standards_references\|coshh_baseline" app/Services/Rams/Tier1RamsDefaultsService.php` returns 2 or more (both untouched blocks still present)
  </acceptance_criteria>
  <done>config/rams_tier1.php has no baseline_hazards content and a new independent hazard_tiering_enabled flag; Tier1RamsDefaultsService no longer injects the fixed 11 into an empty hazards array; standards/COSHH paths are provably untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the 3 render-time fallbacks (paths #2, #3, #4)</name>
  <files>app/Support/Rams/SectionComposers/RiskAssessmentComposer.php, resources/views/pdf/rams.blade.php, resources/views/pdf/rams-v2.blade.php</files>
  <read_first>app/Support/Rams/SectionComposers/RiskAssessmentComposer.php, resources/views/pdf/rams.blade.php, resources/views/pdf/rams-v2.blade.php</read_first>
  <action>
    In `RiskAssessmentComposer::compose()`, replace the `$raw = (array) (...)` config-fallback chain with `$raw = (array) ($rd['hazards'] ?? $gd['hazards'] ?? []);`. Update the class docblock (`:9-14`) to remove the "last-resort falls back to config('rams_tier1.baseline_hazards')" sentence.

    In `resources/views/pdf/rams.blade.php`, delete the `260712-twi Task 2` comment block and the `if (empty($hazards) && config('rams_tier1.enabled', true)) { ... }` block that follows it — THIS IS THE LIVE TEMPLATE, the highest-priority removal in the whole phase. After deletion, `$hazards` stays exactly whatever `$data['hazards']` supplied. The existing `@if(! empty($hazards)) ... @else <p class="note-text">No hazards identified.</p> @endif` branch (`:1305-1363`) needs no changes — it already handles the true-empty case correctly.

    In `resources/views/pdf/rams-v2.blade.php`, delete the identical block at its corresponding location (same comment header, same `if` structure).
  </action>
  <verify>
    <automated>grep -c "baseline_hazards" app/Support/Rams/SectionComposers/RiskAssessmentComposer.php resources/views/pdf/rams.blade.php resources/views/pdf/rams-v2.blade.php</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "baseline_hazards" app/Support/Rams/SectionComposers/RiskAssessmentComposer.php resources/views/pdf/rams.blade.php resources/views/pdf/rams-v2.blade.php` returns 0 for all three files combined
    - `grep -rn "rams_tier1.enabled" resources/views/pdf/rams.blade.php` returns 0 matches related to hazards (any remaining `rams_tier1.enabled` reference in this file, if present elsewhere for a different purpose, is unrelated to hazards — verify no hazards-related usage survives)
  </acceptance_criteria>
  <done>None of the 3 render-time paths re-inject the fixed 11; an empty hazards array reaches the "No hazards identified." branch untouched in both PDF templates and produces an empty hazards array from the composer.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite Tier1BaselineHazardsRenderTest — assert the opposite of the removed behaviour</name>
  <files>tests/Feature/Rams/Tier1BaselineHazardsRenderTest.php</files>
  <read_first>tests/Feature/Rams/Tier1BaselineHazardsRenderTest.php</read_first>
  <action>
    Rename `test_baseline_hazards_render_when_reviewed_data_hazards_is_empty` to `test_no_baseline_injected_when_hazards_empty` and rewrite its body: with `config(['rams_tier1.enabled' => true])` and `$this->renderWith($this->baseData(['hazards' => []]))`, assert `assertStringContainsString('No hazards identified.', $html)` and `assertStringNotContainsString('Working at Height', $html)` (old Title-Case baseline string — case-sensitive comparison is the deliberate canary distinguishing old baseline vocabulary from the new sentence-case skill vocabulary) and `assertStringNotContainsString('Manual Handling of AV Equipment', $html)` and `assertStringNotContainsString('Electrical Isolation', $html)`.

    Keep `test_engineer_supplied_hazards_render_verbatim_and_baseline_is_not_injected` unchanged (still valid — engineer values always win, and there is no baseline left to not-inject either way, so this becomes a simpler but still-meaningful regression guard).

    Keep `test_disabled_flag_leaves_hazards_empty_no_baseline_injected` unchanged (still valid — proves the render layer is stable regardless of `rams_tier1.enabled`'s value now that no fallback reads it for hazards).

    Update the class docblock (`:8-24`) to describe the new contract: the render-time fallback no longer exists; hazards render exactly what `$data['hazards']` contains; Phase 26 supersedes the quick-task-260712-twi behaviour these tests originally locked.
  </action>
  <verify>
    <automated>php artisan test --filter=Tier1BaselineHazardsRenderTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=Tier1BaselineHazardsRenderTest` exits 0
    - Test file still contains exactly 3 test methods
    - `grep -c "No hazards identified" tests/Feature/Rams/Tier1BaselineHazardsRenderTest.php` returns 2 or more (appears in both the rewritten test 1 and the unchanged test 3)
  </acceptance_criteria>
  <done>All 3 tests in this file pass against the new no-fallback behaviour; test 1 now proves the OPPOSITE of what it proved before this phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `.env` -> config('rams_tier1.*') | Deploy-time configuration, not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-26-04 | Denial of Service (data-quality) | Removal of the 4 render-time fallbacks | mitigate | A RAMS with zero matching hazards now renders "No hazards identified." instead of silently padding with 11 irrelevant hazards — this is the INTENDED behaviour per HAZ-02, verified by the rewritten Tier1BaselineHazardsRenderTest; the 4 `always` tier-1 hazards (seeded in Plan 26-01, wired in Plan 26-04) mean a fully-empty register only occurs if the resolver itself is disabled |
| T-26-SC | Tampering | Composer installs | accept | No new packages in this plan |
</threat_model>

<verification>
`php artisan test --filter=Tier1BaselineHazardsRenderTest` exits 0. `grep -rn "baseline_hazards" config/ app/ resources/` returns 0 matches.
</verification>

<success_criteria>
Paths #1/#2/#3/#4 no longer re-inject the fixed 11 under any circumstance; a new, independent `hazard_tiering_enabled` flag exists ready for Plan 26-04 to consume, satisfying the phase's env-flag-reversibility constraint without coupling to standards/COSHH.
</success_criteria>

<output>
Create `.planning/phases/26-hazard-library-structural-inversion/26-03-SUMMARY.md` when done
</output>
