---
phase: 26-hazard-library-structural-inversion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Rams/HazardIncludeWhenResolver.php
  - tests/Unit/Services/Rams/HazardIncludeWhenResolverTest.php
autonomous: true
requirements: [HAZ-02]
user_setup: []

must_haves:
  truths:
    - "Given a HazardTemplate collection and empty signals, only include_when='always' rows are returned"
    - "Given a tier-2 signal that matches a hazard's signal key, that hazard is returned with needs_confirmation=false"
    - "Given no positive keyword match for a tier-3 hazard, that hazard is STILL returned, with needs_confirmation=true (D-06 — never silently dropped)"
    - "A row with include_when=null is never returned by the tiered resolver (D-04 — manual-only, explicit-pick-only)"
  artifacts:
    - path: "app/Services/Rams/HazardIncludeWhenResolver.php"
      provides: "resolve(Collection $library, array $signals): Collection — tier 1/2/3 include-when evaluation"
    - path: "tests/Unit/Services/Rams/HazardIncludeWhenResolverTest.php"
      provides: "one test per tier plus the D-06 always-include-when-unconfirmed case"
  key_links:
    - from: "app/Services/Rams/HazardIncludeWhenResolver.php"
      to: "app/Models/HazardTemplate.php"
      via: "reads ->include_when, writes transient ->needs_confirmation / ->match_tier attributes"
      pattern: "include_when"
---

<objective>
Build the new tiered include-when evaluator as a standalone, unit-testable service: given the full visible hazard library and the job's captured signals, return the subset that should auto-populate a fresh RAMS, each tagged with how confidently it matched. This is pure logic with no DB writes and no dependency on the seeded content — it can be fully proven in isolation before anything wires it in (Plan 26-04).

Purpose: HAZ-02's inversion ("register starts empty and is added to") needs one place that implements D-05's three-tier rule and D-06's "include and flag, never silently drop" rule, so `RiskTemplateResolverService` (Plan 26-04) has a single, tested call to make.
Output: `HazardIncludeWhenResolver` class + unit tests covering every tier and the D-06 fallback.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/26-hazard-library-structural-inversion/26-CONTEXT.md
@.planning/phases/26-hazard-library-structural-inversion/26-RESEARCH.md
@app/Services/RiskTemplateResolverService.php
@app/Core/Modules/KnowledgeLibrary/HazardLibraryService.php

<interfaces>
`HazardTemplate` (`app/Models/HazardTemplate.php`) relevant readable attributes: `id`, `name`, `include_when` (string|null, per the convention Plan 26-01 establishes: `'always'` / `'signal:<key>'` / `'ai:<key>'` / `null`). Eloquent models accept dynamic attribute assignment via `__set`/`__get` even for keys not in `$fillable`/`$casts` — this resolver uses that to attach transient `needs_confirmation` (bool) and `match_tier` (string) properties onto the returned instances WITHOUT calling `->save()`, mirroring the existing dynamic-pseudo-object pattern already used in `HazardLibraryService::resolveFromSeeds()`'s unmatched-seed wrapping (`app/Core/Modules/KnowledgeLibrary/HazardLibraryService.php:98-110`).

`RiskTemplateResolverService`'s existing fixed-vocabulary constant-map discipline to follow (`app/Services/RiskTemplateResolverService.php:29-58`): `PPE_ACTIVITY_MAP`, `ACCESS_EQUIPMENT_MAP` are `private const array<string,string[]>` keyed by `EquipmentClassifierService` activity keys (`display_installation`, `ceiling_works`, `av_rack`, `audio_installation`, `video_conferencing`, `control_systems`, `cable_management`, `structured_cabling`). This resolver's tier-2 evaluation reuses these same activity keys as one of its two signal types (the other being narrative keyword scan).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HazardIncludeWhenResolver — tier 1/2/3 evaluation</name>
  <files>app/Services/Rams/HazardIncludeWhenResolver.php</files>
  <read_first>.planning/phases/26-hazard-library-structural-inversion/26-CONTEXT.md, app/Services/RiskTemplateResolverService.php, app/Core/Modules/KnowledgeLibrary/HazardLibraryService.php</read_first>
  <behavior>
    - Test 1: `resolve($library, ['activities'=>[], 'drilling_required'=>false, 'scope_narrative'=>''])` on a library containing one row per include_when value returns exactly the 4 `always` rows, each with `needs_confirmation=false`, `match_tier='always'`.
    - Test 2: with `signals['activities'] = ['ceiling_works']`, the row whose `include_when='signal:mounting_above_reach'` (or `'signal:ceiling_void_access'` or `'signal:display_mount_or_rack'`) is included with `needs_confirmation=false`, `match_tier='deterministic'`.
    - Test 3: with `signals['drilling_required'] = true`, rows `'signal:drilling_or_percussive'`, `'signal:any_penetration'`, `'signal:any_drilling'` are all included, `needs_confirmation=false`.
    - Test 4: with `signals['scope_narrative'] = 'new mains isolation required before the rack is powered'`, the row `'signal:mains_connection'` is included (keyword match on "mains").
    - Test 5: with empty signals, every `'ai:<key>'` row is STILL included (D-06), each with `needs_confirmation=true`, `match_tier='ai_unconfirmed'`.
    - Test 6: with `signals['scope_narrative'] = 'warehouse installation, loading bay access required'`, the row `'signal:'` — no, the row `'ai:vehicle_plant'` is included with `needs_confirmation=false`, `match_tier='ai_confirmed'` (positive keyword pre-check overrides the unconfirmed default).
    - Test 7: a row with `include_when=null` is never returned regardless of signals.
  </behavior>
  <action>
    Create `app/Services/Rams/HazardIncludeWhenResolver.php`, namespace `App\Services\Rams`, no constructor dependencies. Public method `resolve(\Illuminate\Support\Collection $library, array $signals): \Illuminate\Support\Collection` where `$signals` has keys `activities` (string[]), `drilling_required` (bool), `scope_narrative` (string, already lowercased by the caller or lowercase it internally with `Str::lower`).

    Define 3 private const arrays: `TIER2_ACTIVITY_SIGNALS` mapping signal key -> activity key(s) that satisfy it (`mounting_above_reach` => `['ceiling_works']`, `display_mount_or_rack` => `['display_installation','av_rack','ceiling_works']`, `ceiling_void_access` => `['ceiling_works']`, `first_fix_cabling` => `['structured_cabling','cable_management']`); `TIER2_KEYWORD_SIGNALS` mapping signal key -> keyword array for narrative scan (`mounting_above_reach` => `['above standing reach','overhead mount','high level mount','pillar mount']`, `ceiling_void_access` => `['ceiling void','void access','riser','above the ceiling grid','ceiling tiles','comms room']`, `mains_connection` => `['mains connection','mains disconnection','isolate the','new circuit','electrical connection','mains isolation','disconnect the existing supply']`, `strip_out_or_decommission` => `['strip-out','strip out','decommission','removal of existing','de-install']`); `TIER2_DRILLING_SIGNALS` — the flat list of signal keys satisfied purely by `drilling_required === true` (`drilling_or_percussive`, `any_penetration`, `any_drilling`); `TIER3_KEYWORD_PRECHECK` mapping ai-key -> keyword array (`occupied_premises` => `['occupied','live building','staff present','out of hours','during business hours']`, `asbestos` => `['asbestos','pre-2000','pre 2000','age unknown','built before 2000']`, `vehicle_plant` => `['warehouse','workshop','yard','loading bay']`, `lone_working` => `['lone work','single operative','one engineer','split areas','split location']`, `road_risk` => `['travel day','overnight stay','significant distance','outside normal travel radius']`).

    Evaluation per library row: parse `include_when` — `'always'` => include, `needs_confirmation=false`, `match_tier='always'`. `null` => skip entirely (return false from the filter). `'signal:<key>'` => include with `needs_confirmation=false`, `match_tier='deterministic'` if EITHER the activity-signal check OR the keyword-signal check OR (for keys in `TIER2_DRILLING_SIGNALS`) `drilling_required===true` is satisfied for that key; otherwise skip (tier 2 hazards that don't match are dropped, not flagged — D-06's "include and flag" applies only to tier 3, where no reliable signal exists at all by design). `'ai:<key>'` => ALWAYS include (never skip); set `needs_confirmation=false, match_tier='ai_confirmed'` if the narrative contains any `TIER3_KEYWORD_PRECHECK[key]` keyword, else `needs_confirmation=true, match_tier='ai_unconfirmed'`.

    Use `Illuminate\Support\Str::contains`/`str_contains` for keyword matching against the lowercased narrative — fixed vocabulary only, no regex (Security Domain guidance, matches `RiskTemplateResolverService`'s existing discipline). Return `$library->filter(...)->map(function ($t) { $t->needs_confirmation = ...; $t->match_tier = ...; return $t; })->values()`.
  </action>
  <verify>
    <automated>php artisan test --filter=HazardIncludeWhenResolverTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=HazardIncludeWhenResolverTest` exits 0 with all 7 behaviors covered
    - `grep -c "class HazardIncludeWhenResolver" app/Services/Rams/HazardIncludeWhenResolver.php` returns 1
    - `grep -c "'always'" app/Services/Rams/HazardIncludeWhenResolver.php` returns 1 or more
    - A row with `include_when` set to `null` is provably excluded: covered by Test 7
  </acceptance_criteria>
  <done>HazardIncludeWhenResolver correctly evaluates all three tiers per D-05/D-06, verified by 7 passing unit tests with no DB dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests — full tier + D-06 coverage</name>
  <files>tests/Unit/Services/Rams/HazardIncludeWhenResolverTest.php</files>
  <read_first>tests/Unit/Services/Rams/MethodStatementAssociatedRisksTest.php</read_first>
  <action>
    Write `tests/Unit/Services/Rams/HazardIncludeWhenResolverTest.php`, namespace `Tests\Unit\Services\Rams`, extending `Tests\TestCase`, with NO `RefreshDatabase` trait (pure unit — the whole point of Task 1's design is independence from the schema). Build the fixture library as `collect([new HazardTemplate(['id'=>1,'name'=>'...','include_when'=>'...']), ...])` — unsaved model instances (never call `->save()`). Cover the 7 behaviors from Task 1's `<behavior>` block as 7 distinct test methods, each asserting on the returned Collection's count, the presence/absence of specific `name` values, and each returned item's `needs_confirmation`/`match_tier` values.
  </action>
  <verify>
    <automated>php artisan test --filter=HazardIncludeWhenResolverTest</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=HazardIncludeWhenResolverTest` exits 0
    - Test file contains exactly 7 test methods (one per behavior)
    - No test in this file uses `RefreshDatabase` or touches a real DB connection
  </acceptance_criteria>
  <done>7 unit tests pass, proving tier 1/2/3 evaluation and the D-06 include-and-flag guarantee, independent of any DB schema or seeded data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Scope narrative text -> keyword matcher | Text originates from parsed quote PDFs / form fields, already validated/sanitised upstream (not new user input surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-26-02 | Information Disclosure / correctness | `HazardIncludeWhenResolver` keyword scan | mitigate | Matching is restricted to the 4 fixed, reviewed keyword arrays defined in Task 1 (`TIER2_KEYWORD_SIGNALS`, `TIER3_KEYWORD_PRECHECK`) — no open-ended regex over arbitrary narrative text, matching `RiskTemplateResolverService::ACCESS_EQUIPMENT_MAP`/`PPE_ACTIVITY_MAP`'s existing discipline |
| T-26-03 | Tampering | `include_when=null` rows | mitigate | Explicitly excluded from the tiered filter regardless of signal content (Test 7) — D-04's manual-only guarantee is enforced in this class, independent of upstream mass-assignment protections from Plan 26-01 |
</threat_model>

<verification>
`php artisan test --filter=HazardIncludeWhenResolver` exits 0. All 7 unit tests green, no DB dependency.
</verification>

<success_criteria>
A fully tested, standalone tiered resolver exists, ready for Plan 26-04 to wire into `RiskTemplateResolverService`. HAZ-02's evaluation logic (D-05 tiers, D-06 fallback, D-04 null-exclusion) is proven correct in isolation.
</success_criteria>

<output>
Create `.planning/phases/26-hazard-library-structural-inversion/26-02-SUMMARY.md` when done
</output>
