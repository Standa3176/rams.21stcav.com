---
phase: 46-visit-lifecycle
plan: 08
type: execute
wave: 5
depends_on: ["46-03", "46-05", "46-07"]
files_modified:
  - tests/Feature/Visits/VisitLifecycleEndToEndTest.php
  - tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [VL-01, VL-02, VL-03, VL-04, VL-05, VL-06, VL-07, VL-08, VL-09, VL-10, VL-11]
must_haves:
  truths:
    - "One test walks the whole phase: a PM creates a visit, the engineer returns it having read the survey's findings, the PM sends it back, the engineer resubmits, the PM accepts — and nothing the engineer captured was ever overwritten"
    - "The fence still bites: three deliberate breakages go red and are reverted"
    - "The user has seen the page and judged it simple enough to use"
  artifacts:
    - path: "tests/Feature/Visits/VisitLifecycleEndToEndTest.php"
      provides: "the phase's one end-to-end proof, HTTP level, across both auth boundaries"
  key_links:
    - from: "tests/Feature/Visits/VisitLifecycleEndToEndTest.php"
      to: "resources/views/worksheets/public-show.blade.php"
      via: "the unauthenticated engineer GET that proves the carry-forward reached the install"
      pattern: "worksheet/"
---

<objective>
Prove the phase as one thing rather than as seven, and then show it to the user — because the
acceptance criterion the user actually set ("a clean workflow that is simple to use") is not one a
test can settle.

Purpose: every prior plan proved its own half. Nobody has yet walked a job from "create the visit"
to "accepted" and checked that the survey's findings reached the install engineer on the way.
Output: one end-to-end test, the fence re-proved, the VL traceability table filled in, and a human
checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46-visit-lifecycle/46-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/sketches/004-delivery-cockpit/README.md
@.planning/phases/46-visit-lifecycle/46-03-SUMMARY.md
@.planning/phases/46-visit-lifecycle/46-05-SUMMARY.md
@.planning/phases/46-visit-lifecycle/46-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: One test, the whole job</name>
  <files>tests/Feature/Visits/VisitLifecycleEndToEndTest.php</files>
  <behavior>
    One test method, written as a narrative, crossing the staff/public boundary in both directions:

    1. A PM (authenticated) opens the cockpit at `?module=site_survey`, POSTs Create visit with a
       date and two labour resources. Assert: one `visits` row, one engineer link, one activity row.
    2. The engineer (UNAUTHENTICATED, by token) opens `/survey/{token}`, and the survey is submitted
       with all ten D-01 fields populated.
    3. The PM creates an INSTALL visit from `?module=worksheet`, producing the worksheet link.
    4. **The moment the phase exists for:** the install engineer (UNAUTHENTICATED) opens
       `/worksheet/{token}` and all ten survey fields are on the page. Then the survey is edited and
       the link is fetched again — the NEW value is there and the old one is gone. Assert both.
    5. The PM sends the SURVEY visit back with a reason. Assert: the engineer's link is writable
       again, the engineer sees the reason, the client-facing worksheet link does NOT contain it,
       and `site_surveys.submitted_at` is unchanged.
    6. The engineer resubmits. Assert: the survey relocks with no flag cleared by hand.
    7. The PM adds an office note and raises a snag on the install visit. Assert: one `visit_notes`
       row, one `snags` row with `status = open`, and the wrapped worksheet byte-identical.
    8. The PM accepts the install visit. Assert: `accepted_by_user_id` and `accepted_at` set, the
       module's progress sentence moved by exactly one, and the scope lock sentence is on the page.
    9. **The closing assertion, and the one that matters most:** across the whole walk, the survey's
       `submitted_at`, `survey_data` and `access_token`, and the worksheet's `access_token`, are
       byte-identical to the values they held the moment the engineer submitted. The office acted on
       the return five times and changed none of it.
  </behavior>
  <action>
    Write it as a single narrative test with commented sections, not nine small ones. The value here
    is the sequence: each prior plan already unit-proved its own step, and what nobody has proved is
    that the steps compose.

    Use `Bus::fake()` for `BuildWorksheetJob` and set the worksheet's generated data directly, as
    Plan 46-04's tests do — this test is about the lifecycle, not about document generation.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter VisitLifecycleEndToEndTest</automated>
  </verify>
  <done>The nine-step walk passes, including the read-live proof at step 4 and the byte-identical closing assertion at step 9.</done>
</task>

<task type="auto">
  <name>Task 2: Re-prove the fence, and close the requirements</name>
  <files>tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php, .planning/REQUIREMENTS.md</files>
  <action>
    **Re-prove the fence can still fail — the 45-07 ritual, three ways.** A guard nobody has watched
    fail is a guard nobody should trust, and this fence was substantially edited by Plan 46-04.
    Break it, record what went red verbatim, revert:
      1. Point `cockpitRegion()`'s XPath at a class that does not exist — the vacuity guard fires.
      2. Truncate the extracted region to 2,000 characters — the BOTTOM bracket fires.
      3. Inject `&lt;button onclick="alert(1)"&gt;Upload files&lt;/button&gt;` into the panel — the handler ban
         and the `Upload files` affordance fire, and `&lt;button` does NOT, because 46-04 lifted it
         deliberately. That asymmetry is the proof the retirement was surgical rather than a
         weakening.

    **Run the whole gate set and record every number verbatim in the SUMMARY:**
      - `gate-46.ps1 -Path tests/Feature/Cockpit` and `-Path tests/Unit/Cockpit`
      - `gate-46.ps1 -Path tests/Feature/Surveys` and `-Path tests/Feature/Worksheets`
      - `gate-46.ps1 -Filter LabourResourceClientSurfacePrivacyTest`
      - `gate-46.ps1 -Baseline` — `&gt;= 159 passed AND 0 failed`, never an equality against 161
      - `gate-46.ps1 -Hashes` — a mismatch is a STOP, never a hash to refresh

    **Then update `.planning/REQUIREMENTS.md`'s traceability table** for VL-01..VL-11, each from
    "Planned" to the plan that delivered it and what proves it — following the LR/VIS rows' style.
    **VL-12 stays a GAP** and its row must not be quietly marked done: no part of this phase scopes
    the RAMS or worksheet generators to a visit's rooms, and saying otherwise would be the exact
    failure that requirement exists to prevent.

    Do NOT run `state.advance-plan` or `state.update-progress` — both have corrupted
    `.planning/STATE.md` repeatedly.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Baseline</automated>
  </verify>
  <done>Three deliberate breakages recorded red and reverted; every gate green; VL-01..VL-11 traced; VL-12 still reads GAP.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Does it look simple?</name>
  <what-built>
    The delivery cockpit can now be written from. Each module drawer offers at most one Quick action
    — Create visit on Site survey and First fix and install, Generate document on RAMS, O&amp;M and
    Cable schedule, nothing at all on the other four. A returned visit's row offers at most four:
    Accept, Send back, Add note, Raise a snag. No JavaScript was added; every panel and every open
    form is a bookmarkable URL and the back button works.

    And the install engineer's link now carries what the survey knew: access constraints, site
    risks, health and safety and the surveyor's notes, alongside the parking, comms-room, delivery
    and distance facts it already carried — read live from the survey, so editing the survey changes
    what the engineer sees.
  </what-built>
  <how-to-verify>
    1. Start the app and open a project's cockpit with the cockpit flag on.
    2. Open **Site survey**. There should be ONE action. Click **Create visit**, set a date, tick a
       couple of names, submit. You should land back on the same open drawer with a link issued.
    3. Open **First fix and install** and do the same.
    4. Open the worksheet engineer link in a private window — no login. Find the site logistics
       drawer. **Are the survey's access constraints, risks and H&amp;S notes there?** Edit the survey
       in the app, reload the link, and check the new wording appears.
    5. Back on the cockpit, on a returned visit: **Accept**, **Send back**, **Add note**,
       **Raise a snag**. Four, no more.
    6. Then the question this checkpoint exists for, and the only one no test can answer:
       **does this page still look simple and unscary?** You rejected an earlier design for being
       busy. If any drawer now feels like a control panel, say which one and what should go — the
       cap is deliberate and removing something is a supported outcome, not a failure.
    7. Second question: **is anything missing that you expected to be able to do here?** Sending a
       document to a client and uploading files are Phase 48 on purpose; snag outcomes and parts are
       Phase 47 on purpose. Anything else you reach for and cannot find is a finding.
  </how-to-verify>
  <resume-signal>Type "approved", or name the drawer that feels busy and what should come off it</resume-signal>
  <action>
    PAUSE and present the walkthrough above to the user. Do not self-approve and do not infer
    approval from a green test run: the property being checked here — "simple to use", in the
    user's own words — is not one any assertion in this repo can settle, and Phase 45's own human
    check (45-14) exists for the same reason.

    If the user names a drawer as busy, record what they said VERBATIM in the SUMMARY under
    "Deferred Issues" and stop. Removing a control is a supported outcome and belongs in its own
    change, not in an unsupervised edit at the end of a phase.
  </action>
  <done>The user has typed "approved", or has named what should come off and that wording is recorded verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| staff POST -> public token GET | The end-to-end test is the only place both sides of the boundary are exercised in one sequence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-08-01 | Tampering | a fence that passes vacuously after being edited | mitigate | The three-way breakage ritual, with the `<button`-does-NOT-fire asymmetry proving the retirement was surgical |
| T-46-08-02 | Repudiation | a phase marked complete on a requirement it did not deliver | mitigate | VL-12 stays a GAP in the traceability table and is named in the checkpoint copy |
| T-46-08-03 | Information Disclosure | the whole-phase client/engineer split | mitigate | Step 5 of the end-to-end test asserts the send-back reason is on the engineer link and absent from the client-signed worksheet link |
| T-46-08-SC | Tampering | npm/pip/composer installs | mitigate | **No package is installed by this plan.** If a task appears to need one, STOP — the legitimacy gate has not been run for this phase |
</threat_model>

<verification>
`VisitLifecycleEndToEndTest` green. Every gate in Task 2 green, each recorded verbatim. D-06
baseline `>= 159 passed AND 0 failed`. Three protected files hash-identical. Human checkpoint
approved.
</verification>

<success_criteria>
- One test walks the phase end to end and closes on a byte-identical engineer record.
- The fence was proved to bite three ways and reverted.
- VL-01..VL-11 traced to the plans that delivered them; VL-12 still reads GAP.
- The user has judged the page simple enough to use, or named what should come off it.
</success_criteria>

<output>
Create `.planning/phases/46-visit-lifecycle/46-08-SUMMARY.md` when done
</output>
