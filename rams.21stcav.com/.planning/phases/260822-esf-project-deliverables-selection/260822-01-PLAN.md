---
phase: 260822-esf
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_08_22_140000_create_project_deliverables_and_audits_tables.php
  - app/Models/ProjectDeliverable.php
  - app/Models/ProjectDeliverableAudit.php
  - app/Models/Project.php
  - app/Services/ProjectDeliverablesService.php
  - tests/Unit/Services/ProjectDeliverablesServiceTest.php
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06]

must_haves:
  truths:
    - "A project can carry an explicit Required / Not required / Not yet decided state for each of the 9 canonical deliverables (site_survey, rams, worksheet, om, cable_schedule, install_programme, drawings, snagging, programming)"
    - "Every state change is attributable to a user, a timestamp, and an optional free-text reason, recorded in an append-only audit table — never overwritten"
    - "Creating a document against a deliverable currently marked Not required flips it back to Required and writes an audit row explaining why, without throwing"
  artifacts:
    - path: "database/migrations/2026_08_22_140000_create_project_deliverables_and_audits_tables.php"
      provides: "project_deliverables (project_id, deliverable_key, state, timestamps, unique) + project_deliverable_audits (project_deliverable_id, user_id, action, reason, before/after snapshot, timestamps)"
    - path: "app/Models/ProjectDeliverable.php"
      provides: "KEY_* (9 canonical deliverable keys) + STATE_* (3-state enum) constants, $fillable, relations"
      exports: ["KEY_SITE_SURVEY", "KEY_RAMS", "KEY_WORKSHEET", "KEY_OM", "KEY_CABLE_SCHEDULE", "KEY_INSTALL_PROGRAMME", "KEY_DRAWINGS", "KEY_SNAGGING", "KEY_PROGRAMMING", "ALL_KEYS", "STATE_REQUIRED", "STATE_NOT_REQUIRED", "STATE_NOT_YET_DECIDED"]
    - path: "app/Services/ProjectDeliverablesService.php"
      provides: "setState() / autoFlipIfNotRequired() / setInitialStates() — the sole write path for project_deliverables + project_deliverable_audits"
      exports: ["setState", "autoFlipIfNotRequired", "setInitialStates"]
  key_links:
    - from: "app/Services/ProjectDeliverablesService.php"
      to: "app/Models/ProjectDeliverableAudit.php"
      via: "DB::transaction() writing both rows together"
      pattern: "ProjectDeliverableAudit::create"
    - from: "app/Models/Project.php"
      to: "app/Models/ProjectDeliverable.php"
      via: "deliverables() HasMany relation, deliverableState() reads relationLoaded() only"
      pattern: "function deliverables"
---

<objective>
Ship the shared foundation every other plan in this phase builds on: the `project_deliverables` / `project_deliverable_audits` schema (D-01 three-state model, D-03 full audit trail), the canonical 9-item deliverable-key vocabulary (D-04, with D-05's Programming-is-flag-only and D-06's Quotes/Assets/Project-Data exclusion baked in as the ONLY vocabulary — nothing else is selectable), `Project::deliverables()` + a safe `Project::deliverableState()` reader, `Project::snaggingSignoffs()` (a genuinely new relation — see the standing-hazard note below), and `ProjectDeliverablesService` (D-02's soft-gate auto-flip + D-03's audit-on-every-write).

**⚠ Standing-hazard finding (verified against code, not assumed from RESEARCH/PATTERNS):** "Snagging" has **no dedicated model**. `DocumentArtifactStorage::TYPE_SNAGGING` exists, but the actual data behind it is `App\Models\CommissioningSignoff` — one row per `install_programme_id` (`CommissioningSignoff::$fillable` includes `snagging_pdf_path`; `InstallProgramme::commissioningSignoff(): HasOne`). There is no `Project::snagging()` relation today and no `SnaggingItem`/`Snagging` class anywhere in `app/`. RESEARCH.md's and PATTERNS.md's phrasing ("Snagging needs `$project->snagging()->count()` or equivalent once its relation exists") undersold this — it is not a simple missing accessor on an existing model, it is a two-hop relation through `InstallProgramme`. This plan adds `Project::snaggingSignoffs(): HasManyThrough` (`CommissioningSignoff::class, InstallProgramme::class, 'project_id', 'install_programme_id'`) so every later plan (tab counts, D-17 backfill, D-02 auto-flip) has one canonical way to ask "does this project have snagging data".

Purpose: Nothing else in this phase can work without this — the status machine (Plan 02), health filter (Plan 03), tab strip (Plan 04), auto-flip wiring (Plan 05), retrofit (Plan 06), edit UI (Plan 07), and import checklist (Plan 08) all read or write through this schema/service.
Output: Migrated schema, `ProjectDeliverable` + `ProjectDeliverableAudit` models, `Project` model additions, `ProjectDeliverablesService`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/260822-esf-project-deliverables-selection/260822-CONTEXT.md
@.planning/phases/260822-esf-project-deliverables-selection/260822-RESEARCH.md
@.planning/phases/260822-esf-project-deliverables-selection/260822-PATTERNS.md
@database/migrations/2026_08_13_140000_add_needs_review_and_logo_path_to_device_stencils_and_create_device_stencil_audits.php
@app/Models/DeviceStencilAudit.php
@app/Models/DeviceStencil.php
@app/Core/Modules/Projects/ProjectService.php
@app/Models/Project.php
@app/Models/InstallProgramme.php
@app/Models/CommissioningSignoff.php
</context>

<interfaces>
From `app/Core/Modules/Projects/ProjectService.php:63-99` (the pattern `ProjectDeliverablesService::setState()` must mirror for its DB::transaction + before/after logging shape — NOT copied verbatim, this project is document-audit shaped, not status-transition shaped):
```
public function transition(Project $project, string $toStatus, User $user, ?string $note = null): Project
```

From `app/Models/DeviceStencilAudit.php` (exact model shape to mirror for `ProjectDeliverableAudit`, adding the `reason` column DeviceStencilAudit is missing — confirmed gap, do not copy that column list verbatim):
```
public const ACTION_PROMOTE = 'promote';
protected $fillable = ['device_stencil_id', 'user_id', 'action', 'before_snapshot', 'after_snapshot'];
protected $casts = ['before_snapshot' => 'array', 'after_snapshot' => 'array'];
public function stencil(): BelongsTo { return $this->belongsTo(DeviceStencil::class, 'device_stencil_id'); }
public function user(): BelongsTo { return $this->belongsTo(\App\Models\User::class); }
```

From `app/Models/InstallProgramme.php:96-99` (the relation `snaggingSignoffs()` chains through):
```
public function commissioningSignoff(): HasOne
{
    return $this->hasOne(CommissioningSignoff::class, 'install_programme_id');
}
```
`InstallProgramme`'s own FK to `Project` is the default `project_id` (confirmed via `Project::installProgrammes(): HasMany { return $this->hasMany(InstallProgramme::class); }` at `Project.php:363-366`, no explicit FK override).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migration + ProjectDeliverable + ProjectDeliverableAudit models + Project.php relations</name>
  <files>database/migrations/2026_08_22_140000_create_project_deliverables_and_audits_tables.php, app/Models/ProjectDeliverable.php, app/Models/ProjectDeliverableAudit.php, app/Models/Project.php</files>
  <read_first>
    - database/migrations/2026_08_13_140000_add_needs_review_and_logo_path_to_device_stencils_and_create_device_stencil_audits.php (schema block lines 48-60, down() index-then-column-drop order lines 73-87 — not directly needed here since we are not dropping an indexed column from an existing table, but the file's overall migration-hygiene style is the template)
    - app/Models/DeviceStencilAudit.php (full file — model shape to mirror, MINUS the missing `reason` column)
    - app/Models/Project.php lines 1-220 (fillable/casts/relations block — insert new relations alongside `siteSurveys()`, `installProgrammes()`, `drawings()`)
    - app/Models/InstallProgramme.php lines 86-99 (`commissioningSignoff(): HasOne`, FK `install_programme_id`)
  </read_first>
  <action>
    Create migration `database/migrations/2026_08_22_140000_create_project_deliverables_and_audits_tables.php` with a docblock explaining the D-01/D-03/D-04 rationale (mirror the device_stencil_audits migration's docblock style). In `up()`: `Schema::create('project_deliverables', ...)` with `$table->id()`, `$table->foreignId('project_id')->constrained('projects')->cascadeOnDelete()`, `$table->string('deliverable_key', 30)`, `$table->string('state', 20)->default('not_yet_decided')->index()`, `$table->timestamps()`, `$table->unique(['project_id', 'deliverable_key'])`. Then `Schema::create('project_deliverable_audits', ...)` with `$table->id()`, `$table->foreignId('project_deliverable_id')->constrained('project_deliverables')->cascadeOnDelete()`, `$table->foreignId('user_id')->constrained('users')`, `$table->string('action', 30)`, `$table->text('reason')->nullable()` (D-03's gap vs the device_stencil_audits precedent — REQUIRED, do not omit), `$table->json('before_snapshot')->nullable()`, `$table->json('after_snapshot')->nullable()`, `$table->timestamps()`. `down()`: `Schema::dropIfExists('project_deliverable_audits')` then `Schema::dropIfExists('project_deliverables')` (audits table first — it has the FK).

    Create `app/Models/ProjectDeliverable.php`: `public const KEY_SITE_SURVEY = 'site_survey'; public const KEY_RAMS = 'rams'; public const KEY_WORKSHEET = 'worksheet'; public const KEY_OM = 'om'; public const KEY_CABLE_SCHEDULE = 'cable_schedule'; public const KEY_INSTALL_PROGRAMME = 'install_programme'; public const KEY_DRAWINGS = 'drawings'; public const KEY_SNAGGING = 'snagging'; public const KEY_PROGRAMMING = 'programming';` and `public const ALL_KEYS = [self::KEY_SITE_SURVEY, self::KEY_RAMS, self::KEY_WORKSHEET, self::KEY_OM, self::KEY_CABLE_SCHEDULE, self::KEY_INSTALL_PROGRAMME, self::KEY_DRAWINGS, self::KEY_SNAGGING, self::KEY_PROGRAMMING];` in this exact order (D-04's canonical list order). Add `public const STATE_REQUIRED = 'required'; public const STATE_NOT_REQUIRED = 'not_required'; public const STATE_NOT_YET_DECIDED = 'not_yet_decided';`. `protected $fillable = ['project_id', 'deliverable_key', 'state'];`. Relations: `project(): BelongsTo` to `Project::class`; `audits(): HasMany` to `ProjectDeliverableAudit::class`. Add a class docblock stating D-06 explicitly: Quotes/Asset Register/Project Data are inputs, not deliverables, and MUST NEVER be added to `ALL_KEYS`; Programming (D-05) is a tracked flag with no generator, no tab, no storage type — its presence in `ALL_KEYS` is intentional and complete, nothing else needs to be built for it in this phase.

    Create `app/Models/ProjectDeliverableAudit.php` mirroring `DeviceStencilAudit.php`'s shape exactly, with these differences: `public const ACTION_MANUAL_CHANGE = 'manual_change'; public const ACTION_AUTO_FLIP = 'auto_flip'; public const ACTION_IMPORT_DEFAULT = 'import_default'; public const ACTION_BACKFILL = 'backfill';`. `protected $fillable = ['project_deliverable_id', 'user_id', 'action', 'reason', 'before_snapshot', 'after_snapshot'];`. `protected $casts = ['before_snapshot' => 'array', 'after_snapshot' => 'array'];`. Relations: `deliverable(): BelongsTo` to `ProjectDeliverable::class` via `project_deliverable_id`; `user(): BelongsTo` to `\App\Models\User::class`.

    Edit `app/Models/Project.php`: add `public function deliverables(): HasMany { return $this->hasMany(ProjectDeliverable::class); }` alongside the other relations (near `installProgrammes()`/`drawings()`). Add `public function snaggingSignoffs(): \Illuminate\Database\Eloquent\Relations\HasManyThrough { return $this->hasManyThrough(\App\Models\CommissioningSignoff::class, \App\Models\InstallProgramme::class, 'project_id', 'install_programme_id'); }` with a docblock explaining WHY this exists (no direct Project→Snagging relation exists in the codebase; Snagging is `CommissioningSignoff`, one per `InstallProgramme`) so a future reader is not tempted to look for a `Snagging` model that does not exist. Add `public function deliverableState(string $key): ?string { if (! $this->relationLoaded('deliverables')) { return null; } $row = $this->deliverables->firstWhere('deliverable_key', $key); return $row?->state ?? \App\Models\ProjectDeliverable::STATE_NOT_YET_DECIDED; }` with a docblock explaining this is DELIBERATELY safe to call on an unsaved/pure-unit-test `Project` instance — it NEVER triggers a relation query (checks `relationLoaded()` first, never accesses `$this->deliverables` unguarded), returning `null` when the caller has not eager-loaded the relation. This is required for `canTransitionTo()` (Plan 02) to remain callable from `tests/Unit/ProjectTransitionTest.php`'s pure `PHPUnit\Framework\TestCase` (no DB, no container).
  </action>
  <acceptance_criteria>
    - After `RefreshDatabase`, `project_deliverables` has `project_id` FK, `deliverable_key` varchar(30), an indexed `state` varchar(20) defaulting to `not_yet_decided`, and a unique index on `(project_id, deliverable_key)`.
    - `project_deliverable_audits` has `project_deliverable_id` FK (cascade), `user_id` FK, `action` varchar(30), a **nullable `reason` text column**, `before_snapshot`/`after_snapshot` json.
    - `ProjectDeliverable::ALL_KEYS` has exactly 9 entries in the D-04 order, and does NOT contain `quotes`, `assets`, or `data`/`project_data` (D-06).
    - `(new \App\Models\Project(['status' => 'quote_imported']))->deliverableState('site_survey')` returns `null` (relation not loaded) without issuing any database query — provable by running it inside a pure `PHPUnit\Framework\TestCase` with no DB connection configured.
  </acceptance_criteria>
  <verify>
    <automated>"/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -l app/Models/ProjectDeliverable.php app/Models/ProjectDeliverableAudit.php app/Models/Project.php database/migrations/2026_08_22_140000_create_project_deliverables_and_audits_tables.php</automated>
  </verify>
  <done>Migration applies cleanly against SQLite; both models exist with the exact constant/relation shape above; Project.php's new methods are additive (no existing method signature changed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProjectDeliverablesService (D-02 auto-flip, D-03 audit-on-write) + tests</name>
  <files>app/Services/ProjectDeliverablesService.php, tests/Unit/Services/ProjectDeliverablesServiceTest.php</files>
  <behavior>
    - `setState()` on a project with no existing row for that key creates the row (default snapshot `state: not_yet_decided`), updates it to the requested state, and writes ONE `ProjectDeliverableAudit` row with `action = ACTION_MANUAL_CHANGE`, the acting user's id, the given `reason` (nullable), and before/after snapshots reflecting the state transition.
    - `setState()` called twice with the same target state still writes a SECOND audit row (every explicit write is audited, even a no-op state change — D-03 "who, when" must be provable even for a confirm-no-change action).
    - `autoFlipIfNotRequired()` on a deliverable currently `not_required` flips it to `required` and writes an audit row with `action = ACTION_AUTO_FLIP` and a non-null system-generated `reason` mentioning the deliverable key.
    - `autoFlipIfNotRequired()` on a deliverable currently `required` or `not_yet_decided` is a no-op — no state change, NO audit row written (only actual flips are audited).
    - `setInitialStates()` writes one row + one `ACTION_IMPORT_DEFAULT` audit row per key in the given `[key => state]` array, in a single `DB::transaction()` (all-or-nothing).
  </behavior>
  <read_first>
    - app/Http/Controllers/Admin/DeviceStencilController.php lines 220-245 (the exact before-snapshot / update / after-snapshot / audit-row-inside-transaction shape — all 3 write sites in that file share this shape; use it as the template, not `ProjectService::transition()`, since this is a data-audit write not a status-transition write)
    - app/Models/ProjectDeliverable.php, app/Models/ProjectDeliverableAudit.php (Task 1 output — read for exact constant/fillable names)
  </read_first>
  <action>
    Create `app/Services/ProjectDeliverablesService.php` with a class docblock stating: "All writes to project_deliverables MUST go through this service — mirrors the house rule stated in ProjectService's docblock for project status transitions." Implement `public function setState(Project $project, string $key, string $newState, User $user, ?string $reason = null, string $action = ProjectDeliverableAudit::ACTION_MANUAL_CHANGE): ProjectDeliverable` — inside `DB::transaction()`: `$row = ProjectDeliverable::firstOrCreate(['project_id' => $project->id, 'deliverable_key' => $key], ['state' => ProjectDeliverable::STATE_NOT_YET_DECIDED]);` capture `$before = ['state' => $row->state];`, `$row->update(['state' => $newState]);`, `$after = ['state' => $newState];`, `ProjectDeliverableAudit::create(['project_deliverable_id' => $row->id, 'user_id' => $user->id, 'action' => $action, 'reason' => $reason, 'before_snapshot' => $before, 'after_snapshot' => $after]);`, return `$row->fresh()`. Implement `public function autoFlipIfNotRequired(Project $project, string $key, User $user): void` — load-or-skip: `$row = ProjectDeliverable::where('project_id', $project->id)->where('deliverable_key', $key)->first(); if ($row === null || $row->state !== ProjectDeliverable::STATE_NOT_REQUIRED) { return; }` then call `$this->setState($project, $key, ProjectDeliverable::STATE_REQUIRED, $user, "Auto-flipped: a {$key} document was created while marked Not required.", ProjectDeliverableAudit::ACTION_AUTO_FLIP);` (D-02's soft gate — never blocks, always allows the create, this only repairs the flag afterward). Implement `public function setInitialStates(Project $project, array $states, User $user, string $action = ProjectDeliverableAudit::ACTION_IMPORT_DEFAULT): void` — `DB::transaction(function () use ($project, $states, $user, $action) { foreach ($states as $key => $state) { $this->setState($project, $key, $state, $user, null, $action); } });`.

    Create `tests/Unit/Services/ProjectDeliverablesServiceTest.php` extending `Tests\TestCase` with `use Illuminate\Foundation\Testing\RefreshDatabase;`, covering every case in the `<behavior>` block above. Use `Project::factory()->create()` (confirmed to exist, despite `TESTING.md` — do not use `Project::create([...])` boilerplate) and `User::factory()->create()`.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=ProjectDeliverablesServiceTest` — all cases from `<behavior>` pass, including the negative case (no audit row on a not-required → required no-op skip, i.e. calling `autoFlipIfNotRequired` when state is already `required`).
    - `assertDatabaseCount('project_deliverable_audits', N)` assertions are used to prove audit-row COUNTS, not just presence (distinguishes "wrote 1 row" from "wrote 2 rows" for the double-`setState` case).
  </acceptance_criteria>
  <verify>
    <automated>"/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" artisan test --filter=ProjectDeliverablesServiceTest</automated>
  </verify>
  <done>ProjectDeliverablesService is the sole write path; every test in the behavior list is green; `php -l` clean on the service file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Authenticated user → `project_deliverable_audits.reason` | Free-text field, authenticated-user-controlled, rendered later in Plan 07's edit UI and Plan 04's tab strip |
| Authenticated user → `project_deliverables.state` | Constrained to a 3-value enum at the service layer, never a free string from the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-260822-01 | Tampering | `ProjectDeliverablesService::setState()` `$newState` param | mitigate | Callers (Plans 07/08) validate `state` via Laravel `FormRequest`/`$request->validate()` with `in:required,not_required,not_yet_decided` BEFORE calling this service — the service itself does not re-validate, so every controller call site must validate. Documented in this service's docblock. |
| T-260822-02 | Information Disclosure / Tampering | `reason` free-text field | accept | Stored as plain text, rendered via Blade `{{ }}` escaping everywhere it is later displayed (Plans 04/07) — no `{!! !!}` planned or permitted. No PII expected (internal ops tool, shared workspace per existing threat model). |
| T-260822-03 | Elevation of Privilege | Who may call `setState()`/`autoFlipIfNotRequired()` | accept | Shared-workspace app by design (see `WorksheetPolicy` precedent) — any authenticated user may change any project's deliverables, matching `ProjectService::transition()`'s existing D-19 "any authenticated user" rule. Not tightened here per phase instructions. |
</threat_model>

<verification>
`php artisan test --filter=ProjectDeliverablesServiceTest` green. `"/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -l` clean on every file in `files_modified`. Fresh `RefreshDatabase` run confirms both new tables exist with the exact columns specified.
</verification>

<success_criteria>
`project_deliverables` + `project_deliverable_audits` exist and migrate cleanly on SQLite; `ProjectDeliverable::ALL_KEYS` is the single canonical 9-item list; `ProjectDeliverablesService` is the only way to write a state change, and every write is audited with who/when/optional-why; `Project::deliverableState()` is safe to call with no DB access when the relation is not loaded.
</success_criteria>

<output>
Create `.planning/phases/260822-esf-project-deliverables-selection/260822-01-SUMMARY.md` when done.
</output>
