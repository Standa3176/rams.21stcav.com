---
phase: 260822-esf
plan: 5
type: execute
wave: 2
depends_on: ["260822-01"]
files_modified:
  - app/Http/Controllers/RamsController.php
  - app/Http/Controllers/WorksheetController.php
  - app/Http/Controllers/OmManualController.php
  - app/Http/Controllers/CableScheduleController.php
  - app/Http/Controllers/SiteSurveyController.php
  - app/Http/Controllers/InstallProgrammeController.php
  - app/Http/Controllers/ProjectDrawingController.php
  - app/Http/Controllers/CommissioningSignoffController.php
  - tests/Feature/Projects/ProjectDeliverableAutoFlipTest.php
autonomous: true
requirements: [D-02]

must_haves:
  truths:
    - "Creating any of the 8 tab-eligible deliverable documents against a deliverable currently marked Not required silently flips it back to Required — the create action itself is never blocked"
    - "The flip is audited (who/when/why) exactly once per create, never twice, never for a deliverable that was already Required or Not yet decided"
  artifacts:
    - path: "tests/Feature/Projects/ProjectDeliverableAutoFlipTest.php"
      provides: "One test per wired creation entry point proving the auto-flip fires exactly when the deliverable was not_required, and never blocks the create"
  key_links:
    - from: "app/Http/Controllers/RamsController.php"
      to: "app/Services/ProjectDeliverablesService.php"
      via: "autoFlipIfNotRequired() called immediately after the model row is created"
      pattern: "autoFlipIfNotRequired"
---

<objective>
Implement D-02's soft gate: creating a document against a deliverable currently marked Not required flips it back to Required automatically, with no block anywhere. This closes the loop the rest of the phase depends on — without this, a user who creates a RAMS "anyway" after Plan 04's "Add anyway" tab-strip control (or from the generate button directly, bypassing that control) would leave the flag silently wrong.

**8 wiring points, verified against live code this session (not assumed from RESEARCH):**

| Deliverable key | Controller::method | Verified row-creation call |
|---|---|---|
| `rams` | `RamsController::generateFromProject()` (`:227-264`) | `RamsDocument::create([...])` (~line 264) |
| `worksheet` | `WorksheetController::generateFromProject()` (`:147-166`) | `Worksheet::create([...])` (`:151-159`) |
| `om` | `OmManualController::generateFromProject()` (`:153`) | verify the exact `OmManual::create(...)` line during `read_first` — same "create row, then dispatch job" shape as RAMS/Worksheet, not independently confirmed line-by-line this session |
| `cable_schedule` | `CableScheduleController::generateFromProject()` (`:386`) | verify during `read_first` |
| `install_programme` | `InstallProgrammeController::generate()` (`:53`) | verify during `read_first` |
| `site_survey` | `SiteSurveyController::createFromProject()` (`:116-144`) | `$survey = $this->service->createFromProject($project, $user);` (`:143`) |
| `drawings` | `ProjectDrawingController::createSchematic()` / `createRack()` | verify during `read_first` — 2 call sites, both wire the same `drawings` key |
| `snagging` | `CommissioningSignoffController::finalise()` (`:107-123`) | `$signoff = $this->service->finalise($programme, $request->validated());` (`:112`) — the deliverable is the linked `$programme->project`, not `$programme` itself |

Purpose: D-02 is the "no hard block anywhere" half of the phase's soft-gate philosophy — real jobs change scope constantly, and this makes that safe instead of leaving a stale flag.
Output: 8 controllers each gain one `ProjectDeliverablesService::autoFlipIfNotRequired()` call immediately after their respective row-creation call; one new Feature test file covering all 8.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/260822-esf-project-deliverables-selection/260822-CONTEXT.md
@app/Http/Controllers/RamsController.php
@app/Http/Controllers/WorksheetController.php
@app/Http/Controllers/SiteSurveyController.php
@app/Http/Controllers/CommissioningSignoffController.php
</context>

<interfaces>
From Plan 01's `app/Services/ProjectDeliverablesService.php`:
```
public function autoFlipIfNotRequired(Project $project, string $key, User $user): void
```
No-op (no state change, no audit row) when the deliverable is not currently `not_required`. Never throws. Safe to call unconditionally after every creation.

From Plan 01's `app/Models/ProjectDeliverable.php`: `KEY_RAMS`, `KEY_WORKSHEET`, `KEY_OM`, `KEY_CABLE_SCHEDULE`, `KEY_INSTALL_PROGRAMME`, `KEY_SITE_SURVEY`, `KEY_DRAWINGS`, `KEY_SNAGGING`.

Confirmed shape (`WorksheetController.php:147-166`):
```
public function generateFromProject(Project $project): RedirectResponse
{
    abort_unless(auth()->check(), 403);
    $worksheet = Worksheet::create([...]);
    $this->workerMonitor->ensureRunning();
    BuildWorksheetJob::dispatch($worksheet->id);
    return back()->with('success', 'Worksheet generation queued.');
}
```
The auto-flip call goes immediately after `Worksheet::create([...])`, before the job dispatch (ordering does not matter functionally, but keeps the "record what happened, then kick off the async work" narrative consistent).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire the 6 "generate/create-from-project" controllers (RAMS, Worksheet, O&M, Cable Schedule, Install Programme, Survey)</name>
  <files>app/Http/Controllers/RamsController.php, app/Http/Controllers/WorksheetController.php, app/Http/Controllers/OmManualController.php, app/Http/Controllers/CableScheduleController.php, app/Http/Controllers/InstallProgrammeController.php, app/Http/Controllers/SiteSurveyController.php</files>
  <read_first>
    - app/Http/Controllers/RamsController.php lines 227-270 (generateFromProject, exact `RamsDocument::create([...])` line)
    - app/Http/Controllers/WorksheetController.php lines 140-166 (generateFromProject, already fully read this session)
    - app/Http/Controllers/OmManualController.php lines 153-200 (generateFromProject — find the exact `OmManual::create(...)` call)
    - app/Http/Controllers/CableScheduleController.php lines 380-420 (generateFromProject — find the exact `CableSchedule::create(...)` call)
    - app/Http/Controllers/InstallProgrammeController.php lines 45-90 (generate — find the exact `InstallProgramme::create(...)` call)
    - app/Http/Controllers/SiteSurveyController.php lines 108-150 (createFromProject, already fully read this session — line 143 `$survey = $this->service->createFromProject($project, $user);`)
  </read_first>
  <action>
    In each of the 6 controllers, inject `App\Services\ProjectDeliverablesService` via the constructor (add it to the existing `private readonly` promoted-property list, following each controller's own existing constructor-injection style — do not use the `app()` helper for inline resolution). Immediately after the confirmed row-creation call in each method, add one line: `$this->deliverablesService->autoFlipIfNotRequired($project, \App\Models\ProjectDeliverable::KEY_X, auth()->user());` where `KEY_X` is the matching constant from the table in the objective (`KEY_RAMS`, `KEY_WORKSHEET`, `KEY_OM`, `KEY_CABLE_SCHEDULE`, `KEY_INSTALL_PROGRAMME`, `KEY_SITE_SURVEY`). For `SiteSurveyController::createFromProject()`, place the call after line 143's `$survey = ...` assignment, using the already-in-scope `$project` and `$user` variables (both already resolved at that point in the method — see lines 116-143, already read this session). For the other 5 controllers, use `$project` (the method's own parameter — all 5 are `generateFromProject(Project $project)` / `generate(Project $project)`) and `auth()->user()` for the actor.
  </action>
  <acceptance_criteria>
    - Each of the 6 controllers has exactly one new `autoFlipIfNotRequired()` call, placed after the confirmed row-creation line, before any `return`/redirect.
    - No existing method signature, route, or response shape changes — this is a strictly additive side-effect call.
  </acceptance_criteria>
  <verify>
    <automated>"/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -l app/Http/Controllers/RamsController.php && "/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -l app/Http/Controllers/WorksheetController.php && "/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -l app/Http/Controllers/OmManualController.php && "/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -l app/Http/Controllers/CableScheduleController.php && "/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -l app/Http/Controllers/InstallProgrammeController.php && "/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -l app/Http/Controllers/SiteSurveyController.php</automated>
  </verify>
  <done>All 6 controllers wired; php -l clean on all 6 files.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Drawings + Snagging, plus the full ProjectDeliverableAutoFlipTest suite</name>
  <files>app/Http/Controllers/ProjectDrawingController.php, app/Http/Controllers/CommissioningSignoffController.php, tests/Feature/Projects/ProjectDeliverableAutoFlipTest.php</files>
  <read_first>
    - app/Http/Controllers/ProjectDrawingController.php — find `createSchematic()` and `createRack()`, both create a `ProjectDrawing` row (confirmed 2 distinct routes at `routes/web.php:552-557`)
    - app/Http/Controllers/CommissioningSignoffController.php lines 100-123 (finalise(), already fully read this session — `$programme->project` is how to reach the Project from the InstallProgramme)
    - tests/Feature/Projects/ActualHoursWidgetTest.php (Feature test conventions)
  </read_first>
  <action>
    Inject `ProjectDeliverablesService` into `ProjectDrawingController` and `CommissioningSignoffController` (constructor promotion, matching each file's existing style). In `createSchematic()` and in `createRack()`, immediately after the `ProjectDrawing::create([...])` call in each, add `$this->deliverablesService->autoFlipIfNotRequired($project, \App\Models\ProjectDeliverable::KEY_DRAWINGS, auth()->user());` (both methods receive `Project $project` as a route parameter per `routes/web.php:552-557` — confirm the exact parameter name during read_first and use it verbatim). In `CommissioningSignoffController::finalise()`, immediately after `$signoff = $this->service->finalise($programme, $request->validated());` succeeds (i.e. inside the try block or after the catch, matching the existing method's control flow — do NOT call this before the `finalise()` call, since a failed finalise must not flip anything), add `$this->deliverablesService->autoFlipIfNotRequired($programme->project, \App\Models\ProjectDeliverable::KEY_SNAGGING, auth()->user());`.

    Create `tests/Feature/Projects/ProjectDeliverableAutoFlipTest.php` (`use RefreshDatabase`) with one test per wired entry point (8 total): for each, seed a project with the relevant `ProjectDeliverable` row pre-set to `not_required`, hit the real HTTP route (not the service directly — this is an integration proof of the wiring, not a re-test of `ProjectDeliverablesService` itself, which Plan 01 already covers at the unit level), then assert the `project_deliverables` row for that key is now `required` AND a `project_deliverable_audits` row exists with `action = auto_flip`. Add ONE additional test proving the negative: creating a document when the deliverable is already `required` does NOT write a new audit row (`assertDatabaseCount('project_deliverable_audits', 0)` scoped to that deliverable).
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=ProjectDeliverableAutoFlipTest` — all 9 cases (8 positive + 1 negative) pass.
    - Every one of the 8 controller call sites is exercised via a real HTTP request in the test (not a direct service call), proving the wiring itself, not just the service logic.
  </acceptance_criteria>
  <verify>
    <automated>"/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" artisan test --filter=ProjectDeliverableAutoFlipTest</automated>
  </verify>
  <done>Drawings + Snagging wired; all 8 entry points proven end-to-end via HTTP; the negative (already-required) case proven not to write a spurious audit row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Document-creation request → auto-flip | The acting user is always the already-authenticated request user (or, for `CommissioningSignoffController::finalise()`, the authenticated user submitting the signoff) — no new untrusted input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-260822-09 | Tampering | 8 new `autoFlipIfNotRequired()` call sites | accept | Each call passes a hardcoded `ProjectDeliverable::KEY_*` constant, never a request-supplied value — no injection surface, the key is fixed per call site by this plan's code, not by the browser |
| T-260822-10 | Denial of Service (self-inflicted) | An extra DB read/write per document creation | accept | Bounded to a single indexed lookup (`project_id` + `deliverable_key` unique index from Plan 01) plus, at most, one conditional update+insert — negligible cost on an already-authenticated, low-frequency creation action |
</threat_model>

<verification>
`php artisan test --filter=ProjectDeliverableAutoFlipTest` green (9 cases). `"/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -l` clean on all 8 controllers.
</verification>

<success_criteria>
Every one of the 8 tab-eligible deliverable-creation entry points auto-flips a Not-required flag back to Required, is audited exactly once per real flip, and never blocks the underlying create action.
</success_criteria>

<output>
Create `.planning/phases/260822-esf-project-deliverables-selection/260822-05-SUMMARY.md` when done.
</output>
