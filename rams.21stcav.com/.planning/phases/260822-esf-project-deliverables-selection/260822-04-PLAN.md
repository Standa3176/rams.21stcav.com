---
phase: 260822-esf
plan: 4
type: execute
wave: 2
depends_on: ["260822-01"]
files_modified:
  - app/Http/Controllers/ProjectController.php
  - resources/views/projects/show.blade.php
  - tests/Feature/Projects/ProjectTabStripTest.php
  - tests/Feature/Projects/ProjectStepperTest.php
  - tests/Feature/Projects/ProjectNextStepTest.php
autonomous: true
requirements: [D-04, D-07, D-08, D-09]

must_haves:
  truths:
    - "Drawings and Snagging appear as real tabs on the project show page (they had none before this phase)"
    - "Programming appears in NEITHER the tab strip NOR the Linked Records card — it is checklist-only per D-05, and this plan does not build a tab, adapter, or storage type for it"
    - "A deliverable marked Not required with zero existing documents is muted and moved to the end of the tab strip under a 'Not required' grouping, with a visible way to add it anyway"
    - "A deliverable marked Not required that ALREADY has documents is rendered exactly like a normal populated tab — never muted, never moved, never implying deletion"
    - "The lifecycle stepper shows a distinct 'skipped' visual for Survey Pending on a project that skipped straight to Engineering — never a false 'done' checkmark"
    - "The 'Next Step' hero card never suggests creating a deliverable that is explicitly marked Not required"
  artifacts:
    - path: "resources/views/projects/show.blade.php"
      provides: "Reconciled $tabs array (11 entries: 8 deliverable-tracked + drawings/snagging additions, 3 input tabs unchanged), D-08/D-09 grouping, stepper skip-state, Next-Step chain guards; dead $outputs array removed"
    - path: "app/Http/Controllers/ProjectController.php"
      provides: "show() eager-loads 'deliverables', $linkedRecords gains Drawings + Snagging entries"
  key_links:
    - from: "resources/views/projects/show.blade.php"
      to: "app/Models/Project.php"
      via: "$project->deliverableState(key) driving tab grouping + stepper skip + next-step guards"
      pattern: "deliverableState"
---

<objective>
Reconcile D-07's canonical-list problem — this codebase spells out "which deliverables exist" in **five live places plus one dead one**, not the three named in CONTEXT.md — against D-04's 9-item list, and implement D-08 (muted + moved to end, never hidden) and D-09 (a deliverable holding data is never hidden or muted, regardless of flag).

**Reconciliation scope (verified against the live code this session, not assumed from RESEARCH):**
1. `show.blade.php:764-774` tab strip (9 entries) — gains Drawings + Snagging (2 new tabs). Quotes/Assets/Project Data stay exactly as-is (D-06 exclusions, not deliverables, never muted/grouped).
2. `ProjectController::show()`'s `$linkedRecords` array (`ProjectController.php:175-241`, 6 entries) — gains Drawings + Snagging entries so the "Linked Records" card's generate/view buttons cover them too.
3. `show.blade.php:487-494`'s `$outputs` array — confirmed genuinely dead code this session (`grep -c '\$outputs'` returns exactly 1, its own assignment; never rendered anywhere in the file). Deleted in this plan, not "left" — it is inert and removing it is safe additive-adjacent cleanup, not scope creep.
4. `DocumentArtifactStorage::TYPE_*` and `DocumentEditAdapterRegistry::DEFAULT_MAP` — **NO changes.** `TYPE_SNAGGING` and `TYPE_DRAWING` already exist (added in earlier phases); Snagging has no edit adapter and `type=drawing`'s adapter is confirmed dead — both are explicit CONTEXT.md Deferred Ideas, not silently skipped. Recorded here so this reconciliation is provably complete against all 6 lists RESEARCH.md found, not just the 3 CONTEXT.md named.
5. Programming has NONE of the above (no tab, no linked-record entry, no storage type, no adapter) BY DESIGN (D-05) — this plan explicitly does not add one anywhere.

Purpose: establishes the ONE place a user looks to see every deliverable's status, muted-but-recoverable for not-required-and-empty, fully visible for anything holding data — directly implementing the UX-05 lesson already recorded in this file ("hiding was tried and reverted").
Output: Reconciled tab strip + linked records; D-11's stepper/next-step fallout (Pitfalls 2 and 3) fixed in the same pass since they live in the same view.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/260822-esf-project-deliverables-selection/260822-CONTEXT.md
@.planning/phases/260822-esf-project-deliverables-selection/260822-RESEARCH.md
@.planning/phases/260822-esf-project-deliverables-selection/260822-PATTERNS.md
@app/Http/Controllers/ProjectController.php
@resources/views/projects/show.blade.php
</context>

<interfaces>
From Plan 01: `Project::deliverableState(string $key): ?string`, `ProjectDeliverable::ALL_KEYS` / `KEY_*` / `STATE_*` constants, `Project::snaggingSignoffs(): HasManyThrough`.

Existing `$tabs` array (`show.blade.php:764-774`, exact text):
```
$tabs = [
    ['key' => 'surveys',    'label' => 'Surveys',           'count' => $countSurvey],
    ['key' => 'rams',       'label' => 'RAMS',              'count' => $countRams],
    ['key' => 'worksheets', 'label' => 'Worksheets',        'count' => $countWorksheet],
    ['key' => 'cable',      'label' => 'Cable Schedule',    'count' => $countCable],
    ['key' => 'om',         'label' => 'O&M',               'count' => $countOm],
    ['key' => 'install',    'label' => 'Install Programme', 'count' => $countInstall],
    ['key' => 'quotes',     'label' => 'Quotes',            'count' => $countQuotes],
    ['key' => 'assets',     'label' => 'Asset Register',    'count' => $countAssets],
    ['key' => 'data',       'label' => 'Project Data',      'count' => null],
];
```
Render loop (`show.blade.php:776-792`) — reuse `ws-tab__count--empty` for the muted "0" pill, this is the exact precedent D-08's mute treatment extends.

Existing stepper (`show.blade.php:702-742`) — `$lifecycle = Project::LIFECYCLE` (line 364), `$currentIdx = array_search($project->status, $lifecycle)` (line 365), render loop marks `$isPast = $i < $currentIdx` as done with a green tick, `$isActive` as the current solid pill, else future/grey.

Existing Next-Step chain (`show.blade.php:410-483`) — `elseif ($countSurvey === 0)`, `elseif ($countRams === 0)`, `elseif ($countWorksheet === 0)`, `elseif ($countOm === 0)` — 4 branches, Cable/Install absent from the chain entirely (out of scope, unchanged).

`$countDrawings` / `$countAssets` already exist as LIVE (not eager-loaded) queries (`show.blade.php:395,397`) — mirror this pattern for the new `$countSnagging`, do not add it to `ProjectController::show()`'s `$project->load([...])` eager-load list.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ProjectController::show() — eager-load deliverables + $linkedRecords reconciliation</name>
  <files>app/Http/Controllers/ProjectController.php</files>
  <read_first>
    - app/Http/Controllers/ProjectController.php lines 115-261 (show(), full method, already read this session)
  </read_first>
  <action>
    Edit `ProjectController::show()`. Add `'deliverables'` to the `$project->load([...])` array (line ~121-131), alongside the existing entries. After the existing `$linkedRecords` array definition (ending at line 241), append 2 new entries following the exact same shape as the `Install Programme` entry (no download/status routes, since none exist yet for these types): `['type' => 'Drawings', 'badge_class' => 'badge-blue', 'records' => $project->drawings()->whereNull('superseded_by_id')->get(), 'route_name' => 'projects.drawings.index', 'generate_route' => route('projects.drawings.index', $project), 'generate_label' => 'View Drawings', 'empty_action_label' => null, 'empty_action_route' => null]` and `['type' => 'Snagging', 'badge_class' => 'badge-grey', 'records' => $project->snaggingSignoffs, 'route_name' => null, 'empty_action_label' => null, 'empty_action_route' => null]` — verify `projects.drawings.index` is the correct route name against `routes/web.php:547-548` before using it. Do NOT add a `$countSnagging` computation here — tab counts (`$countDrawings`, `$countAssets`, etc.) are computed in the Blade file's own `@php` block, not the controller; Task 2 adds `$countSnagging` there, matching that existing pattern exactly.
  </action>
  <acceptance_criteria>
    - `$project->load([...])` array in `show()` contains `'deliverables'`.
    - `$linkedRecords` has 8 entries (6 original + Drawings + Snagging), NOT 9 — Programming is absent by design.
    - `php artisan route:list --name=projects.drawings.index` confirms the route name used exists before it is referenced.
  </acceptance_criteria>
  <verify>
    <automated>"/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -l app/Http/Controllers/ProjectController.php</automated>
  </verify>
  <done>show() eager-loads deliverables; $linkedRecords covers all 8 tab-strip-eligible deliverables (Programming excluded by design).</done>
</task>

<task type="auto">
  <name>Task 2: show.blade.php — $tabs reconciliation (D-04/07/08/09), dead $outputs removal, stepper skip-state, Next-Step chain guards</name>
  <files>resources/views/projects/show.blade.php</files>
  <read_first>
    - resources/views/projects/show.blade.php lines 360-495 (counts, next-step chain, dead $outputs block)
    - resources/views/projects/show.blade.php lines 690-793 (stepper render loop, tab strip array + render loop)
  </read_first>
  <action>
    In the `@php` block (~line 386-397): add `$countSnagging = $project->snaggingSignoffs()->count();` alongside the existing `$countDrawings`/`$countAssets` live-query lines. DELETE the entire `$outputs = [...]` block (lines 487-494) — confirmed dead code this session, never rendered.

    Extend the `$tabs` array (line 764) with 2 new entries in this exact position — after `install`, before `quotes` (grouping deliverable-tracked tabs together, ahead of the 3 input tabs which are untouched by D-08/D-09): `['key' => 'drawings', 'label' => 'Drawings', 'count' => $countDrawings, 'deliverable_key' => 'drawings']` and `['key' => 'snagging', 'label' => 'Snagging', 'count' => $countSnagging, 'deliverable_key' => 'snagging']`. Add a `'deliverable_key'` entry to EVERY existing deliverable-tracked tab too (`surveys`→`site_survey`, `rams`→`rams`, `worksheets`→`worksheet`, `cable`→`cable_schedule`, `om`→`om`, `install`→`install_programme`) — `quotes`/`assets`/`data` get `'deliverable_key' => null` (D-06 — never eligible for muting/grouping).

    Rework the render loop (lines 776-792) to implement D-08/D-09: for each `$t` in `$tabs`, compute `$isNotRequiredEmpty = $t['deliverable_key'] !== null && $project->deliverableState($t['deliverable_key']) === \App\Models\ProjectDeliverable::STATE_NOT_REQUIRED && ($t['count'] === null || $t['count'] === 0);` — D-09's rule is enforced here: a tab with `count > 0` NEVER qualifies as not-required-empty, regardless of its flag. Partition `$tabs` (in the `@php` block, using `collect($tabs)->partition(fn($t) => ! ($t['deliverable_key'] !== null && $project->deliverableState($t['deliverable_key']) === \App\Models\ProjectDeliverable::STATE_NOT_REQUIRED && ($t['count'] ?? 0) === 0))`) into `[$primaryTabs, $mutedTabs]`; render `$primaryTabs` first in the existing unmodified button markup, then IF `$mutedTabs` is non-empty render a small non-interactive `<span class="ws-tabs__divider">Not required</span>` text label, then render `$mutedTabs` with an added `ws-tab--muted` class (new CSS class, low-opacity treatment — mirror the existing `ws-tab__count--empty` visual language, do not invent a new colour system) PLUS a small adjacent "+ Add anyway" link/button inside each muted tab's markup that POSTs to Plan 07's forthcoming `projects.deliverables.update` route with `state=required` for that key pre-filled (use a plain `<form method="POST">` with a hidden `deliverable_key` + `state=required` input and a small submit button styled as a text link — do not block on Plan 07 existing yet; this plan's own automated test only asserts the muted grouping renders and the count pill still shows, not that the "add anyway" POST succeeds end-to-end, since the target route lands in Plan 07). Keep the exact comment block at lines 777-782 (the UX-05 note) — do not delete it, it is exactly the lesson D-08/D-09 encode.

    Fix Pitfall 2 (stepper false-done tick): in the stepper render loop (~line 703-741), compute `$surveySkipped = $step === \App\Models\Project::STATUS_SURVEY_PENDING && $project->deliverableState('site_survey') === \App\Models\ProjectDeliverable::STATE_NOT_REQUIRED && $currentIdx > 1;` inside the `@foreach` (or precompute a single boolean before the loop, since only the Survey Pending index can ever be skipped). Add a third render branch BEFORE the existing `@if ($isActive) ... @elseif ($isPast) ...` chain: `@if ($surveySkipped) <!-- muted pill, dash icon instead of check, label suffixed "(skipped — not required)" --> @elseif ($isActive) ... @elseif ($isPast) ...`. Use the SAME visual weight as the existing `@else` (future/grey) branch, not the done-tick (accent-600 checkmark) branch — a skipped stage must never look completed.

    Fix Pitfall 3 (Next-Step chain wrong prompt): guard each of the 4 branches with an explicit not-required check — `elseif ($countSurvey === 0 && $project->deliverableState('site_survey') !== \App\Models\ProjectDeliverable::STATE_NOT_REQUIRED)`, and equivalently for `$countRams === 0`/`rams`, `$countWorksheet === 0`/`worksheet`, `$countOm === 0`/`om`. "Not yet decided" and "required" both keep prompting exactly as today (unchanged behaviour) — ONLY the explicit `not_required` state suppresses the prompt, falling through to the next branch in the chain (or to no next-step card at all if every remaining branch is also suppressed/satisfied).
  </action>
  <acceptance_criteria>
    - `grep -c '\$outputs' resources/views/projects/show.blade.php` returns 0.
    - `$tabs` array has 11 entries (9 original + drawings + snagging); `programming` does NOT appear anywhere in the array.
    - A project fixture with `rams` marked `not_required` and 0 RAMS documents renders the RAMS tab inside the muted group with an "Add anyway" control; the same fixture with 1 RAMS document (still flagged `not_required`) renders RAMS in the PRIMARY group, unmuted (D-09 proof).
    - A project fixture with `site_survey` not-required and status `engineering` renders the stepper's Survey Pending step with a distinct skipped visual (assert the new CSS class or "(skipped" text is present), never the done-tick class.
    - A project fixture with `site_survey` not-required and 0 surveys does NOT render "Create Site Survey" as the Next Step title.
  </acceptance_criteria>
  <verify>
    <automated>"/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -r "require 'vendor/autoload.php'; $app = require 'bootstrap/app.php'; $app->make(Illuminate\Contracts\Console\Kernel::class)->bootstrap(); app('blade.compiler')->compileString(file_get_contents('resources/views/projects/show.blade.php'));"</automated>
  </verify>
  <done>Tab strip reconciled to the D-04 canonical 9 (8 tracked + Programming absent by design); dead code removed; D-08/D-09 grouping implemented without hiding anything; stepper and next-step both D-11-aware.</done>
</task>

<task type="auto">
  <name>Task 3: Feature tests — ProjectTabStripTest, ProjectStepperTest, ProjectNextStepTest</name>
  <files>tests/Feature/Projects/ProjectTabStripTest.php, tests/Feature/Projects/ProjectStepperTest.php, tests/Feature/Projects/ProjectNextStepTest.php</files>
  <read_first>
    - tests/Feature/Projects/ActualHoursWidgetTest.php (Feature test + Project::factory() + assertSee() pattern to mirror for all 3 new files)
  </read_first>
  <action>
    Create `tests/Feature/Projects/ProjectTabStripTest.php` (`use RefreshDatabase`) with cases proving: (1) Drawings and Snagging tab labels are present on `GET /projects/{project}` for any project; (2) Programming label is ABSENT from the rendered page; (3) a not-required+empty deliverable's tab renders inside the muted group (assert a distinguishing marker — e.g. the `ws-tab--muted` class or the "Not required" divider text — appears in the response body); (4) a not-required+non-empty deliverable (create 1 `RamsDocument` for the project, mark `rams` not_required) does NOT render inside the muted group (assert `ws-tab--muted` is absent, or that the divider/RAMS-tab-with-muted-class ordering does not occur).

    Create `tests/Feature/Projects/ProjectStepperTest.php` with cases: (1) a project in `engineering` status with `site_survey` explicitly `not_required` renders a distinguishing skipped-marker for the Survey Pending step, and does NOT render that step with whatever CSS class/marker is used for the normal done-tick; (2) a project in `engineering` status with `site_survey` `required` (normal path, passed through survey_pending in the fixture's `survey_started_at`) renders the NORMAL done-tick for Survey Pending, unaffected by this plan.

    Create `tests/Feature/Projects/ProjectNextStepTest.php` with cases: (1) a project with `site_survey` `not_required`, zero surveys, zero RAMS (RAMS required/undecided) — asserts the Next Step card title is NOT "Create Site Survey" (asserts it falls through to the RAMS prompt or whatever the next applicable branch is); (2) a project with ALL of survey/rams/worksheet/om `not_required` and zero documents of each — asserts NO Next Step card renders at all (`$nextStep` is null) OR renders some other card if package/review state gates it first — assert based on actually observed behaviour after building the fixture, not a guess.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=ProjectTabStripTest` passes (4 cases).
    - `php artisan test --filter=ProjectStepperTest` passes (2 cases).
    - `php artisan test --filter=ProjectNextStepTest` passes (2 cases).
  </acceptance_criteria>
  <verify>
    <automated>"/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" artisan test --filter="ProjectTabStripTest|ProjectStepperTest|ProjectNextStepTest"</automated>
  </verify>
  <done>All 3 new Feature test files pass, covering D-08/D-09 tab grouping, D-11's stepper skip-state, and D-11's Next-Step chain guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| "Add anyway" muted-tab control → deliverables state | Same authenticated-user-controlled write surface as the rest of this phase; the POST target is built by Plan 07 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-260822-07 | Tampering | "Add anyway" form's hidden `deliverable_key` field | mitigate | Value is one of `ProjectDeliverable::ALL_KEYS`, server-side validated against that enum in Plan 07's controller — a tampered key is rejected, not silently accepted |
| T-260822-08 | Information Disclosure | Muted-tab labelling reveals which deliverables were declined | accept | Same shared-workspace visibility as every other tab on this page — no new audience |
</threat_model>

<verification>
`php artisan test --filter="ProjectTabStripTest|ProjectStepperTest|ProjectNextStepTest"` all green. Blade compile check passes on `show.blade.php`. `"/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -l` clean on `ProjectController.php`.
</verification>

<success_criteria>
Tab strip shows all 8 tracked deliverables (+3 unchanged input tabs), Programming appears nowhere; not-required-and-empty tabs are muted/grouped/recoverable, never hidden; anything holding data is always fully visible; the stepper never shows a false done-tick for a skipped survey stage; the Next-Step card never prompts for an explicitly not-required deliverable.
</success_criteria>

<output>
Create `.planning/phases/260822-esf-project-deliverables-selection/260822-04-SUMMARY.md` when done.
</output>
