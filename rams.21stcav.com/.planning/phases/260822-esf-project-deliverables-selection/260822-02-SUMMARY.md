---
phase: 260822-esf
plan: 2
subsystem: project-lifecycle
tags: [laravel, eloquent, state-machine, status-transitions, project-lifecycle]

# Dependency graph
requires: ["260822-01 (project_deliverables schema + Project::deliverableState())"]
provides:
  - "Project::canTransitionTo() narrow D-11 branch: quote_imported -> engineering, gated on relationLoaded('deliverables') + explicit not_required Survey state"
  - "QuoteImportService::confirm() Hook 1 chooses engineering vs survey_pending based on the just-loaded deliverables state"
  - "Proof (not assumption) that both SurveyService Hook 2 call sites (complete(), submitPublic()) become no-ops with zero SurveyService.php changes"
affects: ["260822-esf plan 03 (health filter, D-12)", "plan 04 (tab strip)", "plan 05 (auto-flip wiring)", "plan 06 (retrofit)", "plan 07 (edit UI)", "plan 08 (import checklist)"]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Narrow special-case branch inside a pure-function state machine, gated on relationLoaded() so the model never issues its own query — TRANSITIONS/TRANSITIONS_BACKWARD constants remain the sole source of truth for every other transition"
    - "Probe canTransitionTo(furthest-target) first to pick an auto-advance target, falling back to the adjacent-stage default — same try/catch/log-warning defensive shape as before"

key-files:
  modified:
    - app/Models/Project.php
    - tests/Unit/ProjectTransitionTest.php
    - app/Core/Modules/QuoteImport/QuoteImportService.php
    - tests/Feature/ProjectAutoAdvanceTest.php

key-decisions:
  - "Followed the plan's narrow-special-case design exactly (per RESEARCH.md Open Question 3) — TRANSITIONS/TRANSITIONS_BACKWARD constants untouched; canTransitionTo() gained exactly one new conditional branch scoped to (quote_imported, engineering)"
  - "Verified — not assumed — that both SurveyService::complete() and SurveyService::submitPublic() no-op correctly once a project has already skipped to engineering via Hook 1. Zero changes made to SurveyService.php; both call sites proven with dedicated feature tests."
  - "Rewrote (not deleted) test_cannot_transition_to_completely_skipped_state into 4 conditional cases: no-relation-loaded (still false, preserves original pinned behaviour), not-required (true), required (false), relation-loaded-but-empty (false — absence of a row is not_yet_decided, not not_required)"

requirements-completed: [D-11]

# Metrics
duration: ~35min
completed: 2026-08-22
---

# Phase 260822-esf Plan 2: D-11 Status-Machine Skip Summary

**`Project::canTransitionTo()` grows one narrow, relation-loaded-gated branch letting `quote_imported -> engineering` skip `survey_pending` only when Site Survey is explicitly Not required; `QuoteImportService::confirm()`'s Hook 1 now probes that branch to pick its auto-advance target; both `SurveyService` Hook 2 call sites are proven — with tests, not inference — to no-op without any code change.**

## Performance

- **Duration:** ~35 min
- **Completed:** 2026-08-22
- **Tasks:** 2/2
- **Files modified:** 4 (0 created)

## Task Commits

Each task was committed atomically:

1. **Task 1: canTransitionTo() narrow special case + rewritten ProjectTransitionTest** — `0b8d657` (feat)
2. **Task 2: Hook 1 skip-to-engineering branch + ProjectAutoAdvanceTest cases (including BOTH Hook 2 no-op call sites)** — `ebae2fe` (feat)

## What Changed (file:line)

### `app/Models/Project.php:521-539` (Task 1)

New branch inserted between the existing forward-transitions check and the backward-transitions `return`, inside `canTransitionTo()`:

```php
if (
    $this->status === self::STATUS_QUOTE_IMPORTED
    && $status === self::STATUS_ENGINEERING
    && $this->relationLoaded('deliverables')
    && $this->deliverableState(\App\Models\ProjectDeliverable::KEY_SITE_SURVEY) === \App\Models\ProjectDeliverable::STATE_NOT_REQUIRED
) {
    return true;
}
```

`TRANSITIONS`/`TRANSITIONS_BACKWARD` constants are completely unchanged. A comment directly above the branch explicitly warns against folding it into those constants.

### `tests/Unit/ProjectTransitionTest.php:91-149` (Task 1)

`test_cannot_transition_to_completely_skipped_state` (asserted the skip was *always* invalid — the opposite of D-11) was rewritten into 4 tests:
- `test_engineering_skip_from_quote_imported_requires_explicit_survey_not_required` — no relation loaded, still `assertFalse` (preserves the original pinned behaviour verbatim).
- `test_engineering_skip_allowed_when_survey_explicitly_not_required` — `assertTrue`.
- `test_engineering_skip_blocked_when_survey_is_required` — `assertFalse` (the Wave-0-mandated guard case).
- `test_engineering_skip_blocked_when_deliverables_loaded_but_empty` — `assertFalse` (relation loaded, no row for `site_survey` — absence must read as `not_yet_decided`, not `not_required`).

All other pre-existing tests in the file are byte-for-byte unchanged. File still extends bare `PHPUnit\Framework\TestCase` (confirmed by `use` statement) — no DB dependency introduced.

### `app/Core/Modules/QuoteImport/QuoteImportService.php:423-452` (Task 2)

Hook 1's guard block was replaced. `$linkedProject?->loadMissing('deliverables');` runs first, then a single `if ($linkedProject?->status === Project::STATUS_QUOTE_IMPORTED)` block probes `canTransitionTo(STATUS_ENGINEERING)` to choose `$targetStatus` (engineering if true, survey_pending otherwise), then calls `canTransitionTo($targetStatus)` again before transitioning — same try/catch/`Log::warning` swallow shape as before, now logging `target_status` too.

### `tests/Feature/ProjectAutoAdvanceTest.php:260-368` (Task 2)

3 new tests appended:
- `test_quote_confirm_advances_project_to_engineering_when_survey_not_required` — writes a `ProjectDeliverable` row (`site_survey` = `not_required`) via `ProjectDeliverablesService::setState()` before calling `confirm()`; asserts final status is `engineering`, `engineering_started_at` is not null, `survey_started_at` IS null, and an activity-log row records the `engineering` transition.
- `test_survey_complete_call_site_is_noop_when_project_already_in_engineering` — project pre-seeded in `STATUS_ENGINEERING`, calls `SurveyService::complete()`, asserts status unchanged, no exception.
- `test_survey_submit_public_call_site_is_noop_when_project_already_in_engineering` — same fixture shape, calls `SurveyService::submitPublic()` with a minimal `['surveyor_name', 'survey_date', 'rooms' => []]` payload (mirrors `PublicSurveyControllerTest.php`'s existing shape) and **no authenticated user in the test context**, matching the real public-link flow where `auth()->user()` is null. Asserts status unchanged, no exception, and the `auth()->user() ?? User::find($project->user_id)` fallback resolves without erroring.

## Test Evidence

Real commands, real output (not claims):

```
$ php artisan test --filter=ProjectTransitionTest
Tests:    18 passed (45 assertions)
Duration: 1.89s

$ php artisan test --filter=ProjectAutoAdvanceTest
Tests:    9 passed (21 assertions)
Duration: 8.58s

$ php artisan test --filter="ProjectDeliverablesServiceTest|ProjectTransitionTest|ProjectAutoAdvanceTest"
Tests:    33 passed (93 assertions)
Duration: 5.93s

$ php artisan test --filter="StateTransitionTest|ProjectHealthServiceTest"
Tests:    14 passed (27 assertions)
Duration: 7.43s
```

`php -l` clean on all 4 touched files (`app/Models/Project.php`, `tests/Unit/ProjectTransitionTest.php`, `app/Core/Modules/QuoteImport/QuoteImportService.php`, `tests/Feature/ProjectAutoAdvanceTest.php`). No Blade files touched, so `blade.compiler->compileString()` was not required this plan.

`git diff --stat app/Core/Modules/Survey/SurveyService.php` — empty; `git status --short` for that file shows nothing. Confirmed zero changes to `SurveyService.php`.

**Full-suite baseline was NOT re-run.** Per the plan's constraint notes, the machine is under heavy contention and a full run previously stalled (see 01-SUMMARY.md). Instead, ran a widened regression slice covering every file that reads `canTransitionTo()` or is adjacent to `Project`'s status machine: `ProjectTransitionTest`, `ProjectAutoAdvanceTest`, `ProjectHealthServiceTest`, `ProjectDeliverablesServiceTest`, and `Commissioning\StateTransitionTest` (the one other production call site of `canTransitionTo()`, targeting `STATUS_COMMISSIONING` — confirmed by direct grep that it is unaffected since the new branch is scoped exactly to `(quote_imported, engineering)`). All 47 tests across these 5 classes pass. This is a genuine gap, not a claim of full-suite green — the next executor/verifier should run the full suite when contention allows, per the phase's standing note in 01-SUMMARY.md.

## Guard Fail-Closed Confirmation

Explicit regression-proof performed and reverted (not left in the codebase):

1. Temporarily replaced the D-11 branch's condition with an unconditional `true` for `(quote_imported, engineering)` — i.e. dropped the `relationLoaded('deliverables')` and `deliverableState(...) === STATE_NOT_REQUIRED` checks entirely.
2. Re-ran `--filter=ProjectTransitionTest`: **3 tests failed** — `test_engineering_skip_from_quote_imported_requires_explicit_survey_not_required` (no relation loaded), `test_engineering_skip_blocked_when_survey_is_required`, and `test_engineering_skip_blocked_when_deliverables_loaded_but_empty`. This proves the rewritten test suite catches an always-allowed regression, not just documents intent.
3. Restored the exact original guard (`relationLoaded('deliverables') && deliverableState(KEY_SITE_SURVEY) === STATE_NOT_REQUIRED`).
4. Re-ran `--filter=ProjectTransitionTest`: 18/18 passing again.

**The guard still fails closed.** `canTransitionTo('engineering')` on a `quote_imported` project with no `deliverables` relation loaded at all returns `false` (the original, unchanged default) — confirmed live by the still-passing first test case in the rewritten suite.

## Deviations from Plan

None — plan executed exactly as written. Both tasks' `<action>` blocks were implemented verbatim, including the exact branch condition, the `loadMissing('deliverables')` + probe-then-decide Hook 1 shape, and all 3 new test cases from `<behavior>`/`<action>`.

## Anything Not Done / Done Differently

Nothing skipped. One clarification: the plan's acceptance criteria for Task 2 asked for "9 total" tests in `ProjectAutoAdvanceTest` (6 original + 3 new) — confirmed exactly 9 pass. Full-suite verification against the documented 1-known-failure baseline was not re-run (see Test Evidence) — this mirrors the same gap 01-SUMMARY.md flagged and left open for the phase, not a new gap introduced by this plan.

## Files Needing Upload to Live

None — no deploy performed or requested. When this phase is ready to ship, the files needing upload are: `app/Models/Project.php`, `app/Core/Modules/QuoteImport/QuoteImportService.php` (both production code; the two test files are dev-only and do not need to reach the VPS).

## User Setup Required

None.

## Next Phase Readiness

- `Project::canTransitionTo()`'s D-11 branch and `QuoteImportService::confirm()`'s Hook 1 update are both live and tested — plan 03 (health filter, D-12) and later plans can rely on the status machine correctly skipping `survey_pending` for not-required-Survey projects.
- Plan 03 (`ProjectHealthService`, D-12) should be aware `STATUS_SURVEY_PENDING`'s red-rule at `ProjectHealthService.php:56-60` will simply never fire for a project that skipped straight to `engineering` — no additional guard needed there for the skip itself, only for D-12's own not-required filtering.
- The stepper / "Next Step" chain (D-11's other two consequences, flagged Wave-0-MISSING in VALIDATION.md as `ProjectStepperTest`/`ProjectNextStepTest`) are explicitly out of scope for this plan and remain open work for a later plan in this phase.

---
*Phase: 260822-esf-project-deliverables-selection*
*Completed: 2026-08-22*

## Self-Check: PASSED

All 4 modified files confirmed present on disk with expected content; both task commit hashes (`0b8d657`, `ebae2fe`) confirmed present in git log.
