---
phase: 260822-esf
plan: 8
type: execute
wave: 4
depends_on: ["260822-01", "260822-02", "260822-07"]
files_modified:
  - routes/web.php
  - app/Http/Controllers/QuoteImportController.php
  - app/Core/Modules/QuoteImport/QuoteImportService.php
  - resources/views/quote-import/review.blade.php
  - resources/views/quote-import/deliverables.blade.php
  - tests/Feature/Projects/DeliverableImportDefaultsTest.php
  - tests/Feature/QuoteImportDeliverablesStepTest.php
autonomous: true
requirements: [D-15, D-16]

must_haves:
  truths:
    - "Confirming a quote import always passes through a distinct deliverables-checklist page — never a modal, never a fieldset bolted onto the existing review form"
    - "A quote with no labour/install line items pre-fills RAMS, Worksheet, and Survey as Not required on that checklist — the user confirms or overrides, never authors from a blank state"
    - "The deliverables actually chosen on that page are what get written for the new project — not a hardcoded default applied silently after the fact"
    - "A not-required Site Survey chosen on this page causes the SAME quote-confirm request to skip straight to Engineering (D-11, wired in Plan 02) — the write must happen before Hook 1 reads it"
  artifacts:
    - path: "resources/views/quote-import/deliverables.blade.php"
      provides: "New interstitial page — the distinct step D-16 requires, not a fieldset"
    - path: "app/Core/Modules/QuoteImport/QuoteImportService.php"
      provides: "confirm() writes the submitted deliverables states BEFORE Plan 02's Hook 1 branch runs"
  key_links:
    - from: "resources/views/quote-import/review.blade.php"
      to: "resources/views/quote-import/deliverables.blade.php"
      via: "form action change — review posts to the new interstitial step, not directly to confirm"
      pattern: "quote-import.deliverables-step"
    - from: "app/Core/Modules/QuoteImport/QuoteImportService.php"
      to: "app/Services/ProjectDeliverablesService.php"
      via: "setInitialStates() called before the Hook 1 canTransitionTo() read"
      pattern: "setInitialStates"
---

<objective>
Implement D-15 (import defaults derived from quote content) and D-16 (the checklist is a real interstitial page between review and confirm, per the CONTEXT.md ⚠ CLARIFICATION — NOT a fieldset on the review form, NOT a modal).

**D-16's locked reading, restated exactly as CONTEXT.md's correction requires:** review → confirm is a single form today (`review.blade.php:53-80` → `QuoteImportController::confirm()`). The rejected option is adding a fieldset to that existing form. The required option is a genuinely separate page: a new route, a new view, and a new controller action sitting BETWEEN `review()` and `confirm()`. This plan builds exactly that — `review.blade.php`'s form action changes to POST to a new `quote-import.deliverables-step` route; that new route's controller renders `deliverables.blade.php` (the interstitial) with the review form's 5 project fields carried forward as hidden inputs plus the 9-item checklist pre-filled per D-15; THAT page's form is what finally POSTs to the (extended) existing `quote-import.confirm` route.

**D-15's signal, verified against live code this session:** `EquipmentCategoryClassifier::classify()` (`app/Services/Imports/EquipmentCategoryClassifier.php:210-270`) buckets install/labour/commissioning/RAMS/site-survey SKU tokens into a `services` category, and this `category` value is PERSISTED onto each row of `ProjectPackage::equipment_list` (confirmed at `QuoteWerksImportService.php:146`, `'category' => $category`) — both import paths run every equipment row through this classifier before the review screen loads. `$hasServiceLine = collect($package->equipment_list)->contains(fn($row) => ($row['category'] ?? null) === 'services');` — when `false`, RAMS/Worksheet/Site Survey default to `not_required` on the checklist; every other deliverable defaults to `not_yet_decided` (D-15 only names those 3; nothing in CONTEXT.md licenses defaulting Cable Schedule/Install Programme/Drawings/Snagging/Programming/O&M to anything but the safe unreviewed default).

**Sequencing dependency (why this plan is Wave 4, after Plan 02):** `QuoteImportService::confirm()`'s existing Hook 1 block (extended by Plan 02 to read `$linkedProject->deliverableState('site_survey')` and decide `survey_pending` vs `engineering`) must read a value THIS plan just wrote in the SAME request. This plan's write must be inserted into `confirm()` strictly BEFORE Plan 02's Hook 1 read — get Plan 02's landed code into context before touching this method, not just the plan prose.

Purpose: the phase's single most user-visible surface — the actual "pop up / check box question" the user originally asked for, built as a real step instead of the mechanism most likely to be dismissed unread.
Output: new interstitial route/view/controller action; `confirm()` extended to accept + persist the submitted checklist before the D-11 skip-logic reads it; D-15 defaults computed server-side.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/260822-esf-project-deliverables-selection/260822-CONTEXT.md
@.planning/phases/260822-esf-project-deliverables-selection/260822-RESEARCH.md
@.planning/phases/260822-esf-project-deliverables-selection/260822-PATTERNS.md
@app/Http/Controllers/QuoteImportController.php
@app/Core/Modules/QuoteImport/QuoteImportService.php
@resources/views/quote-import/review.blade.php
@app/Services/Imports/EquipmentCategoryClassifier.php
</context>

<interfaces>
Current 4-step wizard (`QuoteImportController.php`, full file already read this session):
```
create()  -> quote-import.create   (GET, step 1: upload form)
store()   -> quote-import.store    (POST, step 2: process + dispatch extraction)
review()  -> quote-import.review   (GET, step 3: review extracted data)
confirm() -> quote-import.confirm  (POST, step 4: confirm + link to project)
```
This plan inserts a NEW step between 3 and 4 — `deliverablesStep()` on `quote-import.deliverables-step` (GET renders the interstitial after review's form POSTs to it; the simplest correct shape is a single POST-then-render action, i.e. `deliverablesStep()` is itself a POST handler that VALIDATES the review fields and RENDERS the interstitial view directly as its response, rather than redirecting — avoids a second round-trip and avoids needing session storage for the 5 review fields between requests).

Current `review.blade.php` form (`:53-134`, full block already read this session) — `<form method="POST" action="{{ route('quote-import.confirm', $package) }}" id="confirmForm">` with fields `name`, `ref`, `client_name`, `site_address`, `works_description`, `project_id`, and submit button "Confirm & Open Project". This plan changes the `action` to `route('quote-import.deliverables-step', $package)` and the button text to "Continue" (it is no longer the final confirm).

Current `confirm()` validation (`QuoteImportController.php:120-129`, exact text):
```
$validated = $request->validate([
    'name' => ['required', 'string', 'max:200'],
    'ref' => ['nullable', 'string', 'max:50'],
    'client_name' => ['required', 'string', 'max:150'],
    'site_address' => ['required', 'string', 'max:500'],
    'works_description' => ['nullable', 'string'],
    'project_id' => ['nullable', 'integer', 'exists:projects,id'],
    'new_project' => ['nullable', 'boolean'],
]);
```
This plan adds `'deliverables' => ['required', 'array:' . implode(',', \App\Models\ProjectDeliverable::ALL_KEYS)], 'deliverables.*' => [...3-state Rule::in...]` to this same array.

`QuoteImportService::confirm()` signature today: `public function confirm(User $user, ProjectPackage $package, array $overrides = []): ProjectPackage`. This plan adds a 4th parameter: `array $deliverables = []`.

D-15 signal (`EquipmentCategoryClassifier.php`, confirmed live): `$hasServiceLine = collect($package->equipment_list ?? [])->contains(fn($row) => strtolower(trim((string)($row['category'] ?? ''))) === 'services');`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: New route + deliverablesStep() controller action + review.blade.php action change + new deliverables.blade.php view</name>
  <files>routes/web.php, app/Http/Controllers/QuoteImportController.php, resources/views/quote-import/review.blade.php, resources/views/quote-import/deliverables.blade.php</files>
  <read_first>
    - routes/web.php lines 204-214 (existing quote-import route block, exact insertion point)
    - app/Http/Controllers/QuoteImportController.php lines 100-191 (review() and confirm(), full text already read this session)
    - resources/views/quote-import/review.blade.php lines 43-134 (full confirm form, already read this session)
  </read_first>
  <action>
    Add `Route::post('/quote-import/{package}/deliverables-step', [QuoteImportController::class, 'deliverablesStep'])->name('quote-import.deliverables-step');` in `routes/web.php` immediately after the existing `quote-import.review` route (line 213), before `quote-import.confirm` (line 214).

    Add `public function deliverablesStep(Request $request, ProjectPackage $package): View` to `QuoteImportController.php`, immediately after `review()`. Validate the SAME 7 fields `confirm()` validates today (`name`, `ref`, `client_name`, `site_address`, `works_description`, `project_id`, `new_project`) — copy that exact validation array. Compute D-15 defaults: `$hasServiceLine = collect($package->equipment_list ?? [])->contains(fn($row) => strtolower(trim((string)($row['category'] ?? ''))) === 'services');` then `$defaults = []; foreach (\App\Models\ProjectDeliverable::ALL_KEYS as $key) { $defaults[$key] = in_array($key, ['site_survey', 'rams', 'worksheet'], true) ? ($hasServiceLine ? \App\Models\ProjectDeliverable::STATE_NOT_YET_DECIDED : \App\Models\ProjectDeliverable::STATE_NOT_REQUIRED) : \App\Models\ProjectDeliverable::STATE_NOT_YET_DECIDED; }`. Return `view('quote-import.deliverables', compact('package', 'validated', 'defaults'))` — pass the validated project fields through so the interstitial's form can carry them forward as hidden inputs.

    Edit `review.blade.php`: change the form's `action` (line 53) from `route('quote-import.confirm', $package)` to `route('quote-import.deliverables-step', $package)`; change the submit button text (line 120) from "Confirm & Open Project" to "Continue". Leave every other field, the re-extract form, and the Line Items table below completely untouched.

    Create `resources/views/quote-import/deliverables.blade.php`: a new page (extend the same `layouts.app` / page-header pattern `review.blade.php` uses) titled "Confirm Deliverables" with a short intro line explaining the checklist (mirror the tone of D-16's user-facing rationale — "confirm what this project actually needs before it's created"). Render hidden inputs for every one of the 7 validated review fields (`name`, `ref`, `client_name`, `site_address`, `works_description`, `project_id`, `new_project`) so they carry forward unchanged. Render one row per `\App\Models\ProjectDeliverable::ALL_KEYS` entry: label + 3-option radio group named `deliverables[{key}]`, pre-checked to `$defaults[$key]`. Submit button "Confirm & Open Project" (the ORIGINAL button text moves here, since THIS is now the true final confirm step) posting to `route('quote-import.confirm', $package)`. Include a "← Back to review" link to `route('quote-import.review', $package)`.
  </action>
  <acceptance_criteria>
    - `php artisan route:list --name=quote-import.deliverables-step` shows the new POST route, positioned between `quote-import.review` and `quote-import.confirm`.
    - `review.blade.php`'s confirm form now posts to `quote-import.deliverables-step`, not `quote-import.confirm` directly.
    - `deliverables.blade.php` renders 9 radio groups; a package fixture with `equipment_list` containing zero `category === 'services'` rows pre-checks `site_survey`, `rams`, `worksheet` to `not_required`; a package with at least one `services` row pre-checks those 3 to `not_yet_decided` (not `required` — D-15 only names a `not_required` default signal, it does not claim a positive "definitely required" signal exists).
  </acceptance_criteria>
  <verify>
    <automated>"/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -l app/Http/Controllers/QuoteImportController.php && "/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -l routes/web.php</automated>
  </verify>
  <done>The interstitial page exists as a genuinely distinct step (own route, own view, own controller action) between review and confirm; D-15 defaults are computed server-side and correct for both the with-services and without-services cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend confirm() to accept + persist deliverables before Plan 02's Hook 1 reads them; tests</name>
  <files>app/Http/Controllers/QuoteImportController.php, app/Core/Modules/QuoteImport/QuoteImportService.php, tests/Feature/Projects/DeliverableImportDefaultsTest.php, tests/Feature/QuoteImportDeliverablesStepTest.php</files>
  <behavior>
    - Submitting the interstitial checklist with `site_survey=not_required` results in the created/linked project having a `project_deliverables` row for `site_survey` with `state=not_required` BEFORE the project's status is evaluated for auto-advance.
    - The SAME request auto-advances the project straight to `engineering` (not `survey_pending`) — proving the write-then-read ordering from Plan 02's Hook 1 is correct end-to-end, not just at the unit level.
    - Submitting with `site_survey=required` (or any other non-`not_required` state) results in the normal `survey_pending` auto-advance, unchanged from today.
    - `confirm()` rejects a `deliverables` payload containing an unknown key or an out-of-enum state with a 422, mirroring Plan 07's `updateDeliverables()` validation shape.
    - Every deliverables write from this flow is audited with `action = ACTION_IMPORT_DEFAULT` (or `ACTION_MANUAL_CHANGE` if the user changed a value away from the computed default — Claude's call which is simpler to implement correctly; document the choice made in the SUMMARY).
  </behavior>
  <read_first>
    - app/Core/Modules/QuoteImport/QuoteImportService.php lines 392-449 (confirm(), Plan 02's landed Hook 1 code — READ THE ACTUAL FILE, not this plan's prose, since Plan 02 executed before this plan and its exact final line numbers may differ slightly from what was drafted)
    - app/Http/Controllers/QuoteImportController.php lines 116-191 (confirm(), current full text)
  </read_first>
  <action>
    Edit `QuoteImportController::confirm()` — add to the existing validation array: `'deliverables' => ['required', 'array:' . implode(',', \App\Models\ProjectDeliverable::ALL_KEYS)], 'deliverables.*' => ['required', 'string', Rule::in([\App\Models\ProjectDeliverable::STATE_REQUIRED, \App\Models\ProjectDeliverable::STATE_NOT_REQUIRED, \App\Models\ProjectDeliverable::STATE_NOT_YET_DECIDED])]`. Pass `$validated['deliverables']` through to `$this->service->confirm(user: auth()->user(), package: $package, overrides: $overrides, deliverables: $validated['deliverables']);` (both existing call sites inside this method — the "new project" branch and the "existing project" branch both already call `$this->service->confirm(...)`, add the new argument to the SAME call, do not duplicate the logic).

    Edit `QuoteImportService::confirm()` — add the 4th parameter `array $deliverables = []`. Immediately AFTER the existing `DB::transaction(...)` block that updates the package and BEFORE the Hook 1 block (Plan 02's `$linkedProject = $confirmed->project; ... loadMissing('deliverables') ...`), insert: `if ($confirmed->project && $deliverables !== []) { app(\App\Services\ProjectDeliverablesService::class)->setInitialStates($confirmed->project, $deliverables, $user); $confirmed->project->unsetRelation('deliverables'); }` — the `unsetRelation()` call is REQUIRED: Plan 02's Hook 1 immediately calls `$linkedProject->loadMissing('deliverables')`, and `loadMissing()` is a no-op if the relation is already cached (which it might be, from an earlier point in this same request) — explicitly unsetting first guarantees Hook 1's `loadMissing()` actually re-queries and sees the rows just written. Inject `ProjectDeliverablesService` via constructor promotion instead of the `app()` helper if this class already uses constructor injection elsewhere in the file — match its existing style, `app()` shown here only as a fallback if constructor injection is impractical mid-refactor.

    Create `tests/Feature/Projects/DeliverableImportDefaultsTest.php` proving D-15's default computation in isolation (call `deliverablesStep()` via HTTP, assert the rendered defaults) for both the with-services and without-services `equipment_list` fixtures. Create `tests/Feature/QuoteImportDeliverablesStepTest.php` proving the full 5-behavior list above end-to-end via real HTTP requests through `deliverablesStep()` then `confirm()`.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=DeliverableImportDefaultsTest` passes.
    - `php artisan test --filter=QuoteImportDeliverablesStepTest` passes, including the end-to-end not-required-Survey-skips-to-engineering case (proves Plan 02 + this plan's write ordering together, not either in isolation).
    - `grep -n "unsetRelation" app/Core/Modules/QuoteImport/QuoteImportService.php` shows the guard is present.
  </acceptance_criteria>
  <verify>
    <automated>"/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" artisan test --filter="DeliverableImportDefaultsTest|QuoteImportDeliverablesStepTest"</automated>
  </verify>
  <done>confirm() persists the submitted checklist before Hook 1 reads it; the end-to-end not-required-Survey skip works through the real HTTP flow, not just in unit tests; D-15 defaults are correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Quote-import confirm request → initial deliverables state | Authenticated-user-submitted, same posture as every other write in this phase — no new anonymous surface (quote import already requires auth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-260822-14 | Tampering | `confirm()`'s new `deliverables` array | mitigate | Same `array:key1,key2,...` + `Rule::in()` validation shape as Plan 07's `updateDeliverables()` — unknown keys and out-of-enum states are rejected with 422, not silently coerced |
| T-260822-15 | Tampering | Interstitial page's hidden project-field inputs (`name`, `client_name`, etc.) | accept | Identical risk profile to the EXISTING review→confirm form today — this plan does not change how those fields are trusted, it only re-validates them a second time (defense in depth, not a new boundary) |
</threat_model>

<verification>
`php artisan test --filter="DeliverableImportDefaultsTest|QuoteImportDeliverablesStepTest"` green. `"/c/Users/sonny.tanda/.config/herd/bin/php84/php.exe" -l` clean on both edited PHP files. Blade compile check on `review.blade.php` and `deliverables.blade.php`.
</verification>

<success_criteria>
Every quote import passes through a real, distinct deliverables-confirmation page; RAMS/Worksheet/Survey default to Not required when the quote shows no service line; the confirmed checklist is what gets written, and a not-required Survey correctly skips straight to Engineering in the same request.
</success_criteria>

<output>
Create `.planning/phases/260822-esf-project-deliverables-selection/260822-08-SUMMARY.md` when done.
</output>
