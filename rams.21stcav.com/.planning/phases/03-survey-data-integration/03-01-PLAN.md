---
phase: 03-survey-data-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/2026_04_10_000001_add_global_fields_and_superseded_at_to_site_surveys_table.php
  - app/Models/SiteSurvey.php
autonomous: true
requirements:
  - SURV-01
  - SURV-03

must_haves:
  truths:
    - "The site_surveys table has site_risks, access_constraints, h_and_s_notes, and superseded_at columns after migration"
    - "SiteSurvey model allows mass-assignment and casting of all four new fields"
    - "Existing SiteSurvey records are unaffected (all new columns are nullable)"
  artifacts:
    - path: "database/migrations/2026_04_10_000001_add_global_fields_and_superseded_at_to_site_surveys_table.php"
      provides: "Additive schema migration for 4 new nullable columns"
    - path: "app/Models/SiteSurvey.php"
      provides: "Updated model with new fields in $fillable and $casts"
  key_links:
    - from: "app/Models/SiteSurvey.php"
      to: "site_surveys table"
      via: "Eloquent fillable/casts"
      pattern: "site_risks|access_constraints|h_and_s_notes|superseded_at"
---

<objective>
Add four new columns to site_surveys — three global text fields and a superseded_at timestamp — and update the SiteSurvey model to expose them.

Purpose: These four columns are the prerequisite for all Wave 2 work. ProjectDataService (Plan 02), SurveyService enforcement (Plan 03), and public survey form extension (Plan 04) all depend on this schema existing first. No Wave 2 plan can be executed until this migration has been run.

Output: One additive migration file and an updated SiteSurvey model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-survey-data-integration/03-CONTEXT.md
@.planning/phases/03-survey-data-integration/03-RESEARCH.md

<interfaces>
<!-- Current SiteSurvey $fillable (from app/Models/SiteSurvey.php) -->
protected $fillable = [
    'user_id', 'project_id', 'project_name', 'project_ref',
    'client_name', 'site_address', 'survey_date', 'surveyor_name',
    'general_notes', 'status', 'filename', 'access_token',
    'expires_at', 'submitted_at', 'survey_type',
];

<!-- Current SiteSurvey $casts -->
protected $casts = [
    'survey_date'  => 'date',
    'expires_at'   => 'datetime',
    'submitted_at' => 'datetime',
];

<!-- Most recent migration file pattern (from RESEARCH.md) — use as naming/style reference -->
database/migrations/2026_04_05_100000_add_token_fields_to_site_surveys_table.php
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create additive migration for four new site_surveys columns</name>
  <files>database/migrations/2026_04_10_000001_add_global_fields_and_superseded_at_to_site_surveys_table.php</files>
  <read_first>
    - database/migrations/2026_04_05_100000_add_token_fields_to_site_surveys_table.php (migration style reference)
    - app/Models/SiteSurvey.php (confirm current fillable — no room_data column present)
  </read_first>
  <action>
    Create a new anonymous Migration class. In up(): use Schema::table('site_surveys', ...) to add four nullable columns in order:

    1. $table->text('site_risks')->nullable()->after('general_notes');
    2. $table->text('access_constraints')->nullable()->after('site_risks');
    3. $table->text('h_and_s_notes')->nullable()->after('access_constraints');
    4. $table->timestamp('superseded_at')->nullable()->after('h_and_s_notes');

    In down(): $table->dropColumn(['site_risks', 'access_constraints', 'h_and_s_notes', 'superseded_at']);

    Follow the anonymous class pattern from the referenced 2026_04_05_100000 migration exactly. Filename must be exactly: 2026_04_10_000001_add_global_fields_and_superseded_at_to_site_surveys_table.php

    Do NOT use soft-delete for superseded surveys — the superseded_at nullable timestamp is the chosen mechanism (D-07 from CONTEXT.md). Rationale: soft-delete changes admin view scope; status flag complicates existing isDraft()/isCompleted() helpers; timestamp is additive and audit-friendly.

    After creating the file, run: php artisan migrate
  </action>
  <verify>
    <automated>php artisan migrate:status | grep -i "add_global_fields_and_superseded_at"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at database/migrations/2026_04_10_000001_add_global_fields_and_superseded_at_to_site_surveys_table.php
    - php artisan migrate runs without error
    - php artisan migrate:status shows the migration as "Ran" (or equivalent)
    - Migration file contains: $table->text('site_risks')
    - Migration file contains: $table->text('access_constraints')
    - Migration file contains: $table->text('h_and_s_notes')
    - Migration file contains: $table->timestamp('superseded_at')
    - All four columns are nullable
    - down() method contains dropColumn with all four column names
  </acceptance_criteria>
  <done>Migration run and all four columns exist in the site_surveys table.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update SiteSurvey model — add new fields to $fillable and $casts</name>
  <files>app/Models/SiteSurvey.php</files>
  <read_first>
    - app/Models/SiteSurvey.php (full file — current fillable, casts, and helpers)
  </read_first>
  <action>
    Extend the $fillable array to include the four new columns after 'general_notes':
      'site_risks', 'access_constraints', 'h_and_s_notes', 'superseded_at'

    Extend $casts to add:
      'superseded_at' => 'datetime'

    The three text fields (site_risks, access_constraints, h_and_s_notes) do NOT need a cast entry — they are plain text/string, no casting needed.

    Do NOT add isSuperseded() or any new helper methods in this task — those belong in Plan 03 where the enforcement logic lives.

    Do NOT change any existing property, method, relationship, or boot logic. This is additive-only.

    The aligned column formatting used in the existing $fillable array must be preserved (see CLAUDE.md conventions).
  </action>
  <verify>
    <automated>php artisan test tests/Unit/ProjectDataServiceTest.php</automated>
  </verify>
  <acceptance_criteria>
    - app/Models/SiteSurvey.php $fillable contains 'site_risks'
    - app/Models/SiteSurvey.php $fillable contains 'access_constraints'
    - app/Models/SiteSurvey.php $fillable contains 'h_and_s_notes'
    - app/Models/SiteSurvey.php $fillable contains 'superseded_at'
    - app/Models/SiteSurvey.php $casts contains 'superseded_at' => 'datetime'
    - php artisan test tests/Unit/ProjectDataServiceTest.php exits 0 (all 8 existing tests still pass)
    - No existing $fillable entries removed, no existing methods changed
  </acceptance_criteria>
  <done>SiteSurvey model updated. All four columns are mass-assignable. superseded_at casts to datetime. All 8 existing ProjectDataServiceTest tests pass.</done>
</task>

</tasks>

<threat_model>
  <threat id="T-03-01" severity="low">
    <description>Migration adds nullable columns to a live table — existing rows will have NULL for all four new fields, which is the intended default. No data integrity risk.</description>
    <mitigation>All four columns declared nullable. down() drops them cleanly. No data migration needed.</mitigation>
  </threat>
  <threat id="T-03-02" severity="low">
    <description>superseded_at is a timestamp column that marks a survey as soft-archived. If an attacker could set this via mass-assignment on the model, they could supersede an active survey without authorization.</description>
    <mitigation>superseded_at is added to $fillable but is only ever written by SurveyService::create() internally (Plan 03). Public forms do not submit this field. PublicSurveyController::validatePublicSurvey() does not accept it in its validation rules. No user-controlled input path writes superseded_at directly.</mitigation>
  </threat>
</threat_model>

<verification>
Run the full unit test suite to confirm no regressions from model changes:

```
php artisan test tests/Unit/ProjectDataServiceTest.php
```

All 8 tests must pass. Confirm migration ran:

```
php artisan migrate:status | grep "add_global_fields"
```
</verification>

<success_criteria>
- Migration file exists and has been run (migrate:status shows Ran)
- site_surveys table has columns: site_risks (text null), access_constraints (text null), h_and_s_notes (text null), superseded_at (timestamp null)
- SiteSurvey.$fillable contains all four new column names
- SiteSurvey.$casts includes superseded_at => datetime
- php artisan test tests/Unit/ProjectDataServiceTest.php — all 8 tests pass, zero failures
</success_criteria>

<output>
After completion, create `.planning/phases/03-survey-data-integration/03-01-SUMMARY.md`
</output>
