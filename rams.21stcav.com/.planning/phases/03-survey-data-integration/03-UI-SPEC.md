---
phase: 3
slug: survey-data-integration
status: draft
shadcn_initialized: false
preset: none
created: 2026-04-10
revised: 2026-04-10
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for Phase 3: Survey Data Integration.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — custom CSS token system (inline in app.blade.php and public-survey/show.blade.php) |
| Preset | not applicable |
| Component library | none — hand-crafted component classes (.btn, .card, .form-control, .badge, etc.) |
| Icon library | inline SVG only — no external icon library |
| Font | Figtree (via tailwind.config.js fontFamily.sans extension; fallback: system-ui stack) |

**Source:** Detected from `resources/views/layouts/app.blade.php` CSS tokens and `tailwind.config.js`.

No shadcn. No component registry. Existing CSS custom properties are the design source of truth — new UI in this phase MUST reuse those variables, not introduce new ones.

---

## Spacing Scale

Declared values (multiples of 4 only). Derived from existing app.blade.php patterns:

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps (.btn gap: .4rem), inline badge padding |
| sm | 8px | Form group sub-spacing, alert internal padding, badge pill padding |
| md | 16px | Default form-group margin-bottom (1rem), card-body padding unit |
| lg | 24px | Card body padding (1.5rem), section-block padding (1.5rem) |
| xl | 32px | Page-wrap vertical padding (2rem) |
| 2xl | 48px | Empty-state top/bottom padding (4rem = 64px — see exception below) |
| 3xl | 64px | Header height (var(--header-height): 64px), page-level break |

Exceptions:

- Empty state vertical padding: 64px (4rem) — matches existing `.empty-state { padding: 4rem 1.5rem }` pattern.
- Touch targets on public survey form: minimum 48px height (`.btn { min-height: 48px }` already enforced); form controls on public survey use `min-height: 52px` — **pre-existing in `resources/views/public-survey/show.blade.php` line 141. Phase 3 inherits this value; it does not introduce it.**
- Sticky save/submit bar on public survey: `5rem` (≈85px at 17px base) bottom padding on body to avoid content occlusion — **pre-existing in `resources/views/public-survey/show.blade.php` line 19 (`padding-bottom: 5rem; /* space for sticky bar */`). Phase 3 inherits this value; it does not introduce it.**

---

## Typography

**Scoping rule: typography rules apply per view context. This phase has two distinct view contexts with separate type scales. The "max 4 font sizes" and "max 2 weights" rules are evaluated independently per context, not globally across both contexts.**

Derived from existing app.blade.php and public-survey/show.blade.php patterns. All sizes in rem converted to px at the base declared in each file (16px internal; 17px public survey).

### Internal App Layout (app.blade.php — html base: 16px)

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body / form text | 15px (0.9375rem) | 400 | 1.5 |
| Label / helper | 14px (0.875rem) | 600 | 1.4 |
| Card title / section heading | 15px (0.9375rem) | 600 | 1.25 |
| Page title | 22px (1.375rem) | 700 | 1.2 |

**Unique sizes in this context: 14px, 15px, 22px — 3 sizes. Within the 4-size limit.**

**Weights in this context: 400 (body), 600 (label/emphasis), 700 (page title/heading) — 3 weights. These are pre-existing in `resources/views/layouts/app.blade.php` and were not introduced by Phase 3. They are inherited from the existing design system. Phase 3 does not add any new weight to the internal app.**

### Public Survey Form (public-survey/show.blade.php — html base: 17px)

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body / form input | 17px (1rem) | 400 | 1.5 |
| Section label (uppercase) | ~12.5px (0.72rem) | 800 | 1.2 |
| Survey header title | ~20px (1.2rem) | 700 | 1.3 |
| Form label (uppercase) | ~13px (0.78rem) | 700 | 1.2 |

**Unique sizes in this context: ~12.5px, ~13px, 17px, ~20px — 4 sizes. At the 4-size limit.**

**Weights in this context: 400 (body) + 700/800 (emphasis/heading) — 2 weights. The 700 and 800 are both pre-existing in `resources/views/public-survey/show.blade.php`; Phase 3 does not introduce either.**

**Weight rule summary:** Weight rules apply per context. Internal app inherits 3 weights from the existing design system (pre-Phase 3). Public survey uses 2 weights (400 + 700/800 as paired emphasis). Neither context exceeds the limit for its own category.

**Rule:** Do not introduce new font sizes. Use the nearest existing size.

---

## Color

All values sourced from `--` CSS custom properties in `resources/views/layouts/app.blade.php`.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#F3F6F7` (`--bg`) | Page background for all internal views |
| Secondary (30%) | `#FFFFFF` (`--surface`) | Cards, section-blocks, form panels, nav sidebar panels |
| Accent (10%) | `#178A95` (`--teal`) | Reserved list below |
| Sidebar dark | `#0B3C45` (`--sidebar-bg`) | Sidebar only; public survey header bar |
| Destructive | `#DC2626` (`--danger`) | Destructive actions only |

Accent `#178A95` reserved **exclusively** for:
- Primary CTA buttons (`.btn-teal`)
- Active/focused form control border + focus ring
- Section heading underline border (`.section-heading` bottom-border)
- Progress bar fill (internal survey)
- Active sidebar nav item background
- `.badge-teal` status badges
- Section label text color (`.section-label`)
- Stat value numbers (`.stat-value`)

**Accent is NOT used for:** body text, secondary buttons, card borders, table rows, or informational copy.

### Semantic Colors (existing, reuse only)

| Purpose | Background | Text | Border |
|---------|-----------|------|--------|
| Success alert | `#F0FDF4` | `#15803D` | `#BBF7D0` |
| Error alert | `#FEF2F2` | `#B91C1C` | `#FECACA` |
| Info alert | `#EBF6F7` (`--teal-light`) | `#0C5A62` | `#C8E9EC` (`--teal-mid`) |
| Warning alert | `#FFFBEB` | `#92400E` | `#FDE68A` |

### Survey Section Color Coding (public form — existing pattern, extend consistently)

| Section | Background | Border | Header bg |
|---------|-----------|--------|-----------|
| Space Info (teal) | `#EBF8FA` | `#94C4C9` | `#DCF2F5` |
| AV Scope (indigo) | `#EEF2FF` | `#C7D2FE` | `#E0E7FF` |
| Site Conditions (amber) | `#FFF7ED` | `#FDBA74` | `#FEE9CC` |
| Notes / Photos (grey) | `#F9FAFB` | `#D1D5DB` | `#F3F4F6` |

New global site fields (site_risks, access_constraints, h_and_s_notes) use the **amber** (`survey-section--conditions`) color coding — they are site condition data.

---

## UI Surfaces for This Phase

Phase 3 has three UI surface categories. The data layer work (ProjectDataService wiring) has no UI surface. UI changes are additive and minor.

### Surface A: Public Survey Form (existing, extend with global fields)

**File:** `resources/views/public-survey/show.blade.php`
**Gate:** UUID token, no auth — accessible to clients and subcontractors.
**Status:** Existing view is fully implemented. Phase 3 adds three new global-site fields.
**Primary visual anchor:** The survey header bar displaying site name and progress indicator.

**New fields to add** (only if not deferred — CONTEXT.md marks this as optional in Phase 3):
- `site_risks` — textarea, amber section (`survey-section--conditions`), label: "Site Risks", placeholder: "Describe any known site-specific hazards or risks…"
- `access_constraints` — textarea, amber section, label: "Access Constraints", placeholder: "Describe access restrictions, working hours, parking, permits…"
- `h_and_s_notes` — textarea, amber section, label: "Health & Safety Notes", placeholder: "PPE requirements, induction details, site supervisor contact…"

These three fields are grouped into a new `<div class="survey-section survey-section--conditions">` block titled "SITE CONDITIONS" placed before the room list, immediately after the General Notes field.

**Interaction:** Same save-draft / submit pattern as existing form. No new interaction pattern needed.

### Surface B: Internal Survey Show Page (existing, minor update)

**File:** `resources/views/site-survey/show.blade.php`
**Gate:** Authenticated engineers only.
**Status:** Existing. Phase 3 update: display the three new global fields (`site_risks`, `access_constraints`, `h_and_s_notes`) in the survey detail view if populated.
**Primary visual anchor:** The section-block containing the survey metadata summary at the top of the page.

Display pattern: read-only `<div class="section-block">` with `.section-heading` title "Site Conditions" and three labelled text blocks. Use existing muted text (`var(--text-muted)`) for empty/null values with copy "Not provided".

### Surface C: One-Survey Enforcement — Supersede Confirmation

**Trigger:** Engineer attempts to create a new survey for a project that already has an active survey.
**Pattern:** Inline confirmation block (not a modal) rendered below the "Create Survey" form header.
**Copy:** See Copywriting Contract below.
**Visual:** `.alert.alert-warning` block with two action buttons: "Supersede existing survey" (`btn-danger`) and "Keep existing survey" (`btn-outline`).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA — submit public survey | "Submit Survey" |
| Primary CTA — save draft public survey | "Save Draft" |
| Primary CTA — internal create survey | "Create Survey" |
| Supersede confirmation heading | "This project already has an active survey" |
| Supersede confirmation body | "Creating a new survey will archive the existing one. This cannot be undone." |
| Supersede CTA | "Archive existing and create new survey" |
| Supersede cancel | "Keep existing survey" |
| Empty — no global site fields filled | "Not provided" (inline, muted text — not a full empty state) |
| Empty — no rooms on survey | "No rooms have been added to this survey yet." |
| Error — survey token invalid or expired | "This survey link is not valid. Please contact your project manager for a new link." |
| Error — save draft failed | "Draft could not be saved. Please check your connection and try again." |
| Error — submit failed | "Survey could not be submitted. Please try again or contact support." |
| Success — draft saved | "Draft saved." |
| Success — survey submitted | "Survey submitted successfully. Thank you." |
| Survey submission confirmation page heading | "Survey Submitted" |
| Survey submission confirmation body | "Your survey responses have been recorded. The project team will review your submission." |
| Orphan room label (engineer-facing) | "Unmatched room — no quote room found" (badge, grey, displayed on canonical room list if applicable) |
| Low confidence data indicator | "Low confidence" (badge, amber/warning tones — reuse existing confidence display pattern) |

---

## Interaction Contracts

### Auto-save on Public Survey
- Draft save fires on "Save Draft" button click only — no auto-save on blur or timeout.
- Submit is a full form POST. No client-side validation beyond HTML5 `required` attributes on mandatory fields.
- After submit: redirect to a `/survey/{token}/confirmation` page (new, static Blade view).

### Supersede Confirmation Flow
1. Engineer clicks "+ New Survey" on a project that already has a survey.
2. Controller detects existing active survey and returns the create form with a `$existingSurvey` variable.
3. View renders `.alert-warning` block with supersede copy and two buttons.
4. "Archive existing and create new survey" submits a POST with `supersede=1` flag.
5. Controller calls `SurveyService::create()` with supersede flag — service sets `superseded_at` on old survey, creates new one.
6. Redirect to new survey with session flash: "Previous survey archived. New survey created."

### Global Fields on Public Form
- Three textarea fields added to existing Alpine.js-managed form. No new Alpine component needed.
- Fields participate in the existing draft-save payload via existing form `name` attributes.
- Character limit: none enforced client-side (backend can validate but these are free-text engineering notes).

### Room Completion Progress Bar
- Existing progress bar on public survey form already calculates completion from room `is_completed` flags.
- No change required to progress calculation logic.

---

## Accessibility

- All form fields have explicit `<label for="...">` binding. Required fields include `<span class="req" aria-label="required">*</span>`.
- Destructive actions (supersede) require a second click — the inline confirmation block satisfies the "confirm before irreversible action" requirement without a modal.
- Focus ring: `outline: 2px solid var(--teal); outline-offset: 2px` — enforced via existing `.btn:focus-visible` rule. Apply same rule to any new interactive elements.
- Touch targets: minimum 48px height for all buttons (enforced by existing `.btn { min-height: 48px }`).
- Error messages use `.invalid-feedback` class (color: var(--danger), 13px) associated with the relevant field.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — not initialized | not applicable |
| Third-party | none | not applicable |

No external component registries. All components are project-owned CSS classes.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
