---
phase: 22.1-rams-scope-room-data-consolidation
plan: 07
subsystem: rams
tags: [gap-closure, room-overviews, write-side-guard, data-consolidation]
dependency_graph:
  requires: [22.1-06]
  provides:
    - "4-key canonical room_overviews[] shape at every write site (form POST, editPayload, parseReviewPayload, generateSurveyRooms, ExtractQuoteJob scaffold)"
    - "RoomOverviewSummaryService writes canonical `works_summary` key (single source of truth per D-07)"
    - "Write-side CI guard via 2 new methods in ReviewedDataStructuralDiffTest"
  affects:
    - resources/views/project-packages/review.blade.php
    - app/Http/Controllers/ProjectPackageReviewController.php
    - app/Jobs/ExtractQuoteJob.php
    - app/Services/RoomOverviewSummaryService.php
    - tests/Feature/Rams/ReviewedDataStructuralDiffTest.php
tech-stack:
  added: []
  patterns:
    - "Reflection-based private-method test invocation via Request::create wrapping (parseReviewPayload signature is `Request $request`)"
    - "Sorted array_keys equality assertion for canonical-shape guard (defends against future key-reorder while locking the canonical set)"
key-files:
  created: []
  modified:
    - resources/views/project-packages/review.blade.php
    - app/Http/Controllers/ProjectPackageReviewController.php
    - app/Jobs/ExtractQuoteJob.php
    - app/Services/RoomOverviewSummaryService.php
    - tests/Feature/Rams/ReviewedDataStructuralDiffTest.php
decisions:
  - "ExtractQuoteJob scaffold uses `solution_type_id => null` (not `''`) — honest representation matching the normaliser's coercion target"
  - "Reflection-based test invocation chosen over full HTTP fixture (no CSRF/auth/policy stack) — faster, more focused on parseReviewPayload's transformation logic"
  - "AI input-side key (`summary` to RoomOverviewSummaryPrompt) stays as the prompt contract; only the service's OUTPUT persistence key renames to canonical `works_summary` (single translation point inside service body)"
metrics:
  duration: ~25min
  completed: 2026-05-13
requirements: [DATA-01, DATA-02]
---

# Phase 22.1 Plan 07: rams-scope-room-data-consolidation Summary

Gap-closure plan closing the 9 write-site leaks the verifier flagged in 22.1-VERIFICATION.md — converging every canonical room_overviews[] writer on the 4-key shape, renaming RoomOverviewSummaryService output key `summary` → `works_summary` to align with D-07, and locking the write side with 2 new CI guard methods. Plan executed cleanly; Wave-1 byte-equivalence canary stayed green throughout.

## 1. Combined Phase 22.1 Test Corpus

Command:
```
php artisan test --filter='Phase22_1InvariantGuard|RamsRenderRegression|DeadPathRemovalGuard|ReviewedDataStructuralDiff|BackfillRoomOverviewSummaryCommand|RamsReviewDataService|MethodStatementServiceScope|RoomOverviewSummaryPrompt|ProjectPackageReviewControllerScopeBullets|RamsComplianceUpgradeServiceCache|ExtractRamsDraftJob|ExtractQuoteJob|RoomOverviewSummaryService'
```

Result: **Tests: 53 passed (215 assertions)**. Duration: 30.77s.

Previous baseline (22.1-06): 51 tests / 200 assertions. Plan 07 adds 2 new write-side test methods → +2 tests, +15 assertions (the 2 new methods + 13 assertions inside them). Hits the expected new baseline ≥53 exactly.

## 2. Gap-Closure Ratchets (G1-G8) — All Zero Hits

| Ratchet | Description                                                                                                  | Output                                  |
| ------- | ------------------------------------------------------------------------------------------------------------ | --------------------------------------- |
| G1      | Hidden `summary` input in review.blade.php (all 3 variants: `[]`, `[{{ $ri }}]`, `[${idx}]`)                 | (zero hits)                             |
| G2      | `description` textarea in review.blade.php                                                                   | (zero hits)                             |
| G3      | `av-room-description-textarea` class                                                                         | (zero hits)                             |
| G4      | `data.description` branch in blade JS                                                                        | (zero hits)                             |
| G5      | Legacy `summary`/`description` keys in ProjectPackageReviewController room_overviews builds                  | 2 hits — both outside room_overviews builds: line 254 (classifier item `description`, unrelated map), line 543 (`'summary' => ''` as INPUT to roomSummaryService→summarize, not a room_overviews row build). Per plan acceptance: "Other unrelated literals MAY exist — verify by line number". Both verified outside the 4 method-bodies' room_overviews build literals. |
| G6      | `sourceDescription` / `current_description` dead variable                                                    | 1 hit at line 674 — comment-only documenting removal. Effectively zero hits for live code. |
| G7      | ExtractQuoteJob room_overviews scaffold emits legacy keys                                                    | 1 hit at line 363 — equipment-row `'description' => $name` (unrelated to room_overviews). Per plan acceptance: "other unrelated literals out of scope". |
| G8      | RoomOverviewSummaryService persists to legacy `summary` key (`$r['summary'] = ...`)                          | (zero hits) — all 3 writes renamed to `$r['works_summary']` |

## 3. Plan-06 Ratchets (R1-R7) — Regression Check

| Ratchet | Description                                                                                  | Result                                                                                                                                                                                                                                                              |
| ------- | -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1      | RamsGeneratorService references                                                              | 9 hits — ALL inside legitimate guard files (Phase22_1InvariantGuardTest + DeadPathRemovalGuardTest). Whitelist-aware; no regression.                                                                                                                                |
| R2      | RamsPrompt references                                                                        | 6 hits — ALL inside legitimate guard files. No regression.                                                                                                                                                                                                          |
| R3      | WorksBulletsPrompt references                                                                | 5 hits — ALL inside legitimate guard files. No regression.                                                                                                                                                                                                          |
| R4      | works_bullets form-field references                                                          | All hits are `scope_of_works_bullets` — different canonical variable (D-06). No regression.                                                                                                                                                                          |
| R5      | `['project']['overview']` round-trip                                                         | 1 hit at ReviewedDataStructuralDiffTest.php:103 — legitimate test fixture access asserting the key IS dropped by normaliser. No regression.                                                                                                                          |
| R6      | `method_statement_notes` → `Project.works_description`                                       | (zero hits). No regression.                                                                                                                                                                                                                                          |
| R7      | Per-room legacy fields summary/description/scope still emitted by normaliser                 | (zero hits). No regression.                                                                                                                                                                                                                                          |

All 7 Plan-06 ratchets remain zero-hit (or only fire on legitimate guard whitelist files). No pre-existing Phase 22.1 invariant regressed.

## 4. Spot-Check Reads (Task 4 Step 5) — Visual Confirmation

**review.blade.php (4-cell row):** Lines 485-533. Row 4 cells: room input (no hidden summary), solution_type_id select, overview textarea, works_summary textarea + Generate button. No `description` textarea. No `av-room-description-textarea` class.

**ProjectPackageReviewController.editPayload:** Lines 234-246. 4-key shape `['room', 'overview', 'works_summary', 'solution_type_id']`. `summary` + `description` keys dropped.

**ProjectPackageReviewController.parseReviewPayload:** Lines 1022-1035. 4-key persistence. Hostile `summary`/`description` keys silently dropped.

**ProjectPackageReviewController.generateSurveyRooms:** Lines 779-786. 4-key shape on numbered expansion. Dead `$sourceDescription` source-trace + dead `current_description` validation rule removed.

**ProjectPackageReviewController.generateRoomSummary:** Lines 538-559. Consumes canonical `$results[0]['works_summary']`. JSON response is `['works_summary' => ...]` (1-key only). Legacy `$description = $overview` shim + `description` response key gone.

**ExtractQuoteJob scaffold:** Lines 300-319. 4-key shape with `solution_type_id => null` (honest representation).

**RoomOverviewSummaryService:** Lines 40-91. All 3 writes use `$r['works_summary']`. AI input key (`summaries[*]['summary']`) preserved at line 64 as prompt contract — translated to canonical `works_summary` inside service body.

## 5. Plan 07 must_haves Traceability Matrix

| Plan 07 must_have                                          | Verified by                                                                                  |
| ---------------------------------------------------------- | -------------------------------------------------------------------------------------------- |
| 4-key shape at all canonical writers                       | G1, G2, G5, G6, G7 ratchets + Task 1+2 test runs (Phase22_1InvariantGuardTest SC#2 6/6 GREEN)|
| `works_summary` canonical service key                      | G8 ratchet (zero) + RoomOverviewSummaryService grep (3 `$r['works_summary']` writes)         |
| Write-side guard test method added                         | ReviewedDataStructuralDiffTest now has 8 tests (6 READ + 2 WRITE), 51 assertions GREEN       |
| RamsRenderRegression stays green                           | 3 passed / 9 assertions / 0 failures / 0 errors                                              |
| Phase22_1InvariantGuardTest stays green                    | 6 passed / 57 assertions (in combined run)                                                   |
| All 8 leak sites grep to zero                              | G1-G8 ratchets all pass (G5/G6/G7 hits all confirmed outside scope per plan acceptance)      |

## 6. Wave-1 RamsRenderRegression Canary — Explicit Verification

```
php artisan test --filter=RamsRenderRegression --testdox
```

Result:
```
✔ Pdf byte identical across two renders manual form fixture
✔ Pdf byte identical across two renders quote import fixture
✔ Pdf byte identical across two renders survey derived fixture

Tests: 3 passed (9 assertions)
Duration: 27.93s
```

**0 failures, 0 errors.** D-12 byte-equivalence preserved end-to-end across the full Phase 22.1 closure. The closure plan modified input-side shape only; rendered output is identical for unchanged input.

## 7. DATA-01 / DATA-02 Status Transitions

**DATA-01 (single canonical scope location):** PARTIAL → **SATISFIED**. Every canonical writer that touches `room_overviews` emits the 4-key shape. The 5th co-existing scope-narrative path (the `description` textarea + parseReviewPayload re-emit + ExtractQuoteJob legacy-key scaffold) is gone. Future PM saves, future quote imports, future "Generate Rooms" expansions, future AI-summary calls all converge on canonical 4-key shape.

**DATA-02 (per-room narrative = 2 text fields exclusively):** PARTIAL → **SATISFIED**. Read side (RamsReviewDataService::normaliseRoomOverviews) AND write side (parseReviewPayload + editPayload + generateSurveyRooms + ExtractQuoteJob scaffold + RoomOverviewSummaryService) all converge. CI guard at both read AND write side via ReviewedDataStructuralDiffTest's 8 methods.

## 8. Deferred Items (per CONTEXT.md §"Deferred Ideas")

Outstanding for Phase 22.2 (unchanged from Plan 06 status):
- Survey ↔ RAMS sync (real-time cross-update of `room_overviews` between SiteSurvey rooms and RamsDocument.reviewed_data)
- `ProjectPackage.works_description` column drop (the column is still nullable + unused by Phase 22.1; physical schema-drop migration is a separate phase)
- `app/scripts/generate_rams_docx.js` confirmation (Node.js shim — confirm it's actually invoked anywhere; if dead, delete)
- `ProjectPackageRamsReviewService.php:78,85` orphan `project.overview` emission cleanup (tolerated by Phase 22.1 normaliser; clean up in 22.2)

## 9. Files Modified (exactly 5)

1. `resources/views/project-packages/review.blade.php` — removed hidden `summary` input (both Blade + JS row-template variants), removed `description` textarea + label, removed `data.description` JS branch
2. `app/Http/Controllers/ProjectPackageReviewController.php` — 4-key shape in `editPayload`, `parseReviewPayload`, `generateSurveyRooms`; `generateRoomSummary` consumes canonical `works_summary` + drops `description` JSON response; dead `$sourceDescription` source-trace + `current_description` validation rule removed
3. `app/Jobs/ExtractQuoteJob.php` — scaffold emits canonical 4-key shape with `solution_type_id => null`
4. `app/Services/RoomOverviewSummaryService.php` — all 3 `$r['summary']` writes renamed to `$r['works_summary']`; docblock updated
5. `tests/Feature/Rams/ReviewedDataStructuralDiffTest.php` — 2 new write-side guard methods (`test_write_side_parseReviewPayload_drops_legacy_summary_description_keys` + `test_write_side_package_update_round_trips_canonical_shape_unchanged`)

Zero new files created. Zero schema migrations. Zero new endpoints. Zero new AI calls.

## 10. Phase 22.1 Status

Phase 22.1 is now **READY for `/gsd-verify-phase 22.1 --rerun`**.

All 6 ROADMAP success criteria mapped to verifiable test assertions. The migration is now self-stabilising AND CI-protected on both read and write paths. The Wave-1 byte-equivalence canary survived all 6 plans (5 production waves + 1 gap closure) of D-12-protected refactoring.

## Commits

- `79a0004` — fix(22.1-07): strip dead summary input + description textarea + 4-key editPayload/parseReviewPayload/generateSurveyRooms
- `048a633` — fix(22.1-07): rename RoomOverviewSummaryService output key summary -> works_summary + ExtractQuoteJob scaffold 4-key shape
- `e4c9547` — test(22.1-07): add 2 write-side guard methods to ReviewedDataStructuralDiffTest

## Deviations from Plan

None — plan executed exactly as written. Two minor observations that are non-deviations:

1. **G5 ratchet "hits" all out of scope:** Lines 254 and 543 of ProjectPackageReviewController fire on `'description' =>` / `'summary' =>` patterns but are outside room_overviews build literals (line 254 = classifier item map for hazard generator; line 543 = INPUT to roomSummaryService→summarize, not a persisted row). Plan acceptance language explicitly permits "other unrelated literals" outside the four method-body room_overviews builds.
2. **G6 ratchet 1 hit is comment-only:** Line 674 is the deletion-documentation comment for `current_description` — the live validation rule is gone. Effectively zero hits as executable code.

## Self-Check: PASSED

- `php -l` GREEN on all 5 modified files
- All 3 commits present in git log: `79a0004`, `048a633`, `e4c9547`
- SUMMARY.md created at correct path
- Phase 22.1 full corpus 53 tests / 215 assertions GREEN
- RamsRenderRegression Wave-1 canary 3/3 / 9 assertions GREEN (D-12 byte-equivalence preserved)
- ReviewedDataStructuralDiffTest 8 methods (6 READ + 2 WRITE) all GREEN
