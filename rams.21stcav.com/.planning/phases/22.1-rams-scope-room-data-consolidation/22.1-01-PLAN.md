---
phase: 22.1-rams-scope-room-data-consolidation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03, DATA-04, DATA-05]
must_haves:
  truths:
    - "REQUIREMENTS.md contains a §'Phase 22.1 — RAMS Scope/Room-Data Consolidation' section with DATA-01..05 acceptance criteria"
    - "Each DATA-XX entry has a matching ROADMAP success-criterion in plain language"
    - "DATA-XX entries are uniquely identifiable (no duplicate IDs anywhere in REQUIREMENTS.md)"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Phase 22.1 section with 5 acceptance criteria entries"
      contains: "DATA-01"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/ROADMAP.md"
      via: "criterion text matches ROADMAP §'Phase 22.1' Success Criteria 1..5"
      pattern: "DATA-0[1-5]"
---

<objective>
Close the roadmap/requirements gap: write DATA-01..05 into `.planning/REQUIREMENTS.md` so every downstream plan can reference them in its `requirements` frontmatter. ROADMAP.md success criteria #1-5 already define the acceptance bar in plain language; this plan transcribes them into the canonical requirement-ID format used throughout the .planning system.

Purpose: Without this, plans 02-06 have `requirements: [DATA-01..05]` referencing IDs that do not exist in REQUIREMENTS.md, which breaks the planning audit trail and the gsd-tools requirement-coverage check.

Output: A new `§ Phase 22.1 — RAMS Scope/Room-Data Consolidation` section in REQUIREMENTS.md, modelled on the existing Phase 22 section's formatting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/22.1-rams-scope-room-data-consolidation/22.1-CONTEXT.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only change. No code, no runtime surface, no user input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22.1-01 | Information Disclosure | REQUIREMENTS.md | accept | Documentation file already in repo; no PII or secrets are added by transcribing roadmap success criteria into requirement IDs. |

No security-relevant changes — this plan only edits a tracked markdown documentation file. No new endpoints, no runtime, no input validation removed.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add §"Phase 22.1 — RAMS Scope/Room-Data Consolidation" to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — read full file so the insertion matches the existing Phase 22 section format exactly: heading depth, blank-line spacing, intro paragraph + numbered list format)
    - .planning/ROADMAP.md (read §"Phase 22.1: RAMS Scope/Room-Data Consolidation" success criteria #1-6 — the source text for the DATA-XX entries)
    - .planning/phases/22.1-rams-scope-room-data-consolidation/22.1-CONTEXT.md (read for decision context — D-LOCK invariants that inform the acceptance criteria phrasing, e.g. D-12 byte-equivalence test, D-07 backfill pattern)
  </read_first>
  <action>
Insert a new section `### Phase 22.1 — RAMS Scope/Room-Data Consolidation` into `.planning/REQUIREMENTS.md`, placed AFTER the existing `### Phase 22 — Cable Schedule with Port-Level FKs` section and BEFORE the `### Phase 23 — XTEN-AV-Style Renderer` section. Match the existing Phase 22 section's exact formatting (markdown heading depth `###`, single blank intro paragraph, then `- [ ] **DATA-XX**:` bulleted list — checkboxes UNCHECKED because Phase 22.1 has not yet shipped).

Use this exact content verbatim (do NOT paraphrase — the wording is calibrated against ROADMAP.md success criteria and CONTEXT.md D-LOCK decisions):

```
### Phase 22.1 — RAMS Scope/Room-Data Consolidation

Eliminate field-duplication across the 3-stage RAMS pipeline (`form_data` → `reviewed_data` → `generated_data`). The 2026-05-13 audit identified 5 overlapping "scope/works/space narrative" fields at 3 granularities stored in 5 different JSON locations with inconsistent fallback chains. This phase keeps `generated_data` shape backward-compatible (already-rendered RAMS docs unaffected) but consolidates the canonical source of truth, deprecates redundant fields with a backfill, removes dead-path code, and surfaces previously-invisible AI prose for engineer review.

- [ ] **DATA-01**: Single canonical scope location — a project-wide scope edit propagates to ONE canonical JSON location (`reviewed_data.scope_of_works`). The other 4 storage paths (`form_data.works_description`, `reviewed_data.method_statement_notes`, `reviewed_data.project.overview`, `extracted_data.overview` auto-seed to `Project.works_description`) are deprecated with backfill where data must be preserved.
- [ ] **DATA-02**: Per-room narrative carries exactly TWO text fields — `overview` (engineer-typed prose, the human source of truth) and `works_summary` (AI install-action bullets). The legacy `summary`, `description`, and `scope` fields are removed from the `RamsReviewDataService::normaliseRoomOverviews()` schema. Existing data preserved via the DATA-04 backfill.
- [ ] **DATA-03**: Five dead-path files/code paths removed per the 2026-05-13 audit — `app/Services/RamsGeneratorService.php` (legacy alternate generator), `app/Core/AI/Prompts/RamsPrompt.php` (would violate CLAUDE.md AI-only-for-formatting constraint if called), `app/Core/AI/Prompts/WorksBulletsPrompt.php` (no remaining consumer), the `reviewed_data.project.overview` round-trip in `RamsReviewDataService::normaliseProject()` line 113, and the project-wide `works_bullets` textarea on `project-packages/review.blade.php` lines 449-469.
- [ ] **DATA-04**: Backfill artisan command `php artisan rams:backfill-room-overview-summary` populates `room_overviews[*].works_summary` from any non-empty legacy `summary` field. Dry-run-default with `--apply` flag (mirrors Phase 22 `cables:backfill-port-fks` pattern). Idempotent. Reports 4 outcome categories per row: `backfilled` / `already-set` / `both-set-no-action` / `neither-set`.
- [ ] **DATA-05**: Byte-equivalence golden-file regression test in `tests/Feature/Rams/RamsRenderRegressionTest.php` asserts existing `reviewed_data` records render byte-identical PDFs before and after the cleanup. Uses `hash_file('sha256', $path)` (Phase 22 WR-02 convention). Skips cleanly via `class_exists` / `is_file($binary)` guards when puppeteer / D2 binaries absent (Phase 22 skip pattern).
```

The insertion point in REQUIREMENTS.md is immediately after the closing of the Phase 22 section's list (after the line `- [x] **DRAW-41**: One-shot backfill command for existing cable_schedule_items — populates port FKs where unambiguous, leaves nullable where ambiguous`) and before the line `### Phase 23 — XTEN-AV-Style Renderer`. Preserve the blank line separator (one blank line above and below the new heading) per the existing section spacing.

Do NOT alter any other section of REQUIREMENTS.md. Do NOT bump the milestone-level "Total requirements: 28" tally at the top — DATA-XX entries are Phase 22.1 (v2.0 internal hygiene), not part of the v2.0 milestone deliverable count.
  </action>
  <verify>
    <automated>grep -c "DATA-0[1-5]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[ \] \*\*DATA-0[1-5]\*\*" .planning/REQUIREMENTS.md` returns exactly `5` (one bullet per requirement, all UNCHECKED)
    - `grep -c "### Phase 22.1 — RAMS Scope/Room-Data Consolidation" .planning/REQUIREMENTS.md` returns exactly `1`
    - `grep "DATA-01" .planning/REQUIREMENTS.md` contains the literal string "Single canonical scope location"
    - `grep "DATA-04" .planning/REQUIREMENTS.md` contains the literal string "rams:backfill-room-overview-summary"
    - `grep "DATA-05" .planning/REQUIREMENTS.md` contains the literal string "hash_file('sha256'"
    - `grep -n "### Phase 22 — Cable Schedule with Port-Level FKs" .planning/REQUIREMENTS.md` line number is LESS THAN `grep -n "### Phase 22.1 — RAMS Scope/Room-Data Consolidation" .planning/REQUIREMENTS.md` line number (i.e. 22.1 is placed after 22)
    - `grep -n "### Phase 22.1 — RAMS Scope/Room-Data Consolidation" .planning/REQUIREMENTS.md` line number is LESS THAN `grep -n "### Phase 23 — XTEN-AV-Style Renderer" .planning/REQUIREMENTS.md` line number (i.e. 22.1 is placed before 23)
    - `grep -c "Total requirements:" .planning/REQUIREMENTS.md` still returns `1` (the milestone-level tally line is preserved unchanged)
    - File still parses as valid markdown (no broken table cells, no orphan list bullets) — visually verifiable by reading the file end-to-end
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md contains the new section between Phase 22 and Phase 23, with DATA-01..05 entries that mirror ROADMAP success criteria #1-5 in canonical requirement-ID form, all 5 unchecked, matching the existing Phase 22 section formatting.
  </done>
</task>

</tasks>

<verification>
- Read the new section end-to-end in REQUIREMENTS.md and confirm visual consistency with Phase 22 section (same heading depth, same bullet style, same line spacing).
- `git diff .planning/REQUIREMENTS.md` shows ONLY the new section inserted — no other lines modified.
- Run `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" frontmatter validate ".planning/phases/22.1-rams-scope-room-data-consolidation/22.1-01-PLAN.md" --schema plan` — must return valid=true.
</verification>

<success_criteria>
- DATA-01..05 acceptance criteria written into REQUIREMENTS.md
- Section formatting matches existing Phase 22 / 23 pattern exactly
- No other section of REQUIREMENTS.md is modified
- Plans 02-06 can now reference DATA-XX in their `requirements` frontmatter without dangling IDs
</success_criteria>

<output>
After completion, create `.planning/phases/22.1-rams-scope-room-data-consolidation/22.1-01-SUMMARY.md` per the standard summary template documenting:
1. The exact line range in REQUIREMENTS.md where the new section was inserted
2. The mapping from ROADMAP success-criterion-number to DATA-XX ID
3. Confirmation that Plans 02-06 may now cite `requirements: [DATA-0X]` in their frontmatter
</output>
