# Phase 22.1: RAMS Scope/Room-Data Consolidation — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-13
**Phase:** 22.1-rams-scope-room-data-consolidation
**Areas discussed:** AI-written `description` field visibility

---

## Pre-discussion: Audit & boundary

Prior step: 2026-05-13 audit (`.planning/audits/rams-room-fields-audit-2026-05-13.md`) identified 5 clusters of overlap. User chose Phase 22.1 boundary = Clusters 1+2+5 (Clusters 3+4 defer to Phase 22.2).

| Option | Description | Selected |
|--------|-------------|----------|
| Use audit's proposed boundary | Phase 22.1 = Clusters 1+2+5. Survey↔RAMS sync defers to 22.2. | ✓ |
| Bundle everything into 22.1 | All 5 clusters atomic. Higher risk (in-flight survey workflows). | |
| Different scope | User describes alternative. | |
| Don't insert yet | Pause to re-read audit. | |

**User's choice:** Use audit's proposed boundary
**Notes:** Smaller, lower-risk first pass; survey↔RAMS sync gets a feature flag and dedicated phase.

---

## Gray area selection

User picked **1 of 4** areas to discuss. Unselected areas default to the audit's recommended path.

| Option | Description | Selected for deep-dive |
|--------|-------------|----------|
| AI-written `description` field visibility | Per-room AI prose currently invisible (no edit UI). CLAUDE.md says "AI output must be reviewable". | ✓ |
| `works_description` writer rule | Stop auto-seeding from QuoteWerks PDF? | (defaulted to audit recommendation) |
| Project-wide `works_bullets` textarea fate | Deprecate / rewire / grace period? | (defaulted to audit recommendation) |
| Regression strategy + backfill mechanics | Golden-file PDF byte test + artisan with --apply? | (defaulted to audit recommendation) |

**User's choice:** AI-written `description` field visibility (only)
**Notes:** Other 3 areas accept the audit's pre-stated recommendations without further discussion.

---

## AI-written `description` field — surface or drop?

| Option | Description | Selected |
|--------|-------------|----------|
| Drop it (Recommended) | Stop generating `description`. Use engineer-typed `overview` as method-statement prompt input. Net: 1 fewer AI call per room, no invisible AI output, simpler schema. | ✓ |
| Surface it as editable textarea in rooms review form | Default-collapsed expandable. Engineer can edit/accept. Cost: UI complexity, AI call + regeneration trigger. | |
| Surface it in a separate 'AI suggestions' page | Less clutter on rooms form, but PMs might skip the tab. Effectively invisible if missed. | |
| Keep invisible (audit's concern theoretical) | Accept CLAUDE.md violation. Internal AI→AI handoff argument. | |

**User's choice:** Drop it
**Notes:** Resolves the CLAUDE.md "AI output must be reviewable" tension cleanly. The engineer-typed `overview` is already a human-authored prose paragraph — its quality as method-statement prompt input should equal or exceed the AI-paraphrased `description`. Bonus: 1 fewer AI call per room (cost + latency win).

---

## Claude's Discretion

Three gray areas defaulted to the audit's recommendations (user did not request deep-dive):

1. **`works_description` writer rule** — Audit recommends: stop auto-seeding from raw QuoteWerks PDF. Raw text still appears in review form as starting suggestion. `Project.works_description` becomes "PM-typed/approved only".
2. **Project-wide `works_bullets` textarea** — Audit recommends: deprecate. No RAMS consumer reads it. Survey re-derives bullets from `scope_of_works_bullets` (computed) or `room_overviews[*].works_summary` union.
3. **Regression + backfill mechanics** — Audit recommends: golden-file PDF byte-identity test (Phase 22 `CableScheduleXlsxRegressionTest` pattern) + artisan command `rams:backfill-room-overview-summary` with `--apply` flag (Phase 22 `BackfillCablePortFksCommand` pattern), dry-run-default, 4 outcome categories.

Plus all Cluster 5 dead-path removals (D-07 through D-11) and Cluster 1 derived-field optimisations (D-05, D-06) defaulted to the audit's recommendations.

---

## Deferred Ideas

Captured in CONTEXT.md `<deferred>` section. Headline items:

- **Phase 22.2 — Survey↔RAMS sync + cross-document propagation** — `av_requirements` ↔ `room_overviews[*].overview` one-way sync (survey upstream), formalised `Project.works_description` writer rule, `ProjectPackage.works_description` column drop.
- **Future polish** — Permit auto-derive miss when scope lives only in per-room bullets (the keyword scan at `pdf/rams.blade.php:386-410` doesn't include `works_summary`). Worth a v2.1 polish phase, not 22.1 because the fix adds complexity.
- **5-minute verification** — Confirm `app/scripts/generate_rams_docx.js` is dead before next milestone close.
