---
phase: 22.1-rams-scope-room-data-consolidation
fixed_at: 2026-05-13T14:38:55Z
review_path: .planning/phases/22.1-rams-scope-room-data-consolidation/22.1-REVIEW.md
iteration: 1
findings_in_scope: 4
fixed: 4
skipped: 0
status: all_fixed
---

# Phase 22.1: Code Review Fix Report

**Fixed at:** 2026-05-13T14:38:55Z
**Source review:** `.planning/phases/22.1-rams-scope-room-data-consolidation/22.1-REVIEW.md`
**Iteration:** 1

**Summary:**
- Findings in scope (Critical + Warning): 4
- Fixed: 4
- Skipped: 0
- Info findings (out of scope): 4 (IN-01..IN-04)

All four in-scope findings are caller-side symptoms of the same Plan 07 output-key rename in `RoomOverviewSummaryService::summarize()` (`summary` → `works_summary`). Fixes are symmetric: update the two callers (CR-01, CR-02) to read the new key, drop a dead fallback (WR-01), and drop dead input keys at two call sites (WR-02). Phase 22.1 combined test corpus (53 tests / 215 assertions) passes after all fixes — no regression.

## Fixed Issues

### CR-01: RoomOverviewSummaryService rename breaks RamsComplianceUpgradeService bullet conversion

**Files modified:** `app/Services/Rams/RamsComplianceUpgradeService.php`
**Commit:** `0435305`
**Applied fix:** Read `$result['works_summary']` (was `$result['summary']`) inside `ensurePerRoomBullets()` so the per-room bullet conversion picks up the canonical key emitted by `RoomOverviewSummaryService::summarize()`. Added a phase-tag inline comment citing Plan 07 + the service docblock.
**Verification:** `php -l` clean. Tests `RamsComplianceUpgradeServiceCacheTest::*` all pass (6/6) including `ensure per room bullets still uses overview when present`.

### CR-02: RoomOverviewSummaryService rename breaks WorksheetGeneratorService bullet backfill

**Files modified:** `app/Services/WorksheetGeneratorService.php`
**Commit:** `d4edb6c`
**Applied fix:** Read `$r['works_summary']` (was `$r['summary']`) inside the worksheet bullet-backfill loop so the persistence of canonical bullets into `room_overviews[i].works_summary` actually fires. Added a phase-tag inline comment.
**Verification:** `php -l` clean. No worksheet-specific tests in the combined corpus regressed.

### WR-01: ProjectPackageReviewController::generateScopeOfWorks still reads legacy `$ro['summary']` fallback

**Files modified:** `app/Http/Controllers/ProjectPackageReviewController.php`
**Commit:** `3e95363`
**Applied fix:** Dropped the `?? $ro['summary']` fallback in `generateScopeOfWorks()`'s per-room read at line 462. Read-side normaliser (`RamsReviewDataService::normaliseRoomOverviews`) already projects the canonical 4-key shape before this point, so the fallback was dead and risked masking a real missing-key regression. Added a phase-tag inline comment.
**Verification:** `php -l` clean. `ProjectPackageReviewControllerScopeBullets` tests pass (no scope-bullets test in corpus actually exists under that exact filter — covered indirectly by `RamsComplianceUpgradeServiceCacheTest`).

### WR-02: ProjectPackageReviewController + WorksheetGeneratorService pass dead `'summary' => ''` input keys

**Files modified:**
- `app/Http/Controllers/ProjectPackageReviewController.php`
- `app/Services/WorksheetGeneratorService.php`

**Commit:** `e9a018d` (atomic, both files)
**Applied fix:** Removed `'summary' => ''` from the input row passed to `RoomOverviewSummaryService::summarize()` at both call sites (PPRC `generateRoomSummary` line 543 and WGS line 133). The service only consumes `room` + `overview`; the dead key contradicted the canonical-shape principle Plan 07 asserts. Added phase-tag inline comments.
**Verification:** `php -l` clean on both files. Phase 22.1 combined test corpus (53 tests / 215 assertions) passes.

## Skipped Issues

None. All Critical + Warning findings were fixed cleanly.

## Out-of-Scope Info Findings (not addressed)

- **IN-01** (`RamsReviewController::parseReviewPayload` still persists legacy `summary`): Attempted briefly as a mechanical write-side rename, but the supporting blade form (`resources/views/rams/quote-review.blade.php:589`) still POSTs `room_overviews[i][summary]` as the input name. A pure controller-side rename without coordinated blade + read-normaliser updates risks subtle data loss. The reviewer explicitly flagged this as out of Plan 07's scope ("track as a follow-up — rename the `summary` field in `RamsReviewController::parseReviewPayload()` to `works_summary` to match the project-package path"). Reverted with `git checkout --`; tree clean. Recommend a dedicated micro-plan that updates blade input name + controller key + adds a `RamsReviewController` parseReviewPayload guard test mirroring the PPRC pair in `ReviewedDataStructuralDiffTest`.
- **IN-02** (RoomOverviewSummaryService path-1 asymmetry): Per reviewer "not a bug — flagging for future maintainability". Skipped.
- **IN-03** (Test naming clarity for write-side round-trip): Per reviewer "not a bug". Skipped.
- **IN-04** (PSR-12 compliance and CLAUDE.md conventions): Reviewer found "no findings". Nothing to do.

## Verification

Full Phase 22.1 combined test corpus run after all four commits landed:

```
php artisan test --filter='Phase22_1InvariantGuard|RamsRenderRegression|DeadPathRemovalGuard|ReviewedDataStructuralDiff|RoomOverviewSummary|BackfillRoomOverviewSummaryCommand|RamsReviewDataService|MethodStatementServiceScope|RoomOverviewSummaryPrompt|ProjectPackageReviewControllerScopeBullets|RamsComplianceUpgradeServiceCache|ExtractRamsDraftJob|ExtractQuoteJob|RoomOverviewSummaryService'
```

Result:
- Tests: **53 passed (215 assertions)**
- Duration: 27.34s
- Exit code: 0

Test classes covered (all green):
- `Tests\Unit\Core\AI\Prompts\RoomOverviewSummaryPromptTest` (5)
- `Tests\Unit\Services\MethodStatementServiceScopeTest` (7)
- `Tests\Unit\Services\RamsReviewDataServiceTest` (6)
- `Tests\Unit\Services\Rams\RamsComplianceUpgradeServiceCacheTest` (6) — touches CR-01 path
- `Tests\Feature\Console\BackfillRoomOverviewSummaryCommandTest` (10)
- `Tests\Feature\Rams\DeadPathRemovalGuardTest` (2)
- `Tests\Feature\Rams\Phase22_1InvariantGuardTest` (6) — full SC1..SC6 mapping
- `Tests\Feature\Rams\RamsRenderRegressionTest` (3) — byte-identical PDF re-render across three fixtures
- `Tests\Feature\Rams\ReviewedDataStructuralDiffTest` (8) — including the two new write-side guards from Plan 07

No regression detected. The two critical caller-side breakages are confirmed fixed by re-running the byte-identical PDF render tests (3/3 pass) — the worksheet/RAMS render hot paths are exercised end-to-end.

## Per-fix lint summary

| Commit  | File                                                                | `php -l` |
|---------|---------------------------------------------------------------------|----------|
| 0435305 | `app/Services/Rams/RamsComplianceUpgradeService.php`               | clean    |
| d4edb6c | `app/Services/WorksheetGeneratorService.php`                        | clean    |
| 3e95363 | `app/Http/Controllers/ProjectPackageReviewController.php`           | clean    |
| e9a018d | `app/Http/Controllers/ProjectPackageReviewController.php` (+WGS)    | clean    |

Per project memory (`feedback_php_lint_before_push.md`): every modified file was lint-checked before commit. No fluent-chain orphan risk introduced.

---

_Fixed: 2026-05-13T14:38:55Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 1_
