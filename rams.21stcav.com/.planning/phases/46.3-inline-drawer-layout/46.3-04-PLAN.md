---
phase: 46.3-inline-drawer-layout
plan: 04
type: execute
wave: 4
depends_on: ["46.3-01", "46.3-02", "46.3-03"]
files_modified:
  - tests/Feature/Cockpit/CockpitInlineDrawerEndToEndTest.php
  - .planning/phases/46.3-inline-drawer-layout/46.3-COUNT-LEDGER.md
  - .planning/phases/46.3-inline-drawer-layout/46.3-DEPLOY-NOTE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [DL-01, DL-02, DL-03, DL-04, DL-05, DL-06]
must_haves:
  truths:
    - "A PM can walk the whole page through HTTP: open a module, read it, get back, open another, and generate — with JavaScript off"
    - "All six ROADMAP success criteria for Phase 46.3 are asserted somewhere, and the assertion is named"
    - "The user has judged the page 'simple to use' and 'less scary' in their own words — it is not self-approved"
  artifacts:
    - path: "tests/Feature/Cockpit/CockpitInlineDrawerEndToEndTest.php"
      provides: "The through-HTTP walk of the new layout — open, collapse, back, re-open, generate"
      min_lines: 80
    - path: ".planning/phases/46.3-inline-drawer-layout/46.3-DEPLOY-NOTE.md"
      provides: "The one thing that will break this deploy if forgotten: cockpit.css is a Vite entry"
  key_links:
    - from: "tests/Feature/Cockpit/CockpitInlineDrawerEndToEndTest.php"
      to: "route('projects.cockpit', ...)"
      via: "every step is a real GET against the real route, never a rendered component in isolation"
      pattern: "projects.cockpit"
---

<objective>
Walk the new layout end to end through HTTP, reconcile every count and every retired assertion the
phase moved into one ledger, write down the deploy step that will otherwise be forgotten, and then
STOP for the user — because the two things this phase is actually judged on, "simple to use" and
"less scary", are their words and cannot be self-approved.

Purpose: closes DL-01..DL-06. Output: one end-to-end test, a reconciled ledger, a deploy note, the
REQUIREMENTS table updated, and a blocking human checkpoint.

**THIS PLAN SHIPS NO NEW BEHAVIOUR.** If the walk finds a gap, the gap is RECORDED BY NAME with the
reason — the way 46.2-06 recorded four unopened format cells and DC-07 — and fixed by a follow-up
plan, not patched here. A closing plan that quietly adds a feature is how a phase stops being
reviewable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46.3-inline-drawer-layout/46.3-CONTEXT.md
@.planning/phases/46.3-inline-drawer-layout/46.3-01-SUMMARY.md
@.planning/phases/46.3-inline-drawer-layout/46.3-02-SUMMARY.md
@.planning/phases/46.3-inline-drawer-layout/46.3-03-SUMMARY.md
@.planning/phases/46.3-inline-drawer-layout/46.3-COUNT-LEDGER.md
@.planning/phases/46.3-inline-drawer-layout/46.3-BRACKET-RITUAL.md
@.planning/phases/46.2-doc-creation-cockpit/46.2-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The through-HTTP walk of the new layout</name>
  <files>tests/Feature/Cockpit/CockpitInlineDrawerEndToEndTest.php</files>
  <behavior>
    Every step is a real authenticated GET against `projects.cockpit`, asserted on the response body.
    No component is rendered in isolation and nothing is grepped out of a string where a DOM query
    will do.

    1. Bare page: four module rows, no panel, one Recent activity panel, the "Open full project"
       footnote present.
    2. Open the FIRST module: one row (the right one), one panel, and the panel node after that row
       inside the module container. The other three rows are absent from the body — asserted by their
       titles' absence as well as by the element count, because a row could be rendered empty.
    3. From that page, follow the way-back anchor's own href (parse it out of the body — do not
       construct the URL in the test, or the test proves the route and not the control). Assert the
       response is the bare page with four rows again. **That is the D-02 trap, closed by walking it.**
    4. Open a DIFFERENT module directly. Same shape.
    5. Switch tab to `files`, then `notes`: one panel each time, still beneath its own row, still one
       row, and the way back still present on both.
    6. Open `?action=generate`: the form discloses, the closed-state control's new copy is gone and
       the submit button's `Generate document` is present, the Format fieldset renders the radios
       that already existed, and the Worksheet still says its PDF is not available (DC-07).
    7. Recent activity: extract its subtree at steps 1, 2, 4, 5 and 6 and assert all of them equal.
    8. No `<script`, no handler attribute and no `<select` in the cockpit region at any step — reuse
       the fence's own constant lists rather than re-typing them, so the walk cannot drift from the
       fence.
    9. No row count moves across the whole walk: snapshot every table in
       `CockpitReadOnlyFenceTest::WRITE_SURFACE_TABLES` before and after. A GET walk writes nothing.
  </behavior>
  <action>
    Write `CockpitInlineDrawerEndToEndTest` covering the nine steps above.

    **Spend boundary, inherited from 46.2-06 and non-negotiable.** `QUEUE_CONNECTION` is `sync` in
    `phpunit.xml`, so an un-faked `dispatch()` RUNS the generator inline and spends real money on the
    user's Anthropic account. This walk stops at DISCLOSURE — it opens `?action=generate` and asserts
    the form renders. **It does not submit the form.** If a step ever reaches a generator, put
    `Bus::fake()` first and `Http::fake()` in `setUp()`, and assert the dispatch rather than the
    bytes. Never run a generator to see what happens.

    Derive the module list from `CockpitModulePresenter::moduleMap()`, never a literal — 46.2 D-01
    already moved that number from nine to four once.

    Name each test method after the criterion it proves, and put the ROADMAP criterion number in its
    docblock, so the closing table in Task 2 is a lookup rather than an argument.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter CockpitInlineDrawerEndToEndTest</automated>
  </verify>
  <done>The walk is green; every step asserts on a real HTTP response; no generator was run; the write-surface snapshot is unchanged across the walk.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the ledger, write the deploy note, close the requirements</name>
  <files>.planning/phases/46.3-inline-drawer-layout/46.3-COUNT-LEDGER.md, .planning/phases/46.3-inline-drawer-layout/46.3-DEPLOY-NOTE.md, .planning/REQUIREMENTS.md</files>
  <action>
    **Reconcile the ledger against the code, not against the plans.** For every entry in
    `46.3-COUNT-LEDGER.md`, open the file it names and confirm the number is what the ledger says.
    46.2-06 reported a fence list as unpinned on the strength of a single-line grep against a
    multi-line assertion, and had to retract it — so read the assertion, not a grep hit. If a ledger
    entry is wrong, correct it and say it was corrected.

    Add the phase's closing table: each of the six ROADMAP success criteria, the DL requirement it
    maps to, the test method that proves it, and the verdict. **A criterion with no named test is a
    GAP and is recorded as one** — never marked complete on the strength of a plan having intended it.

    Reconcile the four pinned fence counts one final time: FORBIDDEN_MARKUP 2, DEFERRED_AFFORDANCES
    21, BANNED_HANDLER_ATTRIBUTES 9, WRITE_SURFACE_TABLES 13. Expected unchanged across the whole
    phase. If any moved, the ledger must show which plan moved it, by name, with the reason.

    **Write `46.3-DEPLOY-NOTE.md`.** One page, the deploy-critical facts only:
      - `resources/css/cockpit.css` is a Vite entry (`vite.config.js:32`). **This phase's layout is
        entirely in that file. Deploying without `npm run build` ships the new markup against the
        old stylesheet** — the rows will not collapse, the drawer will not sit beneath its row, and
        the page will look broken rather than merely unchanged. Say it in those words.
      - The three sha256-pinned files are untouched; a mismatch on deploy is a STOP, not a refresh.
      - Nothing else in the phase needs a migration, a queue restart or a config change.
      - Note the repo's own deploy traps that apply: deploy as `stcav`, not `root`.

    Update `.planning/REQUIREMENTS.md`: add DL-01..DL-06 to the status table in the same shape as the
    VL/RV/DC rows, each with the plan number, the date and one sentence of what actually proves it.
    Mark DL-01..DL-06 Complete ONLY where Task 1's walk names a passing test; anything else is
    recorded as a gap with its reason.

    **Do not run `state.advance-plan` or `state.update-progress`** — they have corrupted STATE.md
    five times. Edit STATE.md by hand if the orchestrator asks for it, or leave it to the
    orchestrator.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1</automated>
  </verify>
  <done>The ledger reconciles against the code; the closing table names a test per criterion or a gap; the deploy note exists and leads with the Vite entry; REQUIREMENTS carries DL-01..DL-06 with honest verdicts.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The cockpit's module panel is now an inline drawer beneath the row you clicked. While it is open
    the other rows are gone, so you see one module at a time. Recent activity has moved out of the
    module and into its own panel on the right. The "N visits" phrase is off the rows, and the
    generate control now says it opens a form and names the formats it will offer.

    Everything is query-string state — no JavaScript anywhere on the page — so the back button works
    and every view is bookmarkable.
  </what-built>
  <how-to-verify>
    FIRST, because the layout lives in a Vite-built stylesheet:

      1. Run `npm run build` before looking at the page. Without it you will be looking at the new
         markup wearing the old stylesheet, which will look broken rather than new.

    Then, on the live cockpit for a real project:

      2. Open the page with no module selected. Four module rows; Recent activity in its own panel on
         the right. Is the right-hand panel where you wanted it, and is it showing enough entries?
      3. Click anywhere on the Site survey row — not on the chevron, in the MIDDLE of the row, and
         then on the far LEFT near the icon. Each click must open the drawer. **If only the chevron
         works, stop and say so** — that is the one failure this phase was most likely to ship and it
         is invisible to every test we have except one.
      4. With the drawer open: the other three rows should be gone, and the drawer should sit
         directly beneath the Site survey row.
      5. **Now get back to the list.** Without being told where to look, find the way back. Did you
         find it immediately, or did you have to hunt? That question IS the acceptance criterion for
         D-02 — if you hunted, it is not done.
      6. Open a different module. Switch between Overview / Files / Notes. The way back should be
         there on every one of them.
      7. On any module, click the generate control. Does it now read as opening a form? Can you tell
         before clicking that Word and PDF are behind it? (On Worksheet it will say Word only — there
         is no worksheet PDF, and that is a known gap, DC-07, still waiting on your decision.)
      8. Press the browser back button a few times. Every state should be a real page you can go back
         through.
      9. Check no row says "0 visits" or "2 visits" any more.

    Then the two questions this phase is actually judged on, in your words:

      A. Is it SIMPLE TO USE?
      B. Is it LESS SCARY than before?

    If either is no, say what you saw — a "no" here is the point of the checkpoint, not a failure of it.
  </how-to-verify>
  <resume-signal>
    Type "approved" to close Phase 46.3, or describe what you saw. Answer step 5 and questions A and
    B explicitly — those three are the ones that cannot be checked by a test.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test suite → generators | `QUEUE_CONNECTION=sync` means an un-faked dispatch spends real money. The walk stops at disclosure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46.3-10 | Denial of service | unauthorised AI spend | mitigate | The walk never submits the document form. If a future step must, `Bus::fake()` + `Http::fake()` and assert the dispatch — the 46.2-06 pattern. |
| T-46.3-11 | Repudiation | write-surface tables | mitigate | Snapshot all 13 tables before and after the walk; a GET walk moves none. |
| T-46.3-12 | Tampering | ledger accuracy | mitigate | Every ledger entry is reconciled by reading the assertion, not by grepping a line — the 46.2-06 retraction is the precedent. |
| T-46.3-SC | Tampering | npm/composer installs | mitigate | **This plan installs nothing.** `npm run build` is a BUILD of existing sources, not an install; it adds no dependency. If `npm install` is ever required, STOP — a blocking human checkpoint and a legitimacy audit come first. |
</threat_model>

<verification>
- `gate-46.ps1` → cockpit suite `0 failed`.
- `gate-46.ps1 -Hashes` → three sha256 match `4abd2b24`. **A MISMATCH IS A STOP, never a hash to
  refresh.** Working-tree `Get-FileHash` only; `git hash-object` will NOT match on this CRLF checkout.
- `gate-46.ps1 -Baseline` → `>= 159 passed AND 0 failed`. **Never an equality against 161.**
- Run gate-46.ps1 IN THE FOREGROUND. **Do not background it.**
- **Never** `migrate:fresh --env=testing`; **never** `state.advance-plan` / `state.update-progress`;
  **never** `git stash`.
- `D-46.2-06-01` is NOT fixed by this phase.
</verification>

<success_criteria>
- All six ROADMAP criteria for Phase 46.3 map to a named passing test, or are recorded as gaps.
- The ledger reconciles against the code, including the four pinned fence counts.
- `46.3-DEPLOY-NOTE.md` leads with `npm run build`.
- The human checkpoint is answered by the user. **It is never self-approved**, and
  `workflow.auto_advance` does not apply to it.
</success_criteria>

<output>
Create `.planning/phases/46.3-inline-drawer-layout/46.3-04-SUMMARY.md` when done.
</output>
