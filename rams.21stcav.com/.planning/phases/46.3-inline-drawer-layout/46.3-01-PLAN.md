---
phase: 46.3-inline-drawer-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - app/Support/Cockpit/CockpitModulePresenter.php
  - app/Support/Cockpit/CockpitPanelPresenter.php
  - resources/views/components/cockpit/module-row.blade.php
  - resources/views/components/cockpit/doc-form.blade.php
  - resources/css/cockpit.css
  - tests/Unit/Cockpit/CockpitModulePresenterTest.php
  - tests/Feature/Cockpit/CockpitSpineTest.php
  - tests/Feature/Cockpit/CockpitVisualTest.php
  - tests/Feature/Cockpit/CockpitDocumentFormTest.php
  - tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php
  - .planning/phases/46.3-inline-drawer-layout/46.3-COUNT-LEDGER.md
autonomous: true
requirements: [DL-03, DL-04, DL-06]
must_haves:
  truths:
    - "No module row shows a visit count or any visit phrase"
    - "Every module row still shows exactly one count, and that count is a document count"
    - "The closed-state generate control reads as opening a form and names the formats it will offer"
    - "Clicking anywhere on a module row still opens that module — the stretched-link pairing is intact and asserted from both sides"
  artifacts:
    - path: "app/Support/Cockpit/CockpitModulePresenter.php"
      provides: "count_mode COUNT_DOCUMENTS for all four modules; COUNT_VISITS no longer reached by any row"
      contains: "COUNT_DOCUMENTS"
    - path: "resources/views/components/cockpit/doc-form.blade.php"
      provides: "Closed-state control whose copy is derived from the offered format set"
    - path: "tests/Feature/Cockpit/CockpitVisualTest.php"
      provides: "The stretched-link assertion, widened to measure the pairing rather than two unrelated declarations"
    - path: ".planning/phases/46.3-inline-drawer-layout/46.3-COUNT-LEDGER.md"
      provides: "Every count and every retired assertion this phase moves, by name, with the decision cited"
  key_links:
    - from: "app/Support/Cockpit/CockpitModulePresenter.php"
      to: "app/Support/Cockpit/CockpitPanelPresenter.php"
      via: "the document relation name is READ from the panel presenter's own map, never a second copy"
      pattern: "documentRelation"
    - from: "resources/views/components/cockpit/doc-form.blade.php"
      to: "$offered"
      via: "the control's copy is built from the offered format keys"
      pattern: "offered"
---

<objective>
Take the vestigial visit phrase off the module rows (D-04), relabel the closed-state generate
control so the Word/PDF choice behind it is discoverable (D-05), and — before any of the layout
work starts — strengthen the stretched-link assertion so the silent failure mode this phase is most
likely to ship is caught by a test rather than by review.

Purpose: these three changes touch the ROW and the ROW'S ONE CONTROL. They are independent of the
drawer's position, so they land first, green, and every later plan inherits a click-target
assertion that already bites.

Output: four module rows carrying a document count and no visit phrase; a generate control that
reads as opening a form and says which formats it will offer; a widened click-target test; and
`46.3-COUNT-LEDGER.md`, which is where every count and every retired assertion in this phase is
recorded by name.

**THIS PLAN ADDS NO FORMAT, NO ROUTE AND NO FIELD.** D-05 is a relabel. `CockpitDocumentFormPresenter`
is NOT edited, `DOCUMENT_FIELD_MAP` is NOT edited, and `DocumentFormatInventoryTest` must stay green
with its missing set exactly `['worksheet.pdf']`. If this plan finds itself opening the form
presenter, it has drifted — stop and report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46.3-inline-drawer-layout/46.3-CONTEXT.md
@.planning/phases/45-visit-model-read-only-cockpit/45-BASELINE.md
@.planning/quick/260920-rwc-whole-module-row-opens-the-side-panel/260920-rwc-SUMMARY.md
@.planning/phases/46.2-doc-creation-cockpit/46.2-COUNT-LEDGER.md
@app/Support/Cockpit/CockpitModulePresenter.php
@app/Support/Cockpit/CockpitPanelPresenter.php
@resources/views/components/cockpit/module-row.blade.php
@resources/views/components/cockpit/doc-form.blade.php

<interfaces>
<!-- Extracted so no executor has to go looking. -->

CockpitModulePresenter (app/Support/Cockpit/CockpitModulePresenter.php)
  private const MODULE_MAP — four entries keyed by ProjectDeliverable::KEY_SITE_SURVEY,
    KEY_WORKSHEET, KEY_RAMS, KEY_OM. Each holds: title, description, icon, hue, count_mode,
    relation, visit_types.
  Today: site_survey and worksheet are count_mode COUNT_VISITS with relation null.
         rams -> 'ramsDocuments', om -> 'omManuals', both COUNT_DOCUMENTS.
  public static function moduleMap(): array
  private function count(Project, array $definition, array $section): string
  private function visitPhrase(array $section): string   -- the "N visits · reconstructed" builder
  private function phrase(int $n, string $singular, string $plural): string
  private function documentCount(Project, array $definition): int  -- $project->{$relation}->count()

CockpitPanelPresenter (app/Support/Cockpit/CockpitPanelPresenter.php)
  private const DOCUMENTS — keyed site_survey/worksheet/rams/drawings/om, each with a `relation`:
    site_survey -> 'siteSurveys', worksheet -> 'worksheets', rams -> 'ramsDocuments',
    om -> 'omManuals'.
  public static function documentModules(): array   -- array_keys(self::DOCUMENTS)

doc-form.blade.php
  :118  $openUrl = route('projects.cockpit', [... 'action' => 'generate'])
  :123  $offered = array_filter($formats, fn ($routeName) => $routeName !== null)
  :124  $missing = array_keys(array_filter($formats, fn ($routeName) => $routeName === null))
  :128  $formatLabels = ['word' => 'Word', 'pdf' => 'PDF']
  :139  <a class="cav-qa__control" href="{{ $openUrl }}">Generate document</a>   <-- THE RELABEL
  :289  <button class="cav-qa__control" type="submit">Generate document</button> <-- NOT the relabel

cockpit.css — the stretched-link pairing
  :402  .cav-cockpit .cav-module { position: relative; }
  :495  .cav-cockpit .cav-module__open::after { content:''; position:absolute; inset: 0; }
  :521  .cav-cockpit .cav-module__open:focus-visible { ... }
  :525  .cav-cockpit .cav-module__open:focus-visible::after { outline: 2px solid var(--cav-focus) }

CockpitVisualTest::test_the_whole_module_row_is_the_click_target_without_javascript() :329-378
  Five regex assertions over cockpit.css plus two token-presence checks.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint Group DL, and open the count ledger</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/46.3-inline-drawer-layout/46.3-COUNT-LEDGER.md</files>
  <action>
    Mint requirement group **DL — Inline drawer layout (Phase 46.3)** in `.planning/REQUIREMENTS.md`,
    in the same shape as Group DC at :194, with six entries mapping one-to-one onto the ROADMAP's six
    success criteria for Phase 46.3:

      DL-01 — Clicking a module row opens its panel directly beneath that row (D-01).
      DL-02 — While a drawer is open the other module rows are not rendered, and the route back to
              the list is an obvious, named, JavaScript-free anchor (D-02).
      DL-03 — Recent activity renders in its own panel outside any module and shows the same entries
              whichever module is open (D-03).
      DL-04 — No module row shows a visit count or visit phrase; every row still shows exactly one
              count (D-04).
      DL-05 — The closed-state generate control reads as opening a form and makes the existing
              Word/PDF choice discoverable, WITHOUT changing which formats are offered (D-05).
      DL-06 — The read-only fence still bites with both `cockpitRegion()` brackets judged against the
              new structure, and the whole-row click target is intact.

    Also add the group to the index line at :19 alongside `DC-01..DC-11, Phase 46.2`.

    Then create `46.3-COUNT-LEDGER.md` with three empty sections that later tasks and later plans
    fill: **Counts moved**, **Assertions retired by name**, and **Consequences recorded, not
    papered over**. Seed it with the phase's standing numbers so a later reader can see what the
    phase inherited: FORBIDDEN_MARKUP 2, DEFERRED_AFFORDANCES 21, BANNED_HANDLER_ATTRIBUTES 9,
    WRITE_SURFACE_TABLES 13; cockpit suite 361 passed / 0 failed entering; D-06 baseline gate
    `>= 159 passed AND 0 failed`.
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const r=f.readFileSync('.planning/REQUIREMENTS.md','utf8');const l=f.readFileSync('.planning/phases/46.3-inline-drawer-layout/46.3-COUNT-LEDGER.md','utf8');const ids=['DL-01','DL-02','DL-03','DL-04','DL-05','DL-06'];const miss=ids.filter(i=>!r.includes(i));if(miss.length){console.error('missing',miss);process.exit(1)}if(!/Counts moved/.test(l)||!/Assertions retired by name/.test(l)){console.error('ledger sections missing');process.exit(1)}console.log('ok')"</automated>
  </verify>
  <done>All six DL ids exist in REQUIREMENTS.md, the index line names the group, and the ledger exists with its three sections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Widen the stretched-link assertion so it measures the PAIRING</name>
  <files>tests/Feature/Cockpit/CockpitVisualTest.php</files>
  <behavior>
    - The existing five regex assertions stay, unchanged and un-loosened.
    - NEW: an assertion that `.cav-module__open` carries NO `position` declaration of its own in any
      rule in cockpit.css. This is the half that today is asserted nowhere: the current test proves
      `.cav-module` is relative and the `::after` is `inset: 0`, but a `position: absolute` added to
      `.cav-module__open` itself would satisfy both of those and still collapse the overlay onto the
      28px chevron. Scan every rule whose selector ends in `.cav-module__open` (NOT `::after`, NOT
      `:focus-visible::after`) and fail if any declares `position:`.
    - NEW: an assertion that no rule in cockpit.css removes the row's containing block — i.e. there
      is no `.cav-module` rule declaring `position: static`, and no rule that sets `position` on an
      element BETWEEN `.cav-module` and `.cav-module__open` in the markup. State in the test's
      docblock that "between" here means the module row has no wrapper element around the anchor;
      if a later plan introduces one, THIS assertion is the one that must be extended, and extending
      it is maintenance while deleting it is the failure.
    - NEW: a DOM-side assertion, because every assertion above reads CSS text and none of them reads
      the page. Render the cockpit and assert that inside each `.cav-module` element there is
      EXACTLY ONE descendant `<a>` element, that its href carries `module={key}` for that row's own
      module, and that the row contains no `<button>`, no `tabindex` and no second anchor. The
      stretched link only works when the row holds exactly one anchor; a second one added later
      would sit under the overlay and be unreachable, which is silent in CSS and visible here.
    - The test name and its docblock must say, in one sentence a reviewer can act on, WHAT IS
      MEASURED: the containing block, the overlay, the absence of a competing `position`, and the
      one-anchor-per-row invariant. A reviewer must not have to infer it.
  </behavior>
  <action>
    Extend `CockpitVisualTest::test_the_whole_module_row_is_the_click_target_without_javascript()`
    with the four behaviours above, or split the DOM half into a sibling test named for what it
    measures if the method grows past readable length — either shape is fine, but BOTH halves must
    run in the cockpit suite.

    Do NOT weaken any existing regex to make a new one fit. If the CSS has to move for a later plan,
    the later plan retargets the regex in the commit that moves the CSS and says so — the same rule
    the fence's brackets live under.

    Write this task BEFORE touching any CSS or markup in Task 3, and confirm the widened test passes
    on the untouched tree. A new assertion that is green on arrival and green after the change tells
    you nothing; prove it bites by temporarily adding `position: absolute` to the
    `.cav-module__open` rule, watching it go red, and reverting. Record that red in the summary.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter CockpitVisualTest</automated>
  </verify>
  <done>CockpitVisualTest passes on the untouched tree; the temporary `position: absolute` injection was shown to turn it red and was reverted; the tree is clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Strip the visit phrase from the rows (D-04) and relabel the generate control (D-05)</name>
  <files>app/Support/Cockpit/CockpitModulePresenter.php, app/Support/Cockpit/CockpitPanelPresenter.php, resources/views/components/cockpit/doc-form.blade.php, tests/Unit/Cockpit/CockpitModulePresenterTest.php, tests/Feature/Cockpit/CockpitSpineTest.php, tests/Feature/Cockpit/CockpitDocumentFormTest.php, tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php, .planning/phases/46.3-inline-drawer-layout/46.3-COUNT-LEDGER.md</files>
  <behavior>
    - Site survey row: count reads `N documents` / `1 document`, never `N visits`.
    - Worksheet row: the same. The qualifier suffixes `· reconstructed` and `· superseded` no longer
      appear on any row.
    - RAMS and O&M rows: unchanged, still `N documents`.
    - Every one of the four rows still renders exactly one `cav-module__count` element, so
      `CockpitSpineTest:658` stays green rather than being retired.
    - A not-required module still reads `Not required` — that branch is untouched.
    - The closed-state generate anchor's copy names the formats it will offer, derived from
      `$offered`: both offered reads as a form-opening phrase naming Word and PDF; Word-only
      (Worksheet, DC-07) reads the same phrase naming Word alone. The anchor must not promise a
      format the module does not offer.
    - `DocumentFormatInventoryTest` and `CockpitDocumentFormPresenterTest` stay green with the
      missing set exactly `['worksheet.pdf']`.
  </behavior>
  <action>
    **D-04.** In `CockpitModulePresenter::MODULE_MAP`, move site_survey and worksheet from
    `COUNT_VISITS` to `COUNT_DOCUMENTS` and give each the document relation it already has in
    `CockpitPanelPresenter::DOCUMENTS` — `siteSurveys` and `worksheets`. **Do not hand-copy those
    relation names.** Two maps naming the same relation is exactly how they come to disagree: add a
    `public static function documentRelation(string $key): ?string` to `CockpitPanelPresenter` that
    reads its own `DOCUMENTS` map, and have the module presenter read through it, OR — if reading
    across presenters at const-definition time is awkward in PHP — keep the literal and add a unit
    assertion that for every module key the two maps name the SAME relation. One or the other, never
    neither.

    **`visit_types` STAYS on both entries.** It feeds Phase 45's invariant that every `Visit::TYPE_*`
    reaches exactly one module row (DC-02) and the panel's own visit list, neither of which this
    phase touches. Say so at the entries, because the next reader will see a visit key on a row that
    no longer counts visits and assume it is dead.

    **`visitPhrase()` and `COUNT_VISITS` lose their last caller.** Decide deliberately and record the
    decision in the ledger: either delete both (this repo's own precedent for a dead drawer
    component) or keep them with a comment naming who might use them. Prefer DELETE for
    `visitPhrase()` if nothing else calls it — `CockpitSectionPresenter::visitQualifiers()` is a
    DIFFERENT method with a live caller in the panel and must NOT be touched. Verify with a
    repo-wide grep before deleting, and put the grep in the summary.

    **RECORD THE COST, do not bury it.** `visitPhrase()`'s docblock calls the row's count phrase
    "the page's at-rest disclosure slot" — the one place a reconstructed or superseded visit is
    disclosed without opening anything. Removing it means that disclosure now exists ONLY inside the
    panel's visit rows. Write that into the ledger's "Consequences recorded" section as a named
    consequence of D-04, with the phrase the user used ("strip 0 visits") beside it, so whoever
    reads this later sees it was known and chosen rather than lost.

    **Retire the affected assertions BY NAME with the decision cited**, exactly as 46.2 retired the
    every-visit-type invariant — never delete one to make a red test pass. At minimum these, each
    rewritten to assert the new document phrase or replaced by a named successor:
      CockpitModulePresenterTest:350  '0 visits'  (site_survey)
      CockpitModulePresenterTest:358  '1 visit'
      CockpitModulePresenterTest:362  '2 visits'
      CockpitModulePresenterTest:389  '1 visit · reconstructed'
      CockpitModulePresenterTest:409  '3 visits · 2 reconstructed'
      CockpitModulePresenterTest:422  '2 visits · superseded'
      CockpitModulePresenterTest:426  '3 visits · 2 superseded'
      CockpitModulePresenterTest:446  (the qualifier round-trip)
      CockpitModulePresenterTest:493  '2 visits' (worksheet)
    Each retirement goes in the ledger with the line number, the old expectation, the new one, and
    `D-04` as the reason. If the executor finds a tenth, it goes in the ledger too.

    ALSO add one NEW assertion that the phase can be judged on: over the rendered page, no module row
    contains the substring ` visit` or ` visits` anywhere in its subtree. That is criterion 3 stated
    as a test rather than as a hope.

    **D-05 — a relabel and nothing else.** In `doc-form.blade.php`, replace ONLY the closed-state
    anchor copy at :139. The submit button at :289 keeps `Generate document`, because that control
    genuinely does generate. Build the anchor's copy from `$offered` and `$formatLabels`, which are
    already in scope at :123-128 — something of the shape "Create document — Word or PDF" when both
    are offered and "Create document — Word" when one is, with the joining word derived from the
    count rather than hardcoded for two. The exact wording is the executor's discretion inside these
    three constraints:
      (a) it must read as opening a form, not as producing a file;
      (b) it must name only formats actually in `$offered`;
      (c) **before using it, check the chosen string as a SUBSTRING against all 21
          DEFERRED_AFFORDANCES keys and against the FORBIDDEN_MARKUP entries**, the same check
          46.2-05 made for `Generate document`. Put the check in the summary. If it collides,
          choose different copy — never lift a fence entry for a label.
    Add the copy-vs-fence check as a real assertion in `CockpitReadOnlyFenceTest` or
    `CockpitDocumentFormTest` so a later copy edit cannot quietly collide.

    **NOTHING about the formats changes.** No new format key, no new route, no edit to
    `CockpitDocumentFormPresenter`. The Worksheet still says plainly that its PDF is not available
    (DC-07) via the existing `$missing` note.

    Fence counts: judge all four (2 / 21 / 9 / 13) against this plan and state each verdict by name
    in the summary. The expectation is 2 / 21 / 9 / 13 unchanged — this plan adds no control, no
    handler, no script and no write. If one genuinely must move, move it by name with the reason
    inline, keeping the assertion exact.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1</automated>
  </verify>
  <done>
    The cockpit suite is `>= 361 passed, 0 failed`. No module row renders a visit phrase.
    `DocumentFormatInventoryTest` and `CockpitDocumentFormPresenterTest` are green with the missing
    set exactly `['worksheet.pdf']`. Every retired assertion is named in the ledger with D-04 cited.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PM browser → cockpit GET | Only reads. `?module=` is already coerced to a known key and never echoed. |
| Presenter → project relations | `documentCount()` counts a relation named by a const map, never by user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46.3-01 | Tampering | `MODULE_MAP.relation` | mitigate | The relation name comes from a const map (or from `CockpitPanelPresenter::documentRelation()`); no request value ever reaches `$project->{$relation}`. Asserted by the two-maps-agree unit test. |
| T-46.3-02 | Information disclosure | new anchor copy | mitigate | The copy is built from `$offered`, which is the presenter's own format map — it can name no document and no project data. |
| T-46.3-03 | Elevation of privilege | none | accept | This plan adds no route, no control and no write. The cockpit's existing authorisation is untouched. |
| T-46.3-SC | Tampering | npm/composer installs | mitigate | **This plan installs nothing.** If an install becomes necessary, STOP: RESEARCH.md has no Package Legitimacy Audit for this phase and a blocking human checkpoint is required before any install. |
</threat_model>

<verification>
- `powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1` → `>= 361 passed, 0 failed`.
- `powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Path tests/Feature/Documents` → green, `worksheet.pdf` still the only missing cell.
- `powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Hashes` → all three sha256 match. **A MISMATCH IS A STOP, never a hash to refresh.**
- `powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Baseline` → `>= 159 passed AND 0 failed`. **Never an equality against 161.**
- Run gate-46.ps1 IN THE FOREGROUND. **Do not background it** — it has hung silently for 26 minutes at near-zero CPU with an empty output file.
- **Never** `artisan migrate:fresh --env=testing`. There is no `.env.testing`; it would drop every table in the user's real local `database/database.sqlite`.
- **Never** `state.advance-plan` / `state.update-progress` (corrupted STATE.md five times). **Never** `git stash`.
</verification>

<success_criteria>
- DL-04: no module row shows a visit count or visit phrase, asserted over the rendered page.
- DL-05: the closed-state control reads as opening a form and names only offered formats; the
  format set is byte-unchanged.
- DL-06 (half): the stretched-link test now measures the pairing and was proven to go red.
- Every retired assertion is in `46.3-COUNT-LEDGER.md` by name with D-04 cited.
- Fence counts 2 / 21 / 9 / 13, each judged by name.
- No hex literal in any Blade file; every new class `cav-`-prefixed.
- `D-46.2-06-01` (the `<x-edit-action-bar/>` in a CSS comment) is NOT fixed here — out of scope.
</success_criteria>

<output>
Create `.planning/phases/46.3-inline-drawer-layout/46.3-01-SUMMARY.md` when done.
</output>
