---
phase: 46.3-inline-drawer-layout
plan: 03
type: execute
wave: 3
depends_on: ["46.3-02"]
files_modified:
  - resources/views/projects/cockpit.blade.php
  - resources/views/components/cockpit/panel.blade.php
  - resources/views/components/cockpit/activity-panel.blade.php
  - resources/css/cockpit.css
  - tests/Feature/Cockpit/CockpitPanelTest.php
  - tests/Feature/Cockpit/CockpitPageTest.php
  - tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php
  - .planning/phases/46.3-inline-drawer-layout/46.3-COUNT-LEDGER.md
autonomous: true
requirements: [DL-03]
must_haves:
  truths:
    - "Recent activity renders outside any module, in its own right-hand panel"
    - "It shows the same entries whichever module is open, and it renders with no module open at all"
    - "No module panel carries a Recent activity card any more"
  artifacts:
    - path: "resources/views/components/cockpit/activity-panel.blade.php"
      provides: "The project-level Recent activity panel, rendering x-cockpit.activity-row exactly as the panel card did"
      min_lines: 20
    - path: "resources/views/components/cockpit/panel.blade.php"
      provides: "The module panel without the Recent activity card, and without the `activity` prop"
  key_links:
    - from: "resources/views/projects/cockpit.blade.php"
      to: "x-cockpit.activity-panel"
      via: "rendered from the page's own $activity, at page level, never inside the module loop"
      pattern: "activity-panel"
    - from: "resources/views/components/cockpit/activity-panel.blade.php"
      to: "x-cockpit.activity-row"
      via: "the same row component the panel card used — the feed is moved, not re-implemented"
      pattern: "activity-row"
---

<objective>
Move Recent activity out of the module panel and into its own right-hand panel (D-03). It is
project-level data — it has always shown the same three entries whichever module was open — and the
inline drawer of Plan 46.3-02 vacated the column it now fills.

Purpose: DL-03, and the last of the user's three asks. Output: one new
`x-cockpit.activity-panel`, one card removed from `panel.blade.php`, one prop dropped, and the
right column back in use.

**THE FEED IS MOVED, NOT REBUILT.** It renders the same `x-cockpit.activity-row` with the same five
attributes off the same controller-supplied `$activity` collection. No new query, no new presenter
method, no change to what an entry says. If this plan finds itself editing
`ProjectActivityLog` or a presenter's activity derivation, it has drifted — stop and report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46.3-inline-drawer-layout/46.3-CONTEXT.md
@.planning/phases/46.3-inline-drawer-layout/46.3-02-SUMMARY.md
@.planning/sketches/004-delivery-cockpit/README.md
@resources/views/components/cockpit/panel.blade.php
@resources/views/projects/cockpit.blade.php

<interfaces>
<!-- Extracted so no executor has to go looking. -->

The block being moved, today in panel.blade.php inside the `@if ($tab === 'overview')` arm:

  <div class="cav-panel__card">
    <span class="cav-panel__card-head">Recent activity</span>
    @if ($activity->isNotEmpty())
      <ul class="cav-acts">
        @foreach ($activity as $entry)
          <x-cockpit.activity-row :initials :actor :phrase :hue="$entry['hue'] ?? 0" :at />
        @endforeach
      </ul>
    @else
      <x-cockpit.hint>Nothing has been recorded against this project yet.</x-cockpit.hint>
    @endif
  </div>

  Its docblock comment is load-bearing and must travel with it:
  "Project-wide on purpose: the log has no module column, so the same feed renders under every
   module rather than a filtered one that would silently drop every entry carrying no metadata hint."

panel.blade.php @props — `'activity' => null` is dropped by this plan.
  Line in the @php block: `$activity = $activity ?? collect();` goes with it.

cockpit.blade.php — `$activity` is ALREADY in page scope (it is passed to the panel at :158), so no
  controller change is required. Confirm that before assuming it; if the controller only derives it
  conditionally on `$openModule`, move the derivation so it is unconditional — the panel must render
  with no module open.

Existing assertions that name the feed
  CockpitPanelTest:20   docblock: "Recent activity feed."
  CockpitPanelTest:273  section header comment "── Recent activity (D-14) ──"
  CockpitPanelTest:287  assertStringContainsString('Recent activity', $html) — over PANEL html

Sketch 004
  D-14 — "a Recent activity feed in the panel". **This plan narrows D-14**, the way 46.2 narrowed
  D-16. Record the narrowing where D-08..D-16 live; do not edit D-14's wording away.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the feed into x-cockpit.activity-panel and render it at page level</name>
  <files>resources/views/components/cockpit/activity-panel.blade.php, resources/views/projects/cockpit.blade.php, resources/views/components/cockpit/panel.blade.php, resources/css/cockpit.css</files>
  <behavior>
    - With NO module open, the page renders exactly one Recent activity panel.
    - With any module open and any tab selected, the page renders exactly one Recent activity panel,
      and its rendered subtree is IDENTICAL across all four modules and all three tabs. Assert
      identity by comparing the extracted subtree strings, not by counting entries — "same entries
      whichever module is open" is criterion 2 and equality is what it means.
    - No `cav-panel` subtree contains the string `Recent activity` any more.
    - With no activity at all, the panel renders its hint sentence rather than disappearing — an
      absent panel would read as a page fault, which is the same reasoning the not-required module
      row uses.
  </behavior>
  <action>
    Create `resources/views/components/cockpit/activity-panel.blade.php` taking one prop,
    `activity`, defaulting to an empty collection. Move the block above into it verbatim, including
    its docblock comment about why the feed is project-wide — that comment is now MORE relevant, not
    less, because the panel's position finally matches what the data always was. Add one sentence
    saying D-03 moved it here and why.

    Class names: the card head class `cav-panel__card-head` belonged to the module panel. Give the
    new panel its own `cav-`-prefixed names rather than borrowing the module panel's BEM block — the
    same reasoning `cav-pnote` used when it refused to share `cav-note`. Two unrelated things sharing
    a class is how a later styling edit reaches something it was not aimed at. Keep `cav-acts` and
    `x-cockpit.activity-row` as they are; those are the feed's own.

    In `cockpit.blade.php`, render `<x-cockpit.activity-panel :activity="$activity" />` at page
    level, as a sibling of `.cav-modules` inside `.cav-layout` — the position the drawer vacated in
    Plan 46.3-02. It renders UNCONDITIONALLY, with or without `?module=`. Confirm `$activity` is
    derived unconditionally in `ProjectCockpitController`; if it is gated on `$openModule`, ungate
    it and say so in the summary.

    In `panel.blade.php`: delete the Recent activity card, drop the `activity` prop and its
    `$activity = $activity ?? collect();` line, and leave a short comment in its place saying where
    the feed went and which decision moved it — the same treatment the quick-actions block and the
    Returned tab got at :~300. Then remove `:activity="$activity"` from the panel's invocation in
    `cockpit.blade.php`. A prop left wired to nothing is the wiring-that-can-never-fire this file's
    own comments warn about.

    **CSS.** `.cav-layout` becomes a two-column grid again: the module column (now carrying the
    inline drawer) and the activity column. Plan 46.3-02 collapsed it to one column deliberately; this
    plan restores the second for a different occupant. Say that in the rule's comment so the next
    reader does not read the two commits as a revert. Whether the panel is sticky and how many
    entries it shows are the executor's discretion (recorded as such in 46.3-CONTEXT); pick one,
    state the reason, and if the entry count is capped, cap it where the controller already caps it
    rather than adding a second opinion in Blade.

    Revisit the responsive blocks Plan 46.3-02 left. At narrow widths the activity column must stack
    BELOW the module list — the module list is the page's purpose and the feed is context. Use the
    existing breakpoints; do not invent a fourth.

    **No hex literal may reach a Blade file.** All colour is a `--cav-*` token declared on
    `.cav-brand` in `cav-tokens.css`. Every class `cav-`-prefixed. No Tailwind utility in this
    subtree.

    **Run `CockpitVisualTest`'s click-target test after every CSS edit.** Reinstating a grid changes
    the module column's width and containing context, and the stretched-link pairing fails silently.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1</automated>
  </verify>
  <done>One activity panel on every render including the bare page; its subtree is byte-identical across all modules and tabs; no module panel contains `Recent activity`; the suite is green with 0 failed.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the panel-side activity assertions by name, and record the D-14 narrowing</name>
  <files>tests/Feature/Cockpit/CockpitPanelTest.php, tests/Feature/Cockpit/CockpitPageTest.php, tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php, .planning/sketches/004-delivery-cockpit/README.md, .planning/phases/46.3-inline-drawer-layout/46.3-COUNT-LEDGER.md</files>
  <action>
    Retire the panel-side assertions BY NAME with D-03 cited, each replaced by a page-level successor
    rather than deleted — the same discipline 46.2 used for the every-visit-type invariant:
      CockpitPanelTest:287 `assertStringContainsString('Recent activity', $html)` over panel html →
        becomes TWO assertions: the page carries exactly one Recent activity panel, and the panel
        subtree carries none. A single relocated assertion would pass on a page that renders the feed
        twice.
      CockpitPanelTest:20 and :273 — the docblock and the section header still say the feed is the
        panel's. Update both; a docblock that lies is the next agent's wrong premise.
    Grep for every other test naming `Recent activity`, `cav-acts` or `activity-row` and judge each,
    listing all hits in the summary even when the verdict is "unchanged".

    Add the identity assertion named in Task 1's behaviour block to `CockpitPanelTest` (or a named
    sibling): the activity subtree is equal across all four modules times all three tabs, plus the
    bare page. That is DL-03's second half and it has never been asserted, because until now the feed
    had no position from which it could differ.

    **Record the D-14 narrowing in the sketch**, where D-08..D-16 live, exactly as 46.2 recorded
    D-16's reversal: a `> ⚠️` note above D-14 saying D-03 of Phase 46.3 moved the feed out of the
    panel to its own project-level panel, that the feed's CONTENT and its project-wide scope are
    unchanged, and that D-14's wording is kept verbatim as the record of what was decided.

    While the sketch is open, add the same treatment for **D-09** — "the drawer is a right side panel
    with tabs" — narrowed by D-01 of this phase to an inline drawer beneath the clicked row. The
    tabs, the tokens, the row design and the query-string state all survive; only the position
    changed. Plan 46.3-02 made that change; recording it is this task's, so the sketch is edited once
    rather than three times.

    Judge the four pinned fence counts by name (expectation: 2 / 21 / 9 / 13 unchanged — this plan
    ships no control, no handler and no new table read) and record the verdicts in the ledger.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1</automated>
  </verify>
  <done>Every retired assertion is named in the ledger with D-03 cited and has a successor; the sketch carries narrowing notes above D-09 and D-14 with the original wording intact; the suite is green with 0 failed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Activity log → page | Actor names and phrases are app-authored; PM free text is deliberately NOT copied into the feed (VL-10). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46.3-08 | Information disclosure | activity feed moved to page level | mitigate | The feed was ALREADY project-wide and already rendered on every open module; moving it outside the module changes who sees it not at all. The page's own authorisation is unchanged. Assert equality of the subtree across renders so no module-specific entry can appear. |
| T-46.3-09 | Tampering | escaped output | mitigate | `x-cockpit.activity-row` is reused unchanged; escaped output only, as every cockpit component requires (T-46-07-05 greps for unescaped output). |
| T-46.3-SC | Tampering | npm/composer installs | mitigate | **This plan installs nothing.** An install would require a blocking human checkpoint first; no Package Legitimacy Audit exists for this phase. |
</threat_model>

<verification>
- `gate-46.ps1` → cockpit suite `0 failed`.
- `gate-46.ps1 -Hashes` → three sha256 match `4abd2b24`. **A MISMATCH IS A STOP.** Working-tree
  `Get-FileHash` only.
- `gate-46.ps1 -Baseline` → `>= 159 passed AND 0 failed`. **Never equality against 161.**
- Foreground only — **do not background gate-46.ps1.**
- **Never** `migrate:fresh --env=testing`; **never** `state.advance-plan` / `state.update-progress`;
  **never** `git stash`.
- `D-46.2-06-01` is NOT fixed here.
</verification>

<success_criteria>
- DL-03: one Recent activity panel, outside any module, on every render; its subtree identical across
  all four modules and all three tabs; no module panel carries the feed.
- The sketch records the D-09 and D-14 narrowings without altering their original wording.
- Fence counts 2 / 21 / 9 / 13, judged by name.
- The stretched-link test is green after every CSS edit.
</success_criteria>

<output>
Create `.planning/phases/46.3-inline-drawer-layout/46.3-03-SUMMARY.md` when done.
</output>
