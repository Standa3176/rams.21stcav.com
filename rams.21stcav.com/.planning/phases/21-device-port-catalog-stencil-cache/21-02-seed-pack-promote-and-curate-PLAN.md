---
phase: 21-device-port-catalog-stencil-cache
plan: 02
type: execute
wave: 2
tags: [drawings, device-catalog, seed-data, curation, v2.0]
depends_on: [21-01]
files_modified:
  - resources/data/device-stencils-seed/_INDEX.md
  - resources/data/device-stencils-seed/neat-bar-pro.json
  - resources/data/device-stencils-seed/samsung-qm65c-t.json
  - resources/data/device-stencils-seed/clickshare-bar-pro.json
  - resources/data/device-stencils-seed/sennheiser-tcc2.json
  - resources/data/device-stencils-seed/netgear-gs312tp.json
  - resources/data/device-stencils-seed/_v1.3-promoted.json
  - resources/data/device-stencils-seed/_top-50-gap.json
  - app/Services/Drawings/DeviceStencilSeedReader.php
  - database/seeders/DeviceStencilSeeder.php
  - database/seeders/DatabaseSeeder.php
  - tests/Unit/Services/Drawings/DeviceStencilSeedReaderTest.php
  - tests/Feature/Drawings/DeviceStencilSeederTest.php
  - tests/Feature/Drawings/SeedPackCoverageTest.php
  - tests/fixtures/seed-coverage/top-50-snapshot.json
autonomous: true
requirements: [DRAW-33]

must_haves:
  truths:
    - "Running php artisan db:seed --class=DeviceStencilSeeder upserts the seed pack into device_stencils + device_ports as source=engineer-curated, idempotent across re-runs (DRAW-33 / D-05)"
    - "All 53 v1.3 device-port-catalog.json entries are promoted into device_stencils as Tier 1.5 stencils (auto-generic body shell + manufacturer/model/part_number from the v1.3 JSON), tagged metadata.needs_phase_24_curation=true (D-05 step 2 — promote ALL 53)"
    - "The 5 spike stencils (Neat Bar Pro / Samsung QM65C-T / ClickShare Bar Pro / Sennheiser TCC2 / Netgear GS312TP) are promoted from resources/data/draw-io-stencils/21cav-mtr-spike.json into per-stencil curation manifests at resources/data/device-stencils-seed/{slug}.json with full mxgraph_xml + ports list (D-05 step 1)"
    - "Re-running the seeder a second time produces zero new rows (idempotency via whereRaw('LOWER(TRIM(part_number)) = ?', [...]) + updateOrCreate matching pattern from DeviceCatalogSeeder)"
    - "A coverage assertion run against an INDEPENDENT top-50 reference list (sourced per D-15 — live DB query, frozen snapshot, OR hand-chosen synthetic fixtures with documented provenance, NEVER generated from the seed pack itself) shows ≥80% of hardware part_numbers have a curated DeviceStencil row (D-05 / D-15 / milestone success criterion 2)"
    - "The v1.3 device-port-catalog.json file at resources/data/device-port-catalog.json is UNTOUCHED — Phase 18's rack render still consumes it for u_height / weight / current / BTU lookups (D-10)"
  artifacts:
    - path: "resources/data/device-stencils-seed/_INDEX.md"
      provides: "Engineer-readable index of every per-stencil manifest in the seed pack; documents the manifest schema; lists provenance (spike / v1.3 catalog / hand-curated gap-fill)"
    - path: "resources/data/device-stencils-seed/neat-bar-pro.json"
      provides: "Promoted Neat Bar Pro stencil from spike — full curation manifest with mxgraph_xml + 6 ports"
    - path: "resources/data/device-stencils-seed/samsung-qm65c-t.json"
      provides: "Promoted Samsung QM65C-T stencil from spike — full curation manifest with mxgraph_xml + 9 ports"
    - path: "resources/data/device-stencils-seed/clickshare-bar-pro.json"
      provides: "Promoted ClickShare Bar Pro stencil from spike — full curation manifest with mxgraph_xml + 7 ports"
    - path: "resources/data/device-stencils-seed/sennheiser-tcc2.json"
      provides: "Promoted Sennheiser TCC2 stencil from spike — full curation manifest with mxgraph_xml + 4 ports"
    - path: "resources/data/device-stencils-seed/netgear-gs312tp.json"
      provides: "Promoted Netgear GS312TP stencil from spike — full curation manifest with mxgraph_xml + 12 ports"
    - path: "resources/data/device-stencils-seed/_v1.3-promoted.json"
      provides: "Bulk manifest of ALL 53 v1.3 device-port-catalog.json entries promoted to Tier 1.5 stencils per D-05 step 2. Each entry tagged metadata.needs_phase_24_curation=true. Generated by a one-time script run during Plan 21-02 Task 2 — committed verbatim to repo."
    - path: "resources/data/device-stencils-seed/_top-50-gap.json"
      provides: "Bulk manifest of the GAP-FILL devices (top-50 21CAV quote-volume part_numbers NOT already covered by spike + v1.3 promotion). Estimated 10-25 entries depending on overlap; one entry per part_number; same shape as per-file manifests but bundled."
    - path: "app/Services/Drawings/DeviceStencilSeedReader.php"
      provides: "Read-only loader that walks resources/data/device-stencils-seed/*.json + the underscored bulk files (_v1.3-promoted.json + _top-50-gap.json), validates each manifest shape (part_number/manufacturer/model/mxgraph_xml/ports[] required), returns flat list ready for the seeder. Memoised."
      exports: ["all", "validate"]
    - path: "database/seeders/DeviceStencilSeeder.php"
      provides: "Idempotent seeder using updateOrCreate matched on whereRaw('LOWER(TRIM(part_number)) = ?', [...]) — mirrors DeviceCatalogSeeder pattern. Creates DeviceStencil + DevicePort rows. Sets source=engineer-curated for every entry. Logs count of stencils-upserted + ports-upserted."
      contains: "class DeviceStencilSeeder"
    - path: "database/seeders/DatabaseSeeder.php"
      provides: "Updated to call DeviceStencilSeeder via $this->call() (idempotent — re-runnable as part of full reseed)"
    - path: "tests/fixtures/seed-coverage/top-50-snapshot.json"
      provides: "Frozen INDEPENDENT top-50 reference list per D-15 fallback rule. Top-line comment documents provenance: 'Generated YYYY-MM-DD from production projects 1..N — DO NOT regenerate from seed pack'. Used by SeedPackCoverageTest when live DB query unavailable."
  key_links:
    - from: "DeviceStencilSeeder::run"
      to: "DeviceStencilSeedReader::all"
      via: "constructor injection"
      pattern: "DeviceStencilSeedReader"
    - from: "DeviceStencilSeeder::run (per-stencil loop)"
      to: "DeviceStencil::updateOrCreate + DevicePort::updateOrCreate"
      via: "whereRaw('LOWER(TRIM(part_number)) = ?', [...]) idempotency match"
      pattern: "updateOrCreate"
    - from: "DeviceStencilSeedReader"
      to: "resources/data/device-stencils-seed/*.json + _v1.3-promoted.json + _top-50-gap.json"
      via: "file_get_contents + json_decode"
      pattern: "device-stencils-seed"
    - from: "SeedPackCoverageTest"
      to: "tests/fixtures/seed-coverage/top-50-snapshot.json (or live DB query)"
      via: "INDEPENDENT fixture per D-15 — never sourced from seed pack"
      pattern: "top-50-snapshot|ProjectPackage::query"
---

<objective>
Promote the spike's 5 hand-coded MTR stencils + ALL 53 v1.3 device-port-catalog entries into git-trackable per-stencil curation manifests, then hand-curate the GAP from there up to top-50 21CAV-volume coverage. Ship an idempotent seeder that materialises the manifests into `device_stencils` + `device_ports` rows with `source=engineer-curated` (per D-05).

The result: any project importing a quote that uses one of the seeded part_numbers gets a properly-curated stencil with port rails immediately. Tier 1 auto-generic placeholders (from Plan 21-01) only fire for the long tail outside the seed pack.

Per CONTEXT.md locked decisions:
- D-05 step 2 promotion is **ALL 53 v1.3 entries** (sunk cost — they're already curated as rack metadata; reuse). The seed pack starts with ~53 + 5 spike + delta_gap (top-50 entries not already in the 53), net ~58-78 stencils depending on overlap.
- Promote spike stencils into per-file manifests under `resources/data/device-stencils-seed/{slug}.json` for git-trackable curation (D-05 step 1)
- Promote v1.3 entries into the bulk manifest `_v1.3-promoted.json` (D-05 step 2)
- Hand-curate the gap-fill into `_top-50-gap.json` (D-05 step 3)
- Idempotency via `whereRaw('LOWER(TRIM(part_number)) = ?', [...])` matching pattern from `DeviceCatalogSeeder` (D-05)
- Visual style of curated stencils matches XTEN-AV PAGING SYSTEM reference (manufacturer logo top, name+model below, port rails left/right, connector glyphs outside edge)
- Source enum value = `engineer-curated` for everything in the seed pack
- v1.3 `device-port-catalog.json` UNTOUCHED — its rack metadata stays separate from stencil/port metadata (D-10)
- SeedPackCoverageTest fixture provenance per D-15 — INDEPENDENT of the seed pack (live DB query > frozen snapshot > hand-chosen synthetic, in priority order)

Purpose: deliver DRAW-33 (top-50 hand-curated seed pack) — 1 of 6 Phase 21 requirements.

Output: ~58-78 stencil rows + ~300+ port rows in DB; 5 per-file git-tracked JSON manifests + 2 bulk manifests under `resources/data/device-stencils-seed/`; idempotent seeder; coverage test asserting ≥80% match against an INDEPENDENT top-50 reference (per D-15).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-device-port-catalog-stencil-cache/21-CONTEXT.md
@.planning/phases/21-device-port-catalog-stencil-cache/21-01-schema-models-cache-service-PLAN.md
@CLAUDE.md

# Source data to promote:
@resources/data/draw-io-stencils/21cav-mtr-spike.json
@resources/data/device-port-catalog.json

# v1.3 idempotency pattern this seeder mirrors:
@database/seeders/DeviceCatalogSeeder.php
@app/Services/Drawings/DeviceCatalogService.php

# Plan 21-01 outputs this plan consumes:
@app/Models/DeviceStencil.php
@app/Models/DevicePort.php

<interfaces>
<!-- Manifest schema for resources/data/device-stencils-seed/{slug}.json -->

```json
{
  "part_number": "NEAT-BAR-PRO",
  "slug": "neat-bar-pro",
  "manufacturer": "Neat",
  "model": "Bar Pro",
  "display_name": "Neat Bar Pro Videobar",
  "default_width": 240,
  "default_height": 160,
  "logo_svg_path": "/img/manufacturers/neat.svg",
  "mxgraph_xml": "<shape name=\"21cav.neat-bar-pro\" h=\"160\" w=\"240\" ...>...</shape>",
  "source": "engineer-curated",
  "metadata": {
    "provenance": "spike-260509-ibx",
    "curated_by": "21CAV engineering"
  },
  "ports": [
    {
      "port_id": "hdmi-in",
      "label": "HDMI IN",
      "side": "left",
      "connector_type": "hdmi",
      "signal_type": "video",
      "direction": "in",
      "sort_order": 1,
      "y_pct": 0.20
    },
    ...
  ]
}
```

<!-- Bulk manifest at resources/data/device-stencils-seed/_v1.3-promoted.json -->

```json
{
  "version": "1.0",
  "generated": "2026-05-10",
  "provenance": "Generated from resources/data/device-port-catalog.json (53 entries) — promoted as Tier 1.5 stencils per D-05 step 2",
  "stencils": [
    {
      "part_number": "...",
      "slug": "...",
      "manufacturer": "...",
      "model": "...",
      "mxgraph_xml": "<shape>...auto-generic body...</shape>",
      "source": "engineer-curated",
      "metadata": {
        "provenance": "v1.3-catalog-promoted",
        "needs_phase_24_curation": true,
        "u_height": 1,
        "is_rack_mounted": true
      },
      "ports": [...]
    },
    ...53 entries total...
  ]
}
```

<!-- Bulk manifest at resources/data/device-stencils-seed/_top-50-gap.json (gap-fill ONLY — devices in top-50 not already in spike+v1.3) -->

```json
{
  "version": "1.0",
  "generated": "2026-05-10",
  "provenance": "Hand-curated gap-fill — top-50 21CAV quote-volume part_numbers NOT already covered by spike (5) + v1.3 promotion (53). Source query in Plan 21-02 Task 2 action notes.",
  "stencils": [...]
}
```

<!-- DeviceStencilSeedReader contract -->

```php
class DeviceStencilSeedReader {
    /** @return array<int, array{part_number:string, slug:string, manufacturer:string, model:string, display_name:?string, default_width:int, default_height:int, logo_svg_path:?string, mxgraph_xml:string, source:string, metadata:?array, ports:array<int, array<string,mixed>>}> */
    public function all(): array;

    /** Throws RuntimeException with the file path + missing field on schema violation */
    public function validate(array $manifest, string $path): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote spike stencils → per-file curation manifests + DeviceStencilSeedReader</name>
  <files>
    resources/data/device-stencils-seed/_INDEX.md,
    resources/data/device-stencils-seed/neat-bar-pro.json,
    resources/data/device-stencils-seed/samsung-qm65c-t.json,
    resources/data/device-stencils-seed/clickshare-bar-pro.json,
    resources/data/device-stencils-seed/sennheiser-tcc2.json,
    resources/data/device-stencils-seed/netgear-gs312tp.json,
    app/Services/Drawings/DeviceStencilSeedReader.php,
    tests/Unit/Services/Drawings/DeviceStencilSeedReaderTest.php
  </files>
  <behavior>
    - Each per-stencil JSON file at `resources/data/device-stencils-seed/{slug}.json` matches the manifest schema in &lt;interfaces&gt; — required: part_number, slug, manufacturer, model, mxgraph_xml, source, ports[]; optional: display_name, default_width, default_height, logo_svg_path, metadata
    - The 5 promoted stencils carry mxgraph_xml + ports lifted verbatim (or near-verbatim with minor v2.0 polish) from `resources/data/draw-io-stencils/21cav-mtr-spike.json`. Source = "engineer-curated". metadata.provenance = "spike-260509-ibx".
    - DeviceStencilSeedReader::all() walks every `*.json` file in the seed-pack directory INCLUDING the underscored bulk files (`_v1.3-promoted.json` + `_top-50-gap.json` — handled in Task 2). For Task 1's milestone, only the 5 per-file manifests exist, so all() returns 5 entries.
    - Reader validates each manifest via validate() — throws RuntimeException with the path + missing field name on shape violation; reports the JSON line where possible
    - Memoised per-instance (mirrors DeviceCatalogService memoisation)
    - Files starting with `_` (e.g. `_INDEX.md`, `_v1.3-promoted.json`, `_top-50-gap.json`) need special handling in the reader: `_INDEX.md` is skipped (non-JSON); `_v1.3-promoted.json` + `_top-50-gap.json` are bulk manifests with shape `{stencils: [...]}` — the reader detects this shape (presence of `stencils` key + absence of top-level `part_number`) and flat-maps the inner stencils into the result list.
    - DeviceStencilSeedReaderTest asserts: all() returns 5 entries when only per-file manifests exist; each has the required fields; validate() throws on a fixture missing 'mxgraph_xml'; `_INDEX.md` is skipped (non-JSON); when a fixture bulk file `tests/fixtures/seed-coverage/_test-bulk.json` is provided with shape `{stencils: [...]}`, the reader flat-maps its contents.
    - The 5 mxgraph_xml strings each contain the manufacturer name (Neat / Samsung / Barco-or-ClickShare / Sennheiser / Netgear) — sanity check
  </behavior>
  <action>
    Create `resources/data/device-stencils-seed/_INDEX.md` documenting:
    - The directory's purpose (git-trackable curation manifests, one per device)
    - The manifest schema (mirror the &lt;interfaces&gt; block)
    - The naming convention: `{manufacturer-slug}-{model-slug}.json`, lowercase + hyphens; `_INDEX.md` is reserved (non-JSON); underscore-prefixed `*.json` files are bulk manifests with shape `{stencils: [...]}` (currently `_v1.3-promoted.json` and `_top-50-gap.json`)
    - Provenance breakdown: 5 from spike `260509-ibx`; 53 promoted from v1.3 catalog (in `_v1.3-promoted.json`); gap-fill devices from quote-volume top-50 (in `_top-50-gap.json`)
    - **ClickShare slug note (per D-14):** the brand naming in this directory follows D-14 — `clickshare-bar-pro.json` keeps `clickshare` slug for the product line; future generic Barco entries (e.g. F50 projector) would land at `barco-{model}.json`. Both manufacturer logos coexist at `public/img/manufacturers/{clickshare,barco}.svg`.

    Create the 5 per-stencil JSON files. For each:
    1. Read the matching entry from `resources/data/draw-io-stencils/21cav-mtr-spike.json`
    2. Map: id → derive `slug` from filename; manufacturer/model carry over; logo_url → logo_svg_path; default_size → split into default_width + default_height; mxgraph_shape_xml → mxgraph_xml; ports list maps 1:1 (ports already have port_id, label, type→connector_type, direction, side, y_pct/x_pct, plus add a derived signal_type via the port-type→signal-type mapping table below)
    3. Set source = "engineer-curated"; metadata.provenance = "spike-260509-ibx"
    4. Add a `signal_type` to every port via this lookup (when the spike's `type` field doesn't carry it):
       - `hdmi`/`displayport` → `video`
       - `usb-a`/`usb-b`/`usb-c` → `usb`
       - `rj45`/`rj45-poe` → `network`
       - `audio-3.5mm`/`xlr`/`line-in`/`optical-audio`/`phoenix` → `audio`
       - `power` → `power`
       - `rs232` → `control`

    **Note on ClickShare manifest (per D-14):** for `clickshare-bar-pro.json`, set `manufacturer: "Barco"` (true brand) BUT `display_name: "ClickShare Bar Pro"` AND `logo_svg_path: "/img/manufacturers/clickshare.svg"` (preserves the existing spike-shipped clickshare.svg per D-14). Document this in `_INDEX.md`.

    Create `app/Services/Drawings/DeviceStencilSeedReader.php` with the contract from &lt;interfaces&gt;. Implementation walks `glob(resource_path('data/device-stencils-seed/*.json'))`, decodes each via json_decode. For each decoded value:
    - If top-level key `part_number` exists → it's a single-stencil manifest, validate() and append to result.
    - Else if top-level key `stencils` exists → it's a bulk manifest, iterate `$decoded['stencils']`, validate() each, append.
    - Else → throw RuntimeException("Unknown manifest shape: $path").

    Memoise on first call. Use `resource_path()` (mirrors DeviceCatalogService::path()).

    validate() requires: part_number (string, non-empty), slug (string), manufacturer (string), model (string), mxgraph_xml (string, non-empty, contains '&lt;shape'), source (string in [auto-generated, engineer-curated, ai-extracted]), ports (array). For each port, requires: port_id, label, side (in [left, right, top, bottom]), connector_type, signal_type, direction (in [in, out, io]).

    Write the test file with the assertions listed in &lt;behavior&gt;. Use `resource_path` override OR test against the real files (preferred — they ARE the test fixtures).

    Lint touched PHP files with Herd PHP 8.4.
  </action>
  <verify>
    <automated>php artisan test --filter=DeviceStencilSeedReaderTest &amp;&amp; php -r "echo count(json_decode(file_get_contents('resources/data/device-stencils-seed/neat-bar-pro.json'), true)['ports']) . PHP_EOL;"</automated>
  </verify>
  <done>
    - 5 per-stencil JSON files at `resources/data/device-stencils-seed/` shipped + `_INDEX.md`
    - Each file's mxgraph_xml + ports map to the spike's data accurately
    - ClickShare manifest correctly references clickshare.svg per D-14 (NOT barco.svg)
    - DeviceStencilSeedReader returns the 5 stencils via all() AND correctly flat-maps bulk manifests when present
    - validate() catches malformed manifests
    - Reader test passes
    - Lint clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote ALL 53 v1.3 entries + gap-fill manifest + idempotent DeviceStencilSeeder (HALT-and-confirm batched curation)</name>
  <files>
    resources/data/device-stencils-seed/_v1.3-promoted.json,
    resources/data/device-stencils-seed/_top-50-gap.json,
    database/seeders/DeviceStencilSeeder.php,
    database/seeders/DatabaseSeeder.php,
    tests/Feature/Drawings/DeviceStencilSeederTest.php,
    tests/Feature/Drawings/SeedPackCoverageTest.php,
    tests/fixtures/seed-coverage/top-50-snapshot.json
  </files>
  <behavior>
    - `_v1.3-promoted.json` carries ALL 53 v1.3 device-port-catalog entries promoted as Tier 1.5 stencils per D-05 step 2. Each entry: source=engineer-curated; metadata.provenance="v1.3-catalog-promoted"; metadata.needs_phase_24_curation=true; metadata.u_height + metadata.is_rack_mounted carried from v1.3 JSON; mxgraph_xml is auto-generic body shell with manufacturer/model/part_number filled in (NOT a hand-traced device-card — that's Phase 24's job); ports list populated from the inferable subset (HDMI=video, RJ45=network, USB-*=usb, XLR=audio) where derivable from the v1.3 entry's port_count + port_type fields, OR empty array if no port info.
    - `_top-50-gap.json` carries the GAP-FILL devices: top-50 21CAV quote-volume part_numbers NOT already covered by spike (5) + v1.3 (53). Estimated count: 10-25 devices depending on overlap. Each entry follows the per-stencil schema. The list is derived from a query against existing project quote volume — see action below for derivation method.
    - **Curation pacing — HALT-and-confirm batched workflow (Warning 4 fix):** the gap-fill curation in `_top-50-gap.json` is NOT a single-shot sit-down. The executor curates in batches of 10 stencils per atomic git commit, with explicit HALT points between batches:
      1. Run the derivation query → present top-N candidate list to user → user approves the final scope (count + part_numbers).
      2. Curate batch 1 (first 10 stencils): write into `_top-50-gap.json`, commit `feat(phase-21): seed pack gap-fill batch 1/N (10 stencils)`.
      3. **HALT.** Render a visual diff against the XTEN-AV PAGING SYSTEM reference (manufacturer logo top, name+model below, port rails left/right, connector glyphs outside edge). User reviews the 10 stencils' output via spike admin route, approves "looks right" OR flags specific stencils for re-curation.
      4. Repeat for batches 2..N until gap-fill is complete.
      5. Final commit: `feat(phase-21): seed pack gap-fill complete (N stencils total)`.
    - DeviceStencilSeeder::run() reads ALL manifests via DeviceStencilSeedReader (which now flat-maps bulk + per-file manifests transparently per Task 1 reader contract); upserts each:
      - Match: whereRaw('LOWER(TRIM(part_number)) = ?', [normalised]) — mirrors DeviceCatalogSeeder
      - Use updateOrCreate with the full payload; sets source = manifest.source (defaults to engineer-curated)
      - Inside a DB::transaction so partial failures roll back cleanly
      - For each stencil's ports: deleteWhere(device_stencil_id = X) + insert fresh — the manifest is the source of truth, hand edits to ports outside the manifest are wiped on reseed (intentional; the manifest is git-tracked)
      - Logs: rows_upserted, ports_upserted, total_stencils_in_manifest
    - DatabaseSeeder.php updated to call DeviceStencilSeeder via `$this->call(DeviceStencilSeeder::class)` (placed after DeviceCatalogSeeder which is the v1.3 rack-metadata seeder so the v1.3 catalog runs first)
    - DeviceStencilSeederTest asserts: first run creates ≥58 DeviceStencil rows (5 spike + 53 v1.3 + ≥0 gap-fill) + ≥150 DevicePort rows (estimated; actual count is asserted with floor); second run produces zero new rows (DeviceStencil::count() unchanged); manually edit a port's label, re-run, port label is restored from manifest
    - SeedPackCoverageTest asserts: against an INDEPENDENT top-50 reference list (per D-15 fixture provenance rule), ≥80% of unique part_numbers in the reference have a curated DeviceStencil row (using DeviceStencil::normalisePartNumber matching). Fixture provenance documented in test docblock + the fixture file's top-line comment.
  </behavior>
  <action>
    **Step A — Derive the v1.3-promoted manifest (`_v1.3-promoted.json`):**

    Write a one-time tinker / artisan tinker script to read `resources/data/device-port-catalog.json` and emit `_v1.3-promoted.json`. For each of the 53 entries:
    - part_number = entry['part_no']
    - slug = strtolower(str_replace([' ', '/', '\\'], '-', part_number))
    - manufacturer = entry['manufacturer'] ?? 'Unknown'
    - model = entry['model'] ?? entry['part_no']
    - display_name = entry['name'] ?? "$manufacturer $model"
    - mxgraph_xml = build via the SAME logic as Plan 21-01's AutoGenericStencilGenerator (call the service directly from tinker) — this gives the Tier 1.5 body shell with the v1.3 fields filled in
    - source = "engineer-curated" (so the cache service treats it as curated, not auto-generated)
    - metadata.provenance = "v1.3-catalog-promoted"
    - metadata.needs_phase_24_curation = true
    - metadata.u_height = entry['u_height'] ?? null
    - metadata.is_rack_mounted = entry['is_rack_mounted'] ?? false
    - ports = derive from entry's port hints (if v1.3 catalog has `inputs` / `outputs` arrays, map to side=left/right + connector/signal/direction); otherwise []

    Write the result to `resources/data/device-stencils-seed/_v1.3-promoted.json` with shape `{version, generated, provenance, stencils: [...53 entries...]}`. Commit as a SEPARATE atomic commit before starting the gap-fill: `feat(phase-21): promote 53 v1.3 catalog entries to seed pack as Tier 1.5 stencils`.

    **Step B — Derive the top-50 candidate list for the gap-fill manifest (`_top-50-gap.json`):**

    The planner cannot run a live DB query during planning. Instead, the executor runs:
    ```bash
    php artisan tinker
    \App\Models\ProjectPackage::query()
      ->whereNotNull('extracted_data')
      ->orderBy('created_at', 'desc')
      ->limit(200)
      ->get()
      ->flatMap(fn ($p) => collect($p->extracted_data['equipment'] ?? [])
        ->where('category', 'hardware')
        ->pluck('part_number'))
      ->map(fn ($pn) => strtolower(trim((string) $pn)))
      ->filter()
      ->countBy()
      ->sortDesc()
      ->take(60); // top-60 raw, then planner trims to ~10-25 unique post spike+v1.3 overlap
    ```

    From this top-60, REMOVE part_numbers that are already in `_v1.3-promoted.json` OR the 5 per-file spike manifests. The remainder (estimated 10-25 entries) is the gap-fill candidate.

    The executor presents the gap candidate list to the user for approval, then proceeds to **Step C — HALT-and-confirm batched curation** (per &lt;behavior&gt;).

    **Step C — Batched curation (HALT-and-confirm pacing):**

    For each batch of 10 (or fewer for the final batch):
    1. Hand-curate each stencil's mxgraph_xml + ports list using the Phase 18 `device-port-catalog.json` (53 entries) as the FIRST source for `u_height` / manufacturer / model when the part_number overlaps; hand-derived port lists per device using manufacturer spec sheets / engineer knowledge for in-house common SKUs (Crestron RMC4, Q-SYS Core, ClickShare CX-50, etc.); the spike's mxgraph_xml shape as a template.
    2. Append the batch to `_top-50-gap.json`'s `stencils` array.
    3. Atomic commit: `feat(phase-21): seed pack gap-fill batch X/N (10 stencils)`.
    4. **HALT** — render the spike admin route on a project that uses one of the just-added part_numbers; visually compare against XTEN-AV PAGING SYSTEM reference; capture user approval ("looks right" / "redo stencil X").
    5. If user flags any stencil, fix in a follow-up commit before proceeding to next batch.
    6. After all batches: final commit `feat(phase-21): seed pack gap-fill complete`.

    **If <10 unique high-volume gap part_numbers are available** (smaller dataset), document the actual count in `_top-50-gap.json` metadata and adjust the SeedPackCoverageTest threshold from "≥80% of 50" to "≥80% of $actualCount". Do NOT manufacture fake stencils to hit a number — DRAW-33 says "top 50 from last 12 months of quote volume", so if the actual catalogue is smaller, that's the real top.

    Each entry in `_top-50-gap.json` carries a "_source_method" field set to "hand-curated" / "engineer-supplied". For fully-curated entries the visual style matches the XTEN-AV PAGING SYSTEM reference (per CONTEXT.md visual contract).

    **Step D — Idempotent seeder:**

    Create `database/seeders/DeviceStencilSeeder.php`. Constructor takes `DeviceStencilSeedReader $reader`. run() method:
    1. Load $allStencils = $reader->all() — already includes per-file + bulk manifests flat-mapped per Task 1 reader contract.
    2. Deduplicate by normalised part_number (per-file wins on collision over bulk; bulk-v1.3 loses to bulk-gap on collision — per-file > _top-50-gap.json > _v1.3-promoted.json).
    3. Wrap in DB::transaction(function () use ($allStencils) { ... }) so the whole seed is atomic.
    4. For each stencil: `\App\Models\DeviceStencil::updateOrCreate(['part_number' => $normalised], $payload)`, then `$stencil->ports()->delete()` + insert each port row.
    5. Log final counts via Log::info.

    Update `database/seeders/DatabaseSeeder.php` to call DeviceStencilSeeder after DeviceCatalogSeeder.

    **Step E — Tests:**

    Write `tests/Feature/Drawings/DeviceStencilSeederTest.php` per &lt;behavior&gt;. Use RefreshDatabase. Run the seeder twice and assert idempotency.

    Write `tests/Feature/Drawings/SeedPackCoverageTest.php` per D-15 INDEPENDENT fixture rule:
    - **Preferred path (live DB):** if `ProjectPackage::query()->whereNotNull('extracted_data')->count() >= 50`, derive the top-50 reference at test time via the same query as Step B. Test docblock notes: "Top-50 reference derived live from production-shape data — independent of seed pack per D-15."
    - **Fallback path (frozen snapshot):** otherwise, load `tests/fixtures/seed-coverage/top-50-snapshot.json` (a frozen snapshot generated ONCE at planning time from real production data, with provenance comment in line 1). Test docblock notes: "Top-50 reference loaded from frozen snapshot — generated YYYY-MM-DD from production projects 1..N, NOT from seed pack."
    - **Synthetic fallback (last resort):** if neither is available, build 5 fixture projects at `tests/fixtures/seed-coverage/{1..5}.json` with hand-chosen part_numbers (≥20% intentionally uncatalogued so the 80% threshold is non-trivial). Test docblock notes the hand-pick provenance.

    Create `tests/fixtures/seed-coverage/top-50-snapshot.json` with this top-line comment block (JSON doesn't support comments natively — embed in a `_provenance` field):
    ```json
    {
      "_provenance": "Generated 2026-05-10 from production projects (last 200 packages from ProjectPackage table) — DO NOT regenerate from seed pack. This file exists to make SeedPackCoverageTest assertion non-circular per D-15.",
      "generated_at": "2026-05-10",
      "source_query": "ProjectPackage::query()->whereNotNull('extracted_data')->orderBy('created_at', 'desc')->limit(200)->get()->flatMap(equipment).countBy()->sortDesc()->take(50)",
      "top_50": ["part-no-1", "part-no-2", ...]
    }
    ```

    Lint all touched PHP files.

    Per D-13: SUMMARY.md MUST list the seeder run command as a post-upload step. The seeder is safe to run on production (idempotent).
  </action>
  <verify>
    <automated>php artisan db:seed --class=DeviceStencilSeeder &amp;&amp; php artisan test --filter='DeviceStencilSeederTest|SeedPackCoverageTest'</automated>
  </verify>
  <done>
    - `_v1.3-promoted.json` shipped with ALL 53 entries (per D-05 step 2 — promote ALL, not subset)
    - `_top-50-gap.json` shipped with hand-curated gap-fill (count documented if <expected); each entry validated via DeviceStencilSeedReader::validate()
    - Curation followed HALT-and-confirm batched pacing (10 stencils per commit, visual review between batches)
    - Seeder runs idempotently — second run produces zero new rows
    - SeedPackCoverageTest fixture is INDEPENDENT (live DB query OR frozen snapshot OR documented synthetic) per D-15 — assertion is non-circular
    - `tests/fixtures/seed-coverage/top-50-snapshot.json` shipped with provenance comment
    - Top-50 coverage test passes (≥80% threshold)
    - DatabaseSeeder.php calls DeviceStencilSeeder
    - All tests pass
    - Lint clean
    - v1.3 device-port-catalog.json + DeviceCatalogService.php + DeviceCatalogSeeder.php — `git diff` returns empty for these (D-10)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Curated manifest JSON files → DB | manifest content is engineer-controlled (in repo), not user-supplied — but mxgraph_xml interpolation must still be safe at render time (Plan 21-03 / Phase 23 boundary, not this plan's job) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21.02-01 | Tampering | _v1.3-promoted.json + _top-50-gap.json | accept | Files are git-tracked + reviewed via PR; engineer-controlled. No user write surface. |
| T-21.02-02 | Information Disclosure | manufacturer + part_number in repo | accept | Public information; manufacturer datasheets are world-readable. No customer data leaked. |
| T-21.02-03 | Denial of Service | Seeder running on every deploy | mitigate | Seeder is idempotent (whereRaw + updateOrCreate); ~58-78 stencils + ~300 ports → <2s typical run. Wrap in DB::transaction so a partial failure rolls back cleanly. |
| T-21.02-04 | Spoofing | source enum manually upgraded to engineer-curated | accept | The manifest IS engineer authority; the seeder honouring source from the manifest is the design (D-05). Phase 24's UI will be the authoritative way to flip source on a row, with audit log. |
| T-21.02-05 | Tampering | SeedPackCoverageTest fixture provenance | mitigate | D-15 forbids regenerating the fixture from the seed pack; tests/fixtures/seed-coverage/top-50-snapshot.json carries _provenance field documenting independent origin |
</threat_model>

<verification>
- `php artisan db:seed --class=DeviceStencilSeeder` runs clean against fresh DB; second run logs zero new rows
- `php artisan test --filter='DeviceStencilSeedReaderTest|DeviceStencilSeederTest|SeedPackCoverageTest'` all pass
- `\App\Models\DeviceStencil::where('source', 'engineer-curated')->count()` ≥ 58 (5 spike + 53 v1.3 + gap-fill ≥0)
- `\App\Models\DevicePort::count()` is meaningfully higher than DeviceStencil count (curated stencils average ≥3 ports; v1.3-promoted may have 0-2 each)
- `git diff resources/data/device-port-catalog.json app/Services/DeviceCatalogService.php app/Services/Drawings/SchematicGeneratorService.php database/seeders/DeviceCatalogSeeder.php` returns empty (D-10)
- Sanity check: `php artisan tinker` → `\App\Models\DeviceStencil::where('part_number', 'neat-bar-pro')->first()->ports->count()` should be 6
- Sanity check: `\App\Models\DeviceStencil::where('metadata->needs_phase_24_curation', true)->count()` should be 53 (the v1.3-promoted set)
- Lint clean on all touched PHP files
</verification>

<success_criteria>
- All 6 must_have truths are observable
- DRAW-33 deliverable artifacts in place
- 5 spike stencils promoted; ALL 53 v1.3 entries promoted (per D-05 step 2 — promote ALL); gap-fill manifest hand-curated to top-50 (or capped at actual quote-volume top-N when <50 unique parts available)
- Gap-fill curation followed HALT-and-confirm batched pacing (10 stencils / commit / visual review) per Warning 4 fix
- SeedPackCoverageTest fixture is INDEPENDENT of the seed pack per D-15 — non-circular assertion
- Seeder is idempotent — proven by re-run test
- v1.3 device-port-catalog.json + DeviceCatalogService + DeviceCatalogSeeder UNTOUCHED (D-10)
- Plan 21-03 unaffected (this plan and 21-03 have zero file overlap by design — but per CONTEXT.md critical_constraints, run sequentially not in parallel to avoid feature-test collisions on shared device_stencils / device_ports tables)
</success_criteria>

<output>
After completion, create `.planning/phases/21-device-port-catalog-stencil-cache/21-02-seed-pack-promote-and-curate-SUMMARY.md` following the standard summary template.

**🚨 Files to upload to live (per D-13 / CLAUDE.md local-then-upload workflow):**

1. `resources/data/device-stencils-seed/_INDEX.md`
2. `resources/data/device-stencils-seed/neat-bar-pro.json`
3. `resources/data/device-stencils-seed/samsung-qm65c-t.json`
4. `resources/data/device-stencils-seed/clickshare-bar-pro.json`
5. `resources/data/device-stencils-seed/sennheiser-tcc2.json`
6. `resources/data/device-stencils-seed/netgear-gs312tp.json`
7. `resources/data/device-stencils-seed/_v1.3-promoted.json`
8. `resources/data/device-stencils-seed/_top-50-gap.json`
9. `app/Services/Drawings/DeviceStencilSeedReader.php`
10. `database/seeders/DeviceStencilSeeder.php`
11. `database/seeders/DatabaseSeeder.php`

(Tests + tests/fixtures stay local — do not deploy.)

**Post-upload commands on live (in order):**
```bash
php artisan migrate                                       # safe re-run; no-op if Plan 21-01 already migrated
php artisan db:seed --class=DeviceStencilSeeder           # populates ~58-78 curated stencils + ~300 ports
php artisan cache:clear                                    # belt-and-braces
```

**Verification on live AFTER seed:**
- `php artisan tinker` → `\App\Models\DeviceStencil::where('source', 'engineer-curated')->count()` ≥ 58
- `\App\Models\DeviceStencil::where('metadata->needs_phase_24_curation', true)->count()` should be 53 (v1.3-promoted set)
- Pick a recent quote-imported project: `\App\Models\Project::find(123)->devicesWithStencils()` should return ≥80% of hardware lines with a curated stencil (source=engineer-curated)
- The spike admin route still loads (this plan doesn't touch the spike builder; Plan 21-03 does)
</output>
</content>
