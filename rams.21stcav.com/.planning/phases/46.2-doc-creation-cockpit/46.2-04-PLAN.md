---
phase: 46.2-doc-creation-cockpit
plan: 04
type: execute
wave: 2
depends_on: ["46.2-01"]
files_modified:
  - app/Support/Cockpit/CockpitDocumentFormPresenter.php
  - tests/Unit/Cockpit/CockpitDocumentFormPresenterTest.php
autonomous: true
requirements: [DC-03, DC-04, DC-08]
must_haves:
  truths:
    - "The field set for each of the four documents is one row in one map — adding a fifth document is a row, not a rewrite"
    - "Every field in the map names a file and a symbol in a real generator, and a test proves the symbol is there"
    - "A field the generators ignore cannot reach the form, because the map is the only source of fields"
    - "Every field the project can already answer is pre-filled, so the PM is never asked a question the app knows"
  artifacts:
    - path: "app/Support/Cockpit/CockpitDocumentFormPresenter.php"
      provides: "DOCUMENT_FIELD_MAP — four rows, grouped fields, each with consumer + prefill + target"
      min_lines: 120
      contains: "DOCUMENT_FIELD_MAP"
    - path: "tests/Unit/Cockpit/CockpitDocumentFormPresenterTest.php"
      provides: "The DC-04 proof — every mapped field's symbol found in its named generator file"
  key_links:
    - from: "app/Support/Cockpit/CockpitDocumentFormPresenter.php"
      to: "app/Services/RamsReviewDataService.php"
      via: "RAMS field targets are normaliseProgramme()/normaliseSiteLogistics() keys"
      pattern: "programme\\."
---

<objective>
Build the map that makes this page's next redesign a data edit. Each of the four documents gets one
row describing the fields the PM may enter, and **each field names the file and symbol in a real
generator that consumes it** — proven by a test, not asserted in a comment.

Purpose: D-03 and the CONTEXT's own warning — this page's contents have changed three times while
the visual design held, and "a field the generator ignores is a field that teaches a PM to fill in
noise." Output: `CockpitDocumentFormPresenter` with `DOCUMENT_FIELD_MAP`, no UI yet.

**THE RULE THIS PLAN ENFORCES:** a field may be added to the map only with a file path and a symbol
that `grep` finds in that file. If the executor cannot produce one, the field does not go in — it
goes in the summary as "the user asked for this and no generator consumes it".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46.2-doc-creation-cockpit/46.2-CONTEXT.md
@app/Services/RamsReviewDataService.php
@app/Services/Rams/RamsDisplayPatchService.php
@app/Services/SiteSurveyDocxService.php
@app/Services/OmManualValidationService.php

<interfaces>
<!-- THE FIELD SETS, DERIVED AT PLANNING TIME 2026-09-22 BY READING THE GENERATORS.
     Every row below carries the file:line that consumes it. RE-CONFIRM EACH WITH GREP
     (Task 1's verify does exactly that) — where a grep disagrees with this block, THE GREP WINS
     and the disagreement goes in the summary. -->

=== SITE SURVEY — Word: SiteSurveyDocxService::build()  |  PDF: SurveyPdfService::buildSummary()
  survey_date        → SiteSurveyDocxService.php:48 `$survey->survey_date`; pdf/site-survey/summary.blade.php:43
  surveyor_name      → SiteSurveyDocxService.php:55,129; summary.blade.php:60; _header-meta.blade.php:12
  site_contact_name  → pdf/site-survey/_header-meta.blade.php:13
  site_contact_phone → pdf/site-survey/_header-meta.blade.php:14
  general_notes      → SiteSurveyDocxService.php:57,131; summary.blade.php:74
  PRE-FILLED, DISPLAY ONLY (both generators read them, the project already answers them):
  project_name, project_ref, client_name, site_address → SiteSurveyDocxService.php:52-54,126-128
  CANDIDATES THAT MUST BE GREPPED BEFORE INCLUSION — several are on `validateSurvey()` but may be
  consumed by NEITHER generator: pm_name, pm_phone, pm_email, visit_time, survey_type,
  comms_room_access_status/_notes, parking_restraints, distance_from_base_miles/_notes,
  site_access_notes, delivery_routes, office_review_notes (this last one is CLIENT-PDF-only —
  summary.blade.php:67 guards it with `! $internal`, so if it goes in the map it carries that note).

=== WORKSHEET — Word: WorksheetDocxService  |  PDF: NONE (DC-07 gap, see 46.2-FORMAT-INVENTORY.md)
  project_name, project_ref, client_name, site_address
      → copied onto the Worksheet at creation, WorksheetController.php:154-161. GREP
        WorksheetDocxService for `$worksheet->project_ref` etc before mapping them.
  ⚠ NO OTHER PM INPUT IS CONSUMED. `WorksheetController::generateFromProject(Project $project)`
  takes a Project and nothing else; every other value in the document comes from the AI pipeline
  over the project package via `BuildWorksheetJob`. The user's examples — install dates, engineers,
  site contact — are **RAMS fields, not worksheet fields**. Do not add them here. Say so on the
  panel in one line of copy so the PM understands why this form is short.

=== RAMS — Word: DocxBuilderServiceV2  |  PDF: PdfService::buildRams()
  Values are resolved by RamsDisplayPatchService from `reviewed_data`, normalised by
  RamsReviewDataService. The TARGET for every field below is a key in that normalised payload —
  writing anywhere else is writing to a key `normalise()` will drop on the next pass.

  Group "When" → target `programme.*`
    planned_start_date   → RamsReviewDataService.php:294; RamsDisplayPatchService.php:153
    planned_end_date     → RamsReviewDataService.php:295; RamsDisplayPatchService.php:153
    ongoing              → RamsReviewDataService.php:296  (checkbox: no end date)
    planned_start_time   → RamsReviewDataService.php:297; RamsDisplayPatchService.php:153
    working_hours        → RamsReviewDataService.php:284-286 (enum in_hours|out_of_hours → 2 radios)
  Group "Who" → target `programme.*`
    project_manager_name / _phone / _email → RamsReviewDataService.php:277-279;
                                             RamsDisplayPatchService.php:79,120
    lead_engineer_name / _phone            → RamsReviewDataService.php:280-281;
                                             RamsDisplayPatchService.php:109
    additional_engineers[]                 → RamsReviewDataService.php:282;
                                             RamsDisplayPatchService.php:114-117
    programmers[]                          → RamsReviewDataService.php:287-290;
                                             RamsDisplayPatchService.php:126
  Group "Site" → target `site_logistics.*`
    contact_name / contact_phone / contact_email → RamsReviewDataService.php:305-307;
                                                   RamsDisplayPatchService.php:134-140
    access_type (enum no_special|induction|reception|security|other) + access_notes
                                                → RamsReviewDataService.php:318-321
    parking (yes|no) + parking_notes            → RamsReviewDataService.php:312-315
    install_floor, delivery_area, restrictions  → RamsReviewDataService.php:308-317

  ⚠ DEFECT FOUND AT PLANNING TIME, **NOT THIS PLAN'S TO FIX** — record it, do not map the field:
  `planned_end_time` is READ by RamsDisplayPatchService.php:153 but is NOT EMITTED by
  `RamsReviewDataService::normaliseProgramme()` (the method returns a fresh array and that key is
  absent), so it is ALWAYS ''. A panel field for it would write a value `normalise()` silently
  discards — the exact "field that teaches a PM to fill in noise" failure, in its worst form.
  Raise it as a new defect in the summary and leave it out of the map.

  ⚠ WHERE "ENGINEERS" COMES FROM: RAMS stores engineer NAMES as free text
  (`lead_engineer_name` string, `additional_engineers` string[]). `LabourResource` (Phase 44) IS the
  right source for those names — `LabourResource::active()` filtered by `hasRole(ROLE_ENGINEER)` /
  `hasRole(ROLE_PROGRAMMER)`, rendered NAME ONLY per LR-04, exactly as the now-removed quick-actions
  form did. The map's `prefill` for these fields therefore names `LabourResource`, and the form
  (Plan 46.2-05) offers them as checkboxes whose selected NAMES are written into the string fields.

=== O&M MANUAL — Word: OmManualDocxService  |  PDF: PdfService (om-manuals)
  handover_date → `projects.handover_date` (Project.php:126 fillable, :140 cast date);
                  OmManualGeneratorService.php:397; gated by OmManualValidationService.php:48
  draft         → the `?draft=1` flag on `om-manuals.generate-from-project`
                  (OmManualController.php:175) — two radios, "Draft (placeholders allowed)" /
                  "Final issue (no TBC)". Default: Final issue.
  ⚠ EVERYTHING ELSE THE O&M NEEDS IS NOT A FORM FIELD — it is project, room and drawing data:
  project name, client, site, document date (always today), ≥1 room, per-room equipment and
  narrative, ≥1 drawing (OmManualValidationService.php:37-84). Those belong on the panel as a
  READINESS LIST — "3 rooms · 41 devices · 2 drawings ✓" — never as inputs a PM could type a lie
  into. The validator already returns the missing-field list; render it, do not re-derive it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DOCUMENT_FIELD_MAP — four rows, grouped, every field with a named consumer</name>
  <files>app/Support/Cockpit/CockpitDocumentFormPresenter.php</files>
  <behavior>
    - `CockpitDocumentFormPresenter::documentFieldMap(): array` returns the map as DATA (a
      `private const` exposed by a static accessor), so tests iterate it rather than sample it —
      the same mechanism that made the visit-type invariant provable in `CockpitModulePresenter`.
    - Exactly four keys, identical to `CockpitModulePresenter::moduleMap()`'s four. A unit test
      asserts the two key sets are the same set — so a fifth module can never render a panel with
      no fields, and a fifth field row can never exist with no module.
    - Each document row carries: `generate_route` (the existing POST that starts generation),
      `formats` (`['word' => 'route.name', 'pdf' => 'route.name'|null]`, matching
      `46.2-FORMAT-INVENTORY.md` exactly), `intro` (one line of copy saying what this form does and,
      for Worksheet, why it is short), and `groups`.
    - Each group carries `legend` and `fields`. Each FIELD carries:
        `key`      — the form input name
        `label`    — PM-facing
        `type`     — one of `text|date|time|textarea|checkbox|radio|resource-list`. There is NO
                     `select` type: the fence still bans `<select` and Plan 46.2-03 re-took that
                     ruling. If a field genuinely needs one, the map is not where that is decided —
                     stop and raise it.
        `options`  — for `radio` only, `value => label`
        `target`   — the dotted path the value is written to (e.g. `programme.planned_start_date`,
                     `survey.survey_date`, `project.handover_date`)
        `consumer` — `['file' => <path>, 'symbol' => <string grep must find in that file>]`
        `prefill`  — a named source or null (`project.name`, `LabourResource::active`, ...)
        `rules`    — the Laravel validation rules Plan 46.2-05 will apply verbatim
    - `fieldsFor(string $documentKey): array` returns one document's groups; an unknown key returns
      `[]` and never throws — the same "this presenter never invents a module" contract
      `CockpitModulePresenter::progress()` already keeps.
    - `readiness(Project $project, string $documentKey): array` returns the O&M readiness list by
      DELEGATING to `OmManualValidationService`'s existing missing-field output, and an empty array
      for the other three. It derives no second opinion about what is missing.
  </behavior>
  <action>
    Write the class in `app/Support/Cockpit/`, following `CockpitModulePresenter`'s shape exactly:
    `final class`, a `private const` map, a static accessor returning it, a docblock stating PURE
    READING and that nothing here writes.

    Build the map from the `<interfaces>` block ABOVE, but **confirm every single `consumer` entry
    with a real grep first**. For each candidate field run
    `grep -n "<symbol>" <file>` and include the field only on a hit. Produce the full list of
    candidates you REJECTED and why — that list is a deliverable, not a by-product: it is the
    evidence for DC-04 and the answer to "which of the fields the user named does no generator
    consume".

    Keep the panel simple (DC-08): no group may exceed six fields, and a field the project already
    answers is `prefill`ed and rendered read-only rather than asked. If RAMS's three groups exceed
    that, split "Site" rather than dropping a field — this plan reduces nothing.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter CockpitDocumentFormPresenterTest</automated>
  </verify>
  <done>The map exists with four rows; every field carries a grepped consumer; the rejected-candidate list is written.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The DC-04 proof — no field without a consumer, no consumer without the symbol</name>
  <files>tests/Unit/Cockpit/CockpitDocumentFormPresenterTest.php</files>
  <behavior>
    - `test_every_mapped_field_names_a_file_that_exists()` — `file_exists()` on every
      `consumer.file`, with the field key in the failure message.
    - `test_every_mapped_field_symbol_is_found_in_its_named_generator()` — read each consumer file
      once (cache by path) and `assertStringContainsString($field['consumer']['symbol'], $contents)`.
      **This is the executable form of "a field the generator ignores is a field that teaches a PM
      to fill in noise."** A refactor that renames a generator's field trips this test instead of
      silently producing a form that writes into nothing.
    - `test_the_document_keys_match_the_module_keys_exactly()` — set equality against
      `CockpitModulePresenter::moduleMap()`.
    - `test_no_field_declares_a_select()` — assert no field's `type` is `select`, with a comment
      naming `CockpitReadOnlyFenceTest::FORBIDDEN_MARKUP` and the "lift by name in the commit that
      ships it" rule, so a future author finds the procedure rather than the prohibition.
    - `test_every_field_declares_validation_rules()` — non-empty `rules` on every field, so
      Plan 46.2-05 cannot ship an unvalidated input by omission.
    - `test_the_formats_match_the_format_inventory()` — every non-null `formats` entry passes
      `Route::has()`, and `worksheet.pdf` is null. Keep the null EXACT and comment it with DC-07,
      so closing the gap later is a deliberate edit here.
    - `test_no_group_asks_more_than_six_questions()` — DC-08 as a budget, the sibling of RV-08's
      one-anchor budget. A number, so "simple" is not re-litigated per reviewer.
    - `test_planned_end_time_is_not_a_field()` — assert by name that no field targets
      `programme.planned_end_time`, with the defect and its file:line in the comment. The map's one
      deliberate omission is asserted rather than remembered.
  </behavior>
  <action>
    Extend `Tests\TestCase`. No `RefreshDatabase` is needed for the map tests; add it only for
    `readiness()` if that test touches the database. NEVER run `artisan migrate:fresh --env=testing`.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter CockpitDocumentFormPresenterTest</automated>
  </verify>
  <done>All eight tests green; every field in the map is provably consumed by the generator it names.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| map `target` path → the payload it writes | Every `target` names where a PM value lands; a wrong path writes into a key `normalise()` discards, which fails silently rather than loudly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46.2-09 | Tampering | A `target` pointing at a key `RamsReviewDataService::normalise()` does not emit | mitigate | Every RAMS target is a key literally returned by `normaliseProgramme()` / `normaliseSiteLogistics()` / `normaliseProject()`; `planned_end_time` is excluded by name and asserted absent. Plan 46.2-05 adds the round-trip proof: write through the form, re-normalise, assert the value survived. |
| T-46.2-10 | Information Disclosure | `resource-list` fields rendering `LabourResource` | mitigate | NAME ONLY, per LR-04 and `LabourResource::toClientSafeArray()`. The map's `prefill` names the active-name list; no email or phone is ever placed in an option. Asserted in Plan 46.2-05's render test. |
| T-46.2-11 | Spoofing | Free-text engineer names diverging from the resource list | accept | RAMS has always stored names as free text; this map does not change the storage shape. The resource list is a convenience source, not a foreign key, and pretending otherwise would be a schema change this phase has no mandate for. |
| T-46.2-SC | Tampering | npm/pip/composer installs | accept | This plan installs no package. |
</threat_model>

<verification>
`gate-46.ps1 -Filter CockpitDocumentFormPresenterTest` — green.
`gate-46.ps1 -Baseline` — `>= 159 passed AND 0 failed`. `-Hashes` — three pins unchanged.
This plan touches no Blade, no CSS and no route, so the fence and the cockpit suite must be
UNAFFECTED — confirm with `gate-46.ps1 -Path tests/Feature/Cockpit` (background it, ~4 min).
</verification>

<success_criteria>
- `DOCUMENT_FIELD_MAP` holds exactly four documents whose keys equal the four module keys.
- Every field carries a grep-confirmed `consumer`, a `target`, `rules`, and a `prefill` or null.
- The rejected-candidate list is written, naming every field considered and dropped for having no
  consumer — including every field the user's brief named that no generator consumes.
- `planned_end_time` is excluded, asserted excluded, and raised as a new defect.
- No `select`. No group over six fields.
</success_criteria>

<output>
Create `.planning/phases/46.2-doc-creation-cockpit/46.2-04-SUMMARY.md` when done, with the
rejected-candidate list and the `planned_end_time` defect at the top.
</output>
