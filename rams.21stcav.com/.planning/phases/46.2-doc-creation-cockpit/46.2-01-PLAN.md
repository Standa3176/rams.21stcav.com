---
phase: 46.2-doc-creation-cockpit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Support/Cockpit/CockpitModulePresenter.php
  - tests/Unit/Cockpit/CockpitModulePresenterTest.php
  - tests/Feature/Cockpit/CockpitSpineTest.php
autonomous: true
requirements: [DC-01, DC-02, DC-11]
must_haves:
  truths:
    - "The cockpit renders exactly four module rows — Site survey, Worksheet, RAMS, O&M manual"
    - "The other five modules are absent from MODULE_MAP, not greyed and not emptied"
    - "No visit type can reach two module rows; the three types that reach none are a named allow-list, so a seventh type added later still fails loudly"
    - "The Documents KPI denominator follows the rendered module count with no edit"
  artifacts:
    - path: "app/Support/Cockpit/CockpitModulePresenter.php"
      provides: "MODULE_MAP with exactly four entries, D-16 reversal recorded at the map"
      contains: "KEY_OM"
    - path: "tests/Unit/Cockpit/CockpitModulePresenterTest.php"
      provides: "The retired invariant's two surviving halves, asserted by name"
      contains: "46.2 D-01"
  key_links:
    - from: "app/Support/Cockpit/CockpitModulePresenter.php"
      to: "app/Support/Cockpit/CockpitHeaderPresenter.php"
      via: "documentsCard() counts modules(), never a literal"
      pattern: "modules->count\\(\\)"
---

<objective>
Reduce the cockpit from nine module rows to four — Site survey · Worksheet · RAMS · O&M manual —
by editing the one place the module list lives, and retire Phase 45's "every visit type reaches
exactly one module row" invariant deliberately rather than letting it die as a red test.

Purpose: D-01. This is the load-bearing change of the phase and everything else depends on it.
Output: a four-entry `MODULE_MAP`, a retirement recorded at the invariant's own definition site,
and every count that the reduction moves moved BY NAME with its reason.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46.2-doc-creation-cockpit/46.2-CONTEXT.md
@.planning/sketches/004-delivery-cockpit/README.md
@.planning/phases/45-visit-model-read-only-cockpit/45-BASELINE.md
@app/Support/Cockpit/CockpitModulePresenter.php
@tests/Unit/Cockpit/CockpitModulePresenterTest.php

<interfaces>
<!-- Everything the executor needs. Do NOT go exploring for the module list: it is
     in exactly one place and this is it. -->

The four keys that SURVIVE, from app/Models/ProjectDeliverable.php:
  ProjectDeliverable::KEY_SITE_SURVEY  = 'site_survey'
  ProjectDeliverable::KEY_WORKSHEET    = 'worksheet'
  ProjectDeliverable::KEY_RAMS         = 'rams'
  ProjectDeliverable::KEY_OM           = 'om'

The five that are REMOVED from the cockpit (they stay in ProjectDeliverable::ALL_KEYS, which is
the DELIVERABLES SCREEN's vocabulary and is not this phase's to edit):
  KEY_INSTALL_PROGRAMME, KEY_DRAWINGS, KEY_CABLE_SCHEDULE, KEY_PROGRAMMING, KEY_SNAGGING

Visit types, from app/Models/Visit.php: TYPE_SITE_SURVEY, TYPE_FIRST_FIX, TYPE_INSTALL,
TYPE_COMMISSIONING, TYPE_PROGRAMMING, TYPE_SNAG (iterate Visit::TYPES, never a hand-written list).

Already derived and needing NO edit — verified 2026-09-22, do not "fix" these:
  - CockpitHeaderPresenter::documentsCard() uses `$modules->count()` for its denominator.
  - CockpitVisualTest's three hue tests all iterate moduleMap(); they carry no literal 9.
  - CockpitPageTest's row-count assertions use count(CockpitModulePresenter::moduleMap()).
  - CockpitPanelTest / CockpitReadOnlyFenceTest / CockpitVisitActionsTest iterate
    array_keys(moduleMap()), so they shrink with the map rather than breaking on it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MODULE_MAP becomes four, and the docblock records the reversal</name>
  <files>app/Support/Cockpit/CockpitModulePresenter.php</files>
  <behavior>
    - `CockpitModulePresenter::moduleMap()` returns EXACTLY four entries, in this order:
      `site_survey`, `worksheet`, `rams`, `om`.
    - The five removed keys return `null` from `progress()` (the `?? null` guard already handles an
      unknown key — assert it rather than adding a second guard).
    - `COUNT_NONE` becomes UNREACHABLE: Programming was its only user. The constant and its branch
      are KEPT, not deleted — `ProjectDeliverable.php:14-26` still forbids inventing a Programming
      store, and a future row with no store must still be representable. Docblock it as dormant.
    - The `worksheet` row's title stays `First fix and install` UNLESS the executor confirms the
      four-row design names it `Worksheet`. D-01 names the row "Worksheet"; the map's title is the
      PM-facing label. Change the title to `Worksheet` and the description to
      `Generate the installation worksheet.` — the user named four documents, and a document
      creation tool labels its rows with the document. Record the change in the docblock so a
      reader can see it was a decision and not a typo.
  </behavior>
  <action>
    Edit `MODULE_MAP` down to the four entries above, keeping each surviving entry's existing
    `icon`, `hue`, `count_mode`, `relation` and `visit_types` values BYTE-FOR-BYTE — this plan
    reduces the list, it does not re-tune the survivors. `site_survey` and `worksheet` keep their
    `visit_types` arrays even though the cockpit no longer surfaces visits (Plan 46.2-03), because
    the visit COUNT phrase on the row is a read and stays.

    Rewrite the class docblock's "NINE ROWS, NOT EIGHT (D-16 …)" paragraph. Do not delete it —
    replace it with a paragraph that (a) states FOUR ROWS per 46.2 D-01, (b) names D-16 as the
    decision being reversed and says the reversal is recorded at D-16 itself in
    `.planning/sketches/004-delivery-cockpit/README.md`, (c) states plainly that the invariant
    D-16 was argued from — every visit type reaching exactly one row — IS NOW FALSE and names
    `CockpitModulePresenterTest::test_no_visit_type_reaches_two_module_rows()` and
    `::test_the_visit_types_with_no_module_row_are_exactly_three_and_named()` as what replaced it,
    so the next reader finds the retirement at the definition site rather than inferring a deletion.
    Cite `46.2-CONTEXT.md` D-01 by name.

    Do NOT touch `cockpit.css` or `cav-tokens.css`: the five orphaned `.cav-hue--*` rules are inert
    and `test_every_declared_hue_has_a_rule_and_a_token_pair()` only requires that every DECLARED
    hue has a rule, never the converse. Deleting CSS here would put a second kind of change in a
    commit whose whole job is the map.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter CockpitModulePresenterTest</automated>
  </verify>
  <done>moduleMap() returns four keys in design order; the docblock names the reversal and the retirement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retire the every-visit-type invariant BY NAME, and keep its two surviving halves</name>
  <files>tests/Unit/Cockpit/CockpitModulePresenterTest.php</files>
  <behavior>
    - `test_every_visit_type_maps_to_exactly_one_module()` is GONE AS A NAME and replaced by two
      tests, with a retirement comment where it stood naming 46.2 D-01 as the decision that made it
      impossible and stating that it was retired, not deleted to make a red test pass:
      - `test_no_visit_type_reaches_two_module_rows()` — iterate `Visit::TYPES`, count owners in
        `moduleMap()`, assert `<= 1` for every type. This half of the invariant SURVIVES: a type
        rendering on two rows would still be a bug, and D-01 did not license it.
      - `test_the_visit_types_with_no_module_row_are_exactly_three_and_named()` — collect the types
        with ZERO owners and assert the sorted result is EXACTLY
        `[Visit::TYPE_COMMISSIONING, Visit::TYPE_PROGRAMMING, Visit::TYPE_SNAG]`. This is the
        anti-rot half: a SEVENTH visit type added later, or a fourth type losing its row, still
        fails loudly instead of rendering nowhere — which was D-16's real argument and is worth
        keeping even though its conclusion was reversed.
    - `test_the_nine_module_keys_are_the_canonical_deliverable_vocabulary()` is RENAMED to
      `test_the_four_module_keys_are_a_named_subset_of_the_canonical_deliverable_vocabulary()` and
      asserts BOTH: the rendered keys are exactly `['om', 'rams', 'site_survey', 'worksheet']`
      (sorted), AND every one of them is a member of `ProjectDeliverable::ALL_KEYS` — so a typo'd
      key still fails even though the equality with ALL_KEYS is gone. Comment the narrowing with
      46.2 D-01 and state that `ALL_KEYS` stays NINE because it is the deliverables screen's
      vocabulary and this phase does not touch that screen.
    - `test_a_snag_visit_reaches_the_snagging_row()` is replaced by
      `test_a_snag_visit_reaches_no_module_row_by_design()` — create a `TYPE_SNAG` visit, assert
      `modules()` contains no row whose key is `snagging` and that no surviving row's count phrase
      changed because of it. Comment: Phase 47 owns snags; 46.2 D-01 removed their row here.
  </behavior>
  <action>
    Edit the test file in place. Every removed assertion leaves a comment in its place naming
    46.2 D-01 and the plan number, in the style this repo already uses in
    `CockpitReadOnlyFenceTest::test_the_fence_enumerates_the_whole_deferred_set()`. Do not use
    `assertGreaterThanOrEqual` anywhere. Do not reach for `Visit::TYPES` via a hand-written array.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter CockpitModulePresenterTest</automated>
  </verify>
  <done>The invariant is retired at its definition site and in its test file; both surviving halves are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Move the two literal counts the reduction breaks, by name</name>
  <files>tests/Feature/Cockpit/CockpitSpineTest.php</files>
  <behavior>
    - `test_an_empty_project_still_renders_every_module_row_waiting()` currently asserts
      `count($modules) - 1` `cav-module__count` elements, because Programming was the one
      `COUNT_NONE` row with no count phrase. Programming is gone, so the assertion becomes
      `count($modules)` — every surviving row carries a count phrase. MOVED BY NAME with the
      reason recorded inline: "`count($modules) - 1` → `count($modules)` by Plan 46.2-01, because
      46.2 D-01 removed Programming, the only COUNT_NONE row."
    - The same test's `$this->assertSame('', $this->rowCount($html, 'Programming'));` and
      `$this->assertSame('0 tasks', $this->rowCount($html, 'Programme and commissioning'));` are
      RETIRED BY NAME (their rows no longer exist) and replaced by the four surviving phrases:
      `0 visits` for Site survey, `0 visits` for Worksheet, `0 documents` for RAMS, `0 documents`
      for O&M manual — so the test still enumerates every row rather than sampling one.
    - `test_the_module_titles_are_the_presenter_titles()` (the `array_column(moduleMap(), 'title')`
      helper at :199) needs no edit — it is derived. Confirm, do not change.
  </behavior>
  <action>
    Grep the file for every string literal naming a removed module — `Programming`,
    `Programme and commissioning`, `Drawings`, `Cable schedule`, `Snagging` — and for every one,
    either retire the assertion with a named comment or repoint it at a surviving row. Report any
    occurrence you retire in the summary; do not silently drop one. If the worksheet row's title
    changed in Task 1, update the literal `First fix and install` here too.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Path tests/Feature/Cockpit/CockpitSpineTest.php</automated>
  </verify>
  <done>CockpitSpineTest green with four rows; every moved literal carries its reason inline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module key (query string `?module=`) → presenter lookup | A PM-supplied key reaches `MODULE_MAP[...]`; five previously-valid keys now resolve to nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46.2-01 | Denial of Service | `ProjectCockpitController::resolveModuleKey()` with a now-removed key (e.g. a bookmarked `?module=snagging`) | mitigate | `firstWhere('key', …)` already returns null and the panel is simply absent — the "closed at rest means ABSENT" path. Assert it: a bookmarked removed-module URL renders the page with no panel and HTTP 200, never a 500. Add that assertion in Task 3. |
| T-46.2-02 | Tampering | `progress()` with an unknown module key | accept | `MODULE_MAP[$key] ?? null` already returns null for any unknown key; no filesystem or DB path is built from it. |
| T-46.2-SC | Tampering | npm/pip/cargo installs | accept | This plan installs no package. No `composer require`, no `npm install`. Nothing to audit. |
</threat_model>

<verification>
`gate-46.ps1 -Path tests/Feature/Cockpit` and `-Path tests/Unit/Cockpit` — the cockpit suite runs
long; background it. Expect failures in `CockpitCreateVisitTest`, `CockpitVisitActionsTest`,
`CockpitOfficeNoteAndSnagTest`, `CockpitReturnedTabTest` and `CockpitReturnedTabEndToEndTest` —
those are the visit RENDER assertions and Plan 46.2-03 owns their retirement. Record which ones go
red here and hand the list to 46.2-03; do NOT fix them in this plan and do NOT delete one.

`gate-46.ps1 -Baseline` must still print `>= 159 passed AND 0 failed` (never equality against 161).
`gate-46.ps1 -Hashes` must match 45-BASELINE.md's three pins — a mismatch is a STOP.
</verification>

<success_criteria>
- `moduleMap()` returns exactly `site_survey`, `worksheet`, `rams`, `om`, in that order.
- The class docblock names 46.2 D-01, names D-16 as reversed, and names the two tests that replaced
  the retired invariant.
- `CockpitModulePresenterTest` and `CockpitSpineTest` green, with every moved or retired assertion
  carrying its reason inline and no `assertGreaterThanOrEqual` anywhere.
- The D-06 baseline gate and the three sha256 pins both unchanged.
</success_criteria>

<output>
Create `.planning/phases/46.2-doc-creation-cockpit/46.2-01-SUMMARY.md` when done, including the
list of tests left red for Plan 46.2-03.
</output>
