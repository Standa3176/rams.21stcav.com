---
phase: 46.2-doc-creation-cockpit
plan: 06
type: execute
wave: 4
depends_on: ["46.2-05"]
files_modified:
  - tests/Feature/Cockpit/CockpitDocCreationEndToEndTest.php
  - .planning/phases/46.2-doc-creation-cockpit/46.2-COUNT-LEDGER.md
autonomous: false
requirements: [DC-01, DC-06, DC-08, DC-09, DC-10, DC-11]
must_haves:
  truths:
    - "A PM opens the cockpit, fills a document's form, submits it, and a real Word file appears — proven through HTTP, not through a service call"
    - "The engineer links and the survey→install carry-forward are green on the same commit that unsurfaced them"
    - "The fence can still go red — re-proved on this commit, not inherited from 46.1"
    - "Every exact count this phase moved is listed in one ledger with its old value, new value and reason"
    - "The user has judged the page simple enough to use"
  artifacts:
    - path: "tests/Feature/Cockpit/CockpitDocCreationEndToEndTest.php"
      provides: "The through-HTTP proof for all four documents"
    - path: ".planning/phases/46.2-doc-creation-cockpit/46.2-COUNT-LEDGER.md"
      provides: "Every moved count, old → new, with its plan and reason"
      contains: "→"
  key_links:
    - from: "tests/Feature/Cockpit/CockpitDocCreationEndToEndTest.php"
      to: "routes/web.php projects.cockpit.documents.store"
      via: "real POST through the HTTP kernel, then a real file on disk"
      pattern: "cockpit/documents"
---

<objective>
Prove the phase end to end, re-prove the fence can fail, gather every moved count into one ledger,
and put the page in front of the user — because "simple to use" is their criterion and no assertion
in this repo can settle it.

Purpose: ROADMAP criteria 4 and 5, DC-08, DC-11. Output: an end-to-end test, a count ledger, the
three-way breakage ritual re-run on this commit, and a blocking human checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46.2-doc-creation-cockpit/46.2-CONTEXT.md
@.planning/phases/46.2-doc-creation-cockpit/46.2-FORMAT-INVENTORY.md
@.planning/phases/46.2-doc-creation-cockpit/46.2-RETIREMENT-LEDGER.md
@.planning/phases/46.2-doc-creation-cockpit/46.2-05-SUMMARY.md
@.planning/phases/45-visit-model-read-only-cockpit/45-BASELINE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The end-to-end walk, through HTTP, for all four documents</name>
  <files>tests/Feature/Cockpit/CockpitDocCreationEndToEndTest.php</files>
  <behavior>
    - For EACH of the four documents: authenticate, GET the cockpit, GET it again with
      `?module={key}&action=generate`, POST the form with realistic values through the real HTTP
      kernel, and assert (a) the redirect lands back on the module, (b) the value the PM typed is
      readable where the generator reads it, and (c) a real artifact exists on disk with non-zero
      bytes. Queue jobs are dispatched synchronously (`Queue::fake()` is NOT enough here — the
      point is that a file appears; run the job inline or assert the artifact after
      `Bus::dispatchSync`, whichever the existing generator tests already do).
    - The RAMS case is the load-bearing one: assert a planned start date and an engineer name
      entered on the form survive `RamsReviewDataService::normalise()` AND appear in the generated
      document's data. A value that round-trips through the payload but never reaches the document
      is the failure this test exists to catch.
    - The Worksheet case asserts Word only and asserts the DC-07 sentence is on the page.
    - `test_the_cockpit_renders_exactly_four_module_rows()` — the phase's headline criterion,
      asserted once, plainly, over the rendered page.
    - `test_the_visit_work_is_reachable_at_its_own_routes_on_this_commit()` — POST
      `projects.cockpit.visits.store`, `.accept`, `.send-back`, `.notes`, `.snags` and GET
      `.photos-zip` directly, with no cockpit link anywhere on the page, and assert each behaves as
      Phase 46/46.1 left it. **This is DC-09's proof, and it is the assertion that distinguishes
      "unsurfaced" from "deleted".**
    - `test_the_engineer_links_and_the_carry_forward_are_unchanged()` — delegate rather than
      duplicate: assert the four kept test files exist, then run them in the verify. Do not copy
      their assertions; a second copy drifts.
  </behavior>
  <action>
    Follow `tests/Feature/Cockpit/CockpitReturnedTabEndToEndTest.php`'s shape — it is this repo's
    model for "prove it through the real endpoints, not through the service". NEVER run
    `artisan migrate:fresh --env=testing`.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter CockpitDocCreationEndToEndTest</automated>
  </verify>
  <done>All four documents generate through HTTP; the visit routes still work with nothing linking to them.</done>
</task>

<task type="auto">
  <name>Task 2: The count ledger and the four gates</name>
  <files>.planning/phases/46.2-doc-creation-cockpit/46.2-COUNT-LEDGER.md</files>
  <action>
    Write one table: test class · assertion · old value · new value · plan · reason. It must carry
    every count this phase touched, including the ones that DID NOT MOVE and were re-taken, because
    a re-taken number is a decision and an untouched number is an oversight, and the ledger's job is
    to tell them apart. At planning time the expected set is:

      CockpitPageTest              cockpit write routes        5 → 6    46.2-05
      CockpitPageTest              cockpit GET routes          3 → 3    46.2-05  (re-taken)
      CockpitPageTest              cav-panel__tab              3 → 3    46.2-05  (re-taken)
      CockpitPageTest              cav-kpi                     3 → 3    unchanged, derived
      CockpitReadOnlyFenceTest     DEFERRED_AFFORDANCES       18 → 21   46.2-03
      CockpitReadOnlyFenceTest     WRITE_SURFACE_TABLES       11 → 13   46.2-05
      CockpitReadOnlyFenceTest     FORBIDDEN_MARKUP            2 → 2    46.2-03  (re-taken)
      CockpitReadOnlyFenceTest     BANNED_HANDLER_ATTRIBUTES   9 → 9    46.2-03  (re-taken)
      CockpitSpineTest             cav-module__count      n-1 → n       46.2-01
      CockpitModulePresenterTest   module keys vs ALL_KEYS   9 → 4 subset 46.2-01
      CockpitModulePresenterTest   visit types per row  exactly 1 → at most 1 + a named zero-set  46.2-01

    **Reconcile the table against the code before writing it** — read each assertion and record
    what it ACTUALLY says on this commit. A count that moved and is not in this table is the drift
    the house rule exists to catch, and finding one here is a success, not a failure.

    Then run the four gates and paste their output into the summary:
      `gate-46.ps1 -Path tests/Feature/Cockpit`  ·  `-Path tests/Unit/Cockpit`
      `gate-46.ps1 -Path tests/Feature/Visits`   ·  `-Filter SurveyCarryForward`
      `gate-46.ps1 -Baseline`   — `>= 159 passed AND 0 failed`, NEVER equality against 161
      `gate-46.ps1 -Hashes`     — three pins; a mismatch is a STOP, never a hash to refresh
    Background the long runs (~4 min each).
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Baseline</automated>
  </verify>
  <done>The ledger reconciles with the code, and all four gates are green and pasted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The three-way breakage ritual, and the simple-enough-to-use judgement</name>
  <what-built>
    The cockpit is now four document rows. Opening one slides in a panel carrying that document's
    fields and generates it in Word and PDF from the generators that already existed. The visit
    workflow, the engineer links, the returned-evidence review and the photo ZIP are all still
    there and still working — they are simply no longer on this page.
  </what-built>
  <how-to-verify>
    **PART A — the fence can still fail.** Three breakages, one at a time, each reverted before the
    next, each failure message recorded VERBATIM in the summary. Command for all three:
    `powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter CockpitReadOnlyFenceTest`

      1. VACUOUS EXTRACTION — in `cockpitRegion()`, return `''`.
         EXPECT RED: "The extracted cockpit region is empty — the fence would pass vacuously."
      2. TRUNCATED REGION — in `cockpitRegion()`, return `substr($region, 0, 400)`.
         EXPECT RED on the BOTTOM bracket, not the top. That asymmetry is why there are two.
      3. INJECTED AFFORDANCE, THE ASYMMETRY THAT MATTERS NOW — add
         `<select name="x"><option>y</option></select>` and `<button onclick="void 0">Add document</button>`
         to `doc-form.blade.php`.
         EXPECT THREE separate reds: `<select` (FORBIDDEN_MARKUP — the ruling re-taken in 46.2-03
         against this very surface), `onclick` (BANNED_HANDLER_ATTRIBUTES), and `Add document`
         (still Phase 48). EXPECT NOTHING about `Generate document` — the copy this phase ships.
         That combination proves the fence held against the most input-heavy surface it has covered
         and narrowed by nothing.

    **PART B — the user's judgement, which no test can substitute for.**
      1. Open a project's cockpit. Count the rows: there must be four.
      2. Open each of the four and look at its form. Is it simple? The criterion is yours and it has
         been the same every time: "simple to use", and an earlier design was rejected as "scary".
      3. Generate one document — Word, then PDF — and open the files.
      4. Three specific things to rule on, because the planner made judgements the decisions did not
         cover and they should not pass as settled:
         a. The read-only VISIT LIST and the "0 visits" count phrase were KEPT on the Site survey
            and Worksheet rows. Should they go too, so the page is documents and nothing else?
         b. The Worksheet row's title was changed from "First fix and install" to "Worksheet".
         c. **DC-07: the Worksheet has no PDF.** `worksheets.engineer-report-pdf` is the engineer's
            activity report, not the worksheet, and it 404s if nobody has visited. Producing a
            worksheet PDF means authorising a new render target, which D-04 forbids this phase.
            Options are in `46.2-FORMAT-INVENTORY.md`. This needs your call.
  </how-to-verify>
  <action>
    STOP and hand this to the developer. Record the three verbatim failure messages and the four
    rulings in `46.2-06-SUMMARY.md`. Do not close the phase until the developer has responded.
  </action>
  <resume-signal>Paste the three failure messages, and answer (a), (b) and (c) — or describe what looks wrong.</resume-signal>
  <done>The fence re-proved failable on this commit; the user has judged the page and ruled on the three open questions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a green suite → a working page | A phase can pass every assertion and still ship a page nobody can use, or a document nobody can open |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46.2-18 | Spoofing (a green fence that proves nothing) | `cockpitRegion()` | mitigate | The three-way breakage ritual, re-run on THIS commit with the `<select`-DOES-fire assertion proving the re-taking in 46.2-03 was real and not inherited. |
| T-46.2-19 | Repudiation | "The visit work still works" as a claim | mitigate | Asserted through the real routes on this commit, plus a `git diff --stat` showing zero changed lines under the protected paths (46.2-03's verification). A claim and a proof are different artifacts. |
| T-46.2-20 | Denial of Service | A generated document that is empty or unopenable | mitigate | The end-to-end test asserts non-zero bytes on disk; the human check opens the files. Byte count is necessary and not sufficient, which is why both exist. |
| T-46.2-SC | Tampering | npm/pip/composer installs | accept | This phase installs no package in any of its six plans. |
</threat_model>

<verification>
All four gates green and pasted. The three breakage messages recorded verbatim. The developer has
answered (a), (b) and (c).

Carry forward, unchanged, the two defects that are NOT this phase's: `D-46-05-01`
(`/worksheet/{token}` 500s for a roomless worksheet — live right now) and `D-46-06-01` (the
same-second send-back edge). Add any defect 46.2-04 raised (`programme.planned_end_time` read but
never normalised) to the same carried-forward list.
</verification>

<success_criteria>
- Four rows, four working forms, Word and PDF from existing generators — proven through HTTP.
- The visit work provably reachable and green at its own routes on the same commit.
- The fence provably still able to fail, with `<select` firing on the surface that tested it.
- One count ledger reconciled against the code.
- The user has judged the page and ruled on the visit list, the row title and the DC-07 gap.
</success_criteria>

<output>
Create `.planning/phases/46.2-doc-creation-cockpit/46.2-06-SUMMARY.md` when done.
</output>
