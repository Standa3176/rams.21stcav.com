---
phase: 46.2-doc-creation-cockpit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - routes/web.php
  - app/Http/Controllers/SiteSurveyController.php
  - tests/Feature/Documents/DocumentFormatInventoryTest.php
  - .planning/phases/46.2-doc-creation-cockpit/46.2-FORMAT-INVENTORY.md
autonomous: true
requirements: [DC-06, DC-07]
must_haves:
  truths:
    - "Site survey has a Word download route, served by the SiteSurveyDocxService that already existed and had zero callers"
    - "Every Word and PDF path the four documents have is enumerated in one file, measured not assumed"
    - "The Worksheet document's missing PDF path is recorded as a GAP with the evidence, not papered over"
  artifacts:
    - path: ".planning/phases/46.2-doc-creation-cockpit/46.2-FORMAT-INVENTORY.md"
      provides: "The measured eight-cell Word×PDF matrix for the four documents"
      contains: "NOT DELIVERED"
    - path: "tests/Feature/Documents/DocumentFormatInventoryTest.php"
      provides: "The inventory asserted as routes, so a renamed route fails loudly"
  key_links:
    - from: "routes/web.php"
      to: "app/Services/SiteSurveyDocxService.php"
      via: "site-surveys.docx → SiteSurveyController::downloadDocx → SiteSurveyDocxService::build"
      pattern: "site-surveys\\.docx"
---

<objective>
Establish, by measurement, which of the four documents can actually produce Word and which can
produce PDF — and close the one gap that can be closed without writing a generator: Site survey
Word, whose service `SiteSurveyDocxService` has existed all along with **zero callers**.

Purpose: D-04 says Word AND PDF for all four and forbids writing a new generator. Those two
instructions collide for exactly one cell of the matrix, and the phase must not discover that while
building a form. Output: a wired `site-surveys.docx` route, a measured inventory file, and the
Worksheet-PDF gap recorded as DC-07 NOT DELIVERED for a user decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46.2-doc-creation-cockpit/46.2-CONTEXT.md
@app/Services/SiteSurveyDocxService.php
@app/Http/Controllers/SiteSurveyController.php

<interfaces>
<!-- MEASURED 2026-09-22 at planning time. These are findings, not guesses.
     Re-confirm each with the grep in Task 1 before relying on it. -->

WORD (.docx) paths that exist today:
  RAMS        DocxBuilderServiceV2::build(array $data, RamsDocument $record): string
              reached by GET  rams.download              (routes/web.php:475)
  Worksheet   WorksheetDocxService::build(array $generatedData, Worksheet $worksheet): void
              reached by GET  worksheets.download        (routes/web.php:700)
  O&M         OmManualDocxService
              reached by GET  om-manuals.download        (routes/web.php:613)
  Site survey SiteSurveyDocxService::build(SiteSurvey $survey): string
              reached by NOTHING. `grep -rn "SiteSurveyDocxService" app/ routes/` returns only its
              own definition. No route, no controller, no job. THIS PLAN WIRES IT.

PDF paths that exist today:
  RAMS        PdfService::buildRams(RamsDocument): string   → GET rams.download-pdf      (:476)
  Site survey SurveyPdfService::buildSummary(SiteSurvey, bool $internal): string
                                                            → GET site-surveys.pdf       (:563)
              (also buildBlank() → site-surveys.blank-form, and clientReport())
  O&M         PdfService (om-manuals)                       → GET om-manuals.download-pdf (:614)
  Worksheet   NONE. `worksheets.engineer-report-pdf` (:704) renders `pdf.engineer-report`, which is
              EngineerActivityService::buildReportContext() — the engineer's ACTIVITY report
              (outstanding items, per-room photos, sign-offs) — and `abort_if(!
              $worksheet->hasEngineerActivity(), 404)`. It is a different document, and it 404s for
              a worksheet nobody has visited yet. `ls resources/views/pdf | grep -i work` returns
              nothing: there is no worksheet PDF Blade.

The SiteSurveyController wiring pattern to copy, verbatim in shape (SiteSurveyController.php:536):
```php
public function downloadPdf(Request $request, SiteSurvey $siteSurvey): BinaryFileResponse
{
    $this->authorizeSurvey($siteSurvey);
    $path = $this->pdfService->buildSummary($siteSurvey, $request->boolean('internal', true));
    return response()->download($path, 'site-survey-' . $siteSurvey->id . '.pdf');
}
```
`SiteSurveyDocxService::build()` returns the absolute path and sets `$survey->filename` itself.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Measure the matrix and write the inventory</name>
  <files>.planning/phases/46.2-doc-creation-cockpit/46.2-FORMAT-INVENTORY.md</files>
  <behavior>
    - The file holds one table with four rows (Site survey, Worksheet, RAMS, O&M manual) and two
      format columns, and every cell names EITHER the route + service that produces it OR the word
      GAP with the evidence for the gap.
    - Every claim in the table is backed by a command whose output is pasted under the table:
      `grep -rn "SiteSurveyDocxService\|WorksheetDocxService\|OmManualDocxService\|DocxBuilderServiceV2" app/ routes/ --include=*.php`
      and `grep -n "download\|pdf" routes/web.php | grep -i "rams\|survey\|worksheet\|om-manual"`
      and `ls resources/views/pdf`.
    - The Worksheet PDF cell reads **GAP — DC-07, NOT DELIVERED** and states the three facts that
      make it a gap rather than an oversight: `pdf.engineer-report` is a different document; it
      404s without engineer activity; there is no `pdf.worksheet` Blade, so producing one would
      mean authoring a second renderer, which D-04 forbids.
    - The file ends with the decision the user must take, stated as options with their costs, and
      NOTHING is chosen on their behalf: (a) accept Word-only for the Worksheet in 46.2;
      (b) authorise a `resources/views/pdf/worksheet.blade.php` as a new phase, which is a new
      render target and therefore outside D-04; (c) offer the engineer-activity report as the
      Worksheet's PDF, which is honest only if it is RELABELLED, because it is not the worksheet.
  </behavior>
  <action>
    Run the three commands, paste their real output, and write the table from what they returned —
    not from this plan's `<interfaces>` block, which is a planning-time measurement that must be
    re-confirmed. If any cell disagrees with the block above, the COMMAND WINS and the disagreement
    goes in the summary.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter DocumentFormatInventoryTest</automated>
  </verify>
  <done>The inventory exists, every cell is evidenced, and DC-07 is stated as a decision the user has not yet taken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Site survey Word to the service that already exists</name>
  <files>routes/web.php, app/Http/Controllers/SiteSurveyController.php</files>
  <behavior>
    - `GET site-surveys/{siteSurvey}/docx`, named `site-surveys.docx`, inside the same `auth`
      group and immediately beside `site-surveys.pdf` (routes/web.php:563), so route ordering
      against the `Route::resource('site-surveys', ...)` at :554 behaves identically to the PDF
      route that already works there.
    - `SiteSurveyController::downloadDocx(SiteSurvey $siteSurvey): BinaryFileResponse` calls
      `$this->authorizeSurvey($siteSurvey)` FIRST — the same guard as `downloadPdf`, not a looser
      one — then `app(SiteSurveyDocxService::class)->build($siteSurvey)` and
      `response()->download($path, 'site-survey-' . $siteSurvey->id . '.docx')`.
    - An unauthenticated GET is rejected by the same guard that rejects it on `site-surveys.pdf` —
      asserted, not assumed.
    - A survey with NO rooms still produces a file rather than a 500. This is the exact shape of
      live defect `D-46-05-01` (`/worksheet/{token}` 500s for a roomless worksheet), which is NOT
      this phase's to fix — but shipping a second instance of it would be. `SiteSurveyDocxService`
      iterates `$survey->rooms->sortBy('sort_order')`, which is empty-safe; prove it with a test
      rather than reasoning about it, and if it DOES throw, stop and report rather than patching
      the service.
    - `SiteSurveyDocxService::build()` writes `$survey->update(['filename' => ...])`. That is a
      WRITE on a GET. It is acceptable OUTSIDE the cockpit (this route is not a cockpit route and
      is not covered by the fence's row-count invariance) but it MUST be named in the summary,
      because Plan 46.2-05 will link to this route from inside the cockpit and the fence's
      write-surface rule is about the cockpit's OWN GETs, not about what a link's target does.
      Do not change the service to avoid the write — that is a refactor this phase has no mandate
      for, and the minimal-diff rule applies.
  </behavior>
  <action>
    Add the route and the controller method. Import `SiteSurveyDocxService` at the top of the
    controller in the existing import block; resolve it via constructor injection if the
    constructor already takes services (it takes `SurveyPdfService` at :25), matching the file's
    own convention rather than introducing `app()` calls mid-method.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter DocumentFormatInventoryTest</automated>
  </verify>
  <done>`site-surveys.docx` resolves, downloads a .docx, is guarded identically to `site-surveys.pdf`, and survives a roomless survey.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The inventory as an executable assertion</name>
  <files>tests/Feature/Documents/DocumentFormatInventoryTest.php</files>
  <behavior>
    - A const map of document key → `['word' => 'route.name'|null, 'pdf' => 'route.name'|null]`
      holding exactly the four documents, with `worksheet.pdf => null` carrying an inline comment
      naming DC-07 and the reason.
    - `test_every_named_format_route_exists()` asserts `Route::has()` for every non-null entry, so
      a renamed or deleted route fails here rather than as a dead control on a PM's screen — the
      same discipline as `quick-actions.blade.php`'s existing `Route::has()` guard.
    - `test_the_only_missing_format_is_the_worksheet_pdf()` asserts the set of null cells is
      EXACTLY `['worksheet.pdf']`. This is the anti-rot half: if a later plan quietly drops another
      format, this goes red. Keep it exact; never a floor.
    - `test_site_survey_word_downloads_a_docx()` — authenticated GET on `site-surveys.docx` for a
      survey with one room returns 200 and a `.docx` filename.
    - `test_site_survey_word_survives_a_survey_with_no_rooms()` — the same GET on a roomless survey
      returns 200, not 500 (the D-46-05-01 shape, proven absent here rather than assumed).
    - `test_site_survey_word_is_guarded_like_site_survey_pdf()` — an unauthenticated GET on both
      routes returns the same status.
  </behavior>
  <action>
    Extend `Tests\TestCase` with `RefreshDatabase`, following the shape of the existing
    `tests/Feature/Visits/PublicTokenRoutesUnaffectedTest.php`. NEVER run
    `artisan migrate:fresh --env=testing` — phpunit.xml uses sqlite `:memory:` and there is no
    `.env.testing`; it would wipe `database/database.sqlite`.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter DocumentFormatInventoryTest</automated>
  </verify>
  <done>All five tests green; the missing-format set is exactly one cell and it is named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated staff → `site-surveys.docx` | A route-model-bound `{siteSurvey}` reaches a file writer; this is a new authenticated entry point into a service that had none |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46.2-03 | Elevation of Privilege | `SiteSurveyController::downloadDocx` | mitigate | Call `authorizeSurvey($siteSurvey)` as the FIRST statement — the identical guard `downloadPdf` uses. Asserted by `test_site_survey_word_is_guarded_like_site_survey_pdf()`. |
| T-46.2-04 | Information Disclosure | Generated `.docx` written under `storage/app/site-surveys/` with a predictable name | accept | Matches the existing PDF pipeline's storage convention exactly; the directory is outside the public root and is only reachable through this guarded route. No new exposure is created. |
| T-46.2-05 | Tampering | `SiteSurveyDocxService::build()` writing `filename` during a GET | accept | Documented, not changed. The minimal-diff rule (46.2-CONTEXT.md) forbids refactoring the service; the route is outside the cockpit region so the fence's GET row-count invariance does not cover it, and this plan states that boundary rather than implying it. |
| T-46.2-SC | Tampering | npm/pip/composer installs | accept | This plan installs no package. `phpoffice/phpword` is already a direct dependency used by all three existing DOCX services. |
</threat_model>

<verification>
`gate-46.ps1 -Filter DocumentFormatInventoryTest` — green.
`gate-46.ps1 -Path tests/Feature/Visits` — the engineer-link routes must be untouched by a new
route registration; expect no change.
`gate-46.ps1 -Baseline` — `>= 159 passed AND 0 failed`. `-Hashes` — three pins unchanged.
</verification>

<success_criteria>
- `site-surveys.docx` exists, guarded like `site-surveys.pdf`, serving the pre-existing
  `SiteSurveyDocxService`. No new generator was written.
- `46.2-FORMAT-INVENTORY.md` enumerates all eight cells with pasted command output as evidence.
- DC-07 is recorded as a GAP with three named options and no option chosen for the user.
</success_criteria>

<output>
Create `.planning/phases/46.2-doc-creation-cockpit/46.2-02-SUMMARY.md` when done. Surface the
Worksheet-PDF gap at the TOP of the summary — it is the finding the user needs, not a footnote.
</output>
