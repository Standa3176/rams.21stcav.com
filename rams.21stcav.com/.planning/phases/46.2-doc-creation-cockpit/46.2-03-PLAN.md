---
phase: 46.2-doc-creation-cockpit
plan: 03
type: execute
wave: 2
depends_on: ["46.2-01"]
files_modified:
  - resources/views/components/cockpit/panel.blade.php
  - resources/views/components/cockpit/quick-actions.blade.php
  - resources/views/components/cockpit/returned-tab.blade.php
  - app/Http/Controllers/ProjectCockpitController.php
  - tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php
  - tests/Feature/Cockpit/CockpitCreateVisitTest.php
  - tests/Feature/Cockpit/CockpitVisitActionsTest.php
  - tests/Feature/Cockpit/CockpitOfficeNoteAndSnagTest.php
  - tests/Feature/Cockpit/CockpitReturnedTabTest.php
  - tests/Feature/Cockpit/CockpitReturnedTabEndToEndTest.php
  - tests/Feature/Cockpit/CockpitTabPreservationTest.php
autonomous: true
requirements: [DC-09, DC-10, DC-11]
must_haves:
  truths:
    - "The cockpit offers no visit control — no Create visit, Accept, Send back, Add note or Raise a snag — and no Returned tab"
    - "Every route, model, service and command behind those controls still exists and still works when called directly"
    - "/survey/{token} and /worksheet/{token} behave exactly as before, and the survey→install carry-forward is still live"
    - "Every render assertion the unsurfacing invalidates was retired by name with 46.2 D-01/D-02 cited, never deleted to make a red test pass"
  artifacts:
    - path: "resources/views/components/cockpit/panel.blade.php"
      provides: "Three tabs — Overview, Files, Notes — and no write control"
    - path: ".planning/phases/46.2-doc-creation-cockpit/46.2-RETIREMENT-LEDGER.md"
      provides: "Every retired assertion, its test class, and the decision cited"
      contains: "D-02"
  key_links:
    - from: "tests/Feature/Visits/PublicTokenRoutesUnaffectedTest.php"
      to: "routes/web.php survey/{token} and worksheet/{token}"
      via: "unchanged public routes, asserted green after the unsurfacing"
      pattern: "survey/\\{token\\}"
---

<objective>
Take the visit workflow and the returned-evidence review OFF the cockpit without deleting one line
of the code behind them, and record every test assertion that change invalidates in a ledger that
names the decision rather than in a diff that hides it.

Purpose: D-02. Phases 46 and 46.1 built ~30 plans of working, tested code. This plan removes its
SURFACING only. Output: a panel with three tabs and no write control; a ledger; and green proof
that the engineer links and the survey→install carry-forward are untouched.

**THE THING THIS PLAN MUST NOT BREAK:** the survey→install carry-forward
(`app/Support/Visits/SurveyCarryForward.php`, Plan 46-03). It lives on the engineer's link, not the
cockpit, and it is the one piece with safety consequences — an installing engineer seeing the
surveyor's access constraints, site risks and H&S notes. Losing it is the worst outcome this phase
could produce. If any task in this plan touches it, STOP.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46.2-doc-creation-cockpit/46.2-CONTEXT.md
@.planning/phases/46.2-doc-creation-cockpit/46.2-01-SUMMARY.md
@resources/views/components/cockpit/panel.blade.php
@tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php

<interfaces>
<!-- WHAT IS KEPT (do not open these files to "tidy" them — this plan does not touch them): -->
  app/Models/Visit.php, app/Models/Snag.php
  app/Support/Visits/VisitLinkIssuer.php, app/Support/Visits/SurveyCarryForward.php
  app/Support/Cockpit/VisitEvidence.php, VisitPhotoZipBuilder.php, CockpitEvidencePresenter.php
  app/Http/Controllers/ProjectCockpitActionController.php  (5 POST routes — ALL STAY REGISTERED)
  app/Http/Controllers/ProjectCockpitEvidenceController.php (2 GET routes — BOTH STAY REGISTERED)
  routes/web.php:264-307 — every cockpit visit/evidence route stays exactly as written
  Public: survey/{token}, worksheet/{token} and every child route — UNCHANGED
  Tests that stay exactly as written because they exercise ROUTES, not renders:
    tests/Feature/Visits/PublicTokenRoutesUnaffectedTest.php
    tests/Feature/Visits/SendBackReopensEngineerLinkTest.php
    tests/Feature/Visits/VisitLifecycleEndToEndTest.php
    tests/Unit/Visits/SurveyCarryForwardTest.php
    tests/Feature/Worksheets/SurveyCarryForwardOnEngineerLinkTest.php
    tests/Feature/Cockpit/CockpitEvidenceDownloadTest.php  (the ZIP and photo GETs)

<!-- WHAT IS REMOVED — surfacing only: -->
  resources/views/components/cockpit/quick-actions.blade.php  (the Create-visit form + control)
  resources/views/components/cockpit/returned-tab.blade.php   (the evidence tab)
  panel.blade.php:117-128 — the `returned` tab entry and its `$offersReturned` guard
  panel.blade.php — the `@elseif ($tab === 'returned')` branch and the four review controls
  ProjectCockpitController — `$panelEvidence`, `evidenceFor()`, `resolveAction()`,
    `$quickActionRooms`, `$quickActionPeople`, `$actionVisitId` and the `roomNames()` /
    `activePeople()` helpers they feed

<!-- THE PLANNER'S RULING THAT NEEDS THE USER'S EYES (surface it at the 46.2-06 checkpoint):
     The read-only VISIT LIST on the Overview tab and the row's "0 visits" count phrase STAY.
     46.2-CONTEXT.md D-02 removes the surfacing of the WORKFLOW; a PM generating a worksheet is
     helped, not confused, by seeing that two visits exist. Removing the count phrase would mean
     redesigning the module row, and sketch 004's visual design explicitly SURVIVES this phase.
     This is a judgement, not a decision the user took — say so. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the surfacing — the controls, the tab and the controller wiring</name>
  <files>resources/views/components/cockpit/panel.blade.php, resources/views/components/cockpit/quick-actions.blade.php, resources/views/components/cockpit/returned-tab.blade.php, app/Http/Controllers/ProjectCockpitController.php</files>
  <action>
    Delete `quick-actions.blade.php` and `returned-tab.blade.php` from disk and remove their
    `<x-cockpit.*>` call sites from `panel.blade.php`. Deleting them is correct and is this repo's
    own established rule — see `CockpitPageTest`'s ruling on the superseded accordion: "a dead
    drawer component left on disk is an invitation for a later agent to render one beside the new
    design." These are SURFACING files; nothing in D-02's protected list is a Blade component.

    In `panel.blade.php`: reduce `$tabs` to the literal three — `overview`, `files`, `notes` — and
    delete the `$offersReturned` derivation and the `if ($tab === 'returned')` fallback with it.
    Delete the `@elseif ($tab === 'returned')` branch. Remove the `evidence`, `action`,
    `action-visit-id`, `rooms` and `people` props from `@props`. Rewrite the component docblock's
    fence paragraph so it states what is now true: the cockpit region carries a document form
    (Plan 46.2-05) and NO visit control, and the visit workflow lives at its own routes — naming
    `projects.cockpit.visits.store` / `.accept` / `.send-back` / `.notes` / `.snags` and
    `projects.cockpit.visits.photos-zip` so the next reader finds the kept code instead of
    concluding it was deleted.

    In `projects/cockpit.blade.php`, drop the five now-unused attributes from the `<x-cockpit.panel>`
    call. In `ProjectCockpitController::show()`, delete the variables and private helpers listed in
    `<interfaces>`. Leave the `visits.snags` eager-load ALONE — the read-only visit list still
    renders a snag count.

    DO NOT touch `routes/web.php`. DO NOT touch any file under `app/Models`, `app/Support/Visits`,
    `app/Support/Cockpit/VisitEvidence.php`, `VisitPhotoZipBuilder.php`,
    `CockpitEvidencePresenter.php` or either action/evidence controller. `CockpitEvidencePresenter`
    and `VisitEvidence` become zero-caller services from the cockpit's side — that is deliberate
    and it matches the precedent `SiteSurveyDocxService` set (a written, tested service waiting for
    a surface). Say so in the summary; do not "clean them up".
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Path tests/Feature/Visits</automated>
  </verify>
  <done>The panel renders three tabs and no form; every visit route still resolves; tests/Feature/Visits green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The retirement ledger — every invalidated render assertion, named</name>
  <files>tests/Feature/Cockpit/CockpitCreateVisitTest.php, tests/Feature/Cockpit/CockpitVisitActionsTest.php, tests/Feature/Cockpit/CockpitOfficeNoteAndSnagTest.php, tests/Feature/Cockpit/CockpitReturnedTabTest.php, tests/Feature/Cockpit/CockpitReturnedTabEndToEndTest.php, tests/Feature/Cockpit/CockpitTabPreservationTest.php, .planning/phases/46.2-doc-creation-cockpit/46.2-RETIREMENT-LEDGER.md</files>
  <behavior>
    - Each of the six test classes is split along ONE line, applied identically everywhere:
      an assertion that POSTs to a route and checks the DATABASE or the RESPONSE **stays exactly as
      written** — it proves the kept code and the kept code is unchanged. An assertion that renders
      the cockpit and checks for MARKUP or COPY is **retired**.
    - Every retired test method is replaced by a comment block at its former position naming: the
      method name, 46.2 D-02 (unsurfaced, not deleted), the route where the capability still lives,
      and the plan number. The repo's existing model for this wording is
      `CockpitReadOnlyFenceTest::test_the_fence_enumerates_the_whole_deferred_set()`.
    - `CockpitVisitActionsTest::test_..._renders_exactly_three_controls()` (the `countControls`
      assertions at :622) and `CockpitCreateVisitTest::test_no_module_panel_ever_renders_more_than_
      four_quick_actions()` are retired AND REPLACED, not merely removed, by
      `test_no_module_panel_renders_any_visit_control()` — asserting ZERO. VL-11's cap of four
      becomes a cap of zero rather than an absence of a test, so a later plan that re-renders a
      visit control on this page trips something.
    - `CockpitTabPreservationTest` keeps every case about `?tab=` round-tripping across a POST, with
      `returned` swapped for `notes`; the tab mechanism is not what changed.
    - The ledger file lists every retired method as a table row: class · method · what it asserted ·
      why it is now impossible · where the capability still lives.
  </behavior>
  <action>
    Work class by class. For each, run the class first and let the FAILURES tell you which
    assertions are render assertions — do not guess from the method name, and do not retire a test
    that is still green. A green test is by definition not invalidated by this change, and
    retiring one would be exactly the "deleted to make a change fit" move the house rule forbids.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Path tests/Feature/Cockpit</automated>
  </verify>
  <done>tests/Feature/Cockpit green; every retirement carries its decision inline AND a ledger row; zero assertions deleted without a replacement or a named reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Move the fence's affordance count, and re-take the three rulings</name>
  <files>tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php</files>
  <behavior>
    - `DEFERRED_AFFORDANCES` **18 → 21**, and `assertCount(18, ...)` becomes `assertCount(21, ...)`
      with the move recorded in the existing comment block in the same voice as the 15→18→19→18
      history already there. The three additions are exactly the three strings that were previously
      **LIFTED** from this list and whose affordance the cockpit no longer renders:
        'Create visit' => 'Unsurfaced by 46.2 D-02 — lives at projects.cockpit.visits.store'
        'Add note'     => 'Unsurfaced by 46.2 D-02 — lives at projects.cockpit.visits.notes'
        'Download'     => 'Unsurfaced by 46.2 D-02 — the ZIP still lives at projects.cockpit.visits.photos-zip'
      State the PRINCIPLE that bounds the additions, so the list cannot grow without limit: **an
      entry returns to this list when, and only when, it was once lifted from it and the affordance
      it named has since left the page.** The owner string says "Unsurfaced", never "Phase NN",
      because these are not deferred — they exist and they work, at the routes named.
    - `assertCount(2, FORBIDDEN_MARKUP)` → **2, RE-TAKEN, NOT INHERITED.** `<select` and `<script`
      both stay. Record why the ruling was live again: this phase ships the most input-heavy surface
      the cockpit has carried, so `<select` was genuinely up for retirement. It stays because every
      field the document form needs is a text, date, checkbox or two-to-five-option radio group
      (Plan 46.2-04's field map) — and because if Plan 46.2-05 finds a field that genuinely needs a
      select, **it lifts the entry BY NAME in the commit that ships the control**, exactly as 46-04
      and 46.1-04 did. Never by deletion.
    - `assertCount(9, BANNED_HANDLER_ATTRIBUTES)` → **9, re-taken for the fourth time.** There is
      still no JavaScript on this page; the form's disclosure is `?action=` query-string state.
    - `assertCount(11, WRITE_SURFACE_TABLES)` → **11, unchanged by THIS plan.** Plan 46.2-05 moves
      it; say so here so the next reader knows the number was considered, not skipped.
    - The fence's render loops iterate `array_keys(moduleMap())` and `?action=create-visit`. The
      `create-visit` action no longer exists, so that render call is retired by name and replaced in
      Plan 46.2-05 by `?action=generate`. Until then the loop covers the three tabs only.
  </behavior>
  <action>
    Edit the constants and the count assertions together, in one commit, with every number's reason
    beside it. Never `assertGreaterThanOrEqual`. Never remove an entry to make a change fit.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter CockpitReadOnlyFenceTest</automated>
  </verify>
  <done>The fence is green at 21/2/9/11, each number carrying its reason, and the `<select` ruling is re-taken in writing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated staff → the five cockpit POST routes | The routes stay registered while nothing on the page links to them — reachable by anyone who knows the URL, exactly as before |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46.2-06 | Elevation of Privilege | `ProjectCockpitActionController`'s five POSTs, now link-less | accept | Unchanged risk: these routes were never protected by the absence of a link. Their real guards — `auth`, `@csrf`, per-route validation and route-bound `{project}` scoping — are untouched, and `CockpitVisitActionsTest`'s route-behaviour half still proves them. Removing a link is not a security change in either direction. |
| T-46.2-07 | Information Disclosure | `projects.cockpit.visits.photos-zip`, now link-less | accept | Same reasoning. `CockpitEvidenceDownloadTest` (path traversal, hostile room names, sanitised entry names) stays green and unedited. |
| T-46.2-08 | Repudiation | Office notes and snags still writable with no UI | accept | `ProjectActivityLog` still records who and when. An act with no button is still an audited act. |
| T-46.2-SC | Tampering | npm/pip/composer installs | accept | This plan installs no package and deletes two Blade files. Nothing to audit. |
</threat_model>

<verification>
Background the long runs (~4 min):
  `gate-46.ps1 -Path tests/Feature/Cockpit`  — green
  `gate-46.ps1 -Path tests/Feature/Visits`   — green, UNEDITED
  `gate-46.ps1 -Filter SurveyCarryForward`   — green, UNEDITED. This is the safety-critical one.
  `gate-46.ps1 -Baseline`                    — `>= 159 passed AND 0 failed`
  `gate-46.ps1 -Hashes`                      — three pins match; a mismatch is a STOP.

Prove the unsurfacing is a surfacing change and not a deletion, with a command rather than a claim:
`git diff --stat` must show ZERO lines changed under `app/Models/`, `app/Support/Visits/`,
`routes/web.php`, `app/Http/Controllers/ProjectCockpitActionController.php` and
`app/Http/Controllers/ProjectCockpitEvidenceController.php`. Paste it into the summary.
</verification>

<success_criteria>
- No cockpit render contains `Create visit`, `Accept`, `Send back`, `Add note`, `Raise a snag`,
  `Download all photos` or a `Returned` tab.
- Every visit route, model, service and command is byte-unchanged and green at its own route.
- `/survey/{token}`, `/worksheet/{token}` and the survey→install carry-forward: green, unedited.
- The retirement ledger exists and every retired method has a row and an inline comment.
- DEFERRED_AFFORDANCES at 21 with the return principle stated; 2 / 9 / 11 re-taken in writing.
</success_criteria>

<output>
Create `.planning/phases/46.2-doc-creation-cockpit/46.2-03-SUMMARY.md` when done, carrying forward
the two open defects that are NOT this phase's: `D-46-05-01` (`/worksheet/{token}` 500s for a
roomless worksheet — live right now) and `D-46-06-01` (the same-second send-back edge).
</output>
