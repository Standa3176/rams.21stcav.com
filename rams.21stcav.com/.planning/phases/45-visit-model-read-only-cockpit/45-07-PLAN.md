---
phase: 45-visit-model-read-only-cockpit
plan: 07
type: execute
wave: 5
depends_on: [45-04, 45-05, 45-06]
files_modified:
  - app/Http/Controllers/ProjectCockpitController.php
  - app/Support/Cockpit/CockpitSectionPresenter.php
  - resources/views/projects/cockpit.blade.php
  - resources/views/components/cockpit/drawer.blade.php
  - resources/views/components/cockpit/visit-row.blade.php
  - resources/views/components/cockpit/doc-row.blade.php
  - resources/views/components/cockpit/lifecycle-row.blade.php
  - tests/Feature/Cockpit/CockpitSpineTest.php
  - tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php
autonomous: true
requirements: [VIS-04, VIS-06, VIS-08]
must_haves:
  truths:
    - "All nine deliverable sections render as drawers, every one closed at rest"
    - "A reconstructed visit is disclosed in the summary count slot, so a PM reading the closed spine is never misled by omission"
    - "A superseded visit keeps its position in date order, marked rather than hidden"
    - "Not one of the fifteen write affordances drawn in the sketch is rendered inside the cockpit subtree — proven by a test, not by inspection"
  artifacts:
    - path: "app/Support/Cockpit/CockpitSectionPresenter.php"
      provides: "Maps existing model helpers to per-drawer pip state, count and status text. No queries beyond the controller's eager-loads; NOT an extension of ProjectHealthService"
    - path: "resources/views/components/cockpit/drawer.blade.php"
      provides: "<details> with a one-line <summary>: pip, title, count, status, arrow. Never ships the open attribute"
      contains: "<details"
    - path: "resources/views/components/cockpit/visit-row.blade.php"
      provides: "Date, title, sub-line, chips, status — plus the reconstructed and superseded treatments"
    - path: "tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php"
      provides: "Asserts no form, input, select, button, script or write-affordance copy appears WITHIN the cav-brand cav-cockpit subtree (the shared app layout's logout form, search input, buttons and @vite/Alpine scripts are explicitly out of scope), plus a five-table row-count invariance check proving rendering writes nothing"
      contains: "cav-cockpit"
  key_links:
    - from: "app/Support/Cockpit/CockpitSectionPresenter.php"
      to: "app/Models/Worksheet.php / app/Models/SiteSurvey.php / app/Models/InstallProgramme.php / app/Models/Project.php"
      via: "existing named accessors only — isSigned(), isDraft(), statusLabel(), deliverableState()"
      pattern: "deliverableState|isSigned|statusLabel"
    - from: "resources/views/components/cockpit/visit-row.blade.php"
      to: "app/Models/Visit.php"
      via: "isBackfilled() and isSuperseded() drive the two qualifier treatments"
      pattern: "isBackfilled|isSuperseded"
---

<objective>
The spine. Nine section drawers, closed at rest, each one also its deliverable, with the visit rows
inside them — including the two qualifier treatments that make the record honest: **reconstructed**
(an inference, dashed) and **superseded** (a recorded fact about the paperwork, solid).

Purpose: this is where D-02 and D-04 become visible, and where the read-only fence is enforced as a
test rather than a promise. The sketch draws fifteen write affordances; Phase 45 renders none of
them, and a PM must not be shown a row that looks clickable and is not.
Output: a presenter, four components, the filled-in page, and two feature tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-visit-model-read-only-cockpit/45-UI-SPEC.md
@.planning/phases/45-visit-model-read-only-cockpit/45-CONTEXT.md
@.planning/phases/45-visit-model-read-only-cockpit/45-06-SUMMARY.md

<interfaces>
<!-- Every fact this page needs already has a named, tested accessor. The failure mode to guard
     against is re-deriving one of them slightly differently and producing a cockpit that
     disagrees with the eleven-tab page. Use these; do not invent parallel derivations. -->

Per-drawer state sources (45-UI-SPEC § Where per-drawer state comes from):
```php
// Site survey
SiteSurvey::isDraft();        // :112
SiteSurvey::isCompleted();    // :120   + submitted_at, superseded_at
// First fix / Install
Worksheet::isSigned();            // :177
Worksheet::isStale();             // :287
Worksheet::hasEngineerActivity(); // :375
Worksheet::isReadyForSignoff();   // :420
Worksheet::statusBadgeClass();    // :242
// Programme
InstallProgramme::statusLabel();     // :106
InstallProgramme::statusBadgeClass();// :120
InstallProgramme::isDraft();         // :134
InstallProgramme::isActive();        // :142
InstallProgramme::commissioningSignoff(); // :96-99  — ->exists() for the commissioning pip
// Deliverables (RAMS, drawings, O&M, cable schedule)
Project::deliverableState($key);  // :447+ — safe to call without a query, checks relationLoaded() first
Project::snaggingSignoffs();      // :433-441 HasManyThrough. Its docblock at :418-432 says: do NOT
                                  // look for or add a Project::snagging() shortcut or a Snagging
                                  // model; neither exists.
```

Plan 45-02's model:
```php
Visit::isBackfilled(): bool;   // the D-02 marker
Visit::isSuperseded(): bool;   // DERIVED from the withTrashed() source's superseded_at / deleted_at
Visit::source(): ?Model;       // withTrashed(); null only if force-deleted
```

PROGRAMMING — ProjectHealthService.php:105-107 says it verbatim: "Programming (KEY_PROGRAMMING) is
skipped entirely: no model, table, or relation exists for it anywhere in this codebase (D-05)."
The sketch reached the same conclusion independently. Programming carries a BOX, not a light, and
in Phase 45 that box renders UNTICKED ONLY — nothing can store a tick.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CockpitSectionPresenter — per-drawer state from existing accessors only</name>
  <files>app/Support/Cockpit/CockpitSectionPresenter.php, app/Http/Controllers/ProjectCockpitController.php</files>
  <behavior>
    - For each of the nine sections the presenter returns a small value array: `pip` (one of
      `done` / `attention` / `waiting`, or `null` for the hand-ticked programming section), `title`,
      `count` (the disclosure string), `status` (the visible text beside the pip), and `hint`.
    - **Every value comes from an existing named accessor** (see `<interfaces>`). The presenter
      introduces no new query, no new derivation source and no new capture — criterion 5.
    - **It does NOT extend, subclass or modify `ProjectHealthService`.** That service is the
      dashboard's derivation; changing it changes what the dashboard renders with the cockpit flag
      OFF, which is a criterion-4 violation. It is reused unchanged for the masthead/attention line
      only, which Plan 45-06 already wired.
    - **Count-slot disclosure is mandatory**, because drawers are closed at rest and the count slot
      is the only place a qualifier is visible without the PM opening anything:
      `"3 visits · 2 reconstructed"`, `"2 visits · 1 superseded"`, singular drops the numeral:
      `"1 visit · reconstructed"`. Empty: `"none yet"` with status `"Not started"` and a waiting pip.
    - **A reconstructed or superseded visit never drives a drawer to the attention state.** Gold
      means someone must act; a qualifier is a fact, not an action, and neither can be actioned on a
      read-only page. Neither appears in the attention line's count.
    - The programming section returns `pip => null` and a tick-box in the **unticked** state, with
      status `"Not marked"` and the body copy minus its final "Ticked by ..." sentence.
    - **All nine sections always render**, including not-required ones — a missing drawer would read
      as an app fault.
    - The controller passes the presenter's output to the view; it gains no new query beyond adding
      `visits` and the programme relations to its existing eager-load list.
  </behavior>
  <action>
    Create `app/Support/Cockpit/CockpitSectionPresenter.php` — a plain final class with one public
    method taking the already-loaded `Project` and returning the nine section arrays. Follow the
    repo's service conventions: PSR-12, 4-space indent, aligned array keys, `// -- Label --`
    dividers, a class docblock recording that it deliberately does NOT extend `ProjectHealthService`
    and why.

    Extend the controller's eager-load with the relations the presenter needs (`visits`,
    `installProgrammes`, `activeInstallProgramme`, `worksheets`, plus whatever
    `deliverableState()` requires loaded) so no lazy query fires during render. Keep the controller
    thin — it wires, it does not derive.
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com"; & "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan test tests/Feature/Cockpit 2>&1 | Select-Object -Last 25; if ((git diff --name-only -- app/Services/ProjectHealthService.php app/DTO/ProjectHealth.php resources/views/components/dashboard/health-badge.blade.php) -ne $null) { throw "FORBIDDEN: the dashboard's health derivation was modified - criterion 4 violation" }; "OK"</automated>
  </verify>
  <done>Presenter returns nine sections from existing accessors only; `ProjectHealthService`, its DTO and the health-badge component are unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: drawer, visit-row, doc-row, lifecycle-row + the filled spine</name>
  <files>resources/views/components/cockpit/drawer.blade.php, resources/views/components/cockpit/visit-row.blade.php, resources/views/components/cockpit/doc-row.blade.php, resources/views/components/cockpit/lifecycle-row.blade.php, resources/views/projects/cockpit.blade.php</files>
  <action>
    `drawer.blade.php` — native `<details>` / `<summary>`, **zero JavaScript**, no Alpine. **No
    drawer ever ships the `open` attribute**, including Install (the sketch shows it open purely to
    demonstrate its contents). Open state is not persisted. A summary is one line at rest:
    `[pip] [title] [count] [status] [›]`, the arrow rotating 90° on open, `›` `aria-hidden`.
    `<summary>` gets `list-style: none` and `::-webkit-details-marker { display: none }` but
    **never** `tabindex="-1"` or `pointer-events` tricks, and you must **not** hand-write
    `aria-expanded` — `<details>` announces its own state. `:focus-visible` outline always present.
    Minimum interactive target 44px (16px padding + 15px line-height computes to 48px).
    **Every drawer body contains the mandatory `<x-cockpit.hint>` sentence** — with actions
    stripped, an open drawer must never be empty.

    `visit-row.blade.php` — `[52px date column] [title] [sub-line] [chips] [status]`. Rows are
    **static**: no `cursor: pointer`, no hover colour change, no `tabindex`, no `onclick`. A row
    that looks clickable and is not is worse than one that looks inert. **No pip on a visit row** —
    pips live on summaries only.

    **Reconstructed treatment (D-02) — all four, none optional:**
    1. `<x-cockpit.chip type="reconstructed">` first in the chip strip, before any `RAMS issued` /
       `Worksheet signed` tag.
    2. A 2px **dashed** left border in `--cav-inferred`, inset by `--cav-s1`. Dashed appears nowhere
       else on the page, so it reads as a qualifier rather than decoration, and it survives
       greyscale because it is a line style, not a hue.
    3. Sub-line copy **verbatim** — worksheet-derived: "Type inferred from a signed worksheet — the
       work actually done was not recorded." Survey-derived: "Built from the existing survey record,
       not captured as a visit."
    4. Worksheet-derived only: the inferred type word renders with
       `text-decoration: underline dotted currentColor; text-underline-offset: 2px` and an
       `aria-describedby` pointing at the sub-line. A survey-derived visit's type is not in doubt,
       only its provenance.
    **No colour is used for reconstruction.** Not gold (it is not actionable), not teal (it is not
    confirmed). Neutral tier.

    **Superseded treatment (D-04):**
    1. **Position unchanged** — the row stays in date order in its own type's drawer. Never hidden,
       never moved to a history group, never behind a "show superseded" toggle.
    2. `<x-cockpit.chip type="superseded">` — same geometry as reconstructed but a **solid** 1px
       `--cav-border`. A row may carry both chips, Reconstructed first.
    3. `line-through` on the **wrapped record's title only** — never the whole row. Striking the
       engineer's name and the date would make the surviving fact (someone went) harder to read,
       which is the opposite of D-04's intent.
    4. **No opacity on this row.** `#767676` at 0.7 is 3.03:1 and fails AA for the very meta text
       the row exists to preserve. The `line-through` and the chip are the recession.
    5. Sub-line verbatim: "Superseded {d M Y} — the visit still happened."
    6. Never gold, never counted as attention.

    Source unreadable (force-deleted): chip "Record unavailable" + sub-line "The visit is recorded;
    the document behind it could not be read." A broken source must **not** remove a visit from the
    spine.

    `doc-row.blade.php` — name · value rows inside document drawers. The O&M full-vs-mini `<select>`
    from the sketch renders as **static text** of the current selection, e.g. "Full O&M manual — 42
    pages". No `<select>`, no form control of any kind on this page.

    `lifecycle-row.blade.php` — the uploaded → sent → issued disc rows; discs reuse the Done and
    Waiting shapes from `<x-cockpit.pip>`.

    Fill in `cockpit.blade.php`: three section groups, nine drawers, all closed. Dates render as
    `02 Sep` in rows and `2 September 2026` in prose. British spelling. No exclamation marks. Never
    the word "Error" in user-facing copy.

    **Blade traps:** no `@php(...)` short-form, especially with `?->` or `??`; always
    whitespace-separate `@if` from adjacent output. No Tailwind colour utilities. Every class
    `cav-`-prefixed.
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com"; & "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan view:clear; & "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan test tests/Feature/Cockpit 2>&1 | Select-Object -Last 25; Get-ChildItem storage/framework/views/*.php | ForEach-Object { & "$env:USERPROFILE\.config\herd\bin\php84\php.exe" -l $_.FullName } 2>&1 | Select-String -Pattern "Errors parsing"; $o = Select-String -Path resources\views\components\cockpit\drawer.blade.php -Pattern '<details[^>]*\sopen'; if ($o) { throw "a drawer ships the open attribute - drawers are closed at rest" }; "OK"</automated>
  </verify>
  <done>Nine drawers render closed; reconstructed rows carry all four treatments; superseded rows keep position with a scoped strikethrough and no opacity; no compiled-view parse errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Spine + read-only fence tests</name>
  <files>tests/Feature/Cockpit/CockpitSpineTest.php, tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php</files>
  <behavior>
    **CockpitSpineTest** (flag on, authenticated):
    - All nine sections render, including not-required ones.
    - No `<details` tag in the response carries an `open` attribute.
    - A project with a backfilled visit shows `· reconstructed` in that drawer's summary count slot
      **without the drawer being opened** — the at-rest disclosure D-02 depends on.
    - A project with a superseded visit shows `· superseded` in the count slot, and the row is
      present in the body in date order — assert it is NOT absent and NOT last.
    - A reconstructed or superseded visit does not push its drawer to the attention state and does
      not appear in the attention line's count.
    - The programming section renders the **unticked** box and the status "Not marked". Assert the
      ticked markup is absent — in this phase no data can produce it.
    - Empty project: the nine drawers still render, all waiting, with the page empty-state copy
      "Nothing has been recorded on this job yet".
    - A visit whose source has been force-deleted renders with the "Record unavailable" chip and is
      still on the spine.

    **CockpitReadOnlyFenceTest** (flag on, authenticated):

    **SCOPE — read this before writing a single assertion.** Every fence assertion below is scoped
    to the **cockpit subtree only**, never to the whole response body. The cockpit page extends the
    app layout (Plan 45-06, deliberately), and `resources/views/layouts/app.blade.php` contains, in
    the chrome of *every* authenticated page in this application: **2 `<form>`** (including the
    logout form at `:1356`, `<form method="POST" action="{{ route('logout') }}">`), **1 `<input>`**
    (the command-palette search box), **6 `<button>`** and **6 `<script>`** (`@vite` bundles and
    Alpine). A whole-body assertion of "no `<form`" therefore **fails on its very first run**, and
    an executor meeting that failure would have to decide unsupervised how much of the fence to
    weaken — silently gutting the protection that ROADMAP criteria 3 and 5 rest on. That decision
    is made here, at plan time, instead: **the layout's nav, logout form, command-palette input and
    `@vite`/Alpine `<script>` tags are OUT OF SCOPE of this fence.** They are pre-existing global
    chrome, present identically on the eleven-tab page, owned by no part of Phase 45, and Plan 45-08
    separately asserts by sha256 that this phase did not modify the layout at all. What the fence
    exists to catch is a write affordance appearing **inside the cockpit**, and that is exactly what
    it will catch.

    **How to scope it, concretely.** Extract the cockpit region from the response before asserting.
    Either:
      (a) regex out the `<div class="cav-brand cav-cockpit"> … </div>` subtree from
          `$response->getContent()` — Plan 45-06 guarantees the page root carries both classes and
          that no other element in the app carries `cav-cockpit`; or
      (b) render the view in isolation —
          `view('projects.cockpit', [...])->render()` — which returns the cockpit markup with no
          layout chrome at all. (b) is preferred where the view's data can be built without the
          HTTP round trip; (a) is the fallback and is the form that also proves the composition.
    Whichever is used, **assert first that the extracted region is non-empty and contains the
    masthead's project name**, so a regex that silently matches nothing can never produce a
    vacuously green fence.

    Assertions, all against the extracted cockpit region:
    - The cockpit region contains **no** `<form`, `<input`, `<select`, `<textarea` or `<button`.
    - It contains none of the **fifteen** deferred affordances' copy: "Book another", "Prepare a
      visit", "Add a snag", "Book a visit", "Issue to client", "Send a RAMS", "Upload a drawing",
      "Add document", "Add anyway", "Edit details", "Re-import", "Close this project",
      "Open register", "Export CSV", "Download". (This copy check may safely run against the whole
      response body too — none of these strings appears in the layout — but scope it to the region
      for consistency.)
    - It contains **no `<script`** — Phase 45 ships zero JavaScript **of its own** on this page.
      The layout's `@vite` and Alpine tags live outside the region and are not this phase's to
      remove.
    - It contains no `onclick` and no `cursor: pointer` on a visit row.
    - The one permitted navigation affordance IS present: a link whose text is "Open the full
      project page" pointing at `route('projects.show', $project)`.
    - **The test enumerates the fence as data**, so a future phase that legitimately adds an
      affordance must delete the assertion deliberately rather than watch it silently stop covering
      anything — same anti-rot discipline as
      `LabourResourceClientSurfacePrivacyTest::test_every_enumerated_path_exists()`.
    - **VIS-06 / criterion 5 asserted directly, not by absence-of-buttons.** Snapshot the row counts
      of `visits`, `install_records`, `install_programmes`, `site_surveys` and `worksheets` before
      the request; `GET` the cockpit as an authenticated user; assert **every one of the five counts
      is identical afterwards**. Absence of form controls proves the page offers no way to write;
      this proves that *rendering it* writes nothing — which is the stronger and more literal
      reading of "this phase adds no new writes". Enumerate the five table names as a data array in
      the test so the list is inspectable and a sixth table can be added deliberately.
  </behavior>
  <action>
    Write both files extending `Tests\TestCase` with `RefreshDatabase`, flipping the flag with
    `config(['cockpit.enabled' => true])`, seeding via `Visit::factory()` states from Plan 45-02.
    One `test_snake_case` method per behavior line.

    In `CockpitReadOnlyFenceTest`, write **one private helper** —
    `private function cockpitRegion(string $html): string` — that performs the subtree extraction
    described in the SCOPE block above, `assertNotEmpty()`s the result and asserts it contains the
    masthead project name before returning it. Every fence assertion goes through that helper.
    Docblock the helper with the scope decision verbatim: the shared app layout's logout form,
    command-palette input, nav buttons and `@vite`/Alpine `<script>` tags are pre-existing global
    chrome, are out of scope of this fence by plan-time decision, and are covered instead by Plan
    45-08's sha256 assertion that `layouts/app.blade.php` is byte-identical to its pre-phase state.
    Writing that reasoning into the test file is the point: a later agent must be able to see that
    the narrow scope was chosen deliberately, not conceded when an assertion went red.

    **Do NOT add any cockpit path to
    `tests/Feature/Security/LabourResourceClientSurfacePrivacyTest.php`.** The cockpit is a
    staff-auth PM surface; that file's docblock at `:55-61` explicitly forbids adding staff-auth
    surfaces, because doing so misclassifies them as client-facing. Contact details are legitimately
    visible to the PM — that is exactly what LR-04 sanctions. CONTEXT.md carries a correction block
    on this point. Phase 46 onward adds the client-facing surfaces that do belong in that list.
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com"; & "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan test tests/Feature/Cockpit 2>&1 | Select-Object -Last 40; if ((git diff --name-only -- tests/Feature/Security/LabourResourceClientSurfacePrivacyTest.php) -ne $null) { throw "FORBIDDEN: the cockpit is a staff-auth surface and must not be added to the client-facing privacy scan" }; "OK"</automated>
  </verify>
  <done>Both test files green; the fence is enumerated as data and scoped through the `cockpitRegion()` helper, which asserts the region is non-empty; the five-table row-count invariance test passes; `LabourResourceClientSurfacePrivacyTest` is unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project data → rendered HTML | Denormalised titles, engineer names and document names reach a staff-auth page. All Blade output is escaped by default. |
| the read-only fence | The boundary between a page that reads and a page that acts. Enforced by a test, not by convention. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-07-01 | Tampering | A write affordance leaking onto a read-only page | mitigate | `CockpitReadOnlyFenceTest` asserts the absence of `<form>`, `<input>`, `<select>`, `<textarea>`, `<button>` and `<script>`, plus the copy of all fifteen deferred affordances — all scoped to the `cav-brand cav-cockpit` subtree, because the shared app layout legitimately contains a logout form, a search input, buttons and `@vite`/Alpine scripts on every authenticated page. Plan 45-08 asserts by sha256 that the layout was not modified by this phase. |
| T-45-07-02 | Information Disclosure | Unescaped output via `{!! !!}` | mitigate | No cockpit view uses `{!! !!}`. All values are model attributes rendered with `{{ }}`, escaped by Blade. |
| T-45-07-03 | Information Disclosure | A superseded or soft-deleted record's content being over-exposed | accept | The cockpit shows only what the eleven-tab page already shows to the same authenticated user; D-04 requires the visit remain visible. No new data class is surfaced. |
| T-45-07-04 | Repudiation | A qualifier silently disappearing at phone width | mitigate | The count slot is preserved at ≤480px via the two-row summary (user-signed-off deviation from the sketch). The spine test asserts the disclosure string is present in the markup regardless of viewport, since it is CSS reflow, not conditional rendering. |
| T-45-07-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
`artisan test tests/Feature/Cockpit` green. Every compiled view passes `php -l`.
`git diff` shows `ProjectHealthService`, `ProjectHealth`, `health-badge.blade.php` and
`LabourResourceClientSurfacePrivacyTest` all unmodified.
</verification>

<success_criteria>
- Nine drawers, all closed at rest, each containing its mandatory hint sentence.
- Reconstructed visits carry chip + dashed left border + verbatim sub-line + (worksheet-derived)
  dotted-underlined type word, and are disclosed in the closed summary's count slot.
- Superseded visits keep their date-order position, marked with a solid chip and a strikethrough
  scoped to the deliverable name, with no opacity.
- Programming renders the UNTICKED box only.
- The read-only fence is proven by a test, **scoped to the `cav-brand cav-cockpit` subtree**: no
  form control, no `<script>` of this phase's making, no deferred-affordance copy; one permitted
  text link back to the full project page. The layout's logout form, search input, nav buttons and
  `@vite`/Alpine tags are out of scope by recorded decision, and the extraction helper asserts the
  region is non-empty so the fence can never pass vacuously.
- Rendering the cockpit writes nothing: `visits`, `install_records`, `install_programmes`,
  `site_surveys` and `worksheets` row counts are identical before and after the request.
</success_criteria>

<output>
Create `.planning/phases/45-visit-model-read-only-cockpit/45-07-SUMMARY.md` when done
</output>
