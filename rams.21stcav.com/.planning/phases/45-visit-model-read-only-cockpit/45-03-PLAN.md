---
phase: 45-visit-model-read-only-cockpit
plan: 03
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - config/cockpit.php
  - .env.example
  - package.json
  - package-lock.json
  - resources/css/cav-tokens.css
  - resources/css/cockpit.css
  - vite.config.js
  - tests/Unit/CockpitFlagDefaultTest.php
autonomous: true
requirements: [VIS-10, VIS-05]
user_setup:
  - service: none
    why: "No external service. The only human step is flipping COCKPIT_ENABLED in .env AFTER the backfill has run — deliberately a separate deploy decision."
must_haves:
  truths:
    - "The cockpit flag exists and defaults FALSE — nothing arms itself on deploy"
    - "The 21 --cav-* tokens exist in a file of their own, declared on .cav-brand and nowhere else, so phases 46-51 can import the palette without importing the cockpit's layout"
    - "Poppins 400/600 is self-hosted, not pulled from Google Fonts"
    - "No existing page is retoned — layouts/app.blade.php, resources/css/app.css and tailwind.config.js are untouched"
  artifacts:
    - path: "config/cockpit.php"
      provides: "'enabled' => env('COCKPIT_ENABLED', false) with the defaults-FALSE doctrine documented"
      contains: "false"
    - path: "resources/css/cav-tokens.css"
      provides: "The 21 --cav-* tokens on .cav-brand. Nothing else."
      contains: "--cav-teal"
    - path: "resources/css/cockpit.css"
      provides: "@import of cav-tokens.css + @fontsource/poppins 400/600 + all .cav-cockpit layout CSS"
      contains: "cav-tokens.css"
    - path: "vite.config.js"
      provides: "resources/css/cockpit.css registered as a new input alongside rack-editor.js and spike/main.jsx"
  key_links:
    - from: "resources/css/cockpit.css"
      to: "resources/css/cav-tokens.css"
      via: "@import — the mechanism that makes D-07's token inheritance real rather than intended"
      pattern: "@import.*cav-tokens"
---

<objective>
Everything the cockpit page needs to exist before it can be written: the flag, the brand token
file, the cockpit stylesheet, the self-hosted Poppins, and the Vite wiring.

Purpose: two real preconditions are scheduled here rather than assumed. `@fontsource/poppins` is
**not installed** (verified — `node_modules/@fontsource-variable/` contains `inter` only), so
without this plan the cockpit ships in the Segoe UI fallback. And the UI-SPEC mandates **two** CSS
files, not one: a token file a brand page can import on its own, and a cockpit file that imports it.
Output: config + two stylesheets + one npm dependency + one Vite input. **No route, no controller,
no view** — Plan 45-06 owns those.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-visit-model-read-only-cockpit/45-UI-SPEC.md
@.planning/phases/45-visit-model-read-only-cockpit/45-CONTEXT.md
@config/rams_tier1.php
@vite.config.js

<interfaces>
<!-- The three verified constraints this plan is built around. -->

`--teal-*` ALREADY MEANS BLUE — resources/views/layouts/app.blade.php:65-71:
```css
--teal-700: var(--accent-700);  /* #1E5FE0 electric blue */
--teal-600: var(--accent-600);  /* #2E7BFF */
```
72 occurrences of `teal` in resources/views/surveys/show.blade.php depend on it. Likewise
`tailwind.config.js:60` maps `brand.teal` to `#2E7BFF`. `--cav-` is free (grep returns zero hits).

THE SEAM — resources/views/layouts/app.blade.php:1322 is `@stack('styles')` and :1323 is `</head>`.
The stack is the last thing in `<head>`, after the app's own `:root` at :33.

FLAG PRECEDENT — config/services.php:40-43 and routes/web.php:191-197 +
app/Http/Controllers/SpikeSchematicController.php:19-25:
```php
'spike_schematic_enabled' => env('SPIKE_SCHEMATIC_ENABLED', false),
// route registered UNCONDITIONALLY; the controller does abort_unless(config(...), 404)
```

DEFAULTS-FALSE DOCTRINE — config/rams_tier1.php:119-134 records this repo's own lesson that a gate
defaulting ON is a deploy-order trap. Read it before writing config/cockpit.php.
</interfaces>
</context>

<tasks>

<!--
  NO PACKAGE-LEGITIMACY CHECKPOINT IN THIS PLAN, and that is a recorded decision, not an omission.
  `45-RESEARCH.md` § Package Legitimacy Audit now carries a completed audit of `@fontsource/poppins`,
  performed 2026-09-19 and queried live via `npm view`. Verdict: **[VERIFIED]**, not [ASSUMED] —
  identical repository (`github.com/fontsource/font-files`) and licence (OFL-1.1) to the
  already-shipping, already-trusted `@fontsource-variable/inter`; **no `scripts`, therefore no `postinstall`**;
  **no dependencies**; font files + CSS only, self-hosted through Vite with no runtime CDN call.
  The blocking-human checkpoint that previously stood here existed solely as the fallback-policy
  substitute for a missing audit table. The table now exists, so the install proceeds autonomously.
  Cite `45-RESEARCH.md` § Package Legitimacy Audit as the authority in the SUMMARY.
  The declared fallback is UNCHANGED and still binding: if the install fails for any reason, ship on
  the `'Poppins', 'Segoe UI', Arial` chain in `--cav-font-body` — NEVER a Google Fonts `<link>`,
  which would break the self-host doctrine at `resources/css/app.css:1-6`.
-->

<task type="auto">
  <name>Task 1: config/cockpit.php + .env.example + flag-default test</name>
  <files>config/cockpit.php, .env.example, tests/Unit/CockpitFlagDefaultTest.php</files>
  <behavior>
    - `config('cockpit.enabled')` is `false` when `COCKPIT_ENABLED` is absent from the environment.
    - The flag is a single global boolean. No per-project, per-user or per-role scoping — CONTEXT.md
      names a global env flag as the default and every precedent in this app matches.
  </behavior>
  <action>
    Create `config/cockpit.php` returning `['enabled' => env('COCKPIT_ENABLED', false)]`. Above the
    key, write a block comment in the style of `config/rams_tier1.php:119-134` recording **why the
    default is false**: the cockpit renders against a `visits` table that is empty until
    `visits:backfill --apply` has been run, so arming the flag in the same deploy as the code would
    show an empty page and reproduce the "content gate defaulting ON is a deploy-order trap" failure
    this repo has already suffered. State the deploy order explicitly in the comment: ship code
    (flag absent) → `visits:backfill` dry-run → `visits:backfill --apply` → a separate one-line
    `.env` flip → `artisan config:clear`.

    Add `COCKPIT_ENABLED=false` to `.env.example` with a one-line comment pointing at
    `config/cockpit.php`. Do not edit `.env` (not in git).

    Write `tests/Unit/CockpitFlagDefaultTest.php`: one test asserting the config file's literal
    default is `false`, read via `require config_path('cockpit.php')` with `COCKPIT_ENABLED` unset —
    not via `config()`, which the test harness may have cached. This is the anti-rot guard against
    a future agent "helpfully" flipping the default.
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com"; & "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan test --filter=CockpitFlagDefaultTest 2>&1 | Select-Object -Last 20</automated>
  </verify>
  <done>`config/cockpit.php` exists, defaults false, is documented with the deploy order, and has a test pinning the default.</done>
</task>

<task type="auto">
  <name>Task 2: Install Poppins, write cav-tokens.css and cockpit.css, register the Vite input</name>
  <files>package.json, package-lock.json, resources/css/cav-tokens.css, resources/css/cockpit.css, vite.config.js</files>
  <action>
    **Install** — `npm install --save @fontsource/poppins`. This package is **[VERIFIED]** by the
    completed audit in `45-RESEARCH.md` § Package Legitimacy Audit (same repo, same licence and same
    publisher as the already-shipping `@fontsource-variable/inter`; no `scripts`, no dependencies),
    so no human gate is required. Import weights 400 and 600 only; do not import the whole family.
    If the install fails, fall back to omitting the two `@import` lines and leaving the
    `'Poppins', 'Segoe UI', Arial` chain in `--cav-font-body` — never a Google Fonts `<link>` —
    and record the fallback in the SUMMARY.

    **`resources/css/cav-tokens.css`** — the 21 `--cav-*` tokens from `45-UI-SPEC.md` § Token Set,
    declared on `.cav-brand` and **nothing else**. No layout, no component rules, no selector other
    than `.cav-brand`. Copy the values exactly from the spec's three token tables (structural,
    accent, neutral-state) plus the spacing scale `--cav-s1`..`--cav-s7`. Head the file with a
    comment stating: the `--cav-` prefix is mandatory because `--teal-*`, `--brand-*`, `--accent-*`,
    `--nav-*`, `--ink-*` and `--paper*` are all taken and all mean the blue palette
    (`layouts/app.blade.php:65-71`); and that this file exists separately so phases 46-51 can import
    the palette without importing the cockpit's layout (UI-SPEC Constraint 3).

    Honour the spec's overrides — they are not negotiable and each has a contrast-ledger row:
    `--cav-teal #01889F` is fills and marks only (4.18:1, fails AA for text); every teal **text**
    is `--cav-teal-dark #016E82`; the masthead fill is `#016E82`, not `#01889F`; the masthead
    sub-line is `#FFFFFF`, not the sketch's `#C5E6EE`; `--cav-wait` is `#767676`, not the sketch's
    `#D8D8D8`; `--cav-light #767676` is permitted on white surfaces only; the gold pip carries a
    1.5px `#8A6D13` outline.

    **`resources/css/cockpit.css`** — in order: the two `@import '@fontsource/poppins/400.css'` /
    `600.css` lines, then `@import './cav-tokens.css';`, then all `.cav-cockpit` layout CSS from
    `45-UI-SPEC.md` (§ Layout Contract, § Typography, § State Vocabulary, § Responsive Contract,
    § Accessibility Contract). Rules to obey:
    - **Every class carries a `cav-` prefix.** `.cav-drawer`, `.cav-pip`, `.cav-visit`, `.cav-title`,
      `.cav-count`, `.cav-status`, `.cav-hint`, `.cav-tag`, `.cav-chip`, `.cav-sec`, `.cav-mast`,
      `.cav-attn`, `.cav-tick`. **No single-letter class names, no bare `.btn`/`.card`/`.body`/
      `.close`** — `.btn` has 4 existing rules and `.card` 2 in `layouts/app.blade.php`.
    - **No Tailwind colour/type/spacing utilities anywhere in the cockpit.** They resolve to the
      blue palette.
    - Four states by SHAPE as well as colour: filled circle (done), rotated square = diamond with a
      1.5px `#8A6D13` outline (attention), hollow circle with a 1.5px `--cav-wait` border (waiting),
      16px rounded square (hand-ticked). Greyscale must still distinguish all four.
    - **Opacity may never be applied to an element containing small text** (UI-SPEC opacity rule —
      `#767676` at 0.6 is 2.49:1 and at 0.7 is 3.03:1, both FAIL AA). Recede a not-required drawer
      by opacity on its non-text marks only plus a declared token for its text.
    - Nothing renders below 13px.
    - `:focus-visible { outline: 2px solid var(--cav-focus); outline-offset: 2px }` on `<summary>`
      and every link. Never `outline: none` without a replacement.
    - `@media (prefers-reduced-motion: reduce)` disables the arrow's `transform .18s`, mirroring
      `resources/css/app.css:41`.
    - `≤480px`: the summary becomes a **two-row flex** (row 1 pip + title + arrow; row 2 count +
      status). Do **NOT** copy the sketch's `@media(max-width:480px){ .s,.c{display:none} }` —
      hiding the count destroys the only at-rest disclosure of a reconstructed or superseded visit.
      This deviation is user-signed-off 2026-09-19; do not "restore" the sketch rule.
    - Content column max-width 680px, centred. No multi-column layout at any width. No horizontal
      scroll at 320px.

    **`vite.config.js`** — add `'resources/css/cockpit.css'` to the `laravel({ input: [...] })`
    array, after `'resources/js/spike/main.jsx'`, with a comment matching the existing entries'
    style: per-page bundle, loaded only on the cockpit route, flag-gated behind `COCKPIT_ENABLED`.
    Do not add `cav-tokens.css` as an input — it is imported, not entry-loaded.

    **Touch nothing else.** `resources/views/layouts/app.blade.php`, `resources/css/app.css` and
    `tailwind.config.js` must end this plan byte-identical to how they started. Plan 45-08 asserts
    exactly that.
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com"; $base=(Select-String -Path .planning/phases/45-visit-model-read-only-cockpit/45-BASELINE.md -Pattern '[0-9a-f]{7,40}' | Select-Object -First 1).Matches.Value; if (-not $base) { throw "Cannot read the phase-start SHA from 45-BASELINE.md - the forbidden-file guard cannot be trusted without it" }; npm run build 2>&1 | Select-Object -Last 25; if ((git diff --name-only $base -- resources/views/layouts/app.blade.php resources/css/app.css tailwind.config.js) -ne $null) { throw "FORBIDDEN: an existing-page style file was modified" }; $t = (Select-String -Path resources\css\cav-tokens.css -Pattern '--cav-[a-z0-9-]+\s*:').Count; if ($t -lt 21) { throw "cav-tokens.css declares $t tokens, expected at least 21" }; "OK - build clean, no existing style file touched, $t tokens"</automated>
  </verify>
  <done>`npm run build` succeeds with `cockpit.css` emitted; `cav-tokens.css` holds the 21 tokens on `.cav-brand` only; `layouts/app.blade.php`, `app.css` and `tailwind.config.js` are unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | One new dependency enters the build. It ships font files into the public bundle. |
| `.env` → application config | `COCKPIT_ENABLED` decides whether a whole surface exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03-01 | Elevation of Privilege | `COCKPIT_ENABLED` defaulting true | mitigate | Default is `false` in `config/cockpit.php`, and `CockpitFlagDefaultTest` reads the file's literal default so a later flip is caught by CI, not by a user finding an empty page in production. |
| T-45-03-02 | Tampering | Retoning the whole app by editing the layout's `:root` | mitigate | Tokens are declared on `.cav-brand`, never `:root`. Task 2's verify gate fails the plan if `layouts/app.blade.php`, `app.css` or `tailwind.config.js` shows in `git diff`. |
| T-45-03-SC | Tampering | npm installs (`@fontsource/poppins`) | mitigate | `45-RESEARCH.md` § Package Legitimacy Audit carries a completed audit performed 2026-09-19 via live `npm view`. Verdict **[VERIFIED]**: identical repository (`github.com/fontsource/font-files`) and licence (OFL-1.1) to the already-trusted, already-shipping `@fontsource-variable/inter`; **no `scripts`, so no `postinstall` — the primary npm supply-chain vector is absent**; no dependencies; font files + CSS only, self-hosted via Vite with no runtime CDN call. A human gate would add no information the audit does not already hold, so this plan is autonomous. Fallback on install failure: the declared `'Poppins', 'Segoe UI', Arial` chain — NOT a Google Fonts `<link>`. |
</threat_model>

<verification>
`npm run build` emits a `cockpit` CSS asset. `artisan test --filter=CockpitFlagDefaultTest` green.
`git diff --name-only` shows no change to `resources/views/layouts/app.blade.php`,
`resources/css/app.css` or `tailwind.config.js`.
</verification>

<success_criteria>
- `config/cockpit.php` exists, defaults `false`, documents the deploy order, and is pinned by a test.
- `resources/css/cav-tokens.css` holds the 21 `--cav-*` tokens on `.cav-brand` and nothing else.
- `resources/css/cockpit.css` imports the tokens and Poppins 400/600 and holds the `.cav-cockpit`
  layout, with every class `cav-`-prefixed and no Tailwind colour utility.
- Vite builds it as a per-page bundle. No existing page is retoned.
</success_criteria>

<output>
Create `.planning/phases/45-visit-model-read-only-cockpit/45-03-SUMMARY.md` when done
</output>
