---
phase: 45-visit-model-read-only-cockpit
plan: 04
type: execute
wave: 2
depends_on: [45-01, 45-02]
files_modified:
  - database/migrations/2026_09_19_150000_create_install_records_table.php
  - database/migrations/2026_09_19_150100_add_install_record_id_to_install_programmes.php
  - database/migrations/2026_09_19_150200_add_install_record_fk_to_visits.php
  - app/Models/InstallRecord.php
  - app/Models/InstallProgramme.php
  - app/Models/Project.php
  - database/factories/InstallRecordFactory.php
  - app/Services/InstallProgrammeService.php
  - app/Console/Commands/BackfillInstallRecordsCommand.php
  - tests/Unit/Models/InstallRecordTest.php
  - tests/Feature/InstallProgramme/DurableInstallRecordTest.php
  - tests/Feature/Console/BackfillInstallRecordsCommandTest.php
autonomous: true
requirements: [VIS-09]
must_haves:
  truths:
    - "Regenerating an install programme creates a new install_programmes row under the SAME unchanged durable install_record — a visit filed against the record is never orphaned"
    - "The existing generate / activate / archive behaviour is unchanged, proven by re-running the measured 45-BASELINE subset with zero failures"
    - "install_tasks did not move. No existing FK, unique constraint, cascade rule, index or softDeletes() was altered"
  artifacts:
    - path: "database/migrations/2026_09_19_150000_create_install_records_table.php"
      provides: "install_records — one durable row per project, never archived, never replaced"
    - path: "database/migrations/2026_09_19_150100_add_install_record_id_to_install_programmes.php"
      provides: "NULLABLE install_record_id FK on install_programmes — nullable is mandatory or InstallProgrammeFactory breaks and cascades through the whole programme suite"
      contains: "nullable"
    - path: "app/Models/InstallRecord.php"
      provides: "InstallRecord model — project(), programmes(), activeProgramme(), visits()"
    - path: "app/Console/Commands/BackfillInstallRecordsCommand.php"
      provides: "install-records:backfill — idempotent, dry-run by default, links existing programme rows to one record per project"
  key_links:
    - from: "app/Services/InstallProgrammeService.php"
      to: "app/Models/InstallRecord.php"
      via: "ONE resolve-or-create step inside createForProject(), placed AFTER archiveExisting()"
      pattern: "InstallRecord::firstOrCreate"
    - from: "app/Models/Visit.php"
      to: "app/Models/InstallRecord.php"
      via: "nullable install_record_id — a visit's mandatory parent is the project, not the record"
      pattern: "install_record_id"
---

<objective>
The D-06 split, in the only shape that is achievable behaviour-preservingly: a new durable parent
table **above** `install_programmes`, with a **nullable** FK on `install_programmes`.
`install_tasks` do **not** move.

`InstallProgrammeService::createForProject()` (`:45-47`) calls `archiveExisting($project)` then
`InstallProgramme::create([...])` — every regenerate produces a brand-new row, so anything filed
under it is orphaned the first time a PM rebuilds the task list. `install_records` is the row that
does not change.

Purpose: this is the risk in the phase. D-06's own constraint is that the split must be
behaviour-preserving, and that "if the split cannot be made behaviour-preserving, **stop and raise
it** rather than relaxing criterion 4". The gate is the pass count measured in Plan 45-01.
Output: one CREATE TABLE, two nullable ADD COLUMN FKs, one model, one added step in one service
method, one idempotent link command, and a test that proves durability across a regenerate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-visit-model-read-only-cockpit/45-RESEARCH.md
@.planning/phases/45-visit-model-read-only-cockpit/45-CONTEXT.md
@.planning/phases/45-visit-model-read-only-cockpit/45-BASELINE.md
@app/Services/InstallProgrammeService.php
@app/Models/InstallProgramme.php

<interfaces>
<!-- The exact code being changed, and the constraints around it. -->

app/Services/InstallProgrammeService.php:45-66 — the ONLY writer of install_programmes rows:
```php
public function createForProject(Project $project, User $user): InstallProgramme
{
    $this->archiveExisting($project);

    $programme = InstallProgramme::create([
        'project_id'   => $project->id,
        'generated_by' => $user->id,
        'status'       => InstallProgramme::STATUS_DRAFT,
        'generated_at' => now(),
    ]);

    $programme->load('project');
    $this->generator->generate($programme);
    // Log::info(...) then return $programme;
}
```
`archiveExisting()` (:101-120) is status-change-only and has NO external caller besides
createForProject() and one unit test. It is NOT edited by this plan.

app/Models/Project.php:363-372:
```php
public function installProgrammes(): HasMany;      // ordered created_at desc
public function activeInstallProgramme(): HasOne;  // where status=active, latestOfMany()
```
Project.php:433-441 also has snaggingSignoffs(): HasManyThrough hopping
Project → InstallProgramme → CommissioningSignoff. Do not disturb it.

THE CONSTRAINTS THAT SHAPE THIS (all verified in 45-RESEARCH.md §6):
- install_tasks.install_programme_id — cascadeOnDelete. Never hard-delete a programme row.
- install_task_photos.install_task_id — cascadeOnDelete. Two-hop cascade.
- commissioning_items.install_programme_id — cascadeOnDelete; .install_task_id — nullOnDelete.
- commissioning_signoffs.install_programme_id — UNIQUE + cascadeOnDelete. The hardest constraint.
  Moving signoffs to the durable record would change this key's meaning from "one per generation"
  to "one per project", which SignoffRaceTest and SignoffTransactionTest assert against.
- 4 factories chain off InstallProgramme::factory() (InstallTaskFactory:27,
  CommissioningItemFactory:39, CommissioningSignoffFactory:25). A NOT NULL FK detonates all of them.
- 13 live routes bind {programme} to InstallProgramme by id. Nothing may change what that resolves to.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: install_records table, model, factory, and the two nullable FKs</name>
  <files>database/migrations/2026_09_19_150000_create_install_records_table.php, database/migrations/2026_09_19_150100_add_install_record_id_to_install_programmes.php, database/migrations/2026_09_19_150200_add_install_record_fk_to_visits.php, app/Models/InstallRecord.php, app/Models/InstallProgramme.php, app/Models/Project.php, database/factories/InstallRecordFactory.php, tests/Unit/Models/InstallRecordTest.php</files>
  <behavior>
    - `install_records` has one row per project: `project_id` FK to `projects` with
      `nullOnDelete()` (matching `install_programmes.project_id`'s existing rule — a programme
      legally survives its project with a NULL `project_id`, and the durable record must tolerate
      the same), plus a **unique index on `project_id`** so "one durable record per project" is a
      database fact, not a convention. Timestamps. **No `softDeletes()`** — the record is durable by
      definition; nothing archives or deletes it.
    - `install_programmes.install_record_id` is **NULLABLE**, `constrained('install_records')`,
      `nullOnDelete()`, indexed. Nullable is mandatory: `InstallProgrammeFactory:15-37` does not set
      it, and a NOT NULL column would fail every test that builds a programme directly or
      transitively through `InstallTaskFactory`, `CommissioningItemFactory` or
      `CommissioningSignoffFactory`.
    - `visits.install_record_id` (the column Plan 45-02 created unconstrained) gains its FK here,
      **nullable**, `nullOnDelete()`. A survey visit routinely predates any install record existing.
    - `InstallRecord::programmes(): HasMany` returns every generation, newest first.
      `activeProgramme()` returns the one with `status = active` or null. `visits(): HasMany`.
      `project(): BelongsTo`.
    - `InstallProgramme::installRecord(): BelongsTo` is added — a new relation only. **No existing
      method, constant, cast, `$fillable` entry or relation on `InstallProgramme` is modified.**
    - `Project::installRecord(): HasOne` is added. `installProgrammes()`, `activeInstallProgramme()`
      and `snaggingSignoffs()` are left byte-identical.
    - `install_record_id` is added to `InstallProgramme::$fillable` so `createForProject()` can set
      it in the existing single `create([...])` call rather than needing a second write.
  </behavior>
  <action>
    Write the three migrations in the repo's anonymous-class style. Docblock the
    `install_records` migration with the D-06 rationale and, explicitly, the two things it does NOT
    do: it does not move `install_tasks`, and it does not touch
    `commissioning_signoffs.install_programme_id`'s UNIQUE constraint. State that re-pointing either
    is Phase 51's work per `.planning/ROADMAP.md:295-297, 313`.

    Docblock the `install_programmes` migration with the nullable rationale verbatim: a NOT NULL FK
    breaks `InstallProgrammeFactory` and cascades through the whole programme suite; existing rows
    are linked by `install-records:backfill` (Task 3), not by this migration.

    Write `app/Models/InstallRecord.php` and `database/factories/InstallRecordFactory.php` following
    `app/Models/LabourResource.php` / `LabourResourceFactory`'s shape. Add exactly one relation
    method to each of `InstallProgramme` and `Project`, placed with the other relations, with a
    one-line docblock. PSR-12, 4-space indent, `// -- Label --` dividers.

    Write `tests/Unit/Models/InstallRecordTest.php`: one record per project enforced by the unique
    index (assert a second insert throws `QueryException`); `programmes()` returns multiple
    generations; deleting a project nulls `project_id` rather than deleting the record.

    **Do not edit `InstallProgrammeService` in this task** — Task 2 owns it, so a failure in Task 3's
    gate can be attributed to exactly one change.
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com"; & "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan migrate:fresh --env=testing --force 2>&1 | Select-Object -Last 10; & "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan test --filter=InstallRecordTest 2>&1 | Select-Object -Last 20</automated>
  </verify>
  <done>Three migrations run clean; `InstallRecord` exists; `install_programmes.install_record_id` and `visits.install_record_id` are both nullable; `InstallRecordTest` green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One resolve-or-create step in createForProject(), plus the durability proof</name>
  <files>app/Services/InstallProgrammeService.php, tests/Feature/InstallProgramme/DurableInstallRecordTest.php</files>
  <behavior>
    - **The only genuinely new assertion the split needs:** generate a programme, regenerate it, and
      assert that the two `install_programmes` rows have **different ids** but the **same
      `install_record_id`**, that the `install_records` row's `id` and `created_at` are unchanged
      across both, and that a `Visit` created against that record still resolves after the
      regenerate. That is D-06 satisfied, stated as an executable fact.
    - `archiveExisting()` still runs **first**. The existing assertion
      `archiveExisting_archives_all_draft_and_active_programmes_for_project`
      (`tests/Unit/InstallTaskGeneratorServiceTest.php:346`) depends on that order.
    - Regenerating still archives the previous programme, still returns a `draft`, still generates
      tasks via `InstallTaskGeneratorService`, and `InstallTaskObserver` still fires per task save.
      Timing and ordering of commissioning-item generation are unchanged.
    - A project with no prior programme gets a record created on first generate; a project with one
      reuses it. Concurrent generates cannot create two records — the unique index on `project_id`
      guarantees it, and `firstOrCreate` is the matching idiom.
  </behavior>
  <action>
    Edit `createForProject()` and **nothing else in the file**. Insert exactly one step between
    `$this->archiveExisting($project);` and the `InstallProgramme::create([...])` call:

    resolve-or-create the project's durable record with `InstallRecord::firstOrCreate(['project_id'
    => $project->id])`, then add `'install_record_id' => $record->id` to the **existing**
    `create([...])` array. Do not add a second write, do not reorder the existing statements, do not
    change the `Log::info` payload's existing keys (adding `install_record_id` to it is fine and
    useful). Above the new line, a comment citing D-06 and stating that this record is deliberately
    never archived — `archiveExisting()` operates on programmes only.

    **`archiveExisting()` must end this plan with a zero-line diff.** Confirm with
    `git diff -U0 app/Services/InstallProgrammeService.php` and record the diff line count in the
    SUMMARY — the research doc names this as hard gate 3.

    Write `tests/Feature/InstallProgramme/DurableInstallRecordTest.php` with one
    `test_snake_case` method per behavior line above.
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com"; & "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan test tests/Feature/InstallProgramme/DurableInstallRecordTest.php tests/Unit/InstallTaskGeneratorServiceTest.php 2>&1 | Select-Object -Last 30</automated>
  </verify>
  <done>Durability test green; `InstallTaskGeneratorServiceTest` still fully green; `archiveExisting()` shows zero changed lines in the diff.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: install-records:backfill command + RE-ASSERT the measured baseline</name>
  <files>app/Console/Commands/BackfillInstallRecordsCommand.php, tests/Feature/Console/BackfillInstallRecordsCommandTest.php</files>
  <behavior>
    - `install-records:backfill {project?} {--apply}`. **Dry-run by default**, per the
      `BackfillCablePortFksCommand` precedent (`:20-23, 99-105`).
    - For each distinct `project_id` in `install_programmes`, resolve-or-create one
      `install_records` row and stamp `install_record_id` on every programme row for that project —
      archived ones included. History belongs to the same durable record.
    - **Programmes with `project_id IS NULL` are handled explicitly and get no record** (legal per
      the `nullOnDelete` FK). They are reported in their own outcome category, not silently skipped.
    - Idempotent: a programme that already has a non-null `install_record_id` is counted
      `already-linked` and skipped **before** any work, never re-checked or rewritten.
    - Per-row outcome categories and a counter summary, one `Log::info('install-records:backfill
      completed', [...])` at the end, writes wrapped in `DB::transaction()` and only when `--apply`.
    - The command **never** hard-deletes anything and never calls `save()` on an `install_tasks`,
      `commissioning_items` or `commissioning_signoffs` row.
    - Running it twice with `--apply` produces identical `install_records` row counts and zero
      additional writes on the second run — assert both.
  </behavior>
  <action>
    Copy the structure of `app/Console/Commands/BackfillCablePortFksCommand.php` wholesale:
    signature with an optional positional project arg + `--apply`, description ending "Idempotent
    and dry-run by default.", `(int)` cast on the arg with the SQL-injection note, empty-set early
    exit, `$summary` counter array, per-row `$this->line(sprintf('  #%d — %s: %s', ...))`,
    transaction-wrapped writes, summary block, structured log, `return self::SUCCESS`.

    Outcome categories: `linked` / `already-linked` / `orphan-no-project` / `wrote`.

    Write `tests/Feature/Console/BackfillInstallRecordsCommandTest.php` using
    `$this->artisan('install-records:backfill')` — assert the dry run writes nothing, `--apply`
    links, a second `--apply` is a no-op, and a `project_id IS NULL` programme lands in
    `orphan-no-project`.

    **Then re-assert the D-06 gate.** Run the exact command recorded in `45-BASELINE.md` and compare
    the `Tests:` line against the number measured in Plan 45-01. Record the comparison in the
    SUMMARY as "baseline N → after split N". **If the pass count drops or any new failure appears,
    STOP.** Do not adjust the test, do not relax criterion 4, do not narrow the gate — report it
    to the user as the D-06 stop condition CONTEXT.md asked to be raised.
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com"; & "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan test tests/Feature/Console/BackfillInstallRecordsCommandTest.php 2>&1 | Select-Object -Last 20; & "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan test tests/Unit/InstallTaskGeneratorServiceTest.php tests/Feature/InstallTasks tests/Feature/FieldView tests/Feature/Commissioning tests/Unit/Services/CommissioningItemGeneratorTest.php tests/Unit/Services/CommissioningPdfServiceTest.php tests/Unit/Services/CommissioningServiceTest.php tests/Unit/Services/CommissioningSyncServiceTest.php tests/Unit/Models/CommissioningItemTest.php tests/Unit/Models/CommissioningSignoffTest.php tests/Feature/Projects/ProjectDeliverableAutoFlipTest.php tests/Feature/Authorization/SharedWorkspaceFieldOpsAccessTest.php 2>&1 | Select-Object -Last 15</automated>
  </verify>
  <done>Backfill command tests green AND the 45-BASELINE subset re-runs with the same-or-greater pass count and zero failures. The SUMMARY records both numbers side by side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| console arg → SQL | `install-records:backfill {project?}` takes an operator-supplied id. Cast to `(int)` and parameter-bound, per the `BackfillCablePortFksCommand:53-58, 95-97` precedent. |
| migration → live production data | Two ADD COLUMN migrations and one CREATE TABLE run against a live database holding real programmes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04-01 | Denial of Service | A NOT NULL FK on `install_programmes` | mitigate | The FK is nullable, and Task 1's verify runs `migrate:fresh` plus the factory-dependent suite. The failure mode (hundreds of unrelated tests failing on one constraint) is exactly what the baseline re-run in Task 3 detects. |
| T-45-04-02 | Tampering | Data loss via cascade | mitigate | No hard-delete anywhere in this plan. `install_records.project_id` is `nullOnDelete`, both new FKs are `nullOnDelete`, and `archiveExisting()` — which is status-change-only — is not edited. Four cascadeOnDelete chains hang off `install_programmes`; none is touched. |
| T-45-04-03 | Tampering | `commissioning_signoffs.install_programme_id` UNIQUE semantics | mitigate | Signoffs are not re-parented. The constraint keeps meaning "one per generation", which `SignoffRaceTest` and `SignoffTransactionTest` assert; both are inside the re-asserted baseline subset. |
| T-45-04-04 | Repudiation | A silent behaviour regression | mitigate | The gate is a measured pass count from Plan 45-01, re-run verbatim in Task 3 and recorded side by side in the SUMMARY. A drop is a declared STOP, not a judgement call. |
| T-45-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
`migrate:fresh --env=testing` clean. `InstallRecordTest`, `DurableInstallRecordTest` and
`BackfillInstallRecordsCommandTest` green. The `45-BASELINE.md` subset re-runs at the same-or-greater
pass count with zero failures. `git diff` shows `install_tasks`-related migrations, all four
factories, and `archiveExisting()` unchanged.
</verification>

<success_criteria>
- `install_records` exists, one row per project, unique on `project_id`, no soft-deletes.
- `install_programmes.install_record_id` and `visits.install_record_id` are both nullable FKs.
- `createForProject()` gained exactly one resolve-or-create step, after `archiveExisting()`.
- A regenerate produces a new programme row under the SAME unchanged record, proven by a test.
- `install_tasks` did not move. No existing FK, unique constraint, cascade, index or `softDeletes()`
  was altered.
- The measured behaviour-preservation baseline is re-asserted and recorded.
</success_criteria>

<output>
Create `.planning/phases/45-visit-model-read-only-cockpit/45-04-SUMMARY.md` when done
</output>
