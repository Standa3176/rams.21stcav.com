---
phase: 45-visit-model-read-only-cockpit
plan: 14
type: execute
wave: 6
depends_on: [45-13]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/45-visit-model-read-only-cockpit/45-UI-SPEC.md
autonomous: false
requirements: [VIS-04, VIS-05, VIS-10]
must_haves:
  truths:
    - "A human has seen the replacement page and confirmed it is the accepted design"
    - "The page is legible in greyscale and usable at 320px — state is never colour alone"
    - "The superseded UI-SPEC no longer sits in the phase folder pretending to be current"
    - "The roadmap records the replacement, not just the original eight plans"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 45 plan list extended to 45-09..45-14 with the redesign recorded"
    - path: ".planning/phases/45-visit-model-read-only-cockpit/45-UI-SPEC.md"
      provides: "superseded-marked, or removed in favour of the v1-superseded copy"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/sketches/004-delivery-cockpit/README.md"
      via: "the phase entry names sketch 004 as the design contract"
      pattern: "004-delivery-cockpit"
---

<objective>
Verify the replacement with a human, then tidy the record: mark the superseded spec, extend the
roadmap's Phase 45 entry to cover plans 45-09..45-14, and close out the requirement traceability.

Purpose: the page this phase replaced is deployed and live behind `COCKPIT_ENABLED`, which is
currently true on the server. Nothing mechanical can confirm that what a PM now opens is the design
they accepted. This is the phase's second and final human checkpoint.
Output: a verified page and a truthful planning record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/sketches/004-delivery-cockpit/README.md
@.planning/sketches/004-delivery-cockpit/delivery-cockpit.png
@.planning/phases/45-visit-model-read-only-cockpit/45-13-SUMMARY.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The Phase 45 cockpit has been rebuilt to sketch 004: the app's own blue and Inter inside the
    existing left nav, a masthead, three KPI cards, one stage chip, the module list with status
    chips and counts, and a right-hand side panel with Overview / Files / Notes tabs, a progress
    ring and a Recent activity feed. The panel is driven entirely by the URL — no JavaScript. The
    old teal accordion page is gone.
  </what-built>
  <how-to-verify>
    Run the dev server and open a project that has a survey, a signed worksheet and at least one
    RAMS document.

    1. `COCKPIT_ENABLED=true` in `.env`, then
       `& "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan config:clear`.
       Visit `/projects/{id}/cockpit`.
       EXPECT: the left nav is present and unchanged. The page is blue and Inter — no teal, no
       Verdana, no Poppins. No panel is open.
    2. Click "Open drawer" on **First fix and install**.
       EXPECT: the panel opens on the right; the module list stays visible behind it; the URL now
       carries `?module=...`. Overview shows the progress ring and the visit rows.
    3. Click the **Files** tab, then **Notes**.
       EXPECT: the URL gains `&tab=files` / `&tab=notes`; the panel stays open on the same module.
       Files lists the documents the project actually holds, each openable where a view route
       exists.
    4. Press the browser BACK button twice.
       EXPECT: you return to Files, then to Overview. Copy the `?module=...&tab=files` URL into a
       new tab — EXPECT it opens straight to that state.
    5. Close the panel with its close control. EXPECT: back to the bare URL, panel gone.
    6. **Greyscale.** Apply a greyscale filter (DevTools → Rendering → Emulate vision deficiencies
       → Achromatopsia). EXPECT: every module's state is still readable — the chips read
       "Not started" / "In progress" / "On file" in words, not by colour alone.
    7. **320px.** Set the viewport to 320px wide. EXPECT: no horizontal scrolling of the page; the
       panel stacks below the module list rather than squeezing it; every KPI card is still
       readable.
    8. Set `COCKPIT_ENABLED=false`, `config:clear`, reload. EXPECT: 404. Then open the normal
       eleven-tab project page and the dashboard — EXPECT both look exactly as they did before this
       phase.
    9. Compare side by side with `.planning/sketches/004-delivery-cockpit/delivery-cockpit.png`.
       EXPECT these deliberate differences, and no others:
         - no "Actions" dropdown (Phase 46/48 writes)
         - no Quick actions tiles — Create visit / Add note / Upload files (D-15, Phases 46 and 48)
         - no "..." overflow control on activity rows (a menu trigger)
         - no site contact EMAIL (no such field exists — only name and phone)
         - no "Proposed install date" label (no such field; a planned start may show under its own
           name)
         - ONE stage chip, not two (a project has exactly one status)
         - Programming shows a chip with no count (no Programming model exists)
  </how-to-verify>
  <resume-signal>Type "approved", or describe what does not match</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Mark the superseded spec and update the planning record</name>
  <files>.planning/phases/45-visit-model-read-only-cockpit/45-UI-SPEC.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
    `45-UI-SPEC.md` and `45-UI-SPEC-v1-superseded.md` are currently byte-identical copies of the
    same 43KB document, and only one of them is labelled. Delete `45-UI-SPEC.md`, leaving the
    explicitly-named superseded copy. A later agent reaching for "the UI spec" must not find an
    unlabelled document describing a teal accordion that no longer exists. Note in the summary that
    the superseded copy is retained deliberately: its constraint analysis — token scoping, the
    `.btn`/`.card` collision hazards, the layout's internals — is still factually correct and is
    cited by the new plans.

    In `.planning/ROADMAP.md`, extend the Phase 45 entry: tick `45-08`, then add the six new plans
    with their waves and one-line objectives. Above the plan list, add a short paragraph recording
    that on 2026-09-20 the user supplied and accepted a new design (sketch 004), that it supersedes
    sketch 002 and reverses D-07, and that plans 45-09..45-14 replace the page 45-06 and 45-07
    built. Do not rewrite the original eight entries — they are executed history and the roadmap is
    the record of what happened, not a tidy fiction about what was always intended.

    Confirm the Phase 45 success criteria still hold as written. Criterion 3 says the cockpit shows
    "the programme spine and its section drawers closed at rest". The spine is now a module list and
    the drawer is a side panel, but the property criterion 3 protects — read-only, flag-gated, its
    own route, nothing open until the PM opens it — is intact. Add a dated parenthetical to that
    criterion naming D-09 as the change in form, rather than editing the criterion's substance.

    In `.planning/REQUIREMENTS.md`, update the traceability table rows for VIS-04, VIS-06 and
    VIS-10 to name the new plans alongside the old ones.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Set-Location 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; if (Test-Path .planning/phases/45-visit-model-read-only-cockpit/45-UI-SPEC.md) { Write-Output 'FAIL unlabelled superseded spec still present'; exit 1 }; $r = Get-Content .planning/ROADMAP.md -Raw; foreach ($p in '45-09','45-10','45-11','45-12','45-13','45-14') { if ($r -notmatch $p) { Write-Output ('FAIL roadmap missing ' + $p); exit 1 } }; if ($r -notmatch '004-delivery-cockpit') { Write-Output 'FAIL roadmap does not cite the design contract'; exit 1 }; Write-Output 'PASS'"</automated>
  </verify>
  <done>Only the explicitly-superseded UI spec remains; the roadmap lists 45-09..45-14 with waves and cites sketch 004; VIS-04/06/10 traceability names the new plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning record -> future agents | A stale document in the phase folder is read as current and acted on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-14-01 | Spoofing | 45-UI-SPEC.md | mitigate | The unlabelled copy is deleted so no agent can mistake the teal accordion spec for current guidance. |
| T-45-14-02 | Information disclosure | live flag state | mitigate | `COCKPIT_ENABLED` is true on the server, so the replacement is visible to real users the moment it deploys. The human checkpoint is blocking and precedes the record update for exactly that reason. |
| T-45-14-03 | Repudiation | ROADMAP history | mitigate | The original eight entries are preserved; the redesign is recorded as an addition with its date and cause. |
| T-45-14-SC | Tampering | npm/composer installs | accept | No package-manager task in this plan. |
</threat_model>

<verification>
- The human checkpoint returned "approved".
- `45-UI-SPEC.md` is gone; `45-UI-SPEC-v1-superseded.md` remains.
- ROADMAP Phase 45 lists 45-01..45-14, all ticked, and cites sketch 004.
- `artisan test tests/Feature/Cockpit tests/Unit/Cockpit` still green after the doc edits (they
  touch no code, so they must be).
</verification>

<success_criteria>
- A human has confirmed the page matches the accepted design, with only the deliberate omissions.
- Greyscale and 320px both pass.
- Flag-off still 404s and leaves the rest of the app unchanged.
- The planning record tells the truth about what was built, replaced and why.
</success_criteria>

<output>
Create `.planning/phases/45-visit-model-read-only-cockpit/45-14-SUMMARY.md` when done.
</output>
