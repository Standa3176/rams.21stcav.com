---
phase: 45-visit-model-read-only-cockpit
plan: 13
type: execute
wave: 5
depends_on: [45-12]
files_modified:
  - tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php
  - tests/Feature/Cockpit/CockpitSpineTest.php
  - tests/Feature/Cockpit/CockpitPageTest.php
  - tests/Feature/Cockpit/FlagOffBehaviourUnchangedTest.php
autonomous: true
requirements: [VIS-03, VIS-05, VIS-06, VIS-08, VIS-09]
must_haves:
  truths:
    - "The read-only fence is stronger after the redesign than before it, never weaker"
    - "Every assertion retired here is replaced by one protecting the same real property in the new layout"
    - "The D-02 reconstructed and D-04 superseded treatments are still proven, now in the panel and the row count phrase"
    - "The flag-off proof and its three sha256 locks survive intact"
    - "The measured D-06 baseline still passes at >= 159 passed and 0 failed"
  artifacts:
    - path: "tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php"
      provides: "the fence, extended — handler-attribute ban, 18 deferred affordances, panel-open invariance"
      contains: "DEFERRED_AFFORDANCES"
    - path: "tests/Feature/Cockpit/CockpitSpineTest.php"
      provides: "the D-02/D-04 treatments re-proved against module rows and the panel"
  key_links:
    - from: "tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php"
      to: "resources/views/projects/cockpit.blade.php"
      via: "cockpitRegion() DOMXPath extraction, both brackets retained"
      pattern: "cav-cockpit"
---

<objective>
Reconcile the four existing cockpit test files with the rebuilt page: extend the fence, migrate
every assertion whose subject survived the redesign, and retire — explicitly, one at a time, with a
reason — only those whose subject no longer exists. Then re-run the measured D-06 baseline.

Purpose: a redesign is the moment guards quietly rot. Several assertions in `CockpitPageTest` and
`CockpitSpineTest` describe an accordion that no longer exists, and the tempting move is to delete
them. Most of them were not protecting the accordion; they were protecting a property the accordion
happened to carry. This plan separates the two.
Output: four reconciled test files and a re-run gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-visit-model-read-only-cockpit/45-BASELINE.md
@.planning/phases/45-visit-model-read-only-cockpit/45-11-SUMMARY.md
@.planning/phases/45-visit-model-read-only-cockpit/45-12-SUMMARY.md
@tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php
@tests/Feature/Cockpit/CockpitSpineTest.php

<interfaces>
## The per-test disposition, decided at plan time. Follow it; do not improvise.

### CockpitPageTest (22 tests)
KEEP UNCHANGED — route name resolves with flag off; flag-off 404 emits no cockpit markup;
unauthenticated 403; authenticated renders the project name; health relations eager-loaded before
assess (the probe); controller is GET-only; no write route exists; page root carries both scope
classes and loads cockpit.css; heading order is one h1 then h2s; region has no form control and no
script; no deferred write copy; components carry no raw hex; chip renders its variants; tag and
hint render.

REWRITE (same subject, new markup) —
  * masthead renders name and sub-line -> the new masthead: h1 ref, site address, and the contact
    line present ONLY when a survey supplied it.
  * health failure degrades only the attention line -> health failure degrades only the Overall
    status KPI card; the module list and the other two cards still render.
  * the two attention-line copy tests -> the Overall status card's copy, including its
    "could not be read" state. The property protected is that a missing summary degrades one
    element rather than blanking the page; that property is unchanged.
  * the three section-group headings render in order -> the module rows render, complete and in
    D-11's order, counted against `CockpitModulePresenter::modules()` rather than a literal.
  * footer note and the single navigation link -> the new "Open full project" link.

RETIRE, WITH THE PROPERTY REHOMED —
  * pip renders all three states with shape and accessible name -> RETIRED because the pip
    component is deleted. Its real subject — state is carried by shape and accessible name, not
    colour alone, so it survives greyscale — moves to a new `status-chip` test covering
    not-started / in-progress / on-file.
  * tick box renders unticked and is a square -> RETIRED because the component is deleted and the
    new design has no tick box. Its real subject — nothing claims a completion this phase cannot
    evidence — is rehomed as an assertion that the Programming module renders no count phrase.

### CockpitSpineTest (15 tests)
This file's name no longer describes anything; rename its class docblock, not the file, so git
history stays attached.

KEEP, RETARGETED AT THE MODULE ROW OR THE PANEL —
  * all nine sections render / an empty project still renders them -> module rows, counted against
    the presenter.
  * a not-required section still renders -> a not-required module still renders its row.
  * the four reconstructed treatments; the survey-derived variant and its absent dotted type word;
    superseded keeps its place in date order; strikethrough scoped to the name with no opacity;
    both chips with reconstructed first; a qualifier never drives a row to attention; a
    force-deleted source still renders its visit -> all of these now render inside the panel's
    Overview tab via the retained `visit-row` component. Same assertions, opened with
    `?module={key}`.
  * **the closed-summary disclosure tests are the important ones.** They protected this: a PM who
    opens nothing still learns that a visit is reconstructed or superseded. The accordion summary
    was merely where that lived. Retarget them at the MODULE ROW'S COUNT PHRASE, which is the new
    at-rest slot. If the count phrase does not disclose reconstructed and superseded counts, that
    is a defect in Plan 45-10's output to be fixed there — not an assertion to drop.

RETIRE, MARKUP-ONLY —
  * no drawer ships the `open` attribute -> RETIRED: there is no `<details>` on the page. Replaced
    by "no panel element exists in the DOM when no `?module=` is present", which is the new form of
    closed-at-rest.
  * programming renders the unticked box and "Not marked" -> RETIRED with the tick box; replaced as
    described above.

### CockpitReadOnlyFenceTest (7 tests) — EXTEND ONLY, NEVER WEAKEN
Keep all seven. Keep `cockpitRegion()` and BOTH brackets. The only permitted change to the helper
is the bottom bracket string, which moves to the new "Open full project" link copy in the same
commit as the link itself.

### FlagOffBehaviourUnchangedTest (11 tests) — KEEP ENTIRELY INTACT
All three `PRE_PHASE_HASHES` constants stay as they are. If one fails, that is a STOP: a
sha-locked shared-presentation file has been edited and the plan that edited it is wrong. Do not
update the constant. Add one case only: the flag-off 404 also holds for `?module=` and `?tab=`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the fence</name>
  <files>tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php</files>
  <action>
    Add to `DEFERRED_AFFORDANCES` the three write affordances D-15 names, each with its owning
    phase: `'Create visit' => 'Phase 46'`, `'Add note' => 'Phase 46'`,
    `'Upload files' => 'Phase 48'`. Update `test_the_fence_enumerates_the_whole_deferred_set` to the
    new count — that count assertion is the anti-rot mechanism and must move deliberately, not be
    deleted.

    Extend `test_rows_are_static_and_nothing_is_wired_to_a_handler` with `x-data`, `x-show`,
    `x-init`, `x-if` and `x-text`. Record the ruling above the test in a short comment: **Alpine is
    loaded globally by the layout and is therefore available, and is nonetheless banned inside the
    cockpit region for this phase.** Phase 45 ships no JavaScript; the side panel is URL state
    instead. When Phase 46 introduces writes it may retire this ban — deliberately, with an owner,
    by editing this list.

    Add `test_opening_a_panel_writes_nothing`: for every module key the presenter exposes, GET
    `?module={key}` and assert every one of the five `WRITE_SURFACE_TABLES` row counts is
    unchanged. The existing invariance tests cover the bare page; the panel is new request surface
    and needs its own proof.

    Add `test_a_hostile_module_value_is_not_reflected`: GET with `?module=<script>alert(1)</script>`
    and with a 5,000-character value; assert 200, assert the submitted value appears nowhere in the
    body, and assert the region is still bracket-valid.

    Update `cockpitRegion()`'s bottom bracket to the new final-link copy. **Then re-prove both
    brackets** using 45-07's ritual: break the extraction three ways (a class that does not exist;
    a truncation that keeps the masthead but drops the panel; a `<button>` injected into a module
    row), confirm each goes red for the expected reason, revert all three, and record the three
    results in the summary. A guard nobody has watched fail is a guard nobody should trust.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Set-Location 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; & \"$env:USERPROFILE\.config\herd\bin\php84\php.exe\" artisan test tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php"</automated>
  </verify>
  <done>Eighteen deferred affordances enumerated and counted; Alpine directives banned with the ruling recorded; panel-open invariance and hostile-input tests present; both brackets re-proved by deliberate breakage.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate CockpitPageTest and CockpitSpineTest per the disposition table</name>
  <files>tests/Feature/Cockpit/CockpitPageTest.php, tests/Feature/Cockpit/CockpitSpineTest.php</files>
  <action>
    Work the disposition table in `<interfaces>` above, test by test, in its stated order. For every
    test you retire, leave a one-line comment at the point of removal naming what it protected and
    where that property now lives. A reader six months from now must be able to tell a deliberate
    retirement from a deletion of convenience.

    Two things to hold on to while migrating:
      - Assert the module count against `CockpitModulePresenter::modules()->count()`, never against
        a literal 8 or 9. The checkpoint in Plan 45-10 settled which it is; hardcoding it here would
        make these tests lie if the ruling is revisited.
      - The reconstructed and superseded disclosure assertions retarget at the module row's count
        phrase, which is the page's new at-rest slot. If that phrase omits the qualifiers, fix Plan
        45-10's presenter — do not soften the assertion.

    Rewrite the class docblock of `CockpitSpineTest` to say what it now covers (module rows and the
    panel's Overview tab) and to record that the file keeps its name so its git history stays
    attached to the assertions it has always carried.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Set-Location 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; & \"$env:USERPROFILE\.config\herd\bin\php84\php.exe\" artisan test tests/Feature/Cockpit"</automated>
  </verify>
  <done>Every disposition-table row is carried out; four retirements each carry a comment naming the rehomed property; no module count is a literal.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the measured D-06 baseline and the flag-off proof</name>
  <files>tests/Feature/Cockpit/FlagOffBehaviourUnchangedTest.php</files>
  <action>
    Add the one new case to `FlagOffBehaviourUnchangedTest`: with the flag off, `?module=` and
    `?tab=` still 404 and still emit no cockpit markup. Change nothing else in that file — in
    particular, the three `PRE_PHASE_HASHES` constants are not yours to touch.

    Then re-run the D-06 behaviour-preservation gate **character-identical** to the command block in
    `45-BASELINE.md` — the enumerated twelve-path file list, not the `--filter=` shorthand, whose
    membership drifts as tests are added and would silently move the gate.

    Assert `>= 159 passed AND 0 failed`. **Never assert equality against 161**: two tests self-skip
    on this machine for a missing `ext-imagick`, so a host with the extension prints 161 and this
    one prints 159. A reduction in pass count is a D-06 stop condition — raise it, do not relax
    criterion 4.

    Do NOT run `artisan migrate:fresh --env=testing`. There is no `.env.testing`; it falls back to
    `.env`, where `DB_DATABASE` is commented out, and would wipe `database/database.sqlite` — live
    local dev data. The subset needs no database preparation; `phpunit.xml:37-38` uses sqlite
    `:memory:`.

    Record the verbatim result block in the summary, as 45-06 and 45-07 did.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Set-Location 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; & \"$env:USERPROFILE\.config\herd\bin\php84\php.exe\" artisan test tests/Unit/InstallTaskGeneratorServiceTest.php tests/Feature/InstallTasks tests/Feature/FieldView tests/Feature/Commissioning tests/Unit/Services/CommissioningItemGeneratorTest.php tests/Unit/Services/CommissioningPdfServiceTest.php tests/Unit/Services/CommissioningServiceTest.php tests/Unit/Services/CommissioningSyncServiceTest.php tests/Unit/Models/CommissioningItemTest.php tests/Unit/Models/CommissioningSignoffTest.php tests/Feature/Projects/ProjectDeliverableAutoFlipTest.php tests/Feature/Authorization/SharedWorkspaceFieldOpsAccessTest.php</automated>
  </verify>
  <done>The gate prints at least 159 passed with zero failures and zero errors; the flag-off file gains one case and keeps its three sha256 constants unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test suite -> production behaviour | These tests are the only mechanical proof that the redesign did not open a write path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-13-01 | Tampering | CockpitReadOnlyFenceTest | mitigate | Extend-only. The count assertion on `DEFERRED_AFFORDANCES` forces any future removal to be deliberate; both region brackets are re-proved by deliberate breakage in this plan. |
| T-45-13-02 | Tampering | PRE_PHASE_HASHES | mitigate | Not editable by this plan. A failing hash is a STOP signal about a sha-locked file, never a constant to refresh. |
| T-45-13-03 | Repudiation | retired assertions | mitigate | Each of the four retirements carries an in-file comment naming its rehomed property, so the audit trail lives beside the code rather than only in a summary. |
| T-45-13-04 | Tampering | D-06 baseline gate | mitigate | Run character-identical from 45-BASELINE.md; `>= 159 AND 0 failed`, never `== 161`. |
| T-45-13-SC | Tampering | npm/composer installs | accept | No package-manager task in this plan. |
</threat_model>

<verification>
- `artisan test tests/Feature/Cockpit tests/Unit/Cockpit` — green.
- The twelve-path D-06 subset prints `>= 159 passed`, `0 failed`, `0 errors`.
- `git diff --name-only 4abd2b24 -- resources/views/layouts/app.blade.php resources/css/app.css tailwind.config.js`
  is EMPTY.
- `git diff` on `FlagOffBehaviourUnchangedTest.php` shows no change inside `PRE_PHASE_HASHES`.
- The three deliberate fence breaks are recorded in the summary with their observed failures.
</verification>

<success_criteria>
- The fence bans more than it did before the redesign and brackets the new region at both ends.
- Every retired assertion has a named successor protecting the same property.
- The flag-off proof and the D-06 baseline both still pass, unmodified in substance.
</success_criteria>

<output>
Create `.planning/phases/45-visit-model-read-only-cockpit/45-13-SUMMARY.md` when done.
</output>
