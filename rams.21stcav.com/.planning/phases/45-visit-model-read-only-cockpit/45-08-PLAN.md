---
phase: 45-visit-model-read-only-cockpit
plan: 08
type: execute
wave: 5
depends_on: [45-07]
files_modified:
  - tests/Feature/Cockpit/FlagOffBehaviourUnchangedTest.php
  - .planning/phases/45-visit-model-read-only-cockpit/45-FLAG-OFF-PROOF.md
autonomous: false
requirements: [VIS-05, VIS-03, VIS-09]
must_haves:
  truths:
    - "With the flag off the cockpit route 404s, emits no markup and pushes no styles"
    - "resources/views/layouts/app.blade.php is byte-identical to its pre-phase state"
    - "The eleven-tab project page, the dashboard and the public token routes are unchanged with the flag off — asserted, not inspected"
    - "The measured D-06 baseline still holds at the end of the phase"
  artifacts:
    - path: "tests/Feature/Cockpit/FlagOffBehaviourUnchangedTest.php"
      provides: "ROADMAP criterion 4 as an executable proof — route 404, zero markup, zero pushed styles, layout file hash unchanged, existing pages unaffected"
      contains: "404"
    - path: ".planning/phases/45-visit-model-read-only-cockpit/45-FLAG-OFF-PROOF.md"
      provides: "The recorded result of the whole-phase gates: baseline re-run, flag-off proof, greyscale and 320px observations"
  key_links:
    - from: "tests/Feature/Cockpit/FlagOffBehaviourUnchangedTest.php"
      to: "resources/views/layouts/app.blade.php"
      via: "a sha256 comparison against the pre-phase hash recorded in 45-BASELINE.md"
      pattern: "layouts/app.blade.php"
---

<objective>
Prove criterion 4 rather than assert it: **with the flag off, the application behaves exactly as it
does today.** ROADMAP criterion 4 says "proven by a test, not by inspection", and this plan is that
test. Then close the phase's two UI open items — the greyscale check and the 320px handset check —
which the UI-SPEC explicitly flags as things that must be observed, not assumed.

Purpose: the whole phase is built on a promise that nothing changes until someone flips an env var.
An untested promise of that shape is how a repo ends up with a "content gate defaulting ON is a
deploy-order trap" retrospective.
Output: one feature test, one recorded proof document, and a human verification of the two visual
properties no automated check can settle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/45-visit-model-read-only-cockpit/45-BASELINE.md
@.planning/phases/45-visit-model-read-only-cockpit/45-UI-SPEC.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FlagOffBehaviourUnchangedTest — criterion 4 as an executable proof</name>
  <files>tests/Feature/Cockpit/FlagOffBehaviourUnchangedTest.php</files>
  <behavior>
    With `config(['cockpit.enabled' => false])` — which is also the shipped default:
    - `GET projects/{project}/cockpit` returns **404**.
    - The 404 response body contains **none** of the cockpit's markers: no `cav-brand`, no
      `cav-cockpit`, no `cav-` class at all, no `--cav-` token. The surface emits no markup.
    - `route('projects.cockpit', $project)` resolves **without throwing** — the route is registered
      unconditionally and gated in the controller, per `routes/web.php:191-197`. This assertion is
      what stops a later agent "tidying up" by wrapping the route in an `if`.
    - **No cockpit style is pushed.** `GET projects/{project}` (the eleven-tab page) renders and its
      HTML contains no `cockpit.css` reference, no `@vite` cockpit asset, and no `--cav-` token.
    - **`resources/views/layouts/app.blade.php` is byte-identical to its pre-phase state.** Assert a
      `hash_file('sha256', ...)` against the hash recorded in `45-BASELINE.md`. Message on failure
      must say plainly that adding tokens to that file's `:root` retones every page in the app and
      violates criterion 4 and D-07.
    - `resources/css/app.css` and `tailwind.config.js` are likewise unchanged by hash.
    - The eleven-tab project page still renders 200 with its existing tab markers present, and the
      dashboard still renders 200 with its health badge — the two surfaces
      `ProjectHealthService` feeds.
    - **VIS-03 again, at the end of the phase:** `GET /survey/{token}` and `GET /worksheet/{token}`
      return the same status and the same key page markers as before Phase 45, and the tokens
      themselves are unrotated.
    - `visits` rows existing in the database changes nothing about any of the above. Seed a
      backfilled visit, then re-assert the eleven-tab page and the dashboard are unaffected — the
      new data must be inert with the flag off.
  </behavior>
  <action>
    Write `tests/Feature/Cockpit/FlagOffBehaviourUnchangedTest.php` extending `Tests\TestCase` with
    `RefreshDatabase`, one `test_snake_case` method per behavior line above.

    For the hash assertions: `45-BASELINE.md` records the pre-phase HEAD SHA. Derive the three
    reference hashes at execution time with
    `git show {SHA}:resources/views/layouts/app.blade.php | sha256sum` (PowerShell equivalent), and
    hard-code the resulting hashes in the test with a comment naming the SHA they came from, so the
    test is self-contained and does not shell out to git at runtime. If any of the three files has
    in fact changed, **STOP** — that is a criterion-4 violation, not a hash to update.

    Derive the public route names from source rather than trusting this plan:
    `grep -n "survey/{token}\|worksheet/{token}" routes/web.php`.
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com"; & "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan test tests/Feature/Cockpit/FlagOffBehaviourUnchangedTest.php 2>&1 | Select-Object -Last 40</automated>
  </verify>
  <done>Every flag-off assertion green, including the three file hashes and the public token routes.</done>
</task>

<task type="auto">
  <name>Task 2: Whole-phase gate re-run and the recorded proof</name>
  <files>.planning/phases/45-visit-model-read-only-cockpit/45-FLAG-OFF-PROOF.md</files>
  <action>
    Run three things and record all three in `45-FLAG-OFF-PROOF.md`:

    1. **The D-06 baseline, one last time.** The exact command from `45-BASELINE.md`. Record
       "baseline N (Plan 45-01, SHA xxxx) → end of phase M". Same-or-greater with zero failures, or
       this is a stop condition — report it, do not relax criterion 4.
    2. **The full suite**, so the phase is not green only in its own corner:
       `artisan test`. Record the result. The known pre-existing `QueueRecoverCommandTest`
       full-suite failure (documented in `.planning/STATE.md` as a memory-threshold interaction) is
       **not** to be chased — note it and move on. Any *other* new failure is a real finding.
    3. **`npm run build`** — the cockpit is a Vite entry, and a deploy that skips the build ships
       the page unstyled.

    Then record the **deploy order** in the same document as an operator checklist, because
    `.env` and the production database both hold state the repo does not:
    ship code with `COCKPIT_ENABLED` absent → `npm run build` → `artisan migrate` →
    `install-records:backfill` dry-run then `--apply` → `visits:backfill` dry-run then `--apply` →
    verify row counts → **only then** a separate one-line `.env` flip to `COCKPIT_ENABLED=true` →
    `artisan config:clear`. Never arm the flag in the same deploy as the backfill.
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com"; & "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan test 2>&1 | Select-Object -Last 20; npm run build 2>&1 | Select-Object -Last 10</automated>
  </verify>
  <done>`45-FLAG-OFF-PROOF.md` records the baseline comparison, the full-suite result with any pre-existing failure named, a clean build, and the operator deploy checklist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The complete read-only cockpit: nine drawers closed at rest, the 21CAV brand tokens, the
    reconstructed and superseded treatments, and the read-only fence. Everything automatable has
    been automated — flag-off behaviour, the fence, the contrast ledger's token values and the
    absence of form controls are all covered by tests. Two properties cannot be settled by a test,
    and the UI-SPEC flags both as formally open (§ Open Items 3 and 4).
  </what-built>
  <how-to-verify>
    1. Set `COCKPIT_ENABLED=true` in `.env`, run
       `& "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan config:clear`, then
       `npm run build`.
    2. Open a project with a mixture of visits — at least one reconstructed, one superseded, and one
       empty section. Visit `/projects/{id}/cockpit`.
    3. **Greyscale check.** Apply a greyscale filter (browser devtools → Rendering → Emulate vision
       deficiencies → Achromatopsia, or a desaturated screenshot). Confirm all four states are still
       distinguishable: filled circle (done), outlined diamond (attention), hollow circle (waiting),
       square box (hand-ticked, unticked). If any two are confusable, the shape system has failed
       and colour is doing work it must not do.
    4. **320px handset check.** Narrow to 320px. Confirm: no horizontal scroll; the summary becomes
       two rows (pip + title + arrow, then count + status); **the count slot is still visible** —
       that is the only at-rest disclosure of a reconstructed or superseded visit, and hiding it
       would show an inferred visit as an unqualified fact. The sketch's
       `@media(max-width:480px){ .s,.c{display:none} }` must NOT be in effect; the user signed off
       this deviation on 2026-09-19.
    5. Confirm every drawer is **closed** on load, including Install.
    6. Confirm the page has no buttons, no form controls, and no row that looks clickable.
    7. Set `COCKPIT_ENABLED=false`, `config:clear`, and confirm the project page and dashboard look
       exactly as they did before this phase.
  </how-to-verify>
  <resume-signal>Type "approved", or describe what you saw — particularly any two states that were confusable in greyscale, or anything that reflowed badly at 320px.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` → application | `COCKPIT_ENABLED` decides whether the whole surface exists. It is the phase's single kill switch. |
| repo state → production | The deploy checklist is the boundary between "the code is merged" and "the feature is on". They are deliberately separate decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-08-01 | Elevation of Privilege | The flag failing open | mitigate | `FlagOffBehaviourUnchangedTest` asserts a 404 with zero cockpit markup at the shipped default, and `CockpitFlagDefaultTest` (Plan 45-03) pins the literal default to `false`. |
| T-45-08-02 | Tampering | A silent retoning of every existing page | mitigate | Three sha256 hashes (`layouts/app.blade.php`, `resources/css/app.css`, `tailwind.config.js`) asserted against the pre-phase HEAD. A change is a stop condition, not a hash to update. |
| T-45-08-03 | Denial of Service | Arming the flag before the backfill has run | mitigate | The recorded deploy checklist makes the order explicit, and the flag's default-false plus the dry-run-by-default backfills make the safe path the easy one. |
| T-45-08-04 | Repudiation | "It was already broken" after a regression | mitigate | The baseline is a measured number with a producing command and a HEAD SHA (Plan 45-01), compared once more here at end of phase. |
| T-45-08-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. |
</threat_model>

<verification>
`artisan test tests/Feature/Cockpit/FlagOffBehaviourUnchangedTest.php` green.
Full `artisan test` recorded, with only the documented pre-existing failure.
`npm run build` clean. Human checkpoint approved.
</verification>

<success_criteria>
- With the flag off: the cockpit route 404s, emits no markup, pushes no styles, and
  `route('projects.cockpit', ...)` still resolves.
- `layouts/app.blade.php`, `resources/css/app.css` and `tailwind.config.js` are byte-identical to
  their pre-phase state, asserted by hash.
- The eleven-tab page, the dashboard and the public token routes are unchanged, with and without
  `visits` rows present.
- The measured D-06 baseline holds at the end of the phase.
- Greyscale distinguishes all four states, and the count slot survives 320px — both observed on a
  real render, not inferred.
</success_criteria>

<output>
Create `.planning/phases/45-visit-model-read-only-cockpit/45-08-SUMMARY.md` when done
</output>
