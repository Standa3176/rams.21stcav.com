---
phase: 45-visit-model-read-only-cockpit
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - resources/css/cav-tokens.css
  - .planning/REQUIREMENTS.md
  - .planning/phases/45-visit-model-read-only-cockpit/45-CONTEXT.md
autonomous: true
requirements: [VIS-10]
must_haves:
  truths:
    - "The cockpit's token file carries the app's blue and Inter, not 21CAV teal/Verdana/Poppins"
    - "No cockpit token is added to layouts/app.blade.php's :root — the scoping class survives"
    - "VIS-10 no longer asserts a teal palette that the accepted design contradicts"
  artifacts:
    - path: "resources/css/cav-tokens.css"
      provides: "class-scoped cockpit tokens, app-blue values"
      contains: ".cav-brand"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VIS-10 amended and the amendment dated, not silently rewritten"
  key_links:
    - from: "resources/css/cockpit.css"
      to: "resources/css/cav-tokens.css"
      via: "@import — unchanged, so the Vite entry and manifest stay as built"
      pattern: "@import.*cav-tokens"
---

<objective>
Retarget the cockpit's token layer from 21CAV teal/Verdana/Poppins to the application's own blue and
Inter, per **D-08**, which reverses **D-07**. Keep every structural property of Plan 45-03's token
work — the `.cav-brand` scoping class, the `@stack('styles')` seam, the `@import` chain into
`cockpit.css`, the "no raw hex in Blade" guard — and change only the values.

Purpose: D-08 is the one decision that invalidates work already shipped to production. Doing it
first, alone, in a plan that touches no Blade and no PHP, means every later plan in this set renders
against settled colour instead of re-deciding it, and keeps the reversal auditable as one
values-only commit.
Output: an app-blue `cav-tokens.css`; an amended VIS-10; D-08..D-15 recorded in 45-CONTEXT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/sketches/004-delivery-cockpit/README.md
@.planning/phases/45-visit-model-read-only-cockpit/45-CONTEXT.md
@.planning/phases/45-visit-model-read-only-cockpit/45-BASELINE.md
@resources/css/cav-tokens.css

<interfaces>
The app's real palette, already extracted from `layouts/app.blade.php`. DO NOT open that file to
edit it — it is sha256-locked by `FlagOffBehaviourUnchangedTest`. No exploration needed:

    primary / navy accent   #1E5FE0
    brighter accent         #2E7BFF   (tailwind.config.js `brand.teal`)
    body and heading type   Inter

WHY `--teal-*` IS NOT AVAILABLE TO YOU: that name already means BLUE in this app, and the alias
lives in the sha-locked layout. Every token this plan writes keeps the `--cav-` prefix it already
has. Renaming the prefix would touch 12 Blade components and 25KB of CSS for no user-visible gain
and is explicitly NOT in scope.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget cav-tokens.css to the app's blue and Inter</name>
  <files>resources/css/cav-tokens.css</files>
  <action>
    Rewrite the VALUES in `resources/css/cav-tokens.css`. Do not change its structure, its selector
    or its filename.

    Keep exactly as-is: the `.cav-brand { ... }` scoping selector (tokens must never reach `:root`);
    every token NAME currently declared; the file's position as the thing `cockpit.css` `@import`s.
    Changing a token name would break `cockpit.css` silently — the declaration would fall back to an
    inherited value and the page would render in the app's default colours by accident rather than
    by decision, which is indistinguishable from success until someone looks closely.

    Change: every teal value onto the app's accent family (`#1E5FE0` primary, `#2E7BFF` brighter
    accent; derive hover / soft / border steps from those two rather than introducing a third hue).
    The gold accent family narrows to the single amber stage-chip use the design shows, or is
    dropped if nothing consumes it. Heading and body font stacks become Inter with the app's own
    fallbacks; the Verdana heading stack and the Poppins body stack both go.

    Delete any `@font-face` or `@import` of `@fontsource/poppins` from this file. Do NOT uninstall
    the npm package and do NOT touch `package.json`, `package-lock.json` or `vite.config.js` — that
    forces a lockfile change and a rebuild inside a plan whose entire value is that it is
    values-only. Record the now-unused dependency in the summary as a later cleanup.

    Rewrite the file's header docblock. It currently justifies a 21CAV brand refresh. It must now
    record: that D-08 reversed D-07; that the accepted design (sketch 004) renders the cockpit in
    the app's own palette inside the real app shell; that the `--cav-` prefix is retained for
    mechanical reasons and now means "cockpit-scoped", not "CAV brand"; and that the scoping class
    is the load-bearing part, not the values — a token added to the layout's `:root` would retone
    the whole application and break ROADMAP criterion 4.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Set-Location 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; if (Select-String -Path resources/css/cav-tokens.css -Pattern ':root' -Quiet) { Write-Output 'FAIL tokens escaped into :root'; exit 1 }; if (Select-String -Path resources/css/cav-tokens.css -Pattern 'Verdana|Poppins|01889F|016E82|D4AF37' -Quiet) { Write-Output 'FAIL a superseded D-07 value survives'; exit 1 }; if (-not (Select-String -Path resources/css/cav-tokens.css -Pattern '\.cav-brand' -Quiet)) { Write-Output 'FAIL scoping class lost'; exit 1 }; Write-Output 'PASS'"</automated>
  </verify>
  <done>`cav-tokens.css` declares its tokens only inside `.cav-brand`, carries no teal/gold/Verdana/Poppins value, and every token name `cockpit.css` references still resolves.</done>
</task>

<task type="auto">
  <name>Task 2: Amend VIS-10 and record D-08..D-15</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/45-visit-model-read-only-cockpit/45-CONTEXT.md</files>
  <action>
    VIS-10 currently reads that brand tokens (teal `#01889F`, gold `#D4AF37`, Verdana headings,
    Poppins body) are scoped and never added to the layout's `:root`. Half of that is now false and
    half is the most important constraint in the phase.

    Amend VIS-10 in `.planning/REQUIREMENTS.md` so the requirement states the MECHANISM, not the
    palette: cockpit presentation tokens are declared under a scoping class, never in the shared
    layout's `:root`, and the three shared-presentation files stay byte-identical. Add a dated
    amendment note directly beneath it — "Amended 2026-09-20 by D-08 (sketch 004 accepted); the
    original teal/gold/Verdana/Poppins palette is superseded, the scoping constraint is not" — so
    the reversal appears in the requirement's own history instead of looking like the requirement
    always said this.

    Append to `45-CONTEXT.md` a new section `## Decisions — sketch 004 (2026-09-20)` carrying
    **D-08 through D-15 verbatim from the sketch README**, each in the exact `- **D-08:** text`
    form — the coverage gate recognises only that form and passes silently with "no trackable
    decisions" otherwise. Above them, one line stating that D-01..D-06 still hold unchanged and
    that D-07 is REVERSED by D-08. Copy the decisions; do not restate or reinterpret them.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "Set-Location 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; $c = Get-Content .planning/phases/45-visit-model-read-only-cockpit/45-CONTEXT.md -Raw; $missing = @(); 8..15 | ForEach-Object { $id = 'D-' + $_.ToString('00'); if ($c -notmatch ('\*\*' + $id + ':\*\*')) { $missing += $id } }; if ($missing.Count -gt 0) { Write-Output ('FAIL missing ' + ($missing -join ',')); exit 1 }; if (-not (Select-String -Path .planning/REQUIREMENTS.md -Pattern 'Amended 2026-09-20' -Quiet)) { Write-Output 'FAIL VIS-10 amendment not dated'; exit 1 }; Write-Output 'PASS'"</automated>
  </verify>
  <done>D-08..D-15 are readable in 45-CONTEXT.md in the `- **D-NN:**` form; VIS-10 states the scoping mechanism and carries a dated amendment note naming D-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan edits one stylesheet and two planning documents. No route, no input, no query. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-09-01 | Tampering | resources/css/cav-tokens.css | mitigate | The file is live in production (45-03 shipped it). A token NAME change falls back silently rather than erroring, so Task 1's verify asserts the scoping class and the absence of superseded values, and Plan 45-14 re-checks visually. |
| T-45-09-02 | Tampering | layouts/app.blade.php | mitigate | Not touched here. `FlagOffBehaviourUnchangedTest` asserts its sha256 — if it moves, STOP; do not update the constant. |
| T-45-09-SC | Tampering | npm installs | accept | This plan installs nothing and must not. `@fontsource/poppins` becomes unused but is left in place deliberately — removing it is a lockfile change plus a rebuild, out of scope. |
</threat_model>

<verification>
- `Select-String` over `cav-tokens.css` finds no `:root`, `Verdana`, `Poppins`, `#01889F`,
  `#016E82` or `#D4AF37`.
- `git diff --name-only 4abd2b24 -- resources/views/layouts/app.blade.php resources/css/app.css tailwind.config.js`
  is EMPTY. A non-empty result is a STOP, not something to fix by updating a hash constant.
- `& "$env:USERPROFILE\.config\herd\bin\php84\php.exe" artisan test tests/Feature/Cockpit/FlagOffBehaviourUnchangedTest.php`
  still passes (11 tests). This plan changes no Blade, so it must.
</verification>

<success_criteria>
- The cockpit's tokens are the app's blue and Inter, declared only under `.cav-brand`.
- VIS-10 reads as the scoping mechanism, with the palette reversal dated and attributed to D-08.
- D-08..D-15 are in 45-CONTEXT.md in the gate-recognised form.
- The three sha-locked shared-presentation files are untouched.
</success_criteria>

<output>
Create `.planning/phases/45-visit-model-read-only-cockpit/45-09-SUMMARY.md` when done.
</output>
