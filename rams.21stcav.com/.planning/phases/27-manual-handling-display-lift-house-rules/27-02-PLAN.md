---
phase: 27-manual-handling-display-lift-house-rules
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - app/Services/Rams/RamsComplianceUpgradeService.php
  - database/seeders/HazardTemplateSeeder.php
  - tests/Unit/Services/Rams/RamsComplianceUpgradeServiceDisplayLiftTest.php
autonomous: true
requirements: [RULE-02, RULE-03, RULE-12]

must_haves:
  truths:
    - "Every generated RAMS's §6.7 material-handling row for a display reads DisplayLiftPolicy's band sentence — not the removed 4-person/3-person ladder"
    - "A mount or bracket description never returns display handling text, even when its description contains the word 'display'"
    - "A strip-out job's §6.7 table explicitly states the wall-mount removal sequence for the display being decommissioned — not just a generic team-lift line"
    - "The seeded 'Manual handling' hazard's control text no longer says 'minimum two operatives for every panel size'"
  artifacts:
    - path: "app/Services/Rams/RamsComplianceUpgradeService.php"
      provides: "suggestHandlingMethod() reading DisplayLiftPolicy; deriveMaterialHandling() scanning scope_items.decommission; both entry points (runFromReview/runPipeline) unaffected in call shape"
    - path: "database/seeders/HazardTemplateSeeder.php"
      provides: "Manual handling row controls sourced from DisplayLiftPolicy::genericBandSummary() / ::wallMountRemovalStatement()"
    - path: "tests/Unit/Services/Rams/RamsComplianceUpgradeServiceDisplayLiftTest.php"
      provides: "Non-vacuous proof of the ladder removal, the branch-order fix, and the decommission-scan statement"
  key_links:
    - from: "app/Services/Rams/RamsComplianceUpgradeService.php::suggestHandlingMethod()"
      to: "app/Services/Rams/DisplayLiftPolicy.php::forSize()"
      via: "static call, resolved inch value passed through"
      pattern: "DisplayLiftPolicy::forSize\\("
    - from: "database/seeders/HazardTemplateSeeder.php"
      to: "app/Services/Rams/DisplayLiftPolicy.php"
      via: "static call at seed time, not a hardcoded duplicate string"
      pattern: "DisplayLiftPolicy::(genericBandSummary|wallMountRemovalStatement)\\("
---

<objective>
Wire RULE-02's corrected bands and RULE-12's branch-order fix into
`RamsComplianceUpgradeService::suggestHandlingMethod()`, extend
`deriveMaterialHandling()` to scan the previously-unscanned
`scope_items['decommission']` bucket so RULE-03's wall-mount-removal statement
appears explicitly on real strip-out jobs (not just as a buried generic hazard-control
bullet), and re-source the seeded "Manual handling" hazard's control text from
`DisplayLiftPolicy` so the DB row and the freshly-derived §6.7 text can never
disagree.

Purpose: this is the actual RULE-02/RULE-03/RULE-12 fix — the previous plan only
built the policy class in isolation. Both real generation entry points
(`RamsBuilderService::runFromReview()` and `::runPipeline()`) converge on
`RamsComplianceUpgradeService::upgrade()`, so this single file edit reaches both
without a second wiring pass (confirmed by direct code trace in RESEARCH.md — no
third unwired path exists here, unlike Phase 26's hazard-resolver problem).

Output: `suggestHandlingMethod()` rewritten to delegate to `DisplayLiftPolicy`;
`deriveMaterialHandling()` extended with a decommission scan; each detected item now
also carries structured `min_persons`/`inches` fields (needed by Plan 27-03's GATE-09,
which must independently re-validate structured data, never re-parse rendered
prose); the seeder's Manual handling row sourced from the shared class.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-manual-handling-display-lift-house-rules/27-CONTEXT.md
@.planning/phases/27-manual-handling-display-lift-house-rules/27-RESEARCH.md
@.planning/phases/27-manual-handling-display-lift-house-rules/27-PATTERNS.md
@.planning/reference/21cav-rams-skill/references/house-rules.md
</context>

<interfaces>
<!-- DisplayLiftPolicy's public contract, produced by Plan 27-01. Read the actual
file for exact signatures before editing — do not assume this snapshot is final if
Plan 27-01's SUMMARY records a deviation. -->

From `app/Services/Rams/DisplayLiftPolicy.php` (Plan 27-01):
- `forSize(?float $inches, bool $isSmallControlPanel = false): ?array` — returns
  `null` (no row) or `['min_persons' => int, 'sentence' => string]`.
- `violatesPolicy(int $statedPersons, ?float $inches): bool` — not used by this
  plan (Plan 27-03 consumes it); do not call it here.
- `wallMountRemovalStatement(): string` — the exact RULE-03 sentence.
- `genericBandSummary(): string` — a static-context sentence naming all three
  thresholds, for the seeder.
</interfaces>

<current_state>
`RamsComplianceUpgradeService::suggestHandlingMethod()` (`:1231-1290`, private
static, returns `?string`) currently:
- Extracts `$inches` via a general regex (`:1237-1240`) — UNCHANGED, this plan does
  not touch inch extraction.
- Applies the `$isSmallPanel` ≤14" + keyword exclusion (`:1245-1249`) — UNCHANGED,
  this plan preserves this exact early-return behaviour.
- The display/tv/television/screen branch (`:1252-1266`) hardcodes a 4-person
  band at ≥85" and a 3-person "recommended... may lift with a panel-lift trolley"
  band at ≥65", else a flat 2-person sentence — THIS is the code being replaced.
- The mount/bracket branch (`:1270-1281`) sits AFTER the display branch, so a
  description containing both "mount" and "display" (e.g. "double-arm wall mount
  for 65 inch display") matches the display branch first — THIS branch order is
  the RULE-12 root cause being fixed.

`deriveMaterialHandling()` (`:1086-1213`) scans three sources today: `quote.
line_items`, `scope_items.new_install`, and `rooms[].equipment`. It does NOT scan
`scope_items.decommission` at all — a real gap, not a hypothesis: a display being
stripped out currently produces zero §6.7 rows regardless of RULE-03.

`database/seeders/HazardTemplateSeeder.php`'s "Manual handling" row (`:109-127`)
already contains the RULE-03 wall-mount sentence verbatim in its controls array
(bullet 3) — that bullet does not need rewording, only re-sourcing from the shared
class for single-source-of-truth. Bullet 2 ("Team lift required for all displays —
minimum two operatives for every panel size...") is the stale, unamended RULE-02
wording that DOES need rewriting.
</current_state>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RULE-02 ladder + RULE-12 branch order in suggestHandlingMethod()</name>
  <files>app/Services/Rams/RamsComplianceUpgradeService.php</files>
  <read_first>
    app/Services/Rams/RamsComplianceUpgradeService.php (full read of :1086-1330 — deriveMaterialHandling(), suggestHandlingMethod(), and the three call sites inside deriveMaterialHandling() that build $detectedItems)
    app/Services/Rams/DisplayLiftPolicy.php (Plan 27-01's finished class — exact method signatures)
    .planning/phases/27-manual-handling-display-lift-house-rules/27-PATTERNS.md ("suggestHandlingMethod() (RULE-02 ladder + RULE-12 ordering)" section — the exact current code block being replaced and the exact reorder)
  </read_first>
  <behavior>
    - A description matching the display/tv/television/screen condition at any
      resolved inch value now returns `DisplayLiftPolicy::forSize($inches,
      $isSmallPanel)['sentence']` instead of the hardcoded 4/3/2-person text. The
      `$isSmallPanel` early-return (`:1245-1249`) is unchanged and still runs
      first, so DisplayLiftPolicy is never asked to resolve a scheduling/touch
      panel.
    - A description matching `mount` or `bracket` now resolves BEFORE the
      display/tv/television/screen check is evaluated, so "double-arm wall mount
      for 65 inch display" returns the mount/bracket branch's text, never the
      display branch's text.
    - Each of the three call sites inside `deriveMaterialHandling()` that today
      store `['item' => ..., 'qty' => ..., 'handling_method' => $method]` now
      also store `'min_persons' => ?int` and `'inches' => ?float` — the resolved
      values `suggestHandlingMethod()` computed for that item (null/null for
      non-display items where no band applies, e.g. amps, racks, speakers).
    - No hardcoded team-size number (`4 persons`, `minimum 4`, `minimum 3
      persons`, or the `panel-lift trolley` aid-discharges-a-person construction)
      remains anywhere in `suggestHandlingMethod()`'s display branch after this
      task.
  </behavior>
  <action>
    Change `suggestHandlingMethod(string $description, int $qty = 1): ?string`'s
    return type to `?array` shaped `['sentence' => string, 'min_persons' => ?int,
    'inches' => ?float]` (null unchanged for the ≤14" exclusion). Move the
    `mount`/`bracket` `if` block (`:1270-1281`) to immediately before the
    `display`/`tv`/`television`/`screen` `if` block (`:1252`), unmodified in its
    own internal logic — this is the minimum RULE-12 fix per CONTEXT.md's
    accepted discretion (option b: fix ordering, defer weight-derivation
    explicitly — see Deferred Ideas note below). Replace the entire body of the
    display branch's ≥85"/≥65"/else cascade with one call:
    `DisplayLiftPolicy::forSize($inches, $isSmallPanel)`, returning its
    `min_persons`/`sentence` wrapped in this method's `?array` shape with
    `'inches' => $inches`. Every other branch (`projector`, `rack`, `amplifier`/
    `amp`/`dsp`, `ceiling speaker`, `speaker`, the final catch-all) returns the
    same `?array` shape with `'min_persons' => null` (no display band applies —
    these are not RULE-02's subject) and `'inches' => null`. Update the three call
    sites inside `deriveMaterialHandling()` (`quote.line_items` loop, `scope_items.
    new_install` loop, `rooms[].equipment` loop) to read `$resolved['sentence']`
    into `handling_method` and copy `$resolved['min_persons']`/`$resolved
    ['inches']` into the stored item array. `deriveMaterialHandling()`'s own
    generic `statement` text (`:1206-1210`, "Team lifts (minimum 2 persons)...
    for items over 20 kg") is a separate, non-display-specific weight statement —
    confirmed considered and NOT in scope for this task (RESEARCH.md Pitfall 3);
    do not edit it.

    RULE-12 scope note (record this explicitly, per CONTEXT.md's instruction not
    to silently narrow the requirement): RULE-12's full text requires deriving
    manual-handling controls "from actual equipment weight, manufacturer handling
    requirements and route assessment — never from screen diagonal alone."
    RESEARCH.md confirmed, via direct trace of the quote-ingestion pipeline, that
    `weight_kg`/`display_size_in` structured tags never reach RAMS-path equipment
    items (`QuoteWerksImportService`, `ExtractQuoteJob` produce raw `description`/
    `qty`/`category` dicts only) — coverage is effectively zero. This task
    implements ONLY the "mount rows must not inherit display text" clause (the
    branch-reorder). The weight/manufacturer/route-derivation clause is explicitly
    DEFERRED, not silently dropped — this deferral must be restated verbatim in
    this plan's SUMMARY.md so it reaches VERIFICATION.md.
  </action>
  <verify>
    <automated>php artisan test --filter=RamsComplianceUpgradeServiceDisplayLiftTest</automated>
  </verify>
  <done>
    `suggestHandlingMethod()` returns the new array shape, delegates all display
    bands to `DisplayLiftPolicy::forSize()`, and evaluates the mount/bracket
    branch before the display branch; `deriveMaterialHandling()`'s three existing
    scan loops store `min_persons`/`inches` on every detected item.
  </done>
  <acceptance_criteria>
    - `grep -n "minimum 4 persons\|minimum 3 persons recommended\|panel-lift trolley" app/Services/Rams/RamsComplianceUpgradeService.php` returns zero matches inside `suggestHandlingMethod()`'s body.
    - Calling the reflected `suggestHandlingMethod` with a description containing `"double-arm wall mount for 65 inch display"` returns a sentence matching the mount/bracket branch's wording (containing `wall mount` or `bracket` context), never the display branch's `Team lift` sentence.
    - Calling it with `"Samsung 98 inch display"` returns `min_persons === 3` and a sentence with no aid-discharges-operative construction.
    - Calling it with `"Samsung 43 inch display"` returns `min_persons === 1`.
    - Calling it with `"10.1 inch scheduling touch panel"` still returns `null` (unchanged existing behaviour).
    - `deriveMaterialHandling()`'s returned `items[]` entries for display items now include both `min_persons` and `inches` keys.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RULE-03 decommission-scope scan + seeder re-sourcing</name>
  <files>app/Services/Rams/RamsComplianceUpgradeService.php, database/seeders/HazardTemplateSeeder.php</files>
  <read_first>
    app/Services/Rams/RamsComplianceUpgradeService.php (deriveMaterialHandling()'s three existing scan loops, :1140-1195, as the structural pattern for a fourth loop)
    app/Services/RamsBuilderService.php (:277-281 — where scope_items.decommission is populated on both entry points, before upgrade() runs)
    app/Services/Rams/HazardIncludeWhenResolver.php (:83-88 — the strip_out_or_decommission tier-2 keyword vocabulary, for reference only; NOT called by this task since scope_items.decommission is already the structured signal)
    app/Services/EquipmentClassifierService.php (:26-33 — display_installation activity keywords, the vocabulary to reuse for detecting a display item within the decommission bucket)
    database/seeders/HazardTemplateSeeder.php (:109-127 — the Manual handling row's current controls array, exact bullet text and array shape)
  </read_first>
  <behavior>
    - `deriveMaterialHandling()` gains a fourth scan loop over
      `$data['scope_items']['decommission']`, structurally identical in shape to
      the existing `new_install` loop.
    - When a decommission item's description matches a display keyword (reuse
      `EquipmentClassifierService::ACTIVITY_MAP['display_installation']
      ['keywords']`, not a new list), the produced `handling_method` sentence is
      the resolved band sentence from `suggestHandlingMethod()` WITH
      `DisplayLiftPolicy::wallMountRemovalStatement()` appended — explicitly
      stating the removal-from-mount sequence, not just the generic band text.
    - Non-display decommission items (e.g. a rack being stripped out) are scanned
      through the same `suggestHandlingMethod()` path as before — no wall-mount
      statement appended, since RULE-03 is display-specific per `house-rules.md`.
    - The seeded "Manual handling" hazard's controls array bullet 2 (the stale
      "minimum two operatives for every panel size" text) is replaced with a
      sentence built from `DisplayLiftPolicy::genericBandSummary()`. Bullet 3
      (the wall-mount-removal sentence, already correct) is replaced with a call
      to `DisplayLiftPolicy::wallMountRemovalStatement()` rather than a duplicate
      hardcoded string — single source of truth, per D-03.
  </behavior>
  <action>
    Add a fourth loop to `deriveMaterialHandling()`, iterating `(array)
    ($data['scope_items']['decommission'] ?? [])`, using the same `item_name`/
    `qty` field shape the existing `new_install` loop reads (confirm the actual
    key names by reading the `new_install` loop's field access — do not assume
    `decommission` items share `item_name`/`qty` without checking). For each item,
    determine `$isDisplay` by checking the description against
    `EquipmentClassifierService::ACTIVITY_MAP['display_installation']
    ['keywords']` (inject `EquipmentClassifierService` or call its keyword list
    directly — do not hardcode a second copy of the keyword array). When
    `$isDisplay` is true, call `suggestHandlingMethod()` for the base sentence,
    then concatenate `' ' . DisplayLiftPolicy::wallMountRemovalStatement()` onto
    the returned sentence before storing it as `handling_method`. Store the item
    with a distinguishing label (e.g. append `' (decommission)'` to the item name
    or add a `'phase' => 'decommission'` key — pick one and use it consistently)
    so a §6.7 reader and this plan's test can distinguish a decommission-scan row
    from a new-install row. In `HazardTemplateSeeder.php`, add a `use
    App\Services\Rams\DisplayLiftPolicy;` import and replace the two identified
    control bullets with calls to `DisplayLiftPolicy::genericBandSummary()` and
    `DisplayLiftPolicy::wallMountRemovalStatement()` respectively, preserving the
    surrounding array's structure, ordering, and the other five bullets verbatim.
  </action>
  <verify>
    <automated>php artisan test --filter=RamsComplianceUpgradeServiceDisplayLiftTest</automated>
  </verify>
  <done>
    `deriveMaterialHandling()` includes a fourth scan of `scope_items.decommission`
    producing an explicit wall-mount-removal statement for display strip-out
    items; `HazardTemplateSeeder.php`'s Manual handling row sources both edited
    bullets from `DisplayLiftPolicy`.
  </done>
  <acceptance_criteria>
    - A fixture with `scope_items.decommission` containing an item whose
      description matches a display keyword (e.g. `"Remove existing 65in
      wall-mounted display"`) produces a `material_handling_derived.items` entry
      whose `handling_method` contains both a resolved band sentence AND the
      substrings `lowest practicable height` and `one operative each side`.
    - The same fixture's non-display decommission item (e.g. `"Remove existing
      19in equipment rack"`) produces an entry whose `handling_method` does NOT
      contain `one operative each side`.
    - `HazardTemplateSeeder.php`'s Manual handling row no longer contains the
      literal substring `for every panel size`.
    - Running `php artisan db:seed --class=HazardTemplateSeeder` locally
      (idempotent, upsert-by-name per existing seeder convention) succeeds and
      the seeded row's controls array still has exactly 7 entries in the same
      order.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Non-vacuity test suite for Tasks 1 and 2</name>
  <files>tests/Unit/Services/Rams/RamsComplianceUpgradeServiceDisplayLiftTest.php</files>
  <read_first>
    tests/Unit/Services/Rams/ProjectSpecificRisksGatedTest.php (full file — the reflection-invoke-private-static-method pattern this test must copy: `invokePrivateStatic(string $method, array $args = [])` via `ReflectionMethod::setAccessible(true)`)
    tests/Unit/Services/Rams/RamsComplianceUpgradeServiceCacheTest.php (sibling test file naming/namespace convention)
    app/Services/RamsBuilderService.php (:277-281 — confirms scope_items.decommission's real field shape, to build a realistic fixture)
  </read_first>
  <action>
    Create `Tests\Unit\Services\Rams\RamsComplianceUpgradeServiceDisplayLiftTest
    extends Tests\TestCase`, copying `ProjectSpecificRisksGatedTest`'s
    `invokePrivateStatic()` helper verbatim. Write tests that invoke
    `deriveMaterialHandling` (private static, the public-facing surface for this
    plan's changes) with fixtures covering: (a) a quote line item `"Samsung 98in
    Display"` producing `min_persons === 3` with no aid-discharge wording; (b) a
    quote line item `"Double-arm wall mount for 65 inch display"` producing a
    mount-branch sentence, proving RULE-12's fix is non-vacuous (write the
    assertion so it would have failed against the pre-fix branch order — assert
    the sentence does NOT start with `Team lift` and DOES reference `wall mount`
    or `bracket`); (c) a `scope_items.decommission` entry for a wall-mounted
    display producing the RULE-03 statement per Task 2's acceptance criteria; (d)
    a `scope_items.decommission` entry for a non-display item NOT producing the
    RULE-03 statement. Also invoke `suggestHandlingMethod` directly (reflection)
    for the boundary values 54, 55, 90, 90.1 asserting `min_persons` 1/2/2/3
    respectively, proving RULE-02's ladder removal is non-vacuous (these values
    would have produced 2/2/2/2 under the pre-fix code, since the old ladder only
    branched at 65"/85"). Add one assertion reading the re-seeded
    `HazardTemplateSeeder`'s Manual handling row content (seed in-memory or read
    the seeder's source array directly) confirming it no longer contains `for
    every panel size` and does contain the wall-mount-removal substring.
  </action>
  <verify>
    <automated>php artisan test --filter=RamsComplianceUpgradeServiceDisplayLiftTest</automated>
  </verify>
  <done>
    `tests/Unit/Services/Rams/RamsComplianceUpgradeServiceDisplayLiftTest.php`
    exists and passes, with the plan's SUMMARY recording an explicit revert/
    restore non-vacuity proof for the RULE-12 branch-order fix.
  </done>
  <acceptance_criteria>
    - `php artisan test --filter=RamsComplianceUpgradeServiceDisplayLiftTest`
      passes with at least 8 distinct test methods.
    - At least one test in this file is written such that, if Task 1's branch
      reorder were reverted, it would fail (the mount-shadowed-by-display
      regression case) — confirm this by temporarily reverting the branch order
      locally, observing the test fail, then restoring the fix and observing it
      pass; record this revert/restore proof in this plan's SUMMARY.md.
    - `php artisan test` (full suite) has no new failures beyond this file.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Quote/reviewed-data free text -> `suggestHandlingMethod()` | Engineer-entered or quote-imported item descriptions are parsed by regex/keyword matching. Untrusted in the sense of "not schema-validated," but sourced from the same authenticated internal quote/review pipeline as before this plan — no new external input surface is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-27-02 | Tampering | A description crafted to contain both "mount" and "display" keywords in an order that defeats the new branch ordering | accept | Low severity — worst case is a mount item receiving display-band wording (the pre-existing bug), not a security bypass. The branch reorder is the mitigation; residual risk is a wording-quality issue, not a safety-critical miscalculation, since mount items still receive SOME manual-handling text, never none. |
| T-27-05 | Repudiation / Information Disclosure | `HazardTemplateSeeder` re-seed on live only upserts by name (non-destructive, established Phase 26 convention) | accept | Existing, proven pattern (26-VERIFICATION.md's deploy runbook) — no new risk introduced by re-sourcing two bullet strings. |
</threat_model>

<verification>
`php artisan test --filter=RamsComplianceUpgradeService` (all files matching, not
just the new one) plus `php artisan test` full suite before merge, confirming no
regression in `AccessEquipmentContradictionTest`, `ProjectSpecificRisksGatedTest`,
`RamsComplianceUpgradeServiceCacheTest`, or `MethodStatementAssociatedRisksTest` —
all of which exercise adjacent parts of the same `upgrade()` pipeline.
</verification>

<success_criteria>
`suggestHandlingMethod()` no longer contains the ≥85"/≥65" ladder or the
aid-discharges-operative wording; the mount/bracket branch runs before the display
branch; `deriveMaterialHandling()` scans `scope_items.decommission` and produces an
explicit wall-mount-removal statement for display items found there; the seeded
Manual handling hazard reads from `DisplayLiftPolicy`; all proven by
`RamsComplianceUpgradeServiceDisplayLiftTest` with an explicit revert/restore
non-vacuity check recorded in the plan's SUMMARY. RULE-12's weight-derivation clause
is recorded as explicitly deferred, with the evidence, in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/27-manual-handling-display-lift-house-rules/27-02-SUMMARY.md` when done
</output>
