---
phase: 27-manual-handling-display-lift-house-rules
plan: 08
type: execute
wave: 5
depends_on: ["27-07"]
files_modified:
  - database/migrations/
  - app/Services/RamsBuilderService.php
  - app/Services/Rams/ControlTextRuleViolations.php
  - app/Http/Controllers/RamsReviewController.php
  - app/Services/RamsDataBuilderService.php
  - app/Services/RamsReviewDataService.php
  - app/Services/RamsExtractionDraftBuilderService.php
  - resources/views/rams/review.blade.php
  - tests/Unit/Services/Rams/ControlTextRuleViolationsTest.php
  - tests/Feature/Rams/ReviewedControlRefreshTest.php
autonomous: true
requirements: [RULE-02, RULE-13]

must_haves:
  truths:
    - "Regenerating a RAMS whose reviewed hazard controls were never edited by an engineer replaces those controls with the current library text"
    - "Controls an engineer actually edited survive a regeneration unless they violate a known house rule"
    - "Controls that violate a known house rule are replaced even when the engineer edited them, and the replacement is visible, not silent"
    - "Regenerating 21CQ30960 no longer emits 'over 20 kg' or 'screens and equipment over 40\\\" — minimum two persons'"
  artifacts:
    - path: "app/Services/Rams/ControlTextRuleViolations.php"
      provides: "Registry of detectable house-rule violations in free-text control lines, extensible by later phases"
    - path: "app/Services/RamsBuilderService.php"
      provides: "reviewedToRisk() control precedence gated on a controls_reviewed marker, mirroring score_reviewed"
  key_links:
    - from: "app/Services/RamsBuilderService.php::reviewedToRisk()"
      to: "app/Services/Rams/ControlTextRuleViolations.php"
      via: "tier-1 violation check on engineer-edited control text"
      pattern: "ControlTextRuleViolations::"
    - from: "app/Http/Controllers/RamsReviewController.php"
      to: "app/Services/RamsBuilderService.php::reviewedToRisk()"
      via: "controls_reviewed marker persisted from the review form, mirroring score_reviewed at :246"
      pattern: "controls_reviewed"
---

<objective>
Close Blocker 1 from `27-VERIFICATION.md`: the hazard library's control text never reaches a
RAMS regenerated from existing reviewed data, which is most real jobs.

**The evidence.** Live regeneration of 21CQ30960 (RAMS 100, 2026-08-26) rendered the Manual
Handling hazard as *"Use mechanical aids … for items **over 20 kg**"* and *"Team lift
required for **screens and equipment over 40"** — minimum two persons"*. Neither is the
pre-Phase-27 seeded text nor the corrected text verified in the live `hazard_templates`
table the same day — it is stale `reviewed_data` passed straight through. The surviving text
violates RULE-13 (banned kg threshold) and contradicts RULE-02 (a 40″ display is a
single-operative lift under the agreed bands).

**The cause.** `reviewedToRisk()` maps reviewed hazards 1:1 and calls `resolveFromSeeds()`
only to fill *missing* controls. Plan 26-08 made it replace controls on a **genuine rename**;
`Manual handling` already matches the canonical library name, so no rename fires and the
legacy controls survive.

**Why this matters beyond Phase 27.** Phases 28 and 31 correct house-rule text in the same
library, reached by the same path. Unless this is fixed first, their corrections will be
defeated the same way, on the same jobs, and the same green test suite will hide it.

**The policy (user decision, 2026-08-26), a two-tier union:**
1. **Replace where the reviewed text violates a known house rule** — always, engineer-edited
   or not. A house rule is a settled safety position, not a preference.
2. **Replace whenever the engineer never edited the controls** — the library has moved on and
   there is no human intent to protect.
3. Otherwise the engineer's text stands.

**The provenance mechanism — mirror `score_reviewed`, do not invent one.** The codebase has
already solved this exact question for scores: `RamsBuilderService::reviewedToRisk()` gates
score precedence on a `score_reviewed` marker, and its own comment states the rule —
*"No human ever touched this score through the numeric UI (score_reviewed false or absent) —
the library's typical value wins outright."* Controls get the symmetric `controls_reviewed`
marker, set the same way, read the same way. This also handles legacy documents correctly:
RAMS 99/100 carry no marker, so the library wins and the stale text is replaced.

Output: a `ControlTextRuleViolations` registry; `controls_reviewed` threaded through the
review form, the four builder/DTO services and `reviewedToRisk()`; tests proving all three
policy branches; and a regeneration of 21CQ30960 that no longer carries the banned text.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-manual-handling-display-lift-house-rules/27-VERIFICATION.md
@.planning/phases/27-manual-handling-display-lift-house-rules/27-CONTEXT.md
@.planning/reference/SKILL-RESYNC-2026-08-26.md
</context>

<interfaces>
<!-- Confirmed by direct code read 2026-08-26. Read current state before editing. -->

`app/Services/RamsBuilderService.php::reviewedToRisk()`
- `:445` — `$controls = (array) ($h['control_measures'] ?? []);`
- `:466-471` — the docblock stating the current rename/replace rule
- `:483-508` — the `score_reviewed` precedence branch. **Copy this shape exactly** for controls.

`score_reviewed`'s existing thread — `controls_reviewed` must follow every one of these:
- `app/Http/Controllers/RamsReviewController.php:246` — `($h['score_reviewed'] ?? '0') === '1'`
- `app/Services/RamsDataBuilderService.php:454`
- `app/Services/RamsReviewDataService.php:202`
- `app/Services/RamsExtractionDraftBuilderService.php:174` — seeded `false`
- `app/Services/RiskTemplateResolverService.php:269` — injected candidates force `false`

`resources/views/rams/review.blade.php` — the hazard rows carrying the score inputs and the
existing `score_reviewed` hidden field; the controls textarea lives in the same row.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ControlTextRuleViolations registry</name>
  <files>app/Services/Rams/ControlTextRuleViolations.php</files>
  <read_first>
    app/Services/Rams/DisplayLiftPolicy.php (violatesPolicy(), and the parser this registry reuses)
    app/Services/Rams/RamsComplianceUpgradeService.php (parseStatedTeamSize()/parseStatedInches() from Plan 27-06 — the free-text parsers; decide whether to move them here or call them)
    app/Services/Rams/LegacyHazardNameFoldMap.php (the final/all-static class shape to mirror)
    .planning/reference/21cav-rams-skill/references/hazard-library.md (Manual handling control 1 — the RULE-13 source)
  </read_first>
  <behavior>
    - `final class ControlTextRuleViolations`, all-static, mirroring `LegacyHazardNameFoldMap`'s shape.
    - `public static function detect(string $control): ?string` returns a short machine-readable
      violation key, or null when the line is clean. Implement exactly two detectors this plan:
      - **`kg_threshold`** (RULE-13) — the line states a fixed kg lifting threshold, e.g.
        "over 20 kg", "under 20kg", "above 25 kg". Match a number adjacent to a kg unit in a
        lifting context. Do NOT flag a line merely mentioning a weight as indicative
        (e.g. "panel weighs approximately 32 kg — confirm at survey"), which is permitted.
      - **`size_conditional_lift`** (RULE-02) — the line makes the lift team conditional on a
        screen size in a way that disagrees with `DisplayLiftPolicy`, e.g. "screens and
        equipment over 40\" — minimum two persons". Reuse `DisplayLiftPolicy`/the 27-06 parsers
        for the numbers; this class must not re-encode the bands.
    - `public static function detectAll(array $controls): array` maps index => violation key for
      every offending line.
    - **Extensible by later phases.** Detectors live in one ordered list with a documented shape
      so Phase 28 can add `ffp2` and `confined_space`, and Phase 31 `coshh_wrong_category`,
      without touching `reviewedToRisk()`. Say so in the docblock.
    - **Conservative by construction.** A line it cannot confidently classify is clean. A false
      positive silently overwrites an engineer's deliberate wording on a live safety document;
      a false negative leaves today's behaviour. Prefer the false negative.
  </behavior>
  <action>
    Create `app/Services/Rams/ControlTextRuleViolations.php` implementing `<behavior>`. Reuse
    `RamsComplianceUpgradeService`'s `parseStatedTeamSize()`/`parseStatedInches()` rather than
    writing new parsers — if they must become shared, move them onto this class and have
    `RamsComplianceUpgradeService` call them, keeping exactly one implementation. Do not
    re-encode `DisplayLiftPolicy`'s band numbers anywhere in this file;
    `DisplayLiftPolicySourceGuardTest`'s allow-list must be updated if this file legitimately
    needs to reference the policy.
  </action>
  <verify>
    <automated>php artisan test --filter=ControlTextRuleViolationsTest</automated>
  </verify>
  <done>The registry exists, detects both violations, and every acceptance criterion passes.</done>
  <acceptance_criteria>
    - `detect('Use mechanical aids (sack trucks, lifting trolleys) for items over 20 kg.')` returns `'kg_threshold'`.
    - `detect('Team lift required for screens and equipment over 40" — minimum two persons.')` returns `'size_conditional_lift'`.
    - `detect('Panel weighs approximately 32 kg — confirm at survey.')` returns `null` (indicative weight, permitted).
    - `detect('Wear appropriate gloves and safety footwear at all times.')` returns `null`.
    - Every control line the current `HazardTemplateSeeder` emits for every one of its 18 hazards returns `null` — the app must never flag its own corrected library text. Assert this by iterating the seeder's `standardHazards()`, not by hand-listing strings.
    - Every sentence `DisplayLiftPolicy::forSize()` and `::genericBandSummary()` can emit returns `null`.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: controls_reviewed marker + two-tier precedence in reviewedToRisk()</name>
  <files>app/Services/RamsBuilderService.php, app/Http/Controllers/RamsReviewController.php, app/Services/RamsDataBuilderService.php, app/Services/RamsReviewDataService.php, app/Services/RamsExtractionDraftBuilderService.php, resources/views/rams/review.blade.php</files>
  <read_first>
    app/Services/RamsBuilderService.php (:440-520 — reviewedToRisk()'s control and score handling in full)
    app/Http/Controllers/RamsReviewController.php (:210-260 — the hazard row shape and score_reviewed at :246)
    app/Services/RamsDataBuilderService.php (:445-465)
    app/Services/RamsReviewDataService.php (:195-210)
    app/Services/RamsExtractionDraftBuilderService.php (:165-180)
    resources/views/rams/review.blade.php (the hazard row: controls textarea + the score_reviewed hidden field beside it)
  </read_first>
  <behavior>
    - A `controls_reviewed` boolean is threaded through every place `score_reviewed` already
      travels, with identical semantics and identical defaults (absent === false).
    - The review form sets it **true only when the engineer actually changed the controls
      textarea** — not merely on save. Mirror however `score_reviewed` distinguishes a touch
      from a no-op save; if it does so client-side, do the same, and if it cannot, compare the
      submitted controls against the loaded ones server-side and set the flag on a genuine
      difference. State which mechanism was used in the SUMMARY.
    - `reviewedToRisk()`'s control precedence becomes, in order:
      1. If `ControlTextRuleViolations::detectAll($controls)` is non-empty → **replace the whole
         control list with the library template's controls**, regardless of `controls_reviewed`.
         Record the violation keys on the row (e.g. `controls_replaced_reason`) so the
         replacement is inspectable rather than silent.
      2. Else if `controls_reviewed !== true` → replace with the library template's controls
         (the library has moved on; no human intent to protect). This subsumes the existing
         genuine-rename replacement, which becomes redundant — remove it rather than leaving
         two overlapping rules.
      3. Else keep the engineer's controls untouched.
    - All three branches require `$tpl !== null`. With no matched template there is nothing to
      replace from — keep the reviewed controls and do not throw.
    - Empty reviewed controls keep their existing behaviour (filled from the library).
  </behavior>
  <action>
    Add `controls_reviewed` alongside `score_reviewed` at each of the five interfaces listed
    above and in the review blade. Rewrite `reviewedToRisk()`'s control handling to the ordered
    precedence in `<behavior>`, calling `ControlTextRuleViolations::detectAll()` for tier 1 and
    reading `controls_reviewed` for tier 2, and delete the now-redundant replace-on-rename rule
    (update the `:466-471` docblock, which will otherwise describe behaviour that no longer
    exists). Add `controls_replaced_reason` to the emitted row when tier 1 fires.
  </action>
  <verify>
    <automated>php artisan test --filter=ReviewedControlRefreshTest</automated>
  </verify>
  <done>All three precedence branches behave as specified and every acceptance criterion passes.</done>
  <acceptance_criteria>
    - A reviewed hazard named `Manual handling` with controls `['Use mechanical aids … for items over 20 kg.', 'Team lift required for screens and equipment over 40" — minimum two persons.']` and NO `controls_reviewed` key regenerates with the current library controls, and the output contains neither `over 20 kg` nor `over 40`.
    - The same row with `controls_reviewed => true` STILL regenerates with the library controls, because tier 1 fires on the violations — and the row carries a `controls_replaced_reason` naming `kg_threshold` and `size_conditional_lift`.
    - A reviewed hazard with `controls_reviewed => true` and clean, non-violating custom controls (e.g. `['Site-specific: use the goods lift in Block C only.']`) regenerates with those controls **unchanged**.
    - The same clean custom controls with `controls_reviewed` absent are REPLACED by the library controls.
    - A reviewed hazard whose name matches no library template keeps its controls and does not throw.
    - `score_reviewed` behaviour is unchanged by this plan — assert an existing score-precedence test still passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: proof, including the real 21CQ30960 vocabulary</name>
  <files>tests/Unit/Services/Rams/ControlTextRuleViolationsTest.php, tests/Feature/Rams/ReviewedControlRefreshTest.php</files>
  <read_first>
    tests/Feature/Rams/ReviewedHazardTieringTest.php (the real-seeded-DB, real-resolver pattern; and its test_real_legacy_vocabulary_folds_dedupes_and_restores_library_scores, the model for the live-evidence test below)
    tests/Unit/Services/Rams/DisplayLiftGateEngineerRowsTest.php (discrete-case unit style)
    .planning/phases/27-manual-handling-display-lift-house-rules/27-VERIFICATION.md (the exact live text to fixture)
  </read_first>
  <behavior>
    - `ControlTextRuleViolationsTest` covers Task 1's criteria as discrete cases, including the
      two self-check criteria (seeder text and DisplayLiftPolicy sentences never flagged).
    - `ReviewedControlRefreshTest` covers Task 2's three precedence branches against a real
      seeded DB and the real resolver.
    - **One test uses the REAL live-evidence vocabulary** captured from RAMS 100 in
      `27-VERIFICATION.md` — the exact two control lines — and proves a regeneration emits
      neither. This mirrors `ReviewedHazardTieringTest`'s real-vocabulary test, which is what
      finally closed HAZ-02 after two premature closures.
    - Non-vacuity: revert Task 2's tier-1 branch, confirm the violation tests fail; restore.
      Revert the tier-2 branch, confirm the never-edited test fails; restore. Record both
      failure counts in the SUMMARY.
  </behavior>
  <action>
    Create both test files covering every acceptance criterion from Tasks 1 and 2. Include the
    real-vocabulary regeneration test described in `<behavior>`. Run both revert-and-restore
    non-vacuity checks and record the results.
  </action>
  <verify>
    <automated>php artisan test tests/Unit/Services/Rams tests/Feature/Rams</automated>
  </verify>
  <done>Both filters green, both non-vacuity checks recorded, no new failures across the Rams suites.</done>
  <acceptance_criteria>
    - `php artisan test tests/Unit/Services/Rams tests/Feature/Rams` exits 0 with no new failures against the 280-test baseline.
    - A test named for the real 21CQ30960 vocabulary asserts a regeneration emits neither `over 20 kg` nor `over 40`.
    - Reverting tier 1 fails at least the two violation tests; reverting tier 2 fails at least the never-edited test. Both counts recorded in 27-08-SUMMARY.md.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: backfill controls_reviewed on existing documents</name>
  <files>database/migrations/</files>
  <read_first>
    app/Models/RamsDocument.php (the reviewed_data cast and column type)
    database/migrations/ (the most recent migration — naming and style convention)
    app/Services/RamsBuilderService.php (the finished Task 2 precedence, so the backfill's effect is understood)
  </read_first>
  <behavior>
    - A migration sets `controls_reviewed = true` on every hazard row inside every existing
      `rams_documents.reviewed_data['hazards']` that does not already carry the key.
    - **Rationale, recorded in the migration docblock:** without it, tier 2 treats every legacy
      document as never-reviewed and replaces engineer-authored controls on the next
      regeneration. Tier 1 still corrects rule-violating text regardless of the marker, so the
      safety fix is not weakened — only the collateral is avoided. User decision 2026-08-26.
    - Rows that already carry `controls_reviewed` are left alone. Documents with no
      `reviewed_data`, no `hazards` key, or a non-array shape are skipped without error.
    - `down()` removes the key **only** from rows the migration added it to, or is a documented
      no-op if that cannot be distinguished — say which in the docblock rather than silently
      writing an irreversible migration.
    - Chunked over `rams_documents` rather than loading all rows; this runs against production.
  </behavior>
  <action>
    Create a migration implementing `<behavior>`. Iterate `rams_documents` in chunks, mutate the
    decoded `reviewed_data['hazards'][]` entries to add `controls_reviewed => true` where the key
    is absent, and write back. Report the number of documents and hazard rows touched via the
    migration's output so the production run is auditable.
  </action>
  <verify>
    <automated>php artisan test --filter=ReviewedControlRefreshTest</automated>
  </verify>
  <done>Existing documents carry the marker, new ones are unaffected, and the criteria below pass.</done>
  <acceptance_criteria>
    - After migrating, a `RamsDocument` seeded with `reviewed_data.hazards[0]` lacking `controls_reviewed` has it set to `true`.
    - A hazard row that already had `controls_reviewed => false` still has `false` — the migration does not overwrite an explicit value.
    - A `RamsDocument` with `reviewed_data = null` is skipped without error.
    - A test proves the combined effect: a legacy-shaped document with clean custom controls keeps them after migration + regeneration, while one with the RAMS 100 violating controls has them replaced.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
ASVS L1. The surface is integrity of a safety document and the risk of overwriting deliberate
human input.

| ID | Threat | Severity | Mitigation |
|---|---|---|---|
| T-27-08-01 | **A false positive silently overwrites an engineer's deliberate control wording** on a live safety document — the engineer's site-specific knowledge is lost and they may not notice. | HIGH | Detectors are conservative by construction (Task 1 `<behavior>`); a line that cannot be confidently classified is clean. Two self-check acceptance criteria prove the app never flags its own library or policy text. Tier 1 records `controls_replaced_reason` so the replacement is inspectable, not silent. |
| T-27-08-02 | **Tier 2 would wipe engineer edits made before `controls_reviewed` existed.** Every pre-existing document lacks the marker, so genuine engineer customisation on a legacy RAMS would be replaced on the next regeneration. | HIGH | **RESOLVED by user decision 2026-08-26 — Task 4 backfills `controls_reviewed = true` on all existing reviewed hazards.** Legacy engineer wording is preserved; tier 1 still corrects the actual defect, because the `over 20 kg` and `over 40"` lines are replaced for *violating a rule*, which ignores the marker. Tier 2 therefore governs only documents reviewed from this point on. Rejected alternative: no backfill (guarantees no stale text anywhere, but silently discards site-specific engineer controls, irreversibly per document). |
| T-27-08-03 | **A regression in `score_reviewed`** while threading a parallel marker through the same five interfaces. | MEDIUM | An acceptance criterion requires an existing score-precedence test to still pass. |
| T-27-08-04 | Detector regexes run over engineer-supplied free text. | LOW | Match-and-classify only; no evaluation, no interpolation into a query or unescaped render. |
</threat_model>

<verification>
1. `php artisan test tests/Unit/Services/Rams tests/Feature/Rams` exits 0, no new failures.
2. Both revert-and-restore non-vacuity checks recorded.
3. A regeneration fixtured on RAMS 100's real control text emits neither banned string.
4. Engineer-edited, rule-clean controls survive a regeneration.
5. `score_reviewed` behaviour demonstrably unchanged.
6. T-27-08-02's legacy-document consequence is stated in 27-08-SUMMARY.md.
7. **Live re-verification required before this plan is called done** — regenerate 21CQ30960
   again and confirm the Manual Handling hazard carries library text. HAZ-02 was closed
   prematurely twice on automated evidence; do not repeat it.
</verification>
