---
phase: 27-manual-handling-display-lift-house-rules
plan: 05
type: execute
wave: 4
depends_on: ["27-02", "27-03", "27-04"]
files_modified: []
autonomous: false
requirements: [GATE-09]

must_haves:
  truths:
    - "Regenerating 21CQ30960 on rams.21stcav.com against real production data does not trip GATE-09"
    - "The regenerated document's §6.7 table and Manual Handling hazard row read the corrected 1/2/3-inch bands, not the removed 4-person/3-person ladder"
    - "The fix and the gate agree against real data, not just a fixture — this is the phase's fourth success criterion and cannot be satisfied by automated tests alone"
  artifacts: []
  key_links:
    - from: "rams.21stcav.com (live VPS, deployed as stcav)"
      to: "RamsDocument 98/99 (21CQ30960) DOCX download"
      via: "regenerate button -> BuildRamsDocumentJob -> RamsComplianceUpgradeService::upgrade() -> DocxBuilderService::buildRiskAssessment()"
      pattern: "STATUS_FAILED"
---

<objective>
Deploy Plans 27-01 through 27-04's code to `rams.21stcav.com`, re-seed
`HazardTemplateSeeder` (upsert-by-name, non-destructive — the same runbook Phase 26
used), regenerate 21CQ30960 (VW Blakelands, currently RAMS 98), and confirm on the
live-rendered DOCX that GATE-09 does not fire and the §6.7 table / Manual Handling
hazard row read the corrected bands.

Purpose: ROADMAP's fourth Phase 27 success criterion is explicit that this cannot be
proven on a fixture — "the fix and the gate agree against real data, not just a
fixture." This milestone's standing decision (Phase 26 precedent) is that safety
content is validated on live production data, not staging.

Output: a deployed, re-seeded live application; a regenerated 21CQ30960 DOCX
inspected and confirmed clean; a rollback-flag smoke test proving
`RAMS_DISPLAY_LIFT_GATE=false` is a genuine one-`.env`-edit-and-`config:clear`
off-switch, mirroring Phase 26's proven `RAMS_HAZARD_LIBRARY_TIERING=false` rollback
proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-manual-handling-display-lift-house-rules/27-CONTEXT.md
@.planning/phases/26-hazard-library-structural-inversion/26-VERIFICATION.md
</context>

<current_state>
`26-VERIFICATION.md`'s "Deploy" and "Rollback proof" sections (`:19-40`, `:307-331`)
are the exact runbook this plan follows: deploy as `stcav` (never `root`) to
`/home/stcav/rams.21stcav.com.git/rams.21stcav.com/`, push to both `rams-live` and
`origin`, run `php artisan db:seed --class=HazardTemplateSeeder --force` followed by
`php artisan optimize:clear`, then prove the rollback flag with an `echo ... >> .env
&& php artisan config:clear` / revert cycle — no redeploy required for the rollback
proof itself. 21CQ30960 is currently at RAMS 98, verified clean for Phase 26 on
2026-08-25.
</current_state>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Deploy Phase 27 code + reseed on live</name>
  <files>none — deployment and database reseed on the live VPS, no repository files modified by this task itself</files>
  <action>
    As user `stcav` (never `root`), pull the merged Phase 27 commits (Plans
    27-01..27-04) to `/home/stcav/rams.21stcav.com.git/rams.21stcav.com/` on both
    the `rams-live` and `origin` remotes, matching `26-VERIFICATION.md`'s deploy
    shape exactly. Run `php artisan db:seed --class=HazardTemplateSeeder --force`
    (non-destructive upsert-by-name — Plan 27-02's re-sourced Manual handling
    control text lands here) followed by `php artisan optimize:clear`. Confirm
    the local test suite was fully green (`php artisan test`) before this deploy
    step is executed.
  </action>
  <what-built>
    Plans 27-01 through 27-04's code: `DisplayLiftPolicy`, the corrected
    `suggestHandlingMethod()` bands and RULE-12 branch order, the RULE-03
    decommission-scan, GATE-09's `enforceDisplayLiftGate()` + `RAMS_DISPLAY_LIFT_GATE`
    flag, the re-sourced `HazardTemplateSeeder` Manual handling row, and worksheet
    parity in `SafetyProfileService`/`MethodStatementService`.
  </what-built>
  <how-to-verify>
    1. Confirm the pull landed the expected commit range on both remotes.
    2. Confirm the reseed log line shows the Manual handling row updated in place
       (18 global hazards total, unchanged count from Phase 26 — this reseed only
       changes control text, not hazard count).
    3. Confirm `php artisan optimize:clear` completed without error.
  </how-to-verify>
  <resume-signal>Type "deployed" once the pull, reseed, and optimize:clear steps are confirmed on live, or describe any deploy error encountered.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Regenerate 21CQ30960 and inspect the live DOCX</name>
  <files>none — live document inspection only</files>
  <action>
    On `rams.21stcav.com`, open the 21CQ30960 (VW Blakelands) RAMS record and
    trigger a regenerate. Download the resulting DOCX and inspect §6.7 Material
    Handling and the Manual Handling hazard row against the corrected bands.
  </action>
  <what-built>
    Automated: the code change that should make this regeneration produce
    conforming, GATE-09-clean output.
  </what-built>
  <how-to-verify>
    1. Confirm `RamsDocument.status` does NOT become `FAILED` and no
       `error_message` is set — this is the direct GATE-09-does-not-trip proof.
    2. Open §6.7 Material Handling and confirm every display-lift row states
       1/2/3 operatives per the corrected bands (no "minimum 4 persons", no
       "minimum 3 persons recommended... panel-lift trolley" wording) and that
       any wall-mounted-display decommission/strip-out item (if 21CQ30960's
       scope includes one) explicitly states the lowest-practicable-height /
       one-operative-each-side removal sequence.
    3. Open the Manual Handling hazard row (wherever the hazard register renders
       in this DOCX) and confirm its control text no longer reads "minimum two
       operatives for every panel size" and does contain the wall-mount-removal
       sentence.
    4. If any check fails, do NOT proceed to Task 3 — report the specific failing
       row/sentence so a gap-closure plan can be scoped.
  </how-to-verify>
  <resume-signal>Type "confirmed clean" with the RAMS document ID/version inspected, or describe the specific defect found.</resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Rollback-flag smoke test</name>
  <files>none — .env edit + config cache clear on the live VPS only</files>
  <action>
    On the live server, append `RAMS_DISPLAY_LIFT_GATE=false` to `.env` and run
    `php artisan config:clear` (no redeploy). Confirm `config('rams_tier1.
    display_lift_gate_enabled')` now evaluates false. Remove the appended line
    from `.env` and run `php artisan config:clear` again to restore the
    default-on state, mirroring `26-VERIFICATION.md`'s `sed -i '/^RAMS_HAZARD_
    LIBRARY_TIERING=false$/d' .env && php artisan config:clear` pattern exactly,
    substituting the new variable name.
  </action>
  <what-built>
    `RAMS_DISPLAY_LIFT_GATE` env flag gating GATE-09's throw behaviour.
  </what-built>
  <how-to-verify>
    1. Confirm the flag flips `config('rams_tier1.display_lift_gate_enabled')` to
       false (e.g. via `php artisan tinker` or an equivalent one-line check).
    2. Confirm the flag is back to its default-true state after restoration.
  </how-to-verify>
  <resume-signal>Type "rollback proof closed" once the flip-and-restore cycle is confirmed, or describe any discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| VPS shell access (stcav user) -> `.env` | Only whoever holds SSH access to the production VPS can flip `RAMS_DISPLAY_LIFT_GATE`. Not a request-path input. |
| Live regeneration -> real client-facing document | This task's entire purpose is confirming a real, currently-in-use RAMS document is not wrongly blocked and is not silently wrong. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-27-07 | Denial of Service | Regenerating 21CQ30960 on live and finding GATE-09 DOES fire (a false positive against real data) | mitigate | Task 3's rollback-flag proof is the exact contingency — if Task 2 fails, `RAMS_DISPLAY_LIFT_GATE=false` restores generation immediately while a gap-closure plan is scoped, per this milestone's live-validation posture. |
</threat_model>

<verification>
Task 2's live DOCX inspection is the verification for this entire plan — there is no
separate automated check, per ROADMAP's explicit framing that this success
criterion "cannot be automated-test-only."
</verification>

<success_criteria>
21CQ30960 regenerates on live without tripping GATE-09; its §6.7 table and Manual
Handling hazard row read the corrected bands; the `RAMS_DISPLAY_LIFT_GATE` rollback
flag is proven functional and restored to its default-on state.
</success_criteria>

<output>
Create `.planning/phases/27-manual-handling-display-lift-house-rules/27-05-SUMMARY.md` when done
</output>
