---
phase: 27-manual-handling-display-lift-house-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Rams/DisplayLiftPolicy.php
  - tests/Unit/Services/Rams/DisplayLiftPolicyTest.php
autonomous: true
requirements: [RULE-02, RULE-03]

must_haves:
  truths:
    - "One PHP class resolves display-lift team size for any given inch value — no other file computes its own copy of the 14/55/90-inch thresholds"
    - "The class exposes an independent violation check (violatesPolicy) that a future gate can call without re-deriving the band logic"
    - "The class exposes the exact RULE-03 wall-mount-removal sentence as a single reusable string, so the seeder and any decommission-scan logic never diverge on wording"
  artifacts:
    - path: "app/Services/Rams/DisplayLiftPolicy.php"
      provides: "forSize(), violatesPolicy(), wallMountRemovalStatement(), genericBandSummary(), bands() — the D-03 single shared source"
      min_lines: 90
    - path: "tests/Unit/Services/Rams/DisplayLiftPolicyTest.php"
      provides: "Boundary-by-boundary proof of every band, the no-row exclusion, and the D-05 silent-floor case"
      min_lines: 60
  key_links:
    - from: "app/Services/Rams/DisplayLiftPolicy.php"
      to: "app/Services/Rams/RamsComplianceUpgradeService.php (Plan 27-02)"
      via: "static method call, no instantiation"
      pattern: "DisplayLiftPolicy::forSize\\("
---

<objective>
Create `App\Services\Rams\DisplayLiftPolicy` — the single PHP class that owns every
numeric threshold and every sentence this phase's house rules depend on (RULE-02's
amended bands, RULE-03's wall-mount-removal statement, and the independent
re-validation GATE-09 will call in Plan 27-03). This plan produces the class and its
unit tests only; no existing file is wired to read it yet (that is Plans 27-02/03/04).

Purpose: D-03 requires "one PHP class... every stating point reads it... the gate and
the generator must resolve the team size through the same call, so a future edit
cannot make them disagree." Every other Phase 27 plan depends on this class existing
first (interface-first ordering).

Output: `app/Services/Rams/DisplayLiftPolicy.php` (final class, all-static, no
instance state) + `tests/Unit/Services/Rams/DisplayLiftPolicyTest.php` (no DB, no
Laravel bootstrap needed beyond `Tests\TestCase`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-manual-handling-display-lift-house-rules/27-CONTEXT.md
@.planning/phases/27-manual-handling-display-lift-house-rules/27-RESEARCH.md
@.planning/phases/27-manual-handling-display-lift-house-rules/27-PATTERNS.md
@.planning/reference/21cav-rams-skill/references/house-rules.md
@CLAUDE.md
</context>

<interfaces>
<!-- The exact structural analog this class must mirror. Read the full file, not an
excerpt — every convention (const shape, docblock style, nullable-resolver pattern,
introspection accessor) transfers directly. -->

From `app/Services/Rams/LegacyHazardNameFoldMap.php` (full file, 129 lines):
- `final class LegacyHazardNameFoldMap` — no constructor, all-static.
- `private const MAP = [...]` holding the settled data, with an exhaustive
  provenance docblock above the class explaining where every entry came from
  (git commits, plan IDs, live evidence) — DO NOT skip this docblock; it is this
  project's established defence against a future reader mistaking a settled
  divergence for drift (see `26-VERIFICATION.md`'s repeated reopenings, which this
  exact documentation style was built to prevent).
- `public static function canonicalName(string $legacyName): ?string` — the
  nullable-or-value resolver shape (lines 107-116).
- `public static function all(): array` — public introspection accessor for tests
  (lines 125-128).
</interfaces>

<current_state>
The bands this class encodes do not exist anywhere as a single source today. The
nearest analog is the ladder inside
`app/Services/Rams/RamsComplianceUpgradeService.php::suggestHandlingMethod()`
(`:1252-1265`), which this plan does NOT touch — Plan 27-02 rewires that method to
call this new class. This plan only creates the class and proves it in isolation.
</current_state>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write DisplayLiftPolicy — bands, gate check, and statements</name>
  <files>app/Services/Rams/DisplayLiftPolicy.php</files>
  <read_first>
    app/Services/Rams/LegacyHazardNameFoldMap.php (full file — structural analog)
    .planning/phases/27-manual-handling-display-lift-house-rules/27-CONTEXT.md (D-01 correction block — the governing bands; D-02; D-03; D-05)
    .planning/reference/21cav-rams-skill/references/house-rules.md (:6-19 — the unamended wall-mount-removal sequence, still authoritative)
    .planning/phases/27-manual-handling-display-lift-house-rules/27-PATTERNS.md (Pattern Assignments section for DisplayLiftPolicy.php — the nullable-or-array return shape and the "three outcomes not two" correction)
  </read_first>
  <behavior>
    - `forSize(?float $inches, bool $isSmallControlPanel = false): ?array` returns
      `null` only when `$isSmallControlPanel === true` AND `$inches !== null` AND
      `$inches <= 14` (the existing scheduling/touch/booking/control-panel
      exclusion — an existing behaviour this class now also exposes, not a new
      rule). Every other input returns `['min_persons' => int, 'sentence' =>
      string]`.
    - `$inches === null` (D-05, unresolvable size) returns the 2-operative band
      silently — same shape, same sentence as the 55-90" band, no flag, no
      distinguishing marker in the return value.
    - `$inches !== null && $inches < 55` returns `min_persons => 1`.
    - `$inches !== null && $inches >= 55 && $inches <= 90` returns `min_persons =>
      2`.
    - `$inches !== null && $inches > 90` returns `min_persons => 3`. The sentence
      for this band states the third operative as a floor — it must NOT contain
      any "may lift if using a panel-lift trolley" or equivalent aid-discharges-a-
      person construction (D-02).
    - `violatesPolicy(int $statedPersons, ?float $inches): bool` is an
      INDEPENDENT re-check — it must not call `forSize()` internally, must not
      share a code path with it beyond the same three numeric constants. Returns
      `true` when: `$statedPersons >= 4` (never 4+, any size); OR `$inches !==
      null && $inches > 90 && $statedPersons < 3` (below-floor above 90"); OR
      `$inches !== null && $inches >= 55 && $statedPersons === 1` (1 operative at
      55" or larger). Returns `false` for every other combination, including
      `$inches === null` regardless of `$statedPersons` being 1, 2, or 3 (D-05 —
      an unresolvable size is never a gate error) and including `$statedPersons
      === 1 && $inches !== null && $inches < 55` (the corrected single-operative
      band is correct output, not a defect).
    - `wallMountRemovalStatement(): string` returns the RULE-03 sentence verbatim
      matching `house-rules.md:13-16`'s required sequence: controlled to the
      lowest practicable height, one operative each side, before release from the
      mount, stated as the highest-risk lift on a strip-out.
    - `genericBandSummary(): string` returns a single sentence usable in static
      contexts (the DB seeder, the method-statement fallback) that cannot
      reference one specific inch value — it must state all three numeric
      thresholds (14, 55, 90) and the never-4 rule in prose.
    - `bands(): array` returns the ordered band table (an array of
      `['min_inches' => ?float, 'max_inches' => ?float, 'min_persons' => int]`
      entries covering the 1/2/3 bands, for test introspection) — mirrors
      `LegacyHazardNameFoldMap::all()`'s role.
  </behavior>
  <action>
    Create `final class DisplayLiftPolicy` in `App\Services\Rams`, all-static
    methods, no constructor, no instance ever created. Define named constants for
    every numeric threshold: `SMALL_PANEL_MAX_INCHES = 14`,
    `SINGLE_OPERATIVE_MAX_INCHES = 55` (the exclusive upper bound of the 1-operative
    band — at exactly 55 the 2-operative band applies), `TEAM_LIFT_MAX_INCHES = 90`
    (the inclusive upper bound of the 2-operative band), `MIN_OPERATIVES_UNDER_55 =
    1`, `MIN_OPERATIVES_55_TO_90 = 2`, `MIN_OPERATIVES_ABOVE_90 = 3`,
    `MAX_OPERATIVES_NEVER_EXCEED = 3`. Write an extensive class-level provenance
    docblock recording: the original unamended `house-rules.md:8-11` two-operative-
    regardless-of-size position; the first amendment to a two-operative floor with
    a 3-operative ladder above 90"; the 2026-08-25 correction reinstating the
    single-operative band below 55" after research found `SafetyProfileService` had
    no equivalent of the ≤14" exclusion; and the final settled bands table. Cite
    `27-CONTEXT.md` D-01 and its correction block by name, and `REQUIREMENTS.md`
    RULE-02's amendment note, so a future reader does not mistake the final 1/2/3
    bands for drift from either the skill or an earlier Phase 27 draft. Implement
    the five public methods per `<behavior>` above. Do not call any regex, do not
    accept a raw description string anywhere in this class — inch resolution and
    keyword/category classification (deciding whether an item IS a display, a
    control panel, or something else) stay the caller's responsibility; this class
    only turns a resolved inch value (plus one boolean flag) into a team size and a
    sentence.
  </action>
  <verify>
    <automated>php artisan test --filter=DisplayLiftPolicyTest</automated>
  </verify>
  <done>
    `app/Services/Rams/DisplayLiftPolicy.php` exists as a `final` class with zero
    non-static methods, exposing `forSize()`, `violatesPolicy()`,
    `wallMountRemovalStatement()`, `genericBandSummary()`, and `bands()`, and every
    acceptance criterion below passes.
  </done>
  <acceptance_criteria>
    - `App\Services\Rams\DisplayLiftPolicy` exists, is `final`, has zero non-static
      methods and no `__construct`.
    - `DisplayLiftPolicy::forSize(10.1, true)` returns `null`.
    - `DisplayLiftPolicy::forSize(43.0)` returns `['min_persons' => 1, ...]`.
    - `DisplayLiftPolicy::forSize(55.0)` and `DisplayLiftPolicy::forSize(90.0)` both
      return `['min_persons' => 2, ...]`.
    - `DisplayLiftPolicy::forSize(90.1)` returns `['min_persons' => 3, ...]`, and its
      `sentence` value does not contain the substring `panel-lift trolley` combined
      with any phrase implying the aid reduces the required headcount.
    - `DisplayLiftPolicy::forSize(null)` returns the same shape as
      `DisplayLiftPolicy::forSize(60.0)` (both `min_persons => 2`).
    - `DisplayLiftPolicy::violatesPolicy(4, 43.0) === true`,
      `DisplayLiftPolicy::violatesPolicy(2, 95.0) === true`,
      `DisplayLiftPolicy::violatesPolicy(1, 55.0) === true`.
    - `DisplayLiftPolicy::violatesPolicy(1, 43.0) === false`,
      `DisplayLiftPolicy::violatesPolicy(2, null) === false`,
      `DisplayLiftPolicy::violatesPolicy(3, null) === false`.
    - `DisplayLiftPolicy::wallMountRemovalStatement()` contains the substrings
      `lowest practicable height` and `one operative each side`.
    - `DisplayLiftPolicy::genericBandSummary()` contains `14`, `55`, and `90` as
      distinct numeric tokens.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DisplayLiftPolicyTest — boundary-by-boundary proof</name>
  <files>tests/Unit/Services/Rams/DisplayLiftPolicyTest.php</files>
  <read_first>
    app/Services/Rams/DisplayLiftPolicy.php (the class Task 1 just created — read the final signatures, do not assume)
    tests/Unit/Services/Rams/LegacyHazardNameFoldMapTest.php (full file — the exact test-shape analog: plain `Tests\TestCase`, no DB, one assertion style per case, plus one test iterating the introspection accessor)
  </read_first>
  <action>
    Create `Tests\Unit\Services\Rams\DisplayLiftPolicyTest extends Tests\TestCase`
    mirroring `LegacyHazardNameFoldMapTest`'s structure exactly: no `RefreshDatabase`,
    no factories, one focused `test_*` method per case. Cover: the ≤14" control-panel
    exclusion (null); a value just under 55 (1 operative); exactly 55 and exactly 90
    (2 operatives, both ends of the inclusive band); a value just over 90 (3
    operatives); the never-4 guard and the 1-at-55+ guard and the 2-above-90 guard,
    each via `violatesPolicy()`; the D-05 null-inches case for both `forSize()` (same
    shape as the 60" case) and `violatesPolicy()` (never a violation, for
    `statedPersons` 1, 2, and 3); the >90" sentence does not contain an
    aid-discharges-the-third-operative construction; `wallMountRemovalStatement()`
    and `genericBandSummary()` content assertions per Task 1's acceptance criteria;
    and one test iterating `bands()` asserting exactly three entries in ascending
    `min_inches` order with no gaps (55 immediately follows the first band's upper
    bound, 90 immediately follows the second).
  </action>
  <verify>
    <automated>php artisan test --filter=DisplayLiftPolicyTest</automated>
  </verify>
  <done>
    `tests/Unit/Services/Rams/DisplayLiftPolicyTest.php` exists and passes, with at
    least 10 test methods, none requiring the database.
  </done>
  <acceptance_criteria>
    - `php artisan test --filter=DisplayLiftPolicyTest` passes with at least 10
      distinct test methods.
    - Every acceptance criterion listed under Task 1 has a corresponding assertion
      in this test file (not merely exercised incidentally by another test).
    - No test in this file touches the database or requires `RefreshDatabase`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed in this plan | `DisplayLiftPolicy` is a pure, stateless, deterministic value class with no I/O, no database access, no external input surface. It is not yet wired into any request path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-27-01 | Tampering | `DisplayLiftPolicy`'s numeric constants being silently edited in a future unrelated change without updating the provenance docblock | accept | Low likelihood, low severity — this is an internal safety-content class with the established `LegacyHazardNameFoldMap` provenance-docblock convention as its social/process mitigation, not a code control. Plan 27-03's structural guard test (`DisplayLiftPolicySourceGuardTest`) is the actual technical control, but it is out of this plan's scope since nothing calls this class yet. |
</threat_model>

<verification>
`php artisan test --filter=DisplayLiftPolicy` — both the class and its test pass in
isolation. No other suite depends on this class yet (nothing calls it until Plan
27-02), so the full suite is unaffected by this plan.
</verification>

<success_criteria>
`app/Services/Rams/DisplayLiftPolicy.php` exists, is `final`, all-static, carries the
five methods specified, and every acceptance criterion in Task 1 is proven by a
passing test in `DisplayLiftPolicyTest.php`. No other file in the codebase yet
references `DisplayLiftPolicy` — that wiring is Plans 27-02/03/04.
</success_criteria>

<output>
Create `.planning/phases/27-manual-handling-display-lift-house-rules/27-01-SUMMARY.md` when done
</output>
