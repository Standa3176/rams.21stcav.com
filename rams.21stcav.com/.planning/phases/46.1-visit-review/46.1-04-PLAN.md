---
phase: 46.1-visit-review
plan: 04
type: execute
wave: 3
depends_on: ["46.1-02", "46.1-03"]
files_modified:
  - resources/views/components/cockpit/returned-tab.blade.php
  - tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php
  - tests/Feature/Cockpit/CockpitPanelTest.php
  - tests/Feature/Cockpit/CockpitReturnedTabTest.php
autonomous: false
requirements: [RV-04, RV-07, RV-08]
must_haves:
  truths:
    - "The Returned tab carries exactly one hand-off link, and it downloads every photo on the visit as one ZIP"
    - "The fence's `Download` entry is LIFTED BY NAME with a reason, not deleted; every other deferred affordance stays"
    - "The fence's row-count invariance now holds eleven tables still, including the four this phase reads"
    - "The fence has been re-proved capable of failing, three separate ways, in this commit"
  artifacts:
    - path: "tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php"
      provides: "DEFERRED_AFFORDANCES 19 -> 18 with the lift named; WRITE_SURFACE_TABLES 7 -> 11"
      contains: "LIFTED BY PLAN 46.1-04"
  key_links:
    - from: "resources/views/components/cockpit/returned-tab.blade.php"
      to: "routes/web.php"
      via: "route('projects.cockpit.visits.photos-zip') behind Route::has()"
      pattern: "photos-zip"
---

<objective>
Ship the hand-off link and pay for it at the fence, in one commit.

`CockpitReadOnlyFenceTest::DEFERRED_AFFORDANCES` bans the string `Download` and attributes it to
Phase 48. This phase ships a download, so that entry is **lifted by name, with the plan and the
requirement recorded at the entry** — exactly as Plan 46-04 lifted `<form`/`<input`/`<button` and
46-05 lifted `<textarea`. Nothing else is lifted. In the same commit the fence's row-count
invariance grows to cover the four evidence tables this phase reads, because the ZIP is a GET and
must write nothing.

Purpose: an affordance arriving and a fence entry leaving belong in the same change, so that the
fence is never weakened in a commit that ships nothing and never bypassed by a commit that ships
something.

Output: one anchor, four fence edits, two count assertions moved, and the three-way breakage
ritual re-run and recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46.1-visit-review/46.1-CONTEXT.md
@.planning/phases/46-visit-lifecycle/46-04-PLAN.md
@tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php

<interfaces>
The four fence structures and their current counts (verified 2026-09-21):
```php
private const FORBIDDEN_MARKUP = ['<select', '<script'];                      // assertCount(2)
private const DEFERRED_AFFORDANCES = [ /* 19 entries, incl. 'Download' => 'Phase 48' */ ]; // assertCount(19)
private const BANNED_HANDLER_ATTRIBUTES = [ /* onclick, wire:, x-on:, @click, x-data,
                                               x-show, x-init, x-if, x-text */ ];          // assertCount(9)
private const WRITE_SURFACE_TABLES = ['visits','install_records','install_programmes',
                                      'site_surveys','worksheets','snags',
                                      'project_activity_logs'];                            // assertCount(7)
```

A SECOND place bans the word, and it is not the fence —
`tests/Feature/Cockpit/CockpitPanelTest.php:144`:
```php
public function test_the_link_copy_is_view_and_never_download(): void
{
    $html = $this->panel($project, 'rams', 'files');
    $this->assertStringContainsString('>View<', $html);
    $this->assertStringNotContainsString('Download', $html);
}
```
It is scoped to the FILES tab, where the ruling still holds. It must be NARROWED and renamed, not
deleted.

From Plan 46.1-02:
```php
route('projects.cockpit.visits.photos-zip', ['project' => $project, 'visit' => $visit->id]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The hand-off link, in the slot Plan 46.1-03 left for it</name>
  <files>resources/views/components/cockpit/returned-tab.blade.php</files>
  <behavior>
    - The slot Plan 46.1-03 left empty now holds ONE anchor per sourced visit:
      `Download all photos (ZIP)`, `target="_blank"`, `rel="noopener"`, pointing at
      `projects.cockpit.visits.photos-zip`, guarded by `Route::has()` so a rename degrades to no
      link rather than a `RouteNotFoundException` that would blank the page — the rule
      `CockpitPanelPresenter` already follows for every document route.
    - Beneath it, one quiet sentence: "Grouped by room, into before / after / label folders." A PM
      who is about to drop this into Bitrix should know the shape before they open it.
    - The link is NOT rendered when the visit has zero photos — an offer that produces an archive
      containing only a README is a worse answer than the sentence "No photos have come back yet."
    - THE LINK SITS OUTSIDE `.cav-visit`. `CockpitVisitActionsTest::countControls()` counts
      `<button` and `<a ` INSIDE a visit row, and VL-11's cap of four is measured there. A hand-off
      is not a review control; putting it in the row would consume a quarter of the cap for
      something that is not one of D-02's four acts. A Blade comment at the render site says this,
      so a later tidy-up does not move it in.
    - Its position follows SCC's ordering, which the ROADMAP names as the contract: state → ZIP →
      room data → gallery → sign-off → controls. The hand-off is near the top; the review controls
      are at the bottom (Plan 46.1-05).
  </behavior>
  <action>
    Fill the named slot in `returned-tab.blade.php`. Use the existing `.cav-visit__control` /
    `.cav-visit__control--quiet` visual language rather than inventing a button style — it is the
    same panel and there is no new colour in this phase. Add no `position` to the anchor (the
    stretched-link trap).
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter CockpitReturnedTabTest</automated>
  </verify>
  <done>Exactly one ZIP anchor renders per sourced visit with photos, outside `.cav-visit`, and none renders for a visit with no photos.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The fence pays for it — four edits, two counts moved</name>
  <files>tests/Feature/Cockpit/CockpitReadOnlyFenceTest.php, tests/Feature/Cockpit/CockpitPanelTest.php, tests/Feature/Cockpit/CockpitReturnedTabTest.php</files>
  <behavior>
    - LIFTED, BY NAME, ONE ENTRY:
      `'Download' => 'Phase 48'` is REMOVED from `DEFERRED_AFFORDANCES`, and in its place a comment
      block in the file's existing voice records: lifted by **Plan 46.1-04**, requirement **RV-04**,
      because this phase ships the ZIP hand-off that D-03 calls the Bitrix integration. The comment
      names what was NOT lifted alongside it, so the next reader sees the boundary rather than
      inferring it.
    - STAYS BANNED, NAMED IN THE SAME COMMENT — every one of these is Phase 48 and none of them is
      shipped here: `Upload files`, `Add document`, `Mark as sent`, `Issue to client`,
      `Open register`, `Export CSV`, `Add anyway`, `Upload a drawing`,
      `Send a RAMS to the client`. Also still banned and untouched: `Add a snag` and `Book a visit`
      (Phase 47), `Edit details` (49), `Re-import from QuoteWerks` and `Close this project` (50),
      `Book another survey` and `Prepare a visit`.
    - `assertCount(19, self::DEFERRED_AFFORDANCES)` becomes `assertCount(18, …)`. The existing
      comment — "This number is the anti-rot mechanism: it exists so that dropping an affordance is
      an edit somebody has to make on purpose. It is never to be deleted to make a change fit." —
      is KEPT VERBATIM and extended with the `19 -> 18` step and its reason.
    - `FORBIDDEN_MARKUP` is UNTOUCHED: `<select` and `<script` both stay banned and
      `assertCount(2, …)` is unchanged. This phase adds no form control and no JavaScript.
    - `BANNED_HANDLER_ATTRIBUTES` is UNTOUCHED: all nine stay, and `assertCount(9, …)` is
      unchanged. The gallery uses `loading="lazy"` and `decoding="async"`, which are plain HTML
      attributes and are not on the list. The ban was re-taken by Phase 46 and is re-taken again
      here: a review page that works with JavaScript off is the page a PM reads in a plant room.
    - `WRITE_SURFACE_TABLES` GROWS 7 -> 11, adding `site_survey_photos`, `worksheet_photos`,
      `worksheet_signoffs` and `device_label_photos`, with a comment explaining that this phase
      READS four new tables and the fence's whole point is that a GET moves none of them — the ZIP
      is a GET. `assertCount(7, …)` becomes `assertCount(11, …)`. All three existing row-count
      tests (`test_rendering_the_cockpit_changes_no_row_count`,
      `test_opening_a_panel_writes_nothing`, `test_repeated_renders_still_change_no_row_count`)
      pick the four up automatically because they iterate the constant — and
      `test_a_write_is_a_post_and_a_get_is_still_inert` gains the four to its untouched-by-a-create
      list.
    - `everyRegion()` now covers `?tab=returned` automatically, because it iterates
      `ProjectCockpitController::TABS`, which Plan 46.1-03 grew. Add one assertion that the returned
      tab was actually among the regions judged, so growth by iteration is proven rather than
      assumed.
    - `CockpitPanelTest::test_the_link_copy_is_view_and_never_download()` is RENAMED
      `test_the_files_tab_link_copy_is_view_and_never_download()` and its docblock amended: the word
      is still banned ON THE FILES TAB, where a document row must say "View"; it is now permitted on
      the Returned tab, where Plan 46.1-04 ships the hand-off. The assertion itself is UNCHANGED and
      still scoped to `$this->panel($project, 'rams', 'files')`. Deleting it would silently stop
      covering the Files tab.
    - `CockpitReturnedTabTest`'s calm budget (RV-08) moves from "zero non-photo anchors" to
      "EXACTLY one non-photo anchor", with the comment recording that the budget was RAISED by this
      plan and by what.
  </behavior>
  <action>
    Make the four fence edits and the two count moves. Write the comments in the file's existing
    voice — the entries are rulings, not leftovers, and the next phase must inherit a decision
    rather than an omission.

    Do NOT delete any entry to make a change fit, do NOT relax `assertGreaterThanOrEqual(5, $checked)`
    in the CSRF test, and do NOT touch either `cockpitRegion()` bracket. Both brackets stay.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Path tests/Feature/Cockpit</automated>
  </verify>
  <done>The fence is green with 18 deferred affordances, 2 forbidden markup entries, 9 banned handlers and 11 write-surface tables; the Files-tab download ban is narrowed and still green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-prove the fence can fail — the three-way breakage ritual</name>
  <what-built>
    The `Download` entry has been lifted from the read-only fence and the fence's row-count
    invariance has been widened to eleven tables. Every phase since Plan 45-07 has re-proved that
    the fence is still capable of going red after editing it, and it has caught real breakage each
    time. This is that proof, run on THIS commit, with the output recorded in the SUMMARY.
  </what-built>
  <how-to-verify>
    Perform all three breakages one at a time, each reverted before the next. Record the exact
    failure message for each in `46.1-04-SUMMARY.md`.

    1. VACUOUS EXTRACTION. In `cockpitRegion()`, temporarily return `''` instead of the extracted
       subtree. Run:
       `powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter CockpitReadOnlyFenceTest`
       EXPECT RED on "The extracted cockpit region is empty — the fence would pass vacuously."
       Revert.

    2. TRUNCATED REGION. In `cockpitRegion()`, temporarily return
       `substr($region, 0, 400)` — enough to keep the masthead project name and satisfy the TOP
       bracket. Run the same command.
       EXPECT RED on the BOTTOM bracket: "The extracted region stops before the end of the page
       shell". This is the asymmetry the two brackets exist to create.
       Revert.

    3. INJECTED AFFORDANCE — THE ASYMMETRY THAT MATTERS NOW. Temporarily add
       `<button onclick="void 0">Upload files</button>` to `returned-tab.blade.php`. Run the same
       command.
       EXPECT THREE separate reds: the `Upload files` deferred-affordance assertion (still Phase 48),
       and the `onclick` banned-handler assertion, and nothing at all about `Download` — because
       `Download` is now legitimately shipped. That combination is the whole point: the fence
       narrowed by exactly one string and by nothing else.
       Revert.

    Then confirm the tree is clean (`git status --porcelain` shows only this plan's intended files)
    and the fence is green again.
  </how-to-verify>
  <action>
    STOP and hand this to the developer. Perform the three breakages exactly as written below,
    one at a time, reverting each before the next, and record the VERBATIM failure message for
    each in the SUMMARY. Do not proceed to Plan 46.1-05 until the developer has responded.
  </action>
  <resume-signal>Paste the three failure messages, or describe what did not go red.</resume-signal>
  <done>All three breakages produced the expected red, each message recorded verbatim in the SUMMARY, the tree is clean and the fence is green again.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the fence itself | The fence is the boundary. Editing it is the highest-risk act in this phase, because a weakened fence shows green while proving nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46.1-17 | Tampering | over-lifting the fence | mitigate | exactly ONE entry is removed, by name, with the plan and requirement at the entry; the count assertion moves in the same commit and keeps its "never deleted to make a change fit" comment; the nine strings that stay are named in the same comment |
| T-46.1-18 | Spoofing (a green fence that proves nothing) | `cockpitRegion()` | mitigate | both brackets stay, and the three-way breakage ritual re-proves the fence can fail on this commit before it is trusted |
| T-46.1-19 | Tampering | the ZIP GET writing a row | mitigate | `WRITE_SURFACE_TABLES` grows to eleven so the four evidence tables are held still by the three existing row-count tests, which iterate the constant |
| T-46.1-20 | Tampering | the Files tab quietly losing its "View, never Download" rule | mitigate | the test is NARROWED and RENAMED rather than deleted; its assertion is unchanged and still scoped to the Files tab |

</threat_model>

<verification>
`gate-46.ps1 -Path tests/Feature/Cockpit` — green.
`gate-46.ps1 -Path tests/Unit/Cockpit` — green.
`gate-46.ps1 -Baseline` — `>= 159 passed AND 0 failed`. Never equality against 161.
`gate-46.ps1 -Hashes` — the three sha256 values unchanged. A mismatch is a STOP.
Plus the three-way breakage ritual, recorded verbatim in the SUMMARY.
</verification>

<success_criteria>
- One `Download all photos (ZIP)` link per sourced visit with photos, outside `.cav-visit`.
- Exactly one fence entry lifted, by name, with the reason and the requirement at the entry.
- `Upload files`, `Add document`, `Mark as sent`, `Issue to client` and the rest still banned.
- `<select`, `<script` and all nine handler attributes still banned; both counts unchanged.
- Write-surface tables 7 -> 11; both GET row-count invariance tests still green and now wider.
- The fence proved capable of failing three ways, on this commit.
</success_criteria>

<output>
Create `.planning/phases/46.1-visit-review/46.1-04-SUMMARY.md` when done
</output>
