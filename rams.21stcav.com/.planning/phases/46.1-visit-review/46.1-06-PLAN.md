---
phase: 46.1-visit-review
plan: 06
type: execute
wave: 5
depends_on: ["46.1-04", "46.1-05"]
files_modified:
  - tests/Feature/Cockpit/CockpitReturnedTabEndToEndTest.php
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [RV-01, RV-02, RV-04, RV-05, RV-06, RV-07, RV-08]
must_haves:
  truths:
    - "An engineer submits through the real public endpoints and a PM reviews, downloads and accepts through the real cockpit — in one test"
    - "Nothing the engineer captured is altered by anything the PM did"
    - "The three sha256-locked files are still byte-identical to 4abd2b24"
    - "The user has seen the tab and said whether it is calm"
  artifacts:
    - path: "tests/Feature/Cockpit/CockpitReturnedTabEndToEndTest.php"
      provides: "The one test that walks the whole loop through HTTP, not through models"
  key_links:
    - from: "tests/Feature/Cockpit/CockpitReturnedTabEndToEndTest.php"
      to: "routes/web.php"
      via: "unauthenticated /worksheet/{token} POSTs, then authenticated cockpit GETs and POSTs"
      pattern: "public-worksheet"
---

<objective>
Close the phase. One end-to-end walk that goes through the real HTTP surface rather than the model
layer, the full gate set, the requirement ledger, and the one thing no assertion in this repo can
settle: whether the tab is calm.

Purpose: Phase 46 closed on a walk exactly like this and it is what caught the difference between
"the tests pass" and "the product works". This phase's equivalent question is sharper, because the
tab is the first page in the cockpit that renders bytes an unauthenticated stranger uploaded.

Output: the walk, the gates, the ledger, and a blocking human checkpoint for RV-08.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46.1-visit-review/46.1-CONTEXT.md
@.planning/phases/46-visit-lifecycle/46-08-PLAN.md
@.planning/phases/45-visit-model-read-only-cockpit/45-BASELINE.md
@.planning/REQUIREMENTS.md

<interfaces>
The PUBLIC endpoints the walk drives (routes/web.php:126-156, all UNAUTHENTICATED, token-gated):
```php
POST   worksheet/{token}/photos                       public-worksheet.photos.upload
GET    worksheet/{token}/photos/{photo}               public-worksheet.photos.serve
POST   worksheet/{token}/label-photo                  public-worksheet.label-photo.upload
POST   worksheet/{token}/label-photos/{photo}/confirm public-worksheet.label-photo.confirm
POST   survey/{token}/rooms/{room}/photos             survey.photos.upload
```

The AUTHENTICATED cockpit surface:
```php
GET  projects/{project}/cockpit                                       projects.cockpit
GET  projects/{project}/cockpit/visits/{visit}/photos.zip             projects.cockpit.visits.photos-zip
GET  projects/{project}/cockpit/visits/{visit}/photo/{kind}/{photo}   projects.cockpit.visits.photo
POST projects/{project}/cockpit/visits/{visit}/accept                 projects.cockpit.visits.accept
```

THE GATES (45-BASELINE.md, character-identical through gate-46.ps1):
- `-Baseline` -> `>= 159 passed AND 0 failed`. NEVER an equality against 161; two tests self-skip on
  this machine for a missing `ext-imagick` and are not defects.
- `-Hashes` -> `layouts/app.blade.php` `9ED63C4C…`, `resources/css/app.css` `EDAD1982…`,
  `tailwind.config.js` `73BB8AD6…`. A MISMATCH IS A STOP, never a hash to refresh. These are
  WORKING-TREE bytes with CRLF endings — a `git hash-object` value will not match.
- NEVER `artisan migrate:fresh --env=testing`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The end-to-end walk, through HTTP</name>
  <files>tests/Feature/Cockpit/CockpitReturnedTabEndToEndTest.php</files>
  <behavior>
    - THE WALK, in order, one test method:
      1. A PM creates an install visit from the `worksheet` drawer via the existing
         `projects.cockpit.visits.store` POST. A worksheet and its token exist.
      2. UNAUTHENTICATED, at `/worksheet/{token}`: upload two room photos in two different rooms,
         upload a label photo and confirm its serial, and submit a client sign-off. All through the
         real public routes with real multipart uploads over `Storage::fake('local')` — never by
         calling a model.
      3. AUTHENTICATED, at the cockpit: the module row shows the visit; the panel opens; the
         `Returned` tab is offered; it shows both photos, the serial and the client's name and
         signature.
      4. The ZIP GET returns 200, the archive opens, and its entry names carry both room folders
         with `after/` and `label/` sub-folders. Assert on entry names read out of the real
         archive.
      5. The photo GET returns 200 with the uploaded bytes for a photo on this visit, and 404 for a
         photo id belonging to another project.
      6. The PM presses Accept from the Returned tab. It redirects, the visit closes, and exactly
         one `visit_accepted` row is logged.
      7. AFTER ALL OF IT: `worksheets.access_token` and `worksheet_signoffs.signature_png_base64`
         and every `worksheet_photos.filename` are BYTE-IDENTICAL to what step 2 wrote, compared
         through `getRawOriginal()`. **Nothing the office did altered one byte of what the engineer
         sent back** — 46-CONTEXT D-02, still binding, now with photos in scope.
      8. `/worksheet/{token}` still returns 200 unauthenticated after the accept — VL-03 still
         holds, re-proved with this phase's routes in play.
    - A SECOND METHOD, the survey side, shorter: a survey-sourced visit whose engineer answered two
      room questions and uploaded one photo renders per-room answers and a `before/` ZIP folder,
      and the survey's `access_token` and `submitted_at` are byte-identical after the review.
    - A THIRD METHOD, the reconstructed case (D-06 / RV-06): one of the 24-style backfilled visits —
      `Visit::factory()->backfilledFromWorksheet($signedWorksheet)` — shows its photos on the
      Returned tab and offers ZERO controls, while its ZIP link still works. Its evidence is worth
      seeing; nobody performed that review.
    - HOSTILE SWEEP, on the walk's own fixtures rather than in a separate synthetic file: a room
      named `<img src=x onerror=alert(1)>`, a caption of `"><script>alert(1)</script>`, a client
      name of the same and an `original_name` of `../../../../etc/passwd` all survive the walk with
      the page at 200, nothing reflected raw, and no `..` in any ZIP entry name.
  </behavior>
  <action>
    Write `tests/Feature/Cockpit/CockpitReturnedTabEndToEndTest.php` following the structure of
    Phase 46's own end-to-end walk (see `46-08-PLAN.md` and its SUMMARY for the shape and for the
    `getRawOriginal()` comparison idiom). Drive every step through `$this->post(...)` /
    `$this->get(...)`; the point of this file is that it does NOT touch models except to assert.

    Use `Storage::fake('local')` and `UploadedFile::fake()->image(...)`. Do NOT assert on HEIC
    conversion — `ext-imagick` is absent on this machine and two pre-existing tests self-skip for
    exactly that reason.

    Never run `artisan migrate:fresh --env=testing`.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Filter CockpitReturnedTabEndToEndTest</automated>
  </verify>
  <done>All three methods green, including the byte-identical comparison of the engineer's record across the whole review and the post-accept 200 on the unauthenticated link.</done>
</task>

<task type="auto">
  <name>Task 2: The full gate set and the requirement ledger</name>
  <files>.planning/REQUIREMENTS.md</files>
  <behavior>
    - All four gates run and their VERBATIM output is recorded in the SUMMARY:
      `-Path tests/Feature/Cockpit`, `-Path tests/Unit/Cockpit`, `-Baseline`, `-Hashes`.
    - `-Baseline` is judged as `>= 159 passed AND 0 failed`. If it reads 161, that is a machine with
      `ext-imagick`, not a regression. If it reads below 159 or shows any failure, that is a D-06
      STOP condition — raise it, do not relax a criterion.
    - `-Hashes` must match all three values. A mismatch is a STOP: this phase touches
      `resources/css/cockpit.css` (which is NOT one of the three) and must not have touched
      `layouts/app.blade.php`, `resources/css/app.css` or `tailwind.config.js`.
    - The four fence counts are re-read and recorded as a table in the SUMMARY:
      FORBIDDEN_MARKUP 2 · DEFERRED_AFFORDANCES 18 · BANNED_HANDLER_ATTRIBUTES 9 ·
      WRITE_SURFACE_TABLES 11. If any differs, the phase did more to the fence than it was licensed
      to do.
    - `.planning/REQUIREMENTS.md`'s RV traceability rows move from `Planned (…)` to
      `Complete (Plan …, 2026-…)` with the same evidence-carrying voice as the VL rows — each row
      says what was proved and how, not merely that it is done. RV-08's row stays
      `Code-complete; AWAITING THE HUMAN CHECK` until Task 3 resolves, exactly as VL-11's did.
    - `.planning/ROADMAP.md`'s Phase 46.1 `**Requirements:**` line is updated from "Not yet minted"
      to `RV-01..RV-08`, and its two open questions are marked RESOLVED with the ruling and the
      plan that carries it.
  </behavior>
  <action>
    Run the four gates. Update the two `.planning/` documents. Do not tick a ROADMAP checkbox by
    hand beyond the plan list — and never call `state.advance-plan` or `state.update-progress`.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Baseline</automated>
  </verify>
  <done>All four gates recorded verbatim; the four fence counts are 2 / 18 / 9 / 11; the RV ledger and the ROADMAP reflect what shipped.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: RV-08 — is it calm?</name>
  <what-built>
    The `Returned` tab now shows what an engineer sent back and hands it off as one ZIP. This is the
    one criterion no assertion in this repo can settle, and it is the criterion the user has already
    used to reject a design once: *"I want to make it look simple and less scary."* The same shape
    of checkpoint VL-11 got, for the same reason, and it is NOT self-approvable — `auto_advance` does
    not apply.

    What was done to keep it calm, so you can judge whether it worked:
      - ONE hand-off link per visit, near the top; ONE control area, at the bottom; NOTHING per
        photo. No lightbox, no pager, no delete, no caption editor, no reorder.
      - The photos are a contact sheet — one uniform grid per bucket, `loading="lazy"`, each
        thumbnail a plain link to the full image in a new tab.
      - Unanswered survey questions are omitted; the count carries them in two integers instead of
        twenty empty rows.
      - No new chip and no new colour. Existing `--cav-*` tokens only; the tab reuses the panel's
        own card shapes so it reads as the same screen.
      - The tab does not appear on a module that holds no visit backed by an engineer record, so it
        is never an empty fourth tab.
      - Overview still says where every visit stands; it just no longer offers to change it.
  </what-built>
  <how-to-verify>
    1. With `COCKPIT_ENABLED=true`, open a project cockpit that has a returned install visit:
       `/projects/{id}/cockpit?module=worksheet&tab=returned`.
    2. Read down the tab the way a PM would. Can you tell, without scrolling back, what came back
       from site?
    3. Press `Download all photos (ZIP)`. Unzip it. Are the folders ones you would drop into Bitrix
       as they are? This is the acceptance test the phase was given: a PM should not have to click
       each thumbnail one at a time.
    4. Look at the bottom. Are Accept / Send back / Add note / Raise a snag in a place where you
       would only reach them having looked at the evidence?
    5. Open `?module=worksheet&tab=overview`. Confirm it still tells you where each visit stands and
       no longer offers to accept one.
    6. Open a reconstructed visit's Returned tab. It should show its photos and offer nothing.
    7. THE QUESTION: is this simple, or is it the busy control panel you rejected before?
  </how-to-verify>
  <action>
    STOP and hand this to the developer. RV-08 is NOT self-approvable — `workflow.auto_advance`
    does not apply, for the same reason it did not apply to VL-11: "simple and less scary" is
    the user's own wording and no assertion in this repo can settle it. Present the checkpoint
    below and wait.
  </action>
  <resume-signal>Type "approved", or name what feels busy and where.</resume-signal>
  <done>The developer has walked the seven steps and either approved, or named what feels busy and where — recorded verbatim in the SUMMARY and reflected in RV-08's traceability row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated `/worksheet/{token}` and `/survey/{token}` uploads → authenticated cockpit render and ZIP | the whole phase's risk, walked here end to end rather than assumed |
| the three sha256-locked files | a change there retones every authenticated page in the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46.1-25 | Tampering | an office act altering engineer-captured bytes | mitigate | the walk compares `access_token`, `signature_png_base64` and every photo `filename` through `getRawOriginal()` before and after the full review, across a real accept |
| T-46.1-26 | Denial of Service | a live engineer link broken by the review surface | mitigate | `/worksheet/{token}` is fetched unauthenticated AFTER the accept and must still be 200 — VL-03, re-proved with this phase's routes in play |
| T-46.1-27 | Tampering | a silent retone of the whole app | mitigate | `-Hashes` on the three locked files; a mismatch is a STOP, never a hash to refresh |
| T-46.1-28 | Spoofing | a green phase that a PM cannot actually use | mitigate | the blocking human checkpoint; RV-08 is not self-approvable, exactly as VL-11 was not |

</threat_model>

<verification>
`gate-46.ps1 -Path tests/Feature/Cockpit` — green.
`gate-46.ps1 -Path tests/Unit/Cockpit` — green.
`gate-46.ps1 -Baseline` — `>= 159 passed AND 0 failed`.
`gate-46.ps1 -Hashes` — three values match 45-BASELINE.md exactly.
Fence counts: 2 / 18 / 9 / 11.
Plus the human checkpoint.
</verification>

<success_criteria>
- One HTTP-level walk proves the loop: engineer submits, PM reviews, downloads, accepts, and the
  engineer's record is byte-identical afterwards.
- The reconstructed case shows evidence and offers nothing.
- All four gates green and recorded verbatim; the fence counts are exactly 2 / 18 / 9 / 11.
- The RV ledger and the ROADMAP say what shipped and what the two open questions were resolved to.
- RV-08 is settled by the user, not by the executor.
</success_criteria>

<output>
Create `.planning/phases/46.1-visit-review/46.1-06-SUMMARY.md` when done
</output>
