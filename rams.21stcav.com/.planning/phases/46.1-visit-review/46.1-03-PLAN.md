---
phase: 46.1-visit-review
plan: 03
type: execute
wave: 2
depends_on: ["46.1-01"]
files_modified:
  - app/Http/Controllers/ProjectCockpitController.php
  - resources/views/components/cockpit/panel.blade.php
  - resources/views/components/cockpit/returned-tab.blade.php
  - resources/css/cockpit.css
  - tests/Feature/Cockpit/CockpitReturnedTabTest.php
autonomous: true
requirements: [RV-01, RV-02, RV-03, RV-08]
must_haves:
  truths:
    - "A Returned tab sits between Overview and Files and shows the visit's photos, room answers, captured serials and client sign-off"
    - "The tab appears only where there is something to review — never as an empty fourth tab on a document module"
    - "Everything on it is read live: edit the engineer's record and a reload shows the new value"
    - "Nothing on the tab is editable and no capture IP is printed"
  artifacts:
    - path: "resources/views/components/cockpit/returned-tab.blade.php"
      provides: "The tab body — hand-off slot, per-room answers, contact-sheet gallery, serial list, sign-off"
      min_lines: 80
    - path: "tests/Feature/Cockpit/CockpitReturnedTabTest.php"
      provides: "Presence rule, read-live proof, escaping proof, calm budget"
  key_links:
    - from: "resources/views/components/cockpit/panel.blade.php"
      to: "resources/views/components/cockpit/returned-tab.blade.php"
      via: "tab strip entry + body branch on $tab === 'returned'"
      pattern: "returned"
    - from: "app/Http/Controllers/ProjectCockpitController.php"
      to: "app/Support/Cockpit/CockpitEvidencePresenter.php"
      via: "the controller wires, the presenter derives, the view draws"
      pattern: "CockpitEvidencePresenter"
---

<objective>
Render the evidence. A `Returned` tab joins the panel between `Overview` and `Files` and shows what
the engineer actually sent back — photos, per-room answers and notes, captured serials, and the
client sign-off — read live and read-only.

Purpose: this is criterion 1 and the reason the phase exists. A PM can currently accept a visit
without seeing a single thing that came back from site.

Output: the tab, its markup, its stylesheet block and its tests. NOT in this plan: the hand-off link
(Plan 46.1-04, which also lifts the fence entry) and the relocation of the four review controls
(Plan 46.1-05). The tab is built first and empty-handed on purpose, so each of those lands as one
reviewable change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/46.1-visit-review/46.1-CONTEXT.md
@.planning/sketches/004-delivery-cockpit/README.md
@resources/views/components/cockpit/panel.blade.php
@resources/views/components/cockpit/visit-row.blade.php
@app/Http/Controllers/ProjectCockpitController.php

<interfaces>
From Plan 46.1-01:
```php
CockpitEvidencePresenter::evidence(Project $project, Visit $visit): ?array
// keys: has_anything, source_missing, photos_by_bucket, photo_count, rooms, serials, signoff, returned_at
```

From Plan 46.1-02 (route names only — this plan renders no ZIP link):
```php
route('projects.cockpit.visits.photo', ['project' => …, 'visit' => …, 'kind' => …, 'photo' => …]);
```

From app/Http/Controllers/ProjectCockpitController.php (the shape to extend):
```php
public const TABS    = ['overview', 'files', 'notes'];
public const ACTIONS = ['create-visit', 'send-back', 'note', 'snag'];
private function resolveTab(Request $request): string;   // MEMBERSHIP, never validate()
// "the controller wires, the presenter derives, the view draws"
```

From resources/views/components/cockpit/panel.blade.php:
```blade
@php $tabs = ['overview' => 'Overview', 'files' => 'Files', 'notes' => 'Notes']; @endphp
<nav class="cav-panel__tabs" aria-label="{{ $module['title'] }} sections">
    @foreach ($tabs as $key => $label)
        <a class="cav-panel__tab{{ $tab === $key ? ' cav-panel__tab--on' : '' }}" href="…" @if ($tab === $key) aria-current="page" @endif>{{ $label }}</a>
    @endforeach
</nav>
{{-- $visits comes from $module['section']['visits'] --}}
```

STYLING CONSTRAINTS (non-negotiable, from 45-BASELINE.md and the cockpit view's docblock):
- `resources/views/layouts/app.blade.php`, `resources/css/app.css` and `tailwind.config.js` stay
  BYTE-IDENTICAL to `4abd2b24`. sha256-asserted. A MISMATCH IS A STOP, never a hash to refresh.
- All colour is a `--cav-*` token declared in `resources/css/cav-tokens.css`. No hex in Blade.
  No Tailwind colour/type/spacing utilities anywhere in the `cav-cockpit` subtree.
- Every class is `cav-`-prefixed.
- THE STRETCHED-LINK TRAP: `.cav-module` is `position: relative` and `.cav-module__open::after` is
  `inset: 0`. Giving any anchor inside a module row its own `position` silently kills row-click.
  Nothing in this plan may set `position` on an anchor.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The tab exists, and appears only where there is something to review</name>
  <files>app/Http/Controllers/ProjectCockpitController.php, resources/views/components/cockpit/panel.blade.php</files>
  <behavior>
    - `ProjectCockpitController::TABS` becomes `['overview', 'returned', 'files', 'notes']` — the
      ORDER is the tab order and `TABS[0]` is still the fallback, so nothing about `resolveTab()`
      changes. Everything that iterates `TABS` (the fence's `everyRegion()`,
      `test_opening_a_panel_writes_nothing`, `test_every_module_renders_on_every_tab`) picks the new
      tab up automatically, which is the point of iterating the constant.
    - The controller derives `$panelEvidence` — an array keyed by visit id of
      `CockpitEvidencePresenter::evidence()` results — ONLY when a module resolved, and passes it to
      the view. Null when no module is open. Derived in the controller, never in Blade, on the same
      rule the page already follows.
    - THE PRESENCE RULE (open question 1, resolved). The `Returned` tab renders in the tab strip
      **if and only if the open module's drawer holds at least one visit whose `source_type` is
      set** — that is, at least one visit backed by an engineer record that could return something.
      Derived from the visits already on the panel; NO hand-maintained module list, so a Phase 47
      snag visit that gains a worksheet gets the tab the day it does.
      Why not "all nine": `Visit::returnedAt()` reads the SOURCE, so a visit with no source can
      never be RETURNED, SENT_BACK or ACCEPTED — the four document modules (RAMS, Drawings, O&M,
      Cable schedule) hold no visits at all and would show a permanently empty fourth tab.
      Why not "Site survey and First fix only": that hard-codes two module keys, and it would strip
      the review surface from any commissioning / programming / snagging visit that ever does carry
      a source. The rule follows the data, not the module.
    - `?tab=returned` on a module that does not offer the tab is a STALE BOOKMARK, not an error: the
      panel renders the Overview body and marks Overview `aria-current`. The page is still 200 and
      the submitted value is never echoed — the same treatment `?module=` already gets.
    - The tab strip still renders anchors only, still carries `aria-current="page"` on the active
      tab and still carries no `aria-expanded` — the tab strip is navigation between URLs, not a
      disclosure widget.
  </behavior>
  <action>
    Edit `ProjectCockpitController::TABS` to insert `'returned'` at index 1, and extend the
    constant's docblock to say why the order is the tab order and why `TABS[0]` is still the
    fallback. Add `$panelEvidence` to the `compact()` list, derived through the new presenter
    injected in the constructor alongside the four existing ones.

    Edit `panel.blade.php`: add `'evidence' => []` to `@props`, compute
    `$offersReturned = $visits->contains(fn ($v) => $v->source_type !== null)` in the existing
    `@php` block, build `$tabs` conditionally, and add an `@elseif ($tab === 'returned')` branch to
    the body that includes the new component. When `$tab === 'returned'` and
    `! $offersReturned`, coerce `$tab` to `'overview'` at the top of the `@php` block so BOTH the
    strip and the body agree — one variable, not two branches that could disagree.

    Extend the panel's existing docblock with the presence rule and its reasoning, in the same
    voice as the "WHAT IS NOT HERE" section already there.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Path tests/Feature/Cockpit</automated>
  </verify>
  <done>Four tabs render on a module holding a sourced visit; three on one that does not; `?tab=returned` on a module without it renders Overview at 200; every pre-existing cockpit test is still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The tab body — evidence, rendered calmly</name>
  <files>resources/views/components/cockpit/returned-tab.blade.php, resources/css/cockpit.css</files>
  <behavior>
    - ORDER ON THE PAGE, mirroring the SCC panel's contract with this phase's two omissions marked:
      (1) the state line — "Returned {date}" or "Awaiting the engineer" or "Nothing has come back
      from site yet"; (2) **a slot for the hand-off link, left EMPTY by this plan** with a Blade
      comment naming Plan 46.1-04 as its owner; (3) per-room answers and notes; (4) the photo
      contact sheet; (5) captured serials; (6) the client sign-off; (7) **a slot for the review
      controls, left EMPTY by this plan** with a comment naming Plan 46.1-05. The two empty slots
      exist so that neither later plan has to restructure this file — each fills a named hole.
    - MULTIPLE VISITS: a drawer can hold several sourced visits. Each gets its own
      `.cav-panel__card` headed by the visit's date and title, in the same date order the Overview
      spine uses. A visit with no source is not shown on this tab at all — it has nothing to review
      and Overview already lists it.
    - ROOM ANSWERS (survey-sourced): one block per returning room — the room name, then the
      answered questions as a definition list (`<dl>`/`<dt>`/`<dd>`), then the engineer's notes and
      access notes as escaped paragraphs, then "{answered} of {total} questions answered". Rooms
      render per room, not as one flat list (the discretion point in D-04's list): a survey's whole
      value is that a fact belongs to a room.
    - THE GALLERY IS A CONTACT SHEET. One uniform thumbnail grid per bucket, headed
      "Before (survey)" / "After (install)" / "Equipment labels" — the bucket names spelled in
      English, since `before`/`after`/`label` are ZIP folder names, not page copy. Each thumbnail is
      an `<img loading="lazy" decoding="async">` inside ONE plain `<a target="_blank" rel="noopener">`
      to the full image route. No lightbox, no JavaScript, no per-photo control, no delete, no
      caption editor, no reorder, no pager. `alt` is the escaped caption, else
      "{room} — {bucket in English}". `loading`/`decoding` are plain HTML attributes and are not
      among the nine banned handler attributes.
    - SERIALS ARE A LIST, NOT THUMBNAILS (RV-03 / D-05): a table-less list of
      room · device · serial · captured date, each row linking its label photo by the same photo
      route. A row whose serial is still null says "Serial not read yet" rather than rendering an
      empty cell. **`captured_by` IS NEVER RENDERED** — it is an audit field holding
      `ip:…|actor:<sha256-slice>` (see `2026_07_08_170000_backfill_device_label_photos_captured_by_leak.php`),
      and a Blade comment at the render site says so.
    - CLIENT SIGN-OFF: the escaped client name, the signature as an `<img>` from the model's
      `signature_data_uri` accessor, the escaped comments when present, and the signed-at date.
      `ip_address` and `user_agent` are never rendered. When a worksheet came back with no sign-off,
      one italic sentence says so.
    - EMPTY STATES ARE ONE SENTENCE, matching the Files and Notes tabs: "Nothing has come back from
      site yet." for an issued-but-untouched visit; "The visit is recorded; the evidence behind it
      could not be read." when the source was force-deleted.
    - ESCAPED OUTPUT ONLY. No `{!! !!}` anywhere — `CockpitPanelTest::test_no_cockpit_view_uses_unescaped_output()`
      greps every cockpit view for it and will judge this file the moment it exists.
    - NO NEW COLOUR AND NO NEW CHIP. Existing `--cav-*` tokens only; the tab reuses
      `.cav-panel__card`, `.cav-panel__card-head` and `.cav-pnote` shapes so it reads as the same
      panel, not a new screen.
  </behavior>
  <action>
    Create `resources/views/components/cockpit/returned-tab.blade.php` taking
    `:project`, `:module`, `:visits` and `:evidence`. Add a `cav-returned-*` block to
    `resources/css/cockpit.css` — thumbnail grid via `display: grid` with
    `grid-template-columns: repeat(auto-fill, minmax(96px, 1fr))`, `aspect-ratio: 1`,
    `object-fit: cover`, `border-radius: var(--cav-radius-sm)`, `border: 1px solid var(--cav-border)`.
    Set no `position` on any anchor (the stretched-link trap).

    Open the component with a docblock in the house voice covering: the presence rule, the
    contact-sheet ruling and what it deliberately omits, the `captured_by` exclusion, and the two
    named empty slots with their owning plans.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Path tests/Feature/Cockpit</automated>
  </verify>
  <done>The tab renders every evidence kind for a survey-sourced and a worksheet-sourced visit; no `{!! !!}`; no hex; no Tailwind colour utility; no `position` on an anchor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CockpitReturnedTabTest</name>
  <files>tests/Feature/Cockpit/CockpitReturnedTabTest.php</files>
  <behavior>
    - PRESENCE: a project with a worksheet-sourced visit in the `worksheet` drawer renders four tabs
      there; the `rams` drawer renders three. `?tab=returned` on `rams` returns 200 with Overview
      current and the string `returned` never echoed into the body.
    - CONTENT: the tab shows a survey photo, a worksheet photo, a label photo, a room's answered
      question and its notes, a serial, and the client's name and signature — each asserted by its
      own value, not by a container class.
    - RV-02 READ LIVE, the proof that matters: render the tab; then change the room's `notes`, add a
      `WorksheetPhoto` and add a newer `WorksheetSignoff`; re-render; the new note, the new photo
      and the NEWER sign-off name are present and the superseded sign-off name is gone. Then assert
      through `getRawOriginal()` that `visits.updated_at`, `site_surveys.submitted_at`,
      `worksheets.access_token` and `site_surveys.access_token` are byte-identical across all of it
      — rendering the review copies nothing and touches nothing.
    - RV-03: the rendered body contains no `captured_by` value, no `ip:` prefix, no `ip_address` and
      no `user_agent`, with a `DeviceLabelPhoto` row whose `captured_by` is a real
      `ip:203.0.113.9|actor:deadbeef` string actually present in the database.
    - ESCAPING: a room named `<script>alert(1)</script>`, a caption of the same, a question text of
      the same, a client name of the same and a comment of the same are all escaped — asserted on
      the RAW body so even an escaped reflection of the `?tab=` value fails, and asserted through
      `cockpitRegion()`-style bracketing so it cannot pass vacuously.
    - LR-04 ADJACENT: no labour resource email or phone appears in the region even though the
      cockpit is staff-auth — the tab reads no `LabourResource` field, and this test says so.
    - CALM BUDGET (RV-08): outside the `.cav-visit` rows, the Returned tab renders **zero**
      non-photo anchors in this plan (the ZIP link is Plan 46.1-04, which raises the budget to
      exactly one and says so). Every other anchor on the tab is a photo link. Asserted by counting
      `<a ` and subtracting the photo-route hrefs, with a non-vacuity floor of at least three photo
      links.
    - INERTNESS: opening the Returned tab on every module moves no row in any of the eleven
      write-surface tables.
  </behavior>
  <action>
    Write `tests/Feature/Cockpit/CockpitReturnedTabTest.php` in `Tests\Feature\Cockpit` with
    `RefreshDatabase`, reusing the region-extraction and fixture style of `CockpitPanelTest` and
    `CockpitVisitActionsTest`. Do NOT copy the fence's `cockpitRegion()` into this file — extract
    the panel the way `CockpitPanelTest` already does, so there is one fence and one panel helper
    rather than three.

    Never run `artisan migrate:fresh --env=testing`.
  </action>
  <verify>
    <automated>powershell -ExecutionPolicy Bypass -File .planning/phases/46-visit-lifecycle/gate-46.ps1 -Path tests/Feature/Cockpit</automated>
  </verify>
  <done>CockpitReturnedTabTest green; CockpitPageTest, CockpitPanelTest, CockpitSpineTest, CockpitVisitActionsTest, CockpitCreateVisitTest, CockpitOfficeNoteAndSnagTest and CockpitReadOnlyFenceTest all still green with no assertion relaxed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated token page → engineer free text → this view | room names, captions, question text, other_text, client name and comments all originate on `/survey/{token}` and `/worksheet/{token}` |
| query string `?tab=` | resolved by membership, never echoed |
| user-uploaded image bytes → `<img src>` same-origin | served by Plan 46.1-02's route behind auth with `nosniff` + `sandbox` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46.1-12 | Tampering (stored XSS) | engineer free text rendered in the tab | mitigate | escaped Blade output only; no `{!! !!}` — enforced repo-wide by `CockpitPanelTest::test_no_cockpit_view_uses_unescaped_output()`, and this plan's own hostile-input test covers five distinct fields |
| T-46.1-13 | Information Disclosure | `captured_by` / `ip_address` / `user_agent` | mitigate | never rendered; asserted absent from the body with a real `ip:…|actor:…` row present in the database, so the assertion cannot pass by there being nothing to leak |
| T-46.1-14 | Information Disclosure | `?tab=` reflection | mitigate | membership resolution; an unknown tab falls back silently and the submitted string is never echoed. Asserted on the raw body |
| T-46.1-15 | Elevation of Privilege | the tab widening the read surface | accept | every model it reads is already reachable to any authenticated user through the project page; the cockpit is staff-auth with no role model beyond that (`CommissioningController` convention) |
| T-46.1-16 | Information Disclosure | LR-04 — a labour resource's email or phone | mitigate | the tab reads no `LabourResource` field at all; asserted, and `tests/Feature/Security/LabourResourceClientSurfacePrivacyTest.php` is NOT touched |

</threat_model>

<verification>
`gate-46.ps1 -Path tests/Feature/Cockpit` — green, with `CockpitReadOnlyFenceTest` UNCHANGED: this
plan ships no banned string, so the fence must pass with every entry still in place.
`gate-46.ps1 -Path tests/Unit/Cockpit` — green.
`gate-46.ps1 -Baseline` — `>= 159 passed AND 0 failed`.
`gate-46.ps1 -Hashes` — three sha256 values unchanged. A mismatch is a STOP.
</verification>

<success_criteria>
- A `Returned` tab sits between Overview and Files on modules holding a sourced visit, and nowhere
  else.
- It shows photos, per-room answers and notes, captured serials and the client sign-off.
- RV-02 is executable: edit the engineer's record and a reload shows it, with four token/timestamp
  fields proven byte-identical across the render.
- RV-03 is executable: no capture IP reaches the page.
- The word `Download` still does not appear in the region and the fence is untouched.
</success_criteria>

<output>
Create `.planning/phases/46.1-visit-review/46.1-03-SUMMARY.md` when done
</output>
