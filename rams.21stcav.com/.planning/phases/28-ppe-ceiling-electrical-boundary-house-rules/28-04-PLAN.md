---
phase: 28-ppe-ceiling-electrical-boundary-house-rules
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/ProjectPackageReviewController.php
  - app/Http/Controllers/RamsReviewController.php
  - app/Services/DocxBuilderService.php
  - app/Services/Rams/RamsComplianceUpgradeService.php
  - app/Services/RiskMatrixService.php
  - resources/views/pdf/rams-v2.blade.php
  - resources/views/pdf/rams.blade.php
  - tests/Feature/Rams/FfpTwoBannedFromSourceTest.php
autonomous: true
requirements: [RULE-01, GATE-06]

must_haves:
  truths:
    - "No non-backup source file under app/, resources/views/, config/, database/seeders/ or tests/ contains the literal token FFP2, and a test fails the build if one is reintroduced."
    - "Both RAMS review controllers' PPE picklists offer FFP3, not FFP2 (per D-02's deliberate asymmetry with the untouched permit picker)."
    - "The live DOCX renderer's COSHH boilerplate and the ppe_matrix / project-specific-risks hardcoded arrays all read FFP3."
  artifacts:
    - path: "tests/Feature/Rams/FfpTwoBannedFromSourceTest.php"
      provides: "repo-wide static ban on the literal token FFP2, copying HazardInjectionPathsRemovedGuardTest's shape, with the 5-file backup exclusion list"
  key_links:
    - from: "app/Http/Controllers/ProjectPackageReviewController.php PPE_OPTIONS"
      to: "app/Http/Controllers/RamsReviewController.php PPE_OPTIONS"
      via: "identical array contents, both fixed together (per pattern-map note: not meant to diverge)"
      pattern: "Dust Mask \\(FFP3\\)"
---

<objective>
Fix RULE-01/GATE-06's remaining 12 live FFP2 source sites (the 2 PPE-picklist controllers, the
live DOCX renderer's COSHH boilerplate, 4 lines inside `RamsComplianceUpgradeService`'s
hardcoded-controls arrays, 1 dead-code hedge, and 2 lines each in the two PDF blade templates —
14 total live sites minus the 2 already handled by Plan 28-03's `RiskTemplateResolverService`
edits), and add the repo-wide static source-ban test D-04 requires: a test that fails the build
the moment `FFP2` reappears in any non-backup `.php`/`.blade.php` file, so this milestone cannot
be silently reopened by a 15th site added later.

Every site here is a mechanical string replace — `'Dust Mask (FFP2)'` -> `'Dust Mask (FFP3)'`
in the picklists, `'FFP2'` -> `'FFP3'` or the seeder's existing "FFP3 ... face-fit tested" phrasing
elsewhere. `RiskMatrixService.php:133` (the "FFP2 or FFP3" hedge) and both
`resources/views/pdf/rams-v2.blade.php` occurrences are confirmed DEAD/UNREACHABLE in production
this session (zero callers; `RAMS_UNIFIED_COMPOSER` unset) — fixed for source hygiene and to make
the static ban test pass, not because either is a currently-live-document risk. No
live-regeneration proof is attempted against either.

Purpose: satisfy RULE-01's "all 14 live sites" requirement and D-04's static-ban breadth
requirement in one plan, since both are the same mechanical sweep over the same file set.
Output: 12 one-or-two-line string fixes plus one new repo-wide guard test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-ppe-ceiling-electrical-boundary-house-rules/28-CONTEXT.md
@.planning/phases/28-ppe-ceiling-electrical-boundary-house-rules/28-RESEARCH.md
@.planning/phases/28-ppe-ceiling-electrical-boundary-house-rules/28-PATTERNS.md

<interfaces>
Exact current strings at every site this plan touches (verified this session — read each file at
the given line before editing, do not trust this list blind for surrounding context):

- `app/Http/Controllers/ProjectPackageReviewController.php:33` and
  `app/Http/Controllers/RamsReviewController.php:42` — both declare an identical
  `private const PPE_OPTIONS` array containing the element `'Dust Mask (FFP2)'`.
- `app/Services/DocxBuilderService.php:2038` — inside a `$coshhBoilerplate` array literal:
  `['Dust generated by drilling / cutting', 'use FFP2 dust masks when drilling into plasterboard,
  masonry or MDF. Use dust extraction where practicable.']`.
- `app/Services/Rams/RamsComplianceUpgradeService.php` — 4 sites: inside `addPpeMatrix()`
  (`~:349` `'Dust mask (FFP2)'` in the "Drilling / cutting / fixing" task row, `~:361` `'Dust mask
  (FFP2)'` in the "Working in ceiling voids" task row — note lowercase "mask", a different literal
  string than the picklist's "Dust Mask (FFP2)") and inside `addProjectSpecificRisks()`
  (`~:747` `'Dust mask (FFP2) worn when accessing ceiling voids.'`, `~:796` `'FFP2 dust mask and
  safety glasses worn during all drilling and cutting.'`).
- `app/Services/RiskMatrixService.php:133` — `'Wear FFP2 or FFP3 dust masks during all drilling
  and cutting operations.'` (dead code — `grep -c RiskMatrixService
  app/Services/RiskMatrixService.php` returns 1, no external callers exist anywhere in `app/`,
  `tests/`, or `resources/`).
- `resources/views/pdf/rams-v2.blade.php:540,1964` and `resources/views/pdf/rams.blade.php:498,1903`
  — each pair: `'PPE: safety footwear, dust masks (FFP2), safety glasses'` and
  `use FFP2 dust masks when drilling into plasterboard, masonry or MDF. Use dust extraction where
  practicable.` (identical wording in both files; `rams-v2.blade.php` is gated behind
  `RAMS_UNIFIED_COMPOSER`, confirmed unset/false in production).

`tests/Feature/Rams/HazardInjectionPathsRemovedGuardTest.php` (read in full, 96 lines) — the exact
shape to copy: a `phpFilesUnder()` recursive-glob private helper (already handles `.blade.php`
files under `getExtension() === 'php'`), a `$forbiddenStrings` list, a self-exclusion via
`realpath(__FILE__) === realpath($file)`, and a single `assertEmpty($offenders, ...)`.

Confirmed exclusion list (5 files, all frozen/backup copies, independently re-grepped this
session — do not add or remove entries without re-verifying):
`resources/views.backup-260430/pdf/rams.blade.php`,
`resources/views.backup-260430/pdf/rams.blade - keep boarder.php`,
`resources/views.backup-260430/pdf/rams.blade-keep-borders.php`,
`resources/views/pdf/rams.blade - keep boarder.php`,
`resources/views/pdf/rams.blade-keep-borders.php`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the 12 remaining live FFP2 source sites</name>
  <files>app/Http/Controllers/ProjectPackageReviewController.php, app/Http/Controllers/RamsReviewController.php, app/Services/DocxBuilderService.php, app/Services/Rams/RamsComplianceUpgradeService.php, app/Services/RiskMatrixService.php, resources/views/pdf/rams-v2.blade.php, resources/views/pdf/rams.blade.php</files>
  <read_first>
    app/Http/Controllers/ProjectPackageReviewController.php:20-38,
    app/Http/Controllers/RamsReviewController.php:30-48,
    app/Services/DocxBuilderService.php:2025-2045,
    app/Services/Rams/RamsComplianceUpgradeService.php:340-370 and :735-800,
    app/Services/RiskMatrixService.php:120-140,
    resources/views/pdf/rams-v2.blade.php (grep -n FFP2 first to confirm current line numbers),
    resources/views/pdf/rams.blade.php (grep -n FFP2 first to confirm current line numbers)
  </read_first>
  <action>
    In both `ProjectPackageReviewController.php` and `RamsReviewController.php`, change
    `'Dust Mask (FFP2)'` to `'Dust Mask (FFP3)'` inside `PPE_OPTIONS` (both controllers, per D-02's
    stated asymmetry — these picklists change even though the permit-type picker at
    `resources/views/rams/review.blade.php:264` does not).
    In `DocxBuilderService.php`'s `$coshhBoilerplate` array, change `'use FFP2 dust masks when
    drilling into plasterboard, masonry or MDF. Use dust extraction where practicable.'` to `'use
    FFP3 dust masks when drilling into plasterboard, masonry or MDF. Use dust extraction where
    practicable.'`.
    In `RamsComplianceUpgradeService.php`'s `addPpeMatrix()`, change both `'Dust mask (FFP2)'`
    occurrences (the "Drilling / cutting / fixing" and "Working in ceiling voids" task rows) to
    `'Dust mask (FFP3)'`. In the same file's `addProjectSpecificRisks()`, change `'Dust mask (FFP2)
    worn when accessing ceiling voids.'` to `'Dust mask (FFP3) worn when accessing ceiling
    voids.'`, and change `'FFP2 dust mask and safety glasses worn during all drilling and
    cutting.'` to `'FFP3 dust mask and safety glasses worn during all drilling and cutting.'`.
    In `RiskMatrixService.php`, change `'Wear FFP2 or FFP3 dust masks during all drilling and
    cutting operations.'` to `'Wear FFP3 dust masks during all drilling and cutting operations.'`
    (drop the "or FFP3" hedge entirely — the house rule is FFP3, singular; this file has zero
    callers, fix for source hygiene only, no live-path proof needed).
    In both `resources/views/pdf/rams-v2.blade.php` and `resources/views/pdf/rams.blade.php`,
    change both occurrences per file: `'PPE: safety footwear, dust masks (FFP2), safety glasses'`
    -> `'PPE: safety footwear, dust masks (FFP3), safety glasses'`, and `use FFP2 dust masks when
    drilling into plasterboard, masonry or MDF. Use dust extraction where practicable.` -> `use
    FFP3 dust masks when drilling into plasterboard, masonry or MDF. Use dust extraction where
    practicable.` (identical fix in both files — do not diverge the wording between them).
  </action>
  <verify>
    <automated>grep -rl "FFP2" app resources/views config database/seeders tests --include=*.php 2>/dev/null | grep -v "resources/views.backup-260430" | grep -v "keep boarder" | grep -v "keep-borders" || true</automated>
  </verify>
  <acceptance_criteria>The command above returns no output — zero live FFP2 sites remain outside the confirmed 5-file backup exclusion list.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Repo-wide static FFP2 ban test</name>
  <files>tests/Feature/Rams/FfpTwoBannedFromSourceTest.php</files>
  <read_first>
    tests/Feature/Rams/HazardInjectionPathsRemovedGuardTest.php (full file, 96 lines — copy its
    shape nearly verbatim)
  </read_first>
  <action>
    Create `tests/Feature/Rams/FfpTwoBannedFromSourceTest.php`, namespace `Tests\Feature\Rams`,
    `class FfpTwoBannedFromSourceTest extends TestCase`. Copy `HazardInjectionPathsRemovedGuardTest`'s
    `phpFilesUnder()` private recursive-glob helper verbatim. Scan directories:
    `base_path('app')`, `base_path('resources/views')`, `base_path('config')`,
    `base_path('database/seeders')`, `base_path('tests')` — explicitly EXCLUDE
    `base_path('resources/views.backup-260430')` by never including it in the scanned directory
    list (it is a separate directory root, not a subdirectory of `resources/views`, so it is
    naturally excluded by scanning `resources/views` alone — do not scan it). Declare
    `private const EXCLUDED_FILES` with exactly the two remaining path fragments that live INSIDE
    the scanned `resources/views/pdf/` directory: `'resources/views/pdf/rams.blade - keep
    boarder.php'` and `'resources/views/pdf/rams.blade-keep-borders.php'` — skip a file whose path
    ends with either fragment. Forbidden string: `'FFP2'` (case-sensitive — confirm no lowercase
    `ffp2` occurrences exist as part of this test's own verification pass; if any are found,
    add them to the forbidden list as a separate case-insensitive check rather than silently
    ignoring them). Self-exclude via `realpath(__FILE__) === realpath($file)`. Single test method
    `test_ffp2_does_not_appear_in_any_non_backup_source_file()` asserting the offenders array is
    empty, with a failure message listing every offending file:string-needle pair.
  </action>
  <verify>
    <automated>php artisan test --filter=FfpTwoBannedFromSourceTest</automated>
  </verify>
  <acceptance_criteria>Test passes against the post-Task-1 codebase; re-introducing FFP2 anywhere in the scanned tree fails it.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None — this plan edits hardcoded server-side string literals and adds a static analysis test.
No user input crosses any boundary here.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04-01 | Tampering (reintroduction) | Any of the 14 live-or-was-live FFP2 sites | mitigate | `FfpTwoBannedFromSourceTest` fails the default test suite the moment FFP2 reappears in any scanned file, catching reintroduction on paths a live-regeneration proof job would never exercise (D-04's explicit rationale). |
| T-28-04-02 | Denial of Service (test over-broad, blocks legitimate work) | The static ban test's exclusion list | accept | The exclusion list is narrow (2 in-tree paths + 1 whole excluded directory), independently re-grepped and confirmed to match exactly the known backup/frozen files — a legitimate future file would need to literally contain the banned string to trip this test, which is the intended behavior. |
</threat_model>

<verification>
`grep -rl "FFP2" app resources/views config database/seeders tests --include=*.php | grep -v "resources/views.backup-260430" | grep -v "keep boarder" | grep -v "keep-borders"` returns nothing.
`php artisan test --filter=FfpTwoBannedFromSourceTest` exits 0.
</verification>

<success_criteria>
All 12 sites read FFP3 (or, for the RiskMatrixService hedge, FFP3 alone with the hedge removed);
the new static test passes and fails when FFP2 is deliberately reintroduced into any scanned file.
</success_criteria>

<output>
Create `.planning/phases/28-ppe-ceiling-electrical-boundary-house-rules/28-04-SUMMARY.md` when done
</output>
