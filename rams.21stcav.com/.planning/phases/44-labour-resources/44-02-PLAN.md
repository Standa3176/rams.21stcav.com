---
phase: 44-labour-resources
plan: 02
type: execute
wave: 2
depends_on: ["44-01"]
files_modified:
  - routes/web.php
  - app/Http/Controllers/Admin/LabourResourceController.php
  - resources/views/admin/labour-resources/index.blade.php
  - resources/views/admin/labour-resources/form.blade.php
  - tests/Feature/Admin/LabourResourceControllerTest.php
autonomous: true
requirements: [LR-02]
must_haves:
  truths:
    - "An admin can add, edit and deactivate/reactivate a labour resource"
    - "A non-admin cannot reach any labour-resource admin route"
    - "There is no route that hard-deletes a labour resource"
  artifacts:
    - path: "app/Http/Controllers/Admin/LabourResourceController.php"
      provides: "index/create/store/edit/update/toggleActive — no destroy()"
    - path: "resources/views/admin/labour-resources/index.blade.php"
      provides: "List view with role + status badges and email/phone columns (admin-only page)"
    - path: "resources/views/admin/labour-resources/form.blade.php"
      provides: "Shared create/edit form — name, email, phone, roles checkboxes, optional user link"
  key_links:
    - from: "routes/web.php"
      to: "app/Http/Controllers/Admin/LabourResourceController.php"
      via: "Route::middleware('admin')->group(...) named routes"
      pattern: "admin\\.labour-resources\\."
---

<objective>
Admin CRUD for labour resources — add, edit, deactivate/reactivate — following the
`Admin\UserController` precedent exactly (same is_active toggle shape, same explicit-named-route
style, same "no destroy" absence for a record that must never disappear from history per D-02).

Purpose: gives admin the surface CONTEXT.md's Success Criterion 2 asks for, and produces the data
Plan 44-03's PM selector reads from.
Output: `/admin/labour-resources` index + create/edit form + toggle-active, admin-gated, tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-labour-resources/44-CONTEXT.md
@.planning/phases/44-labour-resources/44-01-SUMMARY.md

<interfaces>
<!-- app/Http/Controllers/Admin/UserController.php is the load-bearing precedent for this whole
     plan: create/store/edit/update/toggleActive shape, is_active never destroy()-equivalent for
     THIS resource. LabourResource additionally has NO destroy() at all (User's destroy() is a
     genuine hard-delete for account cleanup — a different concept D-02 explicitly rules out here). -->

From app/Http/Controllers/Admin/UserController.php (read in full during planning):
```php
public function index(): View { $users = User::orderBy('name')->paginate(20); ... }
public function create(): View { return view('admin.users.form', ['user' => null]); }
public function store(Request $request): RedirectResponse { $validated = $request->validate([...]); ... }
public function edit(User $user): View { return view('admin.users.form', compact('user')); }
public function update(Request $request, User $user): RedirectResponse { ... }
public function toggleActive(User $user): RedirectResponse {
    $user->update(['is_active' => ! $user->is_active]);
    ...
    return back()->with('success', "User {$user->name} {$action}.");
}
```

From routes/web.php (inside the existing `Route::middleware('admin')->group(function () { ... })`
block, alongside the User routes at :271-277 and Device routes at :292-297):
```php
Route::get('/admin/users', [UserController::class, 'index'])->name('admin.users.index');
Route::get('/admin/users/create', [UserController::class, 'create'])->name('admin.users.create');
Route::post('/admin/users', [UserController::class, 'store'])->name('admin.users.store');
Route::get('/admin/users/{user}/edit', [UserController::class, 'edit'])->name('admin.users.edit');
Route::put('/admin/users/{user}', [UserController::class, 'update'])->name('admin.users.update');
Route::post('/admin/users/{user}/toggle-active', [UserController::class, 'toggleActive'])
    ->name('admin.users.toggle-active');
```

The `admin` middleware alias resolves to `App\Http\Middleware\EnsureUserIsAdmin` (aborts 403 unless
`auth()->user()?->isAdmin()`) — this IS the real access boundary for this whole controller (per
CONTEXT.md Open Question 1, resolved during planning: `Admin\*` controllers sit behind this real
`admin` route-group middleware, unlike the "shared workspace" `abort_unless(auth()->check())`
pattern used by project-scoped controllers such as `CableScheduleController`. No new auth mechanism
needed — reuse the group that already wraps `UserController`/`DeviceController`/`DeviceStencilController`).

From app/Models/LabourResource.php (Plan 44-01): `ROLES` = `['engineer', 'programmer', 'other']`
constants `ROLE_ENGINEER`/`ROLE_PROGRAMMER`/`ROLE_OTHER`; `roles` is a json-array-cast column;
`is_active` boolean; `user_id` nullable FK to `User`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LabourResourceController + admin routes</name>
  <files>app/Http/Controllers/Admin/LabourResourceController.php, routes/web.php</files>
  <action>
    Create `App\Http\Controllers\Admin\LabourResourceController` with:
    - `index(): View` — `LabourResource::orderBy('name')->paginate(20)`, view
      `admin.labour-resources.index`.
    - `create(): View` — `view('admin.labour-resources.form', ['resource' => null])`.
    - `store(Request $request): RedirectResponse` — validate `name` (`required|string|max:150`),
      `email` (`nullable|email|max:254`), `phone` (`nullable|string|max:30`), `roles`
      (`required|array|min:1`), `roles.*` (`in:` + `implode(',', LabourResource::ROLES)`),
      `user_id` (`nullable|exists:users,id`). Create with `is_active` forced `true` — D-02's "add"
      always starts active; there is no "create inactive" path. Log via `Log::info('Admin: labour
      resource created', [...])` mirroring `UserController::store()`'s logging shape. Redirect to
      `admin.labour-resources.index` with a success flash.
    - `edit(LabourResource $labourResource): View` — `view('admin.labour-resources.form',
      ['resource' => $labourResource])`.
    - `update(Request $request, LabourResource $labourResource): RedirectResponse` — same
      validation as store (no password-style "leave blank" special case exists here — every field
      is always submitted). Does NOT touch `is_active` — that is `toggleActive()`'s job only,
      exactly like `UserController::update()` never touches `is_active`.
    - `toggleActive(LabourResource $labourResource): RedirectResponse` — flips `is_active`, logs,
      `back()->with('success', ...)`. No "cannot suspend yourself" guard needed — a labour resource
      is never the acting admin, that guard in `UserController` doesn't apply here.
    - Deliberately NO `destroy()` method. Docblock the class explaining why: D-02 requires
      deactivation, never hard-delete, because a resource that attended a past visit must keep
      displaying on that history — unlike `UserController::destroy()`, which hard-deletes a login
      account, a different concept this model does not have.

    Add named routes inside the EXISTING `Route::middleware('admin')->group(function () { ... })`
    block in `routes/web.php` (the same block that already holds the User/Device/DeviceStencil
    routes — do not open a new middleware group), directly after the Device routes block:
    `admin.labour-resources.index` (GET `/admin/labour-resources`), `.create` (GET
    `/admin/labour-resources/create`), `.store` (POST `/admin/labour-resources`), `.edit` (GET
    `/admin/labour-resources/{labourResource}/edit`), `.update` (PUT
    `/admin/labour-resources/{labourResource}`), `.toggle-active` (POST
    `/admin/labour-resources/{labourResource}/toggle-active`). Explicit named routes, not
    `Route::resource` — matches the `admin.users.*` pattern exactly (and D-14's precedent from
    Phase 24 of preferring explicit named routes when a resource intentionally lacks a full REST
    surface).
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com"; C:\Users\sonny.tanda\.config\herd\bin\php84\php.exe artisan route:list --name=admin.labour-resources 2>&1 | tail -20</automated>
  </verify>
  <done>All six routes registered under the admin middleware group; no destroy route exists.</done>
</task>

<task type="auto">
  <name>Task 2: Admin views (index + form)</name>
  <files>resources/views/admin/labour-resources/index.blade.php, resources/views/admin/labour-resources/form.blade.php</files>
  <action>
    Build `index.blade.php` following `resources/views/admin/users/index.blade.php`'s structure
    exactly: `@extends('layouts.app')`, the same `page-header` + `.au-table`-style table markup
    (copy the CSS block, rename the local class prefix if desired but keep the same visual
    language), columns Name / Roles / Contact / Status / Actions. "Contact" column shows email and
    phone directly (`{{ $r->email ?: '—' }}` / `{{ $r->phone ?: '—' }}`) — this page sits behind
    the real `admin` middleware (per the interfaces note above), so this is the sanctioned
    PM/admin-only surface D-04 explicitly permits ("visible to the PM and admin only"). Add an
    HTML comment at the top of the file stating plainly that this view is intentionally excluded
    from Plan 44-04's client-facing surface scan because it is admin-gated, not client-facing —
    so a future reader doesn't mistake the email/phone columns here for a privacy bug. Roles column
    renders each value in `roles` as a small badge (`{{ ucfirst($role) }}` per value). Status column
    reuses the Active/Suspended badge pattern from `admin/users/index.blade.php` (relabel
    "Suspended" as "Inactive" to match D-02's "deactivate" language). Actions column: Edit link +
    a toggle-active form (POST to `admin.labour-resources.toggle-active`) labelled
    Deactivate/Reactivate. No delete button — there is no destroy route to point one at.

    Build `form.blade.php` following `resources/views/admin/users/form.blade.php`'s structure:
    shared create/edit form, `$resource` nullable. Fields: Name (text, required), Email (email,
    optional), Phone (text, optional), Roles (a checkbox per `LabourResource::ROLES` value, at
    least one required client-side via `required` is not expressible on a checkbox group — rely on
    server-side validation for the real gate, but disable the submit button via nothing fancier
    than normal HTML; do not add JS validation not already used elsewhere in this form family),
    "Link to an existing login (optional)" — a `<select>` of `User::orderBy('name')->get()` with a
    blank "— none —" option, only relevant when admin already knows this person also has a staff
    account. No day-rate/cost field — explicitly out of scope.
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com"; C:\Users\sonny.tanda\.config\herd\bin\php84\php.exe artisan view:cache 2>&1 | tail -20</automated>
  </verify>
  <done>Both Blade views compile without error (view:cache would fail on a syntax error).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Feature tests</name>
  <files>tests/Feature/Admin/LabourResourceControllerTest.php</files>
  <behavior>
    - Non-admin `actingAs` any of the six named routes -> `assertForbidden()` (403), mirroring
      `DeviceControllerTest::test_non_admin_cannot_access_admin_devices_index`.
    - Admin can GET index; a seeded active resource's name is visible, an inactive one still
      appears (deactivation does not hide history from admin) with an "Inactive" badge.
    - Admin `store()` with valid payload (`roles => ['engineer']`) creates a row with `is_active`
      true regardless of what (if anything) is submitted for it.
    - `store()` rejects a `roles` value outside `LabourResource::ROLES` (422 / session errors,
      nothing persisted) — proves the allow-list is enforced server-side, not just in the checkbox
      markup.
    - `update()` persists a changed `roles` array (e.g. `['engineer']` -> `['engineer',
      'programmer']`) without touching `is_active`.
    - `toggleActive()` flips `is_active` both directions and the row survives
      (`assertDatabaseHas`, not `assertDatabaseMissing`) after each toggle.
    - `Route::has('admin.labour-resources.destroy')` is `false` — the concrete, automatable proof
      that D-02's "never hard-delete" is structural, not just a missing test for a route someone
      could still add carelessly later without this catching it.
  </behavior>
  <action>
    Follow `tests/Feature/Admin/DeviceControllerTest.php`'s shape: `RefreshDatabase`, an `$admin`
    and a `$user` created in `setUp()`, one `test_snake_case` method per behavior line above.
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com"; C:\Users\sonny.tanda\.config\herd\bin\php84\php.exe artisan test --filter=LabourResourceControllerTest 2>&1 | tail -60</automated>
  </verify>
  <done>All LabourResourceControllerTest cases pass, including the destroy-route-absence assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| authenticated non-admin -> admin routes | `EnsureUserIsAdmin` is the real gate; any route added to this controller must live inside the existing `Route::middleware('admin')` group |
| admin form input -> `roles` column | An arbitrary string in `roles` must never bypass the `ROLES` allow-list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-44-04 | Elevation of Privilege | `LabourResourceController` routes | mitigate | Routes live inside the existing `Route::middleware('admin')` group; Task 3's non-admin-403 tests prove it for all six routes, not just index. |
| T-44-05 | Tampering | `store()`/`update()` `roles` input | mitigate | `Rule::in`/`in:` validation against `LabourResource::ROLES` server-side; Task 3 proves an out-of-list value is rejected and nothing persists. |
| T-44-06 | Repudiation-adjacent (accidental data loss) | A future dev adding a `destroy()` route | accept, with a structural tripwire | No audit table is added for this phase (unlike `DeviceStencilAudit` — CONTEXT.md leaves this to discretion and the CRUD here is low-risk enough not to warrant one: it is a name/contact/role record, not artwork or compliance data). The tripwire is Task 3's `Route::has('admin.labour-resources.destroy')` assertion, which fails loudly the moment anyone adds one without also removing this test. |

</threat_model>

<verification>
`php artisan test --filter=LabourResourceControllerTest` all green. Manual: `php artisan route:list
--name=admin.labour-resources` shows exactly 6 routes, all under the `admin` middleware.
</verification>

<success_criteria>
- Admin can add, edit, deactivate and reactivate a labour resource via `/admin/labour-resources`.
- A non-admin gets 403 on every route in this controller.
- No route or controller method can hard-delete a labour resource.
</success_criteria>

<output>
Create `.planning/phases/44-labour-resources/44-02-SUMMARY.md` when done
</output>
