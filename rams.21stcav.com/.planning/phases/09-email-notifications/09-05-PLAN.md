---
phase: 09-email-notifications
plan: 05
type: execute
wave: 3
depends_on: ["09-03", "09-04"]
files_modified:
  - app/Jobs/BuildRamsDocumentJob.php
  - app/Jobs/BuildOmManualJob.php
  - app/Jobs/BuildWorksheetJob.php
  - app/Jobs/BuildCableScheduleJob.php
  - app/Jobs/ExtractRamsDraftJob.php
  - tests/Feature/Notifications/RamsCompletionNotificationTest.php
  - tests/Feature/Notifications/OmManualCompletionNotificationTest.php
  - tests/Feature/Notifications/WorksheetCompletionNotificationTest.php
  - tests/Feature/Notifications/CableScheduleCompletionNotificationTest.php
  - tests/Feature/Notifications/RamsReviewNeededNotificationTest.php
  - tests/Feature/Notifications/DocumentGenerationFailedNotificationTest.php
  - tests/Feature/Notifications/IdempotencyTest.php
  - tests/Feature/Notifications/BccTest.php
  - tests/Feature/PublicSurveyControllerTest.php
autonomous: true
requirements: [NOTF-01, NOTF-01a, NOTF-01b, NOTF-01c, NOTF-01d, NOTF-01e, NOTF-01f, NOTF-02, NOTF-02a, NOTF-03, NOTF-03a, NOTF-03b, NOTF-03c, NOTF-04, NOTF-04a, NOTF-04b, NOTF-04c, NOTF-05, NOTF-05a, NOTF-05b, NOTF-05c, NOTF-05d, NOTF-05f, NOTF-05h]

must_haves:
  truths:
    - "`BuildRamsDocumentJob::handle()` dispatches `RamsReadyMail` after STATUS_COMPLETED flip, gated on `completion_email_sent_at === null`, with timestamp set BEFORE send (RESEARCH 'Idempotency Pattern' — order matters)"
    - "`BuildOmManualJob::handle()`, `BuildWorksheetJob::handle()`, `BuildCableScheduleJob::handle()` each dispatch their respective `*ReadyMail` after STATUS_DRAFT flip — NOT STATUS_FINAL (RESEARCH item 4 / Pitfall 4)"
    - "`ExtractRamsDraftJob::handle()` dispatches `RamsReviewNeededMail` after STATUS_AWAITING_REVIEW flip, gated on `review_needed_email_sent_at === null` (NOTF-03c — column added in 09-01); timestamp set BEFORE send"
    - "Each `Build*Job::failed()` hook dispatches `DocumentGenerationFailedMail` to all admins, gated on `failed_email_sent_at === null`, with truncated error_message (≤500 chars) — Cable uses `$exception->getMessage()` fallback when `$record->error_message` is null"
    - "Every send is wrapped in `try { ... } catch (\\Throwable $e) { Log::warning(...) }` and never rethrows (NOTF-05h / D-12)"
    - "Global BCC applied at every call site via `config('rams.notifications.bcc')` when non-empty (NOTF-05d / D-09)"
    - "9 feature/regression tests in `tests/Feature/Notifications/` exercise every trigger using `Mail::fake()` and pass green"
    - "Existing `PublicSurveyControllerTest` is extended with `Mail::fake()` + `Mail::assertSent(SurveySubmittedMail::class)` (locks NOTF-02a fix)"
  artifacts:
    - path: "app/Jobs/BuildRamsDocumentJob.php"
      provides: "RAMS completion + failure email dispatch wired"
      contains: "completion_email_sent_at"
    - path: "app/Jobs/BuildOmManualJob.php"
      provides: "O&M completion + failure email dispatch wired"
      contains: "completion_email_sent_at"
    - path: "app/Jobs/BuildWorksheetJob.php"
      provides: "Worksheet completion + failure email dispatch wired"
      contains: "completion_email_sent_at"
    - path: "app/Jobs/BuildCableScheduleJob.php"
      provides: "Cable completion + failure email dispatch wired"
      contains: "completion_email_sent_at"
    - path: "app/Jobs/ExtractRamsDraftJob.php"
      provides: "Review-needed email dispatch wired after STATUS_AWAITING_REVIEW (idempotent via review_needed_email_sent_at — NOTF-03c)"
      contains: "review_needed_email_sent_at"
    - path: "tests/Feature/Notifications/RamsCompletionNotificationTest.php"
      provides: "End-to-end RAMS completion mail assertion"
      contains: "Mail::assertSent(RamsReadyMail"
    - path: "tests/Feature/Notifications/IdempotencyTest.php"
      provides: "Locks NOTF-01d / NOTF-04b — second run does not re-send"
      contains: "Mail::assertNothingSent"
    - path: "tests/Feature/Notifications/BccTest.php"
      provides: "Locks NOTF-05d — BCC applied when env set, omitted when empty"
      contains: "hasBcc"
  key_links:
    - from: "BuildRamsDocumentJob::handle (success path)"
      to: "RamsReadyMail dispatch"
      via: "`$record->update(['completion_email_sent_at' => now()]); ... Mail::to(...)->bcc(...)->send(new RamsReadyMail($record));`"
      pattern: "RamsReadyMail"
    - from: "each Build*Job::failed()"
      to: "DocumentGenerationFailedMail dispatch (all admins)"
      via: "`$resolver->resolveAdminRecipients()->each(fn($a) => Mail::to($a->email)->send(new DocumentGenerationFailedMail(...)))`"
      pattern: "DocumentGenerationFailedMail"
    - from: "ExtractRamsDraftJob success path"
      to: "RamsReviewNeededMail dispatch"
      via: "after STATUS_AWAITING_REVIEW flip; gated on `review_needed_email_sent_at === null` (NOTF-03c — column added in 09-01); timestamp set BEFORE send for retry-safety (matches completion-mail pattern)"
      pattern: "RamsReviewNeededMail"
---

<objective>
Wire the six trigger sites into the existing five jobs and lock every behavior with
feature tests using `Mail::fake()`. This plan is the heart of Phase 09 — everything
before this was setup; this is the actual notification system going live.

Purpose: Every NOTF-* requirement that hasn't been delivered by 09-01..09-04 lands
here. The wiring follows the proven idempotency pattern (timestamp-before-send),
the proven try/catch + log pattern, and the proven recipient-resolver pattern. The
feature tests catch regressions for every path.

Output: Five jobs modified (additive — no existing logic touched), nine new feature
test files, one extension to the existing PublicSurveyControllerTest. All tests
green; full feature suite green (no regression).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-email-notifications/09-CONTEXT.md
@.planning/phases/09-email-notifications/09-RESEARCH.md
@.planning/phases/09-email-notifications/09-VALIDATION.md

@app/Jobs/BuildRamsDocumentJob.php
@app/Jobs/BuildOmManualJob.php
@app/Jobs/BuildWorksheetJob.php
@app/Jobs/BuildCableScheduleJob.php
@app/Jobs/ExtractRamsDraftJob.php
@app/Mail/RamsReadyMail.php
@app/Mail/OmManualReadyMail.php
@app/Mail/WorksheetReadyMail.php
@app/Mail/CableScheduleReadyMail.php
@app/Mail/RamsReviewNeededMail.php
@app/Mail/DocumentGenerationFailedMail.php
@app/Services/NotificationRecipientResolver.php
@tests/Feature/PublicSurveyControllerTest.php
@phpunit.xml

<interfaces>
<!-- All five jobs share the identical shape (RESEARCH "Build Job Inventory"): -->
<!--   - constructor: public readonly int $modelId -->
<!--   - handle(): finds model, guards status, runs work, flips status to ready/draft, catches Throwable to flip to failed, rethrows -->
<!--   - failed(\Throwable $e): re-finds model, force-sets STATUS_FAILED + (where column exists) error_message -->

Status flip points to wire AFTER:
- BuildRamsDocumentJob.php   line 138    → STATUS_COMPLETED (RAMS uses _COMPLETED, not _DRAFT)
- BuildOmManualJob.php       line 81     → STATUS_DRAFT
- BuildWorksheetJob.php      line 99     → STATUS_DRAFT
- BuildCableScheduleJob.php  lines 102-104 → STATUS_DRAFT
- ExtractRamsDraftJob.php    lines 130-134 → STATUS_AWAITING_REVIEW

failed() hook line numbers (append to existing logic — do NOT replace):
- BuildRamsDocumentJob.php   line 173
- BuildOmManualJob.php       line 125
- BuildWorksheetJob.php      line 137
- BuildCableScheduleJob.php  line 193
- ExtractRamsDraftJob.php    line 161  → ExtractRamsDraftJob does NOT get a failure-email hook (D-03: only Build*Jobs alert admins on failure; extract failures already log to laravel.log)

Recipient resolver:
- `app(\App\Services\NotificationRecipientResolver::class)`
- `->resolveProjectRecipient($model->project)` — for completion + review-needed (single recipient)
- `->resolveAdminRecipients()` — for failure alerts (Collection of admins)

Mailables (instantiation signatures):
- new RamsReadyMail($ramsDocument)
- new OmManualReadyMail($omManual)
- new WorksheetReadyMail($worksheet)
- new CableScheduleReadyMail($cableSchedule)
- new RamsReviewNeededMail($ramsDocument)
- new DocumentGenerationFailedMail(string $documentType, ?string $projectRef, string $projectName, ?string $errorMessage, string $detailUrl)

Failure-email detail URLs (per type):
- RAMS:        route('rams.show', $record)
- O&M:         route('om-manuals.edit', $record)
- Worksheet:   route('worksheets.show', $record)
- Cable:       route('cable-schedules.edit', $record)

Test infrastructure (verified — no Wave 0 framework install needed):
- `phpunit.xml` already sets MAIL_MAILER=array, QUEUE_CONNECTION=sync, DB_CONNECTION=sqlite :memory:
- Mail::fake() built into Laravel
- Storage::fake('documents') for attachment tests (per H-07 convention)
- Factories for the 4 notifiable models added in plan 09-02b
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Wire completion + failure + review email dispatch into all 5 jobs</name>
  <files>app/Jobs/BuildRamsDocumentJob.php, app/Jobs/BuildOmManualJob.php, app/Jobs/BuildWorksheetJob.php, app/Jobs/BuildCableScheduleJob.php, app/Jobs/ExtractRamsDraftJob.php</files>
  <read_first>
    - app/Jobs/BuildRamsDocumentJob.php FULL FILE — to see the exact structure of handle() success path (around line 138) and failed() (around line 173) before adding code
    - app/Jobs/BuildOmManualJob.php / BuildWorksheetJob.php / BuildCableScheduleJob.php / ExtractRamsDraftJob.php — same structural read
    - 09-RESEARCH.md "Pattern 1: Idempotent Send Inside Job Success Path" (lines 172–203 — verbatim block to adapt per job)
    - 09-RESEARCH.md "Pattern 2: Idempotent Failure Alert in failed() Hook" (lines 209–238 — verbatim block to adapt per job)
    - 09-RESEARCH.md "Idempotency Pattern" (lines 834–862 — explains WHY timestamp-before-send order matters)
    - 09-RESEARCH.md "Pitfall 4" — O&M/Worksheet/Cable fire on STATUS_DRAFT, NOT STATUS_FINAL
    - 09-RESEARCH.md "Pitfall 5" — strict separation: completion in handle(), failure in failed()
    - 09-RESEARCH.md "BCC Implementation Pattern → Approach B" (lines 789–822 — call-site BCC; explicit, not magic)
  </read_first>
  <action>
For EACH of the four `Build*Job` files, append two blocks:

**Block A — Completion mail (inside `handle()`, AFTER the existing status-flip update, at the end of the success branch):**

Use this template (substitute `{Model}`, `{TYPE}`, `{Mailable}` per job):

```php
// Phase 09 / NOTF-01 — completion notification (idempotent via completion_email_sent_at).
$record->refresh();
if ($record->status === {Model}::STATUS_{COMPLETED|DRAFT}      // RAMS=_COMPLETED, O&M/WS/Cable=_DRAFT
    && $record->completion_email_sent_at === null) {

    // Set timestamp FIRST (RESEARCH 'Idempotency Pattern') so a retry sees it set.
    $record->update(['completion_email_sent_at' => now()]);

    try {
        $resolver  = app(\App\Services\NotificationRecipientResolver::class);
        $recipient = $resolver->resolveProjectRecipient($record->project);
        if ($recipient?->email) {
            $pending = \Illuminate\Support\Facades\Mail::to($recipient->email);
            $bcc = config('rams.notifications.bcc');
            if (is_string($bcc) && trim($bcc) !== '') {
                $pending->bcc(trim($bcc));
            }
            $pending->send(new \App\Mail\{Mailable}($record));
            \Illuminate\Support\Facades\Log::info(
                '{ClassName}: completion email dispatched',
                ['{model_id_field}' => $record->id, 'recipient' => $recipient->email]
            );
        }
    } catch (\Throwable $e) {
        \Illuminate\Support\Facades\Log::warning(
            '{ClassName}: completion email send failed',
            ['{model_id_field}' => $record->id, 'error' => $e->getMessage()]
        );
        // Do NOT clear completion_email_sent_at — D-14: timestamp set in same update
        // as dispatch so the queue cannot double-send. We accept "timestamp set but
        // mail vanished" over "double mail to PM".
    }
}
```

Per-job substitutions:
| Job                        | Model            | Status         | Mailable                  | model_id_field          |
|----------------------------|------------------|----------------|---------------------------|-------------------------|
| BuildRamsDocumentJob       | RamsDocument     | STATUS_COMPLETED | RamsReadyMail           | rams_document_id        |
| BuildOmManualJob           | OmManual         | STATUS_DRAFT   | OmManualReadyMail         | om_manual_id            |
| BuildWorksheetJob          | Worksheet        | STATUS_DRAFT   | WorksheetReadyMail        | worksheet_id            |
| BuildCableScheduleJob      | CableSchedule    | STATUS_DRAFT   | CableScheduleReadyMail    | cable_schedule_id       |

**Block B — Failure alert (inside `failed(\Throwable $exception)`, APPENDED to existing logic — after the existing forceSetFailed update):**

```php
// Phase 09 / NOTF-04 — admin failure alert (idempotent via failed_email_sent_at).
$record = {Model}::find($this->{model_id_field});
if ($record && $record->failed_email_sent_at === null) {
    $record->update(['failed_email_sent_at' => now()]);

    try {
        $resolver = app(\App\Services\NotificationRecipientResolver::class);
        $admins   = $resolver->resolveAdminRecipients();
        // Cable: error_message column was added in 09-01, but if it is null
        // we fall back to $exception->getMessage() (RESEARCH Pitfall 3).
        $errorMessage = (string) ($record->error_message ?? $exception?->getMessage() ?? '');
        $errorMessage = $errorMessage !== '' ? substr($errorMessage, 0, 500) : null;

        $bcc = config('rams.notifications.bcc');

        foreach ($admins as $admin) {
            if (! $admin->email) continue;
            $pending = \Illuminate\Support\Facades\Mail::to($admin->email);
            if (is_string($bcc) && trim($bcc) !== '') {
                $pending->bcc(trim($bcc));
            }
            $pending->send(new \App\Mail\DocumentGenerationFailedMail(
                documentType: '{DocTypeLabel}',          // 'RAMS' | 'O&M Manual' | 'Worksheet' | 'Cable Schedule'
                projectRef:   $record->project_ref,
                projectName:  (string) $record->project_name,
                errorMessage: $errorMessage,
                detailUrl:    route('{detail.route.name}', $record),
            ));
        }
    } catch (\Throwable $e) {
        \Illuminate\Support\Facades\Log::warning(
            '{ClassName}: failure-alert email send failed',
            ['{model_id_field}' => $this->{model_id_field}, 'error' => $e->getMessage()]
        );
    }
}
```

Per-job substitutions for Block B:
| Job                       | DocTypeLabel       | detail.route.name           |
|---------------------------|--------------------|------------------------------|
| BuildRamsDocumentJob      | RAMS               | rams.show                    |
| BuildOmManualJob          | O&M Manual         | om-manuals.edit              |
| BuildWorksheetJob         | Worksheet          | worksheets.show              |
| BuildCableScheduleJob     | Cable Schedule     | cable-schedules.edit         |

**Block C — Review-needed email (ExtractRamsDraftJob ONLY, inside `handle()`, AFTER the STATUS_AWAITING_REVIEW update around line 130-134) — IDEMPOTENT per NOTF-03c:**

The previous draft of this plan left review-needed without an idempotency column, exposing a duplicate-email risk on `$tries = 2` retries. Plan 09-01 Task 1 now adds `rams_documents.review_needed_email_sent_at` and Task 3 adds it to `$fillable` + `$casts`. Use the same timestamp-before-send pattern as completion mail.

```php
// Phase 09 / NOTF-03 + NOTF-03c — review-needed notification (idempotent via review_needed_email_sent_at).
$record->refresh();
if ($record->status === \App\Models\RamsDocument::STATUS_AWAITING_REVIEW
    && $record->review_needed_email_sent_at === null) {

    // Set timestamp FIRST (matches completion-mail pattern; locks idempotency for $tries=2 retries).
    $record->update(['review_needed_email_sent_at' => now()]);

    try {
        $resolver  = app(\App\Services\NotificationRecipientResolver::class);
        $recipient = $resolver->resolveProjectRecipient($record->project);
        if ($recipient?->email) {
            $pending = \Illuminate\Support\Facades\Mail::to($recipient->email);
            $bcc = config('rams.notifications.bcc');
            if (is_string($bcc) && trim($bcc) !== '') {
                $pending->bcc(trim($bcc));
            }
            $pending->send(new \App\Mail\RamsReviewNeededMail($record));
            \Illuminate\Support\Facades\Log::info(
                'ExtractRamsDraftJob: review-needed email dispatched',
                ['rams_document_id' => $record->id, 'recipient' => $recipient->email]
            );
        }
    } catch (\Throwable $e) {
        \Illuminate\Support\Facades\Log::warning(
            'ExtractRamsDraftJob: review-needed email send failed',
            ['rams_document_id' => $record->id, 'error' => $e->getMessage()]
        );
        // Do NOT clear review_needed_email_sent_at — same trade-off as completion mail.
    }
}
```

`ExtractRamsDraftJob` does NOT get a Block B (failure alert). D-03: only `Build*Jobs` alert admins on failure; extract failures already log to laravel.log.

**General rules for ALL job edits:**
- Append the new code; never modify existing status-flip lines or the existing try/catch around handle().
- All mailable / model / facade FQCNs may be added as `use` statements at the top OR used inline as `\App\Mail\...`. Inline FQCN keeps the diff surgical and is fine.
- Logging prefix MUST follow project convention: `'{ClassName}: ...'` per CLAUDE.md "Logging" section.
- Do NOT change the job's `$tries`, `$timeout`, or constructor signature.
- Do NOT introduce new instance properties on the job.

After ALL five files are edited, lint each one with `php -l` (binary syntax check — see verify command). The previous draft used a noisy phpunit-grep that produced false positives; this version uses a deterministic lint.
  </action>
  <verify>
    <automated>for f in app/Jobs/BuildRamsDocumentJob.php app/Jobs/BuildOmManualJob.php app/Jobs/BuildWorksheetJob.php app/Jobs/BuildCableScheduleJob.php app/Jobs/ExtractRamsDraftJob.php; do php -l "$f" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "completion_email_sent_at" app/Jobs/BuildRamsDocumentJob.php` exits 0
    - `grep -q "completion_email_sent_at" app/Jobs/BuildOmManualJob.php` exits 0
    - `grep -q "completion_email_sent_at" app/Jobs/BuildWorksheetJob.php` exits 0
    - `grep -q "completion_email_sent_at" app/Jobs/BuildCableScheduleJob.php` exits 0
    - `grep -q "RamsReadyMail" app/Jobs/BuildRamsDocumentJob.php` exits 0
    - `grep -q "OmManualReadyMail" app/Jobs/BuildOmManualJob.php` exits 0
    - `grep -q "WorksheetReadyMail" app/Jobs/BuildWorksheetJob.php` exits 0
    - `grep -q "CableScheduleReadyMail" app/Jobs/BuildCableScheduleJob.php` exits 0
    - `grep -q "DocumentGenerationFailedMail" app/Jobs/BuildRamsDocumentJob.php` exits 0
    - `grep -q "DocumentGenerationFailedMail" app/Jobs/BuildOmManualJob.php` exits 0
    - `grep -q "DocumentGenerationFailedMail" app/Jobs/BuildWorksheetJob.php` exits 0
    - `grep -q "DocumentGenerationFailedMail" app/Jobs/BuildCableScheduleJob.php` exits 0
    - `grep -q "RamsReviewNeededMail" app/Jobs/ExtractRamsDraftJob.php` exits 0
    - `grep -q "review_needed_email_sent_at" app/Jobs/ExtractRamsDraftJob.php` exits 0   (NOTF-03c idempotency wired)
    - `! grep -q "DocumentGenerationFailedMail" app/Jobs/ExtractRamsDraftJob.php` exits 0   (extract job does NOT get failure alert per D-03)
    - **Anchored status-reference checks (per checker I-05 — pin status to the email-dispatch block, not random unrelated status mentions):**
      - `grep -A 2 "completion_email_sent_at === null" app/Jobs/BuildRamsDocumentJob.php | grep -q "STATUS_COMPLETED"` exits 0
      - `grep -A 2 "completion_email_sent_at === null" app/Jobs/BuildOmManualJob.php | grep -q "STATUS_DRAFT"` exits 0
      - `grep -A 2 "completion_email_sent_at === null" app/Jobs/BuildWorksheetJob.php | grep -q "STATUS_DRAFT"` exits 0
      - `grep -A 2 "completion_email_sent_at === null" app/Jobs/BuildCableScheduleJob.php | grep -q "STATUS_DRAFT"` exits 0
      - `grep -A 2 "review_needed_email_sent_at === null" app/Jobs/ExtractRamsDraftJob.php | grep -q "STATUS_AWAITING_REVIEW"` exits 0
    - `grep -q "config('rams.notifications.bcc')" app/Jobs/BuildRamsDocumentJob.php` exits 0   (BCC at call site)
    - `grep -q "substr.*error_message.*0, 500" app/Jobs/BuildRamsDocumentJob.php` exits 0   (NOTF-04c truncation — pattern check)
    - `php -l app/Jobs/BuildRamsDocumentJob.php` reports "No syntax errors"
    - `php -l app/Jobs/BuildOmManualJob.php` reports "No syntax errors"
    - `php -l app/Jobs/BuildWorksheetJob.php` reports "No syntax errors"
    - `php -l app/Jobs/BuildCableScheduleJob.php` reports "No syntax errors"
    - `php -l app/Jobs/ExtractRamsDraftJob.php` reports "No syntax errors"
  </acceptance_criteria>
  <done>All 5 jobs wire dispatch logic per the patterns; all 5 files lint clean (deterministic `php -l` check); review-needed is now idempotent per NOTF-03c.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author 7 trigger feature tests + extend PublicSurveyControllerTest</name>
  <files>tests/Feature/Notifications/RamsCompletionNotificationTest.php, tests/Feature/Notifications/OmManualCompletionNotificationTest.php, tests/Feature/Notifications/WorksheetCompletionNotificationTest.php, tests/Feature/Notifications/CableScheduleCompletionNotificationTest.php, tests/Feature/Notifications/RamsReviewNeededNotificationTest.php, tests/Feature/Notifications/DocumentGenerationFailedNotificationTest.php, tests/Feature/PublicSurveyControllerTest.php</files>
  <read_first>
    - 09-VALIDATION.md "Wave 0 Requirements" — exact list of test files to author
    - 09-RESEARCH.md "Example 5: Feature test for RAMS completion trigger" (lines 555–615 — adapt PHPUnit-class-style; the example uses Pest syntax)
    - phpunit.xml — confirms `MAIL_MAILER=array`, `QUEUE_CONNECTION=sync`, `DB_CONNECTION=sqlite :memory:` already set
    - tests/Feature/PublicSurveyControllerTest.php — existing test to extend with Mail::fake() + Mail::assertSent assertion
    - tests/Feature/Rams/ — pick the most recent test file (e.g., RamsControllerTest.php) to confirm PHPUnit-class style + namespace + factory usage
    - app/Jobs/ExtractRamsDraftJob.php — note that `handle()` runs real PDF text extraction (PdfTextExtractorService + potentially OCR). Calling `handle()` end-to-end in a test is slow / flaky / requires fixture PDFs. See I-08 guidance below for the recommended workaround.
    - Factories already exist (added in plan 09-02b): `RamsDocument::factory()`, `OmManual::factory()`, `Worksheet::factory()`, `CableSchedule::factory()` plus existing `User::factory()` and `Project::factory()`. Use them; do NOT invent new factories mid-task.
  </read_first>
  <behavior>
    Per-test list (one test class per file unless noted):

    **`RamsCompletionNotificationTest.php`** (NOTF-01a, NOTF-01d, NOTF-01f):
    - `test_sends_RamsReadyMail_to_project_owner_when_status_completes`: arrange Mail::fake(), Storage::fake('documents'), seed an owner User, a Project (user_id=owner.id), a RamsDocument (status=GENERATING, completion_email_sent_at=null, ready data); stage a fake docx via Storage::disk('documents')->put('rams/test.docx', '...'); run `(new BuildRamsDocumentJob($rams->id))->handle(...)`; assert Mail::assertSent(RamsReadyMail::class) hasTo(owner.email), the rams record's `completion_email_sent_at` is now non-null, and `count($mail->attachments()) === 1`.
    - `test_does_not_send_when_completion_email_sent_at_already_set`: pre-seed timestamp; run job; assert Mail::assertNothingSent().
    - `test_falls_back_to_admin_when_project_has_no_owner`: project user_id=null; admin user with role=admin exists; assert mail goes to admin.

    **`OmManualCompletionNotificationTest.php`** (NOTF-01b — O&M variant):
    - `test_sends_OmManualReadyMail_when_status_flips_to_draft`: assert mail is sent on STATUS_DRAFT (not STATUS_FINAL — RESEARCH item 4).
    - `test_does_not_send_when_already_sent`: idempotency.

    **`WorksheetCompletionNotificationTest.php`** (NOTF-01b — Worksheet variant):
    - `test_sends_WorksheetReadyMail_when_status_flips_to_draft`.
    - `test_does_not_send_when_already_sent`.

    **`CableScheduleCompletionNotificationTest.php`** (NOTF-01b — Cable variant):
    - `test_sends_CableScheduleReadyMail_when_status_flips_to_draft`.
    - `test_attachment_uses_source_filename_not_filename`: stage a CSV at the cable artifact path; assert the attachment is included with text/csv MIME and filename matches `source_filename`.

    **`RamsReviewNeededNotificationTest.php`** (NOTF-03a, NOTF-03b, NOTF-03c):
    - `test_sends_RamsReviewNeededMail_when_extract_completes`: arrange Mail::fake(), seed minimal RAMS in `STATUS_AWAITING_REVIEW` with `review_needed_email_sent_at=null`; invoke the dispatch helper (see I-08 guidance below); assert Mail::assertSent(RamsReviewNeededMail::class) and assert `review_needed_email_sent_at` is now non-null (NOTF-03c lock).
    - `test_review_email_subject_contains_project_ref_and_for_review_text`: subject matches `/\\[21CQ30017\\] RAMS ready for review/`.
    - `test_does_not_resend_when_review_needed_email_sent_at_already_set`: pre-seed timestamp; invoke dispatch helper; assert Mail::assertNothingSent. **NOTF-03c idempotency lock** — without this test, a `$tries=2` retry would silently re-fire the review email.

    **`DocumentGenerationFailedNotificationTest.php`** (NOTF-04a, NOTF-04b, NOTF-04c):
    - `test_failed_hook_sends_DocumentGenerationFailedMail_to_all_admins`: seed 2 admin users; create a RamsDocument; instantiate `BuildRamsDocumentJob($id)` and call `->failed(new \\Exception('boom'))`; assert Mail::assertSent(DocumentGenerationFailedMail::class) was sent twice (once per admin).
    - `test_failed_hook_does_not_double_send_when_failed_email_sent_at_set`: pre-seed timestamp; call failed(); assert Mail::assertNothingSent().
    - `test_failed_email_body_truncates_error_message_to_500_chars`: create RAMS with error_message of 600 chars; trigger failed(); assert the rendered mailable's stored `errorMessage` constructor arg is exactly 500 chars (use Mail::assertSent callback to inspect the sent mailable).
    - `test_cable_failed_hook_falls_back_to_exception_message_when_error_message_null`: create cable schedule with error_message=null; call `failed(new \\Exception('cable boom'))`; assert the failure email's body contains 'cable boom'.

    **Extension to `tests/Feature/PublicSurveyControllerTest.php`** (NOTF-02a regression guard):
    - Add ONE new test method `test_survey_submission_dispatches_SurveySubmittedMail_to_owner`: Mail::fake(); seed owner User; create a Project owned by owner; create a SiteSurvey with the public token; POST the public submit endpoint with valid data; assert Mail::assertSent(SurveySubmittedMail::class) was sent to owner.email exactly once. This locks the latent-bug fix from plan 09-04.
  </behavior>
  <action>
**Conventions for ALL test files:**
- Namespace: `Tests\Feature\Notifications` (new directory) for the 6 new files; existing namespace for the PublicSurveyControllerTest extension
- Class style: PHPUnit-class style (the codebase uses class-based tests, NOT Pest — adapt RESEARCH Example 5)
- Each class extends `Tests\TestCase`
- `use RefreshDatabase` trait
- Setup uses factories from plan 09-02b: `User::factory()`, `Project::factory()`, `RamsDocument::factory()`, `OmManual::factory()`, `Worksheet::factory()`, `CableSchedule::factory()` — all four notifiable-model factories already exist.
- For attachment tests: `Storage::fake('documents')` then `Storage::disk('documents')->put('{type}/{filename}', 'fake bytes')`.
- For BCC-aware tests in this batch: do NOT set `RAMS_NOTIFICATION_BCC` — those scenarios are exercised in plan 09-05 Task 3 (BccTest). Default empty BCC means no Bcc header.
- For failure tests: instantiate the job directly and call `->failed(new \\Exception('boom'))`. Do NOT dispatch via the queue — call the method on a fresh job instance with the model id constructor arg.
- For completion tests: instantiate the job and call `->handle(...)` with whatever services it constructor-injects. Look at the existing `tests/Feature/Jobs/` or `tests/Feature/Rams/` examples for the canonical "run a job synchronously in a feature test" pattern. If `handle()` requires services that are hard to mock (e.g., the AI builder), you may instead manually set the model status to the completion-target status, refresh, and invoke the email block via a small private helper extracted from the job (see I-08 guidance below).

**I-08 Guidance — `RamsReviewNeededNotificationTest` (OCR-dependent path):**

`ExtractRamsDraftJob::handle()` runs real PDF text extraction via `PdfTextExtractorService` and may fall through to `PdfOcrExtractorService` (which shells to `tesseract`) — too slow / fragile for CI. Two options:

- **Recommended (refactor):** Extract the email-dispatch block of `ExtractRamsDraftJob::handle()` into a private method `private function dispatchReviewNeededEmail(RamsDocument $record): void { ... }` and call that helper from inside `handle()` AND from the feature test. The test seeds a RAMS in `STATUS_AWAITING_REVIEW`, then calls `(new ExtractRamsDraftJob($id))->dispatchReviewNeededEmail($rams)` (use Reflection if the method is private, or change its visibility to `public` for testability — preferred since the dispatch is an internal seam, not a public API). This avoids running real PDF/OCR work in CI.
- **Alternative (mock):** Use Mockery doubles on `PdfTextExtractorService`, `PdfOcrExtractorService`, and any other `handle()` collaborators, then call `handle()` end-to-end. Heavier setup, but acceptable if helper extraction is undesirable.

PREFER the helper-extraction approach. Note in the test class docblock: "Tests the dispatch seam directly to avoid OCR latency; end-to-end PDF flow is exercised by `tests/Feature/Rams/ExtractRamsDraftJobTest.php` (existing) where applicable."

**Step 1 (RED):** Create all 6 new test files + the extension. Run `vendor/bin/phpunit --testsuite=Feature --filter=Notifications` and confirm tests fail (because some scenarios depend on the wiring code already landed in Task 1 — they should now PASS for behaviors covered by Task 1, and we are confirming each test file is well-formed).

**Step 2 (GREEN):** All 6 new test classes pass. The extended PublicSurveyControllerTest also passes (because plan 09-04 Task 3 already refactored SurveyService to use the resolver).

If any test fails because the wiring in Task 1 is wrong (e.g., wrong status constant, wrong route name), fix the wiring in Task 1 — the tests are the contract. Do NOT fix the test by lowering its expectation.

**Don't:**
- Don't use `Queue::fake()` — `phpunit.xml` already sets `QUEUE_CONNECTION=sync` so jobs run in-process anyway.
- Don't use real Postmark — `phpunit.xml` sets `MAIL_MAILER=array` so Mail::fake() takes precedence and no HTTP is attempted.
- Don't add `Mail::assertSentCount(...)` without an explicit type; always pair with the mailable class.
  </action>
  <verify>
    <automated>vendor/bin/phpunit --testsuite=Feature --filter=Notifications</automated>
  </verify>
  <acceptance_criteria>
    - All 6 new test files exist under `tests/Feature/Notifications/`
    - `tests/Feature/PublicSurveyControllerTest.php` contains a new method matching `test_survey_submission_dispatches_SurveySubmittedMail`
    - `vendor/bin/phpunit --testsuite=Feature --filter=Notifications` exits 0
    - `vendor/bin/phpunit tests/Feature/PublicSurveyControllerTest.php` exits 0   (NOTF-02 + NOTF-02a regression guard)
    - `vendor/bin/phpunit --testsuite=Feature` exits 0   (NO regression in any other feature test — moved here from Task 1 per checker I-03 because the new test files now exist and the assertion is meaningful)
    - `vendor/bin/phpunit` (full suite) exits 0
    - `grep -rq "Mail::assertSent(RamsReadyMail" tests/Feature/Notifications/` exits 0
    - `grep -rq "Mail::assertSent(OmManualReadyMail" tests/Feature/Notifications/` exits 0
    - `grep -rq "Mail::assertSent(WorksheetReadyMail" tests/Feature/Notifications/` exits 0
    - `grep -rq "Mail::assertSent(CableScheduleReadyMail" tests/Feature/Notifications/` exits 0
    - `grep -rq "Mail::assertSent(RamsReviewNeededMail" tests/Feature/Notifications/` exits 0
    - `grep -rq "Mail::assertSent(DocumentGenerationFailedMail" tests/Feature/Notifications/` exits 0
    - `grep -rq "review_needed_email_sent_at" tests/Feature/Notifications/RamsReviewNeededNotificationTest.php` exits 0   (NOTF-03c idempotency lock present in test)
    - `grep -qE "Mail::assertSent\(.*SurveySubmittedMail" tests/Feature/PublicSurveyControllerTest.php` exits 0  (regression guard added — W-03 fix: single anchored extended regex matches both FQCN `\App\Mail\SurveySubmittedMail::class` and imported short form `SurveySubmittedMail::class`; avoids the previous malformed `\|` alternation)
  </acceptance_criteria>
  <done>All 6 trigger feature tests + 1 regression-guard extension green; full feature suite green; review-needed idempotency (NOTF-03c) is locked by a dedicated test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Idempotency + BCC feature tests</name>
  <files>tests/Feature/Notifications/IdempotencyTest.php, tests/Feature/Notifications/BccTest.php</files>
  <read_first>
    - 09-VALIDATION.md "Wave 0 Requirements" — IdempotencyTest + BccTest entries
    - 09-RESEARCH.md "Idempotency Pattern" (NOTF-01d / NOTF-04b / NOTF-03c)
    - 09-RESEARCH.md "BCC Implementation Pattern" (NOTF-05d)
    - tests/Feature/Notifications/RamsCompletionNotificationTest.php (just authored in Task 2 — copy its setup pattern)
  </read_first>
  <behavior>
    **`IdempotencyTest.php`** (NOTF-01d, NOTF-03c, NOTF-04b — guards under retry):
    - `test_completion_email_not_resent_when_job_handle_runs_twice`: run BuildRamsDocumentJob::handle() twice on the same model with the same state; first run sets timestamp + sends; second run sees the non-null timestamp and skips. Assert `Mail::assertSentCount(1, RamsReadyMail::class)`.
    - `test_failed_email_not_resent_when_failed_hook_runs_twice`: call `->failed(new Exception('boom'))` twice on the same job instance with the same record; first call sets timestamp + sends to N admins; second call skips. Assert `Mail::assertSentCount(N, DocumentGenerationFailedMail::class)` (N = admin count, not 2*N).
    - `test_review_needed_email_not_resent_when_extract_dispatch_runs_twice`: call the review-needed dispatch helper (or `handle()` if mocked) twice on the same RAMS record; first run sets `review_needed_email_sent_at` and sends; second sees the non-null timestamp and skips. Assert `Mail::assertSentCount(1, RamsReviewNeededMail::class)`. **NOTF-03c lock** — closes the gap raised by checker I-04.
    - `test_timestamp_set_before_send_so_concurrent_retry_sees_it`: pseudo-test — verify by inspecting the order of `update()` and `Mail::send()` calls. Easiest way: add a `Mail::fake()` callback that asserts `RamsDocument::find($id)->completion_email_sent_at !== null` AT THE MOMENT the mail is sent (callback-time is after the timestamp update because update happens first per RESEARCH "Idempotency Pattern").

    **`BccTest.php`** (NOTF-05d):
    - `test_completion_email_includes_bcc_when_RAMS_NOTIFICATION_BCC_set`: `config(['rams.notifications.bcc' => 'audit@21stcav.com'])`; run a RAMS completion; assert `Mail::assertSent(RamsReadyMail::class, fn($m) => $m->hasBcc('audit@21stcav.com'))`.
    - `test_completion_email_omits_bcc_when_RAMS_NOTIFICATION_BCC_empty`: ensure `config(['rams.notifications.bcc' => null])`; run completion; assert `Mail::assertSent(RamsReadyMail::class, fn($m) => empty($m->bcc))` (or a similar negative check).
    - `test_failure_email_includes_bcc_when_RAMS_NOTIFICATION_BCC_set`: same shape but for `DocumentGenerationFailedMail` triggered via `->failed(new Exception('x'))`.
    - `test_review_needed_email_includes_bcc_when_RAMS_NOTIFICATION_BCC_set`: same shape but for `RamsReviewNeededMail` triggered via the ExtractRamsDraftJob dispatch helper.
  </behavior>
  <action>
**Step 1 (RED):** Create `tests/Feature/Notifications/IdempotencyTest.php` and `tests/Feature/Notifications/BccTest.php`. Run `vendor/bin/phpunit --testsuite=Feature --filter='Idempotency|Bcc'` and observe failures (or skips if classes are empty). Adapt as needed.

**Step 2 (GREEN):** Each test passes. The behaviors are already implemented by Task 1's wiring; these tests merely lock the behavior against future regression.

Implementation notes:
- For `Mail::assertSentCount(1, RamsReadyMail::class)` after two handle() calls: the second handle() needs the model to be in a state that lets it re-enter the email block. That means: in setup, after the first run, **do not** alter `status` — leave it as `STATUS_COMPLETED`. The second `handle()` call will see status already set + completion_email_sent_at non-null → skip. The assertion confirms only 1 mail sent.
- For BCC test with config override: use `config(['rams.notifications.bcc' => 'audit@21stcav.com'])` BEFORE invoking the job, NOT in the test class constructor. Laravel test isolation resets config between tests but only via `RefreshDatabase` — for config, set per-test in arrange.
- `$mail->hasBcc(...)` is a Laravel test helper available on Mailable instances inside the `Mail::assertSent` closure callback.
- `empty($mail->bcc)` works for the negative case — when no `->bcc()` chained, the property is null/empty array.

If `hasBcc()` does not work on the array transport for any reason, fall back to inspecting the Symfony Email envelope via `$mail->build()->getSymfonyMessage()->getBcc()` — `[CITED: Laravel mail testing docs]`. Prefer `hasBcc()` first.

**Do NOT** add `Mail::raw()` calls or test the wire format directly. Stay at the `Mail::assertSent` API level.
  </action>
  <verify>
    <automated>vendor/bin/phpunit --testsuite=Feature --filter='Idempotency|Bcc'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f tests/Feature/Notifications/IdempotencyTest.php`
    - File exists: `test -f tests/Feature/Notifications/BccTest.php`
    - `vendor/bin/phpunit tests/Feature/Notifications/IdempotencyTest.php` exits 0
    - `vendor/bin/phpunit tests/Feature/Notifications/BccTest.php` exits 0
    - `vendor/bin/phpunit --testsuite=Feature` exits 0   (FULL feature suite green)
    - `vendor/bin/phpunit` (full project suite) exits 0
    - `grep -q "Mail::assertSentCount" tests/Feature/Notifications/IdempotencyTest.php` exits 0
    - `grep -q "review_needed_email_sent_at" tests/Feature/Notifications/IdempotencyTest.php` exits 0   (NOTF-03c idempotency lock in suite)
    - `grep -qE "hasBcc|getBcc" tests/Feature/Notifications/BccTest.php` exits 0
    - `grep -q "config\\(\\['rams.notifications.bcc'" tests/Feature/Notifications/BccTest.php` exits 0
    - `grep -q "Mail::assertSent(RamsReviewNeededMail" tests/Feature/Notifications/BccTest.php` exits 0   (review-needed BCC scenario covered)
  </acceptance_criteria>
  <done>Idempotency (including NOTF-03c review-needed) + BCC behaviors are locked by automated tests; full feature + unit suite green; Phase 09 mail dispatch behavior is fully specified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job state → outbound email | Each job dispatches mail with project metadata; recipient list comes from internal DB (resolver-controlled) |
| BCC env var → every system email | Misconfigured BCC could leak audit copies to wrong inbox |
| failed() retry → admin alerts | `failed()` could fire repeatedly under operational stress; idempotency guards prevent flood |
| ExtractRamsDraftJob retry → review-needed | `$tries=2` could fire the review email twice without the NOTF-03c column guard added in 09-01 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | I (Information disclosure) | Trigger sites in 5 jobs | mitigate | All recipient lookups go through `NotificationRecipientResolver` (resolver-tested in 09-02). No raw email-from-input paths in any of these triggers. |
| T-09-03 | I (Information disclosure) | BCC application via `config('rams.notifications.bcc')` | mitigate | BCC is applied at the call site, not embedded in the mailable, so the value is auditable per trigger; default empty string yields no BCC; tested in BccTest. |
| T-09-05 | D (Cost runaway / mail flooding) | Completion + failure + review-needed dispatch in queued jobs | mitigate | Idempotency columns (NOTF-01d, NOTF-03c, NOTF-04b) plus the timestamp-before-send order (RESEARCH "Idempotency Pattern") prevent both job retries AND `failed()` re-fires from doubling sends. Locked by IdempotencyTest including the NOTF-03c review-needed scenario. |
| T-09-04 | I (Information disclosure) | Detail-page URLs embedded in failure email | accept | Routes (rams.show / om-manuals.edit / worksheets.show / cable-schedules.edit) all sit behind `auth` middleware + per-resource policy checks; URL exposure does not grant access. |
| T-09-01 | T (Tampering) | DocumentGenerationFailedMail body rendering `error_message` | mitigate | Truncated to ≤500 chars at the trigger site (`substr(..., 0, 500)`); rendered with `{{ }}` Blade escape (no `{!! !!}`) per plan 09-04. Cable falls back to `$exception->getMessage()` when DB column null. |
| T-09-06 | E (Elevation of privilege) | None — all triggers are server-side from queued jobs; no HTTP-facing surface added | accept | No new routes; existing `RamsController@email` (manual-send) untouched. |
</threat_model>

<verification>
- All 5 jobs wired with completion / failure / review email dispatch (review-needed now idempotent per NOTF-03c).
- 9 feature tests under `tests/Feature/Notifications/` green.
- PublicSurveyControllerTest extended with mail assertion green (NOTF-02a regression guard).
- Full project test suite green (`vendor/bin/phpunit`).
- Idempotency (incl. NOTF-03c) + BCC behaviors locked against future regression.
- Anchored greps (per I-05) verify the email-dispatch blocks reference the correct status constants — not just any random STATUS_* mention.
- All 5 job files lint clean via `php -l` (per I-03 — deterministic binary check; replaces the noisy phpunit-grep heuristic).
</verification>

<success_criteria>
- A real RAMS document generation in dev with `MAIL_MAILER=log` writes a rendered RamsReadyMail body to `storage/logs/laravel.log` (manual smoke per validation strategy).
- A failed Build*Job (e.g., simulated by `php artisan tinker` setting status=failed and calling `->failed()`) writes a rendered DocumentGenerationFailedMail to log.
- An ExtractRamsDraftJob success writes a rendered RamsReviewNeededMail to log AND sets `review_needed_email_sent_at` so a retry does not re-send.
- The `completion_email_sent_at`, `failed_email_sent_at`, and `review_needed_email_sent_at` columns are populated on each notifiable model after first dispatch.
- The full project test suite (`vendor/bin/phpunit`) is green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-email-notifications/09-05-SUMMARY.md` with:
- Per-job: line numbers where the dispatch blocks were inserted
- Test file count + total test count green
- Confirmation that PublicSurveyControllerTest still green (NOTF-02 regression)
- Confirmation that NOTF-03c (review-needed idempotency) is wired and tested
- Confirmation `vendor/bin/phpunit` (full suite) is green
- Note whether the OCR-test workaround (helper-extraction vs Mockery) was used in `RamsReviewNeededNotificationTest`
- Manual smoke instructions for `MAIL_MAILER=log` dev verification (defer Postmark smoke to plan 09-06)
</output>
</content>
