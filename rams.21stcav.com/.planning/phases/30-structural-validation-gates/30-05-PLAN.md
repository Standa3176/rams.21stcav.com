---
phase: 30-structural-validation-gates
plan: 05
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/30-structural-validation-gates/30-MEASUREMENT.md
autonomous: true
requirements: [GATE-01, GATE-02, GATE-04, GATE-13, GATE-14]

must_haves:
  truths:
    - "The ROADMAP Phase 30 goal line says five gates, not three"
    - "ROADMAP criterion 1 states GATE-01's failure condition correctly — EITHER support missing"
    - "ROADMAP carries a success criterion for GATE-13 and one for GATE-14"
    - "A read-only corpus measurement procedure exists that an operator can run before flipping any flag"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Corrected Phase 30 goal, corrected criterion 1, two new criteria, plan list"
      contains: "GATE-13"
    - path: ".planning/phases/30-structural-validation-gates/30-MEASUREMENT.md"
      provides: "Read-only per-gate corpus measurement + the three-step arming runbook"
      contains: "config:clear"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "criterion wording matches REQUIREMENTS.md:62 and PORTING-NOTES.md:66-68"
      pattern: "GATE-01"
---

<objective>
Correct the stale and incorrect Phase 30 planning documentation, and write the arming runbook that
turns D-03's "flip after live verification" from an intention into a procedure.

Purpose: three concrete documentation defects block clean phase closeout. (1) The ROADMAP goal line
says "the three gates"; it predates the 2026-08-25 addition of GATE-13/14, which the ROADMAP's own
**Requirements** line and the REQUIREMENTS.md traceability table at `:166-167` both assign to
Phase 30 (D-01). (2) ROADMAP criterion 1 states GATE-01's failure condition backwards — it reads
"no matching hazard row AND no matching `clientReqs` entry", describing a gate that fires only when
BOTH supports are absent and lets the half-supported case through, while `PORTING-NOTES.md:66-68`
and `REQUIREMENTS.md:62` both require BOTH supports and therefore fire when EITHER is missing
(D-05). (3) There are four success criteria covering only GATE-01/02/04; GATE-13 and GATE-14 have
none.

Separately, all five gates ship disarmed and none of their false-positive rates is knowable from
this machine. Without a written measurement procedure, "verify live then flip" has no owner and no
definition of done.

Output: a corrected ROADMAP, an updated REQUIREMENTS traceability status, and `30-MEASUREMENT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-structural-validation-gates/30-CONTEXT.md
@.planning/phases/30-structural-validation-gates/30-RESEARCH.md
@.planning/phases/30-structural-validation-gates/30-01-SUMMARY.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-MEASUREMENT.md
@.planning/reference/21cav-rams-skill/PORTING-NOTES.md

<interfaces>
Canonical GATE-01 wording to match (do not paraphrase loosely):
  `PORTING-NOTES.md:66-68` — a method step / hazard control referencing a document, permit or hold
  point "must have a matching hazard row *and* a matching `clientReqs` entry".
  `REQUIREMENTS.md:62` — same.
  Therefore the gate FIRES when EITHER is missing (D-05).

`clientReqs` does not exist in this application (D-08). Its app equivalent is the union of
`client_responsibilities` and `client_responsibilities_expanded`. Note also that the app's
Section 4 is *Scope of Works* — the skill's "Section 4 — Client responsibilities and pre-start
hold points" does NOT map to the app's Section 4. Where the ROADMAP says `clientReqs`, keep the
term but footnote the app equivalent so a future reader does not grep for a key that is not there.

Measurement precedent: `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-MEASUREMENT.md`
— operator-run `php artisan tinker --execute="…"` on the VPS as `stcav`, `select` plus in-memory
filter, ZERO writes. Copy its exact format.

Existing, already-shipped measurement vehicle: `php artisan rams:refresh-compliance`
(`RamsRefreshComplianceCommand.php:185`) runs `upgrade()` across the corpus. It has a `--dry-run`
branch at `:190-197` that returns BEFORE persisting — use `--dry-run` for measurement. Without the
flag it persists at `:198` and re-renders the DOCX, so it is NOT read-only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct ROADMAP Phase 30 and the REQUIREMENTS traceability status</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <action>
    In `.planning/ROADMAP.md` under `### Phase 30: Structural Validation Gates`:

    Rewrite the **Goal** line so it names five gates, not three — orphan controls, area/method-step
    coverage, residual-vs-initial scoring, hot-works contradiction, and missing risk references —
    against the finished Phase 26 hazard shape (D-01). Note inline that GATE-13 ships built-whole
    but disarmed and is flipped in Phase 31 (D-02), so a reader does not mistake it for descoped.

    Correct **criterion 1** to the canonical condition: GATE-01 errors when a method step or hazard
    control references a document, permit or hold point (e.g. "review the asbestos register") and
    the RAMS lacks EITHER a matching hazard row OR a matching client-responsibility entry — both
    supports are required — proven against a fixture reproducing the canonical asbestos-orphan
    failure named in `PORTING-NOTES.md`. Add a short parenthetical that `clientReqs` is the skill's
    key and its app equivalent is the union of `client_responsibilities` and
    `client_responsibilities_expanded` (D-08).

    Correct **criterion 4**'s scope from "all three gates" to all five, and restate how it is proven:
    by authored fixtures (a defect-bearing 21CQ30960 reproducing the real-world shape, and a clean
    one proving no false positives), not by a live-only UAT step. Those two are the same project at
    two points in time — RAMS 97 of 2026-08-25 carried the defects; criterion 4 wants a post-fix
    regeneration to pass clean.

    Add **criterion 5 (GATE-13)**: errors when a RAMS asserts no hot works while also requiring a
    hot-works permit or listing solder/flux in COSHH — proven against a fixture reproducing the
    21CQ30960 three-way contradiction (RA18 says no hot works, §6.8 requires a hot-works permit for
    soldering, COSHH carries Tin/Lead solder and rosin flux) — and does NOT fire on
    `addPermitAndIsolation()`'s own conditional permit line. Record that it ships disarmed per D-02.

    Add **criterion 6 (GATE-14)**: flags (warn tier, does not block) any method step failing to cite
    a hazard its own text plainly implies where that hazard IS present in the register — proven
    against a fixture reproducing the Step 4 "Display & Mount Installation" defect that cites
    RA11/12/13/21 but omits RA01 Working at Height and RA02 Manual Handling. Record explicitly that
    GATE-14 is warn tier rather than error tier because `associated_risks` is app-generated by
    `crossReferenceMethodStatementRisks()` (`:991-1092`), so a violation is a defect in the app's
    own derivation and is not fixable from the review screen — a blocking error there would be
    unactionable.

    Update the **Plans** line from `TBD` to the nine plans of this phase with a one-line objective
    each, in the `- [ ] 30-NN-PLAN.md — …` format used by Phase 29.

    In `.planning/REQUIREMENTS.md`, update the traceability rows for GATE-01, GATE-02, GATE-04,
    GATE-13 and GATE-14 to reflect Phase 30 planned/in-progress status, and add a note against
    GATE-13 recording the D-02 built-but-disarmed state so it is not later read as shipped-and-armed
    — the exact status ambiguity `REQUIREMENTS.md:72` records for the GATE-11/GATE-12 shared flag.

    Make no other ROADMAP edits. Do not touch Phase 31's entry beyond what is required to keep
    GATE-13's Phase 31 arming task visible.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "cd 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; $r = Select-String -Path .planning\ROADMAP.md -Pattern 'GATE-13','GATE-14','EITHER'; if ($r.Count -lt 3) { Write-Error 'ROADMAP Phase 30 corrections missing'; exit 1 }; Write-Output 'OK'"</automated>
  </verify>
  <done>The goal line names five gates; criterion 1 reads EITHER; criteria 5 and 6 exist; the plan list is populated; REQUIREMENTS.md records GATE-13's built-but-disarmed state.</done>
</task>

<task type="auto">
  <name>Task 2: 30-MEASUREMENT.md — read-only corpus measurement and the arming runbook</name>
  <files>.planning/phases/30-structural-validation-gates/30-MEASUREMENT.md</files>
  <action>
    Write `30-MEASUREMENT.md` in the exact format of `29-MEASUREMENT.md`: operator-run, VPS, as
    `stcav`, `php artisan tinker --execute="…"` with `select` plus in-memory filter, and ZERO writes.

    One section per gate, each stating what to count and what the number decides:
    GATE-01 — documents containing a trigger phrase from the shipped `structural_gate_triggers`
    vocabulary, and of those how many lack hazard-side support, client-responsibility-side support,
    or both. This sets the vocabulary's initial contents (D-06), which is tunable without a deploy.
    GATE-02 — documents with at least one `reviewed_data.room_overviews[*].room`, and of those how
    many carry an area name matching no phase title or step. This is the only way to answer "are
    there production documents with areas but no method steps", which is unanswerable from source.
    GATE-04 — rows where `post_l*post_s > pre_l*pre_s` (would error) and rows where
    `post_severity < pre_severity` (would warn). If warns are corpus-wide the panel is noise; if
    errors are non-zero, arming blocks live regeneration.
    GATE-13 — documents whose free text asserts absence of hot works, confirming D-02's blocker
    empirically.
    GATE-14 — phases whose implication map predicts a hazard present in the register but absent from
    `associated_risks`. The highest-variance number in the phase.

    Record `php artisan rams:refresh-compliance --dry-run` as the corpus-wide vehicle, with the
    explicit warning that WITHOUT `--dry-run` the command persists (`:198`) and re-renders the DOCX,
    so it is not read-only.

    State as an explicit, reasoned conclusion — not an omission — that **no backfill migration is
    needed**, and why this differs from Phase 29. Phase 29's GATE-11 needed a backfill because the
    content it policed was persisted in 46 rows. Phase 30's five gates police relationships between
    sections that are recomputed on every `upgrade()` run, so a regeneration IS the backfill. The
    only persisted artefact Phase 30 introduces is `compliance_warnings`, which is recomputed and
    overwritten wholesale every run and is therefore deliberately backfill-free.

    Then the arming runbook: three separate one-line `.env` changes, in order —
    `RAMS_STRUCTURAL_GATES` (GATE-01+02+04) after this phase's live verification,
    `RAMS_MISSING_RISK_REF_GATE` (GATE-14) independently, and `RAMS_HOT_WORKS_GATE` (GATE-13) NOT in
    this phase — it flips in Phase 31 once RULE-05/GATE-10 make the COSHH table job-conditional
    (D-02). Each flag is independent by doctrine; one gate's rollback must never disarm another's.
    Include the config-cache step: the live VPS runs `config:cache`, so an `.env` edit alone may
    silently do nothing — each flip needs `php artisan config:clear` then `config:cache`, then a
    re-verified regeneration. Flag this as research assumption A4, inferred from the project's CWP
    pattern and NOT verified on this VPS; if wrong the extra clear is harmless, if right and omitted
    the flip does nothing.

    Finally, record two known limitations for the SUMMARY so Phase 31's arming task inherits them:
    (a) `RamsController::review()` never calls `upgrade()` (`:307-326`), so a document last saved
    pre-Phase-30 shows no warnings until the next Save Review or regenerate — accepted behaviour,
    not a defect, because calling `upgrade()` from a read-only GET would turn a view into a failure
    surface for nine gates; (b) `pdf/rams.blade.php:407-409` and `pdf/rams-v2.blade.php:463-465`
    derive a 'Hot Works Permit' row IN THE BLADE from a scope regex, invisible to `upgrade()` — a
    genuine GATE-13 bypass surface, out of scope here because GATE-13 ships disarmed.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "cd 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; $f='.planning\phases\30-structural-validation-gates\30-MEASUREMENT.md'; if (-not (Test-Path $f)) { Write-Error 'missing'; exit 1 }; foreach ($p in 'GATE-01','GATE-02','GATE-04','GATE-13','GATE-14','--dry-run','config:clear','no backfill') { if (-not (Select-String -Path $f -Pattern ([regex]::Escape($p)) -Quiet)) { Write-Error \"missing: $p\"; exit 1 } }; Write-Output 'OK'"</automated>
  </verify>
  <done>30-MEASUREMENT.md exists with a read-only per-gate measurement, the no-backfill conclusion, the three-step arming runbook including the config-cache caveat, and the two known limitations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → future executor behaviour | A wrong criterion is executed as written; ROADMAP criterion 1 currently specifies a gate that lets the half-supported case through |
| operator → live `.env` / config cache | Flag flips change whether safety gates run in production |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-12 | Tampering | ROADMAP criterion 1's inverted AND/OR | mitigate | Corrected to the canonical `PORTING-NOTES.md:66-68` / `REQUIREMENTS.md:62` wording; Plan 30-03's unit tests assert the EITHER semantics independently, so code and doc cannot silently diverge |
| T-30-03 | Tampering | An operator flipping one flag and unintentionally disarming another | mitigate | The arming runbook flips three independent flags as three separate one-line changes, each named, with GATE-13 explicitly excluded from this phase per D-02 |
| T-30-13 | Repudiation | A flag flip that silently does nothing because of `config:cache` | mitigate | Runbook mandates `config:clear` + `config:cache` + a re-verified regeneration after each flip; recorded as unverified assumption A4 rather than asserted as fact |
| T-30-11 | Repudiation | A disarmed gate later read as "shipped and passing" | mitigate | REQUIREMENTS.md records GATE-13's built-but-disarmed state explicitly, avoiding the GATE-11/12 status ambiguity at `REQUIREMENTS.md:72` |
| T-30-SC | Tampering | composer/npm installs | mitigate | Documentation only; zero packages |
</threat_model>

<verification>
- ROADMAP Phase 30 has six success criteria, criterion 1 reads EITHER, and the goal line names five gates
- `30-MEASUREMENT.md` exists and covers all five gates plus the arming runbook
- No code files were modified by this plan — `git diff --name-only` shows only `.planning/` paths
</verification>

<success_criteria>
- D-01's two missing success criteria are written
- D-05's criterion-1 correction is made against the canonical source
- A runnable, read-only, zero-write measurement procedure exists for each of the five gates
- The no-backfill conclusion is stated and reasoned, not omitted
</success_criteria>

<output>
Create `.planning/phases/30-structural-validation-gates/30-05-SUMMARY.md` when done.
</output>
