---
phase: 30-structural-validation-gates
plan: 03
type: execute
wave: 2
depends_on: ["30-01", "30-02"]
files_modified:
  - app/Services/Rams/RamsComplianceUpgradeService.php
  - tests/Unit/Services/Rams/StructuralGatesTest.php
  - tests/Feature/Rams/StructuralGatesSaveReviewGateTest.php
autonomous: true
requirements: [GATE-01, GATE-02]

must_haves:
  truths:
    - "An armed structural gate surfaces as a friendly redirect carrying its message on the Save Review route, not a 500"
    - "GATE-01 errors when a trigger phrase has no supporting hazard row OR no supporting client-responsibility entry"
    - "GATE-02 errors when a named area matches no method-statement phase title or step"
    - "A RAMS with zero areas passes — a manual/form-only document is not rejected"
    - "With RAMS_STRUCTURAL_GATES false neither method is ever called"
  artifacts:
    - path: "app/Services/Rams/RamsComplianceUpgradeService.php"
      provides: "enforceOrphanControlGate() and enforceAreaCoverageGate() plus the structural dispatch block"
      contains: "enforceOrphanControlGate"
    - path: "tests/Unit/Services/Rams/StructuralGatesTest.php"
      provides: "Unit coverage for both gates including the canonical asbestos-orphan failure"
    - path: "tests/Feature/Rams/StructuralGatesSaveReviewGateTest.php"
      provides: "Armed-gate HTTP surfacing proof - redirect with the gate message, not a 500"
  key_links:
    - from: "app/Services/Rams/RamsComplianceUpgradeService.php"
      to: "app/Services/Rams/StructuralGateVocabulary.php"
      via: "signal matching delegated to the shared vocabulary"
      pattern: "StructuralGateVocabulary::"
    - from: "app/Services/Rams/RamsComplianceUpgradeService.php"
      to: "config/rams_tier1.php"
      via: "structural_gate_triggers + structural_gates_enabled"
      pattern: "structural_gate"
---

<objective>
Implement GATE-01 (orphan controls) and GATE-02 (area/method-step coverage) as flag-gated private
static methods on `RamsComplianceUpgradeService`, behind `RAMS_STRUCTURAL_GATES`.

Purpose: these are two of the three checks the phase goal names. Both depend on Plan 30-02's
mirrors to see their subject and Plan 30-01's shared vocabulary to decide what "supported" means.

Output: two gate methods, one dispatch block, one unit test file covering the canonical
asbestos-orphan failure from `PORTING-NOTES.md:66-68`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-structural-validation-gates/30-CONTEXT.md
@.planning/phases/30-structural-validation-gates/30-RESEARCH.md
@.planning/phases/30-structural-validation-gates/30-PATTERNS.md
@.planning/phases/30-structural-validation-gates/30-UI-SPEC.md
@.planning/phases/30-structural-validation-gates/30-01-SUMMARY.md
@.planning/phases/30-structural-validation-gates/30-02-SUMMARY.md
@app/Services/Rams/RamsComplianceUpgradeService.php
@tests/Unit/Services/Rams/CdmEmergencyGateTest.php
@CLAUDE.md

<interfaces>
<!-- Contracts. No codebase exploration required. -->

Gate method shape, verified across all four shipped gates (`:1202`, `:1228`, `:1320`, `:1446`):
  private static function enforceXGate(array $data): array
  - reads input as `(array) ($data['key'] ?? [])` — never assumes presence
  - returns `$data` unchanged on the clean path
  - throws `RamsGenerationException` via `sprintf` on the FIRST violation
  - message tail is always `'… or set RAMS_<FLAG>=false to disable this check.'`
  - docblock states the gate ID, the RULE ID, what it re-checks, and why it is independent

From Plan 30-01 (`app/Services/Rams/StructuralGateVocabulary.php`):
  signalMatchesHazards(string $signal, array $hazards): bool
  signalMatchesClientReqs(string $signal, array $clientReqStrings): bool
  flattenClientResponsibilities(array $data): array
  flattenAreas(array $data): array

From Plan 30-01 config: `rams_tier1.structural_gate_triggers` rows of
`['phrase' => string, 'signal' => string, 'label' => string]`;
flag `rams_tier1.structural_gates_enabled` (default false).

From Plan 30-02: `$data['client_responsibilities_expanded']` and `$data['areas_for_gate']`
are populated on all three entry points.

Method-statement shape (`RamsDataBuilderService.php:500`): each phase is exactly
`['title' => string, 'steps' => string[]]`. Phases carry NO area/room key — GATE-02's match is
necessarily name-based against phase titles and step text.

Hazard shape (`RamsDataBuilderService.php:440-457`): 10 fixed keys, including `hazard` (free text)
and the four clamped scores. `RA##` in any message is `row index + 1` zero-padded, NOT `$h['id']`
(the 260817-r5e correction, documented at `RamsComplianceUpgradeService.php:1000-1006`).

UI-SPEC error copy contract — `{what is wrong} — {which item} — {what to do}`:
  GATE-01: `Orphan control — "{trigger phrase}" references {label} but the RAMS has no supporting
           hazard row and no client-responsibility entry. Add the hazard and the client
           responsibility, or remove the reference.`
  GATE-02: `Area "{area name}" has no method steps. Add at least one method step for this area,
           or remove the area.`
(Adapt GATE-01's wording so it correctly names WHICH support is missing when only one is.)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GATE-01 — enforceOrphanControlGate</name>
  <files>app/Services/Rams/RamsComplianceUpgradeService.php, tests/Unit/Services/Rams/StructuralGatesTest.php</files>
  <behavior>
    - Throws on the canonical failure: a method step reading "review the asbestos register" with no
      asbestos hazard row and no asbestos client-responsibility entry; message names the phrase and
      names `RAMS_STRUCTURAL_GATES`
    - Throws when the asbestos HAZARD ROW exists but no client-responsibility entry does (D-05: the
      gate fires when EITHER support is missing, not only when both are)
    - Throws when the client-responsibility entry exists but no hazard row does
    - Does NOT throw when both supports are present
    - Does NOT throw when no trigger phrase appears anywhere in the document
    - Scans BOTH method-statement step text and hazard control lines for trigger phrases, per
      REQUIREMENTS.md:62 ("a method step / hazard control referencing…")
    - Returns `$data` unchanged, identical by `assertSame`, on every clean path
    - A document with an empty or absent `client_responsibilities_expanded` and an empty
      `client_responsibilities` still evaluates — it does not crash, it reports the missing support
  </behavior>
  <action>
    Add `private static function enforceOrphanControlGate(array $data): array` under the
    `// ===== N. STRUCTURAL GATES (Phase 30)` banner Plan 30-01 placed.

    For each row of `config('rams_tier1.structural_gate_triggers', [])`, scan the combined text of
    every method-statement phase title and step, plus every hazard control line, for the row's
    `phrase` (case-folded containment). When the phrase is present, require BOTH supports via the
    shared vocabulary: `StructuralGateVocabulary::signalMatchesHazards($row['signal'], $data['hazards'] ?? [])`
    AND `::signalMatchesClientReqs($row['signal'], ::flattenClientResponsibilities($data))`. Throw on
    the first trigger missing either support (D-05), with a message that names the trigger phrase,
    the row's `label`, and explicitly WHICH support is absent — an engineer who has added the hazard
    but not the client responsibility must be told that, not told both are missing.

    Do not build any second matching vocabulary. All signal matching goes through
    `StructuralGateVocabulary` (D-07/D-08 — a trigger's definition of satisfaction lives in exactly
    one place). Do not call `HazardIncludeWhenResolver::resolve()`; it queries Eloquent and would
    break the class's "Deterministic. No AI. No database." invariant at `:19`.

    Conservative by construction (pattern S3): an unknown signal, an absent `method_statement`, an
    absent `hazards` key — each is a skip, never a throw. Prefer a miss to a false positive;
    ROADMAP criterion 4 makes "no false positives against legitimate output" an acceptance criterion.

    The docblock must state that this gate is independent of the code that produced the text it
    checks, adapting the paragraph at `:1268-1276`.

    Write the tests first in `tests/Unit/Services/Rams/StructuralGatesTest.php`, namespaced
    `Tests\Unit\Services\Rams`, extending `Tests\TestCase`, no `RefreshDatabase`, reaching the
    private static via the `invokePrivateStatic()` reflection helper copied from
    `CdmEmergencyGateTest.php:50-57`. Include the house non-vacuity proof docblock
    (`CdmEmergencyGateTest.php:18-45`) and actually run the procedure.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "cd 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; php artisan test --filter=StructuralGatesTest"</automated>
  </verify>
  <done>GATE-01 fires on the canonical asbestos-orphan failure and on each half-supported case (D-05), stays silent on supported and untriggered documents, and returns $data identically on clean paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GATE-02 — enforceAreaCoverageGate, plus the structural dispatch block</name>
  <files>app/Services/Rams/RamsComplianceUpgradeService.php, tests/Unit/Services/Rams/StructuralGatesTest.php</files>
  <behavior>
    - Throws when an area name from `areas_for_gate` appears in no phase title and no step string;
      message names the area and names `RAMS_STRUCTURAL_GATES`
    - Does NOT throw when every area name appears in at least one phase title or step
    - **Passes vacuously on zero areas** — a manual/form-only RAMS with no room list is legal and
      must not be rejected
    - Passes when `method_statement` is absent AND `areas_for_gate` is empty
    - Matching is case-folded and whitespace-trimmed
    - With `rams_tier1.structural_gates_enabled` false, running `upgrade()` over a document that
      would violate BOTH gates throws nothing and returns output byte-identical to pre-Phase-30
    - With `structural_gates_enabled` true and the other two Phase 30 flags false, only these gates
      run — flag independence (D-04)
  </behavior>
  <action>
    Add `private static function enforceAreaCoverageGate(array $data): array` beside GATE-01.
    Read areas via `StructuralGateVocabulary::flattenAreas($data)`. For each area name, search every
    phase `title` and every `steps` entry for a case-folded containment match; throw on the first
    area with no match, naming the area.

    Zero areas passes and must — RESEARCH Finding 3 point 2: `buildQuoteSummary()` legitimately
    returns `[]`, `ManualRamsCreationTest` exercises a form-only path with no room list at all, and
    erroring on an empty area list would reject every manual RAMS. State this in the docblock as a
    deliberate decision, not an oversight.

    Record in the docblock the known false-positive risk that name matching carries on generic room
    names ("AV Rack", "Room 1"), and that this is the specific reason D-03 ships the flag disarmed
    pending the corpus measurement Plan 30-05 documents.

    Record one docblock line on `enforceAreaCoverageGate()` noting the matching-order property:
    `cleanTextArtifacts()` runs LAST in `upgrade()` (`:102`) and rewrites phase titles and steps
    (`:1968-1974`), so GATE-02 matches PRE-typo-fix text while the issued document shows POST-fix
    text. The ordering is forced - GATE-14 must read the `associated_risks` written at `:80`, so all
    three new blocks sit after it - and today's typo map makes divergence very unlikely. This is a
    recorded property, not a defect, and no code change follows from it.

    Then add the dispatch block to `upgrade()`. It goes after `crossReferenceMethodStatementRisks()`
    at `:80` and adjacent to the existing `:97` CDM block, before `cleanTextArtifacts()` at `:102`.
    Copy the `:97-101` shape exactly, including repeating the default at the call site:
    the condition reads `config('rams_tier1.structural_gates_enabled', false)` — `false`, not `true`
    (Pitfall 2; `config/rams_tier1.php:121` documents this as a named trap and the project's own
    memory records "a content gate defaulting ON is a deploy-order trap"). Inside the block call
    `enforceOrphanControlGate()` then `enforceAreaCoverageGate()`. GATE-04's call joins this same
    block in Plan 30-06 — leave a comment saying so. Precede the block with the 15-line comment in
    the `:82-96` style: which methods are gated, which env var flips it, that it is new and
    independent, that it ships disarmed per D-03, and that false means byte-identical behaviour.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "cd 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; php artisan test --filter=StructuralGatesTest"</automated>
  </verify>
  <done>GATE-02 fires on an uncovered area, passes on zero areas, and both gates dispatch only when RAMS_STRUCTURAL_GATES is true. StructuralGatesTest green; `php artisan test --filter=Rams` still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: StructuralGatesSaveReviewGateTest - armed-gate HTTP surfacing</name>
  <files>tests/Feature/Rams/StructuralGatesSaveReviewGateTest.php</files>
  <behavior>
    - With `rams_tier1.structural_gates_enabled` TRUE, POSTing a violating payload to
      `/rams/{rams}/update-and-download` returns a redirect (not a 500, not an unhandled exception)
      whose flashed `error` contains the GATE-01 message and the offending trigger phrase
    - The same request with a GATE-02 violation redirects carrying the GATE-02 message naming the area
    - With the flag FALSE, the identical violating payload succeeds normally - no redirect-with-error
    - The persisted `generated_data` is NOT updated when a gate throws (the throw precedes the
      persist at `RamsController.php:623`)
  </behavior>
  <action>
    Create `tests/Feature/Rams/StructuralGatesSaveReviewGateTest.php`, modelled directly on
    `tests/Feature/Rams/DisplayLiftSaveReviewGateTest.php` - the exact analog `30-PATTERNS.md` maps
    for this surface - reusing its `makeRams(User $user)` (`:36-58`) and realistic-payload helper
    (`:61-76`) so the test fails if routing, middleware or validation regress the fix.

    This is deliberately distinct from Plan 30-02's `StructuralGatesDualPathTest`, which POSTs the
    same route with the gates DISARMED: that one proves the mirror is populated, this one proves an
    armed gate's exception is caught and surfaced. `REQUIREMENTS.md:66` records the GATE-06/GATE-09
    precedent as explicitly claiming the gate is "reachable and caught with a friendly redirect on
    all 3 HTTP-reachable `upgrade()` call sites"; Phase 30 must replicate that claim rather than
    inherit it. Note the three catch sites do NOT behave alike: `:598-603` and `:856-861` redirect,
    while `:701` catches `\Throwable`, logs, sets `$rebuildError` and CONTINUES - assert against the
    Save Review site's redirect behaviour specifically.

    Use `config()->set()` to arm the flag per test - never edit `.env`.

    Write the tests first. PowerShell/Herd only.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "cd 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; php artisan test --filter=StructuralGatesSaveReviewGateTest"</automated>
  </verify>
  <done>An armed structural gate produces a redirect carrying its message on the real Save Review route; a disarmed one lets the same payload through; no 500 on either path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engineer/AI-authored free text → gate matching | Method steps, hazard names and room names are untrusted-ish free text the gate parses |
| gate decision → document issuance | A false negative here means a self-contradicting safety document is issued to a client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Repudiation | `enforceOrphanControlGate` / `enforceAreaCoverageGate` failing open | mitigate | Both read the Plan 30-02 mirrors, proven non-vacuous by `StructuralGatesDualPathTest`; unit tests here assert a throw on a populated violating fixture, so a gate that sees nothing fails its own test |
| T-30-10 | Spoofing | A generic area name ("Room 1") matching unrelated step text and masking a real gap | accept | Conservative-by-construction posture prefers a miss to a false positive; documented in the gate docblock and quantified by the Plan 30-05 corpus measurement before `RAMS_STRUCTURAL_GATES` is flipped |
| T-30-03 | Tampering | Dispatch block reusing another gate's flag | mitigate | The block reads `rams_tier1.structural_gates_enabled` only; Plan 30-01's `StructuralGateConfigTest` asserts flag-name independence |
| T-30-22 | Denial of service | An armed gate throwing an uncaught exception into a 500 instead of a redirect | mitigate | `StructuralGatesSaveReviewGateTest` drives the real HTTP route with the flag armed and asserts a redirect carrying the gate message, replicating the GATE-06/GATE-09 claim at `REQUIREMENTS.md:66` |
| T-30-07 | Information disclosure | Gate error message content shown to the user | mitigate | Messages name document fields only — no stack traces, no DB errors, matching all four shipped gate precedents |
| T-30-SC | Tampering | composer/npm installs | mitigate | Zero packages installed by this plan |
</threat_model>

<verification>
- `php artisan test --filter=StructuralGatesTest` green (PowerShell)
- `php artisan test --filter=Rams` green
- `php artisan test --filter=StructuralGatesSaveReviewGateTest` green (PowerShell)
- `php artisan test --filter=StructuralGatesDualPathTest` still green — the mirrors still feed the gates
- Grep confirms the dispatch condition ends in `, false)` not `, true)`
</verification>

<success_criteria>
- GATE-01 fires when EITHER support is missing, per D-05 and `PORTING-NOTES.md:66-68`
- GATE-02 fires on an uncovered area and passes vacuously on zero areas
- Both gates are reachable only via `RAMS_STRUCTURAL_GATES`, which is false
- No second matching vocabulary was created
</success_criteria>

<output>
Create `.planning/phases/30-structural-validation-gates/30-03-SUMMARY.md` when done.
</output>
