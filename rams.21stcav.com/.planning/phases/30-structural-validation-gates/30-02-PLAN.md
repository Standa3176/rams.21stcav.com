---
phase: 30-structural-validation-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/RamsController.php
  - app/Services/RamsBuilderService.php
  - tests/Feature/Rams/StructuralGatesDualPathTest.php
autonomous: true
requirements: [GATE-01, GATE-02]

must_haves:
  truths:
    - "The array upgrade() receives contains the engineer's client_responsibilities_expanded on the Save Review path"
    - "The array upgrade() receives contains a usable area list on the Save Review path and on both builder paths"
    - "A test fails if any of the three mirror sites is removed — the mirrors are proven, not assumed"
    - "Nothing outside the two new gate-visible keys changes behaviour — ensurePerRoomBullets() stays dormant"
  artifacts:
    - path: "app/Http/Controllers/RamsController.php"
      provides: "Save-Review mirrors immediately before upgrade()"
      contains: "client_responsibilities_expanded"
    - path: "app/Services/RamsBuilderService.php"
      provides: "Symmetric mirrors in runFromReview() and runPipeline()"
      contains: "areas_for_gate"
    - path: "tests/Feature/Rams/StructuralGatesDualPathTest.php"
      provides: "Non-vacuity proof that the gate can SEE its subject on every entry point"
  key_links:
    - from: "app/Http/Controllers/RamsController.php"
      to: "RamsComplianceUpgradeService::upgrade"
      via: "mirror written immediately before the call"
      pattern: "areas_for_gate"
    - from: "app/Services/RamsBuilderService.php"
      to: "RamsComplianceUpgradeService::upgrade"
      via: "mirror in both runFromReview and runPipeline"
      pattern: "client_responsibilities_expanded"
---

<objective>
Close the data-reachability gap that would otherwise make GATE-01 and GATE-02 pass vacuously on
every real document: mirror `reviewed_data['client_responsibilities_expanded']` and a gate-private
area list into the pipeline array at every entry point, immediately before `upgrade()`.

Purpose: this is the single most important constraint in the phase (RESEARCH Finding 1). Neither
key reaches `upgrade()` today — `client_responsibilities_expanded` is written only into
`$reviewedData` at `RamsController.php:522-536`, and there is no usable area enumeration at all
(`$data['rooms']` is site-survey-only, `$data['quote']['room_summaries']` returns `[]` when
equipment is empty, `$data['room_overviews']` is never set). A gate that cannot see its data is
worse than no gate, because it reports clean. This is the same bypass class Plan 27-07 closed for
GATE-09 with the `material_handling` mirror.

Output: three coordinated mirror edits plus a dual-path feature test that fails if any of them is
removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-structural-validation-gates/30-CONTEXT.md
@.planning/phases/30-structural-validation-gates/30-RESEARCH.md
@.planning/phases/30-structural-validation-gates/30-PATTERNS.md
@app/Http/Controllers/RamsController.php
@app/Services/RamsBuilderService.php
@tests/Feature/Rams/CdmEmergencyDualPathGateTest.php
@tests/Feature/Rams/DisplayLiftSaveReviewGateTest.php
@CLAUDE.md

<interfaces>
<!-- Exact sites, verified 2026-09-13. Line refs are ±2. -->

The three mirror sites (copy the Plan 27-07 `material_handling` idiom at each):
1. `RamsController.php:586-594` — the Save Review path; the existing mirror block sits
   immediately before the `upgrade()` call at `:598-603`. Source of truth on this path is the
   `$reviewedData` array assembled at `:495-536`.
2. `RamsBuilderService.php:285-294` — `runFromReview()`; the existing `material_handling` mirror
   with its "Mirrored here, immediately before upgrade(), exactly like scope_items above" comment,
   followed by the `upgrade()` call at `:297`.
3. `RamsBuilderService.php:932-939` — `runPipeline()`; the symmetric mirror that reads from
   `$formData` instead and says so explicitly, followed by `upgrade()` at `:942`.

The three catch sites DO NOT behave alike (verified, corrects both CONTEXT and RESEARCH):
- `:598-603` Save Review — catches `RamsGenerationException`, `back()->withInput()->with('error', …)`
- `:701` DOCX rebuild-on-download — catches `\Throwable`, LOGS, sets `$rebuildError`, and CONTINUES
- `:856-861` `downloadPdf()` — catches `RamsGenerationException`, `back()->with('error', …)`

Shape of `reviewed_data['client_responsibilities_expanded']` (`RamsController.php:520-536`):
four fixed buckets `network_readiness` / `licences` / `access` / `power_validation`, each
`['required' => bool, 'notes' => string]`, plus `'additional' => list of ['item', 'notes']`.

Authoritative area list: `reviewed_data['room_overviews'][*]['room']` — written by
`RamsReviewController.php:311`, `ProjectPackageReviewController.php:281/888/1193`,
`ExtractQuoteJob.php:257`. Shape confirmed by the Tilda fixture:
rows of `room`, `overview`, `works_summary`.

**Per-path area source, verified in source 2026-09-13 — read this before writing Task 1:**
- `runFromReview()` — `$parsedQuote['rooms']` IS populated, at `RamsBuilderService.php:192-196`,
  but ONLY inside `if (! empty($reviewedData['room_overviews']))` at `:162`. Use
  `$reviewedData['room_overviews']` directly as the source; do not depend on `$parsedQuote['rooms']`
  existing.
- `runPipeline()` — there is **no** `$parsed['rooms']` equivalent anywhere near the `upgrade()` call
  at `:942`. Confirmed by grep: `room_overviews` appears in this file only at `:162-195`, `:378` and
  `:395`, all on the review path. The source to use is `$record->reviewed_data['room_overviews']`
  (`$record` is in scope — `runPipeline(array $parsed, array $formData, RamsDocument $record)` at
  `:861`), which `ExtractQuoteJob.php:257` writes before the build. On a **form-only** initial build
  no quote was extracted, so that key is legitimately absent and `areas_for_gate` is correctly `[]`.
  That is not a bug and Task 2 must not assert non-empty on this path.

Consumed by Plan 30-01's `StructuralGateVocabulary::flattenAreas()` /
`::flattenClientResponsibilities()`; keys must be exactly `areas_for_gate` and
`client_responsibilities_expanded`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mirror client_responsibilities_expanded and the area list at all three entry points</name>
  <files>app/Http/Controllers/RamsController.php, app/Services/RamsBuilderService.php</files>
  <action>
    At each of the three sites named in the interfaces block, immediately before the `upgrade()`
    call and beside the existing `material_handling` mirror, add two mirrors:

    First, `client_responsibilities_expanded` — copy `reviewed_data['client_responsibilities_expanded']`
    onto the pipeline array, defaulting to an empty array, so GATE-01's D-08 client-responsibility
    half can see the buckets the engineer just typed on THIS request.

    Second, a gate-private area list under the key `areas_for_gate` — a flat list of trimmed,
    non-empty room names derived from `reviewed_data['room_overviews'][*]['room']` (on the
    controller and `runFromReview()` paths) or from the equivalent `$formData` / parsed-rooms
    source on the `runPipeline()` path. Use a gate-private key deliberately, NOT
    `$data['room_overviews']`: per RESEARCH Finding 3 and assumption A2, setting `room_overviews`
    would wake the long-dormant `ensurePerRoomBullets()` (`RamsComplianceUpgradeService.php:165-168`),
    newly firing an AI bullet conversion on the review path — a behaviour change outside this
    phase's scope and against CLAUDE.md's minimal-diff posture.

    Each mirror carries a comment in the Plan 27-07 style naming the coverage gap it closes: that
    without it GATE-01/GATE-02 always see an empty array on the exact path an engineer uses, and
    would therefore report clean on every document. Keep the three sites symmetric and say so, as
    `runPipeline()`'s existing comment does.

    Change nothing else. No new method, no refactor, no reordering of existing calls.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "cd 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; php artisan test --filter=Rams"</automated>
  </verify>
  <done>Both keys are present on the array passed to upgrade() at all three sites; the existing RAMS suite is still green; `ensurePerRoomBullets()` still early-returns on every builder path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StructuralGatesDualPathTest — prove the mirrors, per entry point</name>
  <files>tests/Feature/Rams/StructuralGatesDualPathTest.php</files>
  <behavior>
    - Driving `POST /rams/{rams}/update-and-download` with a realistic payload carrying a populated
      `client_responsibilities_expanded` and a populated `room_overviews`, the array reaching
      `upgrade()` carries non-empty `client_responsibilities_expanded` and non-empty `areas_for_gate`
    - Same assertion via `RamsBuilderService::runFromReview()`
    - Via `RamsBuilderService::runPipeline()` (with the AI call faked): `client_responsibilities_expanded`
      is present, and `areas_for_gate` is present-and-non-empty when the record carries
      `reviewed_data['room_overviews']` (the quote-extracted case) but is present-and-EMPTY on a
      form-only build where no quote was extracted. Assert key PRESENCE on this path in both cases,
      and non-emptiness only in the quote-extracted case — per the per-path source note above, a
      form-only initial build has no room list to mirror and `[]` is the correct answer
    - A document with no `room_overviews` at all yields `areas_for_gate === []` and does NOT error —
      the manual/form-only RAMS path must stay legal
    - The docblock records, per entry point, WHICH of the six `upgrade()` call sites the mirror is
      live on, and names the known asymmetry: `injectDefaultsIntoRamsData()` runs AFTER `upgrade()`
      on the two builder paths (`RamsBuilderService.php:297` then `:302`; `:942` then `:947`), so
      `coshh_baseline` is absent on sites 1-2 and present on sites 3-6
    - The docblock ALSO records the mirror-liveness split, which is the phase's most dangerous
      subtlety: sites 1, 2 and 3 write the mirrors directly on this request, but sites 4
      (`RamsController.php:701`, DOCX rebuild), 5 (`:857`, `downloadPdf()`) and 6
      (`RamsRefreshComplianceCommand.php:185`) all pass `$rams->generated_data` and therefore
      inherit the mirrors ONLY BY PERSISTENCE — only if the document was last written after Phase 30
      ships. On a legacy document those three sites run GATE-02 against an empty area list and
      report clean, and GATE-01's `_expanded` half is blind, which on a document whose only asbestos
      client-responsibility lives in `_expanded` is a FALSE POSITIVE that throws. Add a test
      asserting this inherit-by-persistence behaviour explicitly on site 5 so the property is
      recorded rather than discovered. The remedy is operational, not code: Plan 30-05's arming
      runbook requires a corpus regeneration before `RAMS_STRUCTURAL_GATES` is flipped
  </behavior>
  <action>
    Create `tests/Feature/Rams/StructuralGatesDualPathTest.php` modelled on
    `CdmEmergencyDualPathGateTest.php` (uses `RefreshDatabase` + `Http::fake()` for the pipeline AI
    call) and borrowing the HTTP-level helpers `makeRams(User $user)` and the realistic-payload
    builder from `DisplayLiftSaveReviewGateTest.php:36-76` so the test fails if routing, middleware
    or validation regress the fix.

    Capture the array `upgrade()` receives — the simplest reliable mechanism is to assert on the
    persisted `generated_data` after the request (Save Review persists at `RamsController.php:623`)
    plus a direct assertion on the builder paths' return value; do not add a production hook or
    test-only branch to capture it.

    The docblock must carry the house non-vacuity proof convention from `CdmEmergencyGateTest.php:18-45`:
    a numbered procedure to delete one mirror line, watch the named test fail, restore it, and
    confirm `git diff` is empty. Record the result of actually running that procedure for each of
    the three mirror sites — a dual-path test that has never been proven to fail is the exact
    failure mode this file exists to prevent.

    Tests run through PowerShell/Herd only. A Bash `php … | tail` exits 0 having run nothing.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "cd 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; php artisan test --filter=StructuralGatesDualPathTest"</automated>
  </verify>
  <done>Each of the three mirror sites has a test that fails when that mirror is deleted, and the docblock records the non-vacuity proof having been run. Zero-area documents pass without error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser POST → `RamsController::updateAndDownload()` | Engineer-typed review data (room names, client-responsibility notes) enters the pipeline array here |
| `reviewed_data` → `generated_data` | The mirror copies engineer-authored text into the persisted generated document array |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Repudiation | GATE-01/GATE-02 silently failing open | mitigate | This plan IS the mitigation. `StructuralGatesDualPathTest` proves non-vacuity per entry point, with a recorded delete-one-line-watch-it-fail procedure — a gate that reports clean because it cannot see its subject is indistinguishable from a passing gate |
| T-30-08 | Tampering | Engineer-typed room names / notes now persisted into `generated_data` | mitigate | Values are already persisted in `reviewed_data` and already rendered through Blade `{{ }}` escaping; the mirror moves no data across a trust boundary it had not already crossed. No `{!! !!}` anywhere on these paths |
| T-30-09 | Denial of service | Mirroring on the `downloadPdf()` render path | accept | Two array copies of small in-memory structures; no I/O added |
| T-30-SC | Tampering | composer/npm installs | mitigate | Zero packages installed by this plan |
</threat_model>

<verification>
- `php artisan test --filter=StructuralGatesDualPathTest` green (PowerShell)
- `php artisan test --filter=Rams` green — 349-test RAMS suite baseline holds
- Manual grep confirms exactly three mirror sites, each immediately preceding an `upgrade()` call
- `ensurePerRoomBullets()` still early-returns: no `$data['room_overviews']` write was added
</verification>

<success_criteria>
- `client_responsibilities_expanded` and `areas_for_gate` are present on the array `upgrade()`
  receives at all three entry points
- A deleted mirror is caught by a failing test at every site
- Zero-area and zero-client-responsibility documents remain legal and error-free
- No dormant AI path was woken
</success_criteria>

<output>
Create `.planning/phases/30-structural-validation-gates/30-02-SUMMARY.md` when done.
</output>
