---
phase: 30-structural-validation-gates
plan: 09
type: execute
wave: 6
depends_on: ["30-04", "30-08"]
files_modified:
  - tests/Fixtures/rams/21cq30960/record.json
  - tests/Fixtures/rams/21cq30960-defects/record.json
  - tests/Feature/Rams/Snapshot/PdfSnapshotTest.php
  - tests/Feature/Rams/Snapshot/DocxSnapshotTest.php
  - tests/Feature/Rams/StructuralGatesRealDocumentTest.php
autonomous: false
requirements: [GATE-01, GATE-02, GATE-04, GATE-13, GATE-14]

must_haves:
  truths:
    - "A defect-bearing 21CQ30960 fixture makes GATE-13 and GATE-14 fire on the real-world document shape"
    - "A clean 21CQ30960 fixture passes all five gates with every flag armed — no false positives"
    - "The clean fixture is wired into both snapshot test files, not merely dropped in a folder"
    - "No warning text appears in the regenerated 21CQ30960 PDF or DOCX"
  artifacts:
    - path: "tests/Fixtures/rams/21cq30960/record.json"
      provides: "Clean post-fix regeneration of the canonical real document"
      contains: "_notes"
    - path: "tests/Fixtures/rams/21cq30960-defects/record.json"
      provides: "Pre-fix RAMS 97 shape carrying the GATE-13 and GATE-14 defects"
      contains: "_notes"
    - path: "tests/Feature/Rams/StructuralGatesRealDocumentTest.php"
      provides: "ROADMAP criterion 4, proven by fixture rather than live-only UAT"
  key_links:
    - from: "tests/Feature/Rams/Snapshot/PdfSnapshotTest.php"
      to: "tests/Fixtures/rams/21cq30960"
      via: "explicit new test methods — fixture names are hardcoded literals, there is no data provider"
      pattern: "21cq30960"
---

<objective>
Author the two 21CQ30960 fixtures, wire the clean one into both snapshot test files, and prove
ROADMAP criterion 4 automatically: all five gates armed, against a real regenerated project, passing
clean with no false positives and no warning text in the generated document.

Purpose: 21CQ30960 (VW Blakelands) is the canonical target. Its professional review — RAMS 97 of
2026-08-25 — is what produced GATE-13 and GATE-14 in the first place. The apparent conflict between
ROADMAP criterion 4 ("regenerates clean") and CONTEXT Specifics ("the document containing the
defects") is not a conflict: these are the same project at two points in time. Pre-fix carried the
defects; criterion 4 wants a fresh post-fix regeneration to pass. Both fixtures are needed — one
proves the gates fire on the real-world shape, one proves they do not false-positive.

Two things make this more than a folder drop. First, no 21CQ30960 fixture exists today; grep finds
only docblock mentions. Second, and verified in `30-PATTERNS.md`: fixture names are **hardcoded
string literals** in `PdfSnapshotTest.php` (`:206/207/214/215/235`) and `DocxSnapshotTest.php`
(`:102/104/111/113/139`). There is no data provider. `rams:regenerate-snapshots` auto-discovers a
new directory; the TESTS do not.

Output: two fixtures, snapshot wiring in both files, a real-document gate test, and the phase-gate
full-suite run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-structural-validation-gates/30-CONTEXT.md
@.planning/phases/30-structural-validation-gates/30-PATTERNS.md
@.planning/phases/30-structural-validation-gates/30-VALIDATION.md
@.planning/phases/30-structural-validation-gates/30-03-SUMMARY.md
@.planning/phases/30-structural-validation-gates/30-06-SUMMARY.md
@.planning/phases/30-structural-validation-gates/30-07-SUMMARY.md
@.planning/phases/30-structural-validation-gates/30-08-SUMMARY.md
@tests/Fixtures/rams/tilda-21cq29531/record.json
@tests/Feature/Rams/Snapshot/PdfSnapshotTest.php
@CLAUDE.md

<interfaces>
Fixture precedent — `tests/Fixtures/rams/tilda-21cq29531/`:
  record.json                16,204 bytes   <- the ONLY hand-authored file
  expected-html-v1.html      67,300         <- generated golden
  expected-html-v2.html      67,963         <- generated golden
  expected-docx-v1.xml.norm 177,444         <- generated golden
  expected-docx-v2.xml.norm 177,544         <- generated golden

record.json top-level shape (parsed, verified):
  { "_notes": string,
    "project": { name, client_name, site_address, ref },
    "rams": { project_ref, project_name, client_name, site_address, status,
              generated_data: {...}, reviewed_data: {...}, form_data: {...} } }

`_notes` is load-bearing. Tilda's states why it is hand-crafted, what it was modelled on, and the
regeneration command. Both new fixtures must state the same three things, plus which defects each
deliberately carries or deliberately does not.

Golden capture:  php artisan rams:regenerate-snapshots 21cq30960 --force
  (`RamsRegenerateSnapshotsCommand.php:56-100`; scans `base_path('tests/Fixtures/rams')` for
   directories, writes all four goldens per fixture at `:95`, runs in an always-rolled-back nested
   transaction, prompts unless `--force`.)

Snapshot test mechanics (`PdfSnapshotTest.php:1-120`):
  `#[Group('snapshot')]` on the class (`:54`) — excluded from the default run by `phpunit.xml:21-25`;
  run via `vendor/bin/phpunit --group snapshot`
  `private const FIXTURE_DIR = __DIR__ . '/../../../Fixtures/rams';` (`:58`)
  `private const FIXED_DATE = '2026-07-25 10:30:00';` pinned via `Carbon::setTestNow()` (`:64-77`)
  `renderBothBlades(string $fixture)` (`:85-120`) builds User + Project + RamsDocument from
  `$fx['rams'][...]`, force-sets `created_at`, runs `RamsDisplayPatchService::patch()`, then
  `RamsDocumentComposer::compose()`
  First run CAPTURES the golden and skips the assertion; later runs assert byte-equality (`:31-35`)

The defects to reproduce in `21cq30960-defects`:
  GATE-13 — RA18 asserts no hot works, §6.8 requires a hot-works permit for soldering
            (unconditional wording, not `addPermitAndIsolation():939`'s conditional line),
            COSHH carries Tin/Lead solder and rosin flux. Three sections disagreeing.
  GATE-14 — Step 4 "Display & Mount Installation" cites RA11/12/13/21 but omits RA01 Working at
            Height and RA02 Manual Handling, on a step entirely about lifting displays onto wall
            mounts. Both hazards must be PRESENT in the register — GATE-14 only fires on an
            uncited-but-present hazard.
Note assumption A6: neither the document nor a fixture of it exists in the repo, so the specific RA
numbers are unverified. Reproduce the defect CLASS faithfully; if the RA numbering has to shift to
match the register you author, say so in `_notes`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author both 21CQ30960 fixtures and the real-document gate test</name>
  <files>tests/Fixtures/rams/21cq30960/record.json, tests/Fixtures/rams/21cq30960-defects/record.json, tests/Feature/Rams/StructuralGatesRealDocumentTest.php</files>
  <behavior>
    - With all five gates armed, running `upgrade()` over the CLEAN `21cq30960` fixture throws
      nothing and produces an EMPTY `compliance_warnings` array — ROADMAP criterion 4
    - With `RAMS_HOT_WORKS_GATE` armed, the DEFECTS fixture throws a GATE-13 contradiction naming
      both the permit requirement and the COSHH substance
    - With `RAMS_MISSING_RISK_REF_GATE` armed, the DEFECTS fixture produces GATE-14 warnings naming
      Working at Height and Manual Handling against the Step 4 phase
    - With all five flags false, BOTH fixtures pass silently
    - The clean fixture contains at least one populated area in `reviewed_data.room_overviews`, a
      populated `client_responsibilities_expanded`, and a hazard register with complete pre-scores —
      so GATE-01, GATE-02 and GATE-04 are exercised non-vacuously rather than passing on empty input
  </behavior>
  <action>
    Author `tests/Fixtures/rams/21cq30960/record.json` (clean) and
    `tests/Fixtures/rams/21cq30960-defects/record.json` (defect-bearing), both following the Tilda
    top-level shape exactly. Hand-craft them as Tilda was hand-crafted — the real record is not
    reachable from the dev environment.

    Write the `_notes` for each: why it is hand-crafted, what it was modelled on, the regeneration
    command, and — new for these two — which defects it deliberately carries and which it
    deliberately does not. Record explicitly that these are the same project at two points in time:
    RAMS 97 (2026-08-25, pre-fix) carried the defects; the clean fixture is the post-fix
    regeneration ROADMAP criterion 4 requires.

    The clean fixture must exercise all five gates NON-VACUOUSLY. A fixture that passes because its
    arrays are empty proves nothing — that is the exact failure this phase exists to prevent. It
    needs real areas covered by real method steps, real client-responsibility entries supporting any
    trigger phrases it contains, and complete hazard pre-scores.

    The defects fixture reproduces the two named defect classes. Note the GATE-13 permit reference
    must use UNCONDITIONAL wording; `addPermitAndIsolation()` will add its own conditional line at
    `:939` during `upgrade()` and that line must not be what trips the gate.

    Create `tests/Feature/Rams/StructuralGatesRealDocumentTest.php` driving both fixtures through
    `upgrade()` with `config()->set()` toggling flags per test. This is what converts ROADMAP
    criterion 4 from a live-only UAT step into an automated gate.

    Write the test first, then author the fixtures to satisfy it. PowerShell/Herd only.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "cd 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; php artisan test --filter=StructuralGatesRealDocumentTest"</automated>
  </verify>
  <done>The clean fixture passes all five armed gates with zero warnings and non-empty inputs; the defects fixture fires GATE-13 and warns GATE-14.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the clean fixture into both snapshot test files and capture goldens</name>
  <files>tests/Feature/Rams/Snapshot/PdfSnapshotTest.php, tests/Feature/Rams/Snapshot/DocxSnapshotTest.php</files>
  <action>
    Add explicit new test methods for `21cq30960` to BOTH `PdfSnapshotTest.php` and
    `DocxSnapshotTest.php`, mirroring the existing `tilda-21cq29531` methods. This step is mandatory
    and is NOT a folder drop: the fixture names are hardcoded string literals in both files
    (`PdfSnapshotTest.php:206/207/214/215/235`, `DocxSnapshotTest.php:102/104/111/113/139`) and there
    is no data provider. `rams:regenerate-snapshots` auto-discovers the directory; the tests do not.
    Either add the methods or refactor both files to a provider — adding methods is the smaller diff
    and matches CLAUDE.md's minimal-diff posture.

    Wire ONLY the clean fixture into the snapshot suite. The defects fixture is a gate fixture, not a
    render golden; snapshotting a deliberately-broken document would lock in broken output.

    Then capture the four goldens with `php artisan rams:regenerate-snapshots 21cq30960 --force` and
    review the captured output diff-first before committing it, as Plan 29-14 did: every line of a
    newly-captured golden should be explainable. Confirm the captured PDF and DOCX contain no
    `compliance_warnings` text and no warning `message` string — the Plan 30-04 leak assertion covers
    the mechanism; this confirms it on the real document, which is ROADMAP criterion 4's second half.

    Run the snapshot suite twice: the first run captures and skips the assertion, the second run must
    assert byte-equality and pass.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "cd 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; vendor\bin\phpunit --group snapshot"</automated>
  </verify>
  <done>Both snapshot files carry explicit 21cq30960 methods, four goldens are captured and reviewed, the suite is green on a second run, and neither generated artefact contains warning text.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Phase gate — full suite and golden review</name>
  <what-built>
    All five gates (GATE-01, GATE-02, GATE-04, GATE-13, GATE-14), the `compliance_warnings` channel
    and its review-screen surfaces, the two data-reachability mirrors, the three disarmed kill
    switches, the corrected ROADMAP, the arming runbook, and the two 21CQ30960 fixtures.
  </what-built>
  <how-to-verify>
    Run everything from **PowerShell / Herd**, never from Bash — `php … | tail` from the Bash tool
    exits 0 while running nothing, which is a silently faked green gate.

    1. `php artisan test` — expect green apart from the one known pre-existing `QueueRecoverCommandTest`
       failure documented at Phase 29 closeout. It is not a Phase 30 regression.
    2. `vendor\bin\phpunit --group snapshot` — expect green.
    3. `php artisan test --filter=Rams` — expect the RAMS suite at or above its 349-test /
       1,604-assertion Phase 29 baseline.
    4. Review `git diff` on the four newly captured `21cq30960` goldens. Every line should be
       explainable as output of the clean fixture. Confirm no warning text appears in any of them.
    5. Confirm all three flags are still disarmed: `structural_gates_enabled`,
       `missing_risk_ref_gate_enabled` and `hot_works_gate_enabled` all read `env(..., false)` in
       `config/rams_tier1.php`, and no `.env` on any machine has been edited to arm them. Arming is
       the operator runbook in `30-MEASUREMENT.md`, not part of this phase.
    6. Open the RAMS review screen for any existing document and confirm it looks unchanged — with
       every flag false there are no warnings, so the panel must render nothing at all.
  </how-to-verify>
  <action>Run steps 1-6 below and paste the results, then wait for the developer. Do not proceed past
  this checkpoint on your own — this is the phase gate.</action>
  <resume-signal>Type "approved" or describe what failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture → captured golden | A golden captured from broken output permanently encodes that breakage as expected |
| `generated_data` → captured PDF/DOCX golden | Where a warning leak into a client-facing document would become visible on the real document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02 | Information disclosure | Warning text reaching the regenerated 21CQ30960 PDF or DOCX | mitigate | Asserted on the real document in Task 2 and re-confirmed by human review of the four captured goldens in Task 3, on top of Plan 30-04's mechanism-level assertion |
| T-30-20 | Tampering | Snapshotting the defect-bearing fixture and locking in broken output as golden | mitigate | Only the CLEAN fixture is wired into the snapshot suite; the defects fixture is a gate fixture only, stated in its `_notes` |
| T-30-04 | Repudiation | A clean fixture that passes because its arrays are empty | mitigate | The clean fixture is required to carry populated areas, client responsibilities and complete hazard pre-scores, so all five gates are exercised non-vacuously |
| T-30-21 | Repudiation | A Bash-run test suite reporting green while executing nothing | mitigate | Every verify command in this plan runs through PowerShell; the human checkpoint states the trap explicitly |
| T-30-SC | Tampering | composer/npm installs | mitigate | Zero packages installed across the entire phase (30-RESEARCH Package Legitimacy Audit: not applicable — no package names to verify; `composer.json` and `package.json` unchanged) |
</threat_model>

<verification>
- `php artisan test --filter=StructuralGatesRealDocumentTest` green (PowerShell)
- `vendor\bin\phpunit --group snapshot` green on a second run (PowerShell)
- `php artisan test` green modulo the known `QueueRecoverCommandTest` failure
- All three Phase 30 flags still default false
</verification>

<success_criteria>
- ROADMAP criterion 4 is proven by fixture, automatically, on every test run
- Both the fire-on-defect and no-false-positive properties are demonstrated on the real-world
  document shape
- The clean fixture is genuinely wired into both snapshot test files
- No warning text reaches a client-facing artefact
- The phase ships with every gate disarmed, as D-03 requires
</success_criteria>

<output>
Create `.planning/phases/30-structural-validation-gates/30-09-SUMMARY.md` when done. Record in it:
the two known limitations from Plan 30-05 (`review()` does not run the gates; the Blade-side
hot-works permit derivation at `pdf/rams.blade.php:407-409` is invisible to GATE-13), the
no-backfill-migration conclusion, and the pointer to `30-MEASUREMENT.md` as the arming runbook
Phase 31 inherits.
</output>
