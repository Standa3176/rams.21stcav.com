---
phase: 30-structural-validation-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - config/rams_tier1.php
  - .env.example
  - app/Services/Rams/StructuralGateVocabulary.php
  - app/Services/Rams/RamsComplianceUpgradeService.php
  - tests/Unit/Services/Rams/StructuralGateConfigTest.php
  - tests/Unit/Services/Rams/StructuralGateVocabularyTest.php
  - tests/Feature/Rams/ComplianceWarningsChannelTest.php
autonomous: true
requirements: [GATE-01, GATE-02, GATE-04, GATE-13, GATE-14]

must_haves:
  truths:
    - "Three new, independent kill-switch flags exist and all three default to false"
    - "GATE-01's trigger vocabulary and GATE-14's implication map live in config as data, editable without a deploy"
    - "A single shared vocabulary source answers 'is this signal supported on the hazard side' and 'is it supported on the client-responsibility side' — there is no second parallel vocabulary"
    - "upgrade() writes generated_data['compliance_warnings'] on every run, overwriting wholesale, even when empty"
  artifacts:
    - path: "config/rams_tier1.php"
      provides: "structural_gates_enabled, missing_risk_ref_gate_enabled, hot_works_gate_enabled + structural_gate_triggers + missing_risk_implications"
      contains: "structural_gates_enabled"
    - path: "app/Services/Rams/StructuralGateVocabulary.php"
      provides: "Shared signal-matching helper reusing HazardIncludeWhenResolver's const maps; no DB, no AI"
      exports: ["signalMatchesHazards", "signalMatchesClientReqs", "flattenClientResponsibilities", "flattenAreas"]
    - path: "tests/Feature/Rams/ComplianceWarningsChannelTest.php"
      provides: "Proof the warn key is overwritten wholesale and clears stale entries"
  key_links:
    - from: "app/Services/Rams/StructuralGateVocabulary.php"
      to: "app/Services/Rams/HazardIncludeWhenResolver.php"
      via: "const map reuse (never ::resolve())"
      pattern: "HazardIncludeWhenResolver::"
    - from: "app/Services/Rams/RamsComplianceUpgradeService.php"
      to: "compliance_warnings"
      via: "unconditional wholesale write inside upgrade()"
      pattern: "compliance_warnings"
---

<objective>
Lay the Phase 30 foundation: the three independent kill-switch flags (all disarmed), the
config-resident trigger vocabulary and implication map, the single shared matching helper
every gate will use, and the `compliance_warnings` advisory channel that GATE-04 and GATE-14
write to.

Purpose: four of the five gates are blocked on shared machinery that does not exist yet
(D-04 flags, D-06 vocabulary, D-07/D-08 single-vocabulary rule, UI-SPEC's warn channel).
Building it once, first, is what keeps the five gate plans small and keeps D-07's
"do not build a second, parallel hazard-matching vocabulary" true by construction.

Output: three config flag blocks, two config data tables, one new service class,
one new `generated_data` key, three test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-structural-validation-gates/30-CONTEXT.md
@.planning/phases/30-structural-validation-gates/30-RESEARCH.md
@.planning/phases/30-structural-validation-gates/30-PATTERNS.md
@.planning/phases/30-structural-validation-gates/30-UI-SPEC.md
@config/rams_tier1.php
@app/Services/Rams/HazardIncludeWhenResolver.php
@app/Services/Rams/RamsComplianceUpgradeService.php
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Do not go exploring for these. -->

Existing, read before writing:
- `RamsComplianceUpgradeService::upgrade(array $ramsData): array` — the single dispatch
  point. Method opens at `:36`, body ends `:104`. Existing flag blocks at `:62-64`
  (`display_lift_gate_enabled`, default true), `:75-77` (`ffp2_confined_space_gate_enabled`,
  default true), `:97-101` (`cdm_ae_gate_enabled`, default **false** — the D-03 precedent).
  `crossReferenceMethodStatementRisks()` runs at `:80`, `addPermitAndIsolation()` at `:53`,
  `cleanTextArtifacts()` at `:102`.
- Class docblock invariant at `RamsComplianceUpgradeService.php:19` — "Deterministic. No AI.
  No database." This is load-bearing for Task 2.
- `HazardIncludeWhenResolver` const maps: `TIER2_ACTIVITY_SIGNALS` `:47-52`,
  `TIER2_KEYWORD_SIGNALS` `:58+`, `TIER3_KEYWORD_PRECHECK` including
  `'asbestos' => ['asbestos','pre-2000','pre 2000','age unknown','built before 2000']`
  at `:115-121`. `resolve(Collection $library, array $signals): Collection` at `:150`
  queries Eloquent models — **must not be called** from this phase.
- `config/rams_tier1.php` flag-block idiom: `:101-134` (`cdm_ae_gate_enabled`), terminal line
  `'cdm_ae_gate_enabled' => env('RAMS_CDM_AE_GATE', false),` at `:134`.
- `config/rams_tier1.php` data-table idiom: `standards_references` at `:250-303` —
  banner comment, then a flat list of uniform-key rows.
- `.env.example` currently contains exactly one `RAMS_` line (`:62 RAMS_NOTIFICATION_BCC=`).
  No shipped gate flag has ever been listed there — adding these is a NEW convention, not a
  copied one. Introduce them deliberately, with a comment.

New contract this plan defines (consumed by plans 30-03, 30-06, 30-07, 30-08):

  final class StructuralGateVocabulary
      public const SUPPORTED_SIGNALS: string[]                       // signal keys valid in config
      public static function phrasesForSignal(string $signal): array // from HazardIncludeWhenResolver maps
      public static function signalPresentInText(string $signal, string $haystack): bool
      public static function signalMatchesHazards(string $signal, array $hazards): bool
      public static function signalMatchesClientReqs(string $signal, array $clientReqStrings): bool
      public static function flattenClientResponsibilities(array $data): array  // string[]
      public static function flattenAreas(array $data): array                   // string[]

New `generated_data` key contract (UI-SPEC, exact shape — plans 30-04/30-06/30-08 depend on it):

  'compliance_warnings' => [
      ['gate' => 'GATE-04', 'hazard_index' => int|null, 'hazard' => string,
       'message' => string],
      ['gate' => 'GATE-14', 'hazard_index' => null, 'step_title' => string,
       'hazard' => string, 'message' => string],
  ]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Three disarmed flag blocks and two config vocabulary tables</name>
  <files>config/rams_tier1.php, .env.example, tests/Unit/Services/Rams/StructuralGateConfigTest.php</files>
  <behavior>
    - `config('rams_tier1.structural_gates_enabled')` is false with no env override
    - `config('rams_tier1.missing_risk_ref_gate_enabled')` is false with no env override
    - `config('rams_tier1.hot_works_gate_enabled')` is false with no env override
    - Each of the three reads a DISTINCT env var, and none of the three env var names is
      `RAMS_DISPLAY_LIFT_GATE`, `RAMS_PPE_CEILING_ELECTRICAL_GATE` or `RAMS_CDM_AE_GATE`
      (assert by scanning the raw config source for the three new names appearing exactly once each)
    - `config('rams_tier1.structural_gate_triggers')` is a non-empty list of rows each having
      exactly the keys `phrase`, `signal`, `label`
    - Every `signal` value in `structural_gate_triggers` is a key present in
      `HazardIncludeWhenResolver`'s TIER2/TIER3 const maps (D-07: one vocabulary, no orphan signals)
    - `config('rams_tier1.missing_risk_implications')` is a non-empty list of rows each having
      exactly the keys `phrase`, `signal`, `label`, and every `signal` likewise resolves
  </behavior>
  <action>
    Add three config blocks to `config/rams_tier1.php`, each modelled line-for-line on the
    `cdm_ae_gate_enabled` block at `:101-134` (the only existing default-false block), and each
    carrying the four load-bearing paragraphs that block carries: (1) exactly which methods it
    gates, (2) that false means `upgrade()` proceeds byte-identical with no redeploy, (3) that it
    is a NEW, INDEPENDENT flag per D-04 that deliberately never reuses `RAMS_DISPLAY_LIFT_GATE`,
    `RAMS_PPE_CEILING_ELECTRICAL_GATE` or `RAMS_CDM_AE_GATE`, and (4) the D-03 rationale that
    Phase 30's corpus has not been measured clean so armed-by-default is not available, ending in
    the deploy order (ship disarmed, verify a live regeneration, flip each flag as a separate
    one-line `.env` change). The three terminal lines are
    `structural_gates_enabled => env('RAMS_STRUCTURAL_GATES', false)` gating GATE-01/02/04,
    `missing_risk_ref_gate_enabled => env('RAMS_MISSING_RISK_REF_GATE', false)` gating GATE-14,
    and `hot_works_gate_enabled => env('RAMS_HOT_WORKS_GATE', false)` gating GATE-13. The
    GATE-13 block must additionally record D-02: it ships disarmed and is flipped in Phase 31
    once RULE-05/GATE-10 make the COSHH table job-conditional, because both halves currently
    false-positive corpus-wide — the COSHH half via `Tier1RamsDefaultsService:81`'s unconditional
    `coshh_baseline`, and the permit half via `RamsComplianceUpgradeService::addPermitAndIsolation()`
    emitting its hot-works-permit line on every document at `:939` (RESEARCH Finding 5).

    Add two data tables using the `standards_references` idiom at `:250-303` — banner comment,
    then a flat list of uniform-key rows. `structural_gate_triggers` is GATE-01's D-06 vocabulary:
    rows of `phrase` (the literal lowercase text a method step or hazard control may contain),
    `signal` (a key that already exists in `HazardIncludeWhenResolver`'s const maps), and `label`
    (human wording for the error message). Seed it from the canonical PORTING-NOTES triggers:
    asbestos register, asbestos survey, refurbishment and demolition survey, permit to work,
    hot-works permit, isolation certificate, hold point, and the pre-2000 age-unknown phrasings —
    every row's `signal` MUST be an existing map key (`asbestos` already exists at `:115-121`).
    `missing_risk_implications` is GATE-14's implication map per RESEARCH Finding 7: rows of
    `phrase` (step-action text such as wall mount, onto the bracket, lift the display, above 2m,
    stepladder, podium, ceiling void), `signal`, and `label`. It is explicitly NOT
    `crossReferenceMethodStatementRisks()`'s `$keywordRiskMap` and the banner comment must say so,
    naming the "a gate must never re-derive the thing it is checking" guard at
    `RamsComplianceUpgradeService.php:1268-1276`.

    Add the three env vars to `.env.example`, commented false, with a short note that they are
    Phase 30 gate kill-switches shipping disarmed. Per 30-PATTERNS.md there is no precedent for
    gate flags in `.env.example` — introduce this deliberately and say so in the comment; do not
    present it as matching an existing pattern.

    Write the tests first, then the config. Tests run through PowerShell/Herd only.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "cd 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; php artisan test --filter=StructuralGateConfigTest"</automated>
  </verify>
  <done>All three flags default false, read three distinct new env vars, and the two vocabulary tables validate their signal keys against HazardIncludeWhenResolver's maps. StructuralGateConfigTest green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StructuralGateVocabulary — the single shared matching helper</name>
  <files>app/Services/Rams/StructuralGateVocabulary.php, tests/Unit/Services/Rams/StructuralGateVocabularyTest.php</files>
  <behavior>
    - `phrasesForSignal('asbestos')` returns the phrases from HazardIncludeWhenResolver's
      TIER3_KEYWORD_PRECHECK entry, not a hand-copied list
    - `phrasesForSignal('a-signal-that-does-not-exist')` returns an empty array (never throws)
    - `signalMatchesHazards('asbestos', [...hazard rows named "Asbestos-containing materials"...])`
      is true; against a register with no asbestos hazard it is false
    - `signalMatchesClientReqs` matches case-insensitively against a flat string list
    - `flattenClientResponsibilities` returns the union of `client_responsibilities` (flat string
      list) and the searchable sub-fields of `client_responsibilities_expanded`: each fixed
      bucket's `notes`, every `additional[*].item`, every `additional[*].notes`, plus a synthetic
      label for any fixed bucket whose `required` is true (D-08)
    - `flattenClientResponsibilities` on an array missing both keys returns `[]`, never throws
    - `flattenAreas` reads the gate-private mirror key first and returns trimmed, non-empty,
      de-duplicated area names; returns `[]` when no area source is present
    - The class performs no database query and makes no HTTP/AI call
  </behavior>
  <action>
    Create `app/Services/Rams/StructuralGateVocabulary.php` as a final class of public static
    methods, following the shipped judgement-helper precedent (`DisplayLiftPolicy`,
    `ControlTextRuleViolations`, `SiteEmergencyResolver`) — pattern S5 in 30-PATTERNS.md: the gate
    owns the loop and the throw, this class owns the judgement.

    Signal phrases come from `HazardIncludeWhenResolver`'s const maps per D-07. Per RESEARCH
    Finding 6 you must reuse the **const keyword maps**, never call `resolve()`: `resolve()` takes
    a `Collection` of `HazardTemplate` Eloquent models and would break
    `RamsComplianceUpgradeService`'s "Deterministic. No AI. No database." invariant at `:19`.
    If the maps are private, expose them as public consts on `HazardIncludeWhenResolver` (a
    visibility change only, no logic change — keep the migration diff minimal per CLAUDE.md) and
    read them from here. Union TIER2_ACTIVITY_SIGNALS, TIER2_KEYWORD_SIGNALS and
    TIER3_KEYWORD_PRECHECK when resolving a signal key to its phrase list.

    Matching is conservative by construction (pattern S3): case-folded substring containment,
    unknown signal yields no match rather than a throw, and an unparseable or absent input yields
    no match rather than a violation. Prefer a miss to a false positive — ROADMAP criterion 4
    makes "no false positives against legitimate output" an acceptance criterion.

    `flattenClientResponsibilities` implements D-08's union. The shape of
    `client_responsibilities_expanded` is confirmed at `RamsController.php:520-536`: four fixed
    buckets (`network_readiness`, `licences`, `access`, `power_validation`), each
    `['required' => bool, 'notes' => string]`, plus `'additional' => list of ['item' => string,
    'notes' => string]`. The four fixed buckets carry NO free text beyond `notes`, so their human
    labels must come from a small config map rather than being duplicated from the Blade's
    `$crExpLabels` at `pdf/rams.blade.php:1588-1593`.

    `flattenAreas` reads the gate-private mirror key that Plan 30-02 writes (`areas_for_gate`),
    falling back to `$data['rooms']` when present. Do NOT read or populate
    `$data['room_overviews']` — per RESEARCH Finding 3 / assumption A2 that key is the trigger for
    the long-dormant `ensurePerRoomBullets()` AI path and waking it is a behaviour change this
    phase does not want.

    Write the tests first. Tests run through PowerShell/Herd only.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "cd 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; php artisan test --filter=StructuralGateVocabularyTest"</automated>
  </verify>
  <done>One vocabulary source exists, sourced from HazardIncludeWhenResolver's consts, with zero DB access and conservative miss-over-false-positive semantics. StructuralGateVocabularyTest green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The compliance_warnings advisory channel</name>
  <files>app/Services/Rams/RamsComplianceUpgradeService.php, tests/Feature/Rams/ComplianceWarningsChannelTest.php</files>
  <behavior>
    - After `upgrade()` runs with every Phase 30 flag false, `$result['compliance_warnings']` is
      present and is an empty array
    - An input array carrying a stale `compliance_warnings` entry comes back with that entry gone
      — the key is overwritten wholesale, never appended to
    - Apart from the new key, `upgrade()`'s output with all three flags false is identical to its
      output before this plan (capture the pre-change output in the test as the baseline)
    - Nothing in `upgrade()` throws because of the new key
  </behavior>
  <action>
    Add the `compliance_warnings` write to `upgrade()`. Per 30-PATTERNS.md section 5 the write
    must be **unconditional**, following the `resolveSiteEmergency()` precedent at `upgrade():83-86`
    (unconditional enrichment sitting beside a flag-gated throw): if the key were written only
    inside a flag-gated block, a flag flip from true back to false would leave a stale persisted
    warnings array on every document — a disarmed gate still showing findings.

    Initialise `$ramsData['compliance_warnings'] = []` early in `upgrade()` (before the first
    flag-gated block), and have the later gate blocks append to it. UI-SPEC's contract is
    overwrite-wholesale-every-run, never append to a persisted array, including when the result
    is empty.

    Also add the `// ===== N. STRUCTURAL GATES (Phase 30)` section banner near the end of the
    class where plans 30-03, 30-06, 30-07 and 30-08 will add their gate methods, following the
    numbered-banner idiom at `:1260-1262`. Placement note for those later plans, recorded in the
    banner comment: GATE-13 reads `permit_and_isolation` (written at `:53`) and GATE-14 reads
    `associated_risks` (written at `:80`), so all three new dispatch blocks belong after `:80`,
    adjacent to the existing `:97` block, and before `cleanTextArtifacts()` at `:102`.

    The feature test drives `upgrade()` directly and asserts the three behaviours above. Write it
    first. Tests run through PowerShell/Herd only.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "cd 'C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com'; php artisan test --filter=ComplianceWarningsChannelTest"</automated>
  </verify>
  <done>`compliance_warnings` is written on every upgrade() run, wholesale, empty by default; stale entries clear; output otherwise unchanged with all flags false. ComplianceWarningsChannelTest green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.env` → `config()` → `upgrade()` | Operator-controlled flag values decide whether a safety gate runs at all |
| config data tables → gate matching | Config-authored vocabulary, not user-authored; no untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-03 | Tampering | The three new flag blocks in `config/rams_tier1.php` | mitigate | Each flag reads its OWN new env var; StructuralGateConfigTest asserts none of the three names collides with `RAMS_DISPLAY_LIFT_GATE`, `RAMS_PPE_CEILING_ELECTRICAL_GATE` or `RAMS_CDM_AE_GATE`, so one gate's rollback can never disarm another's (D-04 doctrine) |
| T-30-06 | Elevation of privilege | `StructuralGateVocabulary` calling into Eloquent | mitigate | Reuse `HazardIncludeWhenResolver`'s const maps only; never call `resolve()`. Preserves `RamsComplianceUpgradeService.php:19`'s "No AI. No database." invariant, which also removes a DB round-trip from a render path (DoS surface) |
| T-30-07 | Denial of service | Gate matching runs on the PDF render path | accept | All matching is O(triggers × hazards) over small in-memory arrays with no I/O; measured risk negligible |
| T-30-SC | Tampering | composer/npm installs | mitigate | Phase 30 installs ZERO packages (30-RESEARCH Package Legitimacy Audit: not applicable, no package names to verify). If this task finds itself adding a dependency, the spec has been misread — stop and escalate |
</threat_model>

<verification>
- `php artisan test --filter=StructuralGate` green (PowerShell)
- `php artisan test --filter=ComplianceWarningsChannelTest` green (PowerShell)
- `php artisan test --filter=Rams` still green — no regression from the new unconditional key
- `git diff config/rams_tier1.php` shows three flag blocks whose terminal lines all end `, false),`
</verification>

<success_criteria>
- Three independent, disarmed kill-switch flags exist (D-03, D-04)
- GATE-01's trigger vocabulary and GATE-14's implication map are config data (D-06), with every
  signal key validated against the single shared vocabulary (D-07)
- `StructuralGateVocabulary` is the only matching vocabulary in the phase, is DB-free, and
  implements D-08's client-responsibility union
- `compliance_warnings` is written unconditionally and wholesale on every `upgrade()` run
</success_criteria>

<output>
Create `.planning/phases/30-structural-validation-gates/30-01-SUMMARY.md` when done.
</output>
