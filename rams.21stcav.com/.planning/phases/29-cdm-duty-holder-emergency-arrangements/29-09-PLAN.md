---
phase: 29-cdm-duty-holder-emergency-arrangements
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - resources/views/pdf/rams.blade.php
  - tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php
autonomous: true
requirements: [RULE-08]
must_haves:
  truths:
    - "rams.blade.php's Section 7.0 A&E row never renders blank when site_emergency_resolved is absent — it degrades to the safe hold-point line via SiteEmergencyResolver::resolve(), read-only, never writing back into nearest_hospital"
    - "The primary download path (which always runs upgrade() first) is unchanged — this fix only changes behavior when site_emergency_resolved is missing"
  artifacts:
    - path: "resources/views/pdf/rams.blade.php"
      provides: "Section 7.0 A&E cell falls back to SiteEmergencyResolver::resolve($siteEmerg)['text'] when site_emergency_resolved is absent"
  key_links:
    - from: "resources/views/pdf/rams.blade.php"
      to: "app/Services/Rams/SiteEmergencyResolver.php::resolve()"
      via: "inline fallback call using the already-computed local $siteEmerg array"
      pattern: "SiteEmergencyResolver::resolve\\("
---

<objective>
Close VERIFICATION.md gap 3 / REVIEW.md WR-01: `rams.blade.php:1983` reads
`$data['site_emergency_resolved']['text'] ?? ''` and never invokes `SiteEmergencyResolver::resolve()`
itself. This is masked on the primary download path (`RamsController::downloadPdf()` always runs
`upgrade()` first, which populates `site_emergency_resolved`), but
`RamsRegenerateSnapshotsCommand.php` renders `pdf.rams` via `RamsDisplayPatchService::patch()`
WITHOUT `upgrade()` — so a payload lacking the key renders a BLANK A&E cell, which is worse than
the hold-point line and worse than the pre-Phase-29 defect this phase exists to fix.
`rams-v2.blade.php` and `DocxBuilderService` already delegate correctly to the resolver via the
DTO/composer path — only the legacy `rams.blade.php` is self-insufficient.

Purpose: Make `rams.blade.php` self-sufficient so a missing `site_emergency_resolved` key degrades
to the safe hold-point line, never blank output, matching the D-01 "no render site re-derives
independently... except reads must never leave a caller high and dry" spirit.
Output: Inline resolver fallback in `rams.blade.php`; a regression test proving the fallback
degrades to the hold-point line (not blank) when the key is absent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-CONTEXT.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-VERIFICATION.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-REVIEW.md
</context>

<interfaces>
<!-- resources/views/pdf/rams.blade.php:1974-1990 (Section 7.0) -->
Existing local variable, already computed a few lines above the A&E cell (`@php` block, ~:1975-1977):
```
$siteEmerg = (array) ($data['site_emergency'] ?? ($rams->reviewed_data['site_emergency'] ?? []));
$hasSiteEmerg = ! empty(array_filter($siteEmerg, fn ($v) => is_string($v) ? trim($v) !== '' : ! empty($v)));
```
Current A&E cell read, line 1983:
```
{{ $data['site_emergency_resolved']['text'] ?? '' }}
```
Resolver signature (`app/Services/Rams/SiteEmergencyResolver.php`):
```
public static function resolve(array $siteEmergency): array   // returns ['verified' => bool, 'text' => string]
```
`DocxBuilderService.php` already calls fully-qualified classes inline (e.g.
`\App\Services\Rams\RamsComplianceUpgradeService::DEFAULT_PRINCIPAL_DESIGNER_NOTE` at
`app/Services/DocxBuilderService.php:1706`) — this blade has no `use` import section, so the
fallback call must use the same fully-qualified `\App\Services\Rams\SiteEmergencyResolver::resolve(...)`
form.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fall back to SiteEmergencyResolver::resolve() when site_emergency_resolved is absent</name>
  <files>resources/views/pdf/rams.blade.php, tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php</files>
  <read_first>
    resources/views/pdf/rams.blade.php lines 1965-2000 (Section 7.0 A&E row, the `$siteEmerg`/`$hasSiteEmerg` @php block and the emerg-table)
    tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php (whole file — `renderBothBladesWith()` helper builds a full RamsDocument through `RamsDisplayPatchService::patch()` + `RamsComplianceUpgradeService::upgrade()`; this task's new test must render WITHOUT calling `upgrade()`, matching `RamsRegenerateSnapshotsCommand`'s actual bypass path)
    app/Services/Rams/SiteEmergencyResolver.php (resolve() signature and HOLD_POINT constant, confirm return shape)
  </read_first>
  <behavior>
    - Test: render `pdf.rams` directly with `$data` containing a populated `site_emergency` array (e.g. blank `nearest_hospital`/`hospital_address`) but NO `site_emergency_resolved` key at all (simulating `RamsRegenerateSnapshotsCommand`'s `RamsDisplayPatchService::patch()`-only path) — the rendered Nearest A&E Hospital cell must contain the HOLD_POINT text ("to be confirmed at induction (must be a 24/7 Emergency Department)"), NOT be empty/blank.
    - Test: render `pdf.rams` with `$data` containing a populated `site_emergency` (verified name+address) and NO `site_emergency_resolved` key — the cell must contain the resolved verified text (hospital name + address), not blank, not the hold-point line.
    - Existing test `test_hold_point_state_v1_blade_shows_holdpoint_no_banned_string_no_tbc` (which DOES run the full `upgrade()` pipeline and so already has `site_emergency_resolved` populated) must keep passing unchanged — this fix must not alter behavior when the key IS present.
  </behavior>
  <action>
    In `resources/views/pdf/rams.blade.php`, change the Section 7.0 A&E cell expression at line 1983
    from `{{ $data['site_emergency_resolved']['text'] ?? '' }}` to fall back through
    `\App\Services\Rams\SiteEmergencyResolver::resolve($siteEmerg)['text']` when
    `$data['site_emergency_resolved']` is absent — e.g.
    `{{ $data['site_emergency_resolved']['text'] ?? \App\Services\Rams\SiteEmergencyResolver::resolve($siteEmerg)['text'] }}`,
    reusing the `$siteEmerg` local variable already computed a few lines above (do NOT recompute it
    or read a different source — `$siteEmerg` is already the canonical merge of
    `$data['site_emergency']` / `reviewed_data['site_emergency']`). This is a READ-ONLY fallback —
    it must never write `$siteEmerg['nearest_hospital']` or persist anything back to `$data`,
    `$rams`, or any column; it only affects what this one cell prints. Do not touch
    `rams-v2.blade.php`, `DocxBuilderService.php`, or the composer/DTO layer — they already
    delegate correctly and are out of this gap's scope.
  </action>
  <acceptance_criteria>
    - `grep -n "SiteEmergencyResolver::resolve" resources/views/pdf/rams.blade.php` shows exactly one match, on the Section 7.0 A&E cell line.
    - Rendering `pdf.rams` with `$data` missing `site_emergency_resolved` entirely (no key at all, not even `null`) and a blank `site_emergency` produces a Nearest A&E Hospital cell containing the HOLD_POINT text, not an empty string.
    - Rendering `pdf.rams` with `$data` missing `site_emergency_resolved` and a `site_emergency` with both `nearest_hospital` and `hospital_address` populated produces a cell containing the resolved verified text.
    - `grep -n "site_emergency\['nearest_hospital'\] =" resources/views/pdf/rams.blade.php` returns 0 matches (confirms no write-back was introduced).
    - `php artisan test tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php` — all existing tests plus the new ones pass.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com" && php artisan test tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php</automated>
  </verify>
  <done>rams.blade.php's Section 7.0 A&E cell degrades to the resolver's hold-point or verified text when site_emergency_resolved is absent, never blank; the fallback is read-only; all existing render-site regression tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `RamsRegenerateSnapshotsCommand` / any future caller that skips `upgrade()` → `pdf.rams` render | A render path that bypasses the primary upgrade pipeline must still produce safe output, not blank |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-09-01 | Denial-of-Service (safety-content) / Repudiation | `rams.blade.php` Section 7.0 A&E cell | mitigate | This plan's fix — inline resolver fallback so a missing upstream key degrades to the safe hold-point line, never a blank safety-critical field on a printed document |
| T-29-09-02 | Tampering | Fallback call site | accept | Fallback is read-only (reads `$siteEmerg`, writes nothing) — no persistence path exists for it to corrupt |
</threat_model>

<verification>
`php artisan test tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php` — all pass,
including two new no-`site_emergency_resolved`-key cases. Full RAMS suite
(`php artisan test tests/Unit/Support/Rams tests/Feature/Rams`) should also be run once across
all three gap-closure plans before declaring the phase gap-free (single pattern only, no `|`
alternation in `--filter`, per binding constraints).
</verification>

<success_criteria>
`rams.blade.php` never renders a blank A&E cell regardless of which upstream caller invokes it;
the fallback is read-only and never writes into `nearest_hospital`; existing behavior on the
primary (upgrade()-first) download path is unchanged.
</success_criteria>

<output>
Create `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-09-SUMMARY.md` when done
</output>
