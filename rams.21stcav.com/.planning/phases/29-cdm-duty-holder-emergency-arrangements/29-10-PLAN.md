---
phase: 29-cdm-duty-holder-emergency-arrangements
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Rams/RamsComplianceUpgradeService.php
  - database/migrations/2026_09_12_120000_backfill_cdm_contractor_note.php
  - tests/Feature/Rams/BackfillCdmContractorNoteMigrationTest.php
autonomous: true
requirements: [RULE-07]
gap_closure: true

must_haves:
  truths:
    - "RamsComplianceUpgradeService exposes a single named constant carrying the RULE-07 verbatim anticipated-sole-contractor sentence, so every render site and the backfill migration read one literal instead of duplicating it"
    - "A new idempotent migration exists that adds the contractor_note key to generated_data['cdm_duty_holders'] for any already-persisted row that has cdm_duty_holders but no contractor_note key — without touching a row that already has one (whatever its value) and without running against production"
  artifacts:
    - path: "app/Services/Rams/RamsComplianceUpgradeService.php"
      provides: "DEFAULT_CONTRACTOR_NOTE public const, referenced by addCdmDutyHolders() and (in later plans) by the render-site fallbacks"
    - path: "database/migrations/2026_09_12_120000_backfill_cdm_contractor_note.php"
      provides: "Idempotent backfill closing the 46-already-patched-rows gap for the RULE-07 sentence, mirroring 2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php's shape"
  key_links:
    - from: "app/Services/Rams/RamsComplianceUpgradeService.php::addCdmDutyHolders()"
      to: "DEFAULT_CONTRACTOR_NOTE"
      via: "self::DEFAULT_CONTRACTOR_NOTE reference replacing the inline string literal"
      pattern: "DEFAULT_CONTRACTOR_NOTE"
---

<objective>
Close the data half of 29-UAT.md Gap 3 (RULE-07's prescribed sentence absent from output). The
sentence itself is already correct in code — `addCdmDutyHolders()` at
`app/Services/Rams/RamsComplianceUpgradeService.php:1167` already sets
`cdm_duty_holders['contractor_note']` to the verbatim RULE-07 sentence — but (a) it is an inline
string literal instead of a named constant the render sites (Plan 29-11/29-12) can reference, and
(b) it was never rendered anywhere (that is fixed by Plan 29-11/29-12), and (c) the 46 production
rows the Plan 29-05 backfill already patched (2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php)
were patched BEFORE `contractor_note` existed as a key at all, so those rows' persisted
`generated_data['cdm_duty_holders']` array has no `contractor_note` key — a completed document that
never regenerates again would never pick up the sentence even after Plan 29-11/29-12 ship the render
fix, mirroring exactly the reasoning that made the original CDM backfill (D-02) necessary.

Purpose: give the render-fix plans (29-11, 29-12) one named constant to reference, and land an
idempotent (not yet run) migration so the "46 rows" backfill question is a closed, code-complete
decision rather than a silent gap.

Output: `RamsComplianceUpgradeService::DEFAULT_CONTRACTOR_NOTE` constant; a new backfill migration;
a test proving its idempotency and its guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-UAT.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-CONTEXT.md
@.planning/reference/21cav-rams-skill/references/standards-and-legislation.md
@database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php
@database/migrations/2026_09_05_180000_backfill_ppe_ffp2_and_electrical_exclusion_bullet.php
@tests/Feature/Rams/BackfillCdmDutyHolderMigrationTest.php
@tests/Unit/Services/Rams/CdmDutyHolderWordingTest.php

<interfaces>
Existing shape of `addCdmDutyHolders()` (app/Services/Rams/RamsComplianceUpgradeService.php:1151-1177),
verbatim as currently shipped:

  private static function addCdmDutyHolders(array $data): array
  {
      $project = (array) ($data['project'] ?? []);
      $data['cdm_duty_holders'] = [
          'client'               => trim((string) ($project['client'] ?? '')) ?: '[Client Name]',
          'principal_designer'   => self::DEFAULT_PRINCIPAL_DESIGNER_NOTE,
          'principal_contractor' => self::DEFAULT_PRINCIPAL_CONTRACTOR_NOTE,
          'contractor'           => '21st Century AV Ltd',
          'subcontractor'        => '21st Century AV Ltd',
          'project_manager'      => trim((string) ($project['project_manager'] ?? '')) ?: '[To be confirmed]',
          'site_supervisor'      => trim((string) ($project['lead_engineer'] ?? '')) ?: '[To be confirmed]',
          'cdm_regulation'       => 'Construction (Design and Management) Regulations 2015',
          'contractor_note'      => '21CAV is currently anticipated to be the sole contractor for the AV '
              . 'installation scope. The client shall confirm whether the overall project involves, or is '
              . 'likely to involve, more than one contractor before works commence.',
          'notification'         => 'Most single-visit AV installation works fall below the CDM 2015 '
              . 'notifiable-project threshold. Where the wider project is notifiable, F10 notification is the '
              . "Client's duty (it may be submitted on the Client's behalf) — notifiability is judged on the "
              . "whole project, not 21CAV's scope alone.",
      ];
      return $data;
  }

Existing `DEFAULT_PRINCIPAL_DESIGNER_NOTE` / `DEFAULT_PRINCIPAL_CONTRACTOR_NOTE` public consts sit at
`:1121` and `:1131` — `DEFAULT_CONTRACTOR_NOTE` must be declared in the same block, same visibility
(`public const`), same docblock style.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract DEFAULT_CONTRACTOR_NOTE constant</name>
  <files>app/Services/Rams/RamsComplianceUpgradeService.php</files>
  <read_first>
    app/Services/Rams/RamsComplianceUpgradeService.php:1108-1177 (DEFAULT_PRINCIPAL_DESIGNER_NOTE,
    DEFAULT_PRINCIPAL_CONTRACTOR_NOTE, addCdmDutyHolders() body)
    .planning/reference/21cav-rams-skill/references/standards-and-legislation.md:23-28 (source of the
    verbatim sentence)
  </read_first>
  <action>
    Add a new `public const DEFAULT_CONTRACTOR_NOTE` to `RamsComplianceUpgradeService`, declared
    immediately after `DEFAULT_PRINCIPAL_CONTRACTOR_NOTE` (currently ending at :1132), with the exact
    same string value currently inline at `addCdmDutyHolders()`'s `contractor_note` key: "21CAV is
    currently anticipated to be the sole contractor for the AV installation scope. The client shall
    confirm whether the overall project involves, or is likely to involve, more than one contractor
    before works commence." Give it a docblock in the same style as the two existing constants,
    stating it is verbatim from `standards-and-legislation.md:23-28`, shared between
    `addCdmDutyHolders()` and the render-site fallbacks added in Plan 29-11/29-12. Then change
    `addCdmDutyHolders()`'s `contractor_note` key to read `self::DEFAULT_CONTRACTOR_NOTE` instead of
    the inline string literal — remove the now-redundant inline string entirely so there is exactly
    one literal in the codebase. Do not change the constant's string content — it must byte-match
    what `CdmDutyHolderWordingTest::test_contractor_note_carries_the_verbatim_anticipated_sole_contractor_sentence()`
    already asserts, or that test will fail.
  </action>
  <acceptance_criteria>
    `grep -n "public const DEFAULT_CONTRACTOR_NOTE" app/Services/Rams/RamsComplianceUpgradeService.php`
    returns exactly one match. `grep -c "'21CAV is currently anticipated to be the sole contractor"
    app/Services/Rams/RamsComplianceUpgradeService.php` returns exactly 1 (the constant declaration
    only — no second inline copy). `php artisan test --filter
    test_contractor_note_carries_the_verbatim_anticipated_sole_contractor_sentence` passes unchanged.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Unit/Services/Rams/CdmDutyHolderWordingTest.php</automated>
  </verify>
  <done>DEFAULT_CONTRACTOR_NOTE constant exists, addCdmDutyHolders() references it, no duplicate string literal remains, existing wording test suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Idempotent backfill migration for contractor_note (not run)</name>
  <files>database/migrations/2026_09_12_120000_backfill_cdm_contractor_note.php, tests/Feature/Rams/BackfillCdmContractorNoteMigrationTest.php</files>
  <read_first>
    database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php (full file — shape,
    docblock discipline, chunking, guard style, no-op down())
    tests/Feature/Rams/BackfillCdmDutyHolderMigrationTest.php (test shape to mirror)
    .planning/phases/29-cdm-duty-holder-emergency-arrangements/29-UAT.md:56-68 (gap 3's exact framing)
  </read_first>
  <action>
    Create `database/migrations/2026_09_12_120000_backfill_cdm_contractor_note.php`, structurally
    mirroring `2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php`: a chunked
    `DB::table('rams_documents')->chunkById(100, ...)` scan over `id`, `generated_data`. The
    class docblock must state explicitly, in prose, the decision this plan is required to make per
    29-UAT.md: the 46 production rows the 2026-09-11 backfill already patched were patched BEFORE
    `contractor_note` existed as a key in `addCdmDutyHolders()`'s output shape, so their persisted
    `generated_data['cdm_duty_holders']` array has no `contractor_note` key at all — a call
    (`RamsComplianceUpgradeService::upgrade()` running fresh on any real regeneration always rebuilds
    `cdm_duty_holders` from scratch and so naturally reaches every document on its NEXT regeneration,
    but a COMPLETED document that never regenerates again would never pick up the sentence without
    this migration, mirroring the exact reasoning D-02 gave for the original CDM backfill) — therefore
    this backfill IS warranted, not optional, and is written now so it is one `.env`-adjacent step
    away from running, per the phase's own "make the call explicit" instruction. State plainly that
    this migration is NOT run against production by this plan (out of scope) — Plan 29-14 or a human
    owns running it after the render fix (Plan 29-11/29-12) ships.

    `up()` logic: for each row whose `generated_data['cdm_duty_holders']` key exists AND is a
    non-empty array AND does NOT already have a `contractor_note` key (use
    `array_key_exists('contractor_note', $cdm)`, not `empty()` — an engineer could plausibly have
    intentionally cleared it to an empty string, which must be left alone, matching the exact-guard
    discipline of the analog migration), set
    `$data['cdm_duty_holders']['contractor_note'] = \App\Services\Rams\RamsComplianceUpgradeService::DEFAULT_CONTRACTOR_NOTE;`
    and mark the row touched. Do not touch `reviewed_data['cdm']` — that shape is a list of
    `{role, name}` rows with no room for a contractor-wide note; `contractor_note` only exists on the
    `generated_data['cdm_duty_holders']` keyed shape. Auditable `echo` summary of documents touched,
    matching the analog's format. `down()` is a deliberate no-op with the same "cannot distinguish
    migration-touched from a legitimately-cleared value" justification as the analog, adapted to this
    field.

    Create `tests/Feature/Rams/BackfillCdmContractorNoteMigrationTest.php` mirroring
    `BackfillCdmDutyHolderMigrationTest.php`'s shape: (1) a row with `cdm_duty_holders` present but no
    `contractor_note` key gets the constant's value added; (2) a row that already has
    `contractor_note` set to some other string (simulating a hand-edited value) is left untouched byte
    for byte; (3) a row with `contractor_note` already set to `''` (empty string, simulating a
    deliberate clear) is left untouched, not treated as missing; (4) running `up()` twice produces
    zero additional changes on the second run (idempotency); (5) a row with no `cdm_duty_holders` key
    at all is skipped without error.
  </action>
  <acceptance_criteria>
    Migration file follows the `YYYY_MM_DD_HHMMSS_description.php` naming convention and is NOT
    registered as run in this plan (no `php artisan migrate` invocation in this task). Test file has
    5 test methods matching the 5 cases above, all passing. `grep -n "array_key_exists('contractor_note'"
    database/migrations/2026_09_12_120000_backfill_cdm_contractor_note.php` returns at least 1 match
    (confirms the exact-key-presence guard, not an `empty()`-based guard that would wrongly treat a
    deliberately-cleared empty string as missing).
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Rams/BackfillCdmContractorNoteMigrationTest.php</automated>
  </verify>
  <done>New idempotent migration exists, is guard-correct (array_key_exists, not empty()), is not run against any real database by this task, and its 5-case test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persisted `rams_documents.generated_data` → migration `up()` | A JSON blob previously written by application code or (rarely) hand-edited via the review form; the migration reads and conditionally rewrites it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-10-01 | Tampering | `backfill_cdm_contractor_note` migration | mitigate | `array_key_exists()` exact-key guard (not substring/empty-based) prevents overwriting an engineer-cleared or hand-edited `contractor_note` value; mirrors the D-02 exact-literal discipline already proven correct in the 2026-09-11 migration |
| T-29-10-02 | Repudiation | migration audit trail | mitigate | `echo` summary of documents touched, matching the established analog pattern, so a future production run is auditable |
| T-29-10-SC | Tampering | composer/npm/pip installs | accept | No new package dependency introduced by this plan |
</threat_model>

<verification>
`php artisan test tests/Unit/Services/Rams/CdmDutyHolderWordingTest.php tests/Feature/Rams/BackfillCdmContractorNoteMigrationTest.php tests/Feature/Rams/BackfillCdmDutyHolderMigrationTest.php` — all green, no regressions to the existing backfill test.
</verification>

<success_criteria>
`RamsComplianceUpgradeService::DEFAULT_CONTRACTOR_NOTE` exists and is the single source for the
RULE-07 sentence; the new backfill migration is idempotent, guard-correct, code-complete, and
explicitly NOT run against any database by this plan.
</success_criteria>

<output>
Create `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-10-SUMMARY.md` when done
</output>
