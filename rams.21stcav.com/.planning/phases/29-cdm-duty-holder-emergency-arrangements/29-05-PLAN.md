---
phase: 29-cdm-duty-holder-emergency-arrangements
plan: 05
type: execute
wave: 3
depends_on: ["29-01", "29-03"]
files_modified:
  - database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php
  - tests/Feature/Rams/BackfillCdmDutyHolderMigrationTest.php
  - app/Services/Rams/RamsDisplayPatchService.php
autonomous: true
requirements: [RULE-07]
must_haves:
  truths:
    - "Every already-persisted production row still carrying the CDM '[To be confirmed]' placeholder gets patched to the restated RULE-07 wording, on both reviewed_data['cdm'] and generated_data['cdm_duty_holders']"
    - "The backfill never overwrites a row where an engineer already typed a real duty-holder name (D-02: placeholder-equality guard, not a blanket overwrite)"
    - "Running the migration twice produces zero additional changes on the second run (idempotent)"
    - "The per-project carry-forward never re-propagates a placeholder CDM or A&E value onto a new document (D-04)"
    - "The migration ran only after Plan 29-01's measurement checkpoint recorded a real production count (deploy-order discipline, not a blocking code dependency — the migration itself is safe to write regardless of the count)"
  artifacts:
    - path: "database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php"
      provides: "Idempotent, chunked, dual-column backfill of the CDM placeholder — mirrors the 2026_09_05_180000 precedent"
    - path: "app/Services/Rams/RamsDisplayPatchService.php"
      provides: "Carry-forward guard skipping placeholder cdm/site_emergency values when seeding a new document from a prior completed RAMS"
  key_links:
    - from: "database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php"
      to: "app/Services/Rams/RamsComplianceUpgradeService.php"
      via: "reuses DEFAULT_PRINCIPAL_DESIGNER_NOTE / DEFAULT_PRINCIPAL_CONTRACTOR_NOTE constants (Plan 29-03) rather than re-typing the restated wording"
      pattern: "RamsComplianceUpgradeService::DEFAULT_PRINCIPAL"
---

<objective>
Reach the CDM documents Plan 29-03's code fix cannot touch — already-persisted
`rams_documents` rows whose `reviewed_data['cdm']` or `generated_data['cdm_duty_holders']` still
carry the literal `'[To be confirmed]'` string (D-02, the Phase 28-07 backfill shape). Close the
placeholder-propagation vector in `RamsDisplayPatchService`'s per-project carry-forward (D-04) so
a fixed default cannot be re-poisoned by an older document's placeholder on the very next RAMS
created for the same project.

Purpose: RULE-07's code fix (Plan 29-03) only reaches documents that get a fresh `upgrade()`
pass. A "completed" document may never regenerate again; only a backfill reaches it. A&E needs
no equivalent backfill (D-02: it is template text, never persisted — Plan 29-04's code fix
reaches every existing document on its next render).

Output: a new idempotent migration, its test, and the carry-forward guard fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-CONTEXT.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-RESEARCH.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-PATTERNS.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-01-SUMMARY.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-03-SUMMARY.md
@database/migrations/2026_09_05_180000_backfill_ppe_ffp2_and_electrical_exclusion_bullet.php
</context>

<interfaces>
<!-- The two CDM shapes this migration and the carry-forward guard must both handle. -->

`generated_data['cdm_duty_holders']` — a KEYED array (from `addCdmDutyHolders()`, Plan 29-03):
`client`, `principal_designer`, `principal_contractor`, `contractor`, `contractor_note`,
`subcontractor`, `project_manager`, `site_supervisor`, `cdm_regulation`, `notification`. Only
`principal_designer`/`principal_contractor` are ever the literal `'[To be confirmed]'` string
under the restated RULE-07 (the other placeholder-shaped keys — `project_manager`/
`site_supervisor` — reflect genuinely missing project data and are NOT patched by this
migration, matching GATE-11's own scope from Plan 29-03).

`reviewed_data['cdm']` — a LIST of `{role, value}` / `{role, name}` rows, written by
`RamsController::updateAndDownload()` (`:504-509`): `$reviewedData['cdm'] =
array_values(array_filter($cdmInput, fn ($row) => ! empty($row['role'])));`. A row's free-text
value field may contain `'To be confirmed'` as a substring if an engineer left the review-form
field at its seeded default.

Current `RamsDisplayPatchService::patch()` carry-forward block (`:407-430`) to modify:
```php
if ($rams->project_id && (empty($rd['site_emergency']) || empty($rd['cdm']))) {
    $prior = RamsDocument::query()
        ->where('project_id', $rams->project_id)
        ->where('id', '!=', $rams->id)
        ->where('status', RamsDocument::STATUS_COMPLETED)
        ->orderByDesc('updated_at')
        ->first();
    if ($prior) {
        $priorRd = $prior->reviewed_data ?? [];
        if (empty($rd['site_emergency']) && ! empty($priorRd['site_emergency'])) {
            $rd['site_emergency'] = $priorRd['site_emergency'];
        }
        if (empty($rd['cdm']) && ! empty($priorRd['cdm'])) {
            $rd['cdm'] = $priorRd['cdm'];
        }
    }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Idempotent backfill migration for the CDM placeholder</name>
  <files>database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php, tests/Feature/Rams/BackfillCdmDutyHolderMigrationTest.php</files>
  <read_first>
    database/migrations/2026_09_05_180000_backfill_ppe_ffp2_and_electrical_exclusion_bullet.php
    (full file — copy its chunked, dual-column, value-equality-guard, auditable-count,
    no-op-`down()` shape verbatim), app/Services/Rams/RamsComplianceUpgradeService.php (the
    `DEFAULT_PRINCIPAL_DESIGNER_NOTE`/`DEFAULT_PRINCIPAL_CONTRACTOR_NOTE` constants added in
    Plan 29-03 — reuse these, do not re-type the wording), tests/Feature/Rams/BackfillPpeFfp2AndElectricalExclusionBulletMigrationTest.php
    (the sibling test file's shape to mirror — locate it under `tests/Feature/Rams/`)
  </read_first>
  <behavior>
    - A row whose `generated_data['cdm_duty_holders']['principal_designer']` is literally
      `'[To be confirmed]'` gets it replaced with
      `RamsComplianceUpgradeService::DEFAULT_PRINCIPAL_DESIGNER_NOTE`; same for
      `principal_contractor` with `DEFAULT_PRINCIPAL_CONTRACTOR_NOTE`. Neither is touched when
      the current value is anything else (including an engineer-typed real name).
    - A row whose `reviewed_data['cdm']` list contains an entry whose `value`/`name` field
      contains the substring `'To be confirmed'` gets that field's value replaced with the same
      restated default for whichever role the row represents (match on the row's `role` field
      case-insensitively against `'Principal Designer'`/`'Principal Contractor'`; rows for any
      other role — Client, Contractor, etc — are left untouched even if they happen to contain
      the substring, since only PD/PC are RULE-07's settled-position fields).
    - Both `reviewed_data` and `generated_data` columns are checked and patched independently
      per row, mirroring the 28-07 precedent's per-column independence.
    - Running `up()` twice produces zero additional row updates on the second run (the
      value-equality/placeholder-absence guard makes this naturally idempotent — no separate
      "already migrated" marker needed).
    - `down()` is a documented deliberate no-op (same non-reversibility justification as the
      28-07 precedent: cannot distinguish a migration-touched row from one an engineer
      independently corrected by hand afterward).
  </behavior>
  <action>
    Create `database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php`
    following the 28-07 migration's exact structural shape: `DB::table('rams_documents')
    ->select('id', 'reviewed_data', 'generated_data')->orderBy('id')->chunkById(100, ...)`, a
    private static `fixCdmPlaceholder(array $data): array` returning `[$data, $changed]` per
    `<behavior>`, per-surface changed-counters (`generatedDataDocsTouched`,
    `reviewedDataDocsTouched`), an auditable `echo` summary at the end of `up()`, and a
    documented no-op `down()`. Write `BackfillCdmDutyHolderMigrationTest.php` mirroring
    `BackfillPpeFfp2AndElectricalExclusionBulletMigrationTest.php`'s shape: seed a
    `RamsDocument` with the placeholder in both columns, run the migration, assert both are
    patched to the exact constant values; seed a second `RamsDocument` with an engineer-typed
    real name (e.g. `'Jane Smith, ABC Construction Ltd'`) in `principal_contractor` and assert it
    is UNCHANGED after the migration runs; run the migration a second time (or re-invoke its
    `up()` logic directly) and assert zero additional rows are updated.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=BackfillCdmDutyHolderMigrationTest` passes
    - A fixture row with `principal_contractor` already set to a real name is byte-identical
      before and after the migration runs
    - Running the migration's `up()` logic twice against the same seeded data produces the same
      `documentsTouched` count only on the first invocation, zero on the second
    - `down()` contains no `DB::table(...)->update(...)` call (grep confirms it is a documented
      no-op)
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=BackfillCdmDutyHolderMigrationTest</automated>
  </verify>
  <done>The migration exists, is idempotent, never overwrites an engineer-typed real name, and is proven by a passing test mirroring the 28-07 precedent's shape.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry-forward guard — skip placeholder cdm/site_emergency values (D-04)</name>
  <files>app/Services/Rams/RamsDisplayPatchService.php</files>
  <read_first>
    app/Services/Rams/RamsDisplayPatchService.php:395-435 (the carry-forward block to modify —
    read the surrounding `patchRamsForDisplay`/similar method for context on where `$rd` comes
    from and how it's persisted afterward), the `fixCdmPlaceholder`-style placeholder-detection
    logic written in Task 1 (reuse the same detection rule — a role/value pair counts as a
    placeholder if its role is Principal Designer/Principal Contractor AND its value contains
    `'To be confirmed'`; a `site_emergency.nearest_hospital` counts as a placeholder if it is
    blank OR equals the `SiteEmergencyResolver::HOLD_POINT` hold-point line)
  </read_first>
  <behavior>
    - Carrying forward `cdm`: for each row in the prior document's `reviewed_data['cdm']`, skip
      (do not copy) any row whose role is Principal Designer/Principal Contractor and whose
      value still contains `'To be confirmed'`. Rows for other roles, or PD/PC rows with a real
      typed value, still carry forward as before.
    - Carrying forward `site_emergency`: skip the ENTIRE `site_emergency` carry-forward (not
      just `nearest_hospital` alone — the existing code copies the whole associative array as
      one unit) when the prior document's `nearest_hospital` is blank or equals the D-05
      hold-point line. When it carries forward, the full associative array (fire wardens,
      defibrillator, etc.) copies exactly as today — those sub-fields carry their own
      genuinely-useful values regardless of the A&E field's state.
    - A prior document whose CDM/A&E values are all genuinely useful (non-placeholder) carries
      forward exactly as it does today — zero behaviour change for the common case.
  </behavior>
  <action>
    Modify the carry-forward block (`:417-430`) so the `cdm` branch filters
    `$priorRd['cdm']` through a new private helper (e.g. `carryForwardEligibleCdmRows(array
    $rows): array`) before assignment, dropping any PD/PC row whose value still contains `'To be
    confirmed'` (case-sensitive substring match, matching the exact string
    `addCdmDutyHolders()`/the migration both use). Modify the `site_emergency` branch to check
    `trim((string) ($priorRd['site_emergency']['nearest_hospital'] ?? '' )) === ''` OR that value
    equals the exact hold-point string before copying — if either is true, do not copy
    `site_emergency` at all (falls through to the fixed default the render pipeline now
    produces via `resolveSiteEmergency()`). Add a short PHPUnit test file or extend an existing
    `RamsDisplayPatchService` test (locate via `tests/Unit/Services/Rams/` or
    `tests/Feature/Rams/` — search for an existing test class covering `patch()`'s carry-forward
    behaviour and add cases to it rather than creating a new file, per this project's convention
    of extending an existing test class when one already covers the method under test) proving:
    a prior document with a placeholder PD/PC row does NOT propagate that row; a prior document
    with a real PD/PC name DOES propagate it; a prior document with a blank/hold-point
    `nearest_hospital` does NOT propagate `site_emergency` at all; a prior document with a real
    verified A&E DOES propagate the full `site_emergency` array.
  </action>
  <acceptance_criteria>
    - The relevant test(s) pass via `php artisan test --filter=RamsDisplayPatchService` (or the
      specific test class name found/created)
    - A carry-forward source document whose `cdm` list has a placeholder-valued Principal
      Contractor row results in that row being ABSENT from the new document's `reviewed_data['cdm']`
      after `patch()` runs
    - A carry-forward source document whose `site_emergency.nearest_hospital` is blank results in
      the new document's `reviewed_data['site_emergency']` remaining empty after `patch()` runs
      (letting the render pipeline's fixed default fill in instead)
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=RamsDisplayPatchService</automated>
  </verify>
  <done>The per-project carry-forward can no longer re-propagate a placeholder CDM row or a blank/hold-point A&E onto a new document; genuinely useful prior values still carry forward exactly as before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Migration → production `rams_documents` JSON columns | A one-shot batch write against every row in the table; the only mutation this phase performs against already-shipped safety documents |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-29-05-01 | Tampering | Migration overwrites an engineer's genuine duty-holder entry | mitigate | Value-equality guard: only ever overwrites a field whose CURRENT value is literally the old placeholder string — proven by an explicit non-vacuity test in Task 1 |
| T-29-05-02 | Repudiation | No audit trail of which rows the migration touched | accept | Matches the 28-07 precedent exactly — an auditable `echo` count at migration end is the established, accepted level of audit for this class of one-shot backfill in this codebase |
| T-29-05-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — this plan introduces no package installs |
</threat_model>

<verification>
`php artisan test --filter=BackfillCdmDutyHolderMigrationTest` and the
`RamsDisplayPatchService` carry-forward tests both green. Migration is idempotent (proven by a
double-run test) and never touches a non-placeholder value.
</verification>

<success_criteria>
Every already-persisted document with the CDM placeholder gets the restated wording without
risking a single engineer-typed real name; the carry-forward mechanism can no longer reseed a
placeholder onto a brand-new document.
</success_criteria>

<output>
Create `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-05-SUMMARY.md` when done
</output>
