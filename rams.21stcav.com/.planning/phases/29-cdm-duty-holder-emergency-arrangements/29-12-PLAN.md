---
phase: 29-cdm-duty-holder-emergency-arrangements
plan: 12
type: execute
wave: 2
depends_on: ["29-10"]
files_modified:
  - app/Services/DocxBuilderService.php
  - tests/Feature/Rams/DocxEmergencySectionRegressionTest.php
autonomous: true
requirements: [RULE-07, RULE-08]
gap_closure: true

must_haves:
  truths:
    - "The DOCX renders a Section 7.0 Site-Specific Emergency Details block carrying nearest A&E + address, fire assembly point, fire warden, first aider, and defibrillator location, reading the same SiteEmergencyResolver-resolved value the PDF uses"
    - "The DOCX Welfare Arrangements First Aid bullet points at the real Section 7.0 block, not at the CDM 2015 Duty Holders section (which carries no A&E data)"
    - "The DOCX CDM 2015 Duty Holders section carries the RULE-07 verbatim anticipated-sole-contractor sentence"
  artifacts:
    - path: "app/Services/DocxBuilderService.php"
      provides: "buildEmergencyProcedures() renders a 7.0 A&E sub-section before 7.1; buildWelfareArrangements() points at it; buildCdmSection() renders contractor_note"
  key_links:
    - from: "app/Services/DocxBuilderService.php::buildEmergencyProcedures()"
      to: "App\\Services\\Rams\\SiteEmergencyResolver::resolve()"
      via: "same resolved-value read pattern as rams.blade.php:1983, never re-derived"
      pattern: "SiteEmergencyResolver::resolve"
    - from: "app/Services/DocxBuilderService.php::buildCdmSection()"
      to: "cdm_duty_holders['contractor_note']"
      via: "new paragraph after the CDM table, mirroring the existing notification paragraph pattern"
      pattern: "contractor_note"
---

<objective>
Close 29-UAT.md Gap 1 (the DOCX carries NO A&E information at all — a regression worse than
pre-Phase-29, since the Welfare bullet cross-references a CDM section that has no A&E data) and the
DOCX half of Gap 3 (RULE-07's sentence never renders in the DOCX CDM table). Both defects live in
`app/Services/DocxBuilderService.php`, the sole DOCX renderer for these two sections — `DocxBuilderServiceV2`
already delegates Emergency Procedures, Welfare Arrangements, and the CDM table to this same file's
methods (confirmed: `DocxBuilderServiceV2.php:54` docblock lists all three as "still delegates to the
legacy seam"), so this plan's fix reaches both the V1 and V2 DOCX build paths with one set of edits.

Purpose: give the DOCX a real Section 7.0 (mirroring the PDF's), point the Welfare bullet at it
instead of an empty CDM section, and render the RULE-07 sentence the CDM table has been silently
computing but never displaying.

Output: `buildEmergencyProcedures()` gains a 7.0 sub-section; `buildWelfareArrangements()`'s pointer
is corrected; `buildCdmSection()` gains a contractor_note paragraph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-UAT.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-CONTEXT.md
@app/Services/Rams/SiteEmergencyResolver.php
@tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php

<interfaces>
`SiteEmergencyResolver::resolve(array $siteEmergency): array{verified: bool, text: string}` — call
this, never re-derive the verified/hold-point branch logic.

`RamsComplianceUpgradeService::DEFAULT_CONTRACTOR_NOTE` (Plan 29-10) — RULE-07 verbatim sentence,
render-side fallback constant.

Current `buildRestOfDocument()` call chain (app/Services/DocxBuilderService.php:352-357):

  $this->buildCdmSection($phpWord, $data);
  ...
  $this->buildWelfareArrangements($phpWord, $data);
  $this->buildEmergencyProcedures($phpWord, $data, $formData);

`$formData = $record->form_data ?? [];` is derived at `:325` — `buildEmergencyProcedures()` does NOT
currently receive `$record`, only `$formData`. `site_emergency` lives on
`$record->reviewed_data['site_emergency']` and is mirrored onto `$data['site_emergency']` at RAMS
save time (same shape `rams.blade.php:1976` reads:
`(array) ($data['site_emergency'] ?? ($rams->reviewed_data['site_emergency'] ?? []))`), and the
resolved value (when `upgrade()` has run) lives at `$data['site_emergency_resolved']['text']`.

`buildEmergencyProcedures()` signature today (`:1545`):
`private function buildEmergencyProcedures(PhpWord $phpWord, array $data, array $formData): void`
— building the 7.0 block needs `$record` too (for the `reviewed_data` fallback), so the signature and
its one call site both need updating.

`buildCdmSection()` (`:1678-1730`) already reads `$data['cdm_duty_holders']`, builds a 7-row table
(Client/PD/PC/Contractor/Subcontractor/PM/Site Supervisor — one row PER key, unlike the PDF's 4-row
table), then renders a `$notification` paragraph in italic `MID_GREY` after the table if non-empty
(`:1721-1729`) — this is the exact pattern to copy for `contractor_note`.

Existing DOCX table-building helpers used throughout the file: `$section->addTable($this->tableStyle())`,
`$table->addRow($height)`, `$row->addCell($width, $bgArray)->addText($text, $font)`. Two-column label/value
tables use `$colW = (int)(self::W_PORT / 2)` (e.g. `buildEmergencyProcedures()`'s existing 7.1 contact
table, `buildCdmSection()`'s CDM table uses a 3400/6466 twip split instead — either split is
acceptable for the new 7.0 table; match whichever is visually closer to a label/value pair, i.e. the
3400/6466 split `buildCdmSection()` already uses).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Section 7.0 A&E block to the DOCX; fix the Welfare bullet pointer</name>
  <files>app/Services/DocxBuilderService.php</files>
  <read_first>
    app/Services/DocxBuilderService.php:1540-1632 (buildEmergencyProcedures(), full method, including
    the 7.1/7.2/7.3 sub-sections this task inserts a new 7.0 before)
    app/Services/DocxBuilderService.php:2134-2163 (buildWelfareArrangements(), the First Aid bullet at
    :2155 this task rewords)
    app/Services/DocxBuilderService.php:321-361 (buildRestOfDocument(), the call site both changed
    methods are invoked from — buildEmergencyProcedures() needs $record added to its call at :357)
    app/Services/Rams/SiteEmergencyResolver.php (resolve() — the only source of the A&E text)
    resources/views/pdf/rams.blade.php:1975-2021 (the PDF's Section 7.0 field list and TBC fallbacks
    to mirror, adapted to the file's own 2-column table convention — see interfaces block)
  </read_first>
  <action>
    Change `buildEmergencyProcedures()`'s signature from
    `private function buildEmergencyProcedures(PhpWord $phpWord, array $data, array $formData): void`
    to add a fourth parameter `RamsDocument $record`, and update its one call site in
    `buildRestOfDocument()` (`:357`) to `$this->buildEmergencyProcedures($phpWord, $data, $formData, $record);`
    — `$record` is already in scope at that call site as `buildRestOfDocument()`'s own parameter.
    Inside `buildEmergencyProcedures()`, immediately before the existing "── 7.1 Emergency Contact
    Numbers ──" block (`:1564`), insert a new "7.0 Site-Specific Emergency Details" sub-section
    heading (same `$this->font(10, bold: true, colour: self::TEAL)` style as the 7.1/7.2/7.3
    headings) followed by a 2-column table (label/value, 3400/6466 twip split matching
    `buildCdmSection()`'s convention) with five rows: "Nearest A&E Hospital" (value = the resolved
    text, read as `$data['site_emergency_resolved']['text'] ?? \App\Services\Rams\SiteEmergencyResolver::resolve($siteEmerg)['text']`
    where `$siteEmerg = (array) ($data['site_emergency'] ?? ($record->reviewed_data['site_emergency'] ?? []))`,
    with the hospital address appended on a following line inside the same cell when
    `$siteEmerg['hospital_address']` is non-empty — mirror the PDF's address-as-sub-line treatment,
    e.g. via `$cell->addText()` twice or a line break within the value); "Fire Assembly Point" (value
    = `$siteEmerg['fire_assembly_point'] ?? '' ?: 'TBC'`); "Fire Warden" (value = the name and, if
    present, contact combined into one cell, e.g. `"{$name} ({$contact})"` when both are non-empty,
    else just the name, else `'TBC'` — this file's 2-column table shape has no room for a separate
    Contact column the way the PDF's 4-column table does, so combine); "First Aider" (same
    name+contact-combined treatment); "Nearest Defibrillator" (value =
    `$siteEmerg['defibrillator_location'] ?? '' ?: 'TBC — confirm at site induction'`). Do not add
    Electrical Isolation Switch or Fire Extinguisher Class rows — out of this task's scope (the phase
    scope enumerates exactly these five fields). Never write back into `$siteEmerg`, `$data`, or
    `$record` — read-only, matching the WR-01 discipline already established for the PDF blade fix.

    In `buildWelfareArrangements()`, change the First Aid bullet's text (`:2155`) from "...Nearest A&E
    — see CDM 2015 — Duty Holders section." to "...Nearest A&E — see Section 7.0." — matching the
    PDF's exact wording (`rams.blade.php:1960`: "Nearest A&E — see Section 7.0."). Update or remove
    the surrounding code comment block (`:2145-2154`) that currently explains why the DOCX points at
    CDM instead of a real Section 7.0 — that reasoning is now obsolete since Section 7.0 exists.
  </action>
  <acceptance_criteria>
    A DOCX built from a RamsDocument with a wholly empty `site_emergency` contains, in
    `word/document.xml`, the text "to be confirmed at induction (must be a 24/7 Emergency Department)"
    (the D-05 hold-point line) and does NOT contain the banned string "to be identified at site
    induction" anywhere. A DOCX built with a verified `nearest_hospital`/`hospital_address` contains
    that hospital's name and address text. The Welfare bullet's rendered text reads "...Nearest A&E —
    see Section 7.0." and no longer references "CDM 2015 — Duty Holders section". Neither
    `DocxBuilderServiceV2::build()` (unified path) nor `DocxBuilderService::build()` (legacy path)
    throws — both delegate Emergency Procedures/Welfare through the same modified methods.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Rams/DocxEmergencySectionRegressionTest.php</automated>
  </verify>
  <done>DOCX Section 7.0 exists with the five specified fields, reads the shared resolver, never blank/never banned-string; Welfare bullet points at it.</done>
</task>

<task type="auto">
  <name>Task 2: CDM section renders contractor_note; DOCX presence regression test</name>
  <files>app/Services/DocxBuilderService.php, tests/Feature/Rams/DocxEmergencySectionRegressionTest.php</files>
  <read_first>
    app/Services/DocxBuilderService.php:1678-1730 (buildCdmSection(), full method, including the
    existing $notification italic-paragraph pattern at :1721-1729 to mirror)
    app/Services/Rams/RamsComplianceUpgradeService.php (DEFAULT_CONTRACTOR_NOTE const from Plan 29-10
    — read the actual name/value after 29-10 lands)
    tests/Unit/Services/Rams/CdmDutyHolderWordingTest.php:71-81 (verbatim string this render must
    match byte-for-byte)
    tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php:359-402
    (test_docx_welfare_bullet_never_contains_banned_string — the established pattern for building a
    real DocxBuilderService output and asserting against its extracted word/document.xml; reuse this
    shape, do not invent a new one)
  </read_first>
  <action>
    In `buildCdmSection()`, immediately after the existing `$notification` paragraph block
    (`:1721-1729`), add an equivalent paragraph for `contractor_note`: trim
    `$cdm['contractor_note'] ?? \App\Services\Rams\RamsComplianceUpgradeService::DEFAULT_CONTRACTOR_NOTE`
    and, if non-empty, render it via `$section->addText(...)` in the same italic `MID_GREY` 8pt style
    as `$notification`, with a `spaceBefore` matching the existing pattern (reuse `$this->font(8,
    italic: true, colour: self::MID_GREY)`). Place it either immediately before or immediately after
    the `$notification` paragraph (either order is acceptable — pick one and keep both present, do not
    replace `$notification`). Create `tests/Feature/Rams/DocxEmergencySectionRegressionTest.php`
    (referenced by Task 1's verify command — this task creates it) with test methods covering: (1)
    wholly-empty `site_emergency` → DOCX contains the hold-point text, not the banned string, not
    blank; (2) verified `site_emergency` → DOCX contains the hospital name and address; (3) Welfare
    bullet text reads the Section 7.0 pointer, not the CDM cross-reference; (4) DOCX built through the
    real `RamsComplianceUpgradeService::upgrade()` pipeline contains the exact verbatim RULE-07
    sentence in `word/document.xml`; (5) none of the three forbidden CDM statements
    (`standards-and-legislation.md:35-41`) appear in the built DOCX's `word/document.xml`. Build the
    real DOCX via `app(DocxBuilderService::class)->build($rams->generated_data ?? [], $rams->fresh())`
    against a real, persisted `RamsDocument` run through `RamsComplianceUpgradeService::upgrade()`
    first (mirroring `SiteEmergencyRenderSitesRegressionTest::renderBothBladesWith()`'s real-pipeline
    approach) — do not hand-build a `cdm_duty_holders`/`site_emergency` fixture that bypasses the real
    upgrade pipeline.
  </action>
  <acceptance_criteria>
    A DOCX built through the real `upgrade()` pipeline contains the exact byte-for-byte string "21CAV
    is currently anticipated to be the sole contractor for the AV installation scope. The client shall
    confirm whether the overall project involves, or is likely to involve, more than one contractor
    before works commence." in `word/document.xml`. None of the three forbidden statements appear.
    All 5 new test methods pass, plus the 2 test methods from Task 1's verify command.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Rams/DocxEmergencySectionRegressionTest.php</automated>
  </verify>
  <done>DOCX CDM section carries the verbatim RULE-07 sentence; 5-method regression test file exists and is green, covering both this task and Task 1's acceptance criteria.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engineer review-form input (`reviewed_data['site_emergency']`) → generated DOCX | Untrusted-by-default free text reaching a client-facing safety document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-12-01 | Information Disclosure | New Section 7.0 A&E block | accept | Same data the PDF already renders; adding it to the DOCX does not expose new information, it closes an information-loss regression |
| T-29-12-02 | Tampering | `buildEmergencyProcedures()` new read path | mitigate | Read-only — never writes back into `$siteEmerg`/`$data`/`$record`, matching the WR-01 discipline; verified by no assignment into those variables anywhere in the diff |
| T-29-12-03 | Tampering | `buildCdmSection()` contractor_note paragraph | mitigate | Server-computed constant/pipeline value only, never user-supplied free text at this call site |
</threat_model>

<verification>
`php artisan test tests/Feature/Rams/DocxEmergencySectionRegressionTest.php` — all 7 test methods
(2 from Task 1's scope, 5 from Task 2) green.
</verification>

<success_criteria>
DOCX Section 7.0 exists, mirrors the PDF's five A&E-adjacent fields, reads
`SiteEmergencyResolver::resolve()`, never blank/never "TBC"/never the banned string. Welfare bullet
points at it. CDM section carries the verbatim RULE-07 sentence.
</success_criteria>

<output>
Create `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-12-SUMMARY.md` when done
</output>
