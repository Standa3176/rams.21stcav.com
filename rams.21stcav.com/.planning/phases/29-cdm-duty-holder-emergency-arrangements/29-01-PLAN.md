---
phase: 29-cdm-duty-holder-emergency-arrangements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/29-cdm-duty-holder-emergency-arrangements/29-MEASUREMENT.md
  - .planning/phases/29-cdm-duty-holder-emergency-arrangements/29-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [RULE-08]
must_haves:
  truths:
    - "The number of production rows carrying the CDM '[To be confirmed]' placeholder is known before any backfill migration is written"
    - "The live RAMS_UNIFIED_COMPOSER setting is known before any render-site fix is written, sourced from config() not .env"
    - "RULE-08's restated two-branch wording is recorded in REQUIREMENTS.md with a RESTATED note, matching RULE-07's precedent style"
    - "ROADMAP.md Phase 29 criterion 2 no longer asserts a named A&E 'by default' — it states the D-05 verified-or-hold-point position"
  artifacts:
    - path: ".planning/phases/29-cdm-duty-holder-emergency-arrangements/29-MEASUREMENT.md"
      provides: "Production CDM-placeholder row count, RAMS_UNIFIED_COMPOSER state, full-suite runtime — read-only checkpoint per D-02"
    - path: ".planning/REQUIREMENTS.md"
      provides: "RULE-08 restated (D-06) with a RESTATED 2026-09-11 note"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 29 criterion 2 restated to match D-05/D-06"
  key_links:
    - from: ".planning/phases/29-cdm-duty-holder-emergency-arrangements/29-MEASUREMENT.md"
      to: "29-05-PLAN.md backfill migration"
      via: "measure-first checkpoint precedes the migration task per D-02/28-07 precedent"
      pattern: "backfill_cdm_duty_holder_placeholder"
---

<objective>
Establish the measure-first checkpoint this phase's backfill migration and render-site fixes
depend on, and restate RULE-08 + ROADMAP Phase 29 criterion 2 to match CONTEXT.md D-05/D-06
before any implementation plan is written against the (currently wrong) roadmap wording.

Purpose: D-02 requires counting affected production rows before the backfill migration is
written (mirrors the Phase 28-07 precedent that prevented an unmeasured, unverified migration
from shipping). D-01/Finding 4 requires knowing the live `RAMS_UNIFIED_COMPOSER` setting via
`config()` before deciding whether the legacy-blade fix, the composer fix, or both actually
reach production output. D-06 requires REQUIREMENTS.md and ROADMAP.md to stop asserting the
forbidden "named A&E by default" position before any other plan can be verified against it.

Output: `29-MEASUREMENT.md` (counts + composer state + suite runtime), REQUIREMENTS.md RULE-08
restated, ROADMAP.md Phase 29 criterion 2 restated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-CONTEXT.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-RESEARCH.md
@.planning/phases/28-ppe-ceiling-electrical-boundary-house-rules/28-07-MEASUREMENT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure local test-suite runtime, set the feedback-latency budget</name>
  <files>.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-VALIDATION.md, .planning/phases/29-cdm-duty-holder-emergency-arrangements/29-MEASUREMENT.md</files>
  <read_first>
    .planning/phases/29-cdm-duty-holder-emergency-arrangements/29-VALIDATION.md (fill its Wave 0
    Requirements checkboxes and frontmatter), .planning/phases/28-ppe-ceiling-electrical-boundary-house-rules/28-07-MEASUREMENT.md
    (format to mirror for the new 29-MEASUREMENT.md file)
  </read_first>
  <action>
    Run `php artisan test` locally (timed) to establish the full-suite runtime. Create
    `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-MEASUREMENT.md` mirroring
    `28-07-MEASUREMENT.md`'s structure — a "Local suite runtime" section recording the wall-clock
    time and pass/fail count, plus placeholder sections "Production CDM placeholder count" and
    "Production RAMS_UNIFIED_COMPOSER state" to be filled by Task 2. Update
    `29-VALIDATION.md` frontmatter: set `wave_0_complete: true` only after Task 2 also
    completes (leave `false` for now if Task 2 has not run), fill the "Estimated runtime" row
    in the Test Infrastructure table with the measured value, and set "Max feedback latency"
    in the Sampling Rate section to roughly 2x the measured full-suite runtime (documented
    rationale: worst case one extra full run plus overhead).
  </action>
  <acceptance_criteria>
    - `29-MEASUREMENT.md` exists and contains a numeric full-suite runtime and pass count from
      a real `php artisan test` run captured in this task (not estimated)
    - `29-VALIDATION.md`'s "Estimated runtime" and "Max feedback latency" rows are no longer "TBD"
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/29-cdm-duty-holder-emergency-arrangements/29-MEASUREMENT.md && grep -q "Local suite runtime" .planning/phases/29-cdm-duty-holder-emergency-arrangements/29-MEASUREMENT.md</automated>
  </verify>
  <done>29-MEASUREMENT.md exists with a real measured runtime; 29-VALIDATION.md's TBD fields are filled.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Measure production CDM placeholder count + confirm live RAMS_UNIFIED_COMPOSER state</name>
  <files>none — read-only production query, no repository files modified by this task</files>
  <action>
    Run the two read-only commands reproduced in `<how-to-verify>` below against the production
    database on `rams.21stcav.com`, logged in as `stcav` (never `root` — a prior verification run
    as root silently `git pull`-failed on dubious ownership and tested pre-fix code, per this
    project's own prior-phase verification notes). Both commands are read-only (a `config()` read
    and a `select` + in-memory filter; zero writes).
  </action>
  <what-built>
    A read-only measurement request for two facts this phase's later plans cannot proceed
    without: (1) how many production `rams_documents` rows still carry the CDM
    `[To be confirmed]` placeholder, and (2) whether production actually runs
    `RAMS_UNIFIED_COMPOSER=true` or `=false`. Neither can be determined from this repo/session —
    both require running commands against the live VPS as `stcav`, per this project's standing
    "validate on live, not just a fixture" practice and RESEARCH.md Finding 4/5.
  </what-built>
  <how-to-verify>
    SSH into the VPS as `stcav` (not root) and run, read-only, from the RAMS app directory:

    1. Confirm the live composer setting:
       `php artisan tinker --execute="echo config('rams.unified_composer') ? 'true' : 'false';"`

    2. Count production rows carrying the CDM placeholder (paste this exact block into
       `php artisan tinker`):
       ```
       $total = \App\Models\RamsDocument::count();
       $placeholderCount = \App\Models\RamsDocument::query()
           ->get(['id', 'reviewed_data', 'generated_data'])
           ->filter(function ($rams) {
               $rd = (array) ($rams->reviewed_data ?? []);
               $gd = (array) ($rams->generated_data ?? []);
               $cdmRowsHavePlaceholder = collect((array) ($rd['cdm'] ?? []))
                   ->contains(fn ($row) => str_contains((string) ($row['name'] ?? ''), 'To be confirmed'));
               $cdmDefaultHasPlaceholder = collect((array) ($gd['cdm_duty_holders'] ?? []))
                   ->contains(fn ($v) => is_string($v) && str_contains($v, 'To be confirmed'));
               return $cdmRowsHavePlaceholder || $cdmDefaultHasPlaceholder;
           })
           ->count();
       echo "Total: {$total}, with CDM placeholder: {$placeholderCount}\n";
       ```

    3. Report both numbers and the composer boolean back here so Task 1's
       `29-MEASUREMENT.md` can be completed with the two remaining sections, and set
       `29-VALIDATION.md` frontmatter `wave_0_complete: true`.
  </how-to-verify>
  <resume-signal>Paste the composer value and the "Total / with CDM placeholder" counts, or say "skip — proceed on worst-case assumptions" to continue with the migration treated as untested against a live count (backfill remains idempotent either way).</resume-signal>
  <acceptance_criteria>The composer boolean and the two counts (total, with CDM placeholder) are recorded in 29-MEASUREMENT.md and this plan's SUMMARY before Plan 29-05's migration is treated as final.</acceptance_criteria>
</task>


<task type="auto">
  <name>Task 3: Restate RULE-08 (REQUIREMENTS.md) and Phase 29 criterion 2 (ROADMAP.md) per D-05/D-06</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    .planning/REQUIREMENTS.md:87-88 (RULE-07's existing RESTATED note is the exact style to
    copy for RULE-08; RULE-08's current one-line text at :88 is what gets replaced),
    .planning/ROADMAP.md:187 (Phase 29 criterion 2, currently asserts "replaced by a named A&E
    with address by default" — the forbidden form per D-05),
    .planning/phases/29-cdm-duty-holder-emergency-arrangements/29-CONTEXT.md D-06 (the exact
    restated RULE-08 wording to use verbatim)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, replace the RULE-08 bullet at `:88` (currently: `**RULE-08**:
    Nearest A&E named with address; "to be identified at site induction" is not acceptable
    output.`) with the D-06 two-branch wording: "Where a 24/7 Emergency Department is verified
    for the site, it is named with full address and postcode, and route/travel time are
    confirmed at induction. Where it is not verified, the document states the hold point and
    the 24/7 ED requirement. The passive string 'to be identified at site induction' never
    appears in either branch." Append a parenthetical note in RULE-07's exact style: "*(**RESTATED
    2026-09-11.** Originally read [quote the current one-line text]. `house-rules.md`
    §'Emergency arrangements' (`:265-273`) is explicit that asserting a named A&E without
    verification is the exact defect GATE-12 exists to catch — see
    `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-CONTEXT.md` D-05/D-06.)*".
    Also update the RULE-08 row in the Traceability table (`Phase 29 | Pending`) — no text
    change needed there beyond confirming it still reads "Pending".
    In `.planning/ROADMAP.md`, replace Phase 29 criterion 2's text (currently: "The Emergency
    Procedures section's nearest-A&E line (...) is replaced by a named A&E with address by
    default") with: "The Emergency Procedures section's nearest-A&E line
    (`resources/views/pdf/rams.blade.php:1960`, currently the literal string 'Nearest hospital
    A&E to be identified at site induction.', plus its `'TBC'` fallbacks) is replaced by a
    resolver that names a verified 24/7 A&E with full address and postcode where one is
    recorded for the site, or states the house-rule hold-point line
    ('Nearest A&E — to be confirmed at induction (must be a 24/7 Emergency Department)') where
    it is not — never a guessed hospital name." Leave criteria 1, 3, 4 and the phase Goal/Plans
    lines untouched.
  </action>
  <acceptance_criteria>
    - `grep -c "RESTATED 2026-09-11" .planning/REQUIREMENTS.md` includes the new RULE-08 note
      (i.e. the count of RESTATED notes in the file increases by exactly one over its
      pre-task value)
    - `.planning/REQUIREMENTS.md` no longer contains the literal string `Nearest A&E named
      with address; "to be identified at site induction" is not acceptable output.` unqualified
      by the two-branch wording
    - `.planning/ROADMAP.md` Phase 29 criterion 2 no longer contains the phrase "by default"
      adjacent to "named A&E"
    - `.planning/ROADMAP.md` Phase 29 criterion 2 contains the exact hold-point string
      "Nearest A&E — to be confirmed at induction (must be a 24/7 Emergency Department)"
  </acceptance_criteria>
  <verify>
    <automated>grep -q "to be confirmed at induction (must be a 24/7 Emergency Department)" .planning/ROADMAP.md && grep -q "RESTATED 2026-09-11" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md RULE-08 and ROADMAP.md Phase 29 criterion 2 both match CONTEXT.md D-05/D-06 — no plan or requirement in the repo still asserts the forbidden "named A&E by default" position.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Planner → production DB | This task only reads (SELECT-shaped tinker queries); no writes occur in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-29-01-01 | Tampering | Manual tinker command pasted by a human on production | accept | Command is read-only (no `->update()`/`->save()` calls); reviewed verbatim in this plan before being given to the operator |
| T-29-01-02 | Information Disclosure | Row counts / composer flag shared in chat | accept | No PII, no credentials; aggregate counts and a boolean config value only |
</threat_model>

<verification>
`29-MEASUREMENT.md` exists with all three sections filled (local runtime, production placeholder
count, production composer state). `.planning/REQUIREMENTS.md:88` shows a RESTATED note in
RULE-07's style. `.planning/ROADMAP.md` Phase 29 criterion 2 no longer says "by default" for a
named A&E.
</verification>

<success_criteria>
Downstream plans (29-05's backfill migration, every render-site plan) can cite a real measured
production count and a confirmed composer state rather than an assumption; REQUIREMENTS.md and
ROADMAP.md are internally consistent with CONTEXT.md D-05/D-06 before any code is written.
</success_criteria>

<output>
Create `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-01-SUMMARY.md` when done
</output>
