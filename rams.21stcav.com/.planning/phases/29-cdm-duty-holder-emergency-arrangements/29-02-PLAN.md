---
phase: 29-cdm-duty-holder-emergency-arrangements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/Rams/SiteEmergencyResolver.php
  - config/rams_tier1.php
  - app/Support/Rams/SectionComposers/EmergencyComposer.php
  - app/Support/Rams/Sections/EmergencySectionDto.php
  - tests/Unit/Services/Rams/SiteEmergencyResolverTest.php
  - tests/Unit/Support/Rams/EmergencyComposerSiteEmergencyResolvedTest.php
autonomous: true
requirements: [RULE-08, GATE-12]
must_haves:
  truths:
    - "A single shared resolver decides the verified-name-vs-hold-point A&E branch — no render site computes this decision independently (D-01, RESEARCH.md anti-pattern)"
    - "The D-05 hold-point line is emitted verbatim, character-for-character from house-rules.md — never paraphrased"
    - "GATE-12's plausibility classifier never flags the D-05 hold-point line as a defect (D-08 conservative-by-construction: the hold-point line is legitimate output, not a violation)"
    - "The new GATE-11/GATE-12 kill-switch defaults false in config/rams_tier1.php, and is its own flag — never RAMS_DISPLAY_LIFT_GATE or RAMS_PPE_CEILING_ELECTRICAL_GATE (D-03)"
  artifacts:
    - path: "app/Services/Rams/SiteEmergencyResolver.php"
      provides: "resolve() -> {verified: bool, text: string} branch resolution (RULE-08) and classify() -> ?string plausibility check (GATE-12, D-08)"
    - path: "config/rams_tier1.php"
      provides: "'cdm_ae_gate_enabled' => env('RAMS_CDM_AE_GATE', false) block"
    - path: "app/Support/Rams/Sections/EmergencySectionDto.php"
      provides: "nearestHospitalResolvedText / nearestHospitalVerified fields carrying the resolved branch for the composer/rams-v2.blade.php path"
  key_links:
    - from: "app/Support/Rams/SectionComposers/EmergencyComposer.php"
      to: "app/Services/Rams/SiteEmergencyResolver.php"
      via: "EmergencyComposer::compose() calls SiteEmergencyResolver::resolve() to populate the DTO"
      pattern: "SiteEmergencyResolver::resolve"
---

<objective>
Build the one genuinely new piece of architecture this phase needs (RESEARCH.md Summary): a
shared, stateless resolver that turns `site_emergency` data into the D-05 two-branch A&E value
(verified name+address, or the exact hold-point line), plus the D-08 plausibility classifier
GATE-12 will call. Wire the resolver into the composer path (`EmergencyComposer` /
`EmergencySectionDto`) now, so the render-site plan (29-04) and the gate-wiring plan (29-03) both
have a single, already-tested value to read rather than re-deriving branch logic themselves.

Purpose: RESEARCH.md's anti-pattern section names "re-deriving the A&E branch logic
independently in each of the 5 render sites" as literally the defect GATE-12 exists to catch —
this plan closes that off structurally before any render site is touched.

Output: `SiteEmergencyResolver` (new class), the `cdm_ae_gate_enabled` config block (disarmed
per D-03), and `EmergencySectionDto` carrying the resolved value for the unified-composer path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-CONTEXT.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-RESEARCH.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-PATTERNS.md
@.planning/reference/21cav-rams-skill/references/house-rules.md
</context>

<interfaces>
<!-- Existing shapes this plan must read/produce exactly, extracted from the codebase. -->

From `app/Support/Rams/Sections/EmergencySectionDto.php` (current — this plan adds two new
readonly properties, does not remove any existing ones):
```php
final readonly class EmergencySectionDto
{
    public function __construct(
        public string $nearestHospital       = '',
        public string $fireAssemblyPoint     = '',
        // ...six more string fields, then...
        public array  $riddorMatrix          = [],
    ) {}
    public static function fromArray(array $data): self { /* ... */ }
    public function isEmpty(): bool { /* ... */ }
}
```

From `app/Support/Rams/SectionComposers/EmergencyComposer.php` (current — reads
`generated_data['site_emergency']` then `reviewed_data['site_emergency']` then `[]`):
```php
final class EmergencyComposer
{
    public function compose(RamsDocument $record): EmergencySectionDto
    {
        $rd = $record->reviewed_data  ?? [];
        $gd = $record->generated_data ?? [];
        $siteEmerg = (array) ($gd['site_emergency'] ?? ($rd['site_emergency'] ?? []));
        // ... builds EmergencySectionDto::fromArray([...]) from $siteEmerg + $rd ...
    }
}
```

`site_emergency`'s canonical keys (from `RamsController::updateAndDownload()`,
`:539-576`, and `EmergencySectionDto`'s own docblock): `nearest_hospital`, `hospital_address`,
`fire_assembly_point`, `fire_warden_name`, `fire_warden_contact`, `first_aider_name`,
`first_aider_contact`, `defibrillator_location`, `electrical_isolation_switch`,
`fire_extinguisher_class`. `SiteEmergencyResolver::resolve()` needs only `nearest_hospital` and
`hospital_address` from this array.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SiteEmergencyResolver — RULE-08 branch resolution + GATE-12 plausibility classifier</name>
  <files>app/Services/Rams/SiteEmergencyResolver.php, tests/Unit/Services/Rams/SiteEmergencyResolverTest.php</files>
  <read_first>
    .planning/reference/21cav-rams-skill/references/house-rules.md:265-273 (the exact hold-point
    wording — copy verbatim, do not paraphrase: "Nearest A&E — to be confirmed at induction (must
    be a 24/7 Emergency Department)"), app/Services/Rams/ControlTextRuleViolations.php:1-50 (the
    static-utility-class shape and "conservative by construction" docblock convention to mirror —
    do not add a constructor, all methods `public static function`),
    .planning/phases/29-cdm-duty-holder-emergency-arrangements/29-PATTERNS.md lines 249-282 (the
    `SiteEmergencyResolver` skeleton — build from this shape, not from scratch)
  </read_first>
  <behavior>
    - `resolve(array $siteEmergency): array` returns `['verified' => false, 'text' =>
      'Nearest A&E — to be confirmed at induction (must be a 24/7 Emergency Department)']` when
      `nearest_hospital` OR `hospital_address` (after `trim()`) is empty.
    - `resolve()` returns `['verified' => true, 'text' => '{name}, {address}. Route and travel
      time confirmed at induction.']` when both `nearest_hospital` and `hospital_address` are
      non-empty after trim.
    - `classify(array $siteEmergency): ?string` returns `null` (clean) when `resolve()` would
      produce the hold-point branch (D-08: the hold-point line is legitimate output, never
      flagged).
    - `classify()` returns `'banned_string'` when the raw `nearest_hospital` value itself
      contains the literal substring `'to be identified at site induction'`
      (case-insensitive) — a defence-in-depth backstop per D-01, even though no render site
      should ever write this value into `nearest_hospital` after this phase.
    - `classify()` returns `'urgent_care_keyword'` when a non-empty `nearest_hospital` matches
      (case-insensitive `stripos`) any of: `'urgent care'`, `'urgent treatment centre'`,
      `'minor injury unit'`, `'minor injuries unit'`, `'walk-in centre'`, `'walk in centre'`,
      `'utc'` (whole-word boundary check on `utc` specifically to avoid false-positiving on
      unrelated hospital names containing the substring — verify with a `preg_match`
      word-boundary test, not bare `stripos`, for that one keyword only).
    - `classify()` returns `'missing_address_or_postcode'` when `nearest_hospital` is non-empty
      but `hospital_address` is empty after trim (a named-but-unaddressed A&E — D-08 rule 3).
    - `classify()` returns `null` (clean) for a fully verified name+address pair that matches no
      urgent-care keyword and contains no banned string — the conservative-by-construction
      default per D-08's inherited Phase 28 D-01 rule.
  </behavior>
  <action>
    Create `App\Services\Rams\SiteEmergencyResolver` as a `final class` with no constructor, all
    methods `public static function`. Declare `private const HOLD_POINT = 'Nearest A&E — to be
    confirmed at induction (must be a 24/7 Emergency Department)';` and a
    `private const URGENT_CARE_KEYWORDS` array holding the six literal keywords listed in
    `<behavior>` (exclude `utc` from this array — handle it via a separate word-boundary regex
    inside `classify()`, documented inline as to why it needs different matching than the other
    five). Implement `resolve()` and `classify()` exactly per `<behavior>`. Write
    `SiteEmergencyResolverTest.php` (mirror `Ffp2ConfinedSpaceGateTest.php`'s direct-call test
    shape — no reflection needed since these are public static methods) with one test method per
    `<behavior>` bullet: `test_resolve_returns_hold_point_when_hospital_blank`,
    `test_resolve_returns_hold_point_when_address_blank`,
    `test_resolve_returns_verified_when_both_present`,
    `test_classify_passes_hold_point_clean`, `test_classify_flags_banned_string`,
    `test_classify_flags_urgent_care_keyword` (parametrised or repeated per keyword, including
    the `utc` word-boundary case with a hospital name like `"St Mary's Hospital"` proving `utc`
    as a substring of an unrelated word does NOT false-positive — pick a real word containing
    "utc" as a substring, e.g. none exists in English hospital names, so instead prove the
    word-boundary check against a name like `"Baseline Health Centre"` which must NOT match, and
    `"Willow UTC"` which MUST match), `test_classify_flags_missing_address`,
    `test_classify_passes_verified_clean`.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=SiteEmergencyResolverTest` passes, all methods green
    - `SiteEmergencyResolver::resolve(['nearest_hospital' => '', 'hospital_address' => ''])['text']`
      is character-identical to `house-rules.md:270`'s quoted hold-point sentence
    - `SiteEmergencyResolver::classify(['nearest_hospital' => '', 'hospital_address' => ''])` is `null`
    - No constructor is declared on the class (grep confirms no `__construct` in the file)
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=SiteEmergencyResolverTest</automated>
  </verify>
  <done>SiteEmergencyResolver exists with resolve()/classify() fully tested per the behavior list; the hold-point line is verbatim and always classifies clean.</done>
</task>

<task type="auto">
  <name>Task 2: Config flag block + wire the resolver into EmergencyComposer/EmergencySectionDto</name>
  <files>config/rams_tier1.php, app/Support/Rams/SectionComposers/EmergencyComposer.php, app/Support/Rams/Sections/EmergencySectionDto.php, tests/Unit/Support/Rams/EmergencyComposerSiteEmergencyResolvedTest.php</files>
  <read_first>
    config/rams_tier1.php:74-98 (the two existing gate-flag blocks — copy the doc-comment
    structure exactly, this new block explicitly documents the `false` default as a deliberate
    divergence per Pitfall 5), app/Support/Rams/Sections/EmergencySectionDto.php (full file, read
    in the interfaces block above — add fields, do not remove any), the SiteEmergencyResolver
    class created in Task 1
  </read_first>
  <action>
    Append a new config block to `config/rams_tier1.php` immediately after the
    `ffp2_confined_space_gate_enabled` block (`:98`): `'cdm_ae_gate_enabled' =>
    env('RAMS_CDM_AE_GATE', false),` with a doc-comment naming the two methods it will gate
    (`enforceCdmGate()`/`enforceEmergencyGate()`, added in Plan 29-03), stating it is a NEW
    independent flag per D-03 (never reuses `RAMS_DISPLAY_LIFT_GATE` or
    `RAMS_PPE_CEILING_ELECTRICAL_GATE`), and explicitly calling out the `false` default as
    deliberately different from the two `true`-default precedents (quote the Pitfall 5
    rationale: the corpus has not been measured clean yet, unlike GATE-06/07/09 at ship time).
    Add two new readonly properties to `EmergencySectionDto`: `public bool
    $nearestHospitalVerified = false` and `public string $nearestHospitalResolvedText = ''`,
    populated in `fromArray()` from new input keys `nearest_hospital_verified` (bool) and
    `nearest_hospital_resolved_text` (string), each defaulting to `false`/`''`. Do not remove or
    rename the existing `nearestHospital` property — it stays as the raw unresolved value for any
    caller that still reads it directly. Modify `EmergencyComposer::compose()` to call
    `\App\Services\Rams\SiteEmergencyResolver::resolve($siteEmerg)` once, and pass its `verified`
    and `text` values into `EmergencySectionDto::fromArray()`'s new
    `nearest_hospital_verified`/`nearest_hospital_resolved_text` keys alongside the existing
    array construction. Write `EmergencyComposerSiteEmergencyResolvedTest.php` proving: an empty
    `site_emergency` composes a DTO with `nearestHospitalVerified === false` and
    `nearestHospitalResolvedText` equal to the hold-point line; a `site_emergency` with both
    `nearest_hospital` and `hospital_address` set composes a DTO with `nearestHospitalVerified
    === true` and `nearestHospitalResolvedText` containing both values.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=EmergencyComposerSiteEmergencyResolvedTest` passes
    - `config('rams_tier1.cdm_ae_gate_enabled')` returns `false` with no `RAMS_CDM_AE_GATE` env
      var set (verify via `php artisan tinker --execute="var_dump(config('rams_tier1.cdm_ae_gate_enabled'));"`
      or an equivalent config-reading unit test)
    - `grep -n "RAMS_DISPLAY_LIFT_GATE\|RAMS_PPE_CEILING_ELECTRICAL_GATE" config/rams_tier1.php`
      shows the new block does not reference either existing env var name
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=EmergencyComposerSiteEmergencyResolvedTest</automated>
  </verify>
  <done>config/rams_tier1.php carries the disarmed cdm_ae_gate_enabled flag; EmergencySectionDto carries the resolved A&E branch for the unified-composer render path, proven by a passing composer test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Engineer review form → `site_emergency.nearest_hospital`/`hospital_address` → resolver → rendered PDF/DOCX | Free-text fields an engineer can type anything into; the resolver's job is to prevent a wrong-but-plausible-looking hospital name reaching a live safety document unverified |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-29-02-01 | Tampering | `SiteEmergencyResolver::classify()` false-negative (an unverified/wrong hospital name passes clean) | accept | D-08 explicitly inherits Phase 28 D-01's conservative-by-construction rule — a false negative leaves today's (already-shipped) behaviour unchanged; a false positive would silently overwrite an engineer's correct entry, which is the worse failure mode |
| T-29-02-02 | Tampering | `SiteEmergencyResolver::classify()` false-positive (a genuinely verified A&E gets flagged) | mitigate | Keyword list is a small, explicit static const array checked with `stripos`/word-boundary regex — no fuzzy matching that could misfire on a real hospital name |
</threat_model>

<verification>
`php artisan test --filter=SiteEmergencyResolverTest` and `php artisan test
--filter=EmergencyComposerSiteEmergencyResolvedTest` both green. `config/rams_tier1.php` diff
shows exactly one new block, default `false`, own env var name.
</verification>

<success_criteria>
A single, fully-tested resolver exists that both the gate (29-03) and every render site (29-04)
can consume without re-deriving branch logic; the new gate flag exists, disarmed, with its own
name.
</success_criteria>

<output>
Create `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-02-SUMMARY.md` when done
</output>
