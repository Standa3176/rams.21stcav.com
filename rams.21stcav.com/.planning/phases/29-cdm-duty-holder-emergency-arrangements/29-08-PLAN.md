---
phase: 29-cdm-duty-holder-emergency-arrangements
plan: 08
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php
  - tests/Feature/Rams/BackfillCdmDutyHolderMigrationTest.php
autonomous: true
requirements: [RULE-07, GATE-11]
must_haves:
  truths:
    - "The reviewed_data['cdm'] PD/PC guard matches only the bare seeded-default literal, never a genuine PM-authored sentence that merely contains the phrase 'To be confirmed'"
    - "No production data-repair or rollback task exists in this plan — production's actual run reported 0 reviewed_data.cdm replacements (latent-defect fix only, per orchestrator scope)"
  artifacts:
    - path: "database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php"
      provides: "fixCdmPlaceholder()'s reviewed_data['cdm'] branch uses an exact-literal/enumerated guard instead of str_contains()"
  key_links:
    - from: "database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php"
      to: "app/Services/Rams/RamsComplianceUpgradeService.php::DEFAULT_PRINCIPAL_DESIGNER_NOTE / DEFAULT_PRINCIPAL_CONTRACTOR_NOTE"
      via: "shared constants, unchanged by this plan"
      pattern: "RamsComplianceUpgradeService::DEFAULT_PRINCIPAL"
---

<objective>
Close VERIFICATION.md gap 2 / REVIEW.md CR-02: the CDM backfill migration's
`reviewed_data['cdm']` guard uses `str_contains($name, 'To be confirmed')` scoped to PD/PC
roles — a genuine PM-authored note such as "PD to be confirmed once client appoints one" would
be wholly replaced by boilerplate, discarding real project-specific text. Production's actual
run reported 0 such replacements this run (confirmed by 29-MEASUREMENT.md), so this is a LATENT
defect, not realised data loss — no repair or rollback task is in scope. The migration file
still carries the defect uncorrected, and it stays in the migrations table (could run again in
a fresh environment, staging refresh, or reseed), so the code itself must be fixed.

Purpose: Tighten the guard so it can never discard genuine engineer-typed text containing the
phrase, matching the exact-literal discipline the `generated_data['cdm_duty_holders']` branch
already correctly uses.
Output: Guard tightened to an exact-literal/enumerated-variant match; a regression test proving
a PM-typed sentence containing the substring survives unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-CONTEXT.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-VERIFICATION.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-REVIEW.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-MEASUREMENT.md
</context>

<interfaces>
<!-- database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php -->
Current constants (class body, top of the anonymous Migration class):
```
private const RAW_PLACEHOLDER = '[To be confirmed]';
private const NAME_SUBSTRING = 'To be confirmed';
private const PD_ROLE = 'principal designer';
private const PC_ROLE = 'principal contractor';
```
Current `reviewed_data['cdm']` branch inside `fixCdmPlaceholder()` (private static method):
```
if ($role === self::PD_ROLE && str_contains($name, self::NAME_SUBSTRING)) {
    $data['cdm'][$i]['name'] = RamsComplianceUpgradeService::DEFAULT_PRINCIPAL_DESIGNER_NOTE;
    $reviewedChanged = true;
} elseif ($role === self::PC_ROLE && str_contains($name, self::NAME_SUBSTRING)) {
    $data['cdm'][$i]['name'] = RamsComplianceUpgradeService::DEFAULT_PRINCIPAL_CONTRACTOR_NOTE;
    $reviewedChanged = true;
}
```
`$name = (string) ($cdmRow['name'] ?? '');` is untrimmed raw value; `$role` is already
lowercased+trimmed above this block. Existing test fixture pattern for PD/PC rows lives in
`tests/Feature/Rams/BackfillCdmDutyHolderMigrationTest.php::fixtureDocument()`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten reviewed_data['cdm'] guard to exact-literal/enumerated match</name>
  <files>database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php, tests/Feature/Rams/BackfillCdmDutyHolderMigrationTest.php</files>
  <read_first>
    database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php (whole file — class docblock's "VALUE-EQUALITY / SUBSTRING GUARD (D-02)" section, `fixCdmPlaceholder()` private static method)
    tests/Feature/Rams/BackfillCdmDutyHolderMigrationTest.php (whole file — `fixtureDocument()`, `test_backfill_never_overwrites_an_engineer_typed_real_name`, `runMigration()` helper)
  </read_first>
  <behavior>
    - Test: a `reviewed_data['cdm']` row with `role => 'Principal Designer'`, `name => 'PD to be confirmed once client appoints one'` (a genuine PM-authored sentence containing the exact substring `'To be confirmed'`, case preserved) is left byte-identical after `up()` runs — it must NOT be replaced by `RamsComplianceUpgradeService::DEFAULT_PRINCIPAL_DESIGNER_NOTE`.
    - Test: a `reviewed_data['cdm']` row with `role => 'Principal Contractor'`, `name => 'Contact details to be confirmed — Alex Carter is acting PD'` is likewise left byte-identical.
    - Existing test `test_backfill_patches_both_columns_and_shapes` must keep passing — the bare-literal case (`name => '[To be confirmed]'` pattern used in `reviewed_data['cdm']` today is actually the bare string `'To be confirmed'` per `fixtureDocument()`'s `self::RAW_PLACEHOLDER` reuse; confirm the exact fixture string used and preserve that this exact case still gets replaced).
    - Existing test `test_backfill_never_overwrites_an_engineer_typed_real_name` and `test_backfill_is_idempotent` must keep passing unchanged.
  </behavior>
  <action>
    In the migration's private const block, replace the single `NAME_SUBSTRING` guard target with
    an enumerated allowlist of exact seeded-default variants that must trigger replacement — at
    minimum the bare literal `'To be confirmed'` and `'[To be confirmed]'` (the bracketed form used
    elsewhere in the codebase), trimmed for comparison. Name the new constant something like
    `NAME_EXACT_VARIANTS` (an array of strings), replacing the single `NAME_SUBSTRING` string
    constant. In `fixCdmPlaceholder()`'s `reviewed_data['cdm']` branch, change the match condition
    from `str_contains($name, self::NAME_SUBSTRING)` to an exact-match check against the trimmed
    `$name` against `self::NAME_EXACT_VARIANTS` (e.g. `in_array(trim($name), self::NAME_EXACT_VARIANTS, true)`).
    Do not touch the `generated_data['cdm_duty_holders']` branch — it already uses correct
    exact-literal equality against `self::RAW_PLACEHOLDER` and is out of this gap's scope. Update
    the class docblock's "VALUE-EQUALITY / SUBSTRING GUARD (D-02)" section to describe the new
    exact-match behavior instead of the old substring behavior (the docblock currently states the
    substring guard is deliberate — correct that prose to match the fix; do not leave stale
    documentation describing removed behavior). Do NOT add any data-repair, re-run, or rollback
    logic — this fixes the code only, matching the orchestrator's explicit scope boundary that
    production reported 0 affected rows this run.
  </action>
  <acceptance_criteria>
    - `grep -n "str_contains(\$name" database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php` returns 0 matches in the `reviewed_data['cdm']` branch (the substring check is gone from that branch).
    - `grep -n "NAME_EXACT_VARIANTS\|in_array(trim(\$name)" database/migrations/2026_09_11_180000_backfill_cdm_duty_holder_placeholder.php` shows the new exact-match guard in place.
    - New test(s) in `BackfillCdmDutyHolderMigrationTest.php` assert `assertSame($before->reviewed_data, $after->reviewed_data, ...)` for the two PM-authored-sentence fixtures described in `<behavior>`.
    - `php artisan test tests/Feature/Rams/BackfillCdmDutyHolderMigrationTest.php` — all tests pass (existing 4 + new).
    - `php artisan test tests/Feature/Rams/CdmEmergencyDualPathGateTest.php` still passes (no shared-constant regression).
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com" && php artisan test tests/Feature/Rams/BackfillCdmDutyHolderMigrationTest.php tests/Feature/Rams/CdmEmergencyDualPathGateTest.php</automated>
  </verify>
  <done>reviewed_data['cdm']'s PD/PC guard matches only exact seeded-default variants; a PM-authored sentence containing "To be confirmed" survives the migration unchanged; all pre-existing migration tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewer-form free text (`reviewed_data['cdm'][*]['name']`) → migration's replace-on-match guard | A migration that runs against production JSON columns; an overly loose match destroys genuine project-specific text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-08-01 | Repudiation / data-integrity | `fixCdmPlaceholder()` reviewed_data['cdm'] branch | mitigate | This plan's fix — exact-literal/enumerated guard so genuine engineer-typed text is never silently discarded, matching the append-only/no-destructive-overwrite spirit of C5 |
| T-29-08-02 | Tampering | Migration re-run in a future environment (staging refresh, fresh seed) | mitigate | Fixing the guard in the migration file itself (not a one-off data patch) so any future run of this same migration is safe |
</threat_model>

<verification>
`php artisan test tests/Feature/Rams/BackfillCdmDutyHolderMigrationTest.php tests/Feature/Rams/CdmEmergencyDualPathGateTest.php`
— all pass. No production migration re-run is performed or planned.
</verification>

<success_criteria>
The reviewed_data['cdm'] guard only replaces the bare seeded-default placeholder variants, never
a genuine PM-authored sentence containing the phrase. No data-repair task exists in this plan
(latent-defect fix only).
</success_criteria>

<output>
Create `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-08-SUMMARY.md` when done
</output>
