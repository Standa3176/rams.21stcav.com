---
phase: 29-cdm-duty-holder-emergency-arrangements
plan: 03
type: execute
wave: 2
depends_on: ["29-02"]
files_modified:
  - app/Services/Rams/RamsComplianceUpgradeService.php
  - app/Services/DocxBuilderService.php
  - tests/Unit/Services/Rams/CdmDutyHolderWordingTest.php
  - tests/Unit/Services/Rams/CdmEmergencyGateTest.php
autonomous: true
requirements: [RULE-07, GATE-11, GATE-12]
must_haves:
  truths:
    - "The CDM duty-holder table never contains the literal '[To be confirmed]' string for principal_designer or principal_contractor after upgrade() runs, on any job (RULE-07 applied unconditionally — no occupied-premises signal exists, see RESEARCH.md Finding 6/Assumption A2)"
    - "The CDM wording never asserts 21CAV unequivocally IS the sole contractor, never claims the client retains Principal Designer responsibilities, never claims 21CAV discharges duties under Regulations 4 or 5, and never claims the Principal Contractor must notify HSE"
    - "GATE-11 throws RamsGenerationException if a placeholder somehow survives addCdmDutyHolders(), independently of the default-filler, mirroring the GATE-06/07 auto-correct-plus-throwing-recheck shape"
    - "GATE-12 throws on a named-but-implausible A&E and passes clean on the D-05 hold-point line, using SiteEmergencyResolver::classify() as its single source of truth"
    - "Both gates are disarmed by default (config('rams_tier1.cdm_ae_gate_enabled') === false) and upgrade() is byte-identical to pre-Phase-29 behaviour when the flag is false, aside from the RULE-07 wording change which is unconditional and NOT behind the gate flag"
  artifacts:
    - path: "app/Services/Rams/RamsComplianceUpgradeService.php"
      provides: "addCdmDutyHolders() restated wording, enforceCdmGate() (GATE-11), enforceEmergencyGate() (GATE-12), a new resolveSiteEmergency() step, all wired into upgrade()"
    - path: "app/Services/DocxBuilderService.php"
      provides: "buildCdmSection() fallback literals updated to match the restated RULE-07 default (the second origin of the old placeholder, per RESEARCH.md Integration Points)"
  key_links:
    - from: "app/Services/Rams/RamsComplianceUpgradeService.php upgrade()"
      to: "app/Services/Rams/SiteEmergencyResolver.php"
      via: "resolveSiteEmergency() calls SiteEmergencyResolver::resolve() and writes $data['site_emergency_resolved']"
      pattern: "SiteEmergencyResolver::resolve"
    - from: "app/Services/Rams/RamsComplianceUpgradeService.php enforceEmergencyGate()"
      to: "app/Services/Rams/SiteEmergencyResolver.php"
      via: "enforceEmergencyGate() calls SiteEmergencyResolver::classify() on the same site_emergency input, never re-deriving the branch logic"
      pattern: "SiteEmergencyResolver::classify"
---

<objective>
Replace the CDM duty-holder table's forbidden defaults with the restated RULE-07 wording
(unconditionally — RESEARCH.md Finding 6 confirms no deterministic "occupied premises" signal
exists anywhere in this codebase, so gating this on a signal that doesn't exist is not an option;
Assumption A2 explicitly asks the planner to state this choice rather than bury it, done here).
Add GATE-11 (CDM placeholder survival) and GATE-12 (A&E plausibility) as a config-gated
independent re-check immediately after the default-filler, mirroring the GATE-06/07/09 shape
verbatim. Fix the second CDM placeholder origin in `DocxBuilderService::buildCdmSection()`'s
fallback literals, which RESEARCH.md's Integration Points section identifies as a second,
independent source of the same defect.

Purpose: this is the pipeline core — every render site (29-04) and the backfill migration
(29-05) depend on the exact wording strings and gate method names this plan establishes.

Output: `addCdmDutyHolders()` restated, `enforceCdmGate()`/`enforceEmergencyGate()` added and
wired, `DocxBuilderService::buildCdmSection()` fallback fixed, both gates covered by tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-CONTEXT.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-RESEARCH.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-PATTERNS.md
@.planning/reference/21cav-rams-skill/references/standards-and-legislation.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-02-SUMMARY.md
</context>

<interfaces>
<!-- Extracted from the codebase; the executor should use these directly. -->

From `app/Services/Rams/RamsComplianceUpgradeService.php` (current pipeline, `:36-77`):
```php
public static function upgrade(array $ramsData): array
{
    // ... existing steps ...
    if (config('rams_tier1.display_lift_gate_enabled', true)) {
        $ramsData = self::enforceDisplayLiftGate($ramsData);
    }
    if (config('rams_tier1.ffp2_confined_space_gate_enabled', true)) {
        $ramsData = self::enforceFfp2AndConfinedSpaceGate($ramsData);
    }
    $ramsData = self::crossReferenceMethodStatementRisks($ramsData);
    $ramsData = self::addCdmDutyHolders($ramsData);
    $ramsData = self::cleanTextArtifacts($ramsData);
    return $ramsData;
}
```

Current `addCdmDutyHolders()` (`:1093-1111`), the method this task modifies — do not
string-replace `'[To be confirmed]'` in place; restructure per `<action>` below:
```php
private static function addCdmDutyHolders(array $data): array
{
    $project = (array) ($data['project'] ?? []);
    $data['cdm_duty_holders'] = [
        'client'               => trim((string) ($project['client'] ?? '')) ?: '[Client Name]',
        'principal_designer'   => '[To be confirmed]',
        'principal_contractor' => '[To be confirmed]',
        'contractor'           => '21st Century AV Ltd',
        'subcontractor'        => '21st Century AV Ltd',
        'project_manager'      => trim((string) ($project['project_manager'] ?? '')) ?: '[To be confirmed]',
        'site_supervisor'      => trim((string) ($project['lead_engineer'] ?? '')) ?: '[To be confirmed]',
        'cdm_regulation'       => 'Construction (Design and Management) Regulations 2015',
        'notification'         => 'F10 notification submitted by Principal Contractor where applicable',
    ];
    return $data;
}
```
Note `'notification'`'s current text ("F10 notification submitted by Principal Contractor")
already violates one of RULE-07's four forbidden statements ("Never write 'the Principal
Contractor must notify HSE'" — `standards-and-legislation.md:35-36`) — this key must also be
rewritten in this task, not just `principal_designer`/`principal_contractor`.

`RamsGenerationException` (`app/Exceptions/RamsGenerationException.php`) is a bare
`extends Exception` — no special constructor, throw with `sprintf()`'d messages as the existing
gates do.

RESEARCH.md Open Question 3 (does `RamsController::download()`'s third catch site,
`:697-705`, surface `RamsGenerationException` as cleanly as the other two?) is CONFIRMED
RESOLVED during planning, no controller change needed: its `catch (\Throwable $e)` block
captures `$e->getMessage()` into `$rebuildError` and surfaces it verbatim via `'DOCX download
failed: ' . $rebuildError . ' — please contact support or try Regenerate.'`
(`RamsController.php:704-723`) — this already surfaces a GATE-11/GATE-12 throw's message
cleanly. No task in this plan touches `RamsController.php`.

`SiteEmergencyResolver::resolve(array $siteEmergency): array` and
`SiteEmergencyResolver::classify(array $siteEmergency): ?string` (from Plan 29-02) are the two
methods this plan's `resolveSiteEmergency()`/`enforceEmergencyGate()` call.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restate addCdmDutyHolders() (RULE-07) unconditionally, fix DocxBuilderService's CDM fallback</name>
  <files>app/Services/Rams/RamsComplianceUpgradeService.php, app/Services/DocxBuilderService.php, tests/Unit/Services/Rams/CdmDutyHolderWordingTest.php</files>
  <read_first>
    app/Services/Rams/RamsComplianceUpgradeService.php:1093-1111 (addCdmDutyHolders, the method
    to rewrite), app/Services/DocxBuilderService.php:1680-1712 (buildCdmSection, the second
    placeholder origin at the `?? '[To be confirmed]'` fallbacks on `principal_designer`/
    `principal_contractor`), .planning/reference/21cav-rams-skill/references/standards-and-legislation.md:17-41
    (the verbatim anticipated-sole-contractor sentence, the conditional Contractor row, the four
    forbidden statements — read this in full, it is short)
  </read_first>
  <behavior>
    - `addCdmDutyHolders()`'s `'client'` key is unchanged (still the client name or
      `'[Client Name]'` fallback).
    - `'principal_designer'` becomes: `'Not formally appointed at this stage — the client will
      confirm Principal Designer arrangements before works commence if the wider project
      requires one (CDM 2015 Regulation 5).'` — never `'[To be confirmed]'`.
    - `'principal_contractor'` becomes: `'If the client appoints a Principal Contractor, 21CAV
      works to their Construction Phase Plan and site arrangements. If 21CAV is confirmed as
      sole contractor, 21CAV prepares and implements the Construction Phase Plan under CDM 2015
      Regulation 15.'` — never `'[To be confirmed]'`, never asserts 21CAV discharges duties
      under Regulations 4 or 5.
    - A new key `'contractor_note'` carries the anticipated-sole-contractor sentence VERBATIM
      from `standards-and-legislation.md:23-28`: `'21CAV is currently anticipated to be the sole
      contractor for the AV installation scope. The client shall confirm whether the overall
      project involves, or is likely to involve, more than one contractor before works
      commence.'`
    - `'notification'` becomes: `'Most single-visit AV installation works fall below the CDM
      2015 notifiable-project threshold. Where the wider project is notifiable, F10
      notification is the Client\'s duty (it may be submitted on the Client\'s behalf) —
      notifiability is judged on the whole project, not 21CAV\'s scope alone.'` — never asserts
      the Principal Contractor must notify HSE.
    - `'contractor'`, `'subcontractor'`, `'cdm_regulation'`, `'project_manager'`,
      `'site_supervisor'` are unchanged (the latter two keep their existing
      `trim(...) ?: '[To be confirmed]'` fallback — that placeholder reflects genuinely unknown
      project data, not the RULE-07 settled position GATE-11 targets; do not remove it).
    - Applied unconditionally on every call — no occupied-premises branch, no new input signal
      (RESEARCH.md Finding 6: no such signal exists in this codebase; inventing one is out of
      scope).
    - `DocxBuilderService::buildCdmSection()`'s two literal `?? '[To be confirmed]'` fallbacks
      for `principal_designer`/`principal_contractor` are replaced with the SAME restated
      strings as above (extract as constants on `RamsComplianceUpgradeService`, e.g.
      `public const DEFAULT_PRINCIPAL_DESIGNER_NOTE` / `DEFAULT_PRINCIPAL_CONTRACTOR_NOTE`, so
      both call sites share one literal each rather than duplicating prose).
  </behavior>
  <action>
    Add two `public const` string constants to `RamsComplianceUpgradeService` holding the
    `principal_designer` and `principal_contractor` restated text (name them
    `DEFAULT_PRINCIPAL_DESIGNER_NOTE` and `DEFAULT_PRINCIPAL_CONTRACTOR_NOTE`) so Plan 29-05's
    backfill migration and `DocxBuilderService::buildCdmSection()` can both reference the exact
    same strings without duplicating them. Rewrite `addCdmDutyHolders()`'s array literal per
    `<behavior>`, referencing the two new constants for `principal_designer`/
    `principal_contractor`. Update `DocxBuilderService::buildCdmSection()`'s `$rows` array
    (`:1706-1707`) so the `?? '[To be confirmed]'` fallbacks become
    `?? \App\Services\Rams\RamsComplianceUpgradeService::DEFAULT_PRINCIPAL_DESIGNER_NOTE` and
    the contractor equivalent — these fallbacks only fire if `cdm_duty_holders` is somehow
    missing the key entirely (defence-in-depth; `addCdmDutyHolders()` always sets both keys, so
    this is belt-and-braces). Write `CdmDutyHolderWordingTest.php` (reflection-invoke
    `addCdmDutyHolders()` per the `Ffp2ConfinedSpaceGateTest.php` pattern) with one test per
    `<behavior>` bullet, plus four explicit negative-assertion tests proving each of the four
    forbidden statements never appears anywhere in the resulting `cdm_duty_holders` array
    (`assertStringNotContainsString` for: `'21CAV is the sole contractor'` unequivocal form,
    `'retains Principal Designer'`, `'Regulation 4'`/`'Regulation 5'` in a duty-discharge
    context, `'Principal Contractor must notify'`).
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=CdmDutyHolderWordingTest` passes
    - `addCdmDutyHolders([])['cdm_duty_holders']['principal_designer']` never equals literal
      `'[To be confirmed]'`
    - `addCdmDutyHolders([])['cdm_duty_holders']['principal_contractor']` never equals literal
      `'[To be confirmed]'`
    - `addCdmDutyHolders([])['cdm_duty_holders']['notification']` does not contain the substring
      `'Principal Contractor'` immediately followed by `'notify'` (case-insensitive)
    - `grep -n "DEFAULT_PRINCIPAL_DESIGNER_NOTE\|DEFAULT_PRINCIPAL_CONTRACTOR_NOTE" app/Services/DocxBuilderService.php`
      shows both constants referenced in `buildCdmSection()`
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=CdmDutyHolderWordingTest</automated>
  </verify>
  <done>addCdmDutyHolders() emits the restated RULE-07 wording unconditionally, on every job, with none of the four forbidden statements present; DocxBuilderService's second placeholder origin is fixed to match.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GATE-11 (CDM placeholder survival) + GATE-12 (A&E plausibility) wired into upgrade()</name>
  <files>app/Services/Rams/RamsComplianceUpgradeService.php, tests/Unit/Services/Rams/CdmEmergencyGateTest.php</files>
  <read_first>
    app/Services/Rams/RamsComplianceUpgradeService.php:1298-1378 (enforceFfp2AndConfinedSpaceGate
    — the exact shape to copy: unconditional check, throw on first violation, message names
    offending value + rule ID + disabling env var), the config flag added in Plan 29-02
    (`cdm_ae_gate_enabled`), tests/Unit/Services/Rams/Ffp2ConfinedSpaceGateTest.php:56-64 (the
    reflection-invoke helper to copy verbatim for testing these two new private static methods)
  </read_first>
  <behavior>
    - `enforceCdmGate(array $data): array` loops `$data['cdm_duty_holders']['principal_designer']`
      and `['principal_contractor']` only (not `project_manager`/`site_supervisor` — those
      legitimately carry a data-dependent placeholder, out of GATE-11's scope per RESEARCH.md
      Integration Points). Throws `RamsGenerationException` if either value is literally
      `'[To be confirmed]'`, naming the field, `GATE-11/RULE-07`, and
      `RAMS_CDM_AE_GATE=false` as the disabling flag. Returns `$data` unchanged otherwise.
    - `enforceEmergencyGate(array $data): array` reads `$data['site_emergency'] ?? []` and calls
      `SiteEmergencyResolver::classify()` on it. Throws `RamsGenerationException` when
      `classify()` returns a non-null string, naming the classification reason, `GATE-12/RULE-08`,
      and `RAMS_CDM_AE_GATE=false`. Returns `$data` unchanged when `classify()` returns `null`
      (covers both the hold-point branch and a fully verified name+address).
    - A new `resolveSiteEmergency(array $data): array` private static method calls
      `SiteEmergencyResolver::resolve($data['site_emergency'] ?? [])` and writes the result to
      `$data['site_emergency_resolved']` (an array with `verified`/`text` keys) — this key is
      what all five render sites (Plan 29-04) will read. Runs BEFORE `enforceEmergencyGate()` in
      the pipeline (the gate classifies the same raw input the resolver reads, so ordering
      relative to the gate does not matter for correctness, but the resolved key must exist
      before any render site runs).
    - Both gates are called back-to-back under the SAME `if
      (config('rams_tier1.cdm_ae_gate_enabled', false))` block, per RESEARCH.md Open Question 1's
      recommendation — mirrors how GATE-06/GATE-07 share one config check inside
      `enforceFfp2AndConfinedSpaceGate()`.
  </behavior>
  <action>
    In `upgrade()`, insert `$ramsData = self::resolveSiteEmergency($ramsData);` immediately
    before the existing `$ramsData = self::addCdmDutyHolders($ramsData);` line. Immediately
    after that line, insert:
    ```
    if (config('rams_tier1.cdm_ae_gate_enabled', false)) {
        $ramsData = self::enforceCdmGate($ramsData);
        $ramsData = self::enforceEmergencyGate($ramsData);
    }
    ```
    Implement `resolveSiteEmergency()`, `enforceCdmGate()`, `enforceEmergencyGate()` as new
    `private static function(array $data): array` methods per `<behavior>`, placed near
    `addCdmDutyHolders()` in the "11. CDM 2015 DUTY HOLDERS" section, renaming that section
    heading comment to "11. CDM 2015 DUTY HOLDERS + SITE EMERGENCY (GATE-11/GATE-12)". Import
    `App\Services\Rams\SiteEmergencyResolver` at the top of the file. Write
    `CdmEmergencyGateTest.php` using the reflection-invoke helper, with test methods:
    `test_enforceCdmGate_throws_on_placeholder_principal_designer`,
    `test_enforceCdmGate_throws_on_placeholder_principal_contractor`,
    `test_enforceCdmGate_passes_on_restated_wording` (feed it the real
    `addCdmDutyHolders()` output), `test_enforceEmergencyGate_throws_on_banned_string`,
    `test_enforceEmergencyGate_throws_on_urgent_care_keyword`,
    `test_enforceEmergencyGate_throws_on_missing_address`,
    `test_enforceEmergencyGate_passes_on_hold_point`,
    `test_enforceEmergencyGate_passes_on_verified_name_and_address`,
    `test_resolveSiteEmergency_writes_resolved_key`. Include the "break-the-fix, watch it fail,
    restore, watch it pass" non-vacuity discipline in the class docblock, mirroring
    `Ffp2ConfinedSpaceGateTest.php:22-51` — document (not just assert) that this was performed.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=CdmEmergencyGateTest` passes, 9 test methods all green
    - `config('rams_tier1.cdm_ae_gate_enabled')` false (no env override) means calling
      `RamsComplianceUpgradeService::upgrade([])` never throws, even with a raw
      `'[To be confirmed]'` forced into `cdm_duty_holders` before the call (gate genuinely
      inert when disarmed)
    - `$ramsData['site_emergency_resolved']['text']` is set after any `upgrade()` call,
      regardless of gate-flag state (the resolver step is unconditional; only the two gates are
      config-gated)
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=CdmEmergencyGateTest</automated>
  </verify>
  <done>GATE-11 and GATE-12 are implemented, wired under one disarmed-by-default flag, both throw/pass correctly per the full behavior matrix, and the resolved A&E value is unconditionally available to render sites.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `upgrade()` pipeline input (engineer-typed `project`/`site_emergency` data) → gate → thrown exception surfaced to the controller | An engineer's typed data can trigger a gate throw; the throw must never leak a stack trace, only the friendly `RamsGenerationException` message (existing controller catch pattern, unchanged by this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-29-03-01 | Denial of Service | `enforceCdmGate()`/`enforceEmergencyGate()` throwing on every regenerate once armed | accept | Gates ship disarmed (D-03); arming is a deliberate later `.env` change made only after the corpus is measured clean, out of this phase's scope |
| T-29-03-02 | Tampering | A future edit accidentally reuses `RAMS_DISPLAY_LIFT_GATE`/`RAMS_PPE_CEILING_ELECTRICAL_GATE` for this new gate | mitigate | New, independent `cdm_ae_gate_enabled` key established in Plan 29-02; this plan's tasks only ever reference that key |
</threat_model>

<verification>
`php artisan test --filter=CdmDutyHolderWordingTest` and `php artisan test
--filter=CdmEmergencyGateTest` both green. `php artisan test --filter=RamsComplianceUpgradeService`
(broader filter) still green — no regression to existing gates/steps.
</verification>

<success_criteria>
The pipeline core for both requirements is complete and independently tested: RULE-07's wording
is unconditional and forbidden-statement-free, GATE-11/GATE-12 exist and are disarmed, and the
resolved A&E value is available for every render site to consume in the next wave.
</success_criteria>

<output>
Create `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-03-SUMMARY.md` when done
</output>
