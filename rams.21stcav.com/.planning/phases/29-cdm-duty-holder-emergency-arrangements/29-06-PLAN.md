---
phase: 29-cdm-duty-holder-emergency-arrangements
plan: 06
type: execute
wave: 4
depends_on: ["29-04", "29-05"]
files_modified:
  - tests/Feature/Rams/CdmEmergencyDualPathGateTest.php
  - tests/Fixtures/rams/tilda-21cq29531/expected-html-v1.html
  - tests/Fixtures/rams/tilda-21cq29531/expected-html-v2.html
  - tests/Fixtures/rams/tilda-21cq29531/expected-docx-v1.xml.norm
  - tests/Fixtures/rams/tilda-21cq29531/expected-docx-v2.xml.norm
autonomous: false
requirements: [GATE-11, GATE-12]
must_haves:
  truths:
    - "GATE-11 and GATE-12 are independently proven reachable from both real generation entry points (buildFromForm()/runPipeline() and buildFromReview()/runFromReview()), not just via reflection on the private methods"
    - "The four tilda-21cq29531 fixture files no longer contain the pre-Phase-29 A&E string and pass their snapshot comparison"
    - "The full test suite (including the snapshot group) is green before this phase is handed to /gsd:verify-work"
    - "A live occupied-premises project regeneration is manually verified against production data, not just fixtures, per this project's standing 'validate on live' practice (ROADMAP criterion 4)"
  artifacts:
    - path: "tests/Feature/Rams/CdmEmergencyDualPathGateTest.php"
      provides: "Proof that GATE-11/GATE-12 fire on both real generation entry points, mirroring Ffp2ConfinedSpaceDualPathGateTest.php"
    - path: "tests/Fixtures/rams/tilda-21cq29531/expected-html-v1.html"
      provides: "Regenerated golden fixture reflecting the restated CDM wording and resolved A&E value"
  key_links:
    - from: "tests/Feature/Rams/CdmEmergencyDualPathGateTest.php"
      to: "app/Services/Rams/RamsComplianceUpgradeService.php"
      via: "drives RamsBuilderService::buildFromForm()/buildFromReview(), which internally call upgrade()"
      pattern: "enforceCdmGate|enforceEmergencyGate"
---

<objective>
Close the phase out: prove GATE-11/GATE-12 are reachable from both real generation entry points
(not just the unit-level reflection tests from Plan 29-03), regenerate the four
`tilda-21cq29531` golden fixtures the render-site changes invalidated, run the full suite
(including the snapshot group) green, and get a human-verified live regeneration against
production data per ROADMAP criterion 4's "not just a fixture" requirement.

Purpose: this is the phase's own verification gate before `/gsd:verify-work` — RESEARCH.md's
Wave 0 gap list and 29-VALIDATION.md's Manual-Only Verifications table both name this exact set
of checks as required before the phase can be considered done.

Output: a passing dual-path test, four regenerated fixtures, a green full suite, and a
human-confirmed live regeneration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-CONTEXT.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-RESEARCH.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-VALIDATION.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-01-SUMMARY.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-02-SUMMARY.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-03-SUMMARY.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-04-SUMMARY.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-05-SUMMARY.md
@tests/Feature/Rams/Ffp2ConfinedSpaceDualPathGateTest.php
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dual-path proof — GATE-11/GATE-12 fire from both real generation entry points</name>
  <files>tests/Feature/Rams/CdmEmergencyDualPathGateTest.php</files>
  <read_first>
    tests/Feature/Rams/Ffp2ConfinedSpaceDualPathGateTest.php (full file — this is the exact
    shape to mirror: one test driving `RamsBuilderService::buildFromForm()`/`runPipeline()`, one
    driving `buildFromReview()`/`runFromReview()`, both asserting the thrown message and the
    resulting `RamsDocument::STATUS_FAILED` status), config/rams_tier1.php
    (`cdm_ae_gate_enabled` — this test must set `config(['rams_tier1.cdm_ae_gate_enabled' =>
    true])` explicitly since the flag defaults false; the existing FFP2 test's target gate
    defaults true so it never needed this override — do not skip it here)
  </read_first>
  <behavior>
    - `test_gate_throws_via_run_pipeline()`: builds a `RamsDocument` via `buildFromForm()` whose
      resulting `cdm_duty_holders` would carry the placeholder, with `cdm_ae_gate_enabled`
      forced true, and asserts a `RamsGenerationException` mentioning `GATE-11` is thrown and
      the document ends up `STATUS_FAILED`.
    - `test_gate_throws_via_run_from_review()`: builds a `RamsDocument` via `buildFromReview()`
      with a `site_emergency` carrying an urgent-care-keyword hospital name (or the banned
      string, or a missing address), `cdm_ae_gate_enabled` forced true, and asserts a
      `RamsGenerationException` mentioning `GATE-12` is thrown and the document ends up
      `STATUS_FAILED`.
    - A third test proves BOTH gates stay silent (no throw) when `cdm_ae_gate_enabled` is left
      at its default `false`, driving the same fixture data through either entry point —
      proving the disarmed-by-default posture holds even when the underlying data would
      otherwise violate GATE-11/GATE-12.
  </behavior>
  <action>
    Create `CdmEmergencyDualPathGateTest.php` in `tests/Feature/Rams/`, copying
    `Ffp2ConfinedSpaceDualPathGateTest.php`'s class structure (`RefreshDatabase`, the
    `fakeClaudeResponse()` helper, the same `RamsDocument::create()` fixture shape for both
    entry points). Since `RamsComplianceUpgradeService::addCdmDutyHolders()` runs unconditionally
    and always sets `principal_designer`/`principal_contractor` to the restated wording (never
    the literal placeholder, per Plan 29-03), the `runPipeline()` GATE-11 fixture must instead
    prove the gate via a `site_emergency`/CDM shape that would only trip the gate if the
    default-filler were somehow bypassed — if constructing a genuine GATE-11 trip through the
    real pipeline proves impossible (because the default-filler is unconditional and always
    correct), pivot this test to prove GATE-11 via direct construction of a `generated_data`
    array with the placeholder forced in BEFORE `buildFromForm()`/`upgrade()` runs, confirming
    the throw fires even though `addCdmDutyHolders()` would have corrected it — i.e. prove the
    gate is a genuine independent re-check, not dead code, exactly like the FFP2 dual-path test
    proves for GATE-06/07. Document whichever approach was needed in the test class docblock,
    matching the existing file's own "why this fixture shape was necessary" documentation style.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=CdmEmergencyDualPathGateTest` passes, 3 test methods green
    - The test file's docblock explains, in the same style as
      `Ffp2ConfinedSpaceDualPathGateTest.php`, why its fixture shape proves the gate is reachable
      via each real entry point rather than only via reflection
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=CdmEmergencyDualPathGateTest</automated>
  </verify>
  <done>GATE-11 and GATE-12 are proven reachable from both real generation entry points, and proven silent when disarmed — not just from the Plan 29-03 reflection-based unit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate tilda-21cq29531 fixtures, run full suite including snapshot group</name>
  <files>tests/Fixtures/rams/tilda-21cq29531/expected-html-v1.html, tests/Fixtures/rams/tilda-21cq29531/expected-html-v2.html, tests/Fixtures/rams/tilda-21cq29531/expected-docx-v1.xml.norm, tests/Fixtures/rams/tilda-21cq29531/expected-docx-v2.xml.norm</files>
  <read_first>
    tests/Feature/Rams/Snapshot/PdfSnapshotTest.php:1-70 (confirms the exact regeneration
    command/mechanism this codebase uses — locate and use the real command it documents, e.g.
    `php artisan rams:regenerate-snapshots tilda-21cq29531`), the four fixture files' current
    content around the known-stale lines (`expected-html-v1.html:1278`,
    `expected-html-v2.html:1313`) to confirm they currently contain the pre-Phase-29 A&E string
    before regenerating
  </read_first>
  <action>
    Run the fixture regeneration command identified in `PdfSnapshotTest.php` scoped to
    `tilda-21cq29531` so only this phase's four known-stale fixtures are touched (not a
    repo-wide regeneration, which would silently absorb unrelated drift). After regeneration,
    diff each of the four files against its pre-regeneration version and confirm the ONLY
    changes are in the A&E/CDM text this phase's plans touched — if the diff shows changes
    unrelated to CDM/A&E/Welfare text, stop and investigate before committing (an unrelated diff
    means either a regression elsewhere or a stale local `.env`/config state affecting the
    render). Run `php artisan test` (fast suite) followed by `vendor/bin/phpunit --group
    snapshot` and confirm both are fully green. Record the final pass counts in this plan's
    SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -c "to be identified at site induction" tests/Fixtures/rams/tilda-21cq29531/expected-html-v1.html`
      returns 0 (was non-zero before this task)
    - `grep -c "to be identified at site induction" tests/Fixtures/rams/tilda-21cq29531/expected-html-v2.html`
      returns 0
    - `php artisan test` exits 0
    - `vendor/bin/phpunit --group snapshot` exits 0
  </acceptance_criteria>
  <verify>
    <automated>php artisan test && vendor/bin/phpunit --group snapshot</automated>
  </verify>
  <done>All four tilda-21cq29531 fixtures are regenerated and free of the banned string; the full suite including the snapshot group is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live production regeneration — CDM and A&E verified on a real occupied-premises project</name>
  <files>none — manual verification against a live deployment, no repository files modified by this task</files>
  <action>
    Nothing further to automate — Tasks 1-2 and Plans 29-01 through 29-05 already implemented,
    tested, and fixture-verified every code change. This task hands off to the human step
    ROADMAP criterion 4 requires: verification against real production data, not just a fixture.
  </action>
  <what-built>
    Every code change this phase makes: the restated CDM wording (RULE-07), the A&E resolver +
    both render-site fixes across all 5 sites (RULE-08), GATE-11/GATE-12 (disarmed by default),
    the production backfill migration, and the carry-forward guard.
  </what-built>
  <how-to-verify>
    Per ROADMAP criterion 4 and this project's standing practice, verify against real production
    data rather than only the fixture suite: pick a live occupied-premises project on
    `rams.21stcav.com`, regenerate its RAMS (Save Review or a full regenerate), and open both the
    PDF and DOCX downloads. Confirm: (1) the CDM 2015 — Duty Holders section states the
    anticipated-sole-contractor position, never "[To be confirmed]" for Principal
    Designer/Principal Contractor; (2) Section 7.0's Nearest A&E row shows either a verified
    name+address+postcode or the exact hold-point line, never "TBC" and never "to be identified
    at site induction"; (3) the Welfare Arrangements First Aid bullet points to Section 7.0
    rather than repeating A&E text. This is a manual step — this phase's code changes have not
    yet been deployed to the VPS; if deploying first is required to verify, deploy per this
    project's standard `git pull` + `php artisan optimize:clear` + `php artisan config:cache` +
    `systemctl restart` (as root, per the standing gotcha) flow before checking.
  </how-to-verify>
  <resume-signal>Confirm "verified live — CDM and A&E both correct on PDF and DOCX" or describe what's wrong.</resume-signal>
  <acceptance_criteria>A real occupied-premises project on rams.21stcav.com regenerates with the anticipated-sole-contractor CDM wording (never "[To be confirmed]" for Principal Designer/Principal Contractor) and either a verified named A&E or the exact hold-point line (never "TBC", never the banned passive string) on both the PDF and DOCX outputs.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Regenerated golden fixtures → future regression tests | A wrongly-regenerated fixture could silently bake in a NEW defect as the "expected" baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-29-06-01 | Tampering | Fixture regeneration silently absorbs an unrelated regression as the new "expected" baseline | mitigate | Task 2 requires a manual diff review scoped to CDM/A&E/Welfare text only before accepting the regenerated fixtures |
</threat_model>

<verification>
`php artisan test --filter=CdmEmergencyDualPathGateTest` green. `php artisan test` and
`vendor/bin/phpunit --group snapshot` both green. Human checkpoint confirms live production
regeneration is correct on both PDF and DOCX.
</verification>

<success_criteria>
The phase is verifiable end-to-end: every automated test is green, the two gates are proven
reachable from real entry points (not just reflection), and a human has confirmed the fix on a
real production document — satisfying ROADMAP criterion 4's explicit "not just a fixture"
requirement.
</success_criteria>

<output>
Create `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-06-SUMMARY.md` when done
</output>
