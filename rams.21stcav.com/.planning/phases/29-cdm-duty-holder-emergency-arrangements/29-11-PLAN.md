---
phase: 29-cdm-duty-holder-emergency-arrangements
plan: 11
type: execute
wave: 2
depends_on: ["29-10"]
files_modified:
  - resources/views/pdf/rams.blade.php
  - resources/views/pdf/rams-v2.blade.php
  - tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php
  - tests/Feature/Rams/CdmContractorNoteRenderRegressionTest.php
autonomous: true
requirements: [RULE-07, RULE-08]
gap_closure: true

must_haves:
  truths:
    - "Rendering a RAMS with a wholly empty site_emergency (every field blank) shows the D-05 hold-point line in Section 7.0's Nearest A&E row on both rams.blade.php and rams-v2.blade.php — never blank, never the literal 'TBC'"
    - "No render of Section 7.0 (on either blade, in any data state) contains the literal substring 'TBC'"
    - "Both blades render the RULE-07 verbatim anticipated-sole-contractor sentence somewhere in the CDM 2015 Duty Holders section, sourced from cdm_duty_holders['contractor_note']"
  artifacts:
    - path: "resources/views/pdf/rams.blade.php"
      provides: "Section 7.0 A&E row rendered unconditionally; CDM section carries the contractor_note paragraph"
    - path: "resources/views/pdf/rams-v2.blade.php"
      provides: "Same two fixes, mirrored"
  key_links:
    - from: "resources/views/pdf/rams.blade.php"
      to: "App\\Services\\Rams\\SiteEmergencyResolver::resolve()"
      via: "unconditional read of $data['site_emergency_resolved']['text'] ?? SiteEmergencyResolver::resolve($siteEmerg)['text'], no longer gated behind $hasSiteEmerg"
      pattern: "site_emergency_resolved"
    - from: "resources/views/pdf/rams.blade.php"
      to: "cdm_duty_holders['contractor_note']"
      via: "new paragraph rendered immediately after the CDM 2015 Duty Holders table"
      pattern: "contractor_note"
---

<objective>
Close 29-UAT.md Gap 2 (the D-05 hold-point line never renders when site_emergency is wholly empty —
the common case) and the blade half of Gap 3 (RULE-07's prescribed sentence never reaches rendered
output) on both PDF render sites: the legacy `rams.blade.php` (the live production path —
`RAMS_UNIFIED_COMPOSER` defaults `false`) and its twin `rams-v2.blade.php` (dormant today, but D-01's
"fix every render site" precedent this phase already established means it must not regress the
moment the composer flag is flipped on).

Purpose: a project with no captured site_emergency data — RAMS 103's actual production state — must
show the compliant D-05 hold-point line, never a blank table and never the literal word "TBC"
anywhere in Section 7.0. The CDM 2015 Duty Holders table must carry the RULE-07 sentence
`RamsComplianceUpgradeService::DEFAULT_CONTRACTOR_NOTE` already computes (Plan 29-10) but has never
been wired into any template.

Output: both blades render the A&E row unconditionally and the contractor_note paragraph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-UAT.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-CONTEXT.md
@.planning/reference/21cav-rams-skill/references/house-rules.md
@app/Services/Rams/SiteEmergencyResolver.php
@tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php

<interfaces>
`SiteEmergencyResolver::resolve(array $siteEmergency): array{verified: bool, text: string}` —
already the single source of the two-branch A&E text; call it, never re-derive.

`RamsComplianceUpgradeService::DEFAULT_CONTRACTOR_NOTE` (new in Plan 29-10) — the RULE-07 verbatim
sentence, for use as a render-side fallback when `$data['cdm_duty_holders']['contractor_note']` is
absent (a document rendered before Plan 29-10's backfill has run).

Current `rams.blade.php` Section 7.0 structure (`:1975-2021`, both blades identical in shape):

  $siteEmerg = (array) ($data['site_emergency'] ?? ($rams->reviewed_data['site_emergency'] ?? []));
  $hasSiteEmerg = ! empty(array_filter($siteEmerg, fn ($v) => is_string($v) ? trim($v) !== '' : ! empty($v)));
  ...
  @if($hasSiteEmerg)
  <table class="emerg-table" ...>
      <tr><!-- Nearest A&E Hospital row, resolver-backed --></tr>
      <tr><!-- Fire Assembly Point, 'TBC' fallback --></tr>
      <tr><!-- Fire Warden / Contact --></tr>
      <tr><!-- First Aider / Contact --></tr>
      <tr><!-- Nearest Defibrillator, 'TBC — confirm at site induction' fallback --></tr>
      <tr><!-- Electrical Isolation Switch, 'TBC — confirm at site induction' fallback --></tr>
      <tr><!-- Fire Extinguisher Class Available, 'TBC — confirm at site induction' fallback --></tr>
  </table>
  @else
  <div ...>TBC AT SITE INDUCTION — MUST BE COMPLETED BEFORE WORKS COMMENCE.<br>...</div>
  @endif

`rams-v2.blade.php` (`:2032-2107`) is the same shape, reading `$emergencyDto->nearestHospitalResolvedText`
/ `$emergencyDto->fireAssemblyPoint` etc. instead of `$siteEmerg[...]`, but the `$hasSiteEmerg` gate and
the `'TBC'`-bearing `@else` banner are structurally identical and must be fixed the same way.

CDM table structure (`rams.blade.php:1808-1848`, `rams-v2.blade.php:1869-1909`, identical): a
`<table class="std-table">` with 4 rows (Client / Principal Designer / Principal Contractor /
Sub-contractor), built from `$cdmMerged` / `$cdmDh = $data['cdm_duty_holders']`. The table has no row
or column for `contractor_note` — it is a `Role | Organisation | Name | Contact` table, and
`contractor_note` is prose, not tabular data.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Section 7.0 A&E row renders unconditionally; reword the empty-state banner</name>
  <files>resources/views/pdf/rams.blade.php, resources/views/pdf/rams-v2.blade.php</files>
  <read_first>
    resources/views/pdf/rams.blade.php:1966-2021 (Section 7.0 block, $hasSiteEmerg gate, TBC banner)
    resources/views/pdf/rams-v2.blade.php:2032-2107 (twin block)
    app/Services/Rams/SiteEmergencyResolver.php (resolve() signature and HOLD_POINT constant — do not
    re-derive its text, only call it)
    tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php:52-77 (OTHER_SITE_EMERGENCY_FIELDS
    fixture and holdPointBranch()/verifiedBranch() helpers — the existing "hold-point state" tests
    always populate these 8 fields, which is exactly why the wholly-empty case escaped detection)
  </read_first>
  <action>
    On `rams.blade.php`: restructure the Section 7.0 block so the `<table class="emerg-table">` tag
    opens unconditionally (not inside `@if($hasSiteEmerg)`). Move the existing "Nearest A&E Hospital"
    `<tr>` (the one reading `$data['site_emergency_resolved']['text'] ?? SiteEmergencyResolver::resolve($siteEmerg)['text']`,
    currently the first row inside the `@if` block) to immediately after the opening `<table>` tag, so
    it always renders regardless of `$hasSiteEmerg` — this is the D-05 "the A&E branch is defined for
    the empty case by design" behaviour the gap requires. Wrap the remaining six `<tr>` rows (Fire
    Assembly Point, Fire Warden, First Aider, Nearest Defibrillator, Electrical Isolation Switch, Fire
    Extinguisher Class Available — none of which has a defined empty-case branch, unlike A&E) inside
    `@if($hasSiteEmerg) ... @endif`, still inside the table. Close the table tag unconditionally.
    Replace the `@else` block's banner text — currently "TBC AT SITE INDUCTION — MUST BE COMPLETED
    BEFORE WORKS COMMENCE." plus a sub-line naming all five fields including "Nearest hospital" — with
    wording that (a) contains no substring "TBC" anywhere, (b) does not restate or contradict the
    hold-point line that now always renders above it, and (c) only refers to the fields that
    genuinely still lack a value: fire assembly point, fire warden, first aider, defibrillator
    location, electrical isolation switch, fire extinguisher class (drop "nearest hospital" from this
    list — it is no longer one of the unconfirmed fields once the hold-point line has rendered). Use
    wording such as "Fire assembly point, fire warden, first aider, defibrillator location, electrical
    isolation switch and fire extinguisher class have not been captured in the review form — confirm
    at site induction." Keep the same visual container (border/background) if useful, but soften from
    the alarming red "MUST BE COMPLETED" framing since the compliant A&E answer is now always present
    above it — an amber informational note is appropriate, not a hard-stop banner. Repeat the
    identical restructuring on `rams-v2.blade.php`, substituting `$emergencyDto->nearestHospitalResolvedText`
    / `$emergencyDto->fireAssemblyPoint` etc. for the legacy `$siteEmerg[...]` reads, keeping the DTO
    field names unchanged.
  </action>
  <acceptance_criteria>
    Rendering either blade with a wholly empty `site_emergency` array (every one of the 9 canonical
    keys blank or absent) produces HTML where: the Nearest A&E Hospital cell contains "to be confirmed
    at induction (must be a 24/7 Emergency Department)"; the string "TBC" does not appear anywhere in
    the rendered Section 7.0 output; the string "to be identified at site induction" (D-05 banned
    string) does not appear. Rendering with all 9 fields populated (the `OTHER_SITE_EMERGENCY_FIELDS`
    fixture plus a verified hospital) is unchanged from current behaviour — the six conditional rows
    still render with their real values, no regression to the already-passing
    `SiteEmergencyRenderSitesRegressionTest` cases.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php</automated>
  </verify>
  <done>Both blades render the A&E row unconditionally, the six other Section 7.0 rows stay conditional, and the empty-state banner contains no "TBC" substring and does not contradict the hold-point line above it.</done>
</task>

<task type="auto">
  <name>Task 2: CDM section renders the RULE-07 contractor_note sentence; new empty-state regression test</name>
  <files>resources/views/pdf/rams.blade.php, resources/views/pdf/rams-v2.blade.php, tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php, tests/Feature/Rams/CdmContractorNoteRenderRegressionTest.php</files>
  <read_first>
    resources/views/pdf/rams.blade.php:1806-1848 (CDM 2015 Duty Holders table, full block, including
    the closing `</table>` this task appends after)
    resources/views/pdf/rams-v2.blade.php:1869-1909 (twin block)
    app/Services/Rams/RamsComplianceUpgradeService.php (DEFAULT_CONTRACTOR_NOTE const, added by Plan
    29-10 — read the actual constant name/value after 29-10 lands, do not assume)
    tests/Unit/Services/Rams/CdmDutyHolderWordingTest.php:71-81 (the exact verbatim string this new
    render must match byte-for-byte)
  </read_first>
  <action>
    On `rams.blade.php`, immediately after the CDM 2015 Duty Holders `</table>` (the closing tag at
    the end of the block read above), add a new paragraph reading the contractor_note value:
    `{{ $data['cdm_duty_holders']['contractor_note'] ?? \App\Services\Rams\RamsComplianceUpgradeService::DEFAULT_CONTRACTOR_NOTE }}`
    styled consistently with the surrounding section (a `<p class="body-para">` or equivalent — match
    whatever class the file's other CDM-adjacent prose paragraphs use, e.g. the notification-style
    paragraph pattern already used elsewhere in this file for permit/exclusion notes). The `??`
    fallback exists for defence-in-depth against a document rendered before Plan 29-10's optional
    backfill has run and whose persisted `cdm_duty_holders` predates the `contractor_note` key
    (mirrors the exact fallback pattern `DocxBuilderService::buildCdmSection()` already uses for
    `principal_designer`/`principal_contractor`). Repeat identically on `rams-v2.blade.php` immediately
    after its CDM table. Add new test methods to
    `tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php`: one asserting a wholly-empty
    `site_emergency` render on `rams.blade.php` shows the hold-point text and never "TBC" (mirrors
    Task 1's acceptance criteria as a committed regression, using a NEW empty array, not
    `OTHER_SITE_EMERGENCY_FIELDS`), and its `rams-v2.blade.php` twin. Create
    `tests/Feature/Rams/CdmContractorNoteRenderRegressionTest.php` with test methods asserting: (1)
    rendering either blade with default `cdm_duty_holders` (built through the real
    `RamsComplianceUpgradeService::upgrade()` pipeline, mirroring
    `SiteEmergencyRenderSitesRegressionTest::renderBothBladesWith()`'s real-pipeline approach — do not
    hand-build a fixture that bypasses `addCdmDutyHolders()`) contains the exact verbatim sentence "21CAV
    is currently anticipated to be the sole contractor for the AV installation scope. The client shall
    confirm whether the overall project involves, or is likely to involve, more than one contractor
    before works commence." on both `rams.blade.php` and `rams-v2.blade.php`; (2) none of the three
    forbidden statements (`standards-and-legislation.md:35-41` — "21CAV is the sole contractor"
    unequivocally, "retains Principal Designer", "Principal Contractor must notify") appear in either
    rendered blade.
  </action>
  <acceptance_criteria>
    Both blades' rendered HTML contains the exact byte-for-byte RULE-07 sentence when
    `cdm_duty_holders` is populated by the real `upgrade()` pipeline. Neither blade's rendered HTML
    contains any of the three forbidden CDM statements. The new empty-`site_emergency` regression test
    methods pass and would have failed against the pre-Task-1 code (i.e. they are genuine presence
    assertions on the hold-point text, not just absence assertions on the banned string).
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Rams/CdmContractorNoteRenderRegressionTest.php tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php</automated>
  </verify>
  <done>Both blades render the verbatim RULE-07 sentence in the CDM section; no forbidden statement appears; new tests lock in the wholly-empty site_emergency case and the contractor_note presence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Engineer review-form input (`reviewed_data['site_emergency']`, `reviewed_data['cdm']`) → rendered PDF | Untrusted-by-default free text reaching a client-facing safety document |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-11-01 | Information Disclosure | Section 7.0 A&E row | accept | No new data surfaced — same `SiteEmergencyResolver::resolve()` call already gated identically elsewhere in the phase; moving it outside `$hasSiteEmerg` does not change what data it reads |
| T-29-11-02 | Tampering | CDM contractor_note paragraph | mitigate | Read-only render of a value computed entirely server-side by `addCdmDutyHolders()`/the Plan 29-10 constant — never user-supplied free text, so no injection surface is introduced |
| T-29-11-03 | Repudiation | Empty-state banner reword | accept | Cosmetic wording change only; no change to what data is captured or who is responsible for capturing it |
</threat_model>

<verification>
`php artisan test tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php tests/Feature/Rams/CdmContractorNoteRenderRegressionTest.php` — all green, including the pre-existing verified/hold-point cases (no regression) and the new wholly-empty-state cases.
</verification>

<success_criteria>
A wholly empty `site_emergency` renders the D-05 hold-point line on both blades, never blank, never
"TBC". The CDM section on both blades carries the verbatim RULE-07 sentence. No forbidden CDM
statement appears anywhere.
</success_criteria>

<output>
Create `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-11-SUMMARY.md` when done
</output>
