---
phase: 29-cdm-duty-holder-emergency-arrangements
plan: 04
type: execute
wave: 3
depends_on: ["29-03"]
files_modified:
  - resources/views/pdf/rams.blade.php
  - resources/views/pdf/rams-v2.blade.php
  - app/Services/DocxBuilderService.php
  - tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php
autonomous: true
requirements: [RULE-08]
must_haves:
  truths:
    - "The banned passive string 'to be identified at site induction' never appears in a rendered PDF (either blade) or DOCX Welfare section again, regardless of RAMS_UNIFIED_COMPOSER state (D-01: fix reaches both legacy and composer paths)"
    - "The literal 'TBC' fallback is gone from both blades' Section 7.0 A&E row (D-07: 'TBC' is not one of the two permitted branches)"
    - "Section 7.0 is the single source of A&E truth — the Welfare First Aid bullet points to it instead of repeating A&E prose (D-07)"
    - "All five render sites read the SAME resolved value from Plan 29-03/29-02 — none re-derives the verified-vs-hold-point branch independently (D-01 anti-pattern)"
  artifacts:
    - path: "resources/views/pdf/rams.blade.php"
      provides: "Welfare bullet pointer + Section 7.0 A&E row both reading $data['site_emergency_resolved']['text']"
    - path: "resources/views/pdf/rams-v2.blade.php"
      provides: "Welfare bullet pointer + Section 7.0 A&E row both reading $emergencyDto->nearestHospitalResolvedText"
    - path: "app/Services/DocxBuilderService.php"
      provides: "buildWelfareArrangements() Welfare bullet pointer, no A&E sentence, no banned string"
  key_links:
    - from: "resources/views/pdf/rams-v2.blade.php"
      to: "app/Support/Rams/Sections/EmergencySectionDto.php"
      via: "reads $emergencyDto->nearestHospitalResolvedText (set in Plan 29-02)"
      pattern: "nearestHospitalResolvedText"
    - from: "resources/views/pdf/rams.blade.php"
      to: "app/Services/Rams/RamsComplianceUpgradeService.php"
      via: "reads $data['site_emergency_resolved']['text'] (set in Plan 29-03's resolveSiteEmergency())"
      pattern: "site_emergency_resolved"
---

<objective>
Fix all five A&E/Welfare render sites RESEARCH.md Finding 3 identifies (two per blade, one in
DocxBuilderService) so the resolved value from Plans 29-02/29-03 is what a real generated
document shows — closing D-01 (all sites, both composer states) and D-07 (Section 7.0 is the
single source of truth, `'TBC'` is banned, the Welfare bullet points rather than repeats).

Purpose: this is the visible, user-facing half of RULE-08 — the resolver and gate built in
29-02/29-03 are inert until a render site actually reads their output instead of its own
hardcoded literal.

Output: both blades and `DocxBuilderService::buildWelfareArrangements()` fixed; a regression
test proving the banned string and `'TBC'` are both structurally gone.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-CONTEXT.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-RESEARCH.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-PATTERNS.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-02-SUMMARY.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-03-SUMMARY.md
</context>

<interfaces>
<!-- Exact current render-site text this plan replaces, extracted from the codebase. -->

Legacy blade `resources/views/pdf/rams.blade.php` — Welfare bullet (`:1960`, identical text in
`rams-v2.blade.php:2021`):
```blade
<li><strong>First Aid:</strong> At least one engineer on site will hold a current First Aid at Work or Emergency First Aid at Work certificate. First aid kit carried at all times. Nearest hospital A&amp;E to be identified at site induction.</li>
```

Legacy blade Section 7.0 A&E row (`:1976-1983`):
```blade
$siteEmerg = (array) ($data['site_emergency'] ?? ($rams->reviewed_data['site_emergency'] ?? []));
...
{{ ($siteEmerg['nearest_hospital'] ?? '') ?: 'TBC' }}
```

v2 blade Section 7.0 A&E row (`:2066`):
```blade
{{ $emergencyDto->nearestHospital ?: 'TBC' }}
```

`app/Services/DocxBuilderService.php::buildWelfareArrangements()` items array (`:2149`):
```php
['First Aid:', 'At least one engineer on site will hold a current First Aid at Work or Emergency First Aid at Work certificate. First aid kit carried at all times. Nearest hospital A&E to be identified at site induction.'],
```

Plan 29-03 established `$data['site_emergency_resolved']` (`['verified' => bool, 'text' =>
string]`) on the legacy-array path (both blades read `$data` — the array passed to the view).
Plan 29-02 established `$emergencyDto->nearestHospitalResolvedText` (string) and
`$emergencyDto->nearestHospitalVerified` (bool) on the composer/DTO path (`rams-v2.blade.php`
reads `$emergencyDto`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Fix both blades — Welfare bullet pointer + Section 7.0 A&E row, both branches</name>
  <files>resources/views/pdf/rams.blade.php, resources/views/pdf/rams-v2.blade.php</files>
  <read_first>
    resources/views/pdf/rams.blade.php:1955-1990 (Welfare Arrangements block through the
    Section 7.0 A&E row — read the surrounding `@php`/`@if($hasSiteEmerg)` structure so the
    replacement fits the existing control flow), resources/views/pdf/rams-v2.blade.php:2015-2070
    (the same block on the v2/composer blade — note it computes `$hospitalAddress` and
    `$hasSiteEmerg` from `$emergencyDto`, not raw `$data`)
  </read_first>
  <action>
    In `resources/views/pdf/rams.blade.php`: replace the Welfare bullet's final sentence "Nearest
    hospital A&amp;E to be identified at site induction." with "Nearest A&amp;E — see Section
    7.0." — keep the certificate/kit sentence before it unchanged. Replace the Section 7.0 A&E
    row's `{{ ($siteEmerg['nearest_hospital'] ?? '') ?: 'TBC' }}` with
    `{{ $data['site_emergency_resolved']['text'] ?? '' }}` — remove the `'TBC'` fallback
    entirely (the resolved key is always set by `resolveSiteEmergency()` in `upgrade()`, so no
    fallback is needed; if `$data` somehow lacks the key on a pre-Phase-29 stale cache, blank
    string is the safe degrade, not `'TBC'`). Leave the `hospital_address` sub-line below it
    unchanged (it is separate, additional detail, not part of the banned-string surface). In
    `resources/views/pdf/rams-v2.blade.php`: apply the identical Welfare bullet fix. Replace
    `{{ $emergencyDto->nearestHospital ?: 'TBC' }}` with
    `{{ $emergencyDto->nearestHospitalResolvedText }}` — no `?:` fallback needed since the DTO
    field always carries either the hold-point line or a verified value, never empty (Plan
    29-02's `EmergencyComposer` always resolves one of the two branches).
  </action>
  <acceptance_criteria>
    - `grep -c "to be identified at site induction" resources/views/pdf/rams.blade.php` returns 0
    - `grep -c "to be identified at site induction" resources/views/pdf/rams-v2.blade.php` returns 0
    - `grep -n "'TBC'" resources/views/pdf/rams.blade.php` shows no match on the A&E row (other
      unrelated `'TBC'` uses elsewhere in the file, if any, are out of this task's scope — only
      the Section 7.0 A&E row is touched)
    - `grep -n "'TBC'" resources/views/pdf/rams-v2.blade.php` shows no match on the A&E row
    - `grep -n "site_emergency_resolved" resources/views/pdf/rams.blade.php` shows the new read
    - `grep -n "nearestHospitalResolvedText" resources/views/pdf/rams-v2.blade.php` shows the new read
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "to be identified at site induction" resources/views/pdf/rams.blade.php resources/views/pdf/rams-v2.blade.php</automated>
  </verify>
  <done>Both blades read the resolved A&E value for both the Welfare pointer and the Section 7.0 row; the banned string and the 'TBC' fallback are gone from both.</done>
</task>

<task type="auto">
  <name>Task 2: Fix DocxBuilderService::buildWelfareArrangements() (D-01 site #5)</name>
  <files>app/Services/DocxBuilderService.php</files>
  <read_first>
    app/Services/DocxBuilderService.php:2130-2165 (buildWelfareArrangements(), the `$items`
    array — confirmed by grep during research this is the ONLY nearest-hospital text anywhere in
    this file; DOCX has no Section 7.0-equivalent A&E table, so no second site needs fixing here)
  </read_first>
  <action>
    In `buildWelfareArrangements()`'s `$items` array, replace the First Aid entry's text "At
    least one engineer on site will hold a current First Aid at Work or Emergency First Aid at
    Work certificate. First aid kit carried at all times. Nearest hospital A&E to be identified
    at site induction." with "At least one engineer on site will hold a current First Aid at
    Work or Emergency First Aid at Work certificate. First aid kit carried at all times. Nearest
    A&E — see CDM 2015 — Duty Holders section." Note: DOCX has no rendered Section 7.0 A&E table
    (confirmed by grep — `nearest_hospital`/`site_emergency` never appear anywhere in
    `DocxBuilderService.php` outside this one bullet), so the DOCX pointer references the
    nearest section that DOES exist in the document rather than a non-existent "Section 7.0" —
    document this discrepancy from the blade wording inline as a one-line comment explaining why
    the DOCX and PDF pointer text differ (PDF has a real Section 7.0 table; DOCX does not).
  </action>
  <acceptance_criteria>
    - `grep -c "to be identified at site induction" app/Services/DocxBuilderService.php` returns 0
    - `grep -n "Nearest A&E" app/Services/DocxBuilderService.php` shows the new pointer text in
      `buildWelfareArrangements()`
  </acceptance_criteria>
  <verify>
    <automated>! grep -q "to be identified at site induction" app/Services/DocxBuilderService.php</automated>
  </verify>
  <done>DOCX Welfare Arrangements section no longer contains the banned A&E sentence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Regression test proving the banned string and 'TBC' are structurally gone</name>
  <files>tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php</files>
  <read_first>
    tests/Feature/Rams/Snapshot/PdfSnapshotTest.php:1-70 (how a real PDF/DOCX render is driven in
    a feature test in this codebase — reuse the same RamsDocument fixture-build approach rather
    than inventing a new one), the two blade files and DocxBuilderService.php as modified by
    Tasks 1-2
  </read_first>
  <behavior>
    - A `RamsDocument` fixture with an empty `reviewed_data['site_emergency']` renders (via the
      real `upgrade()` + blade/DOCX pipeline, not a hand-built fixture) with the exact hold-point
      line present and no `'TBC'`/banned string anywhere in the output.
    - A `RamsDocument` fixture with `nearest_hospital` + `hospital_address` both set renders with
      both values present in the output and no hold-point line, no `'TBC'`, no banned string.
    - This holds under BOTH `config(['rams.unified_composer' => false])` and `true` — the test
      toggles the flag for each assertion, mirroring
      `RamsRegenerateSnapshotsCommand.php:224-231`'s own pattern for proving both paths.
  </behavior>
  <action>
    Create `SiteEmergencyRenderSitesRegressionTest.php` in `tests/Feature/Rams/`. Build a real
    `RamsDocument` with realistic `reviewed_data`/`generated_data` (mirror the minimal fixture
    shape used in `Ffp2ConfinedSpaceDualPathGateTest.php`'s `runFromReview()` example). Run it
    through `RamsComplianceUpgradeService::upgrade()` then render via whatever this codebase's
    PDF-string-render entry point is (locate the render call `PdfSnapshotTest.php` uses — likely
    a `view('pdf.rams', ...)->render()` or the app's PDF service string-output method — and reuse
    it rather than inventing a second one) for the hold-point case and the verified case, under
    each composer state. Assert `assertStringNotContainsString('to be identified at site
    induction', $rendered)` and `assertStringNotContainsString('>TBC<', $rendered)` (or the
    equivalent exact literal `'TBC'` check appropriate to the render output type) on all four
    render combinations (2 data states × 2 composer states). Also build the DOCX path once
    (either composer state — DocxBuilderService's Welfare bullet does not vary by composer
    state) and assert the same banned-string absence on the DOCX XML/text extraction, mirroring
    how `Ffp2ConfinedSpaceGateTest.php`/existing DOCX snapshot tests extract text from a built
    `PhpWord` document for assertion.
  </action>
  <acceptance_criteria>
    - `php artisan test --filter=SiteEmergencyRenderSitesRegressionTest` passes
    - Test covers both `RAMS_UNIFIED_COMPOSER` states explicitly (not just whichever is the
      local `.env` default)
    - Test covers both the hold-point data state and the verified data state
  </acceptance_criteria>
  <verify>
    <automated>php artisan test --filter=SiteEmergencyRenderSitesRegressionTest</automated>
  </verify>
  <done>A real end-to-end render (not a hand-parsed blade snippet) proves the banned string and 'TBC' cannot reach output under either composer state, for both A&E data states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Blade/DOCX template output → PDF/DOCX shipped to a subcontractor or client on a live safety document | Presentation layer only in this plan — no new input parsing; the resolver (29-02) already validated the data, these templates just render its output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-29-04-01 | Tampering | A stale cached `generated_data` blob (pre-Phase-29) reaching a blade without `site_emergency_resolved` set | mitigate | Blade reads default to `?? ''` (blank), never re-introducing `'TBC'` or the banned string as a fallback — blank is the safe degrade until the document's next `upgrade()` pass |
</threat_model>

<verification>
`php artisan test --filter=SiteEmergencyRenderSitesRegressionTest` green. Manual grep across both
blades and `DocxBuilderService.php` confirms zero occurrences of the banned string and zero
`'TBC'` fallbacks on the A&E surface.
</verification>

<success_criteria>
Every render site a real generated document can take reads the same resolved value; the banned
string and the `'TBC'` fallback cannot reach output under either `RAMS_UNIFIED_COMPOSER` state.
</success_criteria>

<output>
Create `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-04-SUMMARY.md` when done
</output>
