---
phase: 29-cdm-duty-holder-emergency-arrangements
plan: 13
type: execute
wave: 3
depends_on: ["29-12"]
files_modified:
  - app/Services/DocxBuilderService.php
  - config/rams_theme.php
  - tests/Unit/Support/Rams/RamsThemeTest.php
  - tests/Feature/Rams/DocxBrandColourRegressionTest.php
autonomous: true
requirements: [RULE-07, RULE-08]
gap_closure: true

must_haves:
  truths:
    - "A DOCX built by DocxBuilderService uses the 21CAV brand palette (#1B7A7A teal, #F4FBFB pale-teal tint, #1A1A2E navy) — never Word's stock 'Blue, Accent 1' defaults (2E74B5/DEEBF7)"
    - "The shared rams_theme.php palette config (the documented single source of truth for both renderers) carries the same corrected brand values, so DocxBuilderServiceV2 and rams-v2.blade.php cannot reintroduce the Word-blue defect the moment RAMS_UNIFIED_COMPOSER is flipped on"
  artifacts:
    - path: "app/Services/DocxBuilderService.php"
      provides: "TEAL/ROW_ALT/DARK_GREY constants corrected to brand hex, with comments removing the stale 260725-rd1 'brand blue' rationale"
    - path: "config/rams_theme.php"
      provides: "brand_blue/brand_blue_dark/brand_blue_tint/alt_row/dark_text palette keys corrected to brand hex"
  key_links: []
---

<objective>
Close 29-UAT.md Gap 4 (the DOCX renders Microsoft Word's stock "Blue, Accent 1" palette — `2E74B5`
headings, `DEEBF7` alt-row shading, `333333` dark text — instead of 21CAV's brand palette that the
PDF already correctly uses: `#1B7A7A` teal, `#F4FBFB` pale-teal tint, `#1A1A2E` navy).

Sequenced after Plan 29-12 (both touch `app/Services/DocxBuilderService.php`) rather than combined
with it, to keep each gap-closure plan to 2 tasks and avoid a same-wave file conflict.

`app/Services/DocxBuilderService.php`'s own `260725-rd1` code comment (`:44-49`) records a deliberate
2026-07-25 decision to shift FROM teal TO "brand blue" to match a hand-crafted reference document —
that decision is what produced today's defect; per this plan's scope, the 2026-09-12 UAT finding and
the user's explicit report ("word version is blue and not to brand ... PDF is to brand colours so
fine") supersede it. This plan does not delete the historical comment's factual record, but corrects
the constant values and removes the now-inaccurate "this IS the brand" framing.

Purpose: the DOCX and PDF must visually match — both are the same document in a different container.

Output: corrected colour constants in both the DOCX renderer and the shared theme config it does not
yet consume (closing the divergence risk for when it eventually does).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-UAT.md
@app/Support/Rams/RamsTheme.php

<interfaces>
Current constants, `app/Services/DocxBuilderService.php:44-61`:

  private const TEAL             = '2E74B5';   // (was 007B8A — now brand blue; kept for compat)
  private const BRAND_BLUE       = '2E74B5';   // H1/H2 headings + accents          (0 usages — dead)
  private const BRAND_BLUE_DARK  = '1F4D78';   // H3 sub-headings                    (0 usages — dead)
  private const BRAND_BLUE_TINT  = 'DEEBF7';   // Alt-row shading (very light blue) (0 usages — dead)
  private const DARK_GREY        = '333333';
  private const MID_GREY         = '666666';
  private const ROW_ALT          = 'DEEBF7';   // was F0FBFC (light teal) — 260725-rd1
  private const WHITE            = 'FFFFFF';

`TEAL` (34 usages), `ROW_ALT` (15 usages), `DARK_GREY` (7 usages) are the only three of these
actually referenced anywhere in the file. `BRAND_BLUE`/`BRAND_BLUE_DARK`/`BRAND_BLUE_TINT` are
declared but never read (`grep -c "self::BRAND_BLUE\b"` / `self::BRAND_BLUE_TINT\b` both return 0).

PDF's actual brand palette, `resources/views/pdf/rams.blade.php` CSS block: `color: #1A1A2E;` (body
text/headings), `color: #1B7A7A;` (section headings, table header backgrounds), `background-color:
#F4FBFB;` (alt-row shading, table tint).

Current `config/rams_theme.php:41-56` palette block:

  'brand_blue'      => '2E74B5',
  'brand_blue_dark' => '1F4D78',
  'brand_blue_tint' => 'DEEBF7',
  'alt_row'         => 'DEEBF7',
  'white'           => 'FFFFFF',
  'dark_text'       => '333333',
  'text_muted'      => '666666',
  ...

Consumed directly (not through DocxBuilderService.php's separate constants) by
`app/Services/DocxBuilderServiceV2.php:191,193,341` (`$theme->palette('brand_blue')`,
`$theme->palette('dark_text')`) and by `RamsTheme::paletteCss()`, which `rams-v2.blade.php` emits as
CSS custom properties — both dormant today (`RAMS_UNIFIED_COMPOSER` defaults `false`) but not fixing
them leaves the exact same defect latent for the day that flag flips.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct brand colour constants in DocxBuilderService.php + rams_theme.php config</name>
  <files>app/Services/DocxBuilderService.php, config/rams_theme.php, tests/Unit/Support/Rams/RamsThemeTest.php</files>
  <read_first>
    app/Services/DocxBuilderService.php:36-62 (constant block + 260725-rd1 comment, full context)
    config/rams_theme.php:20-56 (palette block, full context including doc comment claiming this file
    is "the single source of truth ... any change here reflows both renderers")
    tests/Unit/Support/Rams/RamsThemeTest.php:23-29 (hardcodes '2E74B5'/'DEEBF7' as expected — must be
    updated or this task breaks a passing test)
    resources/views/pdf/rams.blade.php:1-280 (CSS colour block — the canonical brand values to copy)
  </read_first>
  <action>
    In `app/Services/DocxBuilderService.php`: change `TEAL` from `'2E74B5'` to `'1B7A7A'`; change
    `ROW_ALT` from `'DEEBF7'` to `'F4FBFB'`; change `DARK_GREY` from `'333333'` to `'1A1A2E'`. Update
    each constant's trailing comment to state the corrected brand meaning (e.g. "21CAV brand teal —
    corrected 2026-09-12, see 29-UAT.md gap 4" for `TEAL`) and remove the now-inaccurate claim in the
    `260725-rd1` block comment (`:44-49`) that `2E74B5` IS the brand colour — replace it with a note
    that the 2026-07-25 "brand blue" shift was itself the defect the 2026-09-12 UAT found, corrected
    here to match the PDF's actual `#1B7A7A`/`#F4FBFB`/`#1A1A2E` palette. Delete the three dead
    constants `BRAND_BLUE`, `BRAND_BLUE_DARK`, `BRAND_BLUE_TINT` entirely (zero usages confirmed by
    grep — leaving them would let a future caller accidentally reintroduce the wrong hex under a
    plausible-sounding name). Do not rename `TEAL`/`ROW_ALT`/`DARK_GREY` — renaming would force a
    56-call-site diff for no behavioural benefit; the corrected VALUE is what matters.

    In `config/rams_theme.php`: change `'brand_blue' => '2E74B5'` to `'brand_blue' => '1B7A7A'`;
    change `'brand_blue_dark' => '1F4D78'` to `'brand_blue_dark' => '1A1A2E'` (there is no
    intermediate brand shade in the PDF's actual palette — collapse the "dark" variant to the same
    navy used for dark headers/emphasis elsewhere, matching this plan's "dark headers→1A1A2E"
    instruction); change `'brand_blue_tint' => 'DEEBF7'` to `'brand_blue_tint' => 'F4FBFB'`; change
    `'alt_row' => 'DEEBF7'` to `'alt_row' => 'F4FBFB'`; change `'dark_text' => '333333'` to
    `'dark_text' => '1A1A2E'`. Update each line's trailing comment to match. Do not touch any other
    palette key (`white`, `text_muted`, `border`, the risk-band colours) — those are unrelated to this
    gap.

    Update `tests/Unit/Support/Rams/RamsThemeTest.php::test_palette_returns_hex_for_known_key()`
    (`:23-29`) to assert the corrected values: `assertSame('1B7A7A', $theme->palette('brand_blue'))`
    and `assertSame('F4FBFB', $theme->palette('alt_row'))`, replacing the current
    `'2E74B5'`/`'DEEBF7'` assertions. Leave `test_from_config_builds_from_raw_array()` (`:98-113`)
    unchanged — it constructs its own literal `'ABCDEF'` fixture and does not depend on the real
    config values.
  </action>
  <acceptance_criteria>
    `grep -n "2E74B5\|DEEBF7\|1F4D78\|333333" app/Services/DocxBuilderService.php config/rams_theme.php`
    returns zero matches (confirms every Office-default/stale hex is gone from both files — note
    `333333` must also be fully gone, not just the three explicitly-named-in-UAT hexes, since it was
    `DARK_GREY`'s value). `grep -n "BRAND_BLUE\b\|BRAND_BLUE_DARK\|BRAND_BLUE_TINT"
    app/Services/DocxBuilderService.php` returns zero matches (dead constants removed). `php artisan
    test tests/Unit/Support/Rams/RamsThemeTest.php` passes with the corrected assertions.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Unit/Support/Rams/RamsThemeTest.php</automated>
  </verify>
  <done>TEAL/ROW_ALT/DARK_GREY corrected to brand hex; dead BRAND_BLUE_* constants removed; rams_theme.php palette corrected to match; RamsThemeTest updated and green.</done>
</task>

<task type="auto">
  <name>Task 2: Regression test proving no Office-default hex survives a real DOCX build</name>
  <files>tests/Feature/Rams/DocxBrandColourRegressionTest.php</files>
  <read_first>
    tests/Feature/Rams/SiteEmergencyRenderSitesRegressionTest.php:359-402
    (test_docx_welfare_bullet_never_contains_banned_string — the established pattern for building a
    real DOCX via DocxBuilderService and inspecting its extracted word/document.xml; reuse this
    fixture-build shape)
    app/Services/DocxBuilderService.php (post-Task-1 constant block, to confirm the exact corrected
    hex values this test asserts for)
  </read_first>
  <action>
    Create `tests/Feature/Rams/DocxBrandColourRegressionTest.php` with a test that builds a real DOCX
    via `app(DocxBuilderService::class)->build(...)` against a persisted `RamsDocument` fixture
    carrying enough data to exercise every major section (project/team/hazards/method_statement,
    `cdm_duty_holders`, `site_emergency` — reuse or closely mirror the fixture shape from
    `SiteEmergencyRenderSitesRegressionTest::test_docx_welfare_bullet_never_contains_banned_string()`),
    extracts `word/document.xml` from the resulting zip, and asserts: `assertStringNotContainsString('2E74B5', $xml)`,
    `assertStringNotContainsString('DEEBF7', $xml)`, `assertStringNotContainsString('1F4D78', $xml)`,
    `assertStringNotContainsString('333333', $xml)` — none of the four stale/Office-default hex values
    survive a real build. Add positive assertions too:
    `assertStringContainsString('1B7A7A', $xml)` and `assertStringContainsString('F4FBFB', $xml)`
    (confirms the brand colours actually reached the rendered XML, not just that the old ones are
    gone — a build that silently dropped all colour attributes would wrongly pass a negative-only
    assertion).
  </action>
  <acceptance_criteria>
    Test builds a real DOCX (not a hand-parsed constant check) and passes all 6 assertions (4
    negative, 2 positive). Running this test against the pre-Task-1 code (mentally, or by a
    stash/revert-and-restore check during development per the project's established non-vacuity
    discipline) would fail on the `2E74B5`/`DEEBF7` negative assertions.
  </acceptance_criteria>
  <verify>
    <automated>php artisan test tests/Feature/Rams/DocxBrandColourRegressionTest.php</automated>
  </verify>
  <done>New regression test proves a real DOCX build carries only the corrected brand hex, never the Office-default/stale values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Pure presentation/styling change — no data flow, trust boundary, or input surface is touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-13-01 | Tampering | Constant/config value swap | accept | Cosmetic hex-literal change only; no logic branch, no user input, no injection surface |
</threat_model>

<verification>
`php artisan test tests/Unit/Support/Rams/RamsThemeTest.php tests/Feature/Rams/DocxBrandColourRegressionTest.php`
— both green. Full suite spot-check: `php artisan test tests/Unit/Support/Rams tests/Feature/Rams`
still green (no other test hardcodes the old DOCX hex values — confirmed by the same grep run in
Task 1's acceptance criteria scoped to `tests/`).
</verification>

<success_criteria>
DOCX renders `#1B7A7A`/`#F4FBFB`/`#1A1A2E`, matching the PDF, everywhere `TEAL`/`ROW_ALT`/`DARK_GREY`
are used. `rams_theme.php`'s palette matches. No test hardcodes the old Word-default hex.
</success_criteria>

<output>
Create `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-13-SUMMARY.md` when done
</output>
