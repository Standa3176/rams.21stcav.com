---
phase: 29-cdm-duty-holder-emergency-arrangements
plan: 07
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - app/Services/Rams/SiteEmergencyResolver.php
  - tests/Unit/Services/Rams/SiteEmergencyResolverTest.php
autonomous: true
requirements: [RULE-08, GATE-12]
must_haves:
  truths:
    - "SiteEmergencyResolver::classify() returns null (clean) when nearest_hospital is exactly the HOLD_POINT literal, matching the class's own documented D-08 conservative-by-construction guarantee"
    - "GATE-12 never flags its own sanctioned hold-point output as a defect the moment RAMS_CDM_AE_GATE is armed"
  artifacts:
    - path: "app/Services/Rams/SiteEmergencyResolver.php"
      provides: "classify() early clean-return covers both the blank name and the HOLD_POINT literal"
  key_links:
    - from: "app/Services/Rams/SiteEmergencyResolver.php"
      to: "app/Services/Rams/RamsComplianceUpgradeService.php::enforceEmergencyGate()"
      via: "classify() called with the same siteEmergency array that resolve() already wrote the HOLD_POINT text into"
      pattern: "classify\\("
---

<objective>
Close VERIFICATION.md gap 1 / REVIEW.md CR-01: `SiteEmergencyResolver::classify()` only
short-circuits clean on a BLANK `nearest_hospital`. If `nearest_hospital` contains the exact
HOLD_POINT sentence — which is realistic, since that sentence is printed verbatim on every
generated PDF/DOCX and a PM can copy-paste it back into the review form — `classify()` falls
through to the address check and wrongly returns `'missing_address_or_postcode'` instead of
`null`. This misfires GATE-12 the moment `RAMS_CDM_AE_GATE` is armed, which is the explicitly
planned next deploy step.

Purpose: Widen the clean path (D-01/D-08 conservative-by-construction — never narrow it) so
GATE-12 never flags the app's own sanctioned wording as a defect.
Output: `classify()` fixed; a regression test proving the literal HOLD_POINT string passes clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-CONTEXT.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-VERIFICATION.md
@.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-REVIEW.md
</context>

<interfaces>
<!-- Current classify() early-return block, app/Services/Rams/SiteEmergencyResolver.php:99-108 -->
Current code:
```
public static function classify(array $siteEmergency): ?string
{
    $rawName = (string) ($siteEmergency['nearest_hospital'] ?? '');
    $name = trim($rawName);

    if ($name === '') {
        return null;
    }
    ...
```
`self::HOLD_POINT` (private const, same class, line ~35) holds the exact verbatim string:
`'Nearest A&E — to be confirmed at induction (must be a 24/7 Emergency Department)'`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen classify()'s clean-return to cover the HOLD_POINT literal</name>
  <files>app/Services/Rams/SiteEmergencyResolver.php, tests/Unit/Services/Rams/SiteEmergencyResolverTest.php</files>
  <read_first>
    app/Services/Rams/SiteEmergencyResolver.php (whole file — classify() at :99-137, HOLD_POINT const at :35, resolve() at :73-84)
    tests/Unit/Services/Rams/SiteEmergencyResolverTest.php (existing test shapes — test_classify_passes_hold_point_clean at ~:56 only covers the BLANK case today)
  </read_first>
  <behavior>
    - Test: `classify(['nearest_hospital' => self::HOLD_POINT, 'hospital_address' => ''])` returns `null` (the exact CR-01 repro from 29-VERIFICATION.md).
    - Test: `classify(['nearest_hospital' => self::HOLD_POINT, 'hospital_address' => 'some address'])` also returns `null` — the HOLD_POINT literal is clean regardless of what (if anything) is in `hospital_address`, since a PM could paste it into either field independently and the class must never narrow the clean path based on unrelated fields.
    - Existing tests (blank-name clean, banned_string, urgent_care_keyword variants, missing_address_or_postcode for a genuine named hospital with no address) must keep passing unchanged — this is an ADDITIVE widening, not a behavior change for any other input.
  </behavior>
  <action>
    In `classify()`, change the early clean-return check at the top (currently `if ($name === '') { return null; }`) to also return `null` when `$name === self::HOLD_POINT`. Do this as an early check before the `banned_string`/`urgent_care_keyword`/`missing_address_or_postcode` checks — mirror the existing early-return's placement and its inline comment style (the current comment references D-08 conservative-by-construction; extend that comment to name both cases it now covers: blank name and the exact HOLD_POINT sentence). Use `$name` (trimmed), not `$rawName`, for the comparison, matching how the rest of the method already treats `$name`. Do not touch `resolve()` — it already treats the HOLD_POINT text correctly since it never reads `classify()`'s output. Do not alter `URGENT_CARE_KEYWORDS`, `BANNED_STRING`, or any other branch.
  </action>
  <acceptance_criteria>
    - `grep -n "self::HOLD_POINT" app/Services/Rams/SiteEmergencyResolver.php` shows the constant referenced inside `classify()`, not only inside the class docblock and `resolve()`.
    - `SiteEmergencyResolver::classify(['nearest_hospital' => 'Nearest A&E — to be confirmed at induction (must be a 24/7 Emergency Department)', 'hospital_address' => ''])` returns `null`.
    - The new test method(s) in `SiteEmergencyResolverTest.php` assert this via `assertNull(...)`, not a loose truthy check.
    - `php artisan test tests/Unit/Services/Rams/SiteEmergencyResolverTest.php` — all tests pass, including the pre-existing `test_classify_passes_hold_point_clean` and every `urgentCareKeywordProvider`/`missing_address_or_postcode` case.
  </acceptance_criteria>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com" && php artisan test tests/Unit/Services/Rams/SiteEmergencyResolverTest.php</automated>
  </verify>
  <done>classify() returns null for the literal HOLD_POINT sentence in nearest_hospital regardless of hospital_address content; all pre-existing classify()/resolve() tests remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewer-form input → `SiteEmergencyResolver::classify()` | PM/engineer free text (`nearest_hospital`) reaches the plausibility gate that decides whether generated output is auto-flagged/thrown on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-07-01 | Tampering | `classify()` false-positive on sanctioned output | mitigate | This plan's fix — widen the clean-return to the HOLD_POINT literal so a legitimate PM-authored value is never rewritten by GATE-12's auto-correct path |
| T-29-07-02 | Information Disclosure | N/A — no external service, no PII beyond text already visible in the document | accept | Out of scope; classify() only inspects text already destined for the printed document |
</threat_model>

<verification>
`php artisan test tests/Unit/Services/Rams/SiteEmergencyResolverTest.php` — all pass, including
two new HOLD_POINT-literal cases. No other test file should reference `classify()`'s internals,
so no cross-file regression risk.
</verification>

<success_criteria>
`classify()` never flags its own sanctioned HOLD_POINT output as a defect, under any
`hospital_address` value. Conservative-by-construction clean path is widened, never narrowed —
every other classify() behavior (banned_string, urgent_care_keyword, missing_address_or_postcode
for a genuinely named hospital) is unchanged.
</success_criteria>

<output>
Create `.planning/phases/29-cdm-duty-holder-emergency-arrangements/29-07-SUMMARY.md` when done
</output>
