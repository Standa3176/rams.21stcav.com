---
gsd_state_version: 1.0
milestone: v2.0
milestone_name: Engineering-Grade AV Drawings
status: executing
stopped_at: Phase 27 context gathered
last_updated: "2026-08-25T17:58:49.644Z"
last_activity: 2026-08-24 -- Phase 26 Plan 08 complete (gap closure round 2)
progress:
  total_phases: 9
  completed_phases: 7
  total_plans: 39
  completed_plans: 38
  percent: 78
---

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-30)

**Core value:** One dataset powers every document.
**Current focus:** v2.0 drawings core (Phases 17–23) COMPLETE on disk. Milestone paused with two phases remaining: Phase 24 (Stencil Curation UI + Quote-Import Auto-Stub) and Phase 25 (AI Assist + Replacement Wiring). Recent real work has been on out-of-milestone RAMS-ops tracks (QuoteWerks import, RAMS DOCX parity, worksheet) through 2026-07-26.

## Current Position

Milestone: v2.0 (Engineering-Grade AV Drawings) — IN PROGRESS. Phases 17, 18, 20, 21, 22, 22.1, 23 all COMPLETE on disk; Phase 24 IN PROGRESS (Plans 01-08 of the original 9 landed; UAT on that output found 2 gaps, spawning 3 gap-closure plans 24-10/11/12 — all 3 now landed, plus original Plan 24-09 still open); Phase 25 remains unplanned. (Phase 999.1 = deferred backlog.)
Active work: Phase 24 (Stencil Curation UI + Quote-Import Auto-Stub) — gap-closure Plan 24-12 (corrected CONTEXT.md's D-11 and D-17 decision text to match the code fixed by 24-10/24-11, docs-only) shipped 2026-08-14. All 3 UAT gap-closure plans are now complete. Next up: original Plan 24-09 (bounded top-10 curation, human-checkpoint, `autonomous: no`, out of autonomous-executor scope by design) — the only remaining Phase 24 plan.
Reconciliation: 2026-08-09 — STATE synced to disk truth. Earlier STATE claimed "PAUSED at Phase 23 sub-plan 1", but Phase 23 shipped 7/7 plans on 2026-05-15 (commit 23-07). ROADMAP checkboxes for Phases 22 + 23 corrected to [x] in the same pass.
Outstanding: 27 human-UAT items across 5 VERIFICATION.md files (Phases 17, 18, 20, 22, 22.1) remain `human_needed` — automated must-haves all passed; manual browser/prod sign-off pending. Run `/gsd-verify-work <phase>` to clear.
Phase 26 (Hazard Library Structural Inversion, v1.4 track): Plan 26-01 LANDED 2026-08-24 — `hazard_templates` gained `include_when`, seeded with the full 18-hazard 21cav-rams skill library (replacing the old 13), Working at Height residual now `1x4` (HAZ-03), orphan-row cleanup scoped strictly to `is_global=true`. Local SQLite migrated + seeded (18 rows, 0 null `include_when`, 0 `is_global=false` rows affected). 3 commits (`90dffca`, `10f26f0`, `8084ddb`). Rule 1 fix: `HazardTemplateController::store()`/`update()` now null-coalesce `description`. Not deployed to live — Plan 26-06 owns that checkpoint. See `.planning/phases/26-hazard-library-structural-inversion/26-01-SUMMARY.md`.
Plan 26-02 LANDED 2026-08-24 — standalone `HazardIncludeWhenResolver` (`app/Services/Rams/HazardIncludeWhenResolver.php`) implementing D-05's tiered include-when evaluation: `always` unconditional, `signal:<key>` deterministic in-or-out (activity/keyword/drilling match, miss = dropped entirely), `confirm:<key>` ALWAYS returned + ALWAYS flagged `needs_confirmation=true` on every job (2026-08-23 D-05 correction — no AI call at any tier, keyword hits only set a `pre_ticked` UI-ordering hint, never auto-confirm/exclude). `include_when=null` never returned (D-04). 8/8 unit tests pass (52 assertions), no DB dependency. 2 commits (`50048fe`, `70ad97d`). Pure logic — NOT wired into the RAMS pipeline yet (Plan 26-04 does that), so `REQUIREMENTS.md`'s HAZ-02 checkbox is deliberately left `[ ]` Pending despite this plan's frontmatter listing it (the requirement describes end-to-end register behaviour, not yet true until 26-04 lands). See `.planning/phases/26-hazard-library-structural-inversion/26-02-SUMMARY.md`.
Plan 26-03 LANDED 2026-08-24 — deleted `config('rams_tier1.baseline_hazards')` (the fixed 11-hazard array) entirely and killed all 4 render/build-time re-injection paths: `Tier1RamsDefaultsService::injectDefaultsIntoRamsData()` no longer touches `$data['hazards']`; `RiskAssessmentComposer::compose()`'s config-fallback chain removed; `resources/views/pdf/rams.blade.php` — **the LIVE PDF template** — no longer re-injects the fixed 11 at render time (the existing "No hazards identified." else-branch already handled the true-empty case, unchanged); `resources/views/pdf/rams-v2.blade.php` (not live) had the identical block removed too. New independent `config('rams_tier1.hazard_tiering_enabled')` flag added (`env('RAMS_HAZARD_LIBRARY_TIERING', true)`), decoupled from the existing `rams_tier1.enabled` (which still gates `standards_references`/`coshh_products`, untouched) — this is Plan 26-04's wiring point and the one-`.env`-edit rollback the phase's live-validation constraint requires. `Tier1BaselineHazardsRenderTest` rewritten to assert the OPPOSITE of its pre-phase behaviour (empty stays empty, old baseline titles absent). Rule 1 fix: 2 further pre-existing tests (`Tier1RamsDefaultsServiceTest`, `RamsDocumentComposerTest`) that directly asserted the old injection behaviour were broken by this plan's own changes and rewritten to match. 4 commits (`3e3a3fd`, `4914b54`, `4998220`, `0af8cfa`). `--filter=Rams` full namespace: 461 passed, 0 failed. Global `grep -rn "baseline_hazards" config/ app/ resources/` returns 0 matches. See `.planning/phases/26-hazard-library-structural-inversion/26-03-SUMMARY.md`. Next: Plan 26-04 (wire `HazardIncludeWhenResolver` into the live RAMS pipeline).
Plan 26-04 LANDED 2026-08-24 (KEYSTONE — HAZ-02 now genuinely satisfied end-to-end) — deleted `HazardLibraryService`'s last unconditional injection path (`MANDATORY_KEYWORDS`/`mandatoryBaseline()`/`mergeWithMandatory()`; `resolveByIds()`/`resolveFromSeeds()` simplified to 2-parameter signatures; `fuzzyMatch()` preserved, confirmed NOT a tier-3 AI plug-in). `RiskTemplateResolverService` wired to Plan 26-02's `HazardIncludeWhenResolver`: `resolveHazards()` always resolves explicit engineer picks first, then (when `hazard_tiering_enabled` is true) merges tiered matches deduplicated by id/name — flag off degrades to explicit-picks-only, never resurrects the old baseline. Both live call sites — `RamsExtractionDraftBuilderService::build()` (quote-import auto-seed) and `RamsBuilderService::runPipeline()` (manual form + regenerate) — now build and forward a `scope_narrative` signal string (works_summary + works_description + equipment descriptions) for tier-2 keyword matching. `RamsExtractionDraftBuilderService::buildHazards()` emits numeric pre/post likelihood+severity + `needs_confirmation` + `score_reviewed` instead of a Low/Medium/High label (`riskLabel()` removed). Verified end-to-end: a blank-signal quote-import resolves to the 4 always-tier + 5 always-confirm-tier hazards (9 total, never the old baseline); ceiling-mounted-display scope text correctly pulls in Working at height / Manual handling / Restricted access and ceiling voids. Deviation: corrected an inaccuracy in the plan's own Task 2 test description and `must_haves.truths` — "exactly the 4 always-tier hazards" for a blank signal set omits the 5 tier-3 confirm hazards, which CONTEXT.md's binding 2026-08-23 correction says always surface ("up to 5 confirmation rows per job... accepted cost"); implementation follows the correct, already-locked 26-02 design, not the plan's imprecise restatement. 4 commits (`28039e9`, `182e9d6`, `ce5fdc9`, `bd440ab`). `--filter=Rams`: 466 passed, 0 failed. Full suite: 2251 passed, 1 failed (pre-existing `QueueRecoverCommandTest` memory-limit flake, documented, unrelated, not chased). `HazardInjectionPathsRemovedGuardTest` proves zero references to the deleted machinery remain anywhere. `RamsReviewDataService::normaliseHazards()`'s 4-key schema whitelist NOT extended — new score keys will be dropped on the review screen until Plan 26-05 (HAZ-04) extends it; deliberately out of this plan's scope. See `.planning/phases/26-hazard-library-structural-inversion/26-04-SUMMARY.md`. Next: Plan 26-05 (HAZ-04 — editable score defaults + review-screen surfacing of needs_confirmation).
Plan 26-05 LANDED 2026-08-24 (HAZ-04 closed — Phase 26 feature-complete pending live deploy) — `RamsReviewDataService::normaliseHazards()` schema extended: `risk` (Low/Medium/High) replaced with 4 independently-clamped numeric fields (`pre_likelihood`/`pre_severity`/`post_likelihood`/`post_severity`, `max(1, min(5, (int) $v))`) plus `score_reviewed`/`needs_confirmation` booleans; legacy risk-only rows still normalise correctly via a per-field High/Medium/Low bucket fallback (no data migration needed). `RamsBuilderService::reviewedToRisk()` now treats an already-numeric row score as authoritative — the hazard-library re-lookup only fills a genuinely missing slot, never overwrites a value the row supplied — and carries `score_reviewed`/`needs_confirmation` into `generated_data['hazards'][*]`. `quote-review.blade.php`'s hazard row (static + JS "+ Add Row" template, kept in sync) replaces the Low/Medium/High select with 4 editable `<input type="number" min="1" max="5">` fields, a hidden `score_reviewed` marker that flips to `1` client-side on any edit (`markHazardReviewed()`), and a `.badge-needs-confirmation` + `.hazard-needs-confirmation` visual flag for D-06-flagged rows. **Deviation (Rule 3, file not in plan's files_modified):** found and fixed `RamsReviewController::parseReviewPayload()` — the Save/Approve POST path builds `reviewed_data` via its OWN independent hazard parser, completely bypassing `RamsReviewDataService::normalise()`; it was still whitelisting only `activity_key`/`hazard`/`risk`/`control_measures`, which would have silently discarded every new field (including the engineer's own typed scores) on the very first save — the exact failure HAZ-04 exists to prevent, on the one path the plan's `<interfaces>` section didn't name. Fixed by delegating to the same normaliser the GET path uses; also added a hidden `needs_confirmation` round-trip field so a plain Save doesn't silently clear the D-06 flag. 5 commits (`56e7c8a`, `45f4260`, `4a04ca4`, `28ac91e`, `9efda43`). `--filter=Rams`: 475 passed, 0 failed (was 466). Full suite: 2260 passed, 1 failed (same documented pre-existing `QueueRecoverCommandTest` flake, not chased). See `.planning/phases/26-hazard-library-structural-inversion/26-05-SUMMARY.md`. Next: Plan 26-06 (live deploy — 2-week parallel run per CONTEXT.md's `<specifics>`, spot-check job 21CQ30960 VW Blakelands).
Plan 26-06 Tasks 1-2 LANDED 2026-08-24 (Task 3 is a blocking human checkpoint, deliberately NOT run this session) — proved HAZ-02/HAZ-03 through the live DOCX renderer, not a fixture or the non-live PDF composer. `MethodStatementAssociatedRisksTest` gained an 8-hazard non-sequential-id fixture + a new test proving `RA{NN}` refs still resolve 1:1 at that register size, coexisting with the original 3-hazard guard. New `HazardTemplateSeederIdempotencyTest` (belt-and-braces, distinct from Plan 26-01's inline coverage): seeds twice, asserts 18 global rows and a non-null `include_when` on every row after the second run. New `WorkingAtHeightResidualScoreTest`: built through `RiskTemplateResolverService::resolve()` (the live pipeline entry point) and read back out of a real generated `.docx` via `ZipArchive`/`word/document.xml` — Test 1 confirms a blank scope resolves to 9 rows (4 always-tier + 5 confirm-tier, per D-06 — corrected the plan's own literal "exactly 4" wording, matching 26-04-SUMMARY.md's prior correction of the same inaccuracy) and never resurrects the old fixed-11 baseline titles; Test 2 confirms "Working at height" renders the exact string `1×4=4` in the actual document.xml (read directly from `DocxBuilderService::buildRiskAssessment():1274`, not guessed). 2 commits (`f88d6fb`, `661b4cb`) + 1 docs commit (`11dc9bc`). `--filter=MethodStatementAssociatedRisksTest`: 5/5. `--filter=HazardTemplateSeederIdempotencyTest`: 2/2. `--filter=WorkingAtHeightResidualScoreTest`: 2/2. Full suite: 2265 passed, 1 failed (same documented pre-existing `QueueRecoverCommandTest` flake, not chased). HAZ-01..04 were already `[x]` Complete in `REQUIREMENTS.md` from Plans 26-01/26-03/26-04/26-05 — this plan adds proof depth, flips no new checkbox. `gsd-sdk query roadmap.update-plan-progress 26` incorrectly marked 26-06 `[x]` and phase status "Complete" from summary-count alone (it cannot see the internal Task 3 checkpoint gate) — manually corrected both `ROADMAP.md` lines back to reflect the open human checkpoint; do not trust that command's output for phases ending in a `checkpoint:human-verify` task. See `.planning/phases/26-hazard-library-structural-inversion/26-06-SUMMARY.md`. **Next: Task 3 — deploy Plans 26-01..26-05 to `rams.21stcav.com` as `stcav` (never root), `git pull` + `php artisan migrate --force` + `php artisan db:seed --class=HazardTemplateSeeder --force` + clear caches, regenerate project 21CQ30960 (VW Blakelands), manually compare its DOCX hazard register against the source quote, confirm Working at height reads 1x4, then toggle `RAMS_HAZARD_LIBRARY_TIERING=false` -> regenerate -> confirm no baseline resurrection -> toggle back true/removed -> regenerate -> confirm normal behaviour returns. Type "approved" once done, per 26-06-PLAN.md Task 3's `<resume-signal>`.**
Plan 26-07 LANDED 2026-08-24 (gap closure — HAZ-02 now genuinely satisfied on BOTH generation entry points) — live verification (`26-VERIFICATION.md`) found `RamsBuilderService::runFromReview()` (the path every already-reviewed project takes) never wired the tiered `HazardIncludeWhenResolver`, and traced a previously-undocumented SIXTH hazard-injection path (`RamsComplianceUpgradeService::addProjectSpecificRisks()`) as the cause of the unexplained 7→11 hazard-count delta on live (21CQ30960 / RAMS 96). `RiskTemplateResolverService` gained a public `tieredRowsNotAlreadyPresent()` entry point (extracted `fetchTieredCandidates()` shared by both callers, so tier logic can't diverge). `RamsBuilderService::reviewedToRisk()` now merges tier-1/3 candidates onto reviewed picks, forwarding a REAL derived drilling signal (`EquipmentClassifierService::textIndicatesDrilling()` against the reviewed scope narrative, reusing the existing `MOUNT_KEYWORDS` vocabulary — not a second keyword list) instead of a hardcoded `false`; ids re-sequenced 1..N across the merged register in a single pass. `addProjectSpecificRisks()` gated behind the same `rams_tier1.hazard_tiering_enabled` flag — no-op when tiering governs the register (every one of its 7 candidates has a direct or D-02-mapped library equivalent), byte-identical legacy behaviour when disabled. New `HazardResolutionPathGuardTest` — a structural allow-list guard (re-derived from a live repo grep, not hand-copied: exactly 7 sanctioned files) — fails automatically if a future path bypasses the tiered resolver. **Deviation (Rule 1/2, file not in plan's files_modified):** `RamsDataBuilderService::assemble()`'s hazard normaliser was silently dropping `score_reviewed`/`needs_confirmation` from every hazard row on BOTH generation paths (not introduced by this plan, but surfaced by its own confirm-tier assertions) — fixed symmetrically, no schema change. 3 commits (`98149a2`, `ebb3356`, `b89748d`). `ReviewedHazardTieringTest` (5 tests, real seeded DB, real resolver) proves the exact live-evidence gap closed in both directions, including idempotency and the `RAMS_HAZARD_LIBRARY_TIERING=false` rollback on both gated paths independently. Full suite: 2280 passed (+15 from this plan), 1 failed (same documented pre-existing `QueueRecoverCommandTest` memory flake, not chased). `REQUIREMENTS.md` HAZ-02 flipped back to `[x]` with closure narrative. See `.planning/phases/26-hazard-library-structural-inversion/26-07-SUMMARY.md`. **Outstanding (per `26-VERIFICATION.md`, unaffected by this plan):** item 3 (review-screen UI visual confirmation), item 5 (supersede/delete RAMS 96 test artifact), item 7 (manual/live re-verification of RAMS 96 after this plan's code is deployed) — all human/deploy steps, not automated-executor scope.
Plan 26-08 LANDED 2026-08-24 (gap closure round 2 — HAZ-03 closed and provable by test; HAZ-02 CODE-FIXED but deliberately left OPEN pending live re-verification) — round-2 live verification (`26-VERIFICATION.md`) found Plan 26-07's own fix had two further gaps against real project 92 / RAMS 97 data: legacy reviewed hazard names (`Working at Height`, `Manual Handling`, `Electrical Hazards`, `Slips, Trips & Falls (Same Level)`, `Noise and Vibration`, `Working in Occupied Premises`, `Confined Spaces`) collided with their library equivalents instead of deduping — three duplicate pairs, and `Confined Spaces` still rendering in the client-facing document — and a reviewed row's stale numeric score beat the library default even when no human had reviewed it (Working at Height rendering `3x3 -> 2x2` instead of the required `1x4`). New `App\Services\Rams\LegacyHazardNameFoldMap` (16-entry, documented git provenance across 3 groups) is the single, shared, executable form of D-02's fold mapping — consumed ONLY inside `HazardLibraryService::fuzzyMatch()` as the first step, before its existing 3-tier match, so both callers of `resolveFromSeeds()` (the explicit-picks path and `RamsBuilderService::reviewedToRisk()`'s reviewed-data path) fold identically through one choke point. `reviewedToRisk()` now renames a matched row to its resolved template's canonical name, replaces controls unconditionally ONLY on a genuine rename (never on a case-only casing fix, which stays gap-fill-only), gates score precedence on `score_reviewed` (`true` = engineer values win, gap-fill only; `false` OR ABSENT = library default wins outright — the HAZ-03 fix), escalates `needs_confirmation=true` on a folded confirm-tier match, and runs a same-batch dedup pass before the existing tiered merge. Test coverage rebuilt from the REAL 7-name legacy vocabulary observed on live (not the clean 5-name synthetic fixture that let BOTH round 1 and round 2 through) — `ReviewedHazardTieringTest::test_real_legacy_vocabulary_folds_dedupes_and_restores_library_scores` proves zero duplicates, zero `Confined Spaces` rows, the exact `1x4` HAZ-03 residual score, and the exact library control text on renamed rows, all against the live-evidence vocabulary. 3 commits (`71703c7`, `05e5fc7`, `744990f`). **Deviation (Rule 1, self-introduced):** this plan's own `LegacyHazardNameFoldMap` provenance docblock initially referenced the deleted `MANDATORY_KEYWORDS` constant and `mandatoryBaseline()` method by literal name for git-archaeology documentation, tripping Plan 26-04's `HazardInjectionPathsRemovedGuardTest` structural regression guard — reworded to describe the retired machinery without the forbidden literal identifiers, guard passes clean, fixed before commit. Full suite: 2296 passed (+16 from this plan), 1 failed (same documented pre-existing `QueueRecoverCommandTest` memory flake, not chased). `REQUIREMENTS.md`: HAZ-03 flipped to `[x]` complete with closure narrative (Working at Height `3x4 -> 1x4` provable by automated test against the real legacy vocabulary); HAZ-02 deliberately left `[ ]` open — it has been closed prematurely twice already, and per this plan's explicit requirements gate, only the human operator's live re-verification against 21CQ30960/RAMS 97 (regenerated a third time) can close it, not automated-test evidence alone. `ROADMAP.md`'s Phase 26 progress-table row corrected by hand after `roadmap update-plan-progress` incorrectly flipped it to "Complete" from summary-count alone (it cannot see that 26-06 Task 3's live-deploy checkpoint and HAZ-02's live re-verification are both still open) — same known limitation documented in the 26-06 entry above. See `.planning/phases/26-hazard-library-structural-inversion/26-08-SUMMARY.md`. **Outstanding (per `26-VERIFICATION.md` round 2, unaffected code-wise by this plan but blocking on live):** deploy this plan's code to `rams.21stcav.com`, regenerate 21CQ30960 (project 92) a third time, confirm zero duplicate names / no `Confined Spaces` row / Working at height at `1x4` on the live-rendered document, and record whether an electrical-related row appears (its continued absence would indicate project 92's data history, not a code defect — do not re-open HAZ-02 a third time without new evidence of a mechanism failure, per this plan's `<investigation>`).
Status: Ready to execute
Last activity: 2026-08-24 -- Phase 26 Plan 08 complete (gap closure round 2)
Deploy pending: Plan 24-01's 7 code files (migration + model + 2 config/service + 3 services) STILL PENDING live deploy, PLUS Plan 24-02's 4 files (stubber service + 3 import call sites), PLUS Plan 24-03's 4 files (DeviceStencilController + index.blade.php + routes/web.php + navigation.blade.php), PLUS Plan 24-04's 3 files (StencilXmlToSvgRenderer + DeviceStencilController::preview() + routes/web.php — no schema dependency of its own, safe to deploy independently of 24-01's migration since preview() only reads pre-existing DeviceStencil columns), PLUS Plan 24-05's 5 files (UpdateDeviceStencilPortsRequest + DeviceStencilController::edit()/update() + routes/web.php + edit.blade.php + _port-table.blade.php — update() DOES depend on 24-01's migration, since it writes device_stencil_audits rows and reads SOURCE_ENGINEER_CURATED), PLUS Plan 24-06's 4 files (UploadDeviceStencilLogoRequest + DeviceStencilController::uploadLogo() + routes/web.php + edit.blade.php — no NEW schema dependency, but requires `php artisan storage:link` to already exist on live so `Storage::disk('public')` uploads are web-reachable; NOT confirmed live has this — see 24-06-SUMMARY.md), PLUS Plan 24-07's 4 files (StencilPromotionValidator + DeviceStencilController::promote()/discard() + routes/web.php + edit.blade.php — promote()/discard() DEPEND on 24-01's migration, writing device_stencil_audits rows and reading/writing SOURCE_ENGINEER_CURATED/needs_review), PLUS Plan 24-08's 2 files (StencilsReapplyTemplatesCommand + StencilsCoverageReportCommand — CLI-only, no schema of their own but `stencils:reapply-templates` hard-fails without 24-01's `device_stencil_audits` table), PLUS Plan 24-11's 1 file (`app/Http/Controllers/Admin/DeviceStencilController.php` — the D-17 guard narrowing, same file already pending from Plan 24-05/24-07, upload the LATEST version) — `php artisan migrate` MUST run on live BEFORE Plan 24-02's, 24-03's, 24-05's, 24-07's, or 24-08's code is used (all five depend on 24-01's `needs_review`/`logo_path`/`device_stencil_audits` schema). See 24-01-SUMMARY.md, 24-02-SUMMARY.md, 24-03-SUMMARY.md, 24-04-SUMMARY.md, 24-05-SUMMARY.md, 24-06-SUMMARY.md, 24-07-SUMMARY.md, 24-08-SUMMARY.md, and 24-11-SUMMARY.md "Files to upload to live" sections.

### Quick Tasks Completed

| # | Description | Date | Commit | Status | Directory |
|---|-------------|------|--------|--------|-----------|
| 260809-so1 | Survey offline-first photo capture via shared engineer-capture partial (seed of shared package; Worksheet untouched) | 2026-08-09 | 50d6699 | Verified | [260809-so1-survey-offline-photo-capture](./quick/260809-so1-survey-offline-photo-capture/) |
| 260814-o8o | Quote-import review SKU column always rendered "—" for QuoteWerks imports — view read `sku` (vision-path-only key) with no fallback; fixed to `part_number ?? part_no ?? sku`, matching the house pattern elsewhere. Blade-only, no data-layer change. | 2026-08-14 | dacf6ce, ca42688 | Verified | [20260814-quote-import-sku-column](./quick/20260814-quote-import-sku-column/) |
| 260815-ohw | Equipment bulk-select toolbar permanently disabled — `toggleAllInTbody()` only mutated the Alpine `selectedRowIds` array while `_selectedRows()` (called by every `canBulk()` check) read the DOM and reconciled/overwrote the array before `x-model` had flushed `.checked`, wiping the selection on every render. Fixed `toggleAllInTbody()` to set `.checked` directly (mirroring `clearSelection()`) and made `_selectedRows()` a pure read with no write-back. Added structural regression test (PHPUnit can't execute Alpine — asserts Blade source shape, revert-proven). Manual browser verification NOT performed (no browser tooling available this session) — needs human confirm before/alongside deploy. Blade-only, no migration; needs `php artisan optimize:clear` after upload. | 2026-08-15 | 57cdb15, 03b9397 | Complete — human browser verification pending | [20260815-equipment-multiselect-bulk](./quick/20260815-equipment-multiselect-bulk/) |
| 260815-sup | Added `hardware_supply_only` category ("Hardware — supply only (no install)") for client-owned kit 21CAV supplies but does not install. Included in O&M/asset-register output only (`EquipmentCategoryClassifier::isOmIncludedCategory()` shared helper widens the 3 O&M predicates); RAMS/drawings/stencil-coverage/site-survey consumers already use exact `===hardware` matches so the new value is excluded by construction — proven by a new exclusion test. Added dropdown option + own review-screen section, and a bulk category-apply control on the equipment toolbar (reuses the existing per-row category-change DOM-move listener; built on the 260815-ohw multi-select fix, `$root`-only, structural guards still pass). Manual browser verification NOT performed (no browser tooling available this session) — needs human confirm before/alongside deploy. Blade-only + PHP, no migration; needs `php artisan optimize:clear` after upload. | 2026-08-15 | 50703b4, fccb36c, 09331e9 | Complete — human browser verification pending | [20260815-supply-only-category](./quick/20260815-supply-only-category/) |
| 260816-prs | Replaced the category-wide equipment select-all checkbox (selected every row across every room in a category — up to 63 hardware rows, and made the 260815-sup bulk category-change action able to recategorise an entire project in one click) with a per-room select-all. Tagged room header + equipment rows with `data-room`; removed the `<thead>` master checkbox; added a per-room checkbox on each room header wired to new `toggleAllInRoom()` (replaces `toggleAllInTbody()`, same `$root`-only / DOM-driven `.checked` / no-write-in-`_selectedRows()` guarantees from 260815-ohw). Also fixed (Rule 2) two runtime room-relocation paths (`moveEquipRowToArea()`, category-change listener's "General" fallback header) that would otherwise have left `data-room` stale/missing after an in-session edit. Structural test guards repointed + a new thead-checkbox-absence guard added. Manual browser verification NOT performed (no browser tooling available this session) — needs human confirm before/alongside deploy (2-room repro required to prove cross-room isolation). Blade-only, no migration; needs `php artisan optimize:clear` after upload. | 2026-08-16 | 26cf365, 596c2c5 | Complete — human browser verification pending | [20260816-per-room-select-all](./quick/20260816-per-room-select-all/) |
| 260816-rdz | Two independent defects from the package-147 (21CQ30698) blank-schematic investigation. Defect A: `QuoteParserService::extractRooms()`'s keyword-scan fallback harvested full sentence fragments as room names (e.g. "s Boardroom currently contains") — fixed by trimming captures to whole words and rejecting prose (stop-word / short-leading-token / word-count checks); word-count threshold narrowed from the plan's 5 to 9 tokens after the stricter value broke a currently-passing fixture. QuoteParser suite 115→121 passed, 0 failures. Defect B: `DrawingDataResolverService::EXCLUDED_CATEGORIES` is an inclusive-by-default allowlist missing `hardware_supply_only`, so supply-only kit still rendered as a device node in the D2 schematic, contradicting the locked 260815-sup decision (O&M only) — added to the exclusion list. Drawings/Schematic suite 310 passed / 2 skipped / 2 pre-existing DrawIoSpikeController failures (unrelated, not a regression). No migration; needs `php artisan optimize:clear` after upload. | 2026-08-16 | efe275b, eb1ecd0 | Complete | [20260816-room-detect-and-d2-supply-only](./quick/20260816-room-detect-and-d2-supply-only/) |
| 260816-ru4 | Repaired 11 pre-existing Site Survey test failures caused by the `access_token` fillable guard (2026-07-09 security batch dropped `access_token` from `SiteSurvey::$fillable`; two suites — `PublicSurveyControllerTest`, `PublicSurveyQuestionAnswerTest` — still mass-assigned it, so it was silently dropped and `boot::creating` substituted a random UUID instead of the test's controlled token, 404ing every `{token}`-bound route). Fixed by seeding guarded fields via `forceFill(...)->save()`, matching the established `WorksheetTokenExpiryTest` pattern; added `assertNotNull(access_token)` after each seed. Test-only — `SiteSurvey::$fillable` unchanged, no production code touched. `Survey\|Worksheet\|OmManual\|MiniOm` filter: 399 passed/11 failed → 410 passed/0 failed. 11 further latent files (same pattern, not yet observable) logged as follow-up in the SUMMARY, not fixed. Nothing to deploy. | 2026-08-16 | 6f409b4 | Complete | [20260816-survey-token-test-repair](./quick/20260816-survey-token-test-repair/) |
| 260816-t5c | Cleared the test-health backlog: (1) rewrote 2 stale `DrawIoSpikeController` arity lock-tests (broken by the legitimate 3rd-param `SvgSanitizerService` from security batch `9a6837c`) to assert by dependency type instead of parameter count — verified the new tests actually fail when `DrawingService` is removed, then restored the controller byte-identical (production code untouched); (2) closed the 11 further latent `access_token` mass-assignment sites flagged as follow-up by 260816-ru4 — force-filled where the test reads the token back, dropped the (already-no-op) key elsewhere, `SiteSurvey::$fillable` unchanged; (3) recorded the "partial scope is legitimate" product decision (`.planning/notes/tier1-gating-decision.md`) explaining why O&M's blocking "NO TBC POLICY" gate deliberately has no Worksheet/Site-Survey equivalent. `Survey\|Worksheet\|OmManual\|MiniOm` filter: 410 passed/0 failed before and after. Test/docs-only — `deferred-items.md` for Phase 24 updated to mark the 2 DrawIoSpike failures resolved. Nothing to deploy. | 2026-08-16 | b08e45a, ac98f1c | Complete | [20260816-test-health-backlog](./quick/20260816-test-health-backlog/) |
| 260816-uzh | `service_contracts` line items (warranties/support contracts) rendered as device nodes in the v1.3 D2 system schematic — `DrawingDataResolverService::EXCLUDED_CATEGORIES` is an inclusive-by-default allowlist and `services` was excluded but the adjacent `service_contracts` category was missed when the vocabulary grew. Added `service_contracts` to `EXCLUDED_CATEGORIES`; `EXCLUDED_KEYWORDS` deliberately untouched (category-level exclusion is the correct mechanism). Added table-driven regression test in `RackStackForProjectTest.php` covering all 9 canonical categories via reflection into `filterHardware()`, with a guard that fails loudly if the vocabulary grows without an explicit call being made — proven real by removing the fix and confirming the test fails, then restoring. `customer_supplied` deliberately left untouched (also currently survives, but arguably a real part of system topology — client-supplied kit 21CAV wires in — genuine open product decision, documented in SUMMARY, not pre-empted). Drawings/Schematic suite: 313 passed / 2 skipped (D2-binary-dependent) / 0 failed, including the 2 DrawIoSpikeController lock-tests repaired by 260816-t5c. No migration; needs `php artisan optimize:clear` after upload. | 2026-08-17 | c0ba41a, 4e99960 | Complete | [20260816-service-contracts-schematic](./quick/20260816-service-contracts-schematic/) |
| 260817-bxc | Cleared 4 of the last 5 full-suite failures (all stale test assertions, not defects): Item 1 `DocumentArtifactStorage::types()` test asserted a stale 5-entry list vs the current 8 `TYPE_*` constants; Item 2 `CableScheduleXlsxRegressionTest` (D-10 byte-identity guard) was comparing whole-file sha256 of an `.xlsx`, whose `docProps/core.xml` carries build timestamps — the invariant was unverified, not merely red — replaced with a targeted zip-entry comparison (`xl/worksheets/*.xml` + `xl/sharedStrings.xml`, skipping `docProps/*`), proven stable over 3 runs AND proven sensitive by temporarily leaking an FK into a cell (test failed as expected, service reverted cleanly); Item 3 `SnaggingPdfGenerationTest` filename regex predated the M-09 microsecond-timestamp change; Item 4 `ExampleTest` asserted the Laravel stock 200 instead of this app's real guest→login redirect. Item 5 `QueueRecoverCommandTest` (unhealthy queue drain) is NOT fixed — left failing on purpose per the plan: root-caused (confirmed by reproduction) to `QueueRecoverCommand`'s internal `queue:work` call inheriting the 128MB default memory ceiling, which `Illuminate\Queue\Worker::memoryExceeded()` checks against WHOLE-PROCESS memory, not memory used since the call started — in the full suite this trips because ~2,000+ prior tests share one long-lived PHPUnit process; in production this same coupling could mask a genuinely-successful recovery as `EXIT_RECOVERY_FAILED` any time the process is long-lived or the drain itself is memory-heavy — documented as a production finding (not fixed, no `app/` file touched), assertion left un-weakened. Baseline 2,103 passed/5 failed → final 2,108 passed/1 failed (Item 5, intentional)/6 skipped, 8,436 assertions. Test-only, nothing to deploy. | 2026-08-17 | 274f12a, 82d9b9b, 50c228c, 0dd6bf9, d88f298 | Complete — Item 5 open as documented production finding | [20260817-green-the-suite](./quick/20260817-green-the-suite/) |
| 260817-jsg | AI chat edits applied to a RAMS were not reaching the generated document. Cause A: `RamsEditAdapter` deliberately defers artifact regen (data-only, no rebuild) — the visible symptom, but fixing this alone first would have let a confirmed regen silently DELETE data. Cause B (fixed first, per plan ordering): `update_project_field` was the only one of 8 chat ops writing exclusively to `generated_data`, which `RamsBuilderService::buildFromReview()` overwrites wholesale from `reviewed_data` on every regen — so that op's edits were discarded on next regenerate while the other 7 (already `reviewed_data`-based) survived. Fixed by mirroring the edit into `reviewed_data.project_field_overrides`, re-applied onto the freshly-built `project` block last in `runFromReview()`. Then (Task 2) added a user-confirmed regen prompt: `RamsEditAdapter::commitChanges()` now stamps `reviewed_data._pending_regen_since`, extending `RamsDocument::isStale()`/`staleSince()` (computed, no migration — also found the pre-existing `generated_at`-based staleness check was dead code for RAMS, since nothing ever populates that field) so the already-mounted stale-banner surfaces a declined regen for free; the shared chat drawer now asks (`window.appConfirm`, reusing the manual-save flow's exact confirm+hidden-form pattern) before submitting to `rams.regenerate` — never automatic. Task 3: new feature test proves the round trip survives a REAL `BuildRamsDocumentJob` run (AI mocked via `Http::fake`) for both `update_project_field` (asserted against rebuilt `generated_data`) and `add_exclusion` (asserted against `reviewed_data`, its actual read location — `generated_data` never carries an `exclusions` key); regression guard verified by `git stash`-reverting Task 1 and confirming the test fails, then restoring. Caught and fixed a real bug during Task 2: a JS comment referencing `<x-stale-banner>` broke Blade compilation of the whole shared drawer component (all 5 document types) — found via `blade.compiler->compileString()` + `php -l`, not plain `php -l` on the blade source. `Rams\|DocumentEdit` filter: 531→533 passed (1973 assertions), 0 regressions. No migration; needs `php artisan optimize:clear` after upload. Browser verification NOT performed (no browser tooling this session) — needs human confirm before/alongside deploy. | 2026-08-17 | 8e4acfb, a241aa4, 82174a9 | Complete — human browser verification pending | [20260817-rams-ai-edit-regen](./quick/20260817-rams-ai-edit-regen/) |

## Session 2026-07-09 to 2026-07-10 — Jetbuilt redesign + full re-audit closure

A single long working session covering the visual identity reset, security hardening, UX workflows, and two full independent audits. Every commit ships to `live/feat/worksheet-classifier-universal`; 254–598 tests green depending on the suite scope.

**Phase A–D · Jetbuilt-clean visual identity** (`60d18ec`, `1f98126`, `89bcde7`) — palette shift from indigo/teal (`--teal-*` aliased) to navy `#0B2440` + electric-blue accent `#2E7BFF` + slate ink scale + `#F7F9FC` paper. `.card` / `.btn` / `.data-table` / `.section-block` flatten (border-only, no shadow). Sidebar retired; horizontal top-nav with brand → primary links → admin dropdown → ⌘K search chip → user menu. Dashboard rebuilt with single-accent KPI icon set + flat health grid + accent-only chip system.

**Phase E-prep · Auth pages** (`72fe830`) — `layouts/guest.blade.php` gets a token stub `:root` block so login inherits the same tokens. Navy square hex mark instead of indigo gradient. `tailwind.config.js` remaps `brand.*` to accent palette so every Breeze utility resolves through the new tokens.

**Phase E · projects/show rebuild** (`c8c862d`) — 340-line local stylesheet flattens (no coloured left-rail accents, no gradients, no glow shadows). Next-Step hero, workflow stepper, table hover, tabs all speak the accent language. Nine Tailwind `bg-teal-600` CTAs collapse to `.btn.btn-primary`.

**Phase F/1 · projects/index** (`de3b0e8`) — stat cards + filter tabs + search on the shared token stack; project-name link neutralised so the title reads as content, not accent.

**Phase F/2 · survey + worksheet detail** (`fc4f2ab`) — both "share link" hero panels drop the teal-cyan gradient for a flat navy panel with accent-tinted label. Field-table headers drop uppercase + 700 weight for sentence-case ink-500. 300 lines of duplicated hex/gradient CSS removed.

**Phase F/3 · om-manual + public-worksheet cleanup** (`02ab05d`) — bulk-import CSV card retunes from warm cream gradient to flat card primitive. Public sign-off blocked banner uses semantic warning tokens.

**Security WR-01 · M-04 prompt-injection port** (`0766c75`) — new `App\Core\AI\Prompts\Concerns\WrapsUserData` trait extracts MethodStatementPrompt's sentinel-wrap pattern into a shared surface. 6 sibling prompts (OmManualPrompt, WorksheetPrompt, SurveyPrompt, ScopeOfWorksPrompt, RoomOverviewSummaryPrompt, OmRoomOverviewPrompt) now wrap every user-controlled field before interpolation, and every system message tells the model the sentinels are inert data. 13/13 injection-defence tests still green.

**Security WR-03/4/5** (`9a6837c`) — `text/plain` uploads restricted to `.csv` extension so a `.pdf` with `text/plain` bytes can't slip past `ProjectReferenceFileService::validateUpload`. New `App\Services\Drawings\SvgSanitizerService` parses client SVGs with `DOMDocument` (LIBXML_NONET + LIBXML_NOENT), strips `<script>` / `<foreignObject>` / `on*` handlers / `javascript:` and `data:image/svg+xml` schemes. `SiteSurveyController::update` blocks any request that tries to change `project_id` mid-update.

**Batch 6 · A11y** (`ef981be`) — 10 icon-only `×` remove buttons in rams/review + cable-schedule/edit gain `aria-label`; ⌘K palette gains a Tab focus trap so keyboard focus can't escape; `.gsp-item:focus-visible` shows an inset accent ring.

**Batch 7 · Dashboard UX** (`517e479`) — `dashboard.blade.php` when `$projects->isEmpty()` renders a proper onboarding panel (Import Quote PDF · Create by hand · Survey shortcut). Recent-RAMS row routing: generating docs go to `project.show`, everything else to `rams.review`. Shared `<x-dashboard.empty-state>` retunes to accent tokens.

**Batch 11 · Async feedback** (`6b56543`) — failed RAMS + O&M rows expose `error_message` via a `<details><summary>See why</summary>` block instead of hover-only tooltip. Generating rows past 5 minutes flip to amber with "Taking longer than expected…" + elapsed duration.

**Batch 11 deferred** (`0890ac9`) — new `App\Services\WorkerStatusService` (later cached in Batch D), `Worksheet::isReadyForSignoff()` computed state with green "Ready to sign" pill on projects/show + full-width ribbon on worksheets/show, `submitted_notification_sent_at` migration + SurveyService stamp + confirmation banner, `RamsDocument::isStale()` + `OmManual::isStale()` mirroring `Worksheet::isStale()`, new `<x-stale-banner>` shared component mounted on rams/review + om-manual/edit.

**Batch 12 · Component extraction** (`b501dbc`) — new `<x-link-hero>` shared component. Worksheet sign-off + survey engineer-link hero panels both render through it now. ~120 lines of duplicated CSS deleted from the two callers.

**Re-audit · 4 parallel agents** (2026-07-09) — UI · UX · Design · Security. 28 findings surfaced (3 P0, 15 P1, 10 P2). Full artifact at `https://claude.ai/code/artifact/17907090-3995-455f-9e6d-5b55b10d5540`.

**Re-audit P0 batch** (`67c6b84`) — dashboard KPI cards fixed (stat-card `href` was HTML-escaped through `{{ $attrs }}` → all 4 tiles 404'd on click). 9 `bg-teal-600` blocks on projects/show + 17 teal utility hits across 5 drawings/*.blade.php files remapped to `accent-*`. Also fixed 2 orphan `@once` tokens inside CSS comments that were compiling as real Blade directives and 500ing site-survey/show + worksheets/show.

**Re-audit security batch** (`827f3fb`) — 6 retry-generation routes gain `throttle:10,1` (AI cost DoS). `access_token` + `access_token_expires_at` + `submitted_notification_sent_at` dropped from `SiteSurvey::$fillable` and `Worksheet::$fillable`. Test cascade repaired via `forceFill` for the 2 suites that seeded controlled tokens.

**Re-audit batches A/B/C/D** (`588ea8f`, `4de0bc6`, `be99db0`, `397a584`) — 5 UI defects (workflow stepper clip, login title, `.badge-yellow` fill, worker button contrast, RAMS list completed-row fallback), 6 design drift items (command palette gradient pills, mobile responsive-nav-link indigo, O&M editor type rhythm, worksheets/public-show hex sweep, admin avatar gradient, `brand.teal` alias declared), 6 UX gaps (cable-schedules empty-state prop names, quote-import drop zone keyboard reachability, projects/show tab count affordance, RAMS review dirty-state + beforeunload guard, site-surveys empty-state consolidation, Mini O&M sub-label), 2 defence-in-depth (retry-generation authorisation consistency across 4 pipelines + new WorksheetPolicy + CableSchedulePolicy, new `App\Services\WorkerStatusService` caches the top-nav COUNT() query for 15s).

**Post-audit RAMS QA closure** (`01918b1`) — 2 stale `QuoteParserServiceTest` expectations updated to reflect current parser behaviour (SHIPEMAIL from third-party domains no longer leaks into `prepared_by`; dedup now SUMs quantities across duplicate part+area rows). 3 remaining MEDIUM findings closed: M-04 `flock(LOCK_EX)` on cable schedule CSV writes, M-09 6 filename sites upgraded from `Ymd_His` to `Ymd_His_u` (microsecond precision, no same-second retry collision), M-11 filename regex expanded to strip ASCII control chars + DEL. H-02/H-06/H-07/H-09/M-08/M-16 all confirmed shipped in prior passes.

**Cumulative test count green:** 598 across the touched suites (117 QuoteParser + 177 Worksheet + 31 CableSchedule + 273 Rams). The re-audit artifact backlog is now empty; the RAMS QA backlog has only 12 LOW housekeeping items left (`REVIEW-qa-260418.md`), none of which are worth targeting individually.

**Next session pick-up:** either (a) resume v2.0 Phase 23 XTEN-AV renderer work as originally planned, or (b) let the redesign settle in real use for a week before poking further.

## Session 2026-07-08 (evening) — Follow-up moves

Three commits closing the backlog surfaced in PLAN 260708-b7i's SUMMARY.

- **`8d4ea73` — fix(project-show): wire missing _actual-hours-widget include**.
  Widget partial existed at `resources/views/projects/_actual-hours-widget.blade.php`
  but was never `@included` in `projects/show.blade.php`. Controller was passing
  `$canSeeActualHours` + `$actualHours` to the view; view silently ignored them.
  Added the include inside the sticky aside column immediately below the
  Project Summary card. All 5 previously-failing `ActualHoursWidgetTest` cases
  now pass (6/6 total, 19 assertions).

- **`3368fdb` — feat(search): global ⌘K command palette**. Wires the sidebar's
  ⌘K search chip (previously visual-only) to a real JSON endpoint + Alpine
  palette. `GET /search` (GlobalSearchController) does grouped LIKE search
  across Projects / RAMS / Site Surveys / O&M / Worksheets, capped at 5 per
  group, ordered by updated_at desc, min 2-char query. LIKE metacharacters
  escaped before wrapping (`?q=%` doesn't match everything). Alpine palette
  mounts once in `layouts/app.blade.php`, listens to ⌘K/Ctrl+K globally,
  200ms debounced fetch with AbortController for inflight cancellation,
  keyboard-first navigation (↑↓ + Enter + Esc), doc-kind gradient tiles
  (P/R/S/O/W) matching the tier-one preview's doc-type marks. Sidebar chip
  dispatches `open-global-search` custom event so palette + shortcut share
  logic. 10 feature test cases (33 assertions) all pass.

- **`7db8730` — feat(design): second-tier screen sweep — index pages tier-one polish**.
  O&M / RAMS / Site Survey / Worksheet index pages moved onto the shared
  `.page-header` + `.page-header-left` + `.page-subtitle` chrome. RAMS index
  `<style>` block's ~90 lines of hardcoded warm hex (#FFF3CD, #856404,
  #f0f0f0, etc.) retuned to token vars — status pills now rounded 999px with
  hairline borders + color-mix() semantic tints. Emoji glyphs in button
  labels (🗑 ← + ↓ ↺ ✎ 📋 ↻ ✕) replaced with matching 1.75px stroke SVGs
  for consistent iconography. Modal overlay treatment aligned with the ⌘K
  palette (color-mix indigo tint + backdrop-filter blur). Regression test
  filter across RamsController / OmManualController / SiteSurvey / Worksheet:
  200 passed (527 assertions).

## Session 2026-07-08 (afternoon) — Tier-one visual redesign v1

**PLAN 260708-b7i-jetbuilt-tier-one-redesign** — full quick-task with
research + 4 tasks planned, 5 tasks shipped. Anchor: JetBuilt reference

+ user-provided design mockup showing 3-panel showcase with light

sidebar, indigo brand, semantic doc-type icons, radial health score,
gradient sparklines. Interactive preview shipped at
https://claude.ai/code/artifact/687370c1-160a-4b79-a957-b21a1b949b92.

- **Task 1** (`49587f1`) — Foundation. Tailwind theme extended with tier-
  one tokens (cool slate neutrals as semantic aliases, indigo brand,
  restrained shadow ramp, radii + letter-spacing scale). Inter Variable
  self-hosted via `@fontsource-variable/inter`. Base CSS applies the
  Inter feature-settings (cv11/ss01/ss03/zero) globally.

- **Task 2** (`b879ce7`) — Layout shell + sidebar chrome. CSS custom
  property retune at :root in layouts/app.blade.php so every screen
  inherits the new palette through variable indirection — thousands of
  usages of `.card`, `.btn-primary`, `.btn-teal`, `.badge`, `.stat-*`
  across ~200 blade files snap to the new tokens with zero renames.
  Header + logo panel + sidebar all flipped from dark teal/gold/cream
  to light chrome + indigo brand mark. navigation.blade.php rewritten
  with ⌘K search affordance and 2px indigo left-rail on active items.

- **Task 3** (`108bda2`) — Primitive Blade components. The 5 Breeze
  scaffolding leftovers (primary/secondary/danger buttons, text-input,
  nav-link, input-label) shifted from `bg-gray-800`/`indigo-500` to
  the new token classes. Public API preserved.

- **Task 4** (`75d270c`) — Dashboard tier-one polish. KPI colours,
  quick-link inline styles, custom `<style>` block hex values — all
  retuned to token variables. Stat-card component internal `<style>`
  wrapped in @once and value colour flipped to ink (accent lives in
  the icon tile, not the raw number — Linear/Attio/Vercel pattern).

- **Task 5** (`93cc3c8`) — Project detail polish. .page-header
  .page-title bumped from 500 display-font to 700 Inter with tighter
  tracking. Workflow stepper current-step gets indigo gradient + pulse
  ring; done step normalised to var(--success). Progress bar gets
  gradient + glow. Tab count pills softened to tinted (was solid teal).

**Verification:** npm run build clean at every task boundary. Final
CSS ~12.4 kB gzipped. DashboardControllerTest 6/6 pass. 5 pre-existing
`ActualHoursWidgetTest` failures verified as pre-existing on the
pre-session commit `c3e129e` (same pattern as the earlier
PublicWorksheetSignoffTest drift closed in `31a4d52`) — unrelated to
the redesign, worth a separate quick-task.

**Follow-up (logged in SUMMARY.md):** sweep remaining screens
(RAMS review, O&M edit, surveys, worksheets, cable schedules, admin)
into the tier-one hierarchy pattern; extract more repeated markup
into components; wire live global search behind the ⌘K affordance;
retire the 5 ActualHoursWidgetTest failures.

## Session 2026-07-08 — deliverables shipped

Security audit close-out — final M-band pass (audit ref: `.planning/audits/security-audit-2026-05-17.md`):

- **O&M loop close** (`be2f18e`) — `OmManualGeneratorService::generateContent` now merges `extracted_data.manufacturer_support_overrides` + `extracted_data.service_escalation` from the editor into the generated §7 support matrix + §11 escalation path. Closes the storage-vs-render gap flagged in the 2026-07-06 follow-up backlog. `tests/Unit/Services/OmManualOverrideMergeTest` (8 cases / 23 assertions) covers the 4 new normalisation helpers.
- **M-01/02/03/06/07/08 batch** (`35da76d`) — DocumentEditSafetyValidator denylist extended (shell_exec, popen, assert, create_function, include*, require*); `SetSecurityHeaders` middleware registered via `bootstrap/app.php` (X-Frame-Options, CSP frame-ancestors, nosniff, Referrer-Policy, HSTS on HTTPS); `SqlRawInvariantGuardTest` CI ratchet against unsafe whereRaw shapes; `PublicWorksheetController::markSurveyReviewed/markRoomComplete` stops leaking 8 chars of the token as `reviewed_by`, writes `ip:… |actor:<sha256-slice>` instead; `PublicSurveyController::validatePublicSurvey` accepts the resolved SiteSurvey and scopes `rooms.*.id` with `Rule::exists(...)->where('site_survey_id', $survey->id)`; draft-mode O&M gets a diagonal `DRAFT · NOT FOR ISSUE` band on every PDF page + `DRAFT-` filename prefix on PDF and DOCX downloads.
- **Stale signoff test cleanup** (`31a4d52`) — 3 pre-existing `PublicWorksheetSignoffTest` failures fixed. Signoff test payloads gained the missing `happy_with_work='1'` field after the Q19 controller gate change; admin show page assertion switched from Title-Case "Client Sign-Off Link" to the current sentence-case "Client sign-off link" label. Full 12-case suite (50 assertions) green.
- **M-04 prompt-injection defence** (`ba7fa9f`) — Two-layer defence for the method-statement AI pipeline: (a) `MethodStatementPrompt::build()` wraps every user-controllable field in `<<user_data>>…<<end_user_data>>` sentinels, per-item for scope buckets so an injection in one item can't leak into another; sentinels inside adversarial values are neutralised before interpolation; system message briefs the model that content between the tags is inert; (b) `MethodStatementGeneratorService::isValid()` rejects any response missing mandatory H&S phrases (`toolbox talk`, `asbestos`, `permit-to-work`, `assembly point`, `ppe`) or containing injection markers (`ignore the above`, `disregard previous`, echoed sentinels). `MethodStatementInjectionDefenceTest` — 13 cases covering both layers. All 32 method-statement tests green.
- **M-05 token expiry + revoke UI** (`8b007d4`) — Nullable `worksheets.access_token_expires_at` column (mirrors site_surveys precedent; null = never expires). `Worksheet::isTokenExpired()` + `regenerateAccessToken()`. `PublicWorksheetController::resolveWorksheet` aborts 410 Gone on expired tokens (semantic fit — URL was valid, resource still exists, this token is retired). Admin `POST /worksheets/{worksheet}/revoke-token` route + `WorksheetController::revokeToken()` action + red "Revoke & regenerate" button on `worksheets.show` beside Copy/Open. `WorksheetTokenExpiryTest` — 11 cases (past/future/null expiry, 410 on public routes, 404 on old token after revoke, admin auth, revoke rotates + flashes success).

Data fixes applied live (via `artisan tinker` on the server):

- User id 1 renamed `sonny` → `Sonny Tanda`.
- Project id 84 (`SHIPCOMPSTART Volkswagen Group SHIPCOMPEND`) — `name` / `client_name` / `site_address` all cleaned via `preg_replace('/SHIP(COMP|ADD)(START|END)/', '', $s)` + `\s+,` collapse. Final state: `Volkswagen Group, Yeomans Drive, Blakelands, MK14 5AN Milton Keynes`.

Deferred (logged in commits + audit doc RESOLUTION blocks):

- M-04 sub-item 3 (equipment cross-check between AI output and reviewed items) — abbreviated quote-derived names + generic AV terms give too many false positives without a proper name-matching layer. Worth its own plan.

Frontend redesign discussed (not scoped) — no `frontend-design` skill loaded in this session; recommended incremental interventions (design-token pass, targeted screen polish on 2–3 highest-use pages, more component extraction) over wholesale rebuild given internal-tool audience.

**Audit status:** CRITICAL + HIGH + MEDIUM all closed. LOW items untouched but were parked as-designed in the audit doc.

## Session 2026-07-03 — deliverables shipped

Security audit close-out (audit ref: `.planning/audits/security-audit-2026-05-17.md`):

- H-01 (`128d51d`) — `is_active` login gate enforced
- H-02 (`90e3a2d`) — CLOSED as designed (shared-workspace + C-03 fix)
- H-03 (`76ccee0`) — admin-check invariant test + audit doc RESOLUTION block
- H-04 (`16f0421`) — 5 dev/diag scripts removed from repo root
- H-05 (`76ccee0`) — `killProcesses()` deleted, `spawnWorker()` CLI-only guard
- C-01 / C-02 / C-03 — clearcache.php / deploy_docx / /register all removed
- All CRITICAL + HIGH items closed. M-01…M-08 remain.

UX polish + quality-of-life:

- Login page show/hide password toggle (`0c60564`)
- Root URL redirect (`216ce24`) — no more Laravel welcome splash on `/`
- Quote extractor soft-wrap normalisation (session-earlier: `cc2ffa8`)
- Survey PDF prose formatting fixes (session-earlier: `65178a9`, `0212e9b`, `7ab03d6`, `06ff853`)

Tier-1 UX redesign — screen 04 (O&M edit):

- v1 `5355c04` — structured typed fields replacing raw JSON textarea + safety-valve advanced disclosure
- v2 `6d773af` — sticky sidebar rail with per-section status chips + per-room narrative + inline equipment table
- v3 `8501ac2` — full 15-section PDF-parity rail (front matter + §1–§15 + rooms + special notes) with 6 new editable payload keys (distribution_list, revision_history, manufacturer_support_overrides, service_escalation, training_handover, document_control)

## Tier-1 UX redesign — COMPLETE

All 5 screens shipped v1 (some also v2/v3). Live-confirmed by user on each.
Build order followed: 04 → 03 → 05 → 02 → 01.

| Screen | Commits | What shipped |
|---|---|---|
| 04 O&M edit | `5355c04` v1 · `6d773af` v2 · `8501ac2` v3 | v1 structured typed fields replacing raw JSON textarea; v2 sticky sidebar rail + per-room inline equipment editor; v3 full 15-section PDF-parity rail (front matter + §1–§15 + rooms + special notes) with 6 new editable payload keys |
| 03 Edit Project Data | `b144d44` v1 · `9ed45ca` v2 · `836879e` v3 | v1 sticky sidebar rail navigating all 10 sections across both tabs; v2 area picker via native `<datalist>` sourced from Room Overviews; v3 compact spreadsheet-density equipment table with hover-reveal delete + muted category column |
| 05 Worksheet detail | `5beca6f` v1 | Sign-off link promoted to pine-dark hero at top; duplicate footer action row removed (all four buttons already lived in top toolbar) |
| 02 Site Survey view | `2090a34` v1 | Engineer capture link promoted to pine-dark hero at top; old teal mid-page banner removed |
| 01 Project detail | `25fe9df` v1 | Next Step banner tightened (p-7→p-4, icon w-14→w-11, text-xl→text-base); Workflow stepper chips compacted (px-4 py-2 text-sm → px-3 py-1 text-xs) so 7-step lifecycle fits on one line |

Reference artifact: https://claude.ai/code/artifact/cbcf52b9-3972-46e8-952f-c30697265bae

Consistency established across the app: Screens 02 + 05 share the same pine-dark hero pattern for "primary shareable URL" (engineer link · client sign-off link). Screens 03 + 04 share the same sticky-rail navigation pattern (per-section status chips + IntersectionObserver highlight on scroll).

Zero behavioural regressions across all 5 screens — every form field, every route, every save contract unchanged. All 6 O&M edit tests pass (68 assertions covering both structured + raw-JSON paths + safety properties).

## Follow-up backlog (surfaced this Tier-1 sweep, not yet planned)

- Wire `manufacturer_support_overrides` (§10) + `service_escalation` (§11) into `OmManualGeneratorService` so the editable overrides actually take priority on next regen (currently stored in `extracted_data` but generator ignores them — the storage contract is established, plumbing is the missing piece)
- Improve the "Generate O&M — failed" UX on the project detail page: validation error is buried in a `title=""` tooltip; no clear affordance that Draft (TBC) is the actual unblock path
- Site Survey Screen 02 v2: header toolbar simplification (7 buttons → primary + Actions ▾) — deferred pending user feedback on whether v1's hero alone is enough
- Consider `.gitignore` for the audit screenshots in repo root (`01-login.png` through `p10-worksheet-view.png`) or delete them
- Project detail Screen 01 v2: 9-tab strip is still heavy; may benefit from a similar compaction pass

## Next session — pick one

1. **Resume v2.0 Phase 23** (XTEN-AV-style renderer) — the milestone was paused mid-Phase 23 sub-plan 1 ("context gathered"). Full plan already in `.planning/phases/23-xten-av-style-renderer/`. Pre-Tier-1 audit still applies.
2. **Tier-1 follow-up backlog** — the mfg_support_overrides generator wiring is the highest-value follow-up (closes the loop between editor and PDF).
3. **M-band security audit** — 8 remaining items from `.planning/audits/security-audit-2026-05-17.md` (CSP headers, raw SQL binding, AI prompt escaping, UUID rotation, etc.)

## Milestone Progress (v1.3)

| Phase | Plans | Status |
|-------|-------|--------|
| 17. System Schematics + Shared Foundations | 3/3 | ✓ Complete |
| 18. Rack Elevations | 2/2 | ✓ Complete (18-01 + 18-03 done) |
| 19. Floor Plans (Konva) | 0/0 | ⤳ Deferred to v2.0 backlog 999.1 (2026-05-02) |
| 20. Drawing Export + O&M Integration | 2/2 | ✓ Complete (20-01 + 20-02 LANDED 2026-05-03) |

**Total:** 7/7 plans complete, 3/3 phases shipped (100%) — Phase 19 deferred from v1.3 scope; v1.3 milestone READY for `/gsd-complete-milestone`.

## Performance Metrics

| Metric | Value |
|--------|-------|
| Requirements (v1.3 active) | 22 (DRAW-01..13, 21..28, 30) |
| Requirements (v2.0 deferred) | 8 (DRAW-14..20, 29) |
| Phases | 3 active (17, 18, 20) + 1 deferred (19) |
| Coverage | 22/22 v1.3 mapped (100%) |
| Plans estimated (v1.3) | 7 (3 done + 2 + 2) |
| Granularity | standard |
| Completed milestones | v1.0 + v1.1 + v1.2 |
| Phase 17 P01 | 12min | 3 tasks | 19 files |
| Phase 17 P02 | 13min | 3 tasks | 32 files |
| Phase 17 P03 | 11min | 3 tasks | 12 files |
| Phase 18 P01 | 65min | 3 tasks | 18 files |
| Phase 18 P03 | 12min | 3 tasks | 15 files |
| Phase 20 P01 | 35min | 3 tasks | 19 files |
| Phase 20 P02 | 13min | 3 tasks | 12 files |
| Phase 21 P01 | 13min | 3 tasks | 10 files |
| Phase 21 P02 | 21min | 2 tasks | 19 files |
| Phase 21 P03 | 9min | 2 tasks | 21 files |
| Phase 22 P22-01 | 7min | 3 tasks | 7 files |
| Phase 22 P22-02 | 28min | 3 tasks | 8 files |
| Phase 22 P22-03 | 12min | 2 tasks | 4 files |
| Phase 22.1 P01 | 2min | 1 tasks | 1 files |
| Phase 22.1 P02 | 5min | 1 tasks | 2 files |
| Phase 22.1 P03 | 35m | 2 tasks | 9 files |
| Phase 22.1 P04 | 30m | 3 tasks | 13 files |
| Phase 22.1 P05 | 35m | 3 tasks | 10 files |
| Phase 22.1 P06 | 25m | 3 tasks | 3 files |
| Phase 22.1 P07 | 25m | 4 tasks | 5 files |
| Phase 24 P11 (gap-closure) | ~20min | 2 tasks | 2 files |
| Phase 26 P01 | 30min | 3 tasks | 5 files |
| Phase 26 P02 | 45min | 2 tasks | 2 files |
| Phase 26 P03 | 40min | 3 tasks | 9 files |
| Phase 26 P04 | 1h10min | 3 tasks | 6 files |
| Phase 26 P05 | 1h40min | 3 tasks | 6 files |
| Phase 26 P08 | 65min | 3 tasks | 10 files |

## Accumulated Context

### Key Decisions (v2.0)

- **Plan 26-01 LANDED** (2026-08-24) — Hazard Library Structural Inversion, data foundation. `hazard_templates` gained a nullable `include_when` text column (guarded `Schema::hasColumn`, reversible); `HazardTemplateSeeder::standardHazards()` rewritten to emit all 18 hazards from `.planning/reference/21cav-rams-skill/references/hazard-library.md` verbatim (names, full control lists, typical initial/residual L×S), each tagged `always` (4 tier-1: Slips/trips/falls, Low voltage AV connections, Fire and evacuation, COSHH substances), `signal:<key>` (9 tier-2 deterministic), or `confirm:<key>` (5 tier-3, ALWAYS human-confirmation-required — never AI-evaluated, per CLAUDE.md's "AI is ONLY allowed for formatting and method statement structuring — never for inventing scope" constraint and the D-05 2026-08-23 correction). Working at height seeded residual `1×4` (was `2×3` in the old baseline) — the checkable HAZ-03 proof. Upsert-by-name loop preserved verbatim; new orphan-row cleanup (`HazardTemplate::where('is_global', true)->whereNotIn('name', $newNames)->delete()`) removes the 6 D-02-superseded old global hazard names, scoped so it can never touch an `is_global=false` (user-created) row — not a truncate. Local SQLite DB migrated + seeded: 18 `is_global=true` rows, 0 with null `include_when`, 0 `is_global=false` rows existed/affected. Rule 1 auto-fix: `HazardTemplateController::store()`/`update()` threw an undefined-array-key 500 when the (validated-`nullable`) `description` field was omitted from the request — exactly the payload shape the plan's own mandated regression test uses — fixed with `$data['description'] ?? null` in both methods; `include_when` itself remains correctly excluded from the controller's literal-array mass-assignment construction (T-26-01 mitigation verified end-to-end via a real HTTP POST in the new test, not just code inspection). New `tests/Feature/HazardTemplates/HazardTemplateSeederIncludeWhenTest.php` (3 tests / 7 assertions): 18-row count, idempotent re-seed with `is_global=false` fixture byte-identical across two seed runs, and the T-26-01 mass-assignment proof. Live deploy sequence (stcav user, `git pull`, `migrate --force`, `db:seed --force`) documented as a docblock on the test file for Plan 26-06's human checkpoint — NOT executed against rams.21stcav.com in this plan. Commits: `90dffca` + `10f26f0` + `8084ddb`. Requirements HAZ-01 and HAZ-03 satisfied at the data layer; HAZ-02 (resolver) and HAZ-04 (UI score-touch gate) remain for later Phase 26 plans.

- **Plan 24-11 LANDED** (2026-08-14) — Gap-closure: UAT Gap 2 closed. `DeviceStencilController::update()`'s D-17 curated-artwork guard was keying its confirm-to-proceed requirement on `source === SOURCE_ENGINEER_CURATED` alone, which UAT found fires on 96 of 96 real catalogue stencils — only 5 of those 96 (ClickShare Bar Pro 7 ports / Neat Bar Pro 6 / Netgear GS312TP 14 / Samsung QM65C-T 9 / Sennheiser TCC2 4 — 40 ports total) actually carry hand-built mxGraph artwork; the other 91 are bare zero-port stubs sharing the same `source` value (per Phase 21 D-05's deliberate seeding). This directly contradicted D-17's own "the ordinary stub-curation path must stay a single-click save with zero added friction" requirement. **Fix (UAT's candidate (a)):** guard predicate becomes `source === SOURCE_ENGINEER_CURATED && $deviceStencil->ports()->exists() && ! $request->boolean('confirm_regenerate')`, evaluated against the stencil's PRIOR saved ports (before this request's mutation, since the guard runs as the very first statement in `update()`). A zero-port `engineer-curated` stub now saves in one click with no warning; the 5 genuinely-curated stencils keep the full confirm-to-proceed protection (all 5 have ports, so `ports()->exists()` stays true for them). Known/intended consequence documented in the docblock: the FIRST time a stub gets ports added, no confirm is needed — but editing that SAME stencil again once it has ports DOES trigger the guard from then on, matching genuine curated-artwork protection. Accepted residual risk (threat register T-24-11-02, disposition `accept`, not fixed by this plan): an admin could save a curated stencil down to zero ports (`UpdateDeviceStencilPortsRequest` allows an empty `ports` array) and the guard would not re-fire on that stencil's next edit — self-inflicted by an already-privileged admin, fully attributable via the unconditional per-save `DeviceStencilAudit` row (D-03), so left as documented-not-fixed per the plan's own threat model. Test 6 (`test_update_against_engineer_curated_stencil_with_confirm_persists_and_audits_prior_xml`) had its fixture corrected — it previously had ZERO existing ports, so under the new predicate it would have passed for the wrong reason (guard never engaging, not guard-fired-then-bypassed); now inserts a real prior port so the guard-then-bypass path is genuinely exercised. New regression test `test_update_against_engineer_curated_zero_port_stub_saves_without_confirm_regenerate` (Test 8) reproduces the exact Gap 2 scenario (engineer-curated + zero ports + no confirm) and fails against the pre-fix unconditional-source predicate, proving it would have caught the defect. **12/12 `DeviceStencilEditTest` green.** Zero plan deviations. Commits: `dea8981` (Task 1 guard narrowing + docblock) + `b090e9b` (Task 2 Test 6 fixture fix + Test 8). No migration in this plan — only `app/Http/Controllers/Admin/DeviceStencilController.php` needs re-upload to live (already on the pending-deploy list from Plans 24-05/24-07; upload the latest version). **UAT Gap 2 closed. Plan 24-12 (remaining UAT gap) unblocked to proceed independently.**

- **Plan 24-06 LANDED** (2026-08-14) — Phase 24 Wave 5 shipped: per-stencil manufacturer logo upload (DRAW-52). New `UploadDeviceStencilLogoRequest` (`mimes:svg,png,jpg,jpeg` + `max:2048` — deliberately not Laravel's `image` rule, which does not reliably admit `.svg`) + `DeviceStencilController::uploadLogo()`: every SVG upload is mandatorily routed through the existing `SvgSanitizerService::sanitize()` BEFORE it ever touches disk (D-12, T-24-13/T-24-14) — an unparseable/malicious SVG never persists, it bounces back as a 422 validation error instead. PNG/JPG uploads need no sanitisation (no script-execution surface) and are stored as-is via `Storage::disk('public')->putFileAs()`. Both formats write a fixed `device-stencils/{id}/logo.{ext}` path (overwrite-on-reupload is correct — one current logo per stencil, unlike `DeviceLabelPhotoService`'s UUID-per-photo convention) and set `logo_path` to the public `/storage/...` URL (D-15 — a FILE reference, sibling to the legacy inline-SVG-text `logo_svg` column, which this plan leaves untouched). New route `admin.device-stencils.upload-logo` (POST, suffix segment after `{deviceStencil}`, same no-collision pattern as `preview`/`edit`). `edit.blade.php` gains a "Manufacturer Logo" region inside the existing sticky preview card (does not restructure Plan 24-05's two-column grid) — 64px preview when `logo_path` is set, else a greyed-out `ManufacturerLogoResolver::resolveAssetPath()` fallback hint ("Using the built-in {manufacturer} wordmark until a custom logo is uploaded.") when the manufacturer resolves to one of the 20 built-in brand assets, else nothing. Upload form posts multipart to the new route with the exact UI-SPEC helper text and inline `$errors->first('logo')` surfacing. **9 new tests / 31 assertions GREEN** (valid PNG, malicious-SVG-with-`<script>`-and-`on*`-handler sanitisation proof, unparseable-SVG rejection, oversized upload, MIME-type-spoof rejection via an explicit `->mimeType()` override on a fake upload, non-admin 403 block, plus 3 edit-screen view tests for the widget/fallback-hint/helper-text). Broader `tests/Feature/Drawings` regression run: 235 passed / 938 assertions, 2 skipped (D2 binary unavailable), same 2 PRE-EXISTING `DrawIoSpikeController` constructor-arity failures already logged in `deferred-items.md` (predate Phase 24, untouched). Zero plan deviations. Commits: `a0670c4` (Task 1 request+controller+route+tests) + `38aecb6` (Task 2 edit.blade.php widget). **DRAW-52 marked complete in REQUIREMENTS.md and ROADMAP.md — DRAW-52 was delivered solely by this plan (verified: no other 24-0x plan frontmatter lists it).** ⚠️ Deploy-pending: this plan's `uploadLogo()` writes `logo_path` only — no new schema dependency of its own (Plan 24-01's migration already added the nullable `logo_path` column) — but `Storage::disk('public')` requires `php artisan storage:link` to exist on live for uploaded files to be web-reachable; NOT confirmed whether it already does (Phase 21's manufacturer logos are served from `public/img/manufacturers/` directly, not via the storage symlink, so this is a genuinely new live-server dependency — see 24-06-SUMMARY.md). **Plan 24-07 (Promote/Discard actions, Wave 6) unblocked.**

- **Plan 24-02 LANDED** (2026-08-14) — Phase 24 Wave 2 shipped. New `App\Services\QuoteImport\QuoteImportStencilStubber` (D-09) — the single shared auto-stub orchestration service, wired into all 3 quote-import call sites: `ExtractQuoteJob` (PDF upload path, call placed between the `DB::transaction` close and `generateContentPack()`), `QuoteWerksImportService::buildExtractedData` (the DEFAULT import route since 260725-qw4, call placed immediately after `$equipment` is built), and `ReimportQuoteJob` (re-import path — captured `completePendingReimport()`'s previously-discarded return value, call placed strictly AFTER that method's `DB::transaction` closure returns, verified against the real source per RESEARCH.md Assumption A2). Public `stubFromEquipmentLines(array $lines): array{created:int, stencils:DeviceStencil[]}` normalises 3 divergent equipment-line shapes (`part_number`/`sku`, `name`/`description`) behind one method, always re-classifies through the shared `EquipmentCategoryClassifier` (never trusts an upstream `category` key blindly), filters to `hardware`-category lines only (cables/services/consumables never produce a `device_stencils` row), delegates port-template resolution to Plan 24-01's `CategoryPortTemplateResolver` (D-06/D-07 — ambiguous/unrecognised device types get a zero-port `needs_review` stub, never a guess), and delegates stencil caching to `DeviceStencilCacheService`'s `firstOrCreate` contract (idempotent — proven by a repeated-call test asserting zero new rows on re-import). Bulk-inserts `device_ports` via a single raw `DevicePort::insert()` statement only on genuine cache-miss with a non-empty resolved template. All 3 call sites wrap the stubber call in try/catch (mirrors `ExtractQuoteJob`'s existing `generateContentPack()` best-effort pattern) — proven by a dedicated test that forces `CategoryPortTemplateResolver::resolve()` to throw and asserts the parent `ProjectPackage` still reaches `STATUS_EXTRACTED`. **6 new tests / 36 assertions GREEN.** Full `tests/Feature/Drawings` regression run: 197 passed, 2 skipped, same 2 PRE-EXISTING `DrawIoSpikeController` constructor-arity failures already logged in `deferred-items.md` by Plan 24-01 (unrelated, not touched). Zero plan deviations. Commits: `bd3e76c` (Task 1 stubber service) + `2e6dc8d` (Task 2 wire 3 call sites + tests). No requirements mapped to this plan (fulfils the ROADMAP's unnumbered Success Criteria 1+2, not a DRAW-5x UI requirement). **Plan 24-03 (admin list view, Wave 3) unblocked.** ⚠️ Deploy-pending: this plan's code depends on Plan 24-01's migration already being live — `php artisan migrate` MUST run first.

- **Plan 24-01 LANDED** (2026-08-14) — Phase 24 Wave 1 foundation shipped. Schema: `device_stencils` gains indexed `needs_review` boolean + nullable `logo_path` (D-10/D-15); new `device_stencil_audits` table (D-03, `DeviceStencilAudit` model with `ACTION_PROMOTE`/`ACTION_EDIT`/`ACTION_DISCARD_REGENERATE`); PHP-based (not raw-SQL) migration backfill carries existing `metadata.needs_phase_24_curation` flags into the new column — MariaDB prod vs SQLite test diverge on JSON syntax, so the loop is `json_decode()` over each row, not a JSON-extract WHERE clause. `DeviceStencilCacheService::resolveForPartNumber()`'s `firstOrCreate` array now sets `needs_review => true` in the ONE place both future import-time stubbing (Plan 24-02) and the pre-existing Phase 21 lazy-create path share. `DeviceStencil::audits()` HasMany relation added for Plan 24-08's `whereDoesntHave('audits')` re-templating eligibility query. `config/drawings.php` gains `port_templates` (display/switch/bracket/mount) + `port_template_precedence` (D-06/D-07) — version-controlled vocabulary, zero DB table. New `CategoryPortTemplateResolver` deterministically resolves device-type → port template: `cable` beats everything (permanent zero-port short-circuit), explicit precedence rules resolve compound conflicts (canonical "Samsung 65in Display Bracket" → `bracket`, not `display`), single unambiguous keyword match otherwise resolves, anything ambiguous/unrecognised returns `null` (never a guess) — mirrors `DrawingDataResolverService::inferRoleFromName`'s return-null shape, not `EquipmentCategoryClassifier`'s unconditional default. `port_id` assigned deterministically as `{connector_type}-{n}` (1-based per connector_type) for Plan 24-08's dry-run diffing stability. `AutoGenericStencilGenerator` extended (D-05): `build()`/`emitShape()` accept an optional `ports` hint key and, when present, emit a `<connections>` block of named mxGraph constraints (coordinate-mapped by `side`, consumed by `CableRouter::stencilHasConstraints()`'s substring check) plus a provisional dashed/60%-alpha rail-tick + port-label block in `<foreground>` — grammar verified DIRECTLY against the vendored `public/vendor/drawio/mxgraph/src/shape/mxStencil.js` parser (NOT the seed pack, which has zero `dashed`/`strokealpha` precedent): `<dashed dashed="1"/>` (attribute is `dashed`, not `value`), `<strokealpha alpha="0.6"/>` (0.0-1.0 fraction, GLOBAL alpha per the vendored handler — identical to `alpha`/`fillalpha`), batched bare `<line x1 y1 x2 y2/>` ticks + single `<stroke/>` commit, per-port `<text>` labels rendered while the mute is still active, then a MANDATORY `<dashed dashed="0"/><strokealpha alpha="1"/>` reset (state otherwise leaks into whatever composes around the shape next). Zero-ports path is byte-identical to the pre-Phase-24 committed output — the regression fixture was captured by executing the ORIGINAL generator source pulled directly from commit `06c9052` in an isolated namespace, not hand-transcribed, eliminating transcription-error risk. **27 new tests / 115 assertions GREEN** (5 DeviceStencilAuditTest + 6 CategoryPortTemplateResolverTest + 16 AutoGenericStencilGeneratorTest, the latter split across a pre-existing Unit suite + this plan's new Feature suite). Full `tests/Feature/Drawings` + `tests/Unit/Services/Drawings` regression run: 246 passed / 2078 assertions, 2 skipped, 2 PRE-EXISTING unrelated failures (`DrawIoSpikeController` constructor-arity lock tests expecting 2 params, actual 3 — confirmed via `git log` to predate this plan, `DrawIoSpikeController.php` last touched in unrelated commit `9a6837c`; logged to `.planning/phases/24-stencil-curation-ui-quote-import-auto-stub/deferred-items.md`, not fixed, out of scope). One implementation-detail fix during Task 1: SQLite's rebuild-based `ALTER TABLE ... DROP COLUMN` left a dangling index reference when dropping an indexed column inside a multi-column `dropColumn([...])` call — migration `down()` now drops the index explicitly first. Zero plan deviations otherwise. Commits: `7e77cb9` (Task 1 schema+model+cache-wiring) + `2e8bc7d` (Task 2 resolver+config) + `7d93fbd` (Task 3 generator extension). **DRAW-51 complete. Plan 24-02 (`QuoteImportStencilStubber`, Wave 2) unblocked — can call `CategoryPortTemplateResolver::resolve()` and pass its output straight into `AutoGenericStencilGenerator::build(['ports' => ...])`.** ⚠️ Deploy-pending: `php artisan migrate` MUST run on live before any later Phase 24 admin screen opens (Phase 21 D-13) — this plan's migration only ran locally against SQLite `:memory:` under the test suite.

- **Plan 22.1-07 LANDED** (2026-05-13) — Phase 22.1 GAP CLOSURE complete. 9 write-site leaks the verifier flagged in 22.1-VERIFICATION.md closed across 5 files / 4 atomic tasks. **review.blade.php:** removed hidden `summary` input (both Blade static `[$ri]` variant AND JS `[${idx}]` row-template variant), removed `description` textarea + label, removed `data.description` JS branch in `generateRoomSummary()` callback. **ProjectPackageReviewController:** 4-key shape in `editPayload` / `parseReviewPayload` / `generateSurveyRooms`; `generateRoomSummary` consumes canonical `$results[0]['works_summary']`; JSON response shed `description` key; dead `$sourceDescription` source-trace logic + `current_description` validation rule removed. **ExtractQuoteJob:** scaffold emits canonical 4-key shape with `solution_type_id => null` (honest representation matching normaliser coercion target). **RoomOverviewSummaryService:** all 3 internal persistence writes renamed `$r['summary']` → `$r['works_summary']` per D-07 single-source-of-truth lock; AI input JSON key stays `summary` (RoomOverviewSummaryPrompt contract unchanged); single translation point inside service body. **ReviewedDataStructuralDiffTest:** 2 new write-side guard methods added (`test_write_side_parseReviewPayload_drops_legacy_summary_description_keys` invokes the private method via `ReflectionMethod` + `Request::create($payload)` wrapping — passing raw array would TypeError on the `Request $request` signature; `test_write_side_package_update_round_trips_canonical_shape_unchanged` covers the JSON-column cast). **Full Phase 22.1 combined corpus: 53 tests / 215 assertions GREEN** (was 51/200 — exactly +2 tests / +15 assertions from the 2 new methods). **RamsRenderRegression Wave-1 canary STILL GREEN 3/3 / 9 assertions** — D-12 byte-equivalence preserved across all 6 plans (5 production waves + closure). 8 NEW G1-G8 gap-closure ratchets all zero-hit (G5/G6/G7 minor hits all verified outside scope: classifier item map / generateRoomSummary INPUT array / equipment row mapping / removal-doc comment). 7 Plan-06 R1-R7 ratchets remain zero-hit (no regression in pre-existing invariants). **DATA-01 PARTIAL → SATISFIED. DATA-02 PARTIAL → SATISFIED.** Migration now self-stabilising AND CI-protected end-to-end on both read AND write paths. Zero deviations from plan. Zero new files. Zero schema changes. Zero new endpoints. Zero new AI calls. Commits: `79a0004` (Task 1 form+controller cleanup) + `048a633` (Task 2 service+scaffold rename) + `e4c9547` (Task 3 write-side guard tests). **PHASE 22.1 READY FOR `/gsd-verify-phase 22.1 --rerun`.**

- **Plan 22.1-06 LANDED** (2026-05-13) — PHASE 22.1 COMPLETE. Final verification wave shipped two phase-level invariant guards: `ReviewedDataStructuralDiffTest` (D-13 structural JSON diff, 6 tests / 36 assertions — locks dropped keys `project.overview` + per-room `summary` / `description` / `scope` AND locks canonical 11-key project + 4-key per-room schema) + `Phase22_1InvariantGuardTest` (6 tests / 57 assertions — one test method per ROADMAP SC#1..#6 each citing its DATA-XX requirement ID in the PHPDoc). SC#6 (CLAUDE.md AI constraint audit) uses method-scoped `token_get_all()` check — body-only scan of `build()` / `*schema*` / `*response_format*` methods on every BasePrompt subclass under `app/Core/AI/Prompts/`; legitimate `systemMessage()` constraint text referencing forbidden literals does NOT false-positive (BasePrompt itself skipped). **Heuristic-robustness probe verified BOTH directions:** positive (literal `"hazards"` in `systemMessage()` body) PASSED with 40 assertions, negative (literal `"hazards"` in `build()` body) correctly FAILED with the SC#6 violation message, probe file deleted before final commit. **Full Phase 22.1 combined test corpus: 51 tests / 200 assertions GREEN.** Wave-1 `RamsRenderRegression` canary STILL GREEN after all 5 waves: 3/3 / 9 assertions (D-12 byte-equivalence preserved end-to-end). All 7 grep ratchets return zero non-guard hits (R1/R2/R3 hits only inside the 2 legitimate guard files; R4 finds only `scope_of_works_bullets` (a different canonical variable from Phase 22.1 D-06); R5 finds only the legacy fixture access inside the new structural-diff test; R6/R7 zero hits). DATA-01..05 coverage in REQUIREMENTS.md = 5 (one per requirement). 1 Rule-3 deviation: extended `DeadPathRemovalGuardTest` whitelist to recognise `Phase22_1InvariantGuardTest.php` as a legitimate downstream guard file (both legitimately cite the 3 deleted class basenames `RamsGeneratorService`/`RamsPrompt`/`WorksBulletsPrompt` as deletion-target listings). Commits: `dd4b37a` (Task 1 D-13 structural diff test) + `b01f07a` (Task 2 phase-level invariant guard) + `811c138` (Rule-3 DeadPathRemovalGuard whitelist extension). **PHASE 22.1 READY FOR `/gsd-verify-phase 22.1`**. Deferred to Phase 22.2: Survey↔RAMS sync; `ProjectPackage.works_description` column drop; `ProjectPackageRamsReviewService.php:78,85` orphan `project.overview` emission cleanup; `app/scripts/generate_rams_docx.js` confirmation.

- **Plan 22.1-05 LANDED** (2026-05-13) — DATA-01 invariant locked. 6 surgical changes: (1) ExtractQuoteJob drops the `works_description = $extracted['overview']` auto-seed at lines 124/133 (project create + package update). (2) RamsExtractionDraftBuilderService::buildProject drops project.overview write. (3) RamsReviewDataService::normaliseProject drops project-level overview emission — 11 canonical keys exactly. (4) ProjectPackageReviewController save() AND approve() drop method_statement_notes mirroring into Project.works_description (Project::update is now 4 keys). (5) MethodStatementService::buildScope removes the PM-INSTRUCTIONS separator-suffix block. (6) RamsComplianceUpgradeService::upgradeScopeOfWorks gets a read-through cache guard (persisted scope_of_works_bullets short-circuits the heuristic); new public `computeScopeOfWorksBulletsForApprove` method; `ensurePerRoomBullets` drops `$room['description'] / $room['scope']` fallback (D-01 read-site lock). ProjectPackageReviewController::approve persists bullets via the new compute method BEFORE `$package->update`. `pdf/rams.blade.php` line 834 simplified to `$roomOv['overview'] ?? ''`. **Tests: 38/38 / 75 assertions GREEN across the filter union (RamsReviewDataService, MethodStatementService, RamsComplianceUpgradeService, ProjectPackageReviewController via guard, RamsRenderRegression, ScopeConsolidationGuard, DeadPathRemovalGuard).** RamsRenderRegression canary still 3/3 / 9 assertions GREEN — D-12 byte-equivalence preserved. 1 Rule-3 test-mechanics deviation: comment-strip `preg_replace` added to the D-01 guard so the explanatory inline comment mentioning the removed key doesn't false-positive. **Forward flags for Plan 22.1-06:** `RamsController.php:284` still writes `'works_description'` into `RamsDocument.form_data` (form_data JSON snapshot, NOT `Project.works_description` — out of D-03 scope but worth auditing). `ProjectPackageRamsReviewService.php:78,85` emits orphan `project.overview` key + reads `$package->works_description` as fallback — both safely pruned/tolerated by the now-strict normaliser. Commits: `223d404` (RED guards) + `a6cd568` (D-02/D-09) + `6aa140c` (D-03/D-08) + `dca64cf` (RED cache) + `3f405ae` (D-06/D-01 + controller wire). **DATA-01 complete. Plan 22.1-06 (final verification) unblocked.**

- **Plan 22.1-04 LANDED** (2026-05-13) — DATA-03 shipped. 5 dead-path code surfaces removed: 4 PHP files deleted (`app/Services/RamsGeneratorService.php` + `app/Core/Modules/RAMS/RamsGeneratorService.php` (**Rule-3 deviation** — audit missed the `use App\Core\AI\Prompts\RamsPrompt;` import) + `RamsPrompt.php` + `WorksBulletsPrompt.php`) plus `works_bullets` textarea (review.blade.php 446-469) + `generateWorksBullets` JS function + `generateWorksBullets()` controller method + `works-bullets` POST route + `works_bullets` payload key in `parseReviewPayload()`. SurveyController output array key renamed `works_bullets` → `planned_actions` (**Rule-3 deviation** — data source was already `room_overviews[*].works_summary` line 367, only the legacy key NAME pointed at the deleted field by substring). 9 Alpine bindings in `surveys/show.blade.php` updated via `replace_all`. 4 docblock-example scrubs in `BasePrompt`/`AIManager`/`HazardLibraryService`/`QuoteExtractorService` (**Rule-2** — comment-only references that would trip the static guard). **Static guard test `DeadPathRemovalGuardTest`** (2 tests / 5 assertions / 0.94s / no runtime deps) prevents re-introduction — scans `app/`+`tests/` for the 3 class basenames AND asserts the 4 deleted file paths do not exist. **CLAUDE.md AI-only-for-formatting invariant STRENGTHENED** — `RamsPrompt` deletion was critical because its JSON schema declares AI-generated `hazards.controls` + `regulations`, which would have violated the constraint if invoked. **RamsRenderRegression canary STILL GREEN 3/3 / 9 assertions** both before-Task-1 and after-Task-3. SurveyController existing tests 5/5 / 14 assertions PASS. ~786 lines deleted across the wave; +156 lines (the new guard test). Commits: `25c8b0c` (T1 — file deletions + docblock scrubs) + `6467675` (T2 — textarea + rewire + WorksBulletsPrompt delete + SurveyController rename) + `7def9c9` (T3 — static guard test). **DATA-03 complete. Plan 22.1-05 + 22.1-06 unblocked.**

- **Plan 22.1-03 LANDED** (2026-05-13) — DATA-02 + DATA-04 shipped. Pure-function  +  artisan (dry-run-default,  persists, 4-category report — backfilled/already-set/both-set-no-action/neither-set, idempotent: re-run on backfilled rows lands in  because legacy summary preserved verbatim — Plan 06 owns the schema-trim of on-disk JSON). T-22.1-05 SQL-injection mitigated by  cast on  arg + Eloquent parameterised bindings (test runs  and asserts  survives). Schema-trim:  outputs exactly 4 keys (room/overview/works_summary/solution_type_id) — legacy // dropped, load-time backfill block removed. D-01 input swap:  reads  not , falls back to  joined-paragraph when overview empty. D-07 drop:  legacy fallback removed from  in BOTH  AND  (cache key). AI prompt:  no longer instructs description output — single surviving output is the summary bullet list. Controller shim:  returns engineer-typed  verbatim as the  JSON response key for front-end compat (legacy AI-generated prose is gone, but the response shape is preserved). **RamsRenderRegression canary STILL GREEN** (3/3 tests / 9 assertions) — D-LOCK D-10 byte-equivalence preserved across manual-form/quote-import/survey-derived fixtures. **35 Plan-03 tests / 105 assertions GREEN.** 3 Rule-1/3 deviations: (1) ProjectPackageReviewController downstream-consumer fix, (2) 6 existing MethodStatementServiceTest fixtures migrated description→overview, (3) prompt phrase changed from "overview description" to "overview paragraph" to avoid substring false-positive on schema-lock test. Commits:  (RED T1) +  (GREEN T1) +  (RED T2) +  (GREEN T2). **Deploy runbook (REQUIRED):** production must run  immediately after the code ships — legacy  rows with  set but  empty will render empty bullets until backfilled.

- **Plan 22.1-01 LANDED** (2026-05-13) — DATA-01..05 acceptance criteria transcribed into REQUIREMENTS.md §"Phase 22.1 — RAMS Scope/Room-Data Consolidation" (lines 35-44, +10 lines net, strictly additive — verified by `git diff --stat`). Section landed between Phase 22 (line 25) and Phase 23 (now line 45), matching ROADMAP physical order. Wording verbatim from PLAN.md (no paraphrasing) — calibrated against ROADMAP success criteria #1-5 + CONTEXT.md D-LOCK (D-12 byte-equivalence, D-07 backfill pattern). Milestone-level 'Total requirements: 28' tally intentionally unchanged (DATA-XX are Phase 22.1 internal hygiene, not v2.0 milestone deliverables). All 5 bullets unchecked. Plans 02-06 now unblocked — `requirements: [DATA-0X]` citations resolve cleanly via gsd-tools requirement-coverage check. ROADMAP SC #6 (AI prompt audit) intentionally NOT transcribed — it is a CLAUDE.md compliance invariant satisfied transitively by DATA-03 (deletes `RamsPrompt` because its schema would AI-generate `hazards.controls` + `regulations`, violating the constraint). Zero deviations — 9/9 acceptance criteria PASS (grep checks + Phase 22 < 22.1 < 23 line-order + milestone tally preserved + valid markdown). `gsd-tools frontmatter validate` returned `valid=true`. Commit: `7d41d50`.

- **Plan 22-03 LANDED** (2026-05-12) — Phase 22 COMPLETE. Backfill artisan + pure deterministic resolver shipped. `cables:backfill-port-fks {project?} {--apply}` mirrors `RamsRefreshComplianceCommand` signature precedent but FLIPS the default (dry-run implicit, `--apply` opts in — safer for N-rows × M-projects backfill). 4 outcome categories: `matched` (atomic 4-FK write inside `DB::transaction`) / `ambiguous` (**ALL 4 FKs left NULL — D-LOCK + DRAW-41, NO partial writes** even when resolver returns partial source diagnostics; the only write branch is `if ($apply && $tag === 'matched')` with no else) / `no-device-match` (NULL) / `already-set` (resolver NOT called, row UNTOUCHED → idempotent). `CablePortFkResolverService` is PURE (zero DB writes verified by row-count-before-vs-after across `devices`/`device_ports`/`device_stencils`/`cable_schedule_items`); resolves per-side then aggregates: both matched→matched, both no-device→no-device, anything else→ambiguous with partial diagnostics in return shape. Strict matching against normalised `manufacturer model` / `manufacturer part_no` / bare `model` / bare `part_no` with `strlen($candidate) >= 3` guard — Pitfall 3 mitigation prevents trivial-substring cross-match. Empty `connector_type` ports excluded as unknown — Pitfall 4 mitigation, Tier 1.5 stencils (91/96 seeded per Phase 21 P02) explicitly fail deterministic match. `CABLE_TYPE_TO_CONNECTOR` map covers HDMI→hdmi, CAT6/CAT6a/CAT5e/UTP→rj45, USB/USB-C→usb-c, XLR/RS232/3.5mm/Speakon/Phoenix/DP. **T-22-A5 SQL injection** mitigated by `(int) $this->argument('project')` cast + Eloquent parameterised bindings — feature test runs `cables:backfill-port-fks "5; DROP TABLE devices;" --apply` and asserts `Schema::hasTable('devices')` survives. **T-22-A6 wrong-tenant write** impossible by construction — per-project `Device::where('project_id', $scheduleProjectId)->get()` INSIDE the iteration loop means Project A's devices are never loaded while iterating Project B's items; feature test proves cross-project text match leaves Project B's row entirely NULL even when `--apply` runs against ALL projects. Stencil attachment via `setRelation('stencil', $stencil)` because Device has no native `belongsTo(DeviceStencil)` — stencils are looked up by NORMALISED part_number, not FK; command pre-loads stencils per-project via `DeviceStencil::whereIn('part_number', $partNumbers)->with('ports')` and attaches so resolver reads `$device->stencil->ports` without N+1 inside the loop. **21 new tests / 91 assertions:** 10 unit (exact match / two-device ambiguous / zero-match / two-HDMI-ambiguous / Tier-1.5-zero-ports / source-matched-dest-ambiguous-partial / case-insensitive / CAT6→rj45 / empty-connector-excluded / resolver purity) + 11 feature (dry-run default / --apply persists / project arg scoping / idempotency / T-22-A5 / T-22-A6 / already-set skip / summary line contains all 5 category labels / empty DB / ambiguous→ALL 4 FKs NULL / dry-run tag). Full `--filter=Cable` suite: **73 pass + 4 env-skipped / 330 assertions GREEN** (was 52+4 after Plan 22-02 → +21 exactly accounted for, zero regression). **D-LOCK verified:** zero diff against `app/Services/CableScheduleGeneratorService.php` AND `app/Services/Drawings/*` (v1.3 surface invariant preserved across ALL 3 Phase 22 plans). Commits: `52c2e18` (Task 1 RED) + `ed02cdd` (Task 1 GREEN — resolver + fixture fix) + `2fa1c65` (Task 2 RED) + `a5d32f0` (Task 2 GREEN — command + fixture fix). Two Rule-3 fixture deviations (test mechanics only, zero production-code change): (1) two-device-ambiguous test shares stencil via `attachStencilToDevice` helper because catalog stencils are unique on part_number — matches production catalog-sharing semantics; (2) project-scoping fixture uses `DeviceStencil::firstOrCreate` so calling `makeProjectWithThreeItems()` twice doesn't violate the UNIQUE constraint. **DRAW-40 + DRAW-41 complete. PHASE 22 COMPLETE — all 5 requirement IDs (DRAW-37 schema/model, DRAW-38 picker UI, DRAW-39 compat warning + override note, DRAW-40 deterministic backfill, DRAW-41 one-shot artisan with categorised report) satisfied across the 3 plans. Phase 23 (port-to-port renderer) unblocked — can now consume populated FKs OR gracefully fall back to text-only for ambiguous/no-device-match/Tier-1.5 rows.**

- **Plan 22-02 LANDED** (2026-05-12) — Picker UI + cross-project guard + D-10 regression locked. Alpine `x-data="portPicker(...)"` modal in `resources/views/cable-schedule/_port-picker-modal.blade.php` (single instance per page per D-01, side-by-side SOURCE/DEST per D-02, chain-link icon column per D-03 inserted between From and To making the cable table 9-col, picker overwrites From/To with canonical `"{Manufacturer} {Model} ({Port label})"` on Apply per D-04). JS `isCompatible()` mirrors PHP `CableConnectorCompatibilityService::check` exactly (empty/empty → compatible Pitfall 4, exact → compatible, bidirectional allowlist → compatible with note, else "Connector mismatch:"); yellow override-warning banner + REQUIRED 500-char textarea (DRAW-39) blocks Apply until note has non-whitespace content. "Clear ports on this row" button writes NULL to all 5 FK columns + leaves From/To text intact (Open Question 2). ALL 5 modal buttons carry explicit `type="button"` (Pitfall 5 — verified by grep equality of `<button` count vs `type="button"` count). Picker-trigger button per row has `data-row-index` + `data-fk` hidden inputs; vanilla JS bridge in `edit.blade.php` listens to `port-picker:applied` window events and writes the 5 hidden inputs + overwrites text + flips icon `#1B7A7A` teal (set) / `#bbb` faded (unset). `addRow()` JS template extended to render the chain-link column too. `CableScheduleController@update` extended with 5 new validation rules (`exists:devices,id` + `exists:device_ports,id` + `connector_override_note nullable string max:500`) PLUS T-22-A4 cross-project FK injection guard — single `Device::whereIn('id', $submittedDeviceIds)->where('project_id', '!=', $cableSchedule->project_id)->count()` query AFTER `validate()` and BEFORE `DB::transaction()` so `items()->delete()` never runs on failed validation. On mismatch throws `ValidationException::withMessages(['items.0.source_device_id' => '...'])` + `Log::warning` with project_id/user_id/submitted_device_ids. Skipped when `project_id IS NULL` (legacy standalone). Port FKs NOT re-validated cross-project — they're stencil-scoped (cross-project shared). `@edit` eager-loads `items.sourceDevice/sourcePort/destDevice/destPort` AT THE CALL SITE only (D-10 guard — never on `$with`) + builds `$devicesWithPorts` via direct `Device::where('project_id')->with(['stencil.ports' => fn ($q) => $q->orderBy('side')->orderBy('sort_order')])` (resolves A2 — engineers can distinguish multiple physical units of the same model on different rows). 4 new test files + 1 extended: `CableScheduleUpdatePersistsPortFksTest` (3 tests / 20 assertions — picker round-trip + canonical labels + legacy NULL-FK row), `CableScheduleUpdatePersistsOverrideNoteTest` (3 / 10 — DRAW-39 not a hard block, override persists with or without note, 500-char limit), `CableScheduleCrossProjectFkInjectionTest` (5 / 18 — form-encoded + JSON `putJson` + pre-seeded row canary proves no destructive write on validation fail + nonexistent device_id + T-22-A1 mass-assignment), `CableScheduleXlsxRegressionTest` (3 / 50 — 2 PhpSpreadsheet-dependent runtime tests skip cleanly in dev env via `class_exists(...Spreadsheet::class)` guards mirroring D2-binary skip pattern + 1 static source-level D-10 guard scanning 5 v1.3 surface files for forbidden column names + relationship method invocations). `SchematicGeneratorServiceTest` extended with `test_null_fk_cables_render_byte_identical_to_populated_fks_d10_invariant` (skips when D2 binary unavailable, inline `is_file($binary)` pattern per plan instruction). **12 dev tests pass + 3 env-skipped / 116 assertions; full `--filter=Cable` 52 pass / 4 skip / 239 assertions GREEN.** D-10 grep gate verified: zero matches for `source_port_id|dest_port_id|connector_override_note|source_device_id|dest_device_id` across the 5 v1.3 surface files. Static D-10 guard fires first on dev (no PhpSpreadsheet) as canary. T-22-A4 BLOCKING gate passes (5 tests / 18 assertions). View render smoke: modal partial renders 6059 bytes cleanly. Two minor Rule-3 deviations auto-fixed (test mechanics only, zero production-code change): (1) PhpSpreadsheet absent in dev env — added `class_exists` skip guards on byte-identity tests + introduced complementary static source-level D-10 guard that runs everywhere; (2) static guard regex tightened from bare `sourcePort`/`destPort` tokens to `->sourceDevice`/`->sourcePort`/`->destDevice`/`->destPort` method-invocation forms because `SchematicD2SourceBuilder.php:150-205` uses those identifiers as Phase 17 local variable names holding source_port/dest_port STRINGS from `extracted_data['cables']` — unrelated to Phase 22 Eloquent relationships. Zero behavioural deviations. Commits: `9f72e87` (Task 1 guard + 3 test files) + `21690ca` (Task 2 modal + 9-column edit + @edit eager-load + Device import) + `21db172` (Task 3 D-10 regression tests + SchematicGenerator extension). **DRAW-38 + DRAW-39 complete (picker UI + server-side rules + override-note workflow + cross-project FK guard all shipped). Plan 22-03 (backfill command) unblocked.**

- **Plan 22-01 LANDED** (2026-05-12) — Strictly-additive port-FK foundation for Phase 22. 5 nullable columns + 1 compound index on `cable_schedule_items` via `2026_05_15_000000_add_port_fks_to_cable_schedule_items` migration: `source_device_id` / `source_port_id` / `dest_device_id` / `dest_port_id` all `nullOnDelete` (preserves text rep on Device deletion — same state as never-FK'd legacy row), `connector_override_note` text nullable, `cable_schedule_items_port_pair_idx` on `(source_port_id, dest_port_id)` for Phase 23 renderer port-pair lookups. Generic D-09 naming (no rams_/project_ prefix) for SCC merge readiness. `CableScheduleItem` extended to 14 `$fillable` keys (9 originals + 5 Phase 22) + 4 belongsTo relations (sourceDevice/sourcePort/destDevice/destPort) — NO `$with` property (D-10 guard reflection-asserted via `ReflectionProperty(CableScheduleItem::class, 'with')`; class-level eager-load would force 4 LEFT JOINs across XLSX export + bound-PDF + schematic generator read paths). `config/cables.php` ships 4 bidirectional alias pairs (HDMI↔DP active adapter, USB-C↔Thunderbolt, RJ45↔SFP+, USB-C→HDMI Alt Mode) + 8-key `signal_type_colours` map for Phase 23 — single source of truth shared with picker UI via `@js(config('cables.compatibility_aliases'))`. `CableConnectorCompatibilityService::check(string $src, string $dst)` pure-function over config: exact match returns `{compatible:true, reason:null}`; case-insensitive trim normalisation; bidirectional allowlist match returns reason from `note` (A4 — HDMI↔DP works both ways); mismatch returns `Connector mismatch: $src → $dst`; Pitfall 4 — empty/whitespace connector_type returns compatible with "connector type not catalogued — assume compatible" (Tier 1.5 stencil tolerance: 91/96 seeded stencils still need Phase 24 curation). T-22-A1 mass-assignment mitigated by `$fillable` whitelist — `test_unknown_keys_are_dropped_by_fillable` proves unknown items[N][*] keys are silently dropped. **24 new tests pass (5 Feature migration + 7 Unit model relations + 12 Unit service / 77 assertions). 41 tests / 142 assertions across `--filter=Cable|Connector` GREEN with 1 pre-existing D2-binary skip.** D-10 verified: zero diff against SchematicGeneratorService / SchematicD2SourceBuilder / DrawingDataResolverService / CableScheduleXlsxService / CableScheduleGeneratorService. Migration roundtrip clean (`migrate:fresh` 244ms + `rollback --step=1` 308ms + re-apply 275ms). Commits: `ddf12db` (Task 1 migration + tests) + `7c1c55b` (Task 2 model + relation tests) + `040db26` (Task 3 config + service + tests) — TDD RED→GREEN per task. Two minor test-fixture deviations auto-fixed (Rule 3): (1) `Device::create` required `description` field in Task 1 nullOnDelete test fixture (SQLite NOT NULL constraint); (2) swapped `create()` for `forceFill()->save()` on the 2 Task 1 tests that exercise new FK columns before Task 2's `$fillable` whitelist lands — both are test-mechanics adjustments, no production-code change. Zero behavioural deviations. **DRAW-37 + DRAW-39 partially satisfied (schema + model + config + compat service — picker UI + update handler land in Plan 22-02; backfill command in Plan 22-03). Plans 22-02 + 22-03 both unblocked.**

- **Plan 21-03 LANDED** (2026-05-10) — Phase 21 COMPLETE. DRAW-35 manufacturer logos + DrawIoBuilderService rewire shipped. 15 new SVG glyphs at `public/img/manufacturers/` per D-06 (viewBox 0 0 100 30, currentColor, ~400-700 bytes each): crestron/cisco/qsc/bogen/polycom/logitech/shure/sony/extron/biamp/yamaha/atlona/lightware/q-sys/barco. Total directory now **20 SVGs** (5 spike PRESERVED + 15 new). `ManufacturerLogoResolver` case-insensitive substring lookup with most-specific-first needle ordering: q-sys before qsc (avoid 'qsc' substring matching 'q-sys'); clickshare before barco per D-14 (Barco ClickShare products keep using existing clickshare.svg; non-ClickShare Barco F50 etc fall through to new barco.svg); poly aliased to polycom. resolveSvg / resolveAssetPath / knownManufacturers public contract; per-instance file-read memoisation via string|false sentinel. `DrawIoBuilderService` reads from `Project::devicesWithStencils()` (Plan 21-01 cache contract) — every hardware part_number lands a stencil cell. Curated stencils from Plan 21-02 seed pack render with full mxgraph_xml base64-embedded via `shape=stencil(...)`; uncatalogued part_numbers auto-create as Tier 1 placeholders on first read. Layout heuristic INTENTIONALLY shallow per Nit 9 — 4-column grid (videobar/byod/mic | switch | display | other) keyed off the 5 spike-promoted slugs ONLY; NO port-composition heuristic; TODO(phase-23) inline marker. Cable derivation preserved verbatim from spike (canonical Teams Room signal chain); Phase 22 replaces with port-FK lookup. `DrawIoSpikeBuilderService` collapsed from ~560 lines to a 10-line backwards-compat shim per D-08 (@deprecated docblock; class preserved). `DrawIoSpikeController` constructor first parameter type-hint flipped to `DrawIoBuilderService`; second parameter `DrawingService $drawings` PRESERVED per D-08 + Warning 2 (consumed by saveXml/exportSvg) — reflection-asserted in `DrawIoBuilderServiceTest`. D-10 verified: zero diff against `DeviceCatalogService` / `SchematicGeneratorService` / `SchematicD2SourceBuilder` / `DrawingDataResolverService` / `device-port-catalog.json`. D-14 verified: `clickshare.svg` untouched. **20 plan tests / 68 assertions pass (14 ManufacturerLogoResolver + 6 DrawIoBuilderService). Full Drawings suite 131/1633 GREEN with zero regression vs Plan 21-02 baseline (1 pre-existing D2-binary skip on dev).** Commits: `6e6b174` RED + `233dbfc` GREEN (Task 1 logos+resolver); `7d452b5` RED + `5936feb` GREEN (Task 2 builder+shim+controller). Zero deviations — plan executed exactly as written. **DRAW-35 complete. Phase 21 COMPLETE — all 6 requirement IDs (DRAW-31, 32, 33, 34, 35, 36) satisfied across the 3 plans.**

- **Plan 21-02 LANDED** (2026-05-10) — Engineer-curated seed pack v2.0 foundation in place. 5 spike stencils promoted to per-file curation manifests at `resources/data/device-stencils-seed/{slug}.json` with full mxgraph_xml lifted verbatim from spike-260509-ibx + 40 ports total (Neat Bar Pro 6 / Samsung QM65C-T 9 / ClickShare Bar Pro 7 / Sennheiser TCC2 4 / Netgear GS312TP 14). ALL 53 v1.3 device-port-catalog entries promoted as Tier 1.5 stencils in `_v1.3-promoted.json` (auto-generic body shell from AutoGenericStencilGenerator, source=engineer-curated, metadata.needs_phase_24_curation=true; rack metadata u_height/is_rack_mounted/current_draw_a etc preserved in metadata for cross-reference). 39 gap-fill stencils in `_top-50-gap.json` derived from local quote-volume data (4 ProjectPackage rows / 41 unique non-noise hardware part_numbers) with prefix-heuristic manufacturer derivation; metadata.manufacturer_derivation=`unknown-prefix-needs-curation` flag for 9 unidentified-prefix entries. DeviceStencilSeedReader auto-detects per-file vs bulk manifest shape (top-level `part_number` => per-file; top-level `stencils` => bulk flat-mapped); throws RuntimeException with file path + missing field on schema violation; memoised per-instance. DeviceStencilSeeder idempotent — second run produces zero new rows; manual port edits wiped on reseed (manifest is source of truth); wrapped in DB::transaction (T-21.02-03). DatabaseSeeder.php updated to call DeviceStencilSeeder after DeviceCatalogSeeder. Live tinker counts: **96 engineer-curated stencils + 40 ports + 91 needs_phase_24_curation**. SeedPackCoverageTest proves **95.1% coverage** of INDEPENDENT top-41 reference (well above 80% D-05 threshold); fixture at `tests/Fixtures/seed-coverage/top-50-snapshot.json` carries `_provenance` field per D-15 forbidding regeneration from seed pack. **20 plan tests / 1230 assertions pass. 73 / 1448 across full drawings suite (zero regression vs Plan 21-01)**. D-10 verified: zero diff against device-port-catalog.json + DeviceCatalogService + DeviceCatalogSeeder + SchematicGeneratorService + SchematicD2SourceBuilder. D-14 verified: clickshare-bar-pro.json carries manufacturer=Barco BUT logo_svg_path=/img/manufacturers/clickshare.svg. Commits: `46d10b3` RED + `f97379b` GREEN (Task 1 reader); `47c7e24` (Task 2 Step A v1.3 promotion); `d66fadc` RED (Task 2 Steps B+C+E gap manifest + tests); `c218ce0` GREEN (Task 2 Step D seeder + DatabaseSeeder + noise-filter fix). Three minor auto-fixed deviations: (1) noise filter on snapshot fixture (numeric-only SKUs + 'existing' literal placeholders pushed coverage from 78% to 95%); (2) tests/Fixtures path casing correction for Linux production safety; (3) test class seed() method conflict with Illuminate base — replaced with artisan db:seed call. **DRAW-33 complete. Plan 21-03 unblocked.**

- **Plan 21-01 LANDED** (2026-05-10) — v2.0 Engineering-Grade Drawings foundation in place. Two new generic-named tables (device_stencils + device_ports per D-09) with FK cascade and compound unique on (device_stencil_id, port_id). DeviceStencil + DevicePort Eloquent models with enum-style SOURCE_AUTO_GENERATED/ENGINEER_CURATED/AI_EXTRACTED, SIDE_LEFT/RIGHT/TOP/BOTTOM, DIRECTION_IN/OUT/IO constants matching Device::ROLE_* convention. AutoGenericStencilGenerator emits a 220x140 brand-aligned `<shape>` per D-04 (teal #1B7A7A header / #FAFAF6 cream body, manufacturer/model/part_number text, "Tier 1 placeholder" annotation, NO port rails — Phase 24 adds them). XML-escapes every interpolated value via htmlspecialchars(ENT_XML1|ENT_QUOTES) — T-21.01-01 mitigation mirroring DrawIoSpikeBuilderService::xml() pattern. Deterministic across calls; DOMDocument::loadXML returns YES. DeviceStencilCacheService implements firstOrCreate(part_number) cross-project caching contract per D-03 with cache-hit short-circuit BEFORE building (Mockery-asserted: generator NOT invoked on hit). Race-safety rationale documented on docblock — no DB::transaction wrap; unique index makes firstOrCreate atomic; transaction would HURT throughput per T-21.01-03. resolveMany returns enriched lines with stencil = null for empty part_numbers. Project::devicesWithStencils() accessor reads latestPackage->extracted_data['equipment'] (fallback equipment_list), filters category=hardware, routes through cache service so uncatalogued part_numbers auto-create Tier 1 placeholders on first read. Side-effect + race-safety rationale documented in docblock per D-07 + D-03. 35 tests / 126 assertions pass. 89/330 tests in full drawings suite pass (1 pre-existing D2 skip). D-10 verified: zero diff against v1.3 D2 generator surface (DeviceCatalogService / SchematicGeneratorService / SchematicD2SourceBuilder / DrawingDataResolverService / device-port-catalog.json). Commits: 72f7e94+2e333ae (Task 1) + e63da58+06c9052 (Task 2) + 8711123+45ee705 (Task 3) — TDD RED→GREEN per task. Zero deviations. Plan 21-02 + 21-03 unblocked.

### Key Decisions (v1.3)

- **Plan 20-02 LANDED** (2026-05-03) — Phase 20 COMPLETE. v1.3 ready for `/gsd-complete-milestone`. Production hardening: drawings:audit-licenses Artisan command (composer + npm GPL/AGPL detector with --strict mode + pre-existing allowlist for mpdf/dompdf/smalot/tcpdf/nette — predate Phase 20 per Plan 20-01 SUMMARY; live audit clean against 142 composer + 229 npm deps); dedicated 'drawings' queue connection in config/queue.php (driver=database, queue=drawings, retry_after=600 vs default 90 — bound PDFs need 10 min vs 90s; BuildBoundPdfJob already targets via constructor onQueue from Plan 20-01); CHROME_HEADLESS_SHELL_VERSION=147.0.7727.57 pin in .env.example with bump procedure; @font-face Liberation Sans (regular + bold) + DejaVu Sans (regular) declarations in all 3 drawing Blade views (schematic, rack, bound-cover) with font-display: block + graceful fallback chain (Arial → Helvetica → Liberation Sans → DejaVu Sans → sans-serif); public/fonts/.gitkeep so directory exists in git (binaries 100KB+ deploy via runbook); 218-line docs/runbooks/drawings-queue-runbook.md (why-separate-queue + supervisor numprocs=1 --memory=512 --max-jobs=10 --timeout=600 --tries=2 + chrome upgrade procedure with smoke-test gate + license audit deploy gate + fonts setup graceful degradation). Two NON-changes verified: OmManualDocxService Drawings loop already kind-agnostic (Phase 17 P03 + Phase 18 P03 — Plan 20-02 added regression test asserting both kindLabels + 2 <v:imagedata> entries lock the behaviour against future v1.3.x regressions; PhpWord 1.4 emits images via legacy VML <v:imagedata> NOT modern <w:drawing>); PdfRenderService::fromBlade + fromBladeAsPng both still have disable-dev-shm-usage Chromium argument (grep -c returns 4 — Plan 20-01 BoundPdfBuilderService work did NOT remove it; PdfRenderService is single Browsershot hardening surface per Warning 8). pdf:smoke-test --drawings extended to render BOTH schematic + rack via paired helpers renderSchematicSmoke + renderRackSmoke, exit FAILURE if either zero bytes (CRIT-04). 7 plan tests / 25 assertions: 2 OmManualEmbedsRackTest (embed_both + skip_failed_renders), 2 PdfSmokeTestRackTest (renders_rack + fails_when_rack_zero_bytes), 3 AuditDrawingLicensesTest (clean_state + GPL_simulated + LGPL_strict_only) via stub-via-subclass pattern overriding protected runComposerLicenses + runNpmLockGrep helpers. 72-test drawings/console suite green (1 D2 skip, +7 from Wave 1's 65 baseline). Commits: c065b17 + 79c68d2 + e882f40. Deviations: PhpWord <v:imagedata> not <w:drawing> auto-fixed during RED; pre-existing GPL allowlist auto-added to AuditDrawingLicensesCommand to satisfy 'exits 0 against current clean composer state' acceptance criterion.
- **Plan 20-01 LANDED** (2026-05-03) — DRAW-21 bound PDF + DRAW-23 sheet numbering + DRAW-28 ZIP bundle shipped. New: SheetNumberAllocator (AV-201..299 schematics, AV-301..399 racks, set-once on DrawingService::createForProject; superseded rows skipped from count; floor-plan throws InvalidArgumentException — v2.0); BoundPdfBuilderService (FPDI page-by-page concat of cover sheet + every per-drawing PDF; per-drawing failures isolated via try/catch — register row prefixed `[render failed]`, whole bound PDF still completes; on-disk version scan via glob `drawings/bound-{projectId}-v*-*.pdf`); BuildBoundPdfJob ($projectId not $drawingId — project-level artifact; tries=2; timeout=300; queue='drawings' via constructor onQueue() because typed `public string $queue` triggers PHP fatal vs untyped Queueable trait; WithoutOverlapping middleware keyed by bound-pdf-{projectId} releaseAfter(60s)); BoundPdfReadyMail (Project-typed, basename'd PDF attachment); pdf/drawings/bound-cover.blade.php (A4 portrait cover + drawing register table with red-row failure highlighting + banner); pdf/drawings/_title-block.blade.php gets new Sheet row consuming $drawing->sheet_number with @if defensive guard for pre-Phase-20 rows. Three new routes registered BEFORE {drawing} wildcard show route: GET projects/{project}/drawings/bound-pdf (downloadBoundPdf — fresh streamed OR ≤3-drawing inline build OR async dispatch), POST projects/{project}/drawings/bound-pdf/build (regenerateBoundPdf — always async), GET projects/{project}/drawings/bundle.zip (downloadBundle — ZipArchive on-disk build → streamDownload, EVERY addFile via basename($realPath) per T-20-02 + drawing-register.csv via addFromString). Index controller computes $boundPdfStaleBadge (latest mtime vs max drawings.updated_at — MOD-10) → amber pill in index Blade. Index Blade ships Project Documents block (bound PDF + ZIP buttons) + Sheet column on schematic + rack rows. New deps: setasign/fpdi:^2.6 (MIT) + setasign/fpdf:^1.8.6 (permissive — bumped from ^1.8.0 due to PHP 8 fatal in get_magic_quotes_runtime()). Composer downgraded symfony/http-client + symfony/postmark-mailer 8.x → 7.x as side effect (no test regressions). 12 plan tests / 35 assertions: 5 SheetNumberAllocator (block bases, superseded skip, floor-plan throw), 5 BoundPdfBuilder (3-page concat, failure isolation, floor-plan exclusion, kind-group order, ULID filename pattern), 4 BoundPdfDownload (200+%PDF body, 403 non-owner, regen-needed badge after touch, regenerate POST dispatches BuildBoundPdfJob), 3 ZipBundleDownload (ZIP regex'd entries for bound + per-kind-PDF/SVG/PNG + register.csv, no `..`/`/`/`\` in entry names, 403). Deviations: SQLite no FIELD() → CASE-based portable ordering (works MySQL+SQLite); literal-segment routes BEFORE {drawing} wildcard (Phase 18 precedent). 65-test drawings suite green (Phase 17/18 zero regressions). 14 drawings routes total. Commits: feefb41 + 428690d + 184beec.
- **Plan 18-03 LANDED** (2026-05-02) — Phase 18 COMPLETE. RackElevationRenderService synchronous custom Blade SVG renderer (~340 LOC, measured 0.06s for full 42U/30-items vs 1s budget — Warning 8 fix); rack editor Blade view with Alpine + Sortable.js drag-into-U-slots palette + 42U scaffold + per-item lock toggle + cursor-walk lock-aware reorder algorithm in JS; AJAX saveRackCanvas endpoint validates JSON allow-list, runs render synchronously, flips status to ready (or failed on render exception); flipRackMountedFlag endpoint authorises against ProjectPolicy::update (project-scoped, owner-OR-admin) so it works BEFORE any rack drawing exists (Blocker 2 fix from checker iteration 2); pdf/drawings/rack.blade.php landscape A4 view; DrawingExportRendererService::bladeViewFor rack arm now returns 'pdf.drawings.rack' (PDF/SVG/PNG endpoints all light up); Sortable.js dependency in dedicated Vite entry (39 kB gzip 14 kB chunk separate from Alpine bundle); show.blade.php Edit Rack button next to Download buttons (existing kind-agnostic line-66 SVG render branch UNCHANGED — Warning 9 fix). NEW App\Policies\ProjectPolicy registered (Phase 17 didn't ship one; deviation Rule 2). 20 new test cases / 99 assertions: render kind guard, 42U rail, item placement, partial-data asterisks/ratios, CRIT-06 unknown-u_height warning, lock annotation, 1s render budget, XSS escape, edit-page render/404/403, save-canvas success/422/extra-key-drop/lock-roundtrip/cursor-walk-Warning-7, flipRackMounted update / pre-rack regression / non-owner 403. DRAW-07 / DRAW-08 / DRAW-09 (partial — palette ordering + bottom-up rendering, AVIXA auto-place algorithm deferred to v1.3.x or v2.0) / DRAW-10 / DRAW-11 / DRAW-12 / DRAW-13 covered. 11 drawings routes total (6 P17 + 2 P18-01 + 3 P18-03). 48 Drawings tests pass + 1 expected D2 skip on dev.
- **Plan 18-01 LANDED** (2026-05-02) — Phase 18 foundations: devices.u_height + ventilation/is_rack_mounted columns (CRIT-06 nullable-first); 53-entry hand-curated manufacturer JSON pack at resources/data/device-port-catalog.json + DeviceCatalogService memoised reader + idempotent DeviceCatalogSeeder (whereRaw LOWER(TRIM) bound parameter — devices outside the pack stay NULL); DrawingService::generateInitial dispatches by kind via match (schematic = Phase 17 async, rack = synchronous + 42U + 230V scaffold, floor_plan deferred to v2.0); DrawingDataResolverService::rackStackForProject body filled with rack-mounted-first palette; ProjectDrawingController::picker + createRack actions; unified Alpine + Create Drawing modal replaces per-kind buttons (Floor Plan card disabled with "Coming in v2.0" tooltip). 24 new test cases / 72 assertions. DRAW-08 (schema), DRAW-09 (palette ordering — partial), DRAW-11 (multi-rack picker), DRAW-12 (metadata schema) covered.
- **Plan 17-03 LANDED** (2026-05-01) — Drawings render UI + O&M handover wiring complete. DrawingExportRendererService delegates PNG to PdfRenderService::fromBladeAsPng (Warning 8 paid off — Phase 20 CRIT-03 hardening lands in one place). OmManualDocxService Drawings section opens fresh `$drawingsSection = $phpWord->addSection(...)` (Blocker 3 fix — no `$section` reuse). createSchematic uses generateInitial not regenerate (Warning 9 — first version is R0). BuildSchematicJob thumbnail block inserted disjoint from Plan 02's mail dispatch (Warning 6 preserved; DrawingReadyMail/completion_email_sent_at grep returns 6). pdf:smoke-test --drawings flag delivered. 6 drawings routes registered. DRAW-05 (UX scaffolding only) / DRAW-06 / DRAW-26 / DRAW-27 covered. Phase 17 complete (3/3 plans).
- **Plan 17-02 LANDED** (2026-05-01) — D2-driven schematic generator: SchematicGeneratorService + SchematicD2SourceBuilder with full sanitiseLabel() escape (Warning 7), 25-symbol AVIXA-aligned SVG pack (~18 KB), config/drawings.php (D2 binary path + signal-type colour map), DrawingDataResolverService::adjacencyForProject body filled, BuildSchematicJob wired to real generator (placeholder removed; Plan 03 thumbnail-render marker left). 5 feature tests (4 pass deterministic, 1 skips when D2 binary missing on dev). DRAW-01/02/03/04/22 + CRIT-05 + Warning 7 mitigated.
- **Plan 17-01 LANDED** (2026-05-01) — shared drawings foundations now live: project_drawings table, ProjectDrawing model + policy, TYPE_DRAWING storage type, DrawingService (createForProject/generateInitial/regenerate/archivePrior), BuildSchematicJob skeleton with full handle()+failed() bodies, DrawingReadyMail single mailable, DrawingEditAdapter scaffolding, PdfRenderService::waitForJs option + new fromBladeAsPng method.
- **Phase 17 owns shared foundations** — `project_drawings` table, `ProjectDrawing` model + policy, `DocumentArtifactStorage::TYPE_DRAWING`, `BuildSchematicJob` + base job pattern, `DrawingReadyMail` (single mailable + kind discriminator), `PdfRenderService::waitForJs` extension, `DrawingEditAdapter` (extends existing DocumentEdits). Phases 18/19/20 become pure additions.
- **One `project_drawings` table, kind discriminator** — over three near-identical models (mirrors H-07 collapse to one `DocumentArtifactStorage`).
- **Konva.js (vanilla, MIT) over React canvas libs** — TLDraw / Excalidraw rejected (React-only conflicts with Alpine.js stack; ~600 KB framework cost for one screen).
- **D2 CLI for schematics** — server-side native binary (MPL-2.0), SVG output, no headless browser at gen time.
- **Custom Blade SVG for racks** — racks are list-shaped not graph-shaped; D2 + Konva are overkill.
- **DXF stretch goal only (Phase 20)**; DWG is out of scope (LibreDWG GPLv3 = hard blocker).

### Open Decisions

- **GAP-3** ~~Phase 19 mandatory Browsershot+Konva spike~~ — RESOLVED: Phase 19 deferred to v2.0 (2026-05-02). Spike no longer needed in v1.3.
- **GAP-4** ~~Schematic edit-override Phase 19 placement~~ — RESOLVED: full Konva-based schematic editor deferred to v2.0 alongside floor plans. Phase 17 lock-on-edit prompt UX scaffolding stays as the v1.3 ceiling for DRAW-05.
- **GAP-5** — `Device` schema migration timing: `u_height` (decimal) + `requires_ventilation_gap_above/below` (boolean) + `is_rack_mounted` must land in Phase 18 plan 18-01 before the rack editor runs.
- **NEW (2026-05-02)** — Build-vs-buy spike for v2.0 should run when v1.3 ships. 1-week investigation: Lucidchart API / draw.io embed / XTEN-AV / D-Tools. Decides whether v2.0 is native build (~14 weeks) or vendor integration (~3-4 weeks). See memory `v2_engineering_grade_drawings_plan.md`.

### Blockers / Risks

- **CRIT-01..CRIT-06** documented in `.planning/research/PITFALLS.md`; risk register attached to each phase via canonical refs
- Outstanding human UAT carried from v1.2 (Gantt browser confirmation, iOS HEIC end-to-end) — deployment-gated, not blocking v1.3 roadmap

### Todos

- Plan Phase 17 via `/gsd-plan-phase 17`
- Confirm DRAW-29 (DXF) stretch-goal stance with engineering leadership before Phase 20 starts (per GAP-1 in research SUMMARY)

## Session Continuity

**Last session ended:** 2026-08-14 — Plan 24-02 (Phase 24 Wave 2: `QuoteImportStencilStubber` service wired into all 3 quote-import call sites, D-09) completed in ~35 minutes / 2 commits / 5 files (2 created + 3 modified). See the Plan 24-02 LANDED entry under Key Decisions (v2.0) above for full detail. **6/6 new tests / 36 assertions GREEN**; broader Drawings regression suite 197 passed / 2 skipped with the same 2 pre-existing unrelated `DrawIoSpikeController` failures already in deferred-items.md. Commits: `bd3e76c` + `2e6dc8d`. **Plan 24-03 (admin list view, Wave 3) unblocked.** ⚠️ `php artisan migrate` MUST still run on live before Plan 24-02's code is deployed — Plan 24-01's migration has not yet been confirmed live.

**Earlier session ended (2026-08-14):** Plan 24-01 (Phase 24 Wave 1 foundation: needs_review/logo_path/device_stencil_audits schema, CategoryPortTemplateResolver, AutoGenericStencilGenerator D-05 provisional-rail extension) completed in ~55 minutes / 3 commits / 10 files (6 created + 4 modified). See the Plan 24-01 LANDED entry under Key Decisions (v2.0) above for full detail. **27/27 new tests / 115 assertions GREEN**; full Drawings regression suite 246 passed / 2078 assertions with 2 pre-existing unrelated failures logged to deferred-items.md. Commits: `7e77cb9` + `2e8bc7d` + `7d93fbd`. **DRAW-51 complete.**

**Earlier session ended (2026-05-13):** Plan 22.1-05 (DATA-01 consolidation: works_description auto-seed drop, method_statement_notes ↛ Project.works_description, PM-INSTRUCTIONS suffix removed, scope_of_works_bullets persisted at approve-time as read-through cache, ensurePerRoomBullets description-fallback dropped, blade per-room chain simplified, normaliseProject overview key removed) completed in 35 minutes / 5 commits / 10 files (2 created + 8 modified). 6 surgical changes ship the DATA-01 invariant — one canonical scope location, no mirrored writes. **38/38 / 75 assertions GREEN** across RamsReviewDataService (6/15) + MethodStatementService (13/26) + RamsComplianceUpgradeServiceCache (6/6) + ScopeConsolidationGuard (8/14) + DeadPathRemovalGuard (2/5) + RamsRenderRegression (3/9). Canary byte-identity preserved across all three fixtures (manual-form / quote-import / survey-derived). One Rule-3 test-mechanics deviation: D-01 guard strips `//` and `/* */` comments from the extracted method body before substring check so explanatory inline comments mentioning the removed key don't false-positive. **Two forward flags for Plan 22.1-06 final verification:** (1) `RamsController.php:284` writes `'works_description'` into `RamsDocument.form_data` JSON snapshot column (NOT Project.works_description — out of D-03 scope but should be audited as part of the full DATA-01 surface walk); (2) `ProjectPackageRamsReviewService.php:78,85` emits an orphan `project.overview` key + reads `$package->works_description` as a fallback — both safely pruned/tolerated by the now-strict `RamsReviewDataService::normaliseProject` but worth confirming via the structural-JSON-diff D-13 assertion Plan 22.1-06 owns. Commits: `223d404` + `a6cd568` + `6aa140c` + `dca64cf` + `3f405ae`. **Plan 22.1-06 unblocked.**

**Earlier session (2026-05-12):** Plan 22-02 (picker UI + cross-project FK guard + D-10 regression locked) completed in 28 minutes / 3 commits / 8 files (5 created + 3 modified). Alpine `x-data="portPicker(...)"` modal at `resources/views/cable-schedule/_port-picker-modal.blade.php` (single instance per page per D-01, side-by-side SOURCE/DEST per D-02, chain-link icon column inserted between From and To making the cable edit table 9-col per D-03, picker overwrites From/To with canonical `"{Manufacturer} {Model} ({Port label})"` on Apply per D-04, all 5 modal buttons carry `type="button"` per Pitfall 5). JS `isCompatible()` mirrors PHP `CableConnectorCompatibilityService::check` exactly (empty/empty → compat with Pitfall 4 note, exact → compat, bidirectional allowlist → compat with alias note, else mismatch); yellow override-warning + REQUIRED 500-char textarea blocks Apply until non-whitespace content (DRAW-39 client gate). "Clear ports on this row" button writes NULL to all 5 FK columns + leaves From/To text intact (Open Question 2). `CableScheduleController@update` extended with 5 new validation rules + T-22-A4 cross-project FK injection guard — single `Device::whereIn(...)->where('project_id', '!=', $cableSchedule->project_id)->count()` AFTER `validate()` and BEFORE `DB::transaction()` so `items()->delete()` never runs on failed validation (pre-seeded row canary proves no destructive write). `@edit` eager-loads `items.sourceDevice/sourcePort/destDevice/destPort` AT THE CALL SITE only (D-10 — never on `$with`) + builds `$devicesWithPorts` payload via direct `Device::where('project_id')->with(['stencil.ports' => fn ($q) => $q->orderBy('side')->orderBy('sort_order')])` (resolves A2 — engineers can distinguish multiple physical units of the same model). **12 dev tests pass + 3 env-skipped / 116 assertions; `--filter=Cable` 52 pass / 4 skip / 239 assertions GREEN.** D-10 grep gate verified: zero matches across 5 v1.3 surface files. T-22-A4 BLOCKING gate passes (5 tests / 18 assertions — form + JSON `putJson` + pre-seeded canary + nonexistent device + T-22-A1 mass-assignment). Commits: `9f72e87` + `21690ca` + `21db172`. Two Rule-3 test-fixture deviations auto-fixed (PhpSpreadsheet absent in dev env → `class_exists` skip mirroring D2 binary pattern + complementary static D-10 source-scan guard that runs everywhere; static guard regex tightened to `->sourceDevice/Port/destDevice/Port` method-invocation forms to avoid matching SchematicD2SourceBuilder Phase 17 local variable names). Zero behavioural deviations. **DRAW-38 + DRAW-39 COMPLETE (picker UI + server-side rules + override-note workflow + cross-project FK guard all shipped). Plan 22-03 (backfill command) unblocked.**

**Stopped at:** Phase 27 context gathered

**Next session starts:** Plan 24-03 (`/admin/device-stencils` route + nav entry + filterable/searchable index — DRAW-50, depends on 24-01). Live deploy note: neither Plan 24-01's migration nor Plan 24-02's code have been confirmed live yet — `php artisan migrate` is a hard prerequisite before Plan 24-02's stubber (which reads/writes `needs_review` + `device_ports`) is deployed, and both should land before Plan 24-03's admin screen is opened on live.

**Historical entry (2026-05-13), superseded above:** Plan 22.1-06 was the "next session" pointer as of the 2026-05-13 session; it has since landed (see the Key Decisions v2.0 log) along with all of Phase 22.1, 23, and Phase 24 Plan 01.

**Earlier session (2026-05-12):** Plan 22-01 (schema + model + config + compat service — Phase 22 foundation) completed in 7 minutes / 3 commits / 7 files. 5 nullable port-FK columns + compound index, `CableScheduleItem` 14-key `$fillable` + 4 belongsTo, `config/cables.php`, `CableConnectorCompatibilityService::check()`. Commits: `ddf12db` + `7c1c55b` + `040db26`.

**Earlier session (2026-05-10):** Plan 21-03 (manufacturer logos + DrawIoBuilderService rewire) completed in 9 minutes / 4 commits / 21 files. Phase 21 COMPLETE — all 6 DRAW IDs satisfied.

**Earlier session (2026-05-10):** Plan 21-02 (engineer-curated seed pack: 5 spike + 53 v1.3 promoted + 39 gap-fill + idempotent seeder) completed in 21 minutes / 5 commits / 19 files.

**Earlier session (2026-05-10):** Plan 21-01 (schema + models + cache service + Project accessor — v2.0 foundation) completed in 13 minutes / 6 commits / 10 files.

**Earlier session (2026-05-03):** Plan 20-02 (production hardening + O&M rack embed) completed in 13 minutes / 3 commits / 12 files — Phase 20 COMPLETE; v1.3 milestone READY for `/gsd-complete-milestone`.

## Roadmap Overview

| Milestone | Theme | Phases | Status |
|-----------|-------|--------|--------|
| v1.0 | RAMS MVP | 01–07 | ✅ Shipped |
| v1.1 | Operations Dashboard & Notifications | 08–09 (10/11 deferred) | ✅ Shipped 2026-04-25 |
| v1.2 | Installation Programme & Field Management | 12–16 | ✅ Shipped 2026-04-25 |
| v1.3 | Technical Drawings & Schematics | 17–20 | 🚧 In progress |
| v1.4 | Client Portal & Project Visibility | 21–24 | 📋 Planned |
| v1.5 | Financial & Proposal Engine | 25–28 | 📋 Planned |
| v1.6 | Service & Inventory | 29–32 | 📋 Planned |

## Quick Tasks

| ID | Date | Description | Status | Commit |
|----|------|-------------|--------|--------|
| 260726-rf3 CLOSE | 2026-07-29 | **rf3 RAMS render unification phase — COMPLETE (opt-in).** Plan 05b Part 1 shipped 2 atomic commits extending DTO adoption beyond the initial 4-section footprint. (Part 1 Commit 1 — c207236) `resources/views/pdf/rams-v2.blade.php` now consumes DTO for 3 more sections: Standards & Guidance table (`$dto->standardsTable->rows`), Exclusions block (`$dto->exclusions->items`), and §7.0 Site-Specific Emergency Details (`$dto->emergency->{9 keys}` — leverages Plan 05a's 9-key EmergencySectionDto default). (Part 1 Commit 2 — 737bf2a) `DocxBuilderServiceV2` ports Exclusions block from legacy delegation to `buildExclusionsFromDto`; DocxBuilderService gains public seam so V2 can override just that section while delegating the rest. **Conservative deferrals** — executor pulled 3 candidate sections (DOCX Standards, DOCX Emergency, both formats Welfare) because a naive port would have been a CONTENT change disguised as a refactor: DOCX Standards renders nothing today when unpopulated but DTO falls back to config (would add 14KB); DOCX Emergency doesn't render a site_emergency table at all today; Welfare has shape mismatch between V1's single-field `programme.welfare_notes` and DTO's 5-key descriptor model. All 3 deferrals + fix paths documented in `deferred-items.md`. **Snapshot deltas on Tilda (v1 flag-off ↔ v2 flag-on):** PDF HTML +663 bytes (down from +779 pre-Part-1 due to cleaner whitespace), DOCX XML +100 bytes (down from +225) — both well under the 3000-byte / 1000-byte parity guards. Real-content delta = the client-contact `<br>` from Plan 04 Commit 2. 438 fast tests + 6 snapshot tests all green. **Phase-level docs shipped:** PHASE-PAUSED.md renamed → PHASE-COMPLETE.md (status: `complete_opt_in`) + new HOW-TO-CHANGE-RAMS.md one-pager covering "want to change a colour", "want to change a section's data source", "want to add a new section", "found drift between PDF and DOCX", + kill-switch operational guide. **Rollout state:** `RAMS_UNIFIED_COMPOSER=false` remains the default so existing installs get zero behaviour change. Global flag flip deferred to follow-up quick task `260805-rf3-global-flag-flip` — parked until (a) real Tilda fixture pulled from live VPS (currently hand-crafted), (b) snapshot goldens regenerated across all 5 fixture scenarios, (c) 3 DOCX deferrals resolved. Kill-switch removal is a further quick task `260812-rf3-remove-old-render-paths` after 1-week live soak. **21 total code commits across the phase + phase docs.** Zero prod risk today — flag off = legacy paths byte-for-byte unchanged. Deploy: nothing to do — no live env needs the flag flipped. | ✅ COMPLETE (opt-in) | 737bf2a |
| 260727-wt1-p01 | 2026-07-27 | **Phase 260727-wt1 Plan 01 — Schema + seed shipped.** 4 atomic commits (5be1321 + a5aa607 + 2cd6a83 + 97119fe). New `product_taxonomy` DB table (soft-deletable, indexed on sku_pattern + manufacturer+description_pattern composite + worksheet_category + source), `App\Models\ProductTaxonomy` with SoftDeletes + category/source constants, `App\Repositories\ProductTaxonomyRepository` singleton-bound with `findByExactSku` / `findByManufacturerAndKeyword` / `findByKeywordOnly` / `learn` finders. Seeder ports config/worksheet_taxonomy.php: **996 rows** total (12 SKU entries + 895 manufacturer×keyword expansions + 89 keyword-only rows). Idempotent — re-runs are no-ops. **Deviation:** mount_inherit rules land as `worksheet_category='unclassified'` because the ENUM has no mount_inherit slot (behaviour stays driven by config('worksheet_taxonomy.mount_inherit_keywords')). Documented + tested. NO changes to WorksheetClassifier or config file — Plan 02's job. 28 new tests / 355 assertions all green. Migration reversible (up→down→up cycle verified). Prod-safe — pure additive scaffolding. Both remotes pushed. **Plans 02-05 pending** — Plan 02 refactors classifier to consume repo behind WORKSHEET_TAXONOMY_DB kill switch; Plan 03 never-drop-kit rendering; Plan 04 review-page learning writer; Plan 05 admin resource + flag flip. | ✓ Local | 97119fe |
| 260727-fx6 | 2026-07-27 | **Throwaway config fix to unblock Tilda worksheet — SHIPPED (52dc9f3).** Extended `config/worksheet_taxonomy.php` with 3 new manufacturer rules (Crestron audio for Saros/X-Series amp, Crestron VC for 1 Beyond/Automate VX/PTZ cameras/SR camera, SurgeX for power conditioning) + extended Crestron control rule keywords (scheduling/touch screen/TSW-/TSS-/AirMedia/BYOD) + extended mount rule with Crestron manufacturer + multisurface mount kit + tier 3 keyword additions across VC/control/rack. Repositioned mount rule BEFORE Crestron control so "Mount Kit for Scheduling Panel" correctly hits mount_inherit rather than control. New test WorksheetTaxonomyRealConfigTest (16 cases / 38 assertions) loads REAL config + asserts every Tilda-flagged item classifies to a canonical category — regression guard against future reverts. Full Worksheet filter 209/209 green. Tilda worksheet regenerated should render with 0 unclassified items. Throwaway — rules get ported into 260727-wt1 Plan 01 DB seed on phase landing (partially done today via seeder porting current config) and become dead code once WORKSHEET_TAXONOMY_DB flips true. Deploy: `git pull && optimize:clear && config:cache`. NO migration. NO npm build. | ✅ Done | 52dc9f3 |
| 260726-rf3 UPDATE | 2026-07-27 | **rf3 phase advanced — Plan 04 complete + Plan 05a shipped.** Follow-up to yesterday's PHASE-PAUSED after executor spend-limit cutoff. Today: (Plan 04 Commit 2 — 7c6288f) DocxBuilderServiceV2 renders cover from RamsDocumentDTO with CLIENT CONTACT as `addText(name) + addTextBreak() + addText(email)` (parity win — legacy `\n` collapsed to space in Word) + `buildLegacy()` split into public `buildCoverSection` + `buildRestOfDocument` seams so V2 can render cover from DTO while delegating the rest byte-identical; zero behaviour change with flag off. (Plan 04 Commit 3 — 5629cb2) `Tests\Support\DocxXmlNormalizer` strips PhpWord run IDs (w:id) / relationship IDs (r:id) / revision-save IDs (w:rsidR family) / sorts xmlns attributes alphabetically / canonicalises decimal-numeric attrs in sectPr/pgMar/pgSz to 4dp. New DocxSnapshotTest in snapshot group renders Tilda fixture twice (flag off + on), asserts normalised XML matches golden. **Snapshot delta v1→v2: +225 bytes** — exactly the `<w:br/>` + second `<w:r>` block from client contact split; rest byte-identical because it delegates. (Plan 05a Commit A — 0f7bdad) Closes deferred bug 1: `EmergencySectionDto` enforces 9-key `site_emergency` shape with `''` defaults + both blades (pdf.rams + rams-v2) get `?? ''` null-safety on 10 unguarded reads each. Prevents PHP 8.4 undefined-array-key warnings on partial site_emergency data. (Plan 05a Commit B — a743af5) Closes deferred bug 2: `MethodStatementComposer` detects `material_handling` shape — object `{ large_items: [...], handling_notes: "..." }` (prod) populates new `materialHandlingItems` DTO field; legacy string-list shape (fixture data) continues populating `materialHandling` bullets. Backwards compat both directions. Tilda fixture restored with real object shape; snapshot goldens regenerated (+925 bytes html-v1/v2, docx unchanged). **rf3 status: 4.5/5 plans done (01+02+03+04 complete, 05a done, 05b pending).** Kill switch RAMS_UNIFIED_COMPOSER still defaults false. **Plan 05b remainder** (full DTO adoption for compliance-upgrade fields + real Tilda fixture from VPS + fixture parity sweep + global flag flip + HOW-TO-CHANGE-RAMS docs) deferred to next session — needs live VPS data. Full pickup path in `.planning/phases/260726-rf3-rams-render-unification/PHASE-PAUSED.md`. Prod-safe throughout. | ⚠ 4.5/5 | a743af5 |
| 260727-wt1 | 2026-07-27 | **RAMS worksheet catalogue-and-learning phase — PLANNED (execution deferred to next session).** Trigger: Tilda worksheet 14 rendered fragmented, 19 items skipped from kit list because config/worksheet_taxonomy.php has no rule for Crestron scheduling touch screens / Automate VX / AirMedia / SurgeX / IV-CAMERA-4K-PTZ families. Root-cause analysis (2026-07-27) rejected static config expansion (kicks the can), AI-assisted classifier (blocked by CLAUDE.md — AI can't invent scope/equipment), and static DB alone (still needs admin data entry for every new SKU). Chosen fix: DB catalogue + review-page learning + never-drop-kit rendering. **5 sub-plans drafted:** (1) `product_taxonomy` migration + Eloquent model + ProductTaxonomyRepository + config→DB seeder; (2) WorksheetClassifier refactor to consume repo behind WORKSHEET_TAXONOMY_DB kill switch + snapshot parity test (config-path ↔ DB-path); (3) never-drop-kit — worksheet DOCX + PDF render Unclassified items under explicit "Unclassified — Bucket at Review" section, warning banner stays; (4) LearnedTaxonomyWriter service wired to /project-packages/{id}/review save + approve — new worksheet_category dropdown on equipment table (only when equipment category is 'hardware') → learned rows persist keyed by SKU exact match, idempotent; (5) admin resource (Filament or Blade) listing source='learned' rows with promote/correct/delete actions + audit trail + kill switch flipped globally + HOW-TO-CHANGE-TAXONOMY.md docs. Kill switch removal deferred to `260803-wt1-remove-config-fallback` quick task after 1-week soak. **Non-goals:** no changes to the 6 canonical worksheet categories, no AI in classifier, no chrome/palette/font changes, adding a 7th category (wireless_presentation for AirMedia) is a separate product decision (treated as 'control' for now). **Related:** 260726-fx4 Task 4 established the no-silent-lies principle this phase extends; 260726-fx5 added the 8th 'unknown' category on the QW review page that Plan 04 leverages; 260727-fx6 is the throwaway config-only quick task that unblocks Tilda tomorrow while the phase is being built (rules ported into Plan 01 seed on landing). rf3 phase paused mid-Plan-04 remains higher-priority — this phase queues behind it. Full plan set at `.planning/phases/260727-wt1-worksheet-catalogue-and-learning/`. | 📋 Planned | 03e7401 (baseline) |
| 260727-fx6 | 2026-07-27 | **Throwaway config fix to unblock Tilda worksheet — PLANNED (execution deferred).** Extend `config/worksheet_taxonomy.php` with Crestron scheduling / Automate VX / AirMedia / SurgeX / camera keyword rules so Tilda worksheet 14 stops rendering with 19 unclassified items. 30-minute config edit + regression test. Rules get ported into 260727-wt1 Plan 01 DB seed on phase landing and become dead code once WORKSHEET_TAXONOMY_DB flips true. Kept as a quick task because Tilda can't wait 2 days for the proper phase. Plan at `.planning/quick/260727-fx6-taxonomy-unblock-tilda/`. | 📋 Planned | 03e7401 (baseline) |
| 260726-rf3 | 2026-07-26 | **RAMS render unification phase — PAUSED after Plan 03 + Plan 04 Commit 1 of 4 (executor hit account spend limit).** Route A (shared design tokens + typed DTO, keep two renderers) chosen from three unification options. **Shipped (13 commits):** Plan 01 — RamsTheme config + 16 section DTOs + service provider binding (1c366a9 + 5a0daa6 + 13f6555 + b0a2df1) — 86 new tests / 235 assertions all green, 18 new files, pure additive. Plan 02 — RamsDocumentComposer + 16 sub-composers + 5-fixture composer tests + one-line patch-service marker (6c888df + a550a65 + eecea4a + 903e5c5) — 13 new tests / 161 assertions all green, 23 new files. Plan 03 — RAMS_UNIFIED_COMPOSER kill switch + PdfService::buildRams branch + rams-v2.blade.php (partial DTO adoption: cover/doc-control/company-info/sign-off through DTO; compliance-upgrade fields still legacy) + PDF HTML-level snapshot test (Chromium output not deterministic enough for byte-diff) + rams:regenerate-snapshots Artisan command + Tilda fixture hand-crafted (real record 92 inaccessible from local empty DB) — commits e707e9b + 4c3fa81 + c2a1a99 + 68a1a8a. Plan 04 Commit 1 salvaged after executor cutoff — DocxBuilderServiceV2 skeleton + kill-switch branch in DocxBuilderService::build (buildLegacy method extracted from original body verbatim); recursion bug fixed by parent orchestrator (V2 calls buildLegacy not build). Commit 9648cf1. 39/39 Docx filter tests green. **Zero prod behaviour change** — `RAMS_UNIFIED_COMPOSER=false` (default) keeps both renderers on their pre-refactor paths byte-for-byte. Unified pipeline exists but gated. Safe to leave indefinitely. **Pending (paused):** Plan 04 Commits 2-4 (port 16 sections to DTO-consuming methods + DOCX snapshot test + DocxXmlNormalizer + client-contact line-break fix), Plan 05 (parity sweep + drift fixes + deferred items — pre-existing pdf.rams site_emergency null-safety bug + MethodStatementComposer material_handling shape mismatch + full DTO adoption for compliance-upgrade fields + real Tilda fixture from live VPS snapshot + global flag flip + HOW-TO-CHANGE-RAMS docs). Kill switch removal is deferred quick task 260802-rf3-remove-old-render-paths after 1-week soak once Plan 05 lands. **Resume:** see `.planning/phases/260726-rf3-rams-render-unification/PHASE-PAUSED.md` for full pickup instructions. **Deploy notes:** all pushed to origin + live; deploy path is `git pull && optimize:clear && config:cache` — no queue:restart needed until Plan 04/5 land actual DOCX code changes. NO migration. NO npm build. | ⚠ Paused | 9648cf1 |
| 260726-rf2 | 2026-07-26 | **RAMS output-quality — 3 atomic commits (d1a5398 + 7f3fe6d + c206a4a).** User inspected the freshly-generated Tilda RAMS PDF (21CQ29531-05-OPS Rev 1.0, project 88, record_id 92) and flagged 4 issues. (1) `d1a5398` **MethodStatementPrompt::maxTokens 3500 → 8000.** Prod log 2026-07-26 16:15:06 showed `Claude response was truncated (max_tokens reached)` on Tilda's first attempt; retry also truncated → MethodStatementService fell back to the static 5-phase template. RAMS shipped with generic boilerplate (no room names, no kit-specific make+model, no RA-ID cross-refs — all the 260725-rd1 Task 3 improvements lost). Root cause: 3 500 was set for 9 steps × 8 bullets × ~30 words but 260725-rd1 Task 3 added per-room + kit + RA-ID rules that multiply token count by rooms × equipment. Tilda has 3 rooms + 104 equipment items + 7 hazards. 8000 = headroom for ~15 rooms × 9 phases inside Claude Sonnet's 8192 per-response ceiling. Follow-up: per-room chunking deferred pending real telemetry on the 8000 cap. (2) `7f3fe6d` **Client contact splitting.** `resources/views/pdf/rams.blade.php:365` concatenated name + email with " | " → `Wesley Jones | standa3176@googlemail.com` jammed as one line on cover + emergency Site Contact rows. Fix builds the value with explicit `<br>` between pre-escaped name + email, switches the 2 render sites to `{!! !!}` (XSS-safe: `e()` applied before string build; braces only ever receive the pre-escaped variable). (3) `c206a4a` **Segregate decommission/retained + extract parenthesised room suffix.** Two bugs in `RamsDisplayPatchService`: (a) Every scope_items row labelled "NEW INSTALLATION" including "Existing display screen — deinstall and return to client" on Tilda pages 8-9. PDF template correctly renders 3 buckets (DECOMMISSION & HANDBACK / EXISTING — RETAINED / NEW INSTALLATION) but data wasn't segregated. Fix routes by name+notes: "to be retained"/"retained" → retained[] (wins over "Existing " prefix); "Existing " prefix OR "deinstall"/"de-install"/"return to client"/"removal" → decommission[]; else → new_install[]. Preserves existing decommission[]/retained[] arrays when routing bucket is empty. (b) Six Crestron 10.1" scheduling touch screens on Tilda page 6 with blank Room/Area — QW puts room name in `(Vanilla)`/`(Poppy)`/`(Kalonji)`/`(Nutmeg)`/`(Project Room)`/`(Cardamon)` trailing parens where section-header grouping doesn't apply. Fix extracts ` (RoomName)` from end of description into `room` when room is empty, strips from name. Sanity guard rejects specy suffixes (digits, units — kg/mm/hz/v/amp/rev — colons) so `(300 W)` / `(Rev A)` stay in the name. Both bugs transient — `pkg->extracted_data` untouched, only `$gd['scope_items']` mutates. **Tests:** 46/46 MethodStatement filter green. `php -l` clean on all 3 files. NO migration. NO npm build. **Deploy:** `git pull && optimize:clear && config:cache && queue:restart` — `queue:restart` mandatory (worker holds classes in memory, bit us in 260726-fx5 hotfix). **Sanity:** (1) regenerate Tilda RAMS → §6 Method of Works names Oregano/Cinnamon/Saffron, cites Sony QM85 / Crestron 4-Series, ends each phase with "Associated Risks: RA01, RA02, ..."; (2) cover + emergency Site Contact show name + email on separate lines; (3) "Existing X — deinstall" rows land under DECOMMISSION banner; "Existing Shure P300 DSP — to be retained" under RETAINED; (4) six Crestron touch screens have Room = VANILLA/POPPY/KALONJI/NUTMEG/PROJECT ROOM/CARDAMON. Quick task [260726-rf2](./quick/260726-rf2-rams-output-quality/). | ✓ Local | c206a4a |
| 260726-fx5 | 2026-07-26 | **Category-approve unblock + RAMS form auto-populate — 2 atomic commits (8d636d4 + 41536c1).** User reported on `/project-packages/156/review` (Tilda): approve dead-ends with dozens of `equipment.N.category is invalid` errors; also "the box says use dropdown but there isn't one" (muted-text-only styling on the category `<select>`). Separately, on `/rams/{id}/review` several fields that should auto-populate (site_contact, site_vehicles, subtitle) start blank on every new RAMS + site emergency / CDM rows re-typed on every revision. **Bundle A (blocker — `8d636d4`).** Root cause: `RamsReviewValidatorService.php:53` accepted only 5 category values (`hardware,cables,consumables,services,option`) but the 260725-qw3 classifier + review dropdown use the full 7-value canonical set — every QW package with a warranty / care pack / customer-supplied line dead-ended at approve. Fixes: (a) `EquipmentCategoryClassifier::CATEGORIES` gains 8th value `unknown` — escape hatch for rows the classifier can't confidently bucket (PMs resolve at site-survey time; does NOT block RAMS approve, per user directive "if category is not used in RAMS, default to Unknown so they do not hold up RAMS"). (b) validator now sources the enum from `EquipmentCategoryClassifier::CATEGORIES` via `Rule::in()` — the two vocabularies can never fall out of sync again. (c) `review.blade.php` `$categoryOptions` array + JS row-template gain `'Unknown (set later)'` option. (d) Category `<select>` gets visible border (`rgba(0,0,0,.14)`) + native chevron via inline-SVG background + hover/focus states — reads as a picker instead of static muted text. **Bundle B (`41536c1`) — RAMS form auto-populate.** `RamsDisplayPatchService` sections 4/5 + 6 extended: (1) reverse-mirror `$p['site_contact']` ← `$p['client_contact_name']` when empty — pre-fix the resolved contact landed in `client_contact_name` but the form field reads `site_contact`, so the value never surfaced. (2) `$p['site_vehicles']` lifted from `reviewed_data.programme.site_vehicles` (array joined with `\n` for the textarea). (3) `$p['subtitle']` auto-derived `"{site line 1} | {client} | AV Installation"` when PM hasn't set one; skips derivation when both pieces missing (avoids naked ` | AV Installation`). (4) Prior-RAMS auto-carry: on any new/uncompleted RAMS with empty `site_emergency` or `cdm`, seed from the most recent completed RAMS on the same project (ordered by `generated_at` desc). Non-destructive — fills blanks only; transient (no save) — PM can override. Eliminates re-typing nearest hospital / fire wardens / first aiders / defibrillator / isolation switch + CDM duty-holder rows on every revision. **Tests:** 24/24 `EquipmentCategoryClassifier` cases still green (adding `unknown` to the CATEGORIES const doesn't invalidate any existing assertion). `RamsReviewValidatorService` + `RamsDisplayPatchService` have no dedicated unit tests currently — none broken. `php -l` clean on all 4 touched files. NO migration. NO npm build. **Deploy:** `git pull && php artisan optimize:clear && php artisan config:cache`. **Sanity:** (a) re-open `/project-packages/156/review` → click Approve → no `category is invalid` errors + category dropdown visibly reads as a select; (b) create a new RAMS on a project that has a completed prior RAMS → site_contact/site_vehicles/subtitle pre-fill, site emergency + CDM blocks pre-fill from prior. | ✓ Local | 41536c1 |
| 260726-fx4 | 2026-07-26 | **Critical workflow + AI quality lift — 7 code tasks + 1 docs, 8 atomic commits (50825dd + f494538 + 288e245 + 05c6941 + 43c0a36 + 13c69c2 + fc34a71 + 7b90cfc).** Ships the top-6 findings from the 2026-07-26 structural + content audit as 3 bundled sub-plans. **Bundle hf1 — critical workflow fixes**: (1) `50825dd` new `App\Events\SurveySubmitted` event dispatched by `SurveyService::submitPublic` + `Worksheet::isStale` / `OmManual::isStale` / `RamsDocument::isStale` extended to also check `$project->latestSurvey->submitted_at > generated_at` (only fires once `submitted_at` is stamped — in-progress edits do NOT flip banners); new `CableSchedule::isStale` uses `completion_email_sent_at ?? updated_at` as the generated_at proxy (CS has no `generated_data['generated_at']` snapshot); new `Project::latestSurvey()` HasOne mirroring `latestPackage()`. No auto-regeneration — just surfaces the signal via existing `_stale-banner.blade.php`. (2) `f494538` cable schedule error_message: column already existed (Phase 09 migration) but was never written — `BuildCableScheduleJob::handle` catch + `failed()` hook now stamp `mb_substr($e->getMessage(), 0, 990)` alongside status=failed; edit view + index view get red Failed pill + collapsible `<details>See why</details>` mirroring RAMS UX. (3) `288e245` public worksheet sign-off email: new migration `worksheets.signed_notification_sent_at`, new `WorksheetSignedMail` + email blade, `PublicWorksheetController::sign()` resolves owner via `NotificationRecipientResolver`, sends mail, `forceFill()`s timestamp only on successful send (mail failure leaves it null so retries can fire); worksheets/show shows green "Office notified {ago}" pill or amber "Office not notified" fallback; column deliberately NOT `$fillable` per 260709 auth-tokened flag pattern. (4) `05c6941` `RoomOverviewSummaryService::fallbackSummary()` returns empty string (was: "Works: <first sentence>" which masqueraded as AI content); `_summary_fallback = true` marker set on rows where AI unavailable or returned empty; `WorksheetGeneratorService` persists the marker but never the empty works_summary (would clobber prior valid bullets); `review.blade.php` shows amber "⚠ AI unavailable — click Generate to retry" badge when marker set + field empty + overview non-empty. **Bundle cd1 — survey context into AI prompts**: (5) `43c0a36` new `App\Services\SiteConditionsBuilder` static helper extracts engineer_feedback (mounting_heights, wall_construction, brackets_required, cable_routes, access_notes) from `$survey->rooms` into per-room map — empty/null/false fields stripped; `RamsBuilderService` (both buildFromReview + buildFromQuote) injects `$parsedQuote['site_conditions']` via new `latestSurveyForRecord()` helper; `MethodStatementService::generate()` forwards through; `MethodStatementPrompt` systemMessage adds "cite the relevant conditions ... Do NOT invent" rule; `build()` embeds sentinel-wrapped Site conditions JSON block when non-empty; `MethodStatementGeneratorService` cache key gains `site_conditions_hash` + `prompt_version` bumped to `v3-20260726` so existing cached responses can't be served to projects whose survey moved on. (6) `13c69c2` same pattern for O&M — `OmManualGeneratorService::generateContent` injects site_conditions; `OmManualPrompt::forContent` systemMessage instructs "Display mounted at 1900mm from finished floor level" verbatim citation of mounting_heights, wall_construction+brackets_required drive maintenance-access notes, access_notes drives ceiling-void/floor-box procedures; sentinel-wrapped SITE CONDITIONS block embedded when non-empty. **Bundle cq1 — OM prompt scoping**: (7) `fc34a71` `OmManualPrompt::forContent()` per-equipment JSON schema trimmed from 12 fields to 4 canonical (`installation`, `operation`, `maintenance`, `warnings`) — removed 8 fields (`troubleshooting`, `key_specifications`, `support_contacts`, `daily_ops`/`weekly_ops`/`monthly_ops`/`annual_ops`, `installation_notes`, `operation_guide`, `maintenance_schedule`, `manufacturer_contact`) plus top-level `support_contacts` (invented phone numbers); INSTRUCTIONS step 2 explicitly names the 4 kept fields AND lists removed field names with "Do NOT emit" guard; renderer trim skipped — DOCX + PDF blade already only consume deterministic sections (operation_sections, top-level maintenance_schedule, fault_finding, manufacturer_support, rooms_summary), the 12 AI fields never reached render pipeline. (8) `7b90cfc` follow-up: try/catch wraps `latestSurveyForRecord()`'s project relation access so mock-based `RamsBuilderServiceTest` cases without stubbed project relation continue to pass (production branch never fires). **Test tally:** baseline 725 passed / 12 pre-existing failed → final **806 passed / 11 pre-existing failed** across `Docx|Rams|Worksheet|OmManual|Survey|CableSchedule|MethodStatement|RiskAssessment|SiteConditions|RoomOverviewSummary` sweep. **35 new test cases** across 8 new test files. **Deviations:** (a) Task 1 CableSchedule uses `completion_email_sent_at ?? updated_at` proxy since CS has no generated_data snapshot; (b) Task 2 migration skipped — column already existed from Phase 09; (c) Task 7 renderer trim was a no-op — deterministic sections already discard those fields at render time; (d) Task 5 follow-up commit `7b90cfc` added defensive try/catch to keep mock-based tests passing. **1 migration** to run on live (`add_signed_notification_sent_at_to_worksheets`). NO npm build. **Deploy:** `git pull && php artisan migrate --force && php artisan optimize:clear && php artisan config:cache`. **Sanity:** (a) submit public survey → generated docs show stale banner; (b) force-fail a cable schedule → "See why" surfaces error; (c) sign a public worksheet → office gets email + "Office notified" pill lights up; (d) run without AI credits → amber "AI unavailable" badge on empty room summaries (no more "Works: ..." masquerade); (e) regenerate Tilda RAMS → §6 method statement cites specific mounting heights + wall construction + bracket models from the survey; (f) regenerate any O&M → per-equipment sections trimmed to 4 fields, no more invented support phone numbers. | ✓ Local | 7b90cfc |
| 260725-rd1 | 2026-07-25 | **RAMS DOCX design + content parity with hand-crafted Tilda reference (5aef79b + ee75f46 + 92cf99f).** Three atomic commits bring the auto-generated RAMS DOCX up to the visual + structural + content depth of the hand-crafted `21CQ29531-05-OPS Tilda RAMs Rev1.1.docx` reference. **(Task 1 — 5aef79b)** Palette + font shift: heading colour flipped from teal `#007B8A` to brand blue `#2E74B5` — the `TEAL` const NAME is preserved (~30 call-sites would be a diff-inflating mass rename); its VALUE now holds brand blue, and three new named constants `BRAND_BLUE` / `BRAND_BLUE_DARK` / `BRAND_BLUE_TINT` co-exist for explicit callers. `ROW_ALT` shifted `F0FBFC` (light teal) → `DEEBF7` (light blue); every alt-shaded table picks up the new tint automatically. `setDefaultFontName('Arial')` → `'Poppins'` and the `font()` helper's hard-coded `'name' => 'Arial'` follows suit (Word substitutes if Poppins is not installed — no font bundle). New `DocxBuilderPaletteFontTest` opens `word/document.xml` and asserts (a) no `007B8A`, (b) `2E74B5` present, (c) no `F0FBFC`, (d) `DEEBF7` present, (e) `Poppins` present + no `Arial` in run properties. **(Task 2 — ee75f46)** Four missing structural sections added, all matching the reference. **§6.11 Coordination with Other Trades** — always renders; prose paragraph + 5 coordination touch-points (ceiling grid, partitions, electrical isolation, Principal Contractor permits, client IT tie-in). **§6.12 IT / Network Integration Safety** — always renders; intro prose + 6 IT-integration principles (verify network, label cables, firewall rules, test-before-connect, PoE budget, coordinated cutover) + closing prose on power-cycle/network-fail recovery verification. **Explicit Exclusions block** under Scope of Works — reads `reviewed_data['exclusions']` (populated by the `add_exclusion` op from 260723-rr1 or the RamsDisplayPatchService default seed), falls through to `generated_data['exclusions']` for fixtures, empty state prints "No exclusions declared for this project." **Standards & Guidance table** under H&S Policy — reads `$data['standards_references']` (populated by `Tier1RamsDefaultsService` from `config('rams_tier1.standards_references')`); config shape uses `ref`/`title`/`applies_to` (10 standards: BS 7671, BS 6701, BS EN 60849, BS 8492, CDM 2015, HSG 47, HSG 273, AVIXA F502.01, PUWER 1998, BS EN 60825-1) — table iteration tolerates the alt `reference`/`name`/`scope` shape too. `config/rams_tier1.php` NOT edited (H&S-signed-off content). New `DocxBuilderNewSectionsTest` (6 cases). **(Task 3 — 92cf99f)** Method-statement prompt tuning for per-room granularity + kit-specific detail + risk-ID cross-references. `MethodStatementPrompt::systemMessage()` gains three new rules: (1) per-room granularity — every step touching a physical space MUST name the specific room(s); (2) kit-specific detail — steps reference specific make + model from the equipment list, not generic terms; (3) risk-ID cross-references — each phase MUST end with `"Associated Risks: RA01, RA02, RA03"` using RA-IDs from the supplied risk register. `build()` emits a `"Risk register (use these RA-IDs verbatim when cross-referencing):"` context block enumerating hazards with stable `RA{NN}` IDs matching `DocxBuilderService::buildRiskAssessment`'s numbering (block omitted when `hazards[]` empty). `MethodStatementService::generate()` now forwards the full `hazards` array into prompt context so the register can be enumerated. `DocxBuilderService::buildRiskAssessment` needs NO change — the renderer has been outputting `RA{NN}` as the first column of the risk-assessment table since 260514 (line 1012); the AI's cross-references now resolve into that numbering scheme. `MethodStatementPromptTest` gains 6 cases asserting systemMessage rule presence (3), build() risk-register emission with correct RA-IDs, omission when hazards[] empty, per-phase Associated-Risks requirement in the body. **Test tally:** baseline `--filter Docx\|RamsBuilder` 37 tests → after Task 1: 40 → after Task 2: 46. Full sweep `--filter Docx\|RamsBuilder\|MethodStatement\|RiskAssessment` 89 tests / 352 assertions green. `php -l` clean on every edited file. **Deviations:** (a) config shape confirmed to be `ref`/`title`/`applies_to`, table iteration handles both that and the alt shape; (b) exclusions data source falls through to `generated_data` when reviewed_data absent (fixture convenience); (c) no RA-ID docx builder change needed (already there since 260514); (d) no live AI regression run — user on rate-limited Anthropic credits per 2026-07-25 memory, prompt-change verification via unit tests only. NO migrations, NO npm build. **Deploy:** `git pull && php artisan optimize:clear && php artisan config:cache`. **Sanity:** regenerate `21CQ29531-05-OPS` Tilda RAMS from review page → download → open in Word → confirm blue headings (not teal), light-blue alt-row shading, Poppins body font, §6.11 + §6.12 + Exclusions + Standards sections all present. Content-depth verification (per-room + kit + RA-ID cross-refs in §6.6) needs Anthropic credits — will re-verify after top-up. Quick task [260725-rd1](./quick/260725-rd1-tier1-rams-design-and-content-parity/). | ✓ Local | 92cf99f |
| 260413-f2h | 2026-04-13 | Fix room detection, room_overviews scaffold, prepared-by multi-line | ✅ Done | 616ec5a |
| 260413-fjj | 2026-04-13 | Widen part-number regex to accept digit-starting part numbers | ✅ Done | 1ec01b3 |
| 260413-qxb | 2026-04-13 | Pre-fill RAMS create form from project data when no package exists | ✅ Done | 0cf006e |
| 260413-rm9 | 2026-04-13 | Restructure RAMS DOCX to match reference PDF format (9-section, scope items, risk badges, numbered steps) | ✅ Done | 977c0a9 |
| 260414-cnf | 2026-04-14 | Rewrite rams.blade.php PDF template to match 9-section reference format (mirrors DOCX output) | ✅ Done | f9790a1 |
| 260414-gua | 2026-04-14 | Redesign RAMS PDF output to match 21CQ30017 reference document and add PDF download to project page | ✅ Done | ef64a9e |
| 260414-j5p | 2026-04-14 | Add start/end times, waste removal, permits, material handling, CDM, COSHH, welfare, toolbox talk to RAMS | ✅ Done | c428316 |
| 260414-jli | 2026-04-14 | Add scope traceability, client responsibilities expanded, exclusions, decommissioning, commissioning criteria to RAMS | ✅ Done | f33b8b9 |
| 260415-en6 | 2026-04-15 | Fix site survey gaps: PM email, cable routes, rack room, projection/network/sign-off fields | ✅ Done | d47d653 |
| 260426-gvm | 2026-04-26 | Public worksheet sign-off link (UUID-token, single-signature acceptance, optional outstanding-items comments, append-only signoffs, DOCX signature embed) | ✅ Done | e31f58d |
| 260427-qvr | 2026-04-27 | Migrate PDF rendering to Browsershot (RAMS + O&M + Site Survey via single PdfRenderService, pdf:smoke-test command, deployment runbook for chrome symlink + queue-worker user fix + chown). Existing dompdf/mpdf retained for rollback. | ⚠️ Needs Review | 92c95da |
| 260430-um1 | 2026-04-30 | Fix install programme 0 tasks — port two-strategy room/equipment distribution (area-tag grouping + flat-equipment fallback with NON_PHYSICAL_ROOMS guard) into InstallTaskGeneratorService. Verified locally on 3 projects (3/43/31 tasks generated). | ✅ Done | c479364 |
| 260503-ipc | 2026-05-03 | UI polish across 5 editable forms — section cards with teal headers + soft-red empty-field highlight via pure CSS (`:placeholder-shown` + `data-optional` opt-out). 9 files, 2 commits. CSS/Blade only — `git diff app/ routes/ database/ config/` = empty. Visual confirmation pending. | ⚠️ Needs Review | 2edc1ba |
| 260503-rgg | 2026-05-03 | Site Survey form enhancements per engineer feedback — 17 new fields across site-logistics + per-room mounting heights + build-out details (cable routes, wall construction, brackets, table/floor box info, work-at-height methods). Schema-additive (7 site cols + 10 room cols, all nullable), Alpine multi-row UIs, .form-section card chrome from 260503-ipc. 7 files, 3 commits + 3 UI-audit follow-up commits (strip nested chrome, reorder PA panels, space-type guards). RamsBuilder/InstallTask services UNTOUCHED — pick up new data on next regen. | ⚠️ Needs Upload | 9213344 |
| 260503-tfb | 2026-05-03 | Wire site-survey engineer-feedback fields into RAMS generator — extends ProjectContext + RamsDataBuilder with `site_logistics` and per-room `engineer_feedback` blocks; RiskTemplateResolver auto-classifies Working at Height (LOW/MED/HIGH from methods + max mounting height), Dust & Debris (chase-out), Fixings/Substrate Failure (reinforcement), Hidden Services (conduit), Manual Handling (cable runs >30m); rams.blade.php renders Site Logistics section + per-room Engineer Survey Findings subsection. 6 files, 3 commits. Zero new HazardTemplate seeders — all auto-classified hazards resolve to existing global library entries. Defensive null handling — legacy surveys produce identical RAMS output. | ⚠️ Needs Upload | 1e6555e |
| 260503-u2x | 2026-05-03 | Mirror engineer-feedback fields into PUBLIC engineer survey wizard at /survey/{token} — 1636-line Tailwind+Alpine wizard now renders Site Logistics card + 7 per-room sub-sections (Mounting Heights, Working at Height Methods, Cable Routes, Wall Construction & Prep, Table Info, Floor Box Info, Brackets Required) with `.room-subsection` chrome from 260503-rgg followups. Canonical-shape extension (engineer_feedback_site root + per-room engineer_feedback block) + DB-column mirror via `writeEngineerFeedbackToColumns()` at every step-save AND completeRoom. PublicSurveyController validation extended (7 site + 36 room rules, all nullable). 3 files (SurveyController + PublicSurveyController + surveys/show.blade.php), 2 commits, +1131/-11 lines. Tinker render check passes (195KB HTML, all new sections present). Closes the on-site capture gap — engineers' tablet workflow now feeds data into the same DB columns RAMS auto-classification reads. | ⚠️ Needs Upload | bf40034 |
| 260503-w77 | 2026-05-03 | Mirror engineer-feedback fields into PRINTABLE PDF survey fallback (/survey/{token}/download-form) — 3 PDF Blade templates updated so engineers working offline on paper capture the same 17 fields in the same order as the digital wizard for clean 1:1 transcription. Header gains "Site Logistics" section (comms-room access ☐, distance from base, site access notes, delivery routes — all pre-fill from $survey when set). Per-room body gains 7 new sub-sections (Mounting Heights, WAH Methods, Cable Routes 4-row table, Wall Construction & Prep, Table Info, Floor Box Info, Brackets 4-row table). Print stylesheet tightened (table 8.5pt → 8pt, h3 restyled as teal eyebrow). Pure presentation — no controllers, services, or PDF pipeline touched. 3 files, 1 commit, +176/-13 lines. Tinker render check passes (74KB HTML). | ⚠️ Needs Upload | 6d250db |
| 260504-dh8 | 2026-05-04 | Survey + worksheet output usability fixes (UAT feedback during /gsd-verify-work 20). Three issues addressed: (1) populated survey PDF (summary.blade.php) restructured into Site Conditions / AV Requirements / Engineer Findings h3 groupings — AV requirements now use H::narrativeAsTickList() instead of nl2br wall-of-text; engineer-feedback rendering copied verbatim from rams.blade.php (260503-tfb); (2) blank field-form _blank-room-body gets a "Site Conditions" h3 above the existing first table for visual consistency with the 7 EF sub-sections; (3) WorksheetDocxService gains an "Installation Reference" PhpWord section per room surfacing mounting heights / cable routes / brackets / wall prep so installers can cross-check against surveyor specs; (4) public-show.blade.php (engineer tablet view) gets a teal-bordered "Survey Reference" <details> drawer per room (BEFORE kit-list drawer), inline SiteSurvey lookup with class_exists guard. Pure additive — no controllers, models, routes, schema. 4 files, 3 commits, +647/-6 lines. 6 smoke tests pass (regression-safe on rooms with no EF data). | ⚠️ Needs Upload | 360d763 |
| 260504-eji | 2026-05-04 | Sticky Save/Cancel action bar at top of all editable forms — UX request: long forms (survey edit / RAMS review) require scrolling, engineers want save action one tap away. New `.edit-action-bar` CSS in layouts/app.blade.php (`position: sticky; top: var(--header-height); z-index: 90`, full-width bleed via `-1.75rem -2rem` desktop / `-1.25rem -1rem` mobile, title hides <768px). New `<x-edit-action-bar>` Blade component with formId/cancelUrl/saveLabel/cancelLabel props using HTML5 `form="..."` attribute (zero JS). Injected into 5 editable views: site-survey/edit (replaces legacy header buttons), projects/edit, rams/review (only the line-348 update-and-download form gets id; regen/email forms untouched), om-manual/edit, om-manual/edit-devices. worksheets/show confirmed read-only and skipped. Pure CSS+Blade — `git diff app/ routes/ database/ config/ public/` empty. 7 files, 3 commits, +89/-10 lines. | ⚠️ Needs Upload | d632cf4 |
| 260504-gho | 2026-05-04 | Three bundled output-document improvements. (1) Regen Worksheet button on `worksheets/show` and `worksheets/index` (gated to draft/final/failed states) — backend route `worksheets.retry-generation` already existed; only UI was missing. (2) Site Logistics surfaced on the worksheet — DOCX cover header gains a Site Logistics block (5 fields: comms-room access status+notes, parking, distance from base + notes, site access, delivery routes) sourced from latest linked SiteSurvey; public engineer view (`public-show.blade.php`) gains a teal-bordered Site Logistics drawer at project level (BEFORE rooms loop). (3) Mirrored the two missing RAMS PDF sections into DOCX — `DocxBuilderService` gains `buildSiteLogistics()` (inside buildScopeOfWorks) + `buildEngineerFindingsByRoom()` (new section between scope and risk-assessment) with pre-flight `$anyEf` check so byte-identical output on rooms with no EF data. Pure additive — no controllers, routes, models, schema. 5 files, 3 commits, +519/-11 lines. 5 smoke tests pass (regression-safe on legacy worksheets/RAMS). | ⚠️ Needs Upload | 6e00bad |
| 260504-hqe | 2026-05-04 | Engineer link `/worksheet/{token}` survey photos + per-room review gate before sign-off. New `pre_install_confirmations` JSON column on `worksheets` table (nullable) tracks per-room review state `{reviewed_at, reviewed_by_token_prefix}`. Two new public-worksheet endpoints: `serveSurveyPhoto` (cross-project guard via `$photo->room?->survey?->project_id === $worksheet->project_id`, throttle 120/min) and `markSurveyReviewed` (forged-room-name guard via inclusion list from `generated_data['rooms'][*]['name']`, throttle 60/min). public-show.blade.php Survey Reference drawer extended: photo thumbnail strip at drawer top, review checkbox/button at bottom (or green ✓ Reviewed badge after submission). Page-level Sign-off button gets `@disabled` + `data-signoff-blocked` flag with amber warning banner when ANY room with EF/photos is unreviewed; signature-pad JS respects the flag. Soft gate only — no server-side prevention. Backward compat: legacy worksheets / projects without survey produce zero visual change. 5 files (1 migration + Worksheet model + PublicWorksheetController + routes/web.php + public-show.blade.php), 3 commits, +265/-8 lines. 6 smoke tests pass. | ⚠️ Needs Upload | 3df113c |
| 260504-ij9 | 2026-05-04 | Engineer link UX fixes from gsd-ui-auditor review (260504-hqe-UI-REVIEW). Five bundled improvements: (B1) Differentiate Survey Reference (🔍 + teal accent) from AV Works (🛠 + new neutral grey scheme) — drawers were visually identical with same teal+📋 chrome, engineer could mistake one for the other; (B2) Per-room status pill on each room's `<summary>` row — `⚠ Survey not reviewed` (amber) when room has EF/photos and no review; `✓ Reviewed` (green) when reviewed; no pill when no survey applies. Replaces "memorize names from bottom banner" UX; (B3) Move Photos of completed work tray to TOP of each open-room body (was buried under 4 collapsed drawers) — most-frequent action gets primary placement; (H2) Render the amber sign-off-blocked banner ALSO at the top of the page (in addition to the bottom one inside the Sign-off card) with "Jump to first unreviewed room" anchor link — added `id="room-{slug}"` to each room's outer `<details>` to enable in-page navigation; (H5) Drop the redundant checkbox in the Mark Reviewed form — single-tap button now ("✓ I have reviewed this room — mark reviewed") replaces the 2-tap checkbox+button. Pure presentation — 1 file (`public-show.blade.php`), 1 atomic commit, +90/-44 lines. 4 smoke tests pass. Forbidden-paths diff empty (no controllers, models, routes, schema). | ⚠️ Needs Upload | 4ed52e1 |
| 260504-iy4 | 2026-05-04 | Finish all 4 deferred audit items on the engineer link. (H4) Restructured `pre_install_confirmations` JSON to namespace `survey_review.{room}` and `room_complete.{room}` (zero-migration since the column hasn't been deployed yet from 260504-hqe). New Worksheet model accessor methods: `surveyReviewedAt/By(room)`, `roomCompletedAt/By(room)` — gracefully return null on legacy flat-shape data. (H1) New "✅ Mark Room Complete" CTA per room — visible/disabled per gate (survey reviewed if required AND ≥1 worksheet photo); when complete: green ✓ Complete pill on summary + auto-collapse via `data-skip-restore="1"`. New `markRoomComplete` controller endpoint mirroring the markSurveyReviewed forged-name guard pattern (server-side validates room name only — gate is frontend-only soft so engineers on flaky networks aren't blocked). New POST route `worksheet/{token}/rooms/{roomName}/complete` named `public-worksheet.room-complete` (throttle 60/min). First-incomplete-room is now the auto-open default (was always room 0). (H3) Vanilla JS scroll/state restore via sessionStorage keyed per worksheet ID — saves scrollY + open-`<details>`-IDs on form submit, restores on DOMContentLoaded; 5-min stale guard; skips just-completed rooms via `data-skip-restore`. (L3) Read-only after sign-off via single `<fieldset disabled>` wrapping rooms loop + sign-off card when `$latestSignoff` exists; new info banner above sign-off card "🔒 This worksheet was signed by {name} on {date}. Additional sign-offs are recorded as snag/follow-up entries" — signaled lock matches the append-only model docstring intent. 4 files (Worksheet.php + PublicWorksheetController + routes/web.php + public-show.blade.php), 3 commits, +263/-17 lines. 4-scenario render smoke test passes including legacy flat-shape backward compat. After this task the deferred audit items list is empty. | ⚠️ Needs Upload | 42c90b9 |
| 260504-ktt | 2026-05-04 | Fix Box Serial Label OCR for iOS HEIC photos — engineer reported clear photos producing empty AI-extracted fields. Root cause: iOS Safari sends `image/heic` mime type which Claude vision API rejects (only accepts JPEG/PNG/GIF/WEBP); DeviceLabelPhotoService swallows the failure → ai_extracted = null → modal opens with empty fields. Fix: client-side canvas conversion to JPEG before upload via new `convertToJpegBlob(file, maxSide=1600, quality=0.85)` helper — FileReader → Image → canvas (downscaled to ≤2MP, preserving aspect) → toBlob('image/jpeg', 0.85). Falls back to raw file on conversion error (very old browsers etc). Also added UX safety net: when all 5 extracted fields are empty/UNKNOWN, the openLabelReview modal now shows a yellow banner "⚠ AI couldn't read this label clearly. Please type the visible values from the photo into the fields below" — engineers know to fill manually instead of seeing blank fields with no explanation. 1 file (`public-show.blade.php`), 1 atomic commit, +60/-5 lines. Pure JS — no controllers, services, dependencies. | ⚠️ Needs Upload | 3ea1c4e |
| 260504-lat | 2026-05-04 | Visible loading state during Box Serial Label capture so engineers see "something is happening" during the 5-15s AI vision wait. Three progress states cycle on the capture button: ⏳ Processing image... → 📤 Uploading... → 🤖 Reading label... → restored on completion. Pulsing 8px dot spinner (CSS-only `@keyframes lblPulse`, `.label-cap-busy::after`) appears next to the button text during all three phases. File input disabled mid-flow to prevent re-tapping, re-enabled on every exit path (success/error/catch). Text-node-only mutation preserves the wrapped `<input>` so the next capture works. 1 file (`public-show.blade.php`), 1 atomic commit, +59/-3 lines. Pure CSS+JS. | ⚠️ Needs Upload | 6554bcc |
| 260504-m2k | 2026-05-04 | Replace 64 native browser confirm() dialogs with a styled SCC v2 modal across the app. Native dialogs were jarring against the cream/teal design system — discovered after engineer reported the AI Settings save flow felt off-brand. Built a global Alpine.js modal component in layouts/app.blade.php: cream paper card, Fraunces title, Inter body, .btn-outline cancel + .btn-teal/.btn-danger confirm (danger variant auto-applied via destructive-keyword heuristic — delete/permanently/cannot be undone/force-destroy/discard/remove/revoke/archive/suspend). Esc + backdrop-click cancel; Enter confirms. Capture-phase JS interceptors intercept any form submit OR button/link click with `data-confirm="..."` attribute, show modal, replay action on confirmation. Plus global `window.appConfirm(message, opts)` Promise helper for inline JS calls. Sweep replaced 64 instances across 27 view files: 22 `onsubmit="return confirm()"` forms + 30+ `onclick="return confirm()"` buttons + ~5 inline JS confirm() calls (await-converted to Promise pattern). 9 archive/backup file instances skipped (Laravel doesn't load `.bak-*` / `.pre-tailwind-bak` / `.blade2903.php`). After: `grep onsubmit="return confirm\\|onclick="return confirm` returns 0 in live Blades. 27 files, 2 commits, +678/-225 lines. Pure presentation — no controllers, routes, schema. | ⚠️ Needs Upload | e3d6521 |
| 260504-nbt | 2026-05-04 | Fix confirm modal DELETE/PUT method spoof for Safari. Engineer reported "Delete worksheet" from row-actions dropdown on /projects/{id} did nothing on iPad — modal opened, user confirmed, page neither redirected nor showed worksheet as deleted. Root cause: 260504-m2k's confirmAction() called plain `tgt.submit()` to trigger submission post-confirm. On Safari + forms with `display:contents` (the .row-actions form pattern), plain submit() can drop the hidden `_method` input that Laravel's @method('DELETE') / @method('PUT') generates → request goes as POST → Laravel returns 405 Method Not Allowed → silent failure (Network tab unreachable on tablet so no error visible). Fix: use `tgt.requestSubmit(submitBtn)` — modern HTML5 spec'd API that fires a real submit event (our handler short-circuits because data-confirm has been removed by then) AND preserves submit-button association + all form data including @method spoof + CSRF. Includes try/catch fallback chain (requestSubmit → submit → no-op) so very old Safari without requestSubmit support degrades gracefully. 1 file (`layouts/app.blade.php`), 1 atomic commit, +21/-2 lines. Pure JS — no controllers, routes, schema. | ⚠️ Needs Upload | ebf32c3 |
| 260506-fh0 | 2026-05-06 | Site Survey auto-populate fixes — `createFromProject` now seeds `general_notes` from project (or fallback package) `works_description` (3000-char cap + audit log); new lightweight Confirm Rooms review screen between create and edit (GET/POST `site-surveys.confirm-rooms`, reuses `SurveyService::update` for qty-expansion + prune; "Skip" link bypasses to edit). Audit confirmed all project-page "+ New Survey" CTAs already route through `site-surveys.from-project` (3 remaining `site-surveys.create` references are legitimate standalone path — left alone). 4 files, 2 commits, +240/-5 lines. Pure additive — no migrations, no new packages. | ⚠️ Needs Upload | 8e0a5da |
| 260506-jbu | 2026-05-06 | Site Survey PDF intelligence (L1+L2 of three-layer plan; L3 equipment-rule engine deferred to a separate quick task). **L1**: Fixed `SurveyService::createFromProject` overwrite bug at lines 214-221 — quote-specific room `overview` text is now the primary AV Requirements; the static `SolutionType.survey_checklist` is appended as a "Standard checks for this solution type:" supplementary block only when both exist. Stops every Video Conferencing room rendering identically. **L2**: New "Pre-install Checks" h3+table section per room in `pdf/site-survey/summary.blade.php`, rendering existing `SiteSurveyRoomQuestion` rows (sorted by `sort_order`, with answer + other_text columns). Section guarded by `$room->questions->isNotEmpty()` so empty rooms render no empty heading. `SurveyPdfService` extended with `'rooms.questions'` eager-load to prevent N+1. Verified via direct Blade render (Browsershot's puppeteer not provisioned on Windows dev — print-layout step deferred to live UAT, Browsershot is correct on the live server per Phase 20 runbook). 3 files, 2 commits. Pure additive — no schema, no migrations, no packages, no AI changes (the smart pipeline `GenerateSurveyQuestionsJob` already produced questions; PDF now surfaces them). | ⚠️ Needs Upload | 22c1840 |
| 260506-qa9 | 2026-05-06 | Mini O&M auto-built from project data — lightweight Tier 1 client-facing PDF for projects that don't warrant the full AI-assisted O&M Manual pipeline. Built via `--discuss` mode: 7 LOCKED design decisions captured in CONTEXT.md (cover hero auto-pick, confirmed-labels-then-Also-installed asset register, all-rooms-included-with-placeholder, Tier 1 visual matching `pdf/om-manual.blade.php`, manual button trigger, before/after graceful degrade, config-driven warranty/support copy). Five tasks, single wave, zero deviations: (1) `MiniOmBuilderService` pure data aggregator — N+1-safe eager-loads, no write surface (Storage::put / DB::insert grep returns 0); (2) `pdf/mini-om.blade.php` Tier 1 Blade — same teal `#1B7A7A` headings, brand orange `#C07000` accents, `.cover-accent-bar` chrome as full O&M; (3) `mini_om_support` config block in `config/rams.php` (4 keys: support_phone, support_email, warranty_terms, service_ticket_instructions — env-cascading defaults); (4) `MiniOmController` + `projects.mini-om.pdf` route auth-gated via ProjectPolicy::view, renders fresh on every request via `PdfRenderService::fromBlade`, persists artifact via `DocumentArtifactStorage::TYPE_OM` with `mini-om-` filename prefix to differentiate from the heavyweight O&M; (5) "📄 Generate Mini O&M" button + green/amber status pill on `projects/show.blade.php` (✅ Photos captured / ⚠️ Awaiting photos based on worksheet photo count). Smoke-tested across 3 local projects (3, 5, 9 rooms) — all renders 16-38k chars cleanly. PDF print verification deferred to live UAT (Windows dev lacks puppeteer; Browsershot correct on live per Phase 20 runbook). HTML preview at `storage/app/private/mini-om-preview-260506-qa9.html` for visual review before live deploy. 6 files (3 new, 3 modified), 5 atomic commits. Pure additive — no migrations, no new packages, no `om_manuals` row written. | ⚠️ Needs Upload | 0fc2d5d |
| 260508-v7g | 2026-05-08 | Office Survey Review Surface — closes the half-built post-engineer-submission workflow with three connected deliveries: (a) per-room `office_notes` + survey-level `office_review_notes` so office staff can clean up / annotate engineer captures without modifying engineer fields; (b) flat `survey_variations` capture-and-export (no CRM workflow per D-LOCK-1) — types: extra_hardware/extra_labour/cable_change/client_provided_change/access_issue/other; statuses: proposed/quoted/approved/rejected (unconstrained dropdown); each can attach an existing survey photo; (c) Tier 1 client-facing survey report PDF matching `pdf/mini-om.blade.php` design language + variations CSV download for sales quote-revision conversations. Six tasks, single wave, one minor structural deviation auto-fixed (Rule 3 — plan said "before type-specific panels" but those panels render ABOVE engineer-feedback; placed office_notes textarea at end of engineer-feedback group with amber accent chrome). New `survey_variations` table (10 columns + 2 indices) + 2 nullable text columns on existing tables. New `SurveyVariation` model + relations. New `SurveyVariationController` with 3 CRUD routes (D-LOCK-6 auth: `abort_unless(auth()->check(), 403)` matching D-15 ProjectController pattern, NOT ProjectPolicy). New "Variations & Additions" Alpine modal + table on edit page. New `SurveyPdfService::buildClientReport` + Blade `pdf/site-survey/client-report.blade.php` (~200 lines). New `DocumentArtifactStorage::TYPE_SURVEY` constant. New "📄 Client Report" + "📊 Variations CSV" buttons on survey show + project Surveys tab. D-LOCK audit clean: engineer summary PDF untouched (D-LOCK-4: zero diff lines), zero RAMS-related changes (D-LOCK-5: grep returns 0), Tier 1 visual matching Mini O&M (D-LOCK-3). HTML preview at `storage/app/private/client-survey-preview-260508-v7g.html` for visual review before live deploy. 16 files (8 new, 8 modified), 6 atomic commits. **Migration required on live** — must run `php artisan migrate` on production AFTER upload BEFORE clicking new UI. | ⚠️ Needs Upload | 03fe918 |
| 260509-ibx | 2026-05-09 | draw.io / mxGraph build-vs-buy spike — 2-week evaluation gate before committing to v2.0 engineering-grade drawings milestone. User chose draw.io path over native build (saves est. 7-9 weeks if visual fidelity passes). Built via `--discuss` mode (user selected "All clear" — 8 D-LOCKs locked: self-host, lock-on-edit, small Teams Room only, 5 stencils, stub ports no AI, real project hookup, admin-only surface, canvas_state column reuse). Five tasks, single wave, one minor deviation auto-fixed (Rule 1 — XML attribute encoding: `<br/>` in mxGraph value attribute needs HTML-encoding). (1) Vendored draw.io v29.7.12 Apache 2.0 bundle to `public/vendor/drawio/` (132MB / 2899 files; mermaid sub-package dropped to trim). VERSION.md documents tag + license + manual update procedure. (2) Hand-coded 5-stencil pack at `resources/data/draw-io-stencils/21cav-mtr-spike.json` — Neat Bar Pro / Samsung 65" / ClickShare Bar Pro / Sennheiser TeamConnect / Netgear M4250 — each with manufacturer logo glyph, port rails, brand-aligned styling (#1B7A7A teal + #C07000 orange matching Mini O&M). (3) `DrawIoSpikeBuilderService` — pure aggregator, reads ProjectPackage equipment filtered to Teams Room area, emits mxGraph XML with deterministic grid layout. NO AI usage. (4) Admin Blade `admin/drawings/draw-io-spike/{project}` with iframe embed + postMessage handlers (load / save / export-svg). 3 routes, all admin-gated, NOT linked from any user-facing page. (5) `DrawingService::saveSpikeXml()` + `saveSpikeSvg()` reusing v1.3 canvas_state column + archive-prior pattern from Phase 18 P03 (D-LOCK-2). NO MIGRATION (D-LOCK-8 confirmed). v1.3 schematic generator untouched (zero diff against SchematicGeneratorService / SchematicD2SourceBuilder / DrawingDataResolverService). Spike enters 2-week visual-fidelity evaluation phase — end-of-week-2 user compares against Lucidchart Extron Concept reference PDF; verdict drives v2.0 milestone go/no-go. **Bundle = 132MB upload to live** — significant friction; trade for D-LOCK-1 self-host control. | ⚠️ Needs Upload | 2c44952 |
| 260525-pyu | 2026-05-25 | Relax authorization to a shared team workspace — finishes the in-flight D-15/D-19 "Projects are shared" decision. All 4 resource policies (Rams/OmManual/Project/ProjectDrawing) → `return true`; inline owner-or-admin gates + owner-scoped listing filters relaxed to `auth()->check()` across 9 controllers (incl. DocumentEditController seam + QuoteImport/ProjectPackageReview authorizePackage). Any authenticated user now has full view/download/edit/delete on all projects + documents; all `abort_unless(isAdmin)` admin-only gates (restore/force-delete/admin panels/trashed listings) PRESERVED. Fixes prod bug: non-admin staff (zack/alison) got 403 on RAMS PDF/DOCX download. Field-ops cluster (commissioning/install/tasks/time) scoped OUT pending user confirm. New SharedWorkspaceAccessTest 6/6 green (incl. exact bug repro); RamsRenderRegression byte-equiv 3/3 green; full suite 1311 pass / 12 pre-existing fails. 13 src files + 9 test files, 4 commits. **Migration: none. Deploy: git pull + `php artisan optimize:clear`.** | ✅ Deployed | 6b9d0b6 |
| 260525-s8b | 2026-05-25 | Follow-up to 260525-pyu (audit found remaining surfaces). Extends shared workspace to: RAMS upload/processing flow (`QuoteUploadController::authoriseRamsAccess`) + field-ops cluster — Commissioning (×4 controllers), InstallProgramme, Task assign/photo/status, TimeEntry. Relaxed owner/admin/engineer gates → `auth()->check()` across 10 controllers + `TimeEntryService::editEntry` (accountability kept via append-only TimeEntryAudit); un-scoped assigned-engineer task listings in InstallProgramme schedule/field. **PRESERVED:** `TimeEntryService::recordHeartbeat` owner-only liveness guard + all integrity guards (signoff immutability, file-existence 404s, content-sniff 422, throttles). New SharedWorkspaceFieldOpsAccessTest 7/7 (incl. heartbeat negative control + guest→login); full suite 1318 pass / 12 pre-existing fails. 11 src + 9 test files, 3 commits. **Migration: none. Deploy: git pull + `php artisan optimize:clear`.** | ✅ Deployed | 29c3fa3 |
| 260528-h8e | 2026-05-28 | Three quote/survey bugs surfaced by prod project `21CQ30485-03-OPS` (BMRS — items showed under area="Hardware", site survey had empty scope). **Bug A** (`QuoteParserService` ~L1858-94): area-picker walked back to most recent OVERVIEWTITLE without filtering section headers — now calls existing `isNonRoomSectionTitle()` in BOTH the area loop and the rooms collector (symmetric guard). **Bug B** (`ExtractQuoteJob::classifyItemType` L470): consumable desc-keyword fallback was guarded by `if ($upper === '')` so real-part-numbered cables (CS17461, AV16131, PL12980 extension leads) fell through to hardware — guard dropped, +4 keywords (`extension lead`, `mains extension`, `power lead`, `rj45`), dedupe of stray `'fixings', 'fixings'`. Default → hardware preserved (user decision). **Bug C** (`SurveyService::createFromProject` L157-93): `general_notes` was seeded from `works_description` which Phase 22.1 retired — new `resolveProjectScopeForSurvey()` helper uses 5-tier priority (extracted scope_of_works_bullets → overview → room_overviews → legacy works_description × 2), 3000-char cap. 16 new tests / 35 assertions green; RamsRenderRegression byte-equivalence 3/3 GREEN (D-12 preserved); 2 pre-existing reds in QuoteParserServiceTest verified unchanged. 3 src + 3 test files, 3 commits. **Retroactive (orchestrator-queued post-deploy):** `ReimportQuoteJob` for project `21CQ30485-03-OPS` then re-create the survey. Quick task [260528-h8e](./quick/260528-h8e-fix-3-quote-survey-bugs-from-21cq30485-0/). | ✅ Deployed | 72b289f |
| 260601-r4c | 2026-06-01 | **Engineer Reference Files** — new feature. Staff upload plans / drawings / cable schedules / Office docs to a project; engineers view them on the public **worksheet AND survey** links with **inline PDF preview + image gallery + tap-to-download** for CAD/Excel/Word. New `project_reference_files` table + `ProjectReferenceFile` model + `Project::referenceFiles()`. New `ProjectReferenceFileService` (H-07-compliant — adds `DocumentArtifactStorage::TYPE_REFERENCE`; finfo-only MIME sniff + extension allowlist (PDF/PNG/JPEG/WEBP/DWG/DXF/XLSX/XLS/DOCX/DOC/CSV) + deny list (svg/html/php/exe/etc.) + 20 MB cap; ULID-prefixed sanitised filenames; ZIP-as-OOXML gate + octet-stream→DWG/DXF gate mirroring existing patterns). New admin controller (3 authed routes, shared-workspace) + project show card. New public download endpoints on BOTH PublicWorksheetController + PublicSurveyController with explicit **cross-tenant 403 guard before any I/O** (T-r4c-01/02). Shared drawer partial reused on both engineer links. 10-threat STRIDE model. 41 new tests / 106 assertions GREEN; full suite +41 pass / 0 new failures; RamsRenderRegression 3/3 byte-equivalence preserved. 6 atomic commits + docs. **Migration: yes — `2026_06_01_120000_create_project_reference_files_table`. Deploy: git pull + `php artisan migrate --force` + `php artisan optimize:clear`.** Quick task [260601-r4c](./quick/260601-r4c-add-project-level-engineer-reference-fil/). | ✅ Deployed | 0131393 |
| 260602-mlt | 2026-06-02 | **Engineer worksheet polish** (surfaced by live project 75 `21CQ30362-01-OPS` Reading). **(A) Parser kit-list multi-line desc fix:** `QuoteParserService::extractDescriptionAfterTuple()` line 3060-3066 page-break truncator was chopping post-QTYEND description at the first `\d+ of \d+` page-marker line — multi-line descs (e.g. MT300 5-line "The MT300 intelligently connects AVer cameras with…") were captured as first line only. Swapped truncate-on-page-marker for **excise-page-banner-and-continue** `preg_replace` (removes page marker + trailing repeated-tag-header lines, joins desc on both sides). Next-PARTSTART boundary preserved as hard terminator. **(A') Also added** `ship_contact` + `ship_phone` to top-level `extracted_data` keys via `parseTaggedPdf()` return shape (lines 1941-1955) — parser was stripping SHIPCONT/SHIPPHONE as noise in 14+ regexes but never emitting them. **(B) Engineer Reference Files drawer:** `<details>` now `open` when files exist, accent swapped teal `#178A95` → **amber `#C07000`** to differentiate from teal kit drawer. One partial → both worksheet + survey surfaces. **(C) Worksheet/survey header:** new line `Site contact: {name} · <a href="tel:+44…">{phone}</a>` (UK phone normaliser, conditional render). 4 parser unit tests + 3 header feature tests all GREEN (35 assertions); RamsRenderRegression D-12 canary 3/3 GREEN throughout; full suite 1382 pass / 12 pre-existing fails. 5 commits. **Migration: none. Deploy: git pull + `php artisan optimize:clear`. Retro re-extract: project 75 (Reading)** via `reimportPending($user, $existingPackage)` so MT300 + ship_contact land. Quick task [260602-mlt](./quick/260602-mlt-engineer-worksheet-polish-surfaced-by-21/). | ✅ Deployed | 1065174 |
| 260602-o2a | 2026-06-02 | **Worksheet stale-data banner.** When a worksheet snapshot is older than the project package's last edit, surface an amber banner so admins/engineers know to regenerate. New `Worksheet::isStale()` + `staleSince()` accessors using `generated_data['generated_at']` ISO8601 (immutable across status flips — NOT `updated_at` which mutates). New shared partial `_stale-banner.blade.php` with 3 variants: **admin** (full banner + [Regenerate] button wired to existing `worksheets.retry-generation` route + `data-confirm` modal); **pill** (compact amber chip on worksheets/index + projects/show rows, mirrors `bound-pdf` stale-badge pattern); **public** (engineer link, informational only — engineer asks office, no button). Mounted on `worksheets.show`, `worksheets.index`, `projects.show`, `public-show.blade.php`. NOT mirrored on surveys/show (surveys are live-read, no snapshot to be stale — verified). 9 unit + 5 feature tests / 24 assertions all GREEN; RamsRenderRegression 3/3 GREEN. Cross-effect with 260602-mlt: after project 75 re-extract, all worksheets for project 75 will correctly show the stale banner — that's intentional, the parsed data shape DID change. 5 commits (TDD red/green). **Migration: none. Deploy: git pull + `php artisan optimize:clear`.** Quick task [260602-o2a](./quick/260602-o2a-worksheet-stale-data-banner-when-workshe/). | ✅ Deployed | 88586a9 |
| 260602-rcd | 2026-06-03 | **Engineer Report — enhance View + PDF endpoint.** User-pivoted scope: rather than build a new page (which would duplicate ~70% of `worksheets/show` — sign-offs + per-room label captures with lightbox already there), ENHANCE the existing View with the 3 actual gaps. (1) **Completed-Work Photos per room** subsection (reuses `openPhotoLightbox`); (2) **Outstanding Items aggregate** card at top (flat list of every snag across signoffs, hidden when empty); (3) **📄 Engineer Report PDF** button in page-header actions (disabled with tooltip when no engineer activity). New `EngineerActivityService::buildReportContext()` is single source of truth for both View + PDF. New `Worksheet::hasEngineerActivity()` accessor. New PDF endpoint `GET worksheets/{worksheet}/engineer-report.pdf` (PdfRenderService::fromBlade, Browsershot, photos base64-inlined via PdfImageEmbedder mirroring `pdf/site-survey/client-report.blade.php`); throttle 30/min; 404 when no activity. **📄 Engineer Report button on projects.show worksheet rows** — always visible, greyed when no activity. 12 unit + 9 feature tests / 48 assertions GREEN; RamsRenderRegression 3/3 GREEN. 1 deviation: completed-work photo URLs route through existing `public-worksheet.photos.serve` (token-gated, token already on same admin page) since no admin-only serve route exists. 4 commits. **Migration: none. Deploy: git pull + `php artisan optimize:clear`.** Quick task [260602-rcd](./quick/260602-rcd-engineer-report-admin-only-html-pdf-view/). | ✅ Deployed | 03ad3f4 |
| 260603-eha | 2026-06-03 | **Offline photo queue** on the public engineer worksheet link. Engineers in dead zones (comms cupboards, basements, racks) no longer stall when uploads fail. Client-side **IndexedDB queue** survives reload + drains on `window.online` event. New OfflineQueue module + showToast helper + per-row drain (with 200ms inter-post sleep to respect 30/min throttle). Wrapped existing `captureLabel` + `uploadWorksheetPhoto` JS via override-by-reassignment (originals stay callable for online happy-path). HEIC normalisation extended to completed-work photo flow too (bonus: smaller files even online). Quiet `🔄 N pending` chip in page header → click expands queue panel with per-item Retry / Remove / global Retry-all. Failed items NEVER auto-delete (anti-repudiation; ≥3 attempts surfaces warning toast). Server endpoints + contracts UNCHANGED. 820 lines added in single file `resources/views/worksheets/public-show.blade.php`; 3 atomic commits. RamsRenderRegression 3/3 GREEN. Verification = 8-test manual checklist (full body in SUMMARY) covering offline/online/persistence/throttle/regression — IDB layer not unit-testable from Pest. 1 deviation: uploadWorksheetPhoto wrapper inlines POST rather than delegating (rationale: HEIC normalisation + silent network-error routing). **Migration: none. Deploy: git pull + `php artisan view:clear` (Blade-only change).** Quick task [260603-eha](./quick/260603-eha-offline-photo-queue-on-engineer-workshee/). | ✅ Deployed | 4dcf21c |
| 260603-q7t | 2026-06-03 | **QuoteWerks parser — second template variant ("ram" priced)** support. Discovered live (project 78 Cicor Hartlepool) that QuoteWerks generates TWO different export schemes: unpriced "OPS" uses full-word `SITENAMESTART/PARTSTART/QTYSTART/OVERVIEWTITLESTART` tags (parser handled this since v1.0); priced "ram" uses short `H1/H1E/H4S/H4E/D1S/D1E/P1S/P2S/P3S/P4S/P5S` tags with column-layout interleaving (text gets split across markers, e.g. `D1S F D1E irst Floor Training Room`). Parser had ZERO recognition of the short-tag variant → markers leaked as literal text everywhere (`client_name = "H1 Cicor … H1E H4S Jamie Powis H4E"`, postcode `TS25` parsed as a SKU, line items parsed as room names). Added preprocessing pipeline: (1) `detectTagVariant()` — defaults to 'long' on uncertainty so existing PDFs untouched; (2) `translateShortTagsToLong()` — 6-pass: de-interleave column-split markers (`H 1E` → `H1E`), substitute short→long tags, route D1S/D1E pairs (first=OVERVIEWTITLE, second=OVERVIEWTXT), strip `P4S £nnn P4E` price + `P5S X P5S` manufacturer (captured to side-channel for future), backfill synthetic PARTDESC for blank rows; (3) wire-in at `parse()` L148-L152. 23 new tests / fixture from literal pdftotext output of the actual Cicor PDF. RamsRenderRegression 3/3 GREEN; canary verified the wire-in doesn't pollute long-tag path (wire-in commented out → snapshot still byte-identical). 2 pre-existing parser test failures verified unrelated. 2 deviations auto-applied: Pass 6 PARTDESC backfill (without it, `looksLikePartAndQtyOnly` heuristic drops ~12 of 24 Cicor rows); O(N²)→O(N) perf fix in routeDPairs. 4 atomic commits. **Migration: none. Deploy: git pull + `php artisan optimize:clear`. Retro: re-extract project 78 via `reimportPending($user, $prev)` — capture `$prev = $project->latestPackage` BEFORE the call.** Quick task [260603-q7t](./quick/260603-q7t-quotewerks-parser-support-the-priced-ram/). | ⚠️ Needs Upload | 9192768 |
| 260712-w5k | 2026-07-12 | **Tier-1 AV RAMS gap-closure follow-up — closes the 4 deviations found during live verification of 260712-twi on Tilda RAMS #87.** Four atomic commits, zero migrations, additive to `config/rams_tier1.php` + emergency form + 1 regression test. **(T1 72e9d56)** Appended 3 AV-specific baseline hazards to `config/rams_tier1.php` `baseline_hazards` array (total 11, was 8): (a) "Laser Projector Eye Safety (IEC 60825 Class 3B/3R)" — 5 controls covering class labelling, no-direct-viewing, commissioning viewing zone, interlock circuits, warning labels; pre 3×4=12, post 1×3=3. (b) "RF Exposure from Wireless Microphone Systems" — 4 controls covering Ofcom licence-exempt band verification, ICNIRP body-worn distance, antenna install separation, Wi-Fi/DECT/SCADA coordination; pre 2×2=4, post 1×1=1. (c) "Cable Pulling Injuries & Cable Damage" — 5 controls covering Cat6a 110N max pull tension per TIA-568-D, pulling gloves, pull-assistant briefing, cable lubricant, fibre-optic jacket-strain relief kits; pre 3×3=9, post 1×2=2. Existing 8 hazards preserved byte-for-byte. **(T2 68fd605)** Appended `BS EN 60825-1:2014+A11:2021 — Safety of laser products — Equipment classification and requirements` to `standards_references` array (total 10, was 9). Existing 9 standards preserved. **(T3 d24f593)** Added 2 new site emergency capture fields: `site_emergency_electrical_isolation_switch` (text input) + `site_emergency_fire_extinguisher_class` (select dropdown with 7 options: TBC, Class A/B/C/D/F + CO₂ — Class C highlighted as mandatory for AV rack areas). Explicit-whitelist controller pattern extended in both validation (lines 402-409) and mapping (lines 551-559) blocks of `RamsController::updateAndDownload()`. `pdf/rams.blade.php` Section 7.0 emergency details table gets 2 new rows displaying isolation switch location + fire extinguisher class when populated. **(T4 216435a)** Section 7.0 heading investigation from 260712-twi verification RESOLVED — no bug to fix. My earlier grep looked for "Site Emergency" but the actual Blade heading renders as `"Site-Specific Emergency Details"` (with hyphen). Task 4 shipped a 3-case regression test at `tests/Feature/Rams/RamsSection70HeadingTest.php` locking the heading render in both populated and empty branches: (a) reviewed_data.site_emergency populated → PDF contains "Site-Specific Emergency Details" heading; (b) empty → amber "TBC at induction" banner; (c) regression lock proving heading always renders (no bare-table regression path). Commit prefix `test(rams-pdf):` per planner guidance since no source bug was found. Test tally: 293 pass / 1191 assertions across full Rams sweep (14 new). NO migrations required. **Deploy:** `git pull` → `php artisan config:cache` → `php artisan view:clear`. Then regenerate a RAMS to visually confirm: 11 baseline hazards (or engineer-supplied), BS EN 60825-1 in Section 3, 10 emergency form fields, and populated PDF Section 7.0 renders isolation switch + extinguisher class rows. **⚠️ H&S PROFESSIONAL SIGN-OFF STILL PENDING** — 3 new hazards drafted from AVIXA + IEC + Ofcom + TIA-568-D best practice but not yet reviewed by 21CAV's H&S consultant. Deferred / Next: sensitive-space hazard baselines per space type (data centre / broadcast / medical / education), F10 CDM auto-trigger from project meta, formal H&S consultant sign-off session on the full 11-hazard + 10-standard + full COSHH inventory baseline set. Quick task [260712-w5k](./quick/260712-w5k-tier-1-av-rams-gap-closure-3-hazards-las/). | ⚠️ Needs Deploy | 216435a |
| 260712-twi | 2026-07-12 | **Tier-1 AV RAMS content upgrade — closes G-1 to G-20 gaps identified in the Tilda RAMS #87 verification pass.** Five atomic commits, zero migrations, everything rides on existing `reviewed_data` + `generated_data` JSON columns + new config file. **(T1 1091ea4)** New `App\Services\Rams\Tier1RamsDefaultsService` + `config/rams_tier1.php` carrying 4 top-level config blocks: `av_baseline_hazards` (8 canonical AV install hazards each with pre/post L×S scores + control-measure bullets: Working at Height, Manual Handling, Electrical Shock, Dust & Debris, Laser Projector Eye Safety per IEC 60825 Class 3B/3R, Cable Pulling Injuries per Cat6a 110N max tension, RF Exposure per Ofcom, Lone Working / DSE), `standards_references` (BS 7671:2018+A2:2022, BS 8300, BS 5839-1, BS EN 60849, BS EN 60825, AVIXA F501.01, CIBSE Guide E, ISO 45001), `coshh_inventory` (6 AV products with GHS/CLP hazard codes: isopropyl alcohol H225/H319/H336, Sn/Pb solder + rosin flux H360/H373/H317, silicone thermal compound H319/H315, contact cleaner H222/H336, cable pulling lubricant H319, expanding foam H319/H332/H334/H351), and `av_specific_controls`. Wired into `RamsBuilderService` at both `runFromReview()` (~line 254) and `runPipeline()` (~line 698) so PDF + DOCX + persisted `generated_data` all inherit the tier-1 defaults. Fallback layer only — engineer-supplied hazards/COSHH/standards always win. Kill-switch via `RAMS_TIER1_DEFAULTS=false` env flag. Config file carries a safety-critical warning comment block at the top saying H&S consultant review is required before litigation-adjacent use. **(T2 25b42d7)** `pdf/rams.blade.php` Section 5 hazard register: when reviewed hazards is empty, the baseline 8-hazard set from Task 1 is prepended with an italic "Note: baseline AV install hazards populated from company Tier-1 standard. Engineer must review and add project-specific hazards during the review stage." `MethodStatementPrompt` extended with 4 new AV-specific guidance bullets (cable pulling force limits, laser projector commissioning safety, ground-loop test procedure, rack tip-over protection during install). **(T3 0403fb1)** New "Site Emergency Details" fieldset on `resources/views/rams/review.blade.php` — 4 nullable inputs: Nearest A&E hospital (name + postcode + drive time), Nearest defibrillator (location), Electrical isolation switch (location), Fire extinguisher class dropdown. `RamsController::updateAndDownload()` persists to `reviewed_data.site_emergency` AND mirrors into `generated_data.site_emergency` so the PDF's primary lookup path succeeds directly. `pdf/rams.blade.php` Section 7 gets a new "7.0 Site Emergency Details" table above 7.1 when populated, or an amber "⚠ Site emergency details to be confirmed at induction — populate at review stage" flag when empty. Section 7.2 (Accident/Injury) extended with a RIDDOR routing matrix (death/specified injury/over-7-day → HSE F2508 within 15 days; dangerous occurrence → 10 days; occupational disease → 10 days). **(T4 fbcd76d)** `pdf/rams.blade.php` COSHH Assessment section: 6-column structured table (Product · Product Ref · Hazard Codes (GHS/CLP) · PPE Required · Storage · First Aid) populated from `$data['coshh_inventory']`. Engineer-added `$data['coshh']` bullets still render ABOVE the table (superset change). Legacy 4-bullet list kept in `@else` branch as safety net when tier-1 defaults are disabled. **(T5 503461c)** Structural polish on `pdf/rams.blade.php`: TOC page inserted between cover and Section 1 (numbered jump-guide, no page refs since Browsershot doesn't native-render TOC page refs); PPE colour-coding paragraph in Section 6.3 (hi-vis Class 2 for indoor site work, Class 3 for highway/depot, hard hat colours); Applicable Standards two-column table under Section 3 (H&S Policy Statement) populated from `standards_references` config; new Section 6.11 "Coordination & Interfaces with Other Trades" (M&E, ceiling grid, cable tray, IT, fire alarm, joinery, FF&E — standard coordination protocol paragraph about weekly progress meetings + RFI routing); new Section 6.12 "IT & Network Working" (separate laptop policy, no personal USB, verified software installs, credential handling via company password manager, VLAN separation, post-install default-password rotation + client IT handover). Both new sections land after 6.10 (Supervision & QA) and before the non-numbered Decommissioning Procedure heading. 5 files touched (1 config, 1 service, 1 controller, 2 Blade views), 4 new test files (`tests/Unit/Services/Rams/Tier1RamsDefaultsServiceTest.php` + `tests/Feature/Pdf/RamsBaselineHazardInjectionTest.php` + `tests/Feature/Rams/SiteEmergencyDetailsTest.php` + `tests/Feature/Pdf/RamsCoshhInventoryTest.php` + `tests/Feature/Pdf/RamsStructuralUpgradeTest.php`). Test tally: 65 pass / 260 assertions (19 new + 46 existing Rams). **NO migrations required** — deploy is `git pull` → `php artisan config:cache` → `php artisan view:clear`. Then regenerate Tilda RAMS #87 to confirm all 7 acceptance criteria. **⚠️ H&S PROFESSIONAL SIGN-OFF NEEDED** — config/rams_tier1.php carries safety-critical defaults drafted from industry-standard AV install practice, but MUST be reviewed by 21CAV's H&S consultant against their specific insurance requirements + site-specific waivers before wider client-facing use in litigation-adjacent RAMS documents. Deferred / Next in SUMMARY: laser class per specific projector model (needs product-catalog integration), site geolookup for A&E hospital (Google Places API), sensitive-space hazard baselines (data centre/broadcast/medical/education), F10 CDM notification auto-trigger. Quick task [260712-twi](./quick/260712-twi-tier-1-av-rams-content-upgrade-baseline-/). | ⚠️ Needs Deploy | 503461c |
| 260712-ip3 | 2026-07-12 | **Cable schedule polish bundle — negative_keywords + regenerate button + rule preview endpoint.** Follow-up to 260712-euh after verification pass found a real bug on live: "Logitech USB 3.0 webcam" matched the Codec rule (priority 70, keywords include "logitech") instead of the USB 3.0 rule at priority 141 because first-substring-match-wins with priority-first ordering. Three atomic commits. **(T1 c594ffa)** Migration `2026_07_12_100000_add_negative_keywords_to_device_cable_rules` adds nullable JSON `negative_keywords` column on `device_cable_rules`. New private helper `CableScheduleGeneratorService::ruleMatches($lower, $rule)` — positive keywords must match AND no negative keyword may match. Seeder updates codec rule (priority 70) with negative_keywords `["usb 3", "usb 3.0", "usb-c webcam", "usb hub"]` so USB-branded Logitech/Poly devices skip the codec rule and fall through to the USB rules. Camera/PTZ rule (priority 80) same treatment. Amp analog (priority 61 per executor's earlier byte-for-byte split) gets `["lamp", "champagne"]` as defensive-depth. `Admin\DeviceCableRuleRequest` validates `negative_keywords` array with a mirror `negative_keywords_raw` textarea shim following the existing pattern. Admin form adds second textarea under keywords labeled "Negative Keywords (skip rule when any match)". Index view shows "N excl" hint next to tier badge. 9 new tests. **(T2 6944e00)** Regenerate button added to `resources/views/cable-schedule/edit.blade.php` page-header actions. Reuses existing `cable-schedules.retry-generation` route + `CableScheduleController::retryGeneration()` method (planner verified both exist at CableScheduleController.php:480 and routes/web.php:388). Uses existing `data-confirm` pattern from 260504-m2k — no new JS. When `$schedule->status === 'generating'` an amber "Regenerating…" badge shows instead of the button. 4 new tests. **(T3 22fce18)** New GET `/admin/device-cable-rules/preview` endpoint on `Admin\DeviceCableRuleController::preview()`. Accepts `equipment=` and `length_m=` query params (executor renamed from `name=` for clarity). Returns JsonResponse with matched_rule (id/priority/name), tier picked (index/max_m/cable_type/etc.), final result payload, and a full trace array explaining each rule's match/skip decision. Route registered BEFORE Route::resource so `preview` isn't parsed as `{deviceCableRule}` id. New public method `CableScheduleGeneratorService::previewInference()` walks rules in priority order recording trace, then picks tier if length is non-null. Admin UI extends `resources/views/admin/device-cable-rules/index.blade.php` with a "🧪 Test a rule" Alpine.js card at top of page — text input for equipment name + optional number input for length + Preview button + result card showing matched rule, tier, result, and expandable trace. Would have caught the Logitech bug immediately without SSH. 6 new tests. Test tally: 72 CableSchedule + 44 DeviceCableRule = 116 pass + 13 new = 129 total pass / 545+ assertions. Byte-for-byte regression preserved. **Migration + seeder + cache flush required on live** — `php artisan migrate --force` → `php artisan db:seed --class=DeviceCableRulesSeeder --force` → `php artisan cache:clear` → `php artisan view:clear`. Post-deploy: hit `/admin/device-cable-rules/preview?equipment=Logitech%20USB%203.0%20webcam&length_m=20` → should return `matched_priority: 141` (USB 3.0), NOT 70 (codec). Deferred / Next in SUMMARY: cross-rule chain intelligence for redundant paths, per-row decision log on cable schedule edit page (needs inference_rule_id + inference_tier_index schema fields), preview endpoint rate-limiting for external audit consumption. Quick task [260712-ip3](./quick/260712-ip3-cable-schedule-polish-bundle-negative-ke/). | ⚠️ Needs Deploy | 22fce18 |
| 260712-euh | 2026-07-12 | **Cable schedule length-tier decision engine (passive → active copper → fibre auto-swap).** Extension of the 260711-q7q Tier 3-D rules engine. Instead of one flat `cable_type` per rule, each rule now carries an optional `length_tiers` JSON array — the schedule generator picks the correct tier based on the row's `approx_length_m` (populated by T1-E from survey narrative). The cable_type actually SWAPS from HDMI to HDBaseT to fibre, not just gets a warning appended. Five atomic commits. **(T1 3ae22a7)** Migration adds nullable JSON `length_tiers` column on `device_cable_rules`; model casts to array. **(T2 4bd576d)** `inferCableRun($equipName, ?float $lengthM = null)` — new optional length parameter; when the matched rule has tiers AND length is non-null, first-tier-where-`length <= max_m` wins; length exceeds all tiers → last tier + "⚠⚠ Length {L}m exceeds max range" warning; length is null → tier 1 + "⚠ Length not confirmed on survey — assuming passive tier" warning; empty tiers → falls back to flat cable_type (byte-for-byte legacy). Old `DISTANCE_WARNING_RULES` const + `computeDistanceWarnings` method DELETED — tier auto-swap replaces advisory warnings. **(T3 af7deb2)** Seeder populates tiers on 12 of 15 existing rules AND adds 5 new rules — priority 130 USB 2.0 (5m passive → 15m active → 100m Cat6 extender → 300m fibre), 135 USB 3.0/3.1 (3m passive → 10m active → 100m fibre), 140 DisplayPort (2m passive → 10m active → 50m fibre), 145 SDI/HD-SDI (100m Belden 1694A → 300m 4694R → 5000m fibre), 150 Optical fibre backbone (flat). All tiers reference industry-standard AV distances. **(T4 61a72cd)** Admin UI Alpine.js tier editor inside collapsible `<details>` on the rule form; Add Tier / Remove Tier controls; hidden input serialises sorted array on submit. FormRequest validates `length_tiers.*.max_m` (numeric > 0), `.cable_type` (required max 200), etc.; auto-sorts by max_m ascending server-side. Index view shows "N tiers" badge next to the cable_type column. **(T5 51428a6)** 8 tier-selection unit tests (tier 1 / tier 2 / over-max / null-length / no-tiers fallback / USB 3.0 fibre / DP active / SDI low-loss coax) + 4 CRUD Feature tests (add via form / update via form / max_m validation / sort-on-save). Test tally: 97 pass / 467 assertions across DeviceCableRule + CableSchedule suites. Regression protected via null-length calls returning tier 1 which mirrors pre-refactor flat strings. **Migration + seeder + cache flush required on live** — `php artisan migrate --force` → `php artisan db:seed --class=DeviceCableRulesSeeder --force` → `php artisan cache:clear`. Existing schedules unchanged until regenerated. On regeneration: rows with tiered rules AND survey-derived length auto-swap; rows without length default to tier 1 with the "not confirmed" note. Total rules on live after deploy: 20 (15 existing + 5 new). Deferred / Next in SUMMARY: signal-integrity intelligence for AV-over-IP (NDI 5, Dante bandwidth), per-manufacturer rule packs (Shure MXW, Cisco Room Kit, QSC, Extron, Kramer), "test-this-name" preview endpoint on the rules admin index. Quick task [260712-euh](./quick/260712-euh-cable-schedule-length-tier-decision-engi/). | ⚠️ Needs Deploy | 51428a6 |
| 260711-q7q | 2026-07-11 | **Tier 4 admin device editor UI + Tier 3-D data-driven cable rules engine.** Follow-up to 260711-oh4 which shipped `Device.is_critical` / `pse_budget_w` / `pd_load_w` with no admin UI. Two atomic commits, one migration, one seeder. **(Task 1 de45752)** New `/admin/devices` surface — index (paginated 15/page, filter by project, search on manufacturer/model/part_no, badges for signal_role, ✓ for is_critical, PSE budget + PD load columns), edit form (Identity read-only display, editable room_name, signal_role radio pills for source/destination/processor/unclassified, "Set critical" checkbox, PoE Metadata number inputs). New `App\Http\Controllers\Admin\DeviceController` + `DeviceFormRequest` matching UserController pattern. Route added to Admin dropdown in `resources/views/layouts/navigation.blade.php`. 6 feature tests green. **(Task 2 67d783d)** New migration `2026_07_11_000005_create_device_cable_rules_table` (id, name, priority, keywords JSON, cable_type, cores, signal_type, to_endpoint, notes, is_active, timestamps + index on priority+is_active). New `App\Models\DeviceCableRule` with `Cache::remember('device_cable_rules.active', 3600, ...)` + boot hooks flushing cache on save/delete. New `DeviceCableRulesSeeder` populates 15 canonical rules (mic and amp branches split — Shure vs analog / Dante vs analog — for byte-for-byte parity with previous hardcoded logic). New `Admin\DeviceCableRuleController` full resource CRUD + FormRequest + Blade views (`index`, `create`, `edit`, shared `_form` partial). Route added to Admin dropdown. Existing `CableScheduleGeneratorService::inferCableRun()` refactored to load rules from DB via `DeviceCableRule::forInference()` — 13-branch if/else deleted, replaced with a foreach loop. All 8 unrelated consts (LABOUR_KEYWORDS etc.) preserved. Regression test extended with 8 byte-for-byte assertions confirming pre-refactor equivalence. `CableScheduleGeneratorServiceTest` + `CableScheduleDagGenerationTest` + `CableScheduleStoreDeterministicTest` gained `RefreshDatabase` + `$this->seed(DeviceCableRulesSeeder::class)` in setUp() so their existing assertions keep working under the DB-backed rules path. 96 tests / 485 assertions green across the sweep. **Migration + seeder + cache flush required on live** — `php artisan migrate --force` then `php artisan db:seed --class=DeviceCableRulesSeeder --force` (idempotent, updateOrCreate keyed on name) then `php artisan cache:clear` to flush the 1h inference cache so admins see changes immediately. After deploy: `/admin/devices` and `/admin/device-cable-rules` both live — verify with admin login. Cable schedule regeneration picks up any admin rule changes on next generation (1h cache TTL, flushed automatically on rule save/delete via boot hooks). Deferred / Next in SUMMARY: bulk-edit action on `/admin/devices` for setting is_critical + PoE en masse across a project; per-manufacturer rule packs (Shure / QSC / Crestron / Extron); "test this equipment name" preview tool on the rules index. Quick task [260711-q7q](./quick/260711-q7q-tier-4-device-editor-ui-tier-3-d-data-dr/). | ⚠️ Needs Deploy | 67d783d |
| 260711-oh4 | 2026-07-11 | **Cable schedule intelligence — cross-room chains + redundancy + PoE budget bundle.** Three linked upgrades on top of 260711-n2x. **(T2-B-ext d85922c)** Cross-room signal chain traversal — new `CENTRAL_ROOM_KEYWORDS = ['comms', 'rack', 'server', 'it', 'av rack', 'main rack', 'plant room', 'central']` const. `buildSignalGraph()` now accepts both local + central device collections; local room's per-signal_type graph falls back to central sources/processors when local ones are absent (per signal_type independently — mixed cases handled). Cross-room rows get `"Cross-room: {src_room} → {dst_room}"` notes prefix. Projects with NO central-room devices behave byte-for-byte identical to pre-bundle. **(T3-B 3bfd18f)** Redundancy pass for critical processors — 2 migrations add nullable `is_critical` BOOLEAN to `devices` and nullable `is_redundant` BOOLEAN to `cable_schedule_items`. Emitted rows involving an `is_critical=true` processor get a paired backup row with `cable_id` suffix `-R` (e.g. C-001 + C-001-R), `[REDUNDANT] Primary + backup path — diverse routing recommended` notes prefix, `is_redundant=true`. Early-return when no critical devices exist keeps zero cost on typical projects. **(T3-C 922bd3d)** PoE budget solver — 1 migration adds nullable `pse_budget_w` FLOAT + `pd_load_w` FLOAT to `devices`. New `checkPoeBudgets($scheduleId)` runs post-persist: groups PoE-cabled rows by destination switch, sums PD loads, compares to switch's PSE budget. ≥80% → ⚠ warning appended to all rows in the group; ≥100% → ⚠⚠ OVER BUDGET prefix. Soft opt-in — any null metadata skips the check. 3 new migrations + 5 files touched. 13 new tests (4 T2-B-ext + 4 T3-B + 5 T3-C) + all pre-existing = 101 pass / 466 assertions. **Migrations required on live** — `php artisan migrate --force` runs all three additive nullable columns. Features are dark until data is populated (rooms match CENTRAL_ROOM_KEYWORDS naturally; `is_critical` + PoE metadata need admin population — currently admin-DB-only via `tinker` / direct UPDATE; Tier 4 UI to expose these fields in the device editor is flagged as follow-up in SUMMARY). Quick task [260711-oh4](./quick/260711-oh4-cable-schedule-intelligence-t2-b-ext-t3-/). | ⚠️ Needs Deploy | 922bd3d |
| 260711-n2x | 2026-07-11 | **Cable schedule intelligence T1-D + T1-E + T2-A + T2-B bundle (major upgrade).** Follow-up to 260711-ml0. Four atomic commits enrich the row payload with quoted cable products + survey-derived lengths + distance warnings, extract a shared port-resolver, and swap the per-item guessing loop for a graph-based DAG traversal. **(T1-D 81bf1c0)** Quoted cable products override cable_type by signal_type: new `QUOTED_CABLE_SIGNAL_KEYWORDS` const maps consumable-bucket products (Belden 8451, Kramer C-HM/HM-6, etc.) to `video/audio/network/speaker/usb/power`; when a room's signal_type has a quoted match, `cable_type = quoted product name` and notes prefix `"Quoted: {name}"`. Shure+Cat6 reclassifies network→audio. **(T1-E 18f712b)** Survey narrative populates `approx_length_m`: reads `SiteSurveyRoom.cable_routes` (priority 1) → `engineer_feedback['cable_routes']` → `cable_route_desc`, LAST regex match wins. New `DISTANCE_WARNING_RULES` matrix appends warnings to notes when HDMI > 15m / Cat6 PoE > 100m / HDBaseT > 100m / 2-core speaker > 30m. **(T2-A 7cd87f4)** New `App\Services\Cable\StencilPortResolver` service consolidates the bulk part_number lookup pattern; three existing consumers refactored in the same commit (BackfillCablePortFksCommand + CableScheduleController::edit + CableScheduleGeneratorService). Generator now writes `source_device_id` / `source_port_id` / `dest_device_id` / `dest_port_id` on rows where both endpoint stencils have matching-signal-type ports (direction=out/side=right for source, in/left for dest). Auto-generated Tier-1 stencils without ports fall through NULL — row still renders via text. `test_v13_surface_files_have_zero_phase22_column_references` guard updated to remove the generator (opting-in is T2-A's whole purpose); XLSX + schematic read-path guards intact. **(T2-B 7b22378)** DAG traversal replaces flat per-item loop for `generateFromDevices()`: `SIGNAL_PATH_ORDER = ['dsp', 'audio-processor', 'matrix', 'switcher', 'codec', 'amplifier']` orders processors along the chain. `buildSignalGraph()` groups devices per room by signal_type into source/processor/destination buckets; `emitDagEdges()` walks source → ordered processors → destination emitting one row per edge. Sources without a destination emit a single "→ TBC" row + engineer warning. Feature-gated per-room: if every device in a room has `hasUnknownSignalRole()` the room falls back to `generateFromDevicesFlat()` (pre-bundle behaviour byte-for-byte). New tests: `tests/Unit/Services/Cable/StencilPortResolverTest.php` + `tests/Feature/Cable/CableScheduleDagGenerationTest.php` covering DAG traversal cases (source only / source→dest / source→1 proc→dest / source→3 procs→dest / mixed classified+unclassified). Extended `tests/Unit/Services/Cable/CableScheduleGeneratorServiceTest.php` with T1-D and T1-E cases. Test suite: 90 passing / 392 assertions across Unit + Feature Cable + Notification suites. RamsRenderRegression unchecked here (out of scope). 7 files touched (3 new, 4 modified). **NO migration required — pure code deploy.** Legacy generated rows render unchanged until admin regenerates. **Behaviour change** — projects with signal_role-classified Devices emit different row counts (DAG chains, not one-row-per-device). Quick task [260711-n2x](./quick/260711-n2x-cable-schedule-intelligence-t1-d-t1-e-t2/). | ⚠️ Needs Deploy | 7b22378 |
| 260711-ml0 | 2026-07-11 | **Cable schedule intelligence T1 bundle (upgrade).** Three linked improvements after live smoke-test surfaced weaknesses. **(T1-A) Signal-type column + colour-coded XLSX.** Additive migration `2026_07_11_000001_add_signal_type_to_cable_schedule_items` adds `signal_type` VARCHAR(20) nullable on `cable_schedule_items`. `CableScheduleGeneratorService::inferCableRun()` now maps each of the 13 keyword branches to one of 8 signal types (video/audio/speaker/control/network/usb/power/unknown) matching `config('cables.signal_type_colours')` keys — a config surface that was previously orphaned. `CableScheduleXlsxService` inserts a new "Signal" column between "Cable Type" and "Cores"; each row's cell tinted at ~15% opacity of the signal type's hex (audio red, video blue, control green, network purple, speaker teal). **(T1-B) Prefer Device model over quote line items.** When project has real `App\Models\Device` rows, `CableScheduleGeneratorService::generate()` branches to new `generateFromDevices()` which iterates the collection using `Device.room_name` + `is_rack_mounted` + `u_height` for `from_location` (appends " (Rack, {u}U)") and prefixes notes with `[{signal_role}]` when Phase-17 CRIT-05 signal_role is set. Projects without Device rows fall through to existing quote-line logic byte-for-byte. **(T1-C) Word-boundary `matchesAny`.** Swapped `str_contains()` for `preg_match('/\\b{keyword}\\b/i')` so "Microsoft" no longer classifies as microphone, "Ceiling Lamp Fixture" no longer as amplifier, "Cisco Switch" no longer as HDBaseT extender. New `tests/Unit/Services/Cable/CableScheduleGeneratorServiceTest.php` with 4 cases pinning the fix (all green). Regression test's XLSX column count bumped 8→9. **Migration required on live** — must run `php artisan migrate --force` after `git pull`. Existing rows have `signal_type=NULL` and render with no colour tint until regenerated (regenerate any schedule via the ⋮ Retry action). Test suite: 4 new unit + 3 regression + 2 notification + 35 broader = 44 passing / 299 assertions. 5 files (2 new, 3 modified), 3 atomic commits (b9676b6 T1-A + 105432d T1-B + 0eae352 T1-C). Quick task [260711-ml0](./quick/260711-ml0-cable-schedule-intelligence-t1-bundle-si/). | ⚠️ Needs Deploy | 0eae352 |
| 260711-env | 2026-07-11 | **Cable schedule truncation fix (live blocker).** Every cable schedule generation on live failing with `SQLSTATE[22001] Data too long for column 'from_location'` — reproducing on project 65 (Tilda). Root cause: quote line items with empty `name` fall back to `description` (OE Electrics 200+ char product marketing blurbs), and `cable_schedule_items` `from_location` / `to_location` / `cable_type` are VARCHAR(255). Two-layer fix. **(a) Migration** `2026_07_11_000000_widen_cable_schedule_item_location_columns.php` widens the three columns to TEXT via `->change()` (non-lossy; doctrine/dbal already present). Reversible `down()` restores VARCHAR(255) nullable. **(b) Service** `CableScheduleGeneratorService::generate()` + `buildRowsFromEquipmentLines()` clip `$equipName` via `Str::limit($equipName, 180, '…')` before concatenating into `from_location` so the XLSX cell stays readable and inserts stay bounded even if the schema widen is ever rolled back. `inferCableRun($equipName)` still receives the full string so cable-type classification is unchanged. 2 atomic commits (ab64ac8 migration + d0ab3c4 service); `php -l` clean on both files. **Migration required on live** — must run `php artisan migrate --force` after `git pull`. **Deferred / Ops:** RAMS queue worker not running on live — add cron `@reboot sudo -u stcav /usr/local/bin/php /home/stcav/rams.21stcav.com.git/rams.21stcav.com/artisan queue:work --tries=3 --timeout=600 --sleep=3 >> …/queue.log 2>&1 &` mirroring the SCC worker at `/home/stcav/service.21stcav.com`. Quick task [260711-env](./quick/260711-env-fix-cable-schedule-truncation-widen-colu/). | ⚠️ Needs Deploy | d0ab3c4 |
| 260604-p9u | 2026-06-05 | **PdfTextExtractorService — auto-switch to `pdftotext -layout` for short-tag QuoteWerks PDFs.** Follow-up to 260603-q7t: the translator worked on clean column-positioned input, but smalot stripped the column positions destroying the priced-template tag/content relationship (live verified pkg 142 — `H1 H4S H4E` clustered on one line, content on another, no way to pair them). REAL fix is an in-stage flag switch in the existing pdftotext Stage-0 call: detect short-tag variant in `-raw` output → re-run with `-layout` instead → translator (q7t, untouched) works as designed on the cleaner column-preserved text. New `looksLikeShortTagQuoteWerks()` detector + `extractWithPdfToTextLayout()` + `resolvePdfToTextBinary()` helper shared with existing Stage-0. Graceful fallback: layout call throws/returns-empty → log warning + return original `-raw` text (translator's column-split repairs handle the fallback shape). Replaced 260603-q7t's wrong chat-trail fixture with real pdftotext -layout output. 37 new/updated tests / 272 assertions GREEN; RamsRenderRegression 3/3 GREEN; translator (QuoteParserService) UNTOUCHED — q7t idempotency held. 2 deviations: visibility relaxation private→protected for anonymous-subclass test override pattern, warning de-duplication. 3 atomic commits. **Migration: none. Deploy: git pull + `php artisan optimize:clear`. Retro: project 78's THIRD re-extract** via `reimportPending($user, $prev)` — `$prev = $project->latestPackage` BEFORE the call; watch worker log for `"PdfTextExtractorService: using pdftotext -layout output (short-tag variant detected)"`. Quick task [260604-p9u](./quick/260604-p9u-pdftextextractorservice-auto-switch-to-p/). | ⚠️ Needs Upload | b503f52 |
| 260713-sk1 | 2026-07-13 | **Schematic editor discovery spike (React Flow, 2-week throwaway).** Four atomic commits + docs. Goal: prove drag-to-connect + port-type validation + auto-arrange are worth committing 6 months to. All surface namespaced under `spike/*` — one `rm -rf` deletes it. **(T1 5e06310)** Infra: react ^19 + react-dom + @xyflow/react ^12 + @vitejs/plugin-react ^4 (pinned to v4 — v6 needs vite^8, repo is on vite^7.0.7). New `resources/js/spike/main.jsx` entry added to `vite.config.js` — isolated bundle, only fetched on `/spike/schematic-editor`. New `SpikeSchematicController` (5 lines) enforces `abort_unless(config('services.spike_schematic_enabled'), 404)` + `abort_unless(auth()->user()?->isAdmin(), 403)` inside the controller — route lives in the `auth` group (NOT `admin` middleware) so the flag-off 404 fires cleanly for non-admins. Admin nav dropdown: "🧪 Schematic Spike" item behind the same flag. Kill-switch: `SPIKE_SCHEMATIC_ENABLED=false` (default) → 404 + link hidden. **(T2 3d95969)** Custom device catalog + palette: `devices.js` with 5 hardcoded devices (Sony QM85 4K Display, Cisco Room Bar Pro, Shure MXW, QSC Q-Sys Core 110f DSP, Netgear GS308EPP switch), each with typed ports (connector_type + signal_type + direction + side). `DeviceNode.jsx` custom React Flow node renders manufacturer + model + role badge + one `<Handle>` per port docked on declared side; bidi ports render source+target twin handles; port dots colour-ringed by signal type. `DevicePalette.jsx` 240px left sidebar with HTML5 drag-and-drop (custom MIME + text/plain fallback). `signalColours.js` + `compatibilityAliases.js` hardcoded from `config/cables.php` — spike simplicity, no server round-trip. **(T3 3c5f311)** Validation + auto-arrange + toolbar + localStorage. `validation.js` — `isValidConnection()` checks direction (source emits, target receives) then signal-type match, falling back to connector alias (HDMI↔DP, USB-C↔TB, RJ45↔SFP+, USB-C↔HDMI). Invalid → discard edge + flash target port red (600ms CSS animation via data-port-id lookup) + toast at top-right (auto-dismiss 3s). Valid via alias → info toast showing the adapter note. `autoArrange.js` 3-column layout — sources 0px, processors 350px, destinations 700px; within each column sort by dominant output signal (video → audio → control → usb → network), 200px vertical step. `Toolbar.jsx` 🎯 Auto-arrange · ↶ Undo · ↷ Redo · 💾 Save · 🗑 Clear · 📋 Copy JSON. Undo/redo via `[past, present, future]` refs, cap 50, snapshots at commit points only (drop, connect, remove, arrange, clear). localStorage: debounced 500ms auto-save + explicit save + restore-on-mount with version key + id-reservation to avoid collisions. Edges styled by source signal type (video blue, audio red, etc). **(T4 docs)** Spike README at `.planning/spikes/schematic-editor-260713/README.md` (~150 lines: why, scope, success criteria, kill-switch, walkthrough, decision deadline). Inline collapsible walkthrough (`<details>`) at top of `spike/canvas.blade.php` with 3 scenarios (Tilda boardroom build, invalid connection, persistence refresh). **NO migrations. NO env var required by default** (flag defaults false via env()). Deploy: `git pull` + `npm ci` + `npm run build` + `php artisan config:cache` + `php artisan view:clear`. To unlock: set `SPIKE_SCHEMATIC_ENABLED=true` in live `.env`. Bundle: ~124KB gzip for the spike bundle, isolated to `main.jsx`, only fetched on the spike route. **2-week review deadline: 2026-07-27** — commit to Milestone A (device library + editor v1 + DB persist + export) OR delete the branch. Quick task [260713-sk1](./quick/260713-sk1-schematic-editor-discovery-spike/). | 🧪 Spike (Local Only) | 3c5f311 |
| 260725-fx1 | 2026-07-25 | **Two review-page fixes: multi-select delete state drift + tidy-lines description-only + batching (d5325ac + 8d0fc33).** **(Fix 1 d5325ac)** User reported: ticked 3 rows → clicked row-level × on one → toolbar says "2 selected" but bulk **Delete** button greys out. Root cause: row-level `softDelete` flipped `data-deleted="1"`; CSS then hid the row (`display: none`); Alpine's `x-model="selectedRowIds"` re-sync against the hidden checkbox unpredictably dropped that row's ID from the array, leaving `canBulk('delete')` looking at a stale/inconsistent set. Explicit fix: new `equipmentSection._deselectRow(row)` helper — filters the row's ID from `selectedRowIds` (all coerced forms — string/number defensively) AND unchecks the DOM checkbox. `softDelete` / `restore` / `purge` all call it immediately after the state change. Two tooltip refresh: row `×` now `title="Delete this row (use the toolbar Delete for multi-select — tick checkboxes then use the yellow bar at the top)"`; bulk Delete now `title="Move all selected rows to deleted (can be restored)"`. **(Fix 2 8d0fc33)** User reported: "Tidy line items" **keeps failing** on large quotes AND unwantedly rewrites part numbers. Wanted: "should only shorten/tidy desc ie from sales version to simpler engineer ones". Two sub-fixes: (a) `EquipmentLineCleanupPrompt` **now description-only** — explicit system-message rule "you do NOT touch part numbers. You do NOT return a part_number field." Output schema drops `part_number` entirely. Examples rewritten to show only name transformations ("Samsung 55″ QM55C display"). Halves per-line output tokens (~40 vs prior ~80 with part_number+name). (b) `cleanupLines()` **batches** equipment into 40-row chunks via `array_chunk($equipment, 40, true)` (preserving numeric keys for id round-trip). Each batch is an independent AI call — per-batch failures logged with batch index + size, `continue` to next batch. Response returns `batches_total` + `batches_failed` counts. If ALL batches fail → return the 500 the JS knows how to render; if some succeed → 200 with the partial `rows[]` and JS displays "Tidied N lines (X/Y batches failed — retry to catch the rest)". Root cause of prior "keeps failing": Tilda quote has 313 rows × ~80 tokens per row output > 4096 `maxTokens` ceiling → truncated JSON → parse fail → alert. Half tokens + batching keeps every response comfortably inside 4096. JS confirm dialog copy rewritten: title "Tidy line descriptions?", body "Shorten AI descriptions across every line item?… Part numbers are NOT modified." Patch loop only touches the `name` textarea; the `part_number` textarea is deliberately skipped. `php -l` clean on both PHP files. No new tests (pure copy + control-flow refactor; existing --filter ProjectPackageReview tests unaffected). NO migrations, no npm build. **Deploy:** `git pull && php artisan optimize:clear && php artisan config:cache`. Sanity: (1) tick 3 rows → click row × on one → confirm the toolbar Delete stays enabled and count drops to 2; (2) click "Tidy" on a Tilda-sized quote → confirm no failure, only names change, batch-status shown in button. Quick task [260725-fx1](./quick/260725-fx1-multi-select-delete-and-tidy-desc-only/). | ⚠️ Needs Deploy | 8d0fc33 |
| 260725-qw2 | 2026-07-25 | **QuoteWerks room descriptions from CustomMemo01 + intro/closing notes (ec2ad37 + 1c7016b).** Follow-up to 260723-qw1. Diagnosed via tinker probe against `21CQ29531-05-OPS` — the per-room narrative paragraphs visible in the QW PDF (`"Oregano is now using the Crestron small room system with Jabra PanaCast 50 video bar, and offers full room control from a single Crestron panel..."`) do NOT live as separate LineType 4 comment rows (none exist for this quote). They live in **`DocumentItems.CustomMemo01`** on the LineType 32/256 section-header rows themselves. Prior fetch didn't SELECT that column, so the review page rendered blank overview textareas. Also spotted `DocumentHeaders.IntroductionNotes` (`"21st Century AV are pleased to provide..."`) + `ClosingNotes` (`"Please contact me if I can be of further assistance."`) that were being discarded. **(T1 ec2ad37)** Fetcher. `fetchItems()` SELECT appends `CustomMemo01`; `fetchHeader()` SELECT appends `IntroductionNotes, ClosingNotes`. `mapToParsedShape()` gains a `$roomDescriptions[$currentRoom]` map — when we hit a LineType 32/256 header with populated `CustomMemo01`, capture (whitespace-collapsed) keyed by the raw section-header text. Sparse — empty/whitespace-only memos leave the key absent. Last-write-wins on duplicate section headers (not observed in practice). New top-level parsed-shape keys: `room_descriptions` (map), `intro_notes` (?string), `closing_notes` (?string) — null when the header field is empty/whitespace-only. 10 new fetcher unit tests: single-room populates map, multi-room each lands under own key, empty memo omitted, whitespace collapsed, UTF-8 preserved through the multibyte curly-quote round-trip, header intro + closing present + absent + whitespace-only. Fixture helpers `sectionHeader($text)` / `subsectionHeader($text)` gained optional `$memo=''` param — additive, all 15 pre-existing tests unaffected. **(T2 1c7016b)** RAMS-side mapper. `QuoteWerksImportService::buildExtractedData()` zips `parsedShape['rooms']` with `parsedShape['room_descriptions']` into `extracted_data.room_overviews[]` as `[{room, overview}]` (rooms without populated memos get empty overview strings so the review UI still renders a card the PM can fill in manually). Surfaces `intro_notes` → `extracted_data.introduction_notes` and `closing_notes` → `extracted_data.closing_notes` (nullable). 4 new service unit tests: room_overviews zip with mixed populated/missing memos, empty rooms case, intro/closing present + absent. 39 total green across `--filter QuoteWerks|QuoteImport` (was 27 pre-qw2 — 12 new). **NO DB migration.** Additive change — existing `rooms[]` key unchanged, no downstream consumer breaks. Deploy: server as stcav → `git pull && php artisan optimize:clear && php artisan config:cache`. No npm build (no Blade changes). Sanity: re-import `21CQ29531-05-OPS` → review page → each room now has the narrative from CustomMemo01 in its overview field. Explicit non-goals: `CustomMemo02-10` (empty for this quote — separate task if a future quote populates them); UI polish for intro/closing notes (surfacing them on the review page is a separate UI task — this task just makes the data available); backfill of pre-qw2 packages (users can re-import or manually fill); room-name case normalisation (QW's ALL-CAPS section headers map to the review's raw text — case-mapping is a separate quick task). Quick task [260725-qw2](./quick/260725-qw2-room-descriptions-from-custommemo01/). | ⚠️ Needs Deploy | 1c7016b |
| 260723-qw1 | 2026-07-25 | **QuoteWerks direct-import fix — replace broken sqlsrv/wrong-columns/no-revision-handling with verified SCC pattern (fb54140 + 45f55c7 + 5ebdd98 + f57ca3d).** Five-task quick, four atomic commits + docs. The RAMS "QuoteWerks Lookup" tab has never worked in production — deep read revealed it was fundamentally wrong on every dimension: sqlsrv driver (pdo_sqlsrv not installed on VPS), fabricated column names (`SoldToCompanyName`, `Subject`, `TotalSalePrice`, `SalesPerson`, `GroupName`, `SortOrder` — none exist), no live-revision handling, and comments literally saying `[ASSUMED] from standard QuoteWerks API conventions`. Sibling SCC app has a verified pattern already deployed against live QW; this task ports it wholesale with only RAMS-house-style adaptations. **(T1 fb54140)** Foundations. `config/database.php` `quotewerks` block swapped from sqlsrv to `driver=odbc` wired to a system DSN registered on the VPS at `/etc/odbc.ini` as `[QUOTEWERKS_PROD]`. `AppServiceProvider::register()` gains a `DB::extend('odbc')` lazy resolver (only fires on first `DB::connection('quotewerks')` call — blank env vars never break local boot/migrate/tinker). Bare-PDO wrapped in `Illuminate\Database\Connection` with empty database + prefix (DSN abstracts them). No try/catch inside the resolver — `QuoteWerksDbFetcher::fetch()` catches PDOException at the call site and re-throws as `QuoteWerksUnreachableException` (new file, extends RuntimeException; deliberately NOT a Laravel DB exception so it carries no SQL-state metadata). `.env.example` swaps 8 legacy `QW_DB_*` vars for 3 `QUOTEWERKS_ODBC_*` vars (password lives in the VPS `.env` only, copied from SCC). **(T2 45f55c7)** New `App\Services\Imports\Quote\QuoteWerksDbFetcher` (namespace `App\Services\Imports\Quote`) ported verbatim from `service.21stcav.com` Phase 55 Wave 1. No constructor, no dependencies — method-resolvable. Two parameterised SELECTs: header uses `SELECT TOP 1 ID, DocNo, DocType, DocDate, RevisionMasterDocNo, Superceeded, SoldToCompany, ShipToCompany, ShipToContact, ShipToAddress1/2, ShipToCity, ShipToPostalCode, Subtotal, GrandTotal, PreparedBy, CustomMemo01 FROM DocumentHeaders WHERE (DocNo = ? OR RevisionMasterDocNo = ?) AND Superceeded = 0 ORDER BY DocDate DESC` (the QW-native misspelling `Superceeded` is load-bearing — do NOT correct it); items uses `SELECT ID, LineType, QtyBase, ManufacturerPartNumber, Notes, Description, CustomText01, CustomText02, UnitPrice, Manufacturer FROM DocumentItems WHERE DocID = ? AND LineType IN (1, 32, 256) ORDER BY ID` (no LineNumber/SortOrder column exists — the table is a heap with nonclustered PK on ID, PK ordering = operator's row entry order). Mapper `mapToParsedShape()` produces flat SCC-shape output (client / site / site_name / ref / prepared_by / scope_narrative / equipment[] / rooms[]); walks items in fetch order threading `$currentRoom` from LineType 32 (top-level section header) + LineType 256 (subsection header) onto subsequent LineType 1 products (section-header text wins over CustomText01 for the `area` key). Notes column preferred for `description`, Description as fallback when Notes empty (some operators put product text in Description). `normalizeUtf8Row()` fast-paths `mb_check_encoding('UTF-8')` then `mb_convert_encoding` from Windows-1252 for stray ® ™ curly-quote bytes that would otherwise break `json_encode`. `searchByClient()` colocated on the fetcher for the client-name search UI (TOP 20 + Superceeded=0 + LIKE). `PDOException` at any fetch boundary re-thrown as `QuoteWerksUnreachableException` with sanitized "Cannot reach QuoteWerks right now" message + PDOException chained as $previous for logging. 14 unit tests covering the pure mapToParsedShape transformation: header shape (SoldToCompany→client, ShipToCompany→site_name, empty scope→null, address parts joined), item walking (LineType 32 threads onto 3 products, nested 256 updates room mid-walk, Notes-preferred + Description-fallback, QtyBase 0/null defaults to 1, UTF-8 preserved, CustomText01 fallback area, unit_price null on non-numeric, contact fields always null for MVP). **(T3 5ebdd98)** Rewire. `QuoteWerksImportController::lookup(QuoteWerksLookupRequest, QuoteWerksDbFetcher)` — injects the fetcher via method resolution. Flow: (1) validate + uppercase-trim reference; (2) dup-check `ProjectPackage::whereRaw(JSON_UNQUOTE(JSON_EXTRACT(extracted_data, '$.quote_ref')) = ?)` — if found and `!force`, redirect back with warning flash + `qw_force_url` session key + `qw_last_reference`; the Blade renders a "Continue anyway →" link that posts a hidden `?force=1` form on the same route; (3) try/catch `QuoteWerksUnreachableException` → user-safe flash "Cannot reach QuoteWerks right now — please upload the PDF instead" (logs PDOException details server-side); (4) header null → "not found" error flash; (5) DocType != QUOTE via strcasecmp → wrong-doc-type error flash; (6) delegate to `QuoteWerksImportService::importFromParsedShape($user, $parsed)`; (7) redirect to review page with success flash. `search(Request, QuoteWerksDbFetcher)` uses the fetcher's colocated searchByClient — results flashed to session as `qw_search_results` for the Blade to render, matching RAMS's server-flash convention. Response type stays `RedirectResponse` throughout (NOT SCC's JSON SPA shape). `QuoteWerksImportService` rewritten to drop the `QuoteWerksRepository` dependency (class being deleted). `buildExtractedData(parsedShape)` new signature — consumes SCC-shape and produces RAMS canonical extracted_data payload preserving `meta.source='quotewerks_sql'` (the ProjectDataService tier-2 confidence trigger — DO NOT change). Adds `site_name` + `prepared_by` keys the old broken code was missing entirely. `importFromParsedShape(user, parsedShape)` is a thin orchestrator: buildExtractedData → importService->importFromData. `classifyDescription()` kept verbatim. `QuoteWerksLookupRequest` validates `reference` (was `qw_reference` — the Blade had already been POSTing `reference`, so the old validation rule was silently non-matching — that was a hidden bug the rewrite catches). Adds `client` + `force` rules. `QuoteWerksPing` rewritten: checks `pdo_odbc` extension + runs `SELECT TOP 1 DocNo FROM DocumentHeaders` via the odbc connection; diagnostic hints updated for WireGuard tunnel / DSN / CSF causes. **Deleted** — `QuoteWerksRepository.php` (all wrong columns; superseded by fetcher), `QuoteWerksSchema.php` (schema-explorer no longer needed), `QuoteWerksRepositoryTest.php`. `QuoteWerksImportServiceTest` fully rewritten for the new shapes — 13 tests covering meta.source pinning, confidence values, rooms passthrough, equipment key contract (quantity/qty/part_number/part_no/name/description/area/location/category/unit_price/total_price/data_source), total_price = qty × unit_price computation, project_name truncation to 80 chars, project_name fallback to ref when scope empty, prepared_by + site_name carry, missing equipment key defaults, importFromParsedShape return + captured `importFromData` payload keys. **(T4 f57ca3d)** UI polish. `resources/views/quote-import/create.blade.php` — warning flash rendering learns to render a "Continue anyway →" link when `qw_force_url` is set; the link submits a hidden CSRF-safe POST form to `route('quotewerks.lookup')` with `force=1` so the same throttle + form-request validation applies. Reference input pre-fills from `session('qw_last_reference')` on dup-warn (no retyping). Reference helper text calls out revision semantics ("Live revision only — matches on DocNo OR RevisionMasterDocNo where Superceeded = 0") + VPS-only tunnel constraint. Search form switched from GET (was silently broken — GET → POST route mismatch would 405) to POST; results now read from `session('qw_search_results')` matching the flash pattern. `npm run build` clean. **Design locks that survived every task:** (a) RAMS uses Blade + server-flash + redirect throughout, NOT SCC's Alpine SPA + JSON responses; (b) existing routes `/quote-import/quotewerks/lookup` and `/quote-import/quotewerks/search` unchanged; (c) fetcher port is verbatim except namespace + exception `use` — no SQL "improvements", no method reordering, no dependency injection; (d) `meta.source = 'quotewerks_sql'` string is load-bearing for the ProjectDataService tier-2 confidence trigger. Test tally: 27 tests / 103 assertions green across `--filter QuoteWerks|QuoteImport`. NO migrations. **Deploy:** on the VPS as `stcav`, `cd /home/stcav/rams.21stcav.com && git pull && php artisan optimize:clear && grep '^QUOTEWERKS_ODBC_' /home/stcav/service.21stcav.com/.env >> .env && php artisan config:cache && /usr/local/bin/php artisan quotewerks:ping` — ping should output OK + sample DocNo. Then tinker smoke fetch with `21CQ14213` (short-style) or `21CQ29531-05-OPS` (revision suffix). WireGuard tunnel + `/etc/odbc.ini` DSN `[QUOTEWERKS_PROD]` + CSF TCP_OUT 1433 + pdo_odbc extension + read-only SQL login are all pre-provisioned per the parent SCC deployment — no infra work needed. Explicit non-goals (deferred): JSON API / SPA modal (SCC pattern; delta kept small); contact fetch (would need a Contacts table JOIN); project-level total_price (fetcher doesn't currently surface Subtotal — add a follow-up if the review page needs it); activity_logs audit trail (SCC writes 9+ metadata keys per attempt; RAMS's Log::info is enough for now); bulk import; QW-based re-extract (PDF re-extract path unchanged). Quick task [260723-qw1](./quick/260723-qw1-quotewerks-direct-import-fix/). | ⚠️ Needs Deploy | f57ca3d |
| 260723-eq1 | 2026-07-24 | **Equipment line-item editing — soft-delete + qty-split + multi-select bulk toolbar on `/project-packages/{id}/review` (d1e0eac + 813dc5a + 7afe5b1).** Three atomic commits, zero migrations. Ships a graveyard pattern for equipment row deletion so PMs can undo mis-clicks after saving; adds a Split button that clones a `qty>1` row into individual `qty=1` rows preserving all other fields; adds a sticky bulk toolbar keyed off a leading checkbox column for multi-row Delete / Restore / Purge / Split. **(T1 d1e0eac)** `ProjectPackageReviewController::parseReviewPayload()` splits incoming `equipment[]` into two buckets based on the `equipment[N][deleted]` marker — active rows land in `reviewed_data.equipment[]` (clean, no `deleted` key), soft-deleted rows land in `reviewed_data.equipment_deleted[]` with `deleted:true` + `deleted_at` (iso8601) + `deleted_by` (user id) stamps. Round-trip preservation: re-submitted graveyard rows keep their original stamps instead of being re-stamped with `now()` — the DOM re-posts them via hidden inputs. `update()` and `approve()` both go through the split so approvals cannot resurrect deleted rows. `_original_totals` baseline calculates from the active-only payload so historical qty comparisons ignore graveyard rows. `show()` exposes `equipment_deleted[]` to the Blade view via `$reviewPayload['equipment_deleted']`. Downstream services (`OmManualGeneratorService`, `BuildRamsDocumentJob`, `MiniOmBuilderService`, `ProjectPackageRamsReviewService`) all still read `extracted_data.equipment[]` which stays clean — zero changes needed there. 9 unit + feature tests (`tests/Unit/Http/Controllers/ProjectPackageReviewController_EquipmentGraveyardTest` + `tests/Feature/ProjectPackages/EquipmentGraveyardTest`) cover the split, stamps, round-trip, absent-key defaults, `_original_totals` isolation, and `approve()` parity. **(T2 813dc5a)** `resources/views/project-packages/review.blade.php` wraps `#s-equipment` in an Alpine `equipmentSection()` component. Deleted rows render inline alongside active rows carrying `data-deleted="1"` — hidden by default via a CSS rule keyed off the `#s-equipment.show-deleted` body class that the "Show N deleted rows" header strip toggles. Each row's `col-actions` cell hosts BOTH active-mode (× Delete, ⎘ Split) and deleted-mode (↺ Restore, 🗑 Purge) buttons — CSS attribute selectors on `tr[data-deleted]` flip the pair instantly, no JS-driven markup swap. `softDelete()` / `restore()` mutate `data-deleted` + the hidden `[deleted]` input value; `softDelete()` also stamps `deleted_at` (iso8601) + `deleted_by` (user id) hidden inputs so the first POST carries them. `purge()` removes the row from the DOM so `parseReviewPayload` drops it entirely on next POST. `split()` clones a qty>1 row (qty-1) times, re-indexes both plain `name="equipment[N][…]"` attrs and Alpine `:name` bindings (zonePicker's `:name="isFreeText ? '' : 'equipment[N][zone]'"` literal is baked in by Blade at render time and must be rewritten in the clone), sets each clone's quantity to 1, preserves part/name/area/category/zone. JS `equipmentRowTemplate()` (used by `+ Add {category}`) kept in sync — adds `data-deleted="0"`, hidden `[deleted]` flag, `col-actions` button group. Legacy `removeRow()` at line 1797 left intact — still used by activities/hazards/PPE/room/cable tables outside `#s-equipment`. **(T3 7afe5b1)** Multi-select bulk toolbar. `col-select` checkbox column at the start of every equipment row (both Blade and JS template). Master checkbox in each category `<thead>` selects all visible rows in that tbody, respecting the `showDeleted` filter. Sticky `.bulk-toolbar` appears whenever `selectedRowIds.length > 0` — Delete / Restore / Purge / Split / Clear buttons that auto-disable when the action doesn't apply to the current selection (e.g. Split disables if no selected row has qty>1). Bulk actions confirm once for the whole selection (single count-aware confirm dialog), not per row. Row-level buttons refactored to share `_doDelete` / `_doRestore` / `_doPurge` / `_doSplit` internals with the toolbar — single code path. Colspan bumped 7→8 (Blade) and 6→8 (JS empty-state / room-header templates) to cover the new checkbox column. `_reindexRow()` extended to update per-row-select checkbox `value=` so split clones toggle their own new id, not the source row's. Blade view compiles cleanly at 159925 chars post-Task-3. Vite bundle unchanged size (no new deps — pure Alpine on the existing bundle). Test tally: 19 tests / 62 assertions across `tests/Feature/ProjectPackages` — all green. **NO migrations required.** Deploy: `git pull` + `php artisan optimize:clear` + `php artisan config:cache` + `npm run build`. Sanity: on a completed package review, soft-delete a row → save → reload → click "Show N deleted" → row shows with Restore + Purge; try Split on a qty=3 row → 3 rows persist as qty=1 each. Explicit non-goals: no row-level audit log (stamps on the graveyard entry are enough for now), no cross-package undo (deleted rows stay on their package), no server-side split endpoint (pure client-side DOM clone), no regen-impact preview (consumers just skip removed rows on next generation). Quick task [260723-eq1](./quick/260723-eq1-equipment-line-item-editing/). | ⚠️ Needs Deploy | 7afe5b1 |
| 260723-rr1 | 2026-07-23 | **Add `remove_room` op to RAMS + Worksheet document-edit adapters (48ec6ac + 6e103e6).** Diagnosed via the drawer we shipped in b32c341 (mount `<x-document-edit-drawer>` on RAMS review) — engineer typed "exclude Saffron, will be done later" in the AI chat; drawer showed the change, applied it, RAMS regenerated with Saffron still in every section. Root cause: `RamsEditAdapter` had no `remove_room` operation, so the AI mapped user intent to `add_exclusion` (the closest available verb), which only appends free text to the scope-exclusions clause and does nothing to `reviewed_data.room_overviews[]`. Same class of bug in `WorksheetEditAdapter`. Fix adds `applyRemoveRoom` on both — RAMS filters `reviewed_data.room_overviews[]` on the `room` key, Worksheet filters `generated_data.rooms[]` on the `name` key. Case-insensitive + whitespace-tolerant + idempotent (unknown-room retry is `ok:true` no-op) + reindexes array. Both adapters ship an `operationSchemas()` entry for `remove_room` whose `notes` explicitly tell the AI parser to prefer this over `add_exclusion` when user says "exclude / skip / don't include / remove X room". 19 unit tests / 40 assertions green (`RamsEditAdapter_RemoveRoomTest` + `WorksheetEditAdapter_RemoveRoomTest`, both under `tests/Unit/Services/DocumentEdits/`). Broader adapter + DocumentEdit test filter — 120/120 green, no regressions. Explicit non-goals for future quick tasks: O&M has no per-room structure (contacts + maintenance items only) so no filter is possible; Cable is cable-level (from/to location) not room-level; change-preview UI making the diff explicit ("Saffron removed, 4→3 rooms") is a UX improvement worth doing but touches the drawer JS separately; fixing the case-sensitive room-name lookup in `RoomOverviewSummaryService` is a related but separate code path. NO migrations. Deploy: `git pull` + `php artisan optimize:clear` + `php artisan config:cache`. NO npm build required. Post-deploy sanity test: on a completed RAMS with ≥2 rooms, open chat drawer → "exclude <room name>, will be handled later" → apply → regen should show one fewer room. Quick task [260723-rr1](./quick/260723-rr1-remove-room-op-in-doc-edit-adapters/). | ✓ Local | 6e103e6 |
| 260714-qa-lows | 2026-07-14 | **QA LOW backlog closure — 12 items from REVIEW-qa-260418.md swept in one commit (7942bbd).** 4 items were already fixed by intervening work (L-01 stray `count())` file gone; L-02 `QuoteParserService-working0904.php` backup gone; L-03 `WorksheetController::retryGeneration` now precedes `destroy` matching the section divider; L-04 `RamsController::create()` ownership `abort(403)` replaced with shared-workspace branch). 4 fixed inline: **L-05** removed 3 dead `abort_unless(auth()->check(), 403)` calls that could never fail inside the `auth` middleware group (`ProjectController::show()` :118, `ProjectController::transition()` :297, `RamsController::generateFromProject()` :230) — each replaced with a clarifying inline comment. **L-08** extracted 10-line inline `<style>` block from `resources/views/components/section-card.blade.php` into `resources/css/app.css` — CSS no longer re-emitted on every component render, no more specificity race. **L-11** swapped `str_ireplace` in `RamsComplianceUpgradeService::applyTypoFixes()` for a `preg_replace_callback` that inspects the matched word's casing and applies `strtoupper` / `ucfirst` / lowercase to the replacement — "Exisiting" now stays "Existing" instead of being flattened to lowercase mid-sentence. Smoke-tested with 4 inputs. Remaining 4 declined as trivial or informational (L-06 max reduction preference, L-07 unreachable PHP_INT_MIN edge, L-09 review body itself said "None required", L-10 audit not a change, L-12 review body itself said "Not critical"). **Closure log** appended to REVIEW-qa-260418.md documenting per-item disposition. **RAMS QA backlog now empty.** NO migrations. Deploy: `git pull` + `php artisan config:cache` + `npm run build` (for the CSS extract). | ✓ Local | 7942bbd |
