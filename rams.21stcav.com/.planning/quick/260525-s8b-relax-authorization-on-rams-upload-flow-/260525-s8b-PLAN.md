---
phase: 260525-s8b
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Http/Controllers/QuoteUploadController.php
  - app/Http/Controllers/CommissioningController.php
  - app/Http/Controllers/CommissioningItemController.php
  - app/Http/Controllers/CommissioningResyncController.php
  - app/Http/Controllers/CommissioningSignoffController.php
  - app/Http/Controllers/InstallProgrammeController.php
  - app/Http/Controllers/TaskAssignmentController.php
  - app/Http/Controllers/TaskPhotoController.php
  - app/Http/Controllers/TaskStatusController.php
  - app/Http/Controllers/TimeEntryController.php
  - app/Services/TimeEntryService.php
  - tests/Feature/Commissioning/OwnershipGuardTest.php
  - tests/Feature/FieldView/FieldPageTest.php
  - tests/Feature/InstallTasks/InstallTaskStatusUpdateTest.php
  - tests/Feature/InstallTasks/InstallTaskNotesTest.php
  - tests/Feature/InstallTasks/InstallTaskPhotoUploadTest.php
  - tests/Feature/TimeEntries/TimeEntryTest.php
  - tests/Feature/TimeEntries/TimeEntryEditTest.php
  - tests/Unit/Services/TimeEntryServiceTest.php
  - tests/Feature/Authorization/SharedWorkspaceFieldOpsAccessTest.php
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "A logged-in non-admin user (role=user) who is NOT the RAMS owner can reach the RAMS upload processing + checkReady endpoints without a 403"
    - "A logged-in non-admin, non-owner, non-assigned-engineer user can view the commissioning checklist, patch item status/notes, upload/delete/view item photos, resync, preview/finalise/download snagging — without a 403"
    - "A logged-in non-admin, non-owner, non-assigned-engineer user can generate/review/activate/schedule/field-view an install programme, destroy a task, assign tasks/rooms/programmes, patch task status/notes, upload/view/delete task photos — without a 403"
    - "A logged-in non-admin, non-owner, non-assigned-engineer user can clock in/out (start/stop) and retro-edit any time entry — without a 403"
    - "The install programme schedule + field views show ALL tasks to every authenticated user (no assigned-engineer listing filter)"
    - "The strict owner-only heartbeat liveness guard is PRESERVED (a peer cannot keep another user's open session alive — integrity, not access control)"
    - "Genuinely administrative endpoints elsewhere still 403 for non-admin users (no admin-only gate inside these field-ops controllers was touched)"
    - "No rendering/storage/route/migration/AI behaviour changed — this is an authorization-only change"
  artifacts:
    - path: "app/Http/Controllers/QuoteUploadController.php"
      provides: "authoriseRamsAccess relaxed to any authenticated user"
      contains: "auth()->check()"
    - path: "app/Http/Controllers/CommissioningController.php"
      provides: "show abort_if relaxed to any authenticated user"
      contains: "auth()->check()"
    - path: "app/Http/Controllers/InstallProgrammeController.php"
      provides: "all ownership gates relaxed + schedule/field task listing un-scoped"
      contains: "auth()->check()"
    - path: "app/Services/TimeEntryService.php"
      provides: "editEntry relaxed to any authenticated user; recordHeartbeat owner-only PRESERVED"
      contains: "auth"
    - path: "tests/Feature/Authorization/SharedWorkspaceFieldOpsAccessTest.php"
      provides: "Regression proof: non-admin non-owner non-engineer can use all field-ops + RAMS-upload endpoints; heartbeat owner-only preserved"
      min_lines: 90
  key_links:
    - from: "QuoteUploadController::processing/checkReady"
      to: "authoriseRamsAccess"
      via: "abort_unless(auth()->check(), 403)"
      pattern: "auth\\(\\)->check\\(\\)"
    - from: "CommissioningController/Item/Resync/Signoff authorise* helpers"
      to: "any authenticated user"
      via: "abort_unless(auth()->check(), 403)"
      pattern: "auth\\(\\)->check\\(\\)"
    - from: "InstallProgrammeController::schedule/field"
      to: "all tasks (no assigned-engineer filter)"
      via: "removed ->where('assigned_to', ...) listing filter"
      pattern: "\\$programme->tasks"
---

<objective>
Finish the shared-workspace authorization relaxation that quick task 260525-pyu started, covering the two surfaces the post-pyu audit found and the user approved: (1) the RAMS upload flow's private `authoriseRamsAccess` guard, and (2) the entire field-operations cluster (Commissioning, Install Programme, Task assignment/photo/status, Time Entry).

Production reality: all data is owned by sonny (user 1); the other two staff (zack, alison — role=user) hit HTTP 403 on these surfaces because the gates are owner-OR-admin-OR-assigned-engineer. This is a 3-person shared workspace ("D-15: Projects are shared across all authenticated users"). Any authenticated user should be able to fully use these too.

Purpose: Make the field-ops + RAMS-upload surfaces behave as the shared workspace the documents already do (post-260525-pyu). Mirror that plan's approach EXACTLY:
- Inline ownership gates / private authorise* helpers → `abort_unless(auth()->check(), 403)` with a "Shared workspace" comment.
- Owner/assigned-engineer-scoped listings → show ALL records to every authenticated user.
- PRESERVE every genuinely admin-only `isAdmin()` gate and every integrity guard (file-scope checks, the heartbeat liveness owner-only guard).

Output: Relaxed gates + un-scoped listings across 10 controllers + 1 service, with the field-ops 403-asserting test suites updated to the shared-access model, plus a new regression test proving a non-admin non-owner non-engineer user CAN now use these endpoints while integrity/admin guards still hold.

NON-NEGOTIABLE CONSTRAINTS:
- Authorization-only. NO changes to rendering, storage paths (H-07 DocumentArtifactStorage), routes, throttle middleware, validation rules, migrations, AI behaviour, or any business logic.
- Do NOT alter any rendering/byte-equivalence guard's behaviour (RamsRenderRegression, structural diff guards, etc.).
- Preserve every admin-only gate. (Audit confirms there are NO standalone `abort_unless(...isAdmin(), 403)` admin-only gates inside these 10 controllers — the only `isAdmin()` uses are part of the owner-OR-admin ownership expressions being relaxed. Do not invent new admin gates.)
- Out of scope (untouched): admin route group + `Admin\*` controllers, RAMS settings, WorkerMonitor, restore/force-delete, soft-deleted/trashed listings, draw.io spike, public token routes (survey/{token}, worksheet/{token}), DashboardController admin redirect, HazardTemplate personal/global split. None of these live in the 11 files this plan touches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

# Decision lineage already in the codebase (260525-pyu shipped this for documents):
#   ProjectController::show → abort_unless(auth()->check(), 403) "D-15: Projects are shared across all authenticated users."
#   All four resource policies → return true (any authenticated user).
#   Document controller helpers → abort_unless(auth()->check(), 403).
# This plan applies the SAME pattern to the field-ops cluster + RAMS upload flow.
# Reuse the comment style: "// Shared workspace: any authenticated user has full access."

<interfaces>
User model (app/Models/User.php):
```php
public function isAdmin(): bool { return $this->role === 'admin'; }
```

The current gates being relaxed (ALL verified against the codebase — line numbers may shift on edit, match on the expression not the line):

QuoteUploadController.php (private helper, called by processing() + checkReady()):
```php
private function authoriseRamsAccess(Request $request, RamsDocument $rams): void
{
    $user = $request->user();
    if ($user->id !== $rams->user_id && ! $user->isAdmin()) {
        abort(403);
    }
}
```

Commissioning cluster — each controller has a private authorise()/authoriseEdit() helper of this exact shape (owner-OR-admin-OR-assigned-engineer):
```php
$isOwnerOrAdmin = $project->user_id === $user->id || $user->isAdmin();   // or $programme->project->user_id / $item->programme->project->user_id
$isAssignedEngineer = $programme->tasks()->where('assigned_to', $user->id)->exists();
abort_if(! $isOwnerOrAdmin && ! $isAssignedEngineer, 403);
```
- CommissioningController::show — inline (not a helper): builds $isOwnerOrAdmin (~51), $assignedToProgramme (~60-64), abort_if (~66-69)
- CommissioningItemController::authoriseEdit (~389-402)
- CommissioningResyncController::authorise (~60-73)
- CommissioningSignoffController::authorise (~154-167)

InstallProgrammeController — mix of inline owner-OR-admin abort_if and listing filters:
```php
// generate ~55, review ~87, activate ~109, destroyTask ~335 — owner-OR-admin:
abort_if($project->user_id !== auth()->id() && ! auth()->user()->isAdmin(), 403);
// schedule ~145-157 + field ~256-281 — owner-OR-admin gate AND assigned-engineer task LISTING filter
```

TaskAssignmentController — owner-OR-admin (NO engineer branch), assign ~53, assignRoom ~105, assignAll ~153:
```php
abort_if($task->programme->project->user_id !== auth()->id() && ! auth()->user()->isAdmin(), 403);
```

TaskPhotoController::authoriseTaskMutation (~200-207) + TaskStatusController::authoriseTaskMutation (~138-145) — owner-OR-admin-OR-assigned-task:
```php
$isOwnerOrAdmin = $task->programme->project->user_id === $user->id || $user->isAdmin();
$isAssigned = $task->assigned_to === $user->id;
abort_if(! $isOwnerOrAdmin && ! $isAssigned, 403);
```

TimeEntryController::authoriseProjectAccess (~207-221) — owner-OR-admin-OR-assigned-engineer (start/stop):
```php
$isOwnerOrAdmin = $project->user_id === $user->id || $user->isAdmin();
if ($isOwnerOrAdmin) return;
$programme = $project->activeInstallProgramme;
$hasAssigned = $programme && InstallTask::where('install_programme_id', $programme->id)->where('assigned_to', $user->id)->exists();
abort_if(! $hasAssigned, 403);
```

TimeEntryService — TWO entry-level gates (heartbeat + update delegate ownership here, NOT to the controller):
```php
// recordHeartbeat (~172-185) — STRICT owner-only liveness guard (T-15-02-01). PRESERVE.
if ($entry->user_id !== $user->id) { throw new AuthorizationException(...); }
// editEntry (~205-211) — owner-OR-admin retro-edit gate (T-15-02-02). RELAX.
$isOwner = $entry->user_id === $editor->id;
if (! $isOwner && ! $editor->isAdmin()) { throw new AuthorizationException(...); }
```
</interfaces>
</context>

<scope_boundary>
## IN SCOPE — relax to "any authenticated user" (the shared workspace)

1. **RAMS upload flow** — `QuoteUploadController::authoriseRamsAccess` (the one surface 260525-pyu missed).
2. **Commissioning cluster** — Controller, Item, Resync, Signoff ownership helpers.
3. **Install Programme cluster** — generate/review/activate/schedule/field/destroyTask gates AND the assigned-engineer task-listing filters in schedule()/field().
4. **Task cluster** — TaskAssignment (assign/assignRoom/assignAll), TaskPhoto + TaskStatus authoriseTaskMutation.
5. **Time Entry** — controller `authoriseProjectAccess` (start/stop) + `TimeEntryService::editEntry` (retro-edit).

## PRESERVE — DO NOT relax (integrity / liveness / admin, NOT access-control)

1. **`TimeEntryService::recordHeartbeat` strict owner-only** (~174). This is a liveness anti-abuse guard (T-15-02-01): a peer keeping another user's open clock-session alive is a data-integrity problem, not a shared-workspace need. Analogous to the room-scope / cross-project-FK integrity checks 260525-pyu deliberately left untouched. KEEP IT. Flag in summary.
2. **`TimeEntryService::start/stop` one-open-entry guard + lockForUpdate** — concurrency/integrity, not ownership. Untouched.
3. **File-scope / existence integrity checks** — `str_starts_with($file, "snagging_programme_{$programme->id}_")` (CommissioningSignoffController ~88), all `abort_if($path === null, 404)`, `abort_if($item->evidence_photo_path === null, 404)`, `is_file(...)` 404s, the TaskPhoto content-sniff `abort_unless(... 422)`. These are integrity/validation, NOT ownership. Untouched.
4. **`assertMutable` signoff-immutability guard** (CommissioningItemController) — INST-05i state lock, not ownership. Untouched.
5. **Throttle middleware** on time-entry routes (throttle:30,1 / 10,1 / 20,1) — untouched.
6. **Everything outside these 11 files** — admin group, Admin\* controllers, document controllers (already done by pyu), public token routes, DashboardController, HazardTemplate. Untouched.

## NOTE on listing filters
The ONLY owner/engineer-scoped LISTINGS in this cluster are the assigned-engineer task filters in `InstallProgrammeController::schedule` (~155-157) and `::field` (~272-281). Un-scope those so everyone sees ALL tasks. The `TimeEntry::where('project_id')->where('user_id', $user->id)` lookup in field() (~299-302) is your-own-open-session lookup (correct per-user semantics for "am I clocked in") — NOT a listing to un-scope; leave it. No other field-ops index/listing query is owner-scoped (verified by grep).
</scope_boundary>

<tasks>

<task type="auto">
  <name>Task 1: Relax all field-ops + RAMS-upload ownership gates to any authenticated user (preserve heartbeat owner-only + all integrity guards)</name>
  <files>
    app/Http/Controllers/QuoteUploadController.php
    app/Http/Controllers/CommissioningController.php
    app/Http/Controllers/CommissioningItemController.php
    app/Http/Controllers/CommissioningResyncController.php
    app/Http/Controllers/CommissioningSignoffController.php
    app/Http/Controllers/InstallProgrammeController.php
    app/Http/Controllers/TaskAssignmentController.php
    app/Http/Controllers/TaskPhotoController.php
    app/Http/Controllers/TaskStatusController.php
    app/Http/Controllers/TimeEntryController.php
    app/Services/TimeEntryService.php
  </files>
  <action>
    Relax every owner / owner-OR-admin / owner-OR-admin-OR-assigned-engineer ACCESS gate listed below to a plain authenticated-user check. Keep a defensive guard in place (matches the D-15/D-19 style already in ProjectController and the document controllers from 260525-pyu). Use the comment `// Shared workspace: any authenticated user has full access.` on each relaxed gate.

    Match on the EXPRESSION, not the line number (lines shift as you edit). Do NOT touch validation, file-existence 404s, content-sniff 422s, the signoff-immutability guard, throttle middleware, or the heartbeat owner-only guard.

    **QuoteUploadController.php — `authoriseRamsAccess` (~267-274)**
    Replace the body:
    ```php
    private function authoriseRamsAccess(Request $request, RamsDocument $rams): void
    {
        // Shared workspace: any authenticated user has full access.
        abort_unless(auth()->check(), 403);
    }
    ```
    (Keep the method + its two call sites in processing()/checkReady() intact. The `$request`/`$rams` params stay for the signature.) Update the docblock above it from "...unless the current user owns this RAMS document or is admin." to "...unless the request is authenticated. Shared workspace — any authenticated user may view RAMS extraction status."

    **CommissioningController::show (~48-69)** — inline gate. Replace the three lines that build `$isOwnerOrAdmin` (~51), build `$assignedToProgramme` (~59-64), and the `abort_if(! $isOwnerOrAdmin && ! $assignedToProgramme, 403)` (~66-69) with a single:
    ```php
    // Shared workspace: any authenticated user has full access.
    abort_unless(auth()->check(), 403);
    $user = auth()->user();
    ```
    KEEP `$user = auth()->user();` because `$user` is NOT used later in show() — actually verify: it is only used by the removed gate. If `$user` is unused after removal, drop the `$user =` line entirely to keep Pint clean (no unused variable). The `$isOwnerOrAdmin` value is passed to the view at the bottom (`'isOwnerOrAdmin' => $isOwnerOrAdmin`). To avoid touching the view contract, KEEP a `$isOwnerOrAdmin` definition but set it to `true` for any authenticated user: `$isOwnerOrAdmin = true; // Shared workspace`. (The Blade uses it to decide owner-only chrome; in a shared workspace everyone is effectively an owner.) Net result for show(): `abort_unless(auth()->check(), 403);` then `$isOwnerOrAdmin = true;`, drop `$assignedToProgramme` and the `$programme->tasks()->where('assigned_to'...)` existence probe.

    **CommissioningItemController::authoriseEdit (~389-402)** — replace the whole body (the loadMissing + $isOwnerOrAdmin + $isAssignedEngineer + abort_if) with:
    ```php
    private function authoriseEdit(CommissioningItem $item): void
    {
        // Shared workspace: any authenticated user has full access.
        abort_unless(auth()->check(), 403);
    }
    ```
    (You may drop the now-unused `$item->loadMissing('programme.project')` since nothing else in the method needs it; if any caller relies on the relation being loaded elsewhere it is reloaded on demand — but these callers don't, so dropping is safe and Pint-clean.) Update the docblock to "Shared workspace — any authenticated user may mutate commissioning items."

    **CommissioningResyncController::authorise (~60-73)** and **CommissioningSignoffController::authorise (~154-167)** — replace each body with:
    ```php
    private function authorise(InstallProgramme $programme): void
    {
        // Shared workspace: any authenticated user has full access.
        abort_unless(auth()->check(), 403);
    }
    ```
    (Drop the now-unused `$programme->loadMissing('project')` + ownership/engineer probes.) Update each docblock accordingly.

    **InstallProgrammeController.php** — four inline owner-OR-admin abort_if gates + two listing-gate methods:
    - generate (~55-58), review (~87-90), activate (~109-112), destroyTask (~335-338): replace each `abort_if($x->user_id !== auth()->id() && ! auth()->user()->isAdmin(), 403);` (or `$programme->project->user_id` / `$task->programme->project->user_id` variant) with:
      ```php
      // Shared workspace: any authenticated user has full access.
      abort_unless(auth()->check(), 403);
      ```
      Keep the surrounding `$programme->load(...)` / `$task->load(...)` calls — they feed the view/redirect, not the gate.
    - schedule (~141-226): replace the `$isOwnerOrAdmin` (~145-146) + `$hasTasks` (~150) + `abort_if(! $isOwnerOrAdmin && ! $hasTasks, 403)` (~152) block with `abort_unless(auth()->check(), 403); // Shared workspace` and set `$isOwnerOrAdmin = true;` (it is passed to the view at ~224 and used to choose the task filter at ~155). With `$isOwnerOrAdmin = true`, the existing ternary `$tasks = $isOwnerOrAdmin ? $programme->tasks : (...filtered...)` naturally yields ALL tasks — that satisfies the "show all tasks" listing requirement with a one-token change. Leave the rest of schedule() (Gantt, week grouping, colours, logging) untouched.
    - field (~253-317): replace the `$isOwnerOrAdmin` (~256) + the `if (! $isOwnerOrAdmin) { $hasAssigned... abort_if(! $hasAssigned, 403); }` block (~266-270) with `abort_unless(auth()->check(), 403); // Shared workspace` and set `$isOwnerOrAdmin = true;`. The scope default (~273) `$request->query('scope', $isOwnerOrAdmin ? 'all' : 'mine')` then defaults everyone to 'all' (sees all tasks) — correct for shared workspace. The `?scope=mine` toggle still works for anyone who wants to filter to their own. Leave the `TimeEntry::where('project_id')->where('user_id', $user->id)` open-session lookup (~299-302) UNCHANGED — that is the current user's own clock-in state, not a listing.

    **TaskAssignmentController.php** — assign (~53-56), assignRoom (~105-108), assignAll (~153-156): replace each `abort_if($x->...->user_id !== auth()->id() && ! auth()->user()->isAdmin(), 403);` with:
    ```php
    // Shared workspace: any authenticated user has full access.
    abort_unless(auth()->check(), 403);
    ```
    Keep the `$task->load(...)` / `$programme->load('project')` calls.

    **TaskPhotoController::authoriseTaskMutation (~200-207)** and **TaskStatusController::authoriseTaskMutation (~138-145)** — replace each body with:
    ```php
    private function authoriseTaskMutation(InstallTask $task): void
    {
        // Shared workspace: any authenticated user has full access.
        abort_unless(auth()->check(), 403);
    }
    ```
    (The `InstallTask $task` param stays for the signature; the `$task->programme->project` chain is no longer dereferenced, which also avoids a lazy-load. Update docblocks.)

    **TimeEntryController::authoriseProjectAccess (~207-221)** — replace the whole body with:
    ```php
    private function authoriseProjectAccess(Project $project, User $user): void
    {
        // Shared workspace: any authenticated user has full access.
        abort_unless(auth()->check(), 403);
    }
    ```
    (Params stay for the two call sites in start()/stop(). Drop the unused `InstallTask` probe logic.)

    **TimeEntryService.php — TWO distinct gates, treat differently:**
    - `editEntry` (~205-211): RELAX. Replace:
      ```php
      $isOwner = $entry->user_id === $editor->id;
      if (! $isOwner && ! $editor->isAdmin()) {
          throw new AuthorizationException("User {$editor->id} cannot edit entry {$entry->id}.");
      }
      ```
      with:
      ```php
      // Shared workspace: any authenticated user may retro-edit any time entry.
      // The append-only TimeEntryAudit row (written below) preserves accountability
      // by recording edited_by_user_id, so relaxing the gate does not lose the audit trail.
      ```
      (i.e. delete the throw block entirely. `$editor` is still used for the audit row's `edited_by_user_id`, so KEEP the `$editor` param and its later use. Do NOT remove the `use Illuminate\Auth\Access\AuthorizationException;` import — `recordHeartbeat` still throws it.)
    - `recordHeartbeat` (~172-185): **PRESERVE EXACTLY.** Do NOT touch the `if ($entry->user_id !== $user->id) { throw new AuthorizationException(...); }` block. This is the liveness anti-abuse guard (T-15-02-01). Add a one-line comment above it if helpful: `// PRESERVED: strict owner-only liveness guard — a peer must not keep another user's session alive (integrity, not access control).`

    GLOBAL RULES:
    - Do NOT change any route, throttle middleware, validation rule, file-existence/integrity 404, content-sniff 422, the signoff-immutability `assertMutable`, or the TimeEntry one-open-entry/lockForUpdate logic.
    - Do NOT touch any file outside the 11 listed.
    - Keep all `use` imports that remain referenced; remove an import ONLY if it becomes entirely unused AND Pint flags it (none should — `InstallTask`, `User`, `AuthorizationException` all stay referenced elsewhere).
    - After edits, no relaxed method should still compare `user_id`/`assigned_to` for ACCESS (only the preserved heartbeat owner-check, the un-touched per-user open-session lookup, and the audit-row write remain).
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com" && php -l app/Http/Controllers/QuoteUploadController.php && php -l app/Http/Controllers/CommissioningController.php && php -l app/Http/Controllers/CommissioningItemController.php && php -l app/Http/Controllers/CommissioningResyncController.php && php -l app/Http/Controllers/CommissioningSignoffController.php && php -l app/Http/Controllers/InstallProgrammeController.php && php -l app/Http/Controllers/TaskAssignmentController.php && php -l app/Http/Controllers/TaskPhotoController.php && php -l app/Http/Controllers/TaskStatusController.php && php -l app/Http/Controllers/TimeEntryController.php && php -l app/Services/TimeEntryService.php && vendor\bin\pint --test app/Http/Controllers app/Services/TimeEntryService.php</automated>
  </verify>
  <done>
    All listed ACCESS gates relaxed to `abort_unless(auth()->check(), 403)` (or the editEntry throw deleted); InstallProgramme schedule/field `$isOwnerOrAdmin = true` un-scopes the task listing to ALL tasks; heartbeat owner-only guard + all integrity/validation/throttle guards PRESERVED verbatim; no file outside the 11 touched. No syntax errors, Pint clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update the field-ops 403/assigned-only test suites to the shared-access model</name>
  <files>
    tests/Feature/Commissioning/OwnershipGuardTest.php
    tests/Feature/FieldView/FieldPageTest.php
    tests/Feature/InstallTasks/InstallTaskStatusUpdateTest.php
    tests/Feature/InstallTasks/InstallTaskNotesTest.php
    tests/Feature/InstallTasks/InstallTaskPhotoUploadTest.php
    tests/Feature/TimeEntries/TimeEntryTest.php
    tests/Feature/TimeEntries/TimeEntryEditTest.php
    tests/Unit/Services/TimeEntryServiceTest.php
  </files>
  <action>
    Flip every test that asserts a non-owner / non-assigned / stranger gets 403 (or sees only assigned tasks) to assert the NEW shared-access behaviour. Rename each method to reflect the new contract. Mirror the assertion shape of the sibling owner-success test in the same file (read it first). KEEP every unauthenticated→login-redirect test, every state/validation 422 test, every integrity test, and the PRESERVED heartbeat owner-only test unchanged.

    **tests/Feature/Commissioning/OwnershipGuardTest.php**
    - `test_stranger_gets_403_on_checklist_view` (~22): rename to `test_any_authenticated_user_can_view_checklist`; change `->assertForbidden()` to `->assertOk()`.
    - `test_stranger_gets_403_on_status_patch` (~35): rename to `test_any_authenticated_user_can_patch_status`; the stranger PATCH should now succeed — mirror the owner/engineer status-patch success assertion (200 + JSON shape) from ItemStatusPatchTest.php (read it for the exact payload + assertion). If the success path needs a valid status body, supply one (e.g. status=na, which needs no photo/note).
    - `test_engineer_assigned_to_project_a_cannot_patch_project_b_item` (~51): this asserted cross-project isolation via the assigned-engineer model. Under shared access ANY authed user may patch ANY item, so cross-project isolation no longer applies. Rename to `test_any_authenticated_user_can_patch_any_project_item` and flip the `->assertForbidden()` to the success assertion (200). Keep the two-project fixture; it now proves a user with no relationship to project B can still patch project B's item.

    **tests/Feature/FieldView/FieldPageTest.php**
    - `test_unrelated_user_gets_403` (~60): rename to `test_any_authenticated_user_can_view_field_page`; change `->assertForbidden()` to `->assertOk()`.
    - `test_engineer_sees_only_assigned_tasks_by_default` (~81): this asserted the assigned-engineer LISTING filter. Now every authed user defaults to scope=all and sees ALL tasks. Rename to `test_any_authenticated_user_sees_all_tasks_by_default`; keep both task fixtures; change the assertions to `assertSee('My assigned task')` AND `assertSee('Someone else task')` (both visible now). Drop the `assertDontSee`.
    - KEEP `test_owner_can_view_field_page`, `test_unauthenticated_user_is_redirected_to_login`, `test_owner_sees_all_tasks_including_unassigned` unchanged.

    **tests/Feature/InstallTasks/InstallTaskStatusUpdateTest.php**
    - `test_unrelated_user_gets_403` (~121): rename to `test_any_authenticated_user_can_update_status`; replace `->assertForbidden()` with the owner/assigned status-update success assertion from the same file (200 + JSON). Provide a valid status body if needed.

    **tests/Feature/InstallTasks/InstallTaskNotesTest.php**
    - `test_unrelated_user_cannot_save_notes` (~60): rename to `test_any_authenticated_user_can_save_notes`; replace the 403 assertion with the owner notes-save success assertion (200 + `{ notes }`).

    **tests/Feature/InstallTasks/InstallTaskPhotoUploadTest.php**
    - `test_unrelated_user_cannot_upload` (~121): rename to `test_any_authenticated_user_can_upload_photo`; replace 403 with the owner upload success assertion (201 + `{ id, url, ... }`). Reuse the same `UploadedFile::fake()->image(...)` fixture the owner test uses.
    - `test_unrelated_user_cannot_view_photo` (~136): rename to `test_any_authenticated_user_can_view_photo`; replace 403 with the owner photo-view success assertion (200 + image stream). Mirror the owner show() test exactly.

    **tests/Feature/TimeEntries/TimeEntryTest.php**
    - `test_unrelated_user_cannot_clock_in` (~95): rename to `test_any_authenticated_user_can_clock_in`; replace the 403 with the owner clock-in success assertion (200 + `{ id, category, clocked_in_at }`). Mirror the owner start() test.

    **tests/Feature/TimeEntries/TimeEntryEditTest.php**
    - `test_stranger_cannot_edit_foreign_entry` (~93): rename to `test_any_authenticated_user_can_edit_any_entry`; the stranger PATCH should now succeed → replace `->assertForbidden()` + `assertSame(0, TimeEntryAudit::count())` with the owner retro-edit success assertion (200 + the edited field JSON) AND `assertSame(1, TimeEntryAudit::count())` (the audit row IS written, with `edited_by_user_id` = the stranger). Mirror `test_owner_can_edit_category_on_closed_entry` (~22).
    - KEEP `test_cannot_edit_open_entry`, `test_cannot_edit_invalid_field`, `test_cannot_edit_category_to_invalid_value`, and the admin-edit test unchanged (those are state/validation, not ownership).

    **tests/Unit/Services/TimeEntryServiceTest.php**
    - `test_edit_entry_by_stranger_throws` (~216): rename to `test_edit_entry_by_any_user_succeeds_and_writes_audit`; the editEntry call by a non-owner non-admin should now NOT throw — assert it returns the updated entry AND a TimeEntryAudit row exists with `edited_by_user_id` = that user. Mirror `test_edit_entry_by_owner_writes_audit_row` (~160).
    - KEEP all heartbeat tests, state-gate tests, and `test_edit_entry_by_owner_writes_audit_row` unchanged.

    **PRESERVE (do NOT touch):** `tests/Feature/TimeEntries/TimeEntryHeartbeatTest.php::test_heartbeat_returns_403_for_non_owner` — the heartbeat owner-only guard is preserved, so this test MUST stay green unchanged. Verify it still passes in Task 4.

    Do NOT modify RamsRenderRegression or any structural/byte-equivalence guard.
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com" && php artisan test --filter="OwnershipGuard|FieldPage|InstallTaskStatusUpdate|InstallTaskNotes|InstallTaskPhotoUpload|TimeEntryTest|TimeEntryEdit|TimeEntryServiceTest"</automated>
  </verify>
  <done>
    All flipped field-ops tests pass asserting shared access (stranger/non-assigned user now succeeds); the assigned-only listing test now asserts all-tasks-visible; the heartbeat owner-only test is untouched and still green; all state/validation/integrity tests unchanged and green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add a shared-workspace field-ops regression test + full-suite + Pint gate</name>
  <files>
    tests/Feature/Authorization/SharedWorkspaceFieldOpsAccessTest.php
  </files>
  <action>
    Create a focused regression test (RefreshDatabase) proving the field-ops + RAMS-upload shared workspace end-to-end, mirroring the structure of the 260525-pyu SharedWorkspaceAccessTest. Use `User::factory()->create(['role' => 'user'])` for the non-admin, non-owner, non-assigned actor; `['role' => 'admin']` only where an admin is genuinely needed. Read the existing field-ops feature tests (FieldPageTest, ItemStatusPatchTest, TimeEntryTest, OwnershipGuardTest) for the established factory/fixture helpers (Project, InstallProgramme, InstallTask, CommissioningItem, TimeEntry factories) and the exact route names (from routes/web.php: `install-programmes.field`, `commissioning.show`, `install-tasks.status`, `time-entries.start`, `time-entries.update`, etc.).

    Cover these cases (each: owner = user 1 creates the resource; a SECOND role=user user with NO assignment is the actor):

    1. `test_non_assigned_user_can_view_commissioning_checklist` — actor GETs `commissioning.show` for a project owned by user 1 with an active programme → 200 (was 403).
    2. `test_non_assigned_user_can_view_install_field_page_and_sees_all_tasks` — programme has a task assigned to a THIRD user; the actor GETs `install-programmes.field` → 200 AND sees that other user's task (proves listing un-scoped).
    3. `test_non_assigned_user_can_patch_task_status` — actor PATCHes `install-tasks.status` (status=complete) on user-1's task → 200, status persisted.
    4. `test_non_assigned_user_can_clock_in_and_retro_edit_time_entry` — actor POSTs `time-entries.start` → 200; then (on a separately-created CLOSED entry owned by user 1) PATCHes `time-entries.update` (field=category) → 200 and a TimeEntryAudit row records the actor as editor.
    5. `test_non_owner_can_reach_rams_upload_processing_status` — user 1 creates a RamsDocument (status=uploaded, project_id set); the actor GETs `rams.processing` (or hits `checkReady` JSON) → NOT 403 (200 view or 302 redirect-on-ready, NOT 403). This is the surface 260525-pyu missed.
    6. `test_heartbeat_still_owner_only` — user 1 has an OPEN time entry; the actor POSTs `time-entries.heartbeat` for that entry → 403 (PRESERVED integrity guard). This is the negative control proving we did NOT over-relax.
    7. `test_guest_is_redirected_to_login` — unauthenticated GET to `install-programmes.field` (or `commissioning.show`) → 302 redirect to login, proving auth middleware still guards everything.

    For the RAMS-upload case, respect H-07: if a downloadable file is needed use `Storage::fake('local')` and the upload controller's storage convention; but processing()/checkReady() only read status, so no file is strictly required — a RamsDocument row with status=STATUS_UPLOADED suffices for the 403→200 proof.

    Keep the test self-contained and deterministic. Do NOT modify RamsRenderRegression or any byte-equivalence/structural guard.

    After writing the test, run the FULL suite to confirm: no out-of-scope regression, RamsRenderRegression + structural guards still green, and no field-ops test still asserts the old owner/assigned-403 behaviour on a relaxed surface (the heartbeat owner-only test legitimately still asserts 403 — that is correct). Then run Pint across all touched files.
  </action>
  <verify>
    <automated>cd "C:\Users\sonny.tanda\Documents\1 - Claude Projects\Rams2\rams.21stcav.com" && php artisan test && vendor\bin\pint --test app/Http/Controllers app/Services/TimeEntryService.php tests/Feature/Authorization tests/Feature/Commissioning tests/Feature/FieldView tests/Feature/InstallTasks tests/Feature/TimeEntries tests/Unit/Services/TimeEntryServiceTest.php</automated>
  </verify>
  <done>
    New SharedWorkspaceFieldOpsAccessTest passes all 7 cases (including the heartbeat owner-only negative control at #6 and the RAMS-upload 403→200 repro at #5). Full suite green: RamsRenderRegression + all structural guards still pass (byte-equivalence preserved); out-of-scope tests unchanged and green; no relaxed surface still asserts owner/assigned-403. Pint clean across all touched files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest → app | Unauthenticated request crosses the `auth` middleware. MUST still be rejected (redirect to login). |
| authenticated user → any field-ops resource | After this change, ANY logged-in user may read/write ANY commissioning / install-programme / task / time-entry resource. INTENDED model for the 3-person shared workspace, consistent with 260525-pyu. |
| user → another user's open clock-session liveness | A peer MUST still NOT keep another user's open time-entry alive (heartbeat). Preserved owner-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-s8b-01 | Elevation of Privilege | Guest reaching field-ops / RAMS-upload endpoints | mitigate | `auth` middleware unchanged; every relaxed gate still calls `abort_unless(auth()->check(), 403)`; Task 3 case #7 proves guest → login redirect. |
| T-s8b-02 | Tampering / Repudiation | Peer keeping another user's clock-session alive (heartbeat abuse) | mitigate | `TimeEntryService::recordHeartbeat` strict owner-only guard PRESERVED verbatim; Task 3 case #6 + the untouched `TimeEntryHeartbeatTest::test_heartbeat_returns_403_for_non_owner` prove it still 403s. |
| T-s8b-03 | Repudiation | Untracked retro-edit of a peer's timesheet after relaxing editEntry | accept | editEntry relaxed, but the append-only `TimeEntryAudit` row (records `edited_by_user_id` + old/new value + timestamp) is written atomically on every edit — accountability preserved by the audit trail, which is the existing T-15-02-02 control. Shared workspace is the explicit business requirement. |
| T-s8b-04 | Information Disclosure | Cross-user/cross-project field-ops data visible to any staff | accept | Single-tenant internal app for ONE 3-person company; shared workspace is the explicit business requirement (D-15). No multi-tenant boundary exists. |
| T-s8b-05 | Tampering | Render/storage/route behaviour changed as a side effect | mitigate | Authorization-only edits; no rendering/storage-path/route/throttle/validation code touched; Task 3 confirms RamsRenderRegression + structural guards stay green. |
| T-s8b-06 | Tampering | File-scope / existence integrity guards weakened | accept | Snagging-PDF filename-scope check, all file-existence 404s, content-sniff 422, and the signoff-immutability `assertMutable` are NOT ownership gates and are explicitly left untouched. |
</threat_model>

<verification>
- `php artisan test` — full suite green, including RamsRenderRegression + all structural guards (byte-equivalence preserved).
- RAMS-upload repro (SharedWorkspaceFieldOpsAccessTest #5): a role=user non-owner reaches `rams.processing` / `checkReady` → NOT 403.
- Field-ops shared access (#1-#4): non-assigned role=user can view checklist, view field page (sees all tasks), patch task status, clock in, retro-edit time entries.
- Heartbeat owner-only (#6 + untouched TimeEntryHeartbeatTest): peer → 403 (preserved integrity guard).
- Guest (#7): unauthenticated → redirect to login.
- `vendor\bin\pint --test` clean on all touched files.
- Grep sanity: no relaxed field-ops gate still compares `user_id`/`assigned_to` for ACCESS (only the preserved heartbeat owner-check, the per-user open-session lookup, and the audit-row write remain).
</verification>

<success_criteria>
- Any authenticated user (incl. role=user, non-owner, non-assigned-engineer) can fully use the RAMS upload flow and every field-ops surface (Commissioning checklist/items/resync/signoff, Install Programme generate/review/activate/schedule/field/destroy-task, Task assign/photo/status, Time Entry start/stop/retro-edit).
- The install-programme schedule + field views show ALL tasks to every authenticated user.
- The heartbeat liveness owner-only guard is preserved (peer cannot keep another's session alive).
- No admin-only gate, integrity guard, throttle, route, validation rule, storage path, or rendering behaviour changed.
- Field-ops 403/assigned-only test suites updated to shared access; new regression test proves the workspace end-to-end including the preserved-guard negative control.
- Document rendering is byte-identical (RamsRenderRegression green). Full test suite passes; Pint clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260525-s8b-relax-authorization-on-rams-upload-flow-/260525-s8b-SUMMARY.md`.

In the summary, explicitly flag for the user:
- That `TimeEntryService::recordHeartbeat` was deliberately LEFT strict owner-only (a peer cannot keep another user's open clock-session alive — a liveness/integrity guard, not an access-control gate). If the user wants even this relaxed, it is a one-line follow-up — but doing so would let any user keep any session alive, which is almost certainly not desired.
- That `TimeEntryService::editEntry` WAS relaxed to any-authenticated-user, with accountability preserved via the append-only TimeEntryAudit row (records who edited what, when).
- That with 260525-pyu (documents) + this task (field-ops + RAMS upload), the entire authenticated app surface is now a shared workspace except: the admin route group / Admin\* controllers, restore/force-delete, soft-deleted/trashed listings, the heartbeat liveness guard, public token routes, the DashboardController admin redirect, and the HazardTemplate personal/global split (all intentionally preserved).
- Deploy: production via `git push live` then `git pull` + `php artisan optimize:clear` (orchestrator handles deploy).
</output>
