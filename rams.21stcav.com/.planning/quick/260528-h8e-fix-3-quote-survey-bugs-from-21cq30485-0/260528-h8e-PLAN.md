---
quick_id: 260528-h8e
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Services/QuoteParserService.php
  - app/Jobs/ExtractQuoteJob.php
  - app/Core/Modules/Survey/SurveyService.php
  - tests/Unit/Rams/QuoteParserServiceTest.php
  - tests/Unit/Jobs/ExtractQuoteJobClassifyItemTypeTest.php
  - tests/Unit/SurveyServiceScopeSourceTest.php
autonomous: true
must_haves:
  truths:
    - "Quote parser assigns each line-item's area to its real room (e.g. 'Larger Mtg Room'), not the document-structure header above it ('Hardware', 'Services', etc.)."
    - "Real-part-numbered cables and consumables (CS17461 patch lead, AV16131 Cat5e, PL12980 4-gang mains extension) classify as 'consumable', not 'hardware'."
    - "A site survey created from a project gets its general_notes seeded from the canonical scope source (reviewed_data scope_of_works_bullets / extracted_data overview / room overviews) — never empty when any of those exist."
    - "RamsRenderRegression byte-equivalence stays green (D-12 lock preserved)."
  artifacts:
    - path: "app/Services/QuoteParserService.php"
      provides: "Area-picker skips non-room section titles via isNonRoomSectionTitle()"
    - path: "app/Jobs/ExtractQuoteJob.php"
      provides: "classifyItemType() runs consumable desc-keyword check on any part#, with extension-lead keywords added"
    - path: "app/Core/Modules/Survey/SurveyService.php"
      provides: "resolveProjectScopeForSurvey() helper with priority-ordered scope source"
    - path: "tests/Unit/Rams/QuoteParserServiceTest.php"
      provides: "BMRS-shaped fixture: two real rooms each followed by a 'Hardware' sub-header"
    - path: "tests/Unit/Jobs/ExtractQuoteJobClassifyItemTypeTest.php"
      provides: "Classifier regression tests for CS17461 / AV16131 / PL12980 cases"
    - path: "tests/Unit/SurveyServiceScopeSourceTest.php"
      provides: "Priority-order tests for resolveProjectScopeForSurvey()"
  key_links:
    - from: "QuoteParserService::parse() area-picker loop"
      to: "QuoteParserService::isNonRoomSectionTitle()"
      via: "$this->isNonRoomSectionTitle($section['title'])"
      pattern: "isNonRoomSectionTitle"
    - from: "ExtractQuoteJob::classifyItemType()"
      to: "consumable desc-keyword fallback (now unguarded)"
      via: "loop runs for any part# (no `if ($upper === '')` wrapper)"
      pattern: "consumableDescKeywords"
    - from: "SurveyService::createFromProject()"
      to: "resolveProjectScopeForSurvey()"
      via: "helper picks first non-empty of reviewed bullets / extracted bullets / extracted overview / room_overviews concat / works_description"
      pattern: "resolveProjectScopeForSurvey"
---

<objective>
Fix three production bugs surfaced by quote 21CQ30485-03-OPS (BMRS):
- **Bug A:** Parser assigns "Hardware"/"Services" section-header titles as the equipment row's `area`, instead of the real room above it.
- **Bug B:** Item classifier sends real-part-numbered cables/patch leads/mains extensions to `hardware` because the consumable-description fallback is gated on `if ($upper === '')`.
- **Bug C:** `SurveyService::createFromProject()` seeds `general_notes` from `Project.works_description` / `ProjectPackage.works_description` — both intentionally retired by Phase 22.1 D-LOCK.

Purpose: restore correct room-grouped equipment + correct hardware/consumable split + non-empty survey scope without touching the v1.3 RAMS rendering surface or shared-workspace authorization.

Output: 3 production-code edits + 3 PHPUnit test files, all green; RamsRenderRegression unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@app/Services/QuoteParserService.php
@app/Jobs/ExtractQuoteJob.php
@app/Core/Modules/Survey/SurveyService.php
@app/Services/ProjectContextResolver.php
@app/Jobs/ReimportQuoteJob.php

<interfaces>
<!-- Verified during planning. Use these signatures directly. -->

From app/Services/QuoteParserService.php:
- private function isNonRoomSectionTitle(string $title): bool   // line 2455 — already filters hardware/cables/consumables/services/professionalservices/summary/notes/terms/termsandconditions/generalterms
- Area-picker loop at lines 1858-1871 (inside parse-by-tags path)
- Room-collection block at lines 1887-1894 (`$seenRooms` / `$rooms` accumulator) — sits IMMEDIATELY downstream of the area picker, so the same filter applies symmetrically.
- room_overviews filter at line 1916 already calls isNonRoomSectionTitle — proves the helper is the right reuse.

From app/Jobs/ExtractQuoteJob.php:
- private function classifyItemType(string $partNo, string $description): string   // line 420
- `$upper = strtoupper(trim($partNo))` then `$desc = strtolower($description)` at lines 422-423
- consumable desc-keyword loop guarded by `if ($upper === '')` at line 470  ← REMOVE THIS GUARD
- service desc-keyword loop at lines 501-512 already runs unconditionally — mirror that pattern
- default fallthrough `return 'hardware'` at line 514 — KEEP (per user decision)

From app/Core/Modules/Survey/SurveyService.php:
- public function createFromProject(User $user, Project $project, bool $supersede = false): SiteSurvey   // body uses $ctx + $package starting line 148
- `$package` resolved at lines 163-165, then works_description block at lines 166-173, then `$generalNotes = $worksDescription !== '' ? $worksDescription : null;`
- Log emission at lines 189-196 — keep but update `source` label to whichever branch fired.

From app/Services/ProjectContextResolver.php:
- public function resolve(Project $project): array   // returns ['project','equipment','activities','rooms']
- `rooms` is normalised from extracted_data['room_overviews'] (each item has room/overview/summary/works_summary/solution_type_id)
- **ProjectContextResolver does NOT expose scope_of_works_bullets** — neither reviewed nor extracted. Helper must read $package directly (extracted_data['scope_of_works_bullets']).

From app/Http/Controllers/ProjectPackageReviewController.php:
- approve() merges `extracted_data` + computed `scope_of_works_bullets` and persists with `$package->update(['extracted_data' => $merged])`. So `$package->extracted_data['scope_of_works_bullets']` is the canonical post-approval source. `reviewed_data` is NOT a separate column — it's terminology from the Phase 22.1 contract for the same merged extracted_data structure.

From app/Jobs/ReimportQuoteJob.php (FOR DEPLOY RUNBOOK — NOT for the plan):
- Constructor: `__construct(ProjectPackage $pending, User $user, ?ProjectPackage $previousRevision = null, ?string $provider = null)`
- Takes a **ProjectPackage** (the pending re-extract row), NOT a project_id. Orchestrator post-deploy tinker must:
  1. Find the existing approved package for 21CQ30485-03-OPS.
  2. Call `QuoteImportService::reimportPending($project, $user)` to create the new pending package (atomic STATUS_EXTRACTING insert).
  3. Dispatch `ReimportQuoteJob::dispatch($pending, $user, $previousRevision)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bug A — parser ignores non-room section titles when assigning equipment area</name>
  <files>app/Services/QuoteParserService.php, tests/Unit/Rams/QuoteParserServiceTest.php</files>
  <behavior>
    Add two RED tests to QuoteParserServiceTest before editing the parser:

    Test 1 — `test_area_skips_non_room_section_header_between_real_rooms`:
      Build a synthetic OVERVIEWTITLE-tagged input mirroring the BMRS quote shape:
        OVERVIEWTITLE: Larger Mtg Room
        OVERVIEWTITLE: Hardware
        PART FW-75BZ30L | 1 | Sharp 75" display
        OVERVIEWTITLE: Smaller Meeting Room
        OVERVIEWTITLE: Hardware
        PART FW-55BZ30L | 1 | Sharp 55" display
        OVERVIEWTITLE: Professional Services
        OVERVIEWTITLE: Services
        PART DELIVERY | 1 | Delivery to site
      Assert that the parsed equipment items receive `area = "Larger Mtg Room"`, `area = "Smaller Meeting Room"`, `area = "Professional Services"` (or `""` — never `"Hardware"` / `"Services"`).
      Use the existing OVERVIEWTITLE/PART tag format that QuoteParserService already accepts in the tag-based parse path (see lines around 1858 for the input format). If the exact tag protocol is unclear, mirror the format in an existing tag-based test in the file (search for `OVERVIEWTITLE` in QuoteParserServiceTest.php first).

    Test 2 — `test_non_room_section_titles_do_not_appear_in_rooms_list`:
      Same input. Assert `result['rooms']` contains "Larger Mtg Room" and "Smaller Meeting Room" but does NOT contain "Hardware" or "Services" — the `$seenRooms`/`$rooms` accumulator at lines 1887-1894 must be filtered symmetrically.

    Both tests MUST fail before code edits.
  </behavior>
  <action>
    Verified Bug A locations:
    - The area-picker loop at lines 1858-1871 walks `array_reverse($sections)` and stops at the first section whose `is_overview` is false AND title is non-empty AND starts before the tuple. It does NOT check `isNonRoomSectionTitle()`, so "Hardware" (a section header repeated under each real room) wins over the real room name above it.
    - The same loop's output feeds the `$seenRooms`/`$rooms` collection at lines 1887-1894, so the non-room title also pollutes the rooms list.
    - The helper `isNonRoomSectionTitle()` already exists at lines 2455-2477 and is already called at line 1916 to filter the `room_overviews` emission — proven-correct shape and case-folding (whitespace-stripped, lowercased, checks against a static keyword set incl. `hardware`, `services`, `professionalservices`, `summary`, etc.).

    Edits to app/Services/QuoteParserService.php:
    1. Inside the area-picker loop (the `foreach (array_reverse($sections) as $section)` block at lines 1860-1871), add a third skip-condition AFTER the `trim((string) ($section['title'] ?? '')) === ''` check and BEFORE the `$tupleOffset >= $section['start']` check:
       - Skip when `$this->isNonRoomSectionTitle((string) $section['title'])` returns true. Keep the existing `is_overview` and empty-title skips. Add a one-line inline comment: "// Skip QuoteWerks document-structure headers (Hardware / Services / Summary) — they're not rooms (see isNonRoomSectionTitle for the canonical list)."
    2. At the room-collection block (lines 1887-1894), add a symmetric guard so non-room titles never enter `$rooms`/`$seenRooms`. Wrap the existing `if ($area !== '')` body with an additional `&& ! $this->isNonRoomSectionTitle($area)` clause (or add a leading `if ($this->isNonRoomSectionTitle($area)) { continue; }` — pick whichever reads cleaner without flipping control flow for the equipment[] emission above).
       - IMPORTANT: the equipment[] push at lines 1879-1885 must still run with whatever `$area` was assigned (including `''` if all sections were filtered) — the rooms-list filter is purely a SECONDARY effect.

    Run RED tests, confirm failures. Apply edits. Run tests, confirm GREEN.
  </action>
  <verify>
    <automated>php artisan test --filter='QuoteParserServiceTest::test_area_skips_non_room_section_header_between_real_rooms|QuoteParserServiceTest::test_non_room_section_titles_do_not_appear_in_rooms_list'</automated>
  </verify>
  <done>
    Both new tests pass. Full QuoteParserServiceTest suite (`php artisan test --filter=QuoteParserServiceTest`) stays green — zero pre-existing regressions. `grep -n isNonRoomSectionTitle app/Services/QuoteParserService.php` returns at least 3 hits (the original definition + the room_overviews call at ~1916 + the new area-picker call + the new rooms-list guard).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bug B — classifier runs consumable desc-keyword check for any part#, plus extension-lead keywords</name>
  <files>app/Jobs/ExtractQuoteJob.php, tests/Unit/Jobs/ExtractQuoteJobClassifyItemTypeTest.php</files>
  <behavior>
    Create new test file tests/Unit/Jobs/ExtractQuoteJobClassifyItemTypeTest.php. Because `classifyItemType()` is `private`, use `ReflectionMethod` to invoke it (precedent: `ReviewedDataStructuralDiffTest` in STATE.md uses reflection to invoke a private controller method). Construct an `ExtractQuoteJob` with stub constructor args (read the constructor signature first; pass minimal fakes — likely a ProjectPackage + bool flag; if construction is too heavy, instantiate via reflection: `(new \ReflectionClass(ExtractQuoteJob::class))->newInstanceWithoutConstructor()`).

    RED tests (all must fail before the guard is removed):
      - `test_real_part_numbered_cat5e_patch_lead_classifies_as_consumable`:
        invoke(`'CS17461'`, `'7m Black Cat5e Ethernet Patch Lead'`) === `'consumable'`.
      - `test_real_part_numbered_cat5e_10m_classifies_as_consumable`:
        invoke(`'AV16131'`, `'Cat5e RJ45 Ethernet Patch Lead, 10m'`) === `'consumable'`.
      - `test_real_part_numbered_4_gang_mains_extension_classifies_as_consumable`:
        invoke(`'PL12980'`, `'4 Gang Mains Extension Lead'`) === `'consumable'`.
      - `test_real_part_numbered_power_extension_classifies_as_consumable`:
        invoke(`'PL15290'`, `'4 Gang Mains Extension Lead (1m)'`) === `'consumable'`.

    GREEN regression tests (must STILL pass after the change — guard rails for the user's "keep default hardware" decision):
      - `test_unknown_part_with_no_keywords_still_defaults_to_hardware`:
        invoke(`'XYZ999'`, `'Generic Mounting Plate Assembly'`) === `'hardware'`.
      - `test_blank_part_with_cable_description_still_classifies_as_consumable`:
        invoke(`''`, `'HDMI cable 3m'`) === `'consumable'`  // regression on the original blank-desc path.
      - `test_service_keyword_in_description_still_wins_over_unknown_hardware`:
        invoke(`'ENG'`, `'Site survey'`) === `'professional_service'`.
      - `test_cs_prefix_customer_supplied_still_short_circuits_before_consumable_branch`:
        invoke(`'CS-FOO'`, `'Cat5e patch lead'`) === `'customer_supplied'`  // proves the ordering guard (CS check at line 427 still wins).
  </behavior>
  <action>
    Verified Bug B location: line 470 in app/Jobs/ExtractQuoteJob.php — the `if ($upper === '')` wrapper around the `$consumableDescKeywords` loop (lines 470-481) blocks real-part-numbered cables. Mirror the structure of the unconditional `$serviceDescKeywords` loop at lines 501-512.

    Additional keyword research: the existing `$consumableDescKeywords` list (line 471-474) covers `cable`, `cabling`, `trunking`, `conduit`, `patch`, `hdmi`, `bracket`, `fixings` (duplicated), `fastener`, `wall mount`, `sundry`, `materials`. It does NOT cover the BMRS mains-extension cases. Per the prior-investigation note, add: `'extension lead'`, `'mains extension'`, `'power lead'`. Also worth adding (low-risk, well-bounded):
      - `'rj45'` — paired with Cat5e/Cat6 in patch-lead descriptions; safe because RJ45 is exclusively a connector/cable term in this domain.

    DO NOT add bare `'lead'` — too broad (would catch "Project Lead", "Lead Engineer"). Use the multi-word forms only.
    Also dedupe the existing `'fixings', 'fixings'` duplicate at line 473 (currently appears twice — pure cleanup, no behaviour change).

    Edits to app/Jobs/ExtractQuoteJob.php inside `classifyItemType()`:
    1. Remove the `if ($upper === '')` line (470) and its closing brace (line 481). Promote the `$consumableDescKeywords` declaration + foreach to top-level inside the method, sibling to the service-description loop.
    2. Inside the `$consumableDescKeywords` array, add three new entries before the closing `]`: `'extension lead'`, `'mains extension'`, `'power lead'`, `'rj45'`. Dedupe the second `'fixings'` while you're in there. Keep alphabetical-ish ordering for diff readability.
    3. Add an inline comment above the loop: "// Description-keyword fallback runs for ANY part# — real-part-numbered cables (CS17461, AV16131) and extension leads (PL12980) belong in consumables. Unknown items with no keyword match still hit `return 'hardware'` at the bottom (intentional)."
    4. Confirm the existing ordering invariant: customer_supplied check (line 427) and service_contract check (lines 433-450) MUST stay above the consumable description loop so `'CS-FOO'` + `'Cat5e patch lead'` still returns `customer_supplied`. Do not rearrange.

    Run RED tests, confirm failures. Apply edits. Run all 8 tests, confirm GREEN.
  </action>
  <verify>
    <automated>php artisan test --filter=ExtractQuoteJobClassifyItemTypeTest</automated>
  </verify>
  <done>
    All 8 new tests pass. `grep -n "if (\$upper === '')" app/Jobs/ExtractQuoteJob.php` returns NO matches (the offending guard is gone). `grep -c "'extension lead'" app/Jobs/ExtractQuoteJob.php` returns 1. `grep -c "'fixings'" app/Jobs/ExtractQuoteJob.php` returns 1 (dedupe applied).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Bug C — SurveyService scope source switched to canonical post-Phase-22.1 fields</name>
  <files>app/Core/Modules/Survey/SurveyService.php, tests/Unit/SurveyServiceScopeSourceTest.php</files>
  <behavior>
    Create new test file tests/Unit/SurveyServiceScopeSourceTest.php (mirror SurveyServiceTest.php's setUp: RefreshDatabase, container-resolved SurveyService, a User factory, a Project::create).

    Six tests covering the priority ladder of the new `resolveProjectScopeForSurvey($project, $package)` helper. The helper is public so it's directly testable; assert the RETURNED STRING (no DB write yet — keeps tests fast and decoupled from createFromProject's transaction).

    1. `test_prefers_reviewed_scope_of_works_bullets_when_present`:
       Package extracted_data has `['scope_of_works_bullets' => ['Install 75" display.', 'Run Cat5e patch leads.']]` AND a non-empty `overview`. Result === bullets joined with `"\n"` (or `"\n\n"` — pick one, document in code).

    2. `test_falls_back_to_extracted_overview_when_bullets_missing`:
       extracted_data = `['overview' => 'Supply and install AV in the larger meeting room.']` (no `scope_of_works_bullets`). Result === that overview string verbatim.

    3. `test_falls_back_to_concatenated_room_overviews_when_overview_missing`:
       extracted_data = `['room_overviews' => [['room' => 'Larger Mtg Room', 'overview' => 'Sharp 75" with TCC2 ceiling mic.'], ['room' => 'Smaller', 'overview' => 'Sharp 55".']]]`. Result is a string containing both rooms' overviews (suggest format: each room as "Larger Mtg Room: Sharp 75" with TCC2 ceiling mic.\nSmaller: Sharp 55"." — exact format up to implementer, but BOTH room overviews must be present in the output).

    4. `test_falls_back_to_legacy_works_description_when_modern_sources_empty`:
       Package extracted_data is `[]`. `$project->works_description = 'Legacy scope text.'`. Result === 'Legacy scope text.' (last-resort fallback preserved per constraint).

    5. `test_returns_null_when_no_source_has_content`:
       Empty project, empty package extracted_data, null works_description. Result === `null` (createFromProject already converts empty → null for general_notes).

    6. `test_caps_result_at_3000_chars`:
       Build a 4000-char overview. Result has `mb_strlen <= 3000`.

    All 6 RED before helper exists; all 6 GREEN after.
  </behavior>
  <action>
    Verified Bug C location: lines 157-196 in SurveyService.php currently read ONLY `$project->works_description` then `$package?->works_description`. Phase 22.1 D-LOCK (STATE.md Plan 22.1-05 entry) explicitly stopped populating both columns. The canonical scope source post-22.1 lives in:
    1. `$package->extracted_data['scope_of_works_bullets']` (computed at approve-time by `RamsComplianceUpgradeService::computeScopeOfWorksBulletsForApprove` and persisted via `$package->update(['extracted_data' => $merged])` — verified at ProjectPackageReviewController lines 438-455).
    2. `$package->extracted_data['overview']` (preserved verbatim from QuoteWerks per Plan 22.1 D-02 comment in ExtractQuoteJob lines 119-124).
    3. `$package->extracted_data['room_overviews'][*]['overview']` (concatenated as final modern fallback).
    4. Legacy `$project->works_description` / `$package->works_description` (preserved as last-resort tail).

    `ProjectContextResolver::resolve()` does NOT expose scope_of_works_bullets or overview directly — it only normalises rooms/equipment/activities. So the helper must read $package->extracted_data directly. (Documented this finding in the planner-return below.)

    Edits to app/Core/Modules/Survey/SurveyService.php:
    1. Add a new public method `resolveProjectScopeForSurvey(?Project $project, ?ProjectPackage $package): ?string` (public so the test file can call it without reflection). Place near `groupEquipmentByRoom` for visual grouping. Logic:
       - Read `$extracted = (array) ($package?->extracted_data ?? []);`.
       - Priority 1: `$bullets = (array) ($extracted['scope_of_works_bullets'] ?? []);` — if non-empty, join with `"\n"` (each bullet on its own line, no "* " prefix — survey general_notes is plain text, not Markdown).
       - Priority 2: `$overview = trim((string) ($extracted['overview'] ?? ''));` — if non-empty, use verbatim.
       - Priority 3: concatenate non-empty room_overviews as `"$roomName: $roomOverview"` joined by `"\n\n"`.
       - Priority 4: legacy `trim((string) ($project?->works_description ?? ''))`.
       - Priority 5: legacy `trim((string) ($package?->works_description ?? ''))`.
       - First non-empty wins. If all empty, return `null`.
       - Cap final string at 3000 chars via `mb_substr($result, 0, 3000)`.
       - Add full docblock citing Phase 22.1 D-LOCK (D-01/D-02) and quick task 260528-h8e.
    2. In `createFromProject()` (lines 157-196), replace the entire works_description block (lines 157-173) with:
       - One call: `$generalNotes = $this->resolveProjectScopeForSurvey($project, $package);`
       - Keep the existing `$generalNotes !== null` Log::info block (lines 189-196), but update the `source` field to reflect which branch fired (helper should return a tuple or accept a `&$sourceTag` ref param — simplest: have the helper log internally and return only the string; OR change the log key to `'multi-source canonical'` and drop the per-branch attribution since we're now polling 5 sources).
       - Recommendation: keep it simple — drop the per-branch source tagging, change the log message to `'SurveyService: seeded general_notes via resolveProjectScopeForSurvey'` with just `length` and `project_id` / `survey_id`. The function name is self-documenting.
    3. Add `use App\Models\ProjectPackage;` at the top of the file if not already imported (check first — the file already uses `$project->latestPackage` so the model is likely imported transitively; verify with `grep -n 'use App\\Models' app/Core/Modules/Survey/SurveyService.php`).

    Constraint check: this does NOT touch any RAMS-rendering Blade. RamsRenderRegression byte-equivalence is unaffected (the test only renders RAMS docs, not survey general_notes).

    Run RED tests, confirm failures. Apply helper + createFromProject rewire. Run tests, confirm GREEN.
  </action>
  <verify>
    <automated>php artisan test --filter='SurveyServiceScopeSourceTest|SurveyServiceTest'</automated>
  </verify>
  <done>
    All 6 new tests pass. Existing SurveyServiceTest.php tests still green (zero regression). `grep -n resolveProjectScopeForSurvey app/Core/Modules/Survey/SurveyService.php` returns at least 2 hits (definition + call site). `grep -nc "works_description" app/Core/Modules/Survey/SurveyService.php | grep -v ':0$'` shows ≤ 2 hits (the two legacy-fallback reads inside the helper) — the old project/package-level reads in createFromProject are gone.
  </done>
</task>

<task type="auto">
  <name>Task 4: Full-regression sanity sweep + RamsRenderRegression byte-equivalence canary</name>
  <files>(no production-code edits — verification-only)</files>
  <action>
    Run the three new test files together with the v1.3-protected canaries to prove zero collateral damage:
      1. `php artisan test --filter='QuoteParserServiceTest|ExtractQuoteJobClassifyItemTypeTest|SurveyServiceScopeSourceTest|SurveyServiceTest'` — all green.
      2. `php artisan test --filter=RamsRenderRegression` — must report the same 3 tests / 9 assertions GREEN that STATE.md records across the entire Phase 22.1 history. ANY change in this test count is a red flag — investigate before merging.
      3. `php artisan test --filter='Quote|Survey|Extract'` — broad sweep across the affected surface; record pass/fail count for the SUMMARY.

    If any pre-existing test fails because of a fixture that itself relied on the old "Hardware = area" or "blank → consumable only" behaviour, that fixture documents WRONG production output and the test should be updated to assert the corrected behaviour (call this out explicitly in the SUMMARY — do NOT silently flip an assertion). Treat ANY change to RamsRenderRegression behaviour as a HARD STOP — revert + re-plan.

    Document the final counts + any fixture updates in the quick-task SUMMARY.
  </action>
  <verify>
    <automated>php artisan test --filter='QuoteParserServiceTest|ExtractQuoteJobClassifyItemTypeTest|SurveyServiceScopeSourceTest|SurveyServiceTest|RamsRenderRegression'</automated>
  </verify>
  <done>
    Combined run reports ALL tests green. RamsRenderRegression test count is unchanged from STATE.md baseline (3 tests / 9 assertions). Broader sweep `--filter='Quote|Survey|Extract'` recorded in SUMMARY with pass count + assertion count.
  </done>
</task>

</tasks>

<verification>
- Bug A regression guard: synthetic BMRS-shape fixture (two rooms with "Hardware" sub-headers + a Services section) — equipment items get the real room name as `area`, the section headers do not appear in `rooms[]`.
- Bug B regression guard: CS17461 / AV16131 / PL12980 / PL15290 → `'consumable'`; unknown XYZ999 still → `'hardware'`; CS-FOO still → `'customer_supplied'`; service desc still → `'professional_service'`; blank-part HDMI cable still → `'consumable'`.
- Bug C regression guard: priority ladder reviewed bullets → extracted overview → room_overviews concat → legacy fallbacks → null, capped at 3000 chars.
- D-12 / D-LOCK guard: RamsRenderRegression byte-equivalence preserved end-to-end.
- Authorization untouched: zero diff against the 260525-pyu / 260525-s8b shared-workspace surface.
</verification>

<success_criteria>
1. All four tasks' automated tests green.
2. RamsRenderRegression unchanged from STATE.md baseline (3 / 9).
3. Zero diff against `app/Http/Controllers/SiteSurveyController.php`, `app/Models/SiteSurvey.php`, `app/Policies/SiteSurveyPolicy.php`, or any auth gate touched by 260525-pyu / 260525-s8b.
4. Zero diff against RAMS-rendering surface (`resources/views/pdf/rams.blade.php`, `app/Services/RamsBuilderService.php` if present, `app/Services/Rams/*` other than tests).
5. CLAUDE.md "AI is only for formatting" honoured — no AI prompt changes, no AI-driven scope inference.
6. Post-merge orchestrator dispatches `ReimportQuoteJob` for project ref `21CQ30485-03-OPS` (separate runbook step — see <planner-notes-for-orchestrator> below).
</success_criteria>

<planner-notes-for-orchestrator>
**Post-deploy retroactive fix for 21CQ30485-03-OPS:**
`ReimportQuoteJob` takes a `ProjectPackage $pending` (the new pending re-extract row), NOT a project_id. The full dispatch sequence from tinker is:

```php
$project = \App\Models\Project::where('ref', '21CQ30485-03-OPS')->firstOrFail();
$user    = \App\Models\User::where('is_admin', true)->first(); // or whoever owns
$svc     = app(\App\Core\Modules\QuoteImport\QuoteImportService::class);
$pending = $svc->reimportPending($project, $user); // creates the new pending package atomically
\App\Jobs\ReimportQuoteJob::dispatch($pending, $user, $project->latestPackage);
```

After re-extract completes (poll the new package's status), the project's survey will need to be regenerated too (existing one will still have empty general_notes — supersede it via the survey UI's "supersede" flow OR programmatically via `SurveyService::createFromProject($user, $project, supersede: true)`).
</planner-notes-for-orchestrator>

<output>
Create `.planning/quick/260528-h8e-fix-3-quote-survey-bugs-from-21cq30485-0/260528-h8e-SUMMARY.md` when done — include: production diff stats, test counts (before/after for each filter), RamsRenderRegression confirmation, fixture-update callouts if any, and the exact tinker snippet used for the post-deploy re-import.
</output>
