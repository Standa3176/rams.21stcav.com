---
phase: quick
plan: 260415-en6
type: execute
wave: 1
depends_on: []
files_modified:
  - database/migrations/TIMESTAMP_add_survey_room_gap_columns.php
  - app/Models/SiteSurveyRoom.php
  - app/Core/Modules/Survey/SurveyService.php
  - app/Http/Controllers/PublicSurveyController.php
  - app/Mail/SurveySubmittedMail.php
  - resources/views/emails/survey-submitted.blade.php
  - resources/views/public-survey/show.blade.php
  - resources/views/site-survey/_room-form.blade.php
  - resources/views/site-survey/show.blade.php
autonomous: true
requirements: []

must_haves:
  truths:
    - "PM receives log-visible email when engineer submits public survey"
    - "Cable route from/to fields are saved and visible on all survey forms"
    - "Rack room toggle persists and shows RACK ROOM badge on internal show page"
    - "Projection throw and viewing distance fields save for all room types"
    - "Network SSID/VLAN/switch port fields show only when has_network is checked"
    - "Engineer sign-off checkbox and name field are captured per room"
  artifacts:
    - path: "app/Mail/SurveySubmittedMail.php"
      provides: "PM notification mailable"
    - path: "resources/views/emails/survey-submitted.blade.php"
      provides: "Email template"
    - path: "database/migrations/*_add_survey_room_gap_columns.php"
      provides: "All new nullable columns on site_survey_rooms"
  key_links:
    - from: "SurveyService::submitPublic()"
      to: "SurveySubmittedMail"
      via: "Mail::to()->send() after survey update"
    - from: "public-survey/show.blade.php"
      to: "has_network Alpine state"
      via: "x-show on network detail fields"
---

<objective>
Fix six gaps in the site survey system: PM email notification on submission, cable route
from/to structured fields, comms rack room toggle, projection/viewing distance fields,
network detail fields (SSID/VLAN/port), and engineer confirmation sign-off.

Purpose: All changes are additive (nullable columns, new form fields, new mailable).
Output: Single migration, updated model/service/controller, new mailable + template,
updated public form, updated internal form and show page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@app/Models/SiteSurveyRoom.php
@app/Core/Modules/Survey/SurveyService.php
@app/Http/Controllers/PublicSurveyController.php
@app/Mail/RamsDocumentMail.php
@resources/views/public-survey/show.blade.php
@resources/views/site-survey/_room-form.blade.php
@resources/views/site-survey/show.blade.php

<interfaces>
<!-- SiteSurveyRoom::$fillable — current list ends at 'items_to_retain' -->
<!-- SiteSurveyRoom::$casts — booleans: has_power, has_network, requires_additional_power, is_completed -->
<!-- SurveyService::roomAttributes() — returns array of all room columns with ?? null defaults -->
<!-- PublicSurveyController::roomAttributesFromData() — mirrors roomAttributes() for public form -->
<!-- PublicSurveyController::validatePublicSurvey() — rooms.* validation rules -->
<!-- SurveyService::submitPublic() — sets status='completed', submitted_at=now(), logs activity -->
<!-- Project::$owner BelongsTo User via user_id -->
<!-- Mail pattern: app/Mail/RamsDocumentMail.php — no Queueable, uses Envelope + Content -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration + Model + Service + Controller — all 6 gap columns</name>
  <files>
    database/migrations/TIMESTAMP_add_survey_room_gap_columns.php
    app/Models/SiteSurveyRoom.php
    app/Core/Modules/Survey/SurveyService.php
    app/Http/Controllers/PublicSurveyController.php
  </files>
  <action>
    1. Run: php artisan make:migration add_survey_room_gap_columns_to_site_survey_rooms_table
       The migration up() must add ALL of the following to site_survey_rooms in a single Schema::table() call:
       - cable_route_from   varchar(500) nullable after('cable_route_desc')
       - cable_route_to     varchar(500) nullable after('cable_route_from')
       - is_rack_room       boolean nullable default null after('cable_route_to')
       - projection_throw_m decimal(5,2) nullable after('is_rack_room')
       - viewing_distance_m decimal(5,2) nullable after('projection_throw_m')
       - network_ssid       varchar(255) nullable after('network_port_count')
       - network_vlan       varchar(100) nullable after('network_ssid')
       - network_switch_port varchar(100) nullable after('network_vlan')
       - engineer_confirmed  boolean nullable default null after('is_completed')
       - engineer_signature_name varchar(255) nullable after('engineer_confirmed')
       down() must drop all ten columns.
       Run: php artisan migrate

    2. In app/Models/SiteSurveyRoom.php::$fillable add at appropriate positions:
       After 'cable_route_desc': 'cable_route_from', 'cable_route_to', 'is_rack_room'
       After 'projection_throw_m' group: 'projection_throw_m', 'viewing_distance_m'
       After 'network_port_count': 'network_ssid', 'network_vlan', 'network_switch_port'
       After 'is_completed': 'engineer_confirmed', 'engineer_signature_name'
       In $casts add:
         'is_rack_room'           => 'boolean',
         'projection_throw_m'     => 'decimal:2',
         'viewing_distance_m'     => 'decimal:2',
         'engineer_confirmed'     => 'boolean',

    3. In SurveyService::roomAttributes() add to the returned array (maintain alignment with existing style):
       After 'cable_route_desc':
         'cable_route_from'      => $data['cable_route_from']       ?? null,
         'cable_route_to'        => $data['cable_route_to']         ?? null,
         'is_rack_room'          => isset($data['is_rack_room']) ? (bool) $data['is_rack_room'] : null,
       After existing dimensions group or near 'display_mounting':
         'projection_throw_m'    => $data['projection_throw_m']     ?? null,
         'viewing_distance_m'    => $data['viewing_distance_m']     ?? null,
       After 'network_port_count':
         'network_ssid'          => $data['network_ssid']           ?? null,
         'network_vlan'          => $data['network_vlan']           ?? null,
         'network_switch_port'   => $data['network_switch_port']    ?? null,
       After 'is_completed' / 'completed_at':
         'engineer_confirmed'        => isset($data['engineer_confirmed']) ? (bool) $data['engineer_confirmed'] : null,
         'engineer_signature_name'   => $data['engineer_signature_name']   ?? null,

    4. In PublicSurveyController::roomAttributesFromData() mirror Step 3 exactly (same keys, same null-coalescing logic).
       In PublicSurveyController::validatePublicSurvey() add to the rooms.* rules:
         'rooms.*.cable_route_from'         => ['nullable', 'string', 'max:500'],
         'rooms.*.cable_route_to'           => ['nullable', 'string', 'max:500'],
         'rooms.*.is_rack_room'             => ['nullable', 'boolean'],
         'rooms.*.projection_throw_m'       => ['nullable', 'numeric', 'min:0', 'max:999'],
         'rooms.*.viewing_distance_m'       => ['nullable', 'numeric', 'min:0', 'max:999'],
         'rooms.*.network_ssid'             => ['nullable', 'string', 'max:255'],
         'rooms.*.network_vlan'             => ['nullable', 'string', 'max:100'],
         'rooms.*.network_switch_port'      => ['nullable', 'string', 'max:100'],
         'rooms.*.engineer_confirmed'       => ['nullable', 'boolean'],
         'rooms.*.engineer_signature_name'  => ['nullable', 'string', 'max:255'],
  </action>
  <verify>
    php artisan migrate --pretend 2>&1 | grep -i "error" || echo "Migration OK"
    php artisan tinker --execute="echo App\Models\SiteSurveyRoom::first()?->getFillable() ? 'ok' : 'no rows';" 2>/dev/null || echo "Model OK"
  </verify>
  <done>
    Migration runs cleanly. SiteSurveyRoom has all 10 new columns in $fillable. SurveyService::roomAttributes() and PublicSurveyController::roomAttributesFromData() both include all 10 new keys. Validation rules accept all 10 new room fields.
  </done>
</task>

<task type="auto">
  <name>Task 2: PM email mailable + template + wire into submitPublic()</name>
  <files>
    app/Mail/SurveySubmittedMail.php
    resources/views/emails/survey-submitted.blade.php
    app/Core/Modules/Survey/SurveyService.php
  </files>
  <action>
    1. Create app/Mail/SurveySubmittedMail.php:
       - Namespace: App\Mail
       - Use Illuminate\Mail\Mailable, Illuminate\Mail\Mailables\{Content, Envelope}
       - Do NOT use Queueable (send inline — MAIL_MAILER=log in dev)
       - Constructor: public readonly SiteSurvey $survey, public readonly string $recipientName
       - envelope(): Envelope with subject "Site Survey Submitted — {$this->survey->project_name}"
       - content(): Content with view 'emails.survey-submitted'

    2. Create resources/views/emails/survey-submitted.blade.php:
       Plain-text-friendly HTML. Do not use a Blade component layout. Inline styles only.
       Content to show:
         - "Site Survey Submitted" heading
         - Project name, project ref
         - Site address
         - Surveyor name (from $survey->surveyor_name)
         - Survey date (formatted d M Y, or "Not recorded" if null)
         - Rooms completed: count of rooms where is_completed=true out of total rooms
         - H&S Notes: show if $survey->h_and_s_notes is non-empty
         - Site Risks: show if $survey->site_risks is non-empty
         - Footer: "This is an automated notification from the RAMS Platform."
       Pass $survey to the view. Eager-load rooms in the mailable if needed by accessing
       $survey->rooms()->where('is_completed', true)->count() in the view or pass counts
       from the mailable constructor.
       Better approach: pass $completedRooms and $totalRooms as public properties on the
       mailable, computed in the constructor:
         public readonly int $completedRooms;
         public readonly int $totalRooms;
         // In constructor body:
         $this->completedRooms = $survey->rooms()->where('is_completed', true)->count();
         $this->totalRooms     = $survey->rooms()->count();

    3. In SurveyService::submitPublic() add PM email dispatch AFTER the DB::transaction
       closure returns (outside the transaction lambda to avoid holding connection open
       during mail send). After the `return DB::transaction(...)` call, before returning
       $survey:

       Actually: the transaction returns $survey. Restructure to capture the result, then
       send mail, then return:

         $survey = DB::transaction(function () use ($survey, $data) {
             // ... existing code ...
             return $survey->fresh();
         });

         // ── Gap 1: PM email notification ──────────────────────────────────────
         if ($survey->project_id) {
             $project   = \App\Models\Project::with('owner')->find($survey->project_id);
             $recipient = $project?->owner;
             if (! $recipient) {
                 $recipient = \App\Models\User::where('is_admin', true)->first();
             }
             if ($recipient && $recipient->email) {
                 try {
                     \Illuminate\Support\Facades\Mail::to($recipient->email)
                         ->send(new \App\Mail\SurveySubmittedMail($survey, $recipient->name));
                 } catch (\Throwable $e) {
                     \Illuminate\Support\Facades\Log::warning(
                         'SurveyService: failed to send PM notification email',
                         ['survey_id' => $survey->id, 'error' => $e->getMessage()]
                     );
                 }
             }
         }

         return $survey;

       Do NOT move anything else outside the transaction. The existing auto-advance logic
       must remain inside the transaction closure.
  </action>
  <verify>
    php artisan tinker --execute="new App\Mail\SurveySubmittedMail(App\Models\SiteSurvey::first(), 'Test');" 2>&1 | grep -v "^=" | head -5 || echo "Mailable instantiates"
  </verify>
  <done>
    SurveySubmittedMail.php exists with correct Envelope/Content structure. Email view renders project name, ref, address, surveyor, date, room counts, H&S/risks when non-empty. submitPublic() sends mail after transaction commits, with a try/catch that logs warnings on failure but never throws.
  </done>
</task>

<task type="auto">
  <name>Task 3: Public survey form — all 6 gap fields</name>
  <files>
    resources/views/public-survey/show.blade.php
  </files>
  <action>
    All edits are within the per-room card loop. Each room card uses Alpine.js x-data for
    its has_network state. The existing pattern wraps a room in x-data with the network
    checkbox state. If no Alpine x-data wrapper exists on the room card for has_network,
    add one scoped to the network detail block only.

    LOCATE the infrastructure section per room (the .infra-panel div containing
    network_port_count and existing_cabling fields).

    GAP 2 — Cable route from/to:
    After the existing cable_route_desc textarea (search for name="rooms[{{ $ri }}][cable_route_desc]"
    — if present in the infra panel, add after it; if not present in the public form, add
    at the end of the infra panel body):
      <div class="form-group">
          <label class="form-label">Cable Route From <span style="font-weight:400;color:#6B7280;font-size:.82rem;">(optional)</span></label>
          <input type="text" name="rooms[{{ $ri }}][cable_route_from]" class="form-control"
                 value="{{ $room->cable_route_from }}" maxlength="500"
                 {{ $readonly ? 'readonly' : '' }}
                 placeholder="e.g. Main comms room, Rack 1">
      </div>
      <div class="form-group">
          <label class="form-label">Cable Route To <span style="font-weight:400;color:#6B7280;font-size:.82rem;">(optional)</span></label>
          <input type="text" name="rooms[{{ $ri }}][cable_route_to]" class="form-control"
                 value="{{ $room->cable_route_to }}" maxlength="500"
                 {{ $readonly ? 'readonly' : '' }}
                 placeholder="e.g. Room display position, Speaker 1">
      </div>

    GAP 3 — Rack room toggle:
    Add inside the infra panel, near the top (after the infra toggle button area or at
    the start of the infra panel body):
      <div style="margin-bottom:.75rem;">
          <label style="display:flex;align-items:center;gap:.6rem;cursor:pointer;">
              <input type="hidden" name="rooms[{{ $ri }}][is_rack_room]" value="0">
              <input type="checkbox" name="rooms[{{ $ri }}][is_rack_room]" value="1"
                     {{ $room->is_rack_room ? 'checked' : '' }}
                     {{ $readonly ? 'disabled' : '' }}>
              <span style="font-size:.875rem;color:#1F2937;font-weight:500;">This room contains the main comms rack</span>
          </label>
      </div>

    GAP 4 — Projection / viewing distance:
    Add at the end of the infra panel body, before the closing </div> of the panel:
      <div class="form-grid-2">
          <div class="form-group">
              <label class="form-label">Projection Throw (m) <span style="font-weight:400;color:#6B7280;font-size:.82rem;">(optional)</span></label>
              <input type="number" name="rooms[{{ $ri }}][projection_throw_m]" class="form-control"
                     value="{{ $room->projection_throw_m }}" min="0" max="999" step="0.01"
                     {{ $readonly ? 'readonly' : '' }}
                     placeholder="e.g. 3.50">
          </div>
          <div class="form-group">
              <label class="form-label">Viewing Distance (m) <span style="font-weight:400;color:#6B7280;font-size:.82rem;">(optional)</span></label>
              <input type="number" name="rooms[{{ $ri }}][viewing_distance_m]" class="form-control"
                     value="{{ $room->viewing_distance_m }}" min="0" max="999" step="0.01"
                     {{ $readonly ? 'readonly' : '' }}
                     placeholder="e.g. 5.00">
          </div>
      </div>

    GAP 5 — Network detail fields with Alpine.js x-show:
    Find the has_network checkbox in the public form. The room card must have an Alpine
    x-data wrapper that tracks has_network boolean. Look for the existing x-data pattern
    on the room card outer div. If the room card uses x-data="{ ... }" add
    hasNetwork: {{ $room->has_network ? 'true' : 'false' }} to the existing x-data object.
    Wire the has_network checkbox: @change="hasNetwork = $event.target.checked"
    Then after the network_port_count field, add:
      <div x-show="hasNetwork" style="display:none;">
          <div class="form-grid-2">
              <div class="form-group">
                  <label class="form-label">Network SSID <span style="font-weight:400;color:#6B7280;font-size:.82rem;">(optional)</span></label>
                  <input type="text" name="rooms[{{ $ri }}][network_ssid]" class="form-control"
                         value="{{ $room->network_ssid }}" maxlength="255"
                         {{ $readonly ? 'readonly' : '' }}
                         placeholder="Wireless network name">
              </div>
              <div class="form-group">
                  <label class="form-label">VLAN <span style="font-weight:400;color:#6B7280;font-size:.82rem;">(optional)</span></label>
                  <input type="text" name="rooms[{{ $ri }}][network_vlan]" class="form-control"
                         value="{{ $room->network_vlan }}" maxlength="100"
                         {{ $readonly ? 'readonly' : '' }}
                         placeholder="e.g. VLAN 10">
              </div>
              <div class="form-group">
                  <label class="form-label">Switch Port <span style="font-weight:400;color:#6B7280;font-size:.82rem;">(optional)</span></label>
                  <input type="text" name="rooms[{{ $ri }}][network_switch_port]" class="form-control"
                         value="{{ $room->network_switch_port }}" maxlength="100"
                         {{ $readonly ? 'readonly' : '' }}
                         placeholder="e.g. SW1 Gi0/1">
              </div>
          </div>
      </div>

    IMPORTANT: The x-show div must have style="display:none;" as initial state so it hides
    before Alpine hydrates. Alpine will manage visibility after hydration.

    GAP 6 — Engineer sign-off:
    Add at the BOTTOM of each room card, after all sections and before the room card's
    closing </div>. This is outside all section panels so it always shows:
      <div style="margin-top:1rem;padding:1rem;background:#F0FDF4;border:1.5px solid #86EFAC;border-radius:6px;">
          <p style="font-size:.82rem;font-weight:700;color:#166534;margin-bottom:.75rem;">Engineer Sign-off</p>
          <label style="display:flex;align-items:flex-start;gap:.6rem;cursor:{{ $readonly ? 'default' : 'pointer' }};margin-bottom:.75rem;">
              <input type="hidden" name="rooms[{{ $ri }}][engineer_confirmed]" value="0">
              <input type="checkbox" name="rooms[{{ $ri }}][engineer_confirmed]" value="1"
                     {{ $room->engineer_confirmed ? 'checked' : '' }}
                     {{ $readonly ? 'disabled' : '' }}
                     style="margin-top:.2rem;flex-shrink:0;">
              <span style="font-size:.875rem;color:#1F2937;line-height:1.5;">
                  I confirm I have physically inspected this room and the information above is accurate.
              </span>
          </label>
          <div class="form-group" style="margin-bottom:0;">
              <label class="form-label">Your Name (signature)</label>
              <input type="text" name="rooms[{{ $ri }}][engineer_signature_name]" class="form-control"
                     value="{{ $room->engineer_signature_name }}" maxlength="255"
                     {{ $readonly ? 'readonly' : '' }}
                     placeholder="Full name">
          </div>
      </div>
  </action>
  <verify>
    php artisan view:clear && php artisan view:cache 2>&1 | tail -3
  </verify>
  <done>
    Public survey form compiles without errors. Cable route from/to inputs present in infra panel. Rack room checkbox present in infra panel. Projection/viewing distance numeric inputs present. Network SSID/VLAN/switch port fields inside an x-show="hasNetwork" div that hides initially. Engineer sign-off section with checkbox + name field at bottom of each room card.
  </done>
</task>

<task type="auto">
  <name>Task 4: Internal forms (room-form partial + show page) — all gaps</name>
  <files>
    resources/views/site-survey/_room-form.blade.php
    resources/views/site-survey/show.blade.php
  </files>
  <action>
    In resources/views/site-survey/_room-form.blade.php:

    The infra-panel div (style="display:none;") contains measurements and services.
    LOCATE: the 'Access / Hazard Notes' textarea — this appears near the bottom of the
    infra-panel. The cable_route_desc field may not be present (the form only has
    rack_unit_space and cable_route_desc referenced by grep on the service but not in the
    form HTML we read). Check: if cable_route_desc field is not already in the form,
    add it together with the gap fields below.

    After existing_cabling textarea (or after cable_route_desc if present), add:
      GAP 2 — cable_route_from / cable_route_to:
        <div class="form-group">
            <label class="form-label">Cable Route From</label>
            <input type="text" name="rooms[{{ $ri }}][cable_route_from]" class="form-control"
                   value="{{ $val('cable_route_from') }}" maxlength="500"
                   placeholder="e.g. Main comms room">
        </div>
        <div class="form-group">
            <label class="form-label">Cable Route To</label>
            <input type="text" name="rooms[{{ $ri }}][cable_route_to]" class="form-control"
                   value="{{ $val('cable_route_to') }}" maxlength="500"
                   placeholder="e.g. Room display position">
        </div>

      GAP 3 — rack room toggle (add near the cable_route fields or rack_unit_space):
        <div style="margin-bottom:.75rem;">
            <label class="check-item" style="cursor:pointer;">
                <input type="hidden" name="rooms[{{ $ri }}][is_rack_room]" value="0">
                <input type="checkbox" name="rooms[{{ $ri }}][is_rack_room]" value="1"
                       {{ $chk('is_rack_room') ? 'checked' : '' }}>
                <span>Main comms rack room</span>
            </label>
        </div>

      GAP 4 — projection / viewing distance (add in the infra-panel dimensions area):
        <div class="form-group">
            <label class="form-label">Projection Throw (m)</label>
            <input type="number" name="rooms[{{ $ri }}][projection_throw_m]" class="form-control"
                   value="{{ $val('projection_throw_m') }}" min="0" max="999" step="0.01">
        </div>
        <div class="form-group">
            <label class="form-label">Viewing Distance (m)</label>
            <input type="number" name="rooms[{{ $ri }}][viewing_distance_m]" class="form-control"
                   value="{{ $val('viewing_distance_m') }}" min="0" max="999" step="0.01">
        </div>

      GAP 5 — network detail fields (add after network_port_count inputs):
        <div class="form-group">
            <label class="form-label">Network SSID</label>
            <input type="text" name="rooms[{{ $ri }}][network_ssid]" class="form-control"
                   value="{{ $val('network_ssid') }}" maxlength="255">
        </div>
        <div class="form-group">
            <label class="form-label">VLAN</label>
            <input type="text" name="rooms[{{ $ri }}][network_vlan]" class="form-control"
                   value="{{ $val('network_vlan') }}" maxlength="100">
        </div>
        <div class="form-group">
            <label class="form-label">Switch Port</label>
            <input type="text" name="rooms[{{ $ri }}][network_switch_port]" class="form-control"
                   value="{{ $val('network_switch_port') }}" maxlength="100">
        </div>
        NOTE: In the internal form, no x-show condition is needed — always show these fields.

      GAP 6 — engineer sign-off (add at the bottom of the room-card-body div, before its
      closing </div>, after all section panels):
        <div style="margin-top:.75rem;padding:.75rem;background:#F0FDF4;border:1.5px solid #86EFAC;border-radius:6px;">
            <p style="font-size:.8rem;font-weight:700;color:#166534;margin-bottom:.5rem;">Engineer Sign-off</p>
            <label class="check-item" style="margin-bottom:.5rem;">
                <input type="hidden" name="rooms[{{ $ri }}][engineer_confirmed]" value="0">
                <input type="checkbox" name="rooms[{{ $ri }}][engineer_confirmed]" value="1"
                       {{ $chk('engineer_confirmed') ? 'checked' : '' }}>
                <span style="font-size:.875rem;">Physically inspected and confirmed</span>
            </label>
            <div class="form-group" style="margin-bottom:0;">
                <label class="form-label">Engineer Signature Name</label>
                <input type="text" name="rooms[{{ $ri }}][engineer_signature_name]" class="form-control"
                       value="{{ $val('engineer_signature_name') }}" maxlength="255">
            </div>
        </div>

    In resources/views/site-survey/show.blade.php:

    GAP 3 — rack room badge on internal show page:
    Find the per-room display block (near line 495 where $room->room_name is shown and
    $isComplete badge is displayed). After the "Complete" badge (or alongside the existing
    status badges), add:
      @if($room->is_rack_room)
          <span style="font-size:.72rem;font-weight:700;color:#fff;background:#0B3C45;padding:.15rem .5rem;border-radius:20px;margin-right:.3rem;">RACK ROOM</span>
      @endif

    GAP 6 — engineer sign-off display on internal show page:
    Find a location after the room's completion status display (inside the per-room
    section of the show page). Add:
      @if($room->engineer_confirmed || $room->engineer_signature_name)
      <div style="margin-top:.5rem;font-size:.8rem;color:#166534;">
          @if($room->engineer_confirmed)
              <span style="font-weight:700;">&#10003; Engineer confirmed</span>
          @endif
          @if($room->engineer_signature_name)
              &mdash; {{ $room->engineer_signature_name }}
          @endif
      </div>
      @endif
  </action>
  <verify>
    php artisan view:clear 2>&1 | tail -2
    php artisan tinker --execute="view('site-survey.show', ['survey' => App\Models\SiteSurvey::with('rooms')->first(), 'project' => null, 'latestPackage' => null]);" 2>&1 | head -5 || echo "View renders"
  </verify>
  <done>
    _room-form.blade.php includes all gap fields. show.blade.php shows RACK ROOM badge for is_rack_room=true rooms and displays engineer_confirmed + signature name when set. php artisan view:clear runs without error.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public form → server | Unauthenticated POST from engineer's device; all room data untrusted |
| Survey token → survey lookup | UUID token gates all public access; no auth session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-en6-01 | Tampering | rooms.*.engineer_confirmed — checkbox can be set by anyone with the link | accept | Survey is gated by UUID token; engineer identity is name-only, not authenticated. Sign-off is informational, not a legal signature. |
| T-en6-02 | Information Disclosure | PM email reveals project name + site address to log | accept | MAIL_MAILER=log in dev; production mailer sends only to project owner's email which is already trusted. |
| T-en6-03 | Spoofing | engineer_signature_name free text — anyone can type any name | accept | Same rationale as T-en6-01; informational capture only. |
| T-en6-04 | Denial of Service | Mail::send() failure could surface exception to engineer | mitigate | Wrapped in try/catch in submitPublic(); failure logs warning but does not throw — submission proceeds. |
</threat_model>

<verification>
After all tasks complete:
1. php artisan migrate:status | grep add_survey_room_gap — shows Ran
2. Load the public survey URL for an existing survey — page renders, all new fields present
3. Check MAIL_LOG (storage/logs/laravel.log) after submit — SurveySubmittedMail entry visible
4. Load internal site-survey/show page — rooms with is_rack_room show RACK ROOM badge
</verification>

<success_criteria>
- Migration ran cleanly, ten new nullable columns on site_survey_rooms
- SurveySubmittedMail sends (to log) on public survey submission with project/room data
- cable_route_from, cable_route_to inputs save and display on both forms
- is_rack_room toggle saves; RACK ROOM badge appears on internal show page
- projection_throw_m, viewing_distance_m numeric fields save on both forms
- network_ssid/vlan/switch_port show only when has_network checked on public form; always shown on internal form
- engineer_confirmed + engineer_signature_name save on both forms; displayed on internal show page
- Existing survey submission flow (auto-advance, activity log) unchanged
</success_criteria>

<output>
After completion, create .planning/quick/260415-en6-fix-all-site-survey-gaps-pm-email-notifi/260415-en6-SUMMARY.md
</output>
