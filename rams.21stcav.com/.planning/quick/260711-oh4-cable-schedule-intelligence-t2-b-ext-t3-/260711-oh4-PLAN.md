---
quick_id: 260711-oh4
slug: cable-schedule-intelligence-t2-b-ext-t3-b-t3-c
date: 2026-07-11
description: >
  Cable schedule intelligence follow-up to 260711-n2x. Bundles three linked upgrades
  landing as one plan with three atomic commits in order:
    - T2-B-ext: cross-room signal-graph chains (a room with local dest/amp but no
      local source/DSP falls through to devices in a central-room to close the DAG).
    - T3-B: is_critical processor emits paired primary + '-R' redundant rows so
      diverse-routing runs are visible in the schedule.
    - T3-C: PoE budget solver decorates PoE-cable rows destined for a switch with
      a ⚠ / ⚠⚠-OVER-BUDGET warning based on Device.pse_budget_w vs sum(pd_load_w).
  All three are additive. Every existing test must stay green; no config/cables.php,
  CablePrompt, or port-picker Blade changes. The v1.3 surface-file guard already
  omits CableScheduleGeneratorService.php (removed in 260711-n2x) so is_redundant
  + Phase-22 columns can be persisted freely.
must_haves:
  truths:
    - "T2-B-ext: central-room devices act as sources/processors for local rooms that lack the local counterpart; every cross-room row carries a 'Cross-room: {source_room} → {dest_room}' prefix in notes so engineers can spot the chain at a glance. When a project has no central-room match, per-room graphs are byte-for-byte identical to pre-bundle behaviour."
    - "T3-B: Device.is_critical=true processors cause emitDagEdges to emit paired primary + redundant rows — cable_id N (primary) followed by cable_id N-with-'-R' suffix (redundant) — with the [REDUNDANT] notes prefix + is_redundant=true column. Zero critical devices in the room short-circuits with early-return so cost stays flat."
    - "T3-C: after all rows are persisted, checkPoeBudgets groups PoE cables by destination switch and appends '⚠ Switch {name} PoE budget: {total}W of {budget}W ({pct}%)' when total/budget ≥ 0.8, or '⚠⚠ OVER BUDGET Switch {name} PoE budget: {total}W of {budget}W ({pct}%)' when total/budget ≥ 1.0. Null pse_budget_w or any null pd_load_w in the group silently skips (soft opt-in). Non-PoE rows never receive the warning."
    - "All existing tests remain green (598-test suite from 260711-n2x). New tests cover T2-B-ext (4), T3-B (4), T3-C (5). Three additive nullable migrations (000002/000003/000004) are reversible via down()."
  artifacts:
    - path: app/Services/CableScheduleGeneratorService.php
      provides: broadened buildSignalGraph + redundant-row emission + checkPoeBudgets
    - path: app/Models/Device.php
      provides: is_critical + pse_budget_w + pd_load_w in $fillable + $casts
    - path: app/Models/CableScheduleItem.php
      provides: is_redundant in $fillable + $casts
    - path: database/migrations/2026_07_11_000002_add_is_critical_to_devices.php
      provides: devices.is_critical nullable boolean
    - path: database/migrations/2026_07_11_000003_add_is_redundant_to_cable_schedule_items.php
      provides: cable_schedule_items.is_redundant nullable boolean
    - path: database/migrations/2026_07_11_000004_add_poe_metadata_to_devices.php
      provides: devices.pse_budget_w + devices.pd_load_w nullable floats
    - path: tests/Feature/Cable/CableScheduleDagGenerationTest.php
      provides: extended tests for cross-room + redundancy cases
    - path: tests/Unit/Services/Cable/CableScheduleGeneratorServiceTest.php
      provides: extended tests for PoE budget cases
  key_links:
    - from: buildSignalGraph
      to: generateFromDevices (central-devices precomputed once, passed per room)
      pattern: "buildSignalGraph.*localDevices.*centralDevices"
    - from: emitDagEdges
      to: appended redundant-row branch (guarded by is_critical check + early return)
      pattern: "is_critical.*C-.*-R"
    - from: generate + generateFromDevices tail
      to: checkPoeBudgets($schedule->id)
      pattern: "checkPoeBudgets"

deferred_next:
  - "Tier 3-D — externalise inferCableRun keyword branches to a rules table so admins can add new equipment classifiers without a code deploy."
  - "Tier 4 UI — Device editor (admin surface) exposing is_critical + pse_budget_w + pd_load_w; today these must be mutated via direct DB update."
  - "Cross-project cable-run caching — same equipment set across projects re-uses solved cable rows to shave classification time on repeat generations."
---

<objective>
Ship three linked cable-schedule intelligence upgrades as one plan, three atomic commits,
in order T2-B-ext → T3-B → T3-C. Zero regressions to the 260711-n2x DAG generator or the
XLSX regression suite.

**Why the order matters:** T2-B-ext broadens the DAG (cross-room signal chains), T3-B walks
that broadened DAG when doubling critical-processor rows, and T3-C runs post-persist so it
sees every row from both prior tasks. Reordering breaks the chain.

Purpose: Push the CableScheduleGenerator from "room-scoped signal-aware DAG" (post 260711-n2x)
to "project-scoped signal-aware DAG with redundancy hints + PoE budget validation" — i.e.
close the last three gaps engineers flagged during the 260711-n2x review.
Output: 1 plan · 3 tasks · 3 atomic commits · 3 additive migrations · updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@app/Services/CableScheduleGeneratorService.php
@app/Services/Cable/StencilPortResolver.php
@app/Models/Device.php
@app/Models/CableScheduleItem.php
@app/Models/SiteSurveyRoom.php
@database/migrations/2026_07_11_000000_widen_cable_schedule_item_location_columns.php
@database/migrations/2026_07_11_000001_add_signal_type_to_cable_schedule_items.php
@tests/Feature/Cable/CableScheduleDagGenerationTest.php
@tests/Unit/Services/Cable/CableScheduleGeneratorServiceTest.php
@tests/Feature/Cable/CableScheduleXlsxRegressionTest.php

<interfaces>
<!-- Existing service-level consts already declared in CableScheduleGeneratorService (from 260711-n2x). Do NOT recreate. -->
- SIGNAL_PATH_ORDER (line 79)     — processor rank keywords: dsp / audio-processor / matrix / switcher / codec / amplifier
- DISTANCE_WARNING_RULES (line 90) — T1-E length warnings
- QUOTED_CABLE_SIGNAL_KEYWORDS (line 62) — T1-D quoted-cable signal-type map

<!-- Existing method signatures being touched -->
- private function buildSignalGraph(Collection $devicesInRoom): array
    → BROADEN in T2-B-ext to (Collection $localDevices, Collection $centralDevices)
- private function emitDagEdges(CableSchedule $schedule, array $graph, array $overrides,
      ?float $roomLengthM, string $roomName, int &$sortOrder): int
    → EXTEND in T3-B to append a redundant-row after each critical-processor edge.
- public function generate(CableSchedule $schedule): int
- private function generateFromDevices(CableSchedule $schedule, Collection $devices): int
    → EXTEND in T3-C tail-end to call $this->checkPoeBudgets($schedule->id).

<!-- Device model -->
- public function isSource() / isDestination() / isProcessor()  — existing
- public function hasUnknownSignalRole()                        — existing
- $fillable and $casts include qty, u_height, is_rack_mounted, etc.
    → ADD in T3-B: is_critical (fillable) + boolean cast.
    → ADD in T3-C: pse_budget_w + pd_load_w (fillable) + float casts.

<!-- CableScheduleItem model -->
- $fillable already includes cable_id, signal_type, source_device_id/dest_device_id
    → ADD in T3-B: is_redundant (fillable) + boolean cast.

<!-- Migration pattern (from 260711-env / 260711-n2x — same day, same tables) -->
- Schema::table('devices', ...) $t->boolean('is_critical')->nullable()->after('signal_role');
- Schema::table('devices', ...) $t->float('pse_budget_w')->nullable()->after('is_critical');
                                $t->float('pd_load_w')->nullable()->after('pse_budget_w');
- Schema::table('cable_schedule_items', ...) $t->boolean('is_redundant')->nullable()->after('signal_type');
- Each down() drops the added column(s).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: T2-B-ext — cross-room signal-graph chains</name>
  <files>
    app/Services/CableScheduleGeneratorService.php,
    tests/Feature/Cable/CableScheduleDagGenerationTest.php
  </files>
  <action>
    Broaden buildSignalGraph so a "local" room can fall back to devices sitting in a
    designated central room (comms room, AV rack room, equipment room) for any
    signal_type the local room doesn't source/process/destination itself.

    1. Add service-level const alongside SIGNAL_PATH_ORDER:
       ```
       private const CENTRAL_ROOM_KEYWORDS = [
           'comms room', 'comms', 'av rack', 'rack room', 'equipment room',
           'server room', 'central', 'plant room',
       ];
       ```
       (Substring match, case-insensitive against strtolower(room_name).)

    2. Change buildSignalGraph signature:
       BEFORE: private function buildSignalGraph(Collection $devicesInRoom): array
       AFTER : private function buildSignalGraph(Collection $localDevices, Collection $centralDevices): array

       Every existing single-room caller currently in tests passes zero central
       devices — the second argument defaults to an empty collection to keep the
       byte-for-byte no-central-room contract. Do NOT default the parameter itself;
       always pass an explicit empty collect() at every call site so the signature
       stays honest.

    3. Inside buildSignalGraph, bucket $localDevices as today. AFTER the local walk,
       walk $centralDevices and slot each into $graph[signalType]['sources'|'processors'|'destinations']
       ONLY when the target bucket for that signal_type is currently empty in the
       local graph. Concrete rule per signal_type:
       - Local room has a source for signal_type X → do NOT pull a central source
         for X. Local always wins.
       - Local room has no source for signal_type X → pull EVERY central source
         for X. Same logic for processors (empty → central) and destinations
         (rarely useful but consistent).
       This satisfies the locked-fact "mixed cases: room with local audio source
       but no local video source → audio stays local, video pulls from central".

    4. In generateFromDevices (the outer orchestrator), BEFORE the foreach loop:
       ```
       $centralDevices = $devices->filter(function (Device $d) {
           $name = strtolower(trim((string) ($d->room_name ?? '')));
           foreach (self::CENTRAL_ROOM_KEYWORDS as $kw) {
               if ($name !== '' && str_contains($name, $kw)) { return true; }
           }
           return false;
       })->values();
       ```
       Compute once per generation. Pass into every per-room buildSignalGraph call.
       The central-room itself, when the outer loop reaches it, still gets its
       own devices bucketed — its $localDevices IS the central set, and it passes
       an empty central collection to itself (avoid double-counting).

    5. In createDagEdge, when fromDevice->room_name differs from the target
       $roomName (i.e. from a cross-room central device), PREFIX the notes with
       "Cross-room: {source_room} → {dest_room} | " BEFORE the existing role /
       warning notes. Use " | " separator to match the T1-E warning convention.
       Same-room rows must not receive the prefix — early-out on room match.

    6. Update the sinks-only guard (empty($sources) currently continues): with
       central-room fallback, the empty-sources case only triggers when neither
       local nor central provides a source. Keep the continue; the log message
       stays as-is.

    7. Run `php -l app/Services/CableScheduleGeneratorService.php` after every edit.

    8. Extend CableScheduleDagGenerationTest with FOUR new tests:
       - `test_cross_room_dsp_serves_local_amp_speaker_chain` — one Boardroom
         with amp + speaker (both signal_role set), one "Comms Room" with a
         Q-Sys DSP marked ROLE_SOURCE. Boardroom bucket should emit rows for
         DSP → amp → speaker with the "Cross-room: Comms Room → Boardroom"
         prefix on the DSP→amp row and no prefix on the amp→speaker row.
       - `test_project_with_no_central_room_uses_pure_local_dag` — two rooms
         (Boardroom + Reception), no central-room keyword hits, all rows
         must be identical to the pre-bundle output (assert exact row count
         and that no row's notes contain 'Cross-room:').
       - `test_local_source_wins_over_central_source` — Boardroom has a local
         Sony source AND Comms Room has a Q-Sys DSP marked source for the
         same signal_type. The local Sony source is used; central DSP does
         not appear as a source in the Boardroom bucket (grep 'Q-Sys' out of
         Boardroom rows).
       - `test_cross_room_note_prefix_appears_only_on_across_room_rows` — set
         up one cross-room and one same-room edge in the same signal_type
         bucket; assert exactly one row contains 'Cross-room:' and one does
         not.

    Nothing else changes in this task: no migrations, no model changes, no
    checkPoeBudgets, no redundant rows.

    Commit message: `feat(cables): T2-B-ext cross-room signal-graph chains`
    Commit only these two files (service + feature test).
  </action>
  <verify>
    <automated>
      cd "C:/Users/sonny.tanda/Documents/1 - Claude Projects/Rams2/rams.21stcav.com" && \
      php -l app/Services/CableScheduleGeneratorService.php && \
      vendor/bin/phpunit tests/Feature/Cable/CableScheduleDagGenerationTest.php \
                        tests/Feature/Cable/CableScheduleXlsxRegressionTest.php \
                        tests/Unit/Services/Cable/CableScheduleGeneratorServiceTest.php --stop-on-failure
    </automated>
  </verify>
  <done>
    - `php -l` clean.
    - Four new cross-room tests pass.
    - Every existing CableScheduleDagGenerationTest + CableScheduleXlsxRegressionTest
      + CableScheduleGeneratorServiceTest test still green.
    - Single atomic commit touching only the two files listed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: T3-B — redundant-row emission for is_critical processors</name>
  <files>
    database/migrations/2026_07_11_000002_add_is_critical_to_devices.php,
    database/migrations/2026_07_11_000003_add_is_redundant_to_cable_schedule_items.php,
    app/Models/Device.php,
    app/Models/CableScheduleItem.php,
    app/Services/CableScheduleGeneratorService.php,
    tests/Feature/Cable/CableScheduleDagGenerationTest.php
  </files>
  <behavior>
    - When a device with signal_role=processor AND is_critical=true participates in a DAG edge,
      the schedule emits BOTH the primary row (cable_id C-NNN) AND a redundant row (cable_id
      C-NNN-R with the same signal/cable_type/length + is_redundant=true + notes prefix
      "[REDUNDANT] Primary + backup path — diverse routing recommended").
    - When no device in the room has is_critical=true, emitDagEdges short-circuits the
      redundant-check with an early return so a project with 0 critical devices pays
      zero extra cost per edge.
    - is_critical=null (never set) is treated identically to is_critical=false — no
      redundant rows. Backfill safety for pre-migration data.
    - sort_order allocation: primary gets N, redundant gets N+1, next primary gets N+2. The
      global counter increments TWICE per critical-processor edge — never fractional.
      cable_id for the redundant row uses the primary's padded number with a '-R' suffix
      (e.g. primary C-005 → redundant C-005-R, next primary C-007).
  </behavior>
  <action>
    Persist redundancy at the DB layer, then extend the DAG walker to emit paired rows
    when a critical processor is on the edge.

    1. Migration `2026_07_11_000002_add_is_critical_to_devices.php`:
       ```
       public function up(): void {
           Schema::table('devices', function (Blueprint $t) {
               $t->boolean('is_critical')->nullable()->after('signal_role');
           });
       }
       public function down(): void {
           Schema::table('devices', function (Blueprint $t) {
               $t->dropColumn('is_critical');
           });
       }
       ```
       Match the class-anonymous-migration style used in the 000000/000001 files
       (`return new class extends Migration { ... };`).

    2. Migration `2026_07_11_000003_add_is_redundant_to_cable_schedule_items.php`:
       ```
       $t->boolean('is_redundant')->nullable()->after('signal_type');
       ```
       Down drops the column. Same anonymous-class style.

    3. Device model: add `'is_critical'` to $fillable (after 'signal_role') and
       `'is_critical' => 'boolean'` to $casts. Do NOT add default false — nullable
       is intentional so pre-migration rows read as null (treated as false at the
       is_critical === true check site).

    4. CableScheduleItem model: add `'is_redundant'` to $fillable (after 'signal_type'
       position in the list) and `'is_redundant' => 'boolean'` to $casts. Keep the
       existing D-10 no-$with docblock unchanged.

    5. CableScheduleGeneratorService — extend emitDagEdges:

       At the top of the outer foreach loop over $graph as $signalType => $bucket,
       compute a per-signal-type early-out flag:
       ```
       $anyCritical = false;
       foreach ($bucket['processors'] as $p) {
           if ($p->is_critical === true) { $anyCritical = true; break; }
       }
       ```
       If $anyCritical is false, walk the existing chain unchanged — no redundant
       rows, zero extra cost.

       Inside createDagEdge OR a new helper `emitRedundantIfCritical` called after
       each primary createDagEdge call: when the emitted edge's fromDevice is a
       processor with is_critical=true (the more paranoid rule — if EITHER endpoint
       is a critical processor the edge is critical), immediately after the primary
       row is persisted:
       - allocate the next sortOrder (sortOrder++);
       - build cable_id as the PRIMARY's cable_id with a '-R' suffix (NOT the next
         padded number — the redundant row's sort_order is monotonic but its cable_id
         is anchored to the primary for engineer traceability);
       - PREFIX notes with "[REDUNDANT] Primary + backup path — diverse routing recommended | "
         then join to the primary's existing notes;
       - copy every other column verbatim from the primary row (signal_type,
         cable_type, cores, approx_length_m, source_device_id, source_port_id,
         dest_device_id, dest_port_id, from_location, to_location);
       - set is_redundant=true.
       Return the count added (1) so the caller's total accumulator picks it up.

       Concrete refactor: inside createDagEdge, keep track of the primary's
       cable_id and the persisted row. Then, gated by `$this->isCriticalEdge($fromDevice, $toDevice)`
       helper (returns true when either endpoint has signal_role=processor AND
       is_critical=true), emit the redundant row and increment $created.

       Return the running $created from createDagEdge so emitDagEdges' accumulator
       stays correct.

    6. Run `php -l app/Services/CableScheduleGeneratorService.php` and
       `php -l app/Models/Device.php` and `php -l app/Models/CableScheduleItem.php`.

    7. Extend CableScheduleDagGenerationTest with FOUR new tests. Every test creates
       a project, seeds Devices, calls $this->makeSvc()->generate($schedule):
       - `test_critical_processor_emits_redundant_row_pair` — source + processor
         (is_critical=true) + destination → assert 4 rows (2 primary + 2
         redundant). Assert the second row's cable_id ends with '-R' and its
         is_redundant is true. Assert the redundant row's notes start with
         '[REDUNDANT]'.
       - `test_non_critical_processor_emits_single_edge` — same seed as above
         but is_critical=false → assert 2 rows (source→proc, proc→dst), zero
         redundant rows, is_redundant is null on both.
       - `test_multiple_critical_processors_emit_paired_rows_per_edge` — source
         + 2 critical processors + destination → assert 6 rows total (3 primary
         + 3 redundant). Assert each '-R' cable_id maps to a distinct primary
         cable_id (unique pairs).
       - `test_project_without_is_critical_data_uses_default_no_redundancy` — do
         NOT set is_critical on any device (defaults to null via the migration).
         Assert zero rows have is_redundant=true.

       Also assert in each test that sort_order remains strictly monotonic
       (1, 2, 3, ... without gaps or fractional values).

    8. Run migrations then the full cable suite. Both migrations MUST be part of
       the same atomic commit as the service + model + test changes.

    Commit message: `feat(cables): T3-B redundant-row emission for is_critical processors`
    Commit files: both migrations + Device.php + CableScheduleItem.php +
    CableScheduleGeneratorService.php + CableScheduleDagGenerationTest.php.
  </action>
  <verify>
    <automated>
      cd "C:/Users/sonny.tanda/Documents/1 - Claude Projects/Rams2/rams.21stcav.com" && \
      php -l app/Services/CableScheduleGeneratorService.php && \
      php -l app/Models/Device.php && \
      php -l app/Models/CableScheduleItem.php && \
      php artisan migrate --force && \
      vendor/bin/phpunit tests/Feature/Cable/CableScheduleDagGenerationTest.php \
                        tests/Feature/Cable/CableScheduleXlsxRegressionTest.php \
                        tests/Unit/Services/Cable/CableScheduleGeneratorServiceTest.php --stop-on-failure
    </automated>
  </verify>
  <done>
    - Two new migrations up + down cleanly (nullable additive columns).
    - `php -l` clean across all three edited PHP files.
    - Four new T3-B tests pass.
    - Every previously-green test (including Task 1's four cross-room tests) still green.
    - sort_order stays monotonic through paired rows.
    - Single atomic commit touching only the six files listed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: T3-C — PoE budget solver post-persist decorator</name>
  <files>
    database/migrations/2026_07_11_000004_add_poe_metadata_to_devices.php,
    app/Models/Device.php,
    app/Services/CableScheduleGeneratorService.php,
    tests/Unit/Services/Cable/CableScheduleGeneratorServiceTest.php,
    tests/Feature/Cable/CableScheduleDagGenerationTest.php
  </files>
  <behavior>
    - After all rows are persisted, checkPoeBudgets loads every PoE-cable row in
      the schedule (cable_type matches /poe/i), groups them by destination device,
      and for each destination that is a switch (Device.signal_role='destination'
      AND strtolower(manufacturer.' '.model) contains 'switch'):
        budget = destination->pse_budget_w
        total  = sum(source->pd_load_w) for the group
        pct    = round(total / budget * 100)  // integer 0-9999
      - budget=null OR any source pd_load_w=null → skip the group silently (soft opt-in).
      - pct >= 100 → prefix each row's notes with:
          "⚠⚠ OVER BUDGET Switch {switch_display_name} PoE budget: {total}W of {budget}W ({pct}%)"
        joined via " | " with existing notes.
      - 80 <= pct < 100 → prefix each row's notes with:
          "⚠ Switch {switch_display_name} PoE budget: {total}W of {budget}W ({pct}%)"
        joined via " | " with existing notes.
      - pct < 80 → no change.
      - Non-PoE rows (HDMI, XLR, speaker cable, Cat6 non-PoE etc.) never receive
        a budget warning even when they share a destination with PoE rows.
      - Bulk UPDATE per group, not per row (avoid N queries).
    - Runs at the very end of both generate() and generateFromDevices() — after
      all rows persisted, before the summary Log::info line.
  </behavior>
  <action>
    Add PoE metadata to Device, then a post-persist decorator that walks the
    finished schedule and decorates PoE rows.

    1. Migration `2026_07_11_000004_add_poe_metadata_to_devices.php`:
       ```
       public function up(): void {
           Schema::table('devices', function (Blueprint $t) {
               $t->float('pse_budget_w')->nullable()->after('is_critical');
               $t->float('pd_load_w')->nullable()->after('pse_budget_w');
           });
       }
       public function down(): void {
           Schema::table('devices', function (Blueprint $t) {
               $t->dropColumn(['pse_budget_w', 'pd_load_w']);
           });
       }
       ```
       Anonymous-class style matching the earlier migrations. Depends on Task 2's
       000002 migration having landed first (is_critical column must exist for
       the after() clause) — same-day timestamp keeps the ordering correct.

    2. Device model: add `'pse_budget_w'` and `'pd_load_w'` to $fillable (after
       'is_critical') and `'pse_budget_w' => 'float', 'pd_load_w' => 'float'` to
       $casts.

    3. CableScheduleGeneratorService — add private method:
       `private function checkPoeBudgets(int $scheduleId): void`

       Body:
       - $rows = CableScheduleItem::where('cable_schedule_id', $scheduleId)
                 ->whereNotNull('dest_device_id')
                 ->where('cable_type', 'like', '%PoE%')  // case-insensitive on MySQL default collation
                 ->get(['id','cable_type','notes','dest_device_id','source_device_id']);
         (belt-and-braces: also filter in PHP with preg_match('/poe/i', $r->cable_type)
         because the SQL LIKE collation might not be portable to sqlite tests.)
       - Group $rows by dest_device_id.
       - For each group:
           $switch = Device::find($destId);
           if ($switch === null) continue;
           $displayName = trim(($switch->manufacturer ?? '').' '.($switch->model ?? '')) ?: ($switch->description ?? 'Switch');
           $lowerHay    = strtolower($displayName);
           if ($switch->signal_role !== Device::ROLE_DESTINATION) continue;
           if (! str_contains($lowerHay, 'switch')) continue;
           $budget = $switch->pse_budget_w;
           if ($budget === null || $budget <= 0) continue;
           // Sum pd_load_w across every source; if ANY is null bail (soft opt-in).
           $sourceIds = $group->pluck('source_device_id')->filter()->unique()->values();
           $sources   = Device::whereIn('id', $sourceIds)->get(['id','pd_load_w']);
           if ($sources->contains(fn ($d) => $d->pd_load_w === null)) continue;
           $total = (float) $sources->sum('pd_load_w');
           $pct   = (int) round($total / $budget * 100);
           if ($pct < 80) continue;
           $warning = $pct >= 100
               ? sprintf('⚠⚠ OVER BUDGET Switch %s PoE budget: %sW of %sW (%d%%)',
                         $displayName, $this->fmtWatts($total), $this->fmtWatts($budget), $pct)
               : sprintf('⚠ Switch %s PoE budget: %sW of %sW (%d%%)',
                         $displayName, $this->fmtWatts($total), $this->fmtWatts($budget), $pct);
           // Bulk UPDATE per group. CONCAT with existing notes via ' | '.
           $ids = $group->pluck('id')->all();
           CableScheduleItem::whereIn('id', $ids)->update([
               'notes' => \DB::raw("CONCAT('" . addslashes($warning) . " | ', COALESCE(notes, ''))"),
           ]);
       Add a `fmtWatts(float $v)` helper: returns rtrim(rtrim(number_format($v, 2, '.', ''), '0'), '.')
       so 62.0 renders as "62", 62.5 as "62.5", 15.75 as "15.75".

    4. Wire the call at the tail of generate() AND generateFromDevices(), BEFORE
       their Log::info summary lines:
       ```
       $this->checkPoeBudgets($schedule->id);
       Log::info(...); // existing
       ```

    5. Run `php -l` on the service + Device model.

    6. Add FIVE new tests. Put the unit-shaped ones in
       tests/Unit/Services/Cable/CableScheduleGeneratorServiceTest.php (they use
       the DB, so add `use RefreshDatabase;` at the top of the class if not
       already present — the existing tests use in-memory buildRowsFromEquipmentLines
       only, so if adding RefreshDatabase is disruptive, put the new tests in
       CableScheduleDagGenerationTest.php instead which already has it):

       - `test_poe_budget_under_80pct_no_warning` — switch pse_budget_w=100,
         3 sources each pd_load_w=20 (total=60W, 60%) → assert no row's notes
         contains 'PoE budget:'.
       - `test_poe_budget_at_80pct_triggers_warning` — budget=100, 4×20=80W
         (80%) → assert every one of the 4 rows in the group contains
         '⚠ Switch' and '80W of 100W (80%)'.
       - `test_poe_budget_over_100pct_triggers_over_budget_warning` — budget=62,
         4×20=80W (129%) → assert every row's notes contains '⚠⚠ OVER BUDGET'
         and '(129%)'.
       - `test_poe_budget_null_budget_skips_check` — one source pd_load_w=null,
         others populated → assert no warning appears on any row in the group.
         Also test: pse_budget_w=null on the switch, all sources populated → no
         warning (either null branch triggers skip).
       - `test_non_poe_rows_never_receive_budget_warning` — same switch as a
         valid over-budget test, but seed an HDMI cable row alongside the PoE
         rows. Assert the HDMI row's notes never contains 'PoE budget:' even
         though other rows in the same schedule do.

    7. Every existing test in the three cable suites must still be green.

    Commit message: `feat(cables): T3-C PoE budget solver post-persist decorator`
    Commit files: 000004 migration + Device.php + CableScheduleGeneratorService.php
    + whichever test file(s) received the five new tests.
  </action>
  <verify>
    <automated>
      cd "C:/Users/sonny.tanda/Documents/1 - Claude Projects/Rams2/rams.21stcav.com" && \
      php -l app/Services/CableScheduleGeneratorService.php && \
      php -l app/Models/Device.php && \
      php artisan migrate --force && \
      vendor/bin/phpunit tests/Feature/Cable/CableScheduleDagGenerationTest.php \
                        tests/Feature/Cable/CableScheduleXlsxRegressionTest.php \
                        tests/Unit/Services/Cable/CableScheduleGeneratorServiceTest.php --stop-on-failure
    </automated>
  </verify>
  <done>
    - Migration 000004 up + down cleanly.
    - `php -l` clean.
    - Five new T3-C tests pass.
    - Every existing test + Task 1 + Task 2 tests still green.
    - checkPoeBudgets is called at the tail of BOTH generate() and generateFromDevices().
    - PoE rows over threshold receive warning notes; non-PoE and null-metadata rows do not.
    - Single atomic commit for T3-C (files listed above).
  </done>
</task>

</tasks>

<verification>
Full-suite regression:
```
cd "C:/Users/sonny.tanda/Documents/1 - Claude Projects/Rams2/rams.21stcav.com" && \
php artisan migrate --force && \
vendor/bin/phpunit tests/Feature/Cable/ tests/Unit/Services/Cable/ --stop-on-failure
```
Expectation: every existing test PLUS the 13 new tests (4 T2-B-ext + 4 T3-B + 5 T3-C) pass.

Manual smoke (optional but recommended before commit for anyone reviewing):
- Pick any project with a mix of central-room + local-room devices; run
  CableScheduleController's generate flow; open the XLSX; scan for the
  "Cross-room:" prefix appearing where expected.
- Set `is_critical=1` on any processor Device via `tinker` then re-generate;
  confirm paired C-NNN + C-NNN-R rows appear.
- Set `pse_budget_w` low + `pd_load_w` populated on 4 PoE endpoints tied to
  the same switch; confirm ⚠ warning appears on all four rows.

git log after Task 3 should show exactly THREE new commits in order:
```
feat(cables): T3-C PoE budget solver post-persist decorator
feat(cables): T3-B redundant-row emission for is_critical processors
feat(cables): T2-B-ext cross-room signal-graph chains
```
</verification>

<success_criteria>
- Three atomic commits landed in the order T2-B-ext → T3-B → T3-C, each self-contained.
- Three additive nullable migrations (000002 / 000003 / 000004) — up + down both work.
- 13 new tests green; all previously green tests still green (targeting the 598-test baseline).
- Zero changes to config/cables.php, CableSchedulePrompt.php, or the port-picker Blade views.
- Zero regressions to XLSX regression suite.
- `php -l` clean after every service / model edit.
- v1.3 surface-file guard remains as-is (CableScheduleGeneratorService.php stays OFF the list).
- All three features are soft opt-in: no is_critical / no pse_budget_w / no central-room match
  → generator output is byte-for-byte identical to pre-bundle output for that project.
</success_criteria>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CableScheduleController → CableScheduleGeneratorService | Trusted internal boundary; controller has authenticated + authorised the schedule owner. No new attack surface added here. |
| Device / CableScheduleItem $fillable | Mass-assignment guard; NEW fields (is_critical, pse_budget_w, pd_load_w, is_redundant) are boolean/float only, no XSS surface. |
| generate() / checkPoeBudgets() DB writes | Internal-only; no user-supplied strings interpolated into raw SQL. addslashes() on the warning string is defensive against WATTAGE display containing quotes (impossible per fmtWatts, but belt-and-braces). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-oh4-01 | Tampering | Device.is_critical mass assignment via unrelated controller (e.g. DeviceController@update) exposing the new column | mitigate | is_critical added to $fillable ONLY on Device model; every DeviceController write path must add its own validation before this ships as an admin-editable field. This bundle does NOT expose the column via any form or controller — writes happen only via DB or tinker until Tier-4 UI lands. |
| T-oh4-02 | Injection | checkPoeBudgets CONCAT bulk UPDATE injects the warning string via DB::raw | mitigate | Warning string is server-computed from fmtWatts() (numeric-only) + a hard-coded literal + Device fields (manufacturer/model/description) that are already sanitised on device create/update flows. addslashes() applied belt-and-braces; no user-supplied text touches the CONCAT. |
| T-oh4-03 | Denial of Service | O(N²) walk across every processor pair when scanning for is_critical edges | accept | Rooms have single-digit device counts in practice; early-return at $anyCritical=false keeps the fast path O(1). |
| T-oh4-04 | Information Disclosure | Redundant-row notes reveal architectural intent to any user with schedule read access | accept | Users with schedule read access are internal engineers; PoE / redundancy information IS the intended output. |
| T-oh4-05 | Repudiation | No audit log on which processor was flagged critical | accept | Deferred to Tier-4 (Device editor); this bundle only reads is_critical, does not write it. |
| T-oh4-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs — code-only + migrations. Package Legitimacy Gate N/A. |
</threat_model>

<deferred_next>
- **Tier 3-D — data-driven rules engine.** Externalise inferCableRun's substring
  keyword branches (display/HDBaseT/speaker/mic/DSP/…) into a `cable_inference_rules`
  table so admins can add new equipment classifiers without a code deploy. Requires
  admin UI + rule-priority column.

- **Tier 4 UI — device editor exposing new fields.** Add is_critical checkbox +
  pse_budget_w / pd_load_w numeric inputs to the Device editor Blade view (currently
  only mutable via direct DB / tinker). Ship with per-role visibility (pse_budget_w
  only visible when signal_role=destination + name contains 'switch'; pd_load_w
  only when signal_role=source and cable_type resolves to a PoE variant). Adds
  mass-assignment validation to close T-oh4-01.

- **Cross-project cable-run caching.** Same equipment set across projects re-uses
  solved cable rows to shave classification cost on repeat generations. Requires
  a stable hash function over the input device set + a `cable_run_cache` table.
</deferred_next>

<output>
Create `.planning/quick/260711-oh4-cable-schedule-intelligence-t2-b-ext-t3-/260711-oh4-SUMMARY.md`
when all three tasks are done. Summary must list the three commit SHAs in order + the
post-migration test count + one-line notes on any deviations from the plan (expected: none).
</output>
